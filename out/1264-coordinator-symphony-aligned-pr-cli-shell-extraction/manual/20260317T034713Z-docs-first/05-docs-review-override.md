# 1264 Docs Review Override

- No manifest-backed docs-review verdict is claimed for `1264`.
- This docs-first registration is recorded with an explicit override instead of forcing a noisy wrapper run on a long-lived stacked branch before the bounded shell extraction starts.
