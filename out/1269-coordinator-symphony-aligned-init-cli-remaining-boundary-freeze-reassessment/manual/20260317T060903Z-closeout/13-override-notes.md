# 1269 Override Notes

- `node scripts/spec-guard.mjs --dry-run` surfaced only the existing repo-global stale-spec warnings and was treated as informational dry-run output.
- The registration-time docs-first packet recorded an explicit docs-review override instead of claiming a manifest-backed docs-review approval that was not run.
