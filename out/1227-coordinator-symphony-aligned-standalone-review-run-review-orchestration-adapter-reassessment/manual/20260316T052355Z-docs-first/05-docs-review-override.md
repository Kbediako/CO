# 1227 Docs-Review Override

- docs-review override: this docs-only reassessment registration did not claim a manifest-backed docs-review verdict. Adjacent standalone-review docs-only registrations on this wrapper surface have repeatedly stopped at `Run delegation guard` before reaching a docs-local decision, so `1227` records an explicit override instead of pretending approval or failure. `spec-guard`, `docs:check`, and `docs:freshness` are green on the registered tree.
