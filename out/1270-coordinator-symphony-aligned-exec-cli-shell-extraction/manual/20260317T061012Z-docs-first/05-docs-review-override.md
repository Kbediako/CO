# Docs Review Override

`1270` is registered docs-first with deterministic docs gates recorded locally.

No separate docs-review run was opened at registration time. This packet records an explicit override so the lane stays truthful about current evidence rather than implying a manifest-backed docs-review approval that did not happen.
