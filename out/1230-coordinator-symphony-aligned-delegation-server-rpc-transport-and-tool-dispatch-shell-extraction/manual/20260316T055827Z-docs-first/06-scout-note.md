# 1230 Scout Note

- Post-`1228` bounded scout conclusion: the next truthful Symphony-aligned subsystem is `orchestrator/src/cli/delegationServer.ts`, not another standalone-review wrapper lane.
- The preferred first seam is the RPC transport/runtime plus tool-dispatch entry shell around `startDelegationServer(...)`, `runJsonRpcServer(...)`, and `handleToolCall(...)`.
- Current cautious repo-wide estimate is `12-22` more truthful slices, centered around delegation-server, RLM, control/live-dispatch, and doctor/diagnostics families.
- No Telegram or Linear setup/testing is on the critical path for this registration.
