# 1207 Override Notes

- Diff-budget override: the branch carries stacked prior Symphony-alignment lanes, so `node scripts/diff-budget.mjs` required `DIFF_BUDGET_OVERRIDE_REASON` even though the lane-local `1207` diff is bounded to the prompt/context helper extraction, focused tests, and docs/task mirror sync.
- Full-suite override: the direct local `npm run test` rerun advanced through the final visible `tests/cli-orchestrator.spec.ts` suite in [`06-test.log`](/Users/kbediako/Code/CO/out/1207-coordinator-symphony-aligned-standalone-review-prompt-context-builder-extraction/manual/20260315T023422Z-closeout/06-test.log) and then hit the recurring quiet-tail condition instead of emitting a terminal Vitest summary. The task-scoped guard diagnostics manifest records the forced wrapper termination as `SIGTERM` in [`04-test.json`](/Users/kbediako/Code/CO/.runs/1207-coordinator-symphony-aligned-standalone-review-prompt-context-builder-extraction-guard/cli/2026-03-15T02-32-41-682Z-41509be8/errors/04-test.json), so the lane keeps this as an explicit non-green override rather than a claimed clean full-suite pass.
- Bounded-review override: the rerun of `npm run review -- --manifest ...` no longer failed at startup, but the child review drifted for more than six minutes into speculative packaging, lint-surface, and helper-classification checks after already inspecting the direct diff. [`10-review.log`](/Users/kbediako/Code/CO/out/1207-coordinator-symphony-aligned-standalone-review-prompt-context-builder-extraction/manual/20260315T023422Z-closeout/10-review.log) and the child [`review/output.log`](/Users/kbediako/Code/CO/.runs/1207-coordinator-symphony-aligned-standalone-review-prompt-context-builder-extraction-guard/cli/2026-03-15T02-32-41-682Z-41509be8/review/output.log) show sustained low-signal drift with no terminal finding/no-finding verdict, so the pass is recorded as a review-reliability override rather than a clean approval.
- Historical docs-review override retained: the docs-first `docs-review` run for `1207` stopped at `Run delegation guard` before reaching a meaningful diff-local review, and that earlier state remains recorded in [`05-docs-review-override.md`](/Users/kbediako/Code/CO/out/1207-coordinator-symphony-aligned-standalone-review-prompt-context-builder-extraction/manual/20260315T021518Z-docs-first/05-docs-review-override.md).
