# 1200 Override Notes

- `08-diff-budget.log`: used `DIFF_BUDGET_OVERRIDE_REASON="1200 stacked-branch symphony alignment lane"` because the branch-level diff is far larger than the bounded `1200` seam.
- `out/1200-coordinator-symphony-aligned-orchestrator-resume-pre-start-failure-persistence-callback-extraction/manual/20260314T222320Z-docs-first/05-docs-review-override.md`: the docs-first `docs-review` wrapper stopped at `Run delegation guard`, so docs approval remained an explicit override rather than a claimed docs-review green.
- `01a-delegated-diagnostics.json`: the auxiliary delegated diagnostics run `2026-03-14T22-37-59-043Z-81c118c1` failed on its own generic `npm run test` stage. It was kept as manifest-backed delegation evidence only; the authoritative `1200` lane verdict comes from the main-tree validation logs in this closeout packet.
