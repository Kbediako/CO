# 1141 Next Slice Note

- `1141` closes the truthful contract-width problem for the extracted projection-notification seam.
- If Telegram hardening continues immediately, the next bounded slice should be a regression-proof lane rather than another production refactor:
  - add an explicit bridge interleaving backstop for `handleUpdates(...)` and `maybeSendProjectionDelta(...)` so the monotonic `updated_at` / preserved `next_update_id` merge story is directly pinned by tests.
- If a broader refactor is more valuable, the alternative is a post-1141 coordinator-surface reassessment from this now-thinned Telegram baseline instead of forcing another tiny bridge extraction.
