# 1182 Next Slice Note

- Next truthful seam: the remaining cloud-route fallback contract cluster inside `orchestratorCloudRouteShell.ts`.
- Recommended bounded scope:
  - cloud fallback allow/deny policy parsing
  - preflight-failure contract shaping
  - reroute payload assembly for the fallback-to-`mcp` path
- Keep out of scope:
  - `orchestratorExecutionRouter.ts` route-state resolution and branch selection
  - successful cloud preflight dispatch wiring
  - local-route shell now extracted in `1182`
  - broader cloud target executor lifecycle
