# 1263 Next Slice Note

The next truthful nearby shell candidate is `handlePr(...)` in `bin/codex-orchestrator.ts`, which still owns top-level `pr` help gating, subcommand validation, subcommand-to-mode selection, and exit-code mapping above the existing `scripts/lib/pr-watch-merge.js` runner.
