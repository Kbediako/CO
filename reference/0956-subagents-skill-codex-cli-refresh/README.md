# 0956 Codex CLI Refresh Evidence

- Task/spec anchor: `tasks/specs/0956-subagents-skill-codex-cli-refresh.md`.
- Retained evidence files:
  - `codex-cli-refresh-help.txt` records the helper CLI surface cited by the 0956 validation plan.
  - `codex-cli-refresh-dry-run.txt` records the no-op dry-run output cited by the same spec.
  - `README-snippet.txt` preserves the downstream README snippet referenced by the task.
  - `global-guidance-manifest.md` preserves the reviewable snapshot of the global guidance content produced by the task.
- This subtree is historical evidence, not the live source of truth. If the 0956 spec stops citing these files, delete the subtree instead of expanding it opportunistically.
