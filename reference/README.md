# Reference History

- `reference/mirror.config.wp.example.json` remains because `docs/guides/pixel-perfect-local-clones.md` still points downstream operators at a WordPress-friendly mirror template with default allowlist, blocklist, and tracker-strip settings.
- Task-specific historical evidence should carry a local README inside its own subtree. Current retained examples are `reference/plus-ex-15th/README.md` and `reference/0956-subagents-skill-codex-cli-refresh/README.md`.
- If a root-level historical reference loses its current doc or spec consumer, delete it instead of keeping it by default.
