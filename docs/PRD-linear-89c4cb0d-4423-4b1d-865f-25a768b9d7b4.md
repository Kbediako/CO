# PRD - CO-274 provider-worker stdin bootstrap failure classification

## Traceability
- Linear issue: `CO-274` / `89c4cb0d-4423-4b1d-865f-25a768b9d7b4`
- Task id: `linear-89c4cb0d-4423-4b1d-865f-25a768b9d7b4`
- Canonical TECH_SPEC: `tasks/specs/linear-89c4cb0d-4423-4b1d-865f-25a768b9d7b4.md`
- Source anchor: `ctx:sha256:395893ee7f0985529df981453f3c5cc80e32afb59fe1ed6d49d83252a23013ac#chunk:c000001`
- Source payload: `.runs/linear-89c4cb0d-4423-4b1d-865f-25a768b9d7b4/cli/2026-04-21T14-09-01-063Z-719778ff/memory/source-0/source.txt`

## Summary
- Problem Statement: `control-host` launched `provider-linear-worker` runs can exit before meaningful issue execution with `stderr | Reading additional input from stdin...`, while the surfaced diagnosis collapses to generic `provider_runtime`.
- Desired Outcome: classify this stdin/bootstrap seam separately, preserve exact proof and `manifest.json` evidence, and keep retry/resumable queue behavior truthful without implying Linear issue work happened.

## User Request Translation (Context Anchor)
- User intent / needs: complete CO-274 by investigating and fixing the provider-worker bootstrap failure where Codex exits after `Reading additional input from stdin...`.
- Success criteria / acceptance:
  - reproduce or fixture the exact control-host launched failure signature
  - classify the seam separately from generic `provider_runtime`
  - preserve exact stderr/proof/manifest truth, including `linear_audit.attempted_count`
  - keep retry/resumable queue behavior deterministic and truthful
  - distinguish this issue from `CO-224` child-lane appserver stalls and `CO-225` guardrail-summary truth
- Constraints / non-goals: no diagnostic muting, no blanket retry suppression, no adjacent issue scope merge, and no issue-specific CO-271 or CO-272 content fixes.

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `provider-linear-worker`
  - `control-host`
  - `stderr | Reading additional input from stdin...`
  - `provider_runtime`
  - `provider-linear-worker-proof.json`
  - `manifest.json`
  - `linear_audit.attempted_count`
  - retry/resumable queue behavior
  - `CO-224`
  - `CO-225`
- Protected terms / exact artifact and surface names:
  - `provider-linear-worker`
  - `control-host`
  - `stderr | Reading additional input from stdin...`
  - `provider_runtime`
  - `provider-linear-worker-proof.json`
  - `manifest.json`
  - `linear_audit.attempted_count`
- Nearby wrong interpretations to reject:
  - this is the same seam as `CO-224` child-lane appserver stalling
  - this is the same seam as `CO-225` false guardrail-summary truth
  - this is a CO-271 or CO-272 issue-specific content failure rather than a shared provider-worker bootstrap classification gap
  - this can be solved by hiding stderr or suppressing retries instead of preserving proof truth

## Parity / Alignment Matrix
| Surface | Current truth | Reference truth | Target truth / intended delta | Explicitly out-of-scope differences |
| --- | --- | --- | --- | --- |
| `provider-linear-worker-proof.json` | Prior proof shape records generic `provider_runtime`, the stdin signal, failed owner status, and `linear_audit.attempted_count` before issue work. | Proof is the machine-checkable provider-worker outcome surface. | Preserve exact stderr and audit count while adding bootstrap-specific classification/guidance. | Do not erase or rewrite existing historical proof artifacts. |
| `manifest.json` | Provider-worker exits with terminal failure before useful issue progress. | Manifest terminal status should distinguish launch/bootstrap failure from issue-specific failure. | Manifest/read-model summaries expose the stdin/bootstrap boundary rather than only generic `provider_runtime`. | Do not alter unrelated manifest schema or historical manifests. |
| retry/resumable queue behavior | A pre-work failure can look like an ordinary retryable provider runtime problem. | Queue behavior should remain resumable only when ownership/progress truth supports it. | Keep retry/resumable queue behavior deterministic and do not imply Linear work when `linear_audit.attempted_count` stayed at zero. | Do not redesign provider admission or retry policy broadly. |
| Adjacent issue boundaries | `CO-224` owns child-lane appserver startup stalls; `CO-225` owns guardrail-summary truth. | CO-274 is a parent provider-worker bootstrap/stdin handoff failure. | Tests/docs keep CO-274 separate from both adjacent issues. | Do not absorb CO-224 or CO-225 implementation scope. |

## Not Done If
- `control-host` launched `provider-linear-worker` runs can still exit before meaningful work with only generic `provider_runtime` classification when the signal is `Reading additional input from stdin...`.
- `provider-linear-worker-proof.json` or `manifest.json` still fail to distinguish this bootstrap seam from `CO-224` child-lane stalls or `CO-225` guardrail-summary truth.
- retry/resumable queue behavior implies issue-specific progress when `linear_audit.attempted_count` stayed at zero.
- The fix hides `stderr | Reading additional input from stdin...` or `linear_audit.attempted_count` instead of preserving them as evidence.

## Goals
- Rebuild the CO-274 docs-first packet on the Rework branch.
- Reproduce or fixture the stdin/bootstrap failure signature.
- Define and implement a parent-owned classification target that separates stdin/bootstrap failure from generic provider runtime failure.
- Preserve exact proof, manifest, audit, and queue behavior terms.

## Non-Goals
- Do not solve `CO-224` child-lane appserver stalls.
- Do not solve `CO-225` guardrail-summary truth.
- Do not patch CO-271 or CO-272 issue-specific content work.
- Do not weaken retry/resumable queue behavior or hide failed bootstrap evidence.
- Do not redesign provider admission, scheduler, or runtime-mode selection broadly.

## Acceptance Criteria
1. Reproduce or fixture the exact control-host launched `provider-linear-worker` bootstrap failure where Codex exits after `Reading additional input from stdin...`.
2. Classify this seam separately from generic `provider_runtime`, with targeted guidance for the runtime/bootstrap boundary.
3. Preserve truthful proof/manifests for the seam, including exact stderr and no meaningful issue execution.
4. Ensure retry/resumable queue handling for this failure mode is deterministic and does not masquerade as issue-specific progress.
5. Add regression coverage or deterministic fixtures that distinguish this seam from `CO-224` and `CO-225`.

## Stakeholders
- Product: CO operator workflow
- Engineering: Codex orchestrator provider-worker/control-host maintainers
- Design: Not applicable

## Metrics & Guardrails
- Primary Success Metrics: provider-worker bootstrap failures receive a specific, machine-checkable classification while preserving proof and manifest evidence.
- Guardrails / Error Budgets: no weakened retry/resumable semantics, no loss of stderr or audit evidence, no adjacent-issue scope drift.

## User Experience
- Personas: CO operator, provider-worker implementer, reviewer.
- User Journeys: operator sees bootstrap-specific guidance for pre-work provider-worker exits; reviewer can trace exact proof, manifest, source-anchor, and regression evidence.

## Technical Considerations
- Architectural Notes: likely seams include provider-worker failure classification, proof summary hydration, manifest/read-model presentation, and queue retry/resume reporting.
- Dependencies / Integrations: `provider-linear-worker-proof.json`, `manifest.json`, control-host read models, provider issue observability, retry/resumable queue behavior.

## Open Questions
- Should the implementation use a new diagnostic category or preserve `provider_runtime` with a specific subtype?
- Which exact operator remediation text should accompany the bootstrap/stdin classification?

## Approvals
- Product: pending
- Engineering: pending implementation validation
- Design: not applicable
