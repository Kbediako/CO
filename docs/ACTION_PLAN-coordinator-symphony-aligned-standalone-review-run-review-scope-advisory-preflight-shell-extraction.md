# ACTION_PLAN: Coordinator Symphony-Aligned Standalone Review Run-Review Scope Advisory Preflight Shell Extraction

1. Move the cohesive scope/advisory preflight cluster behind a dedicated helper/module without widening scope beyond the remaining pre-launch advisory shell.
2. Rewire `scripts/run-review.ts` so `main()` delegates scope-mode resolution, scope-path collection, large-scope assessment, and advisory prompt/log shaping while keeping broader wrapper orchestration local.
3. Add focused regressions, run the deterministic validation lane, and close with an explicit elegance pass plus truthful override notes if review drifts.
