---
id: 20260514-linear-991643b6-0043-49b8-9411-33b6de78e421
title: "CO-512 governed review contract no-fallback packet"
relates_to: docs/PRD-linear-991643b6-0043-49b8-9411-33b6de78e421.md
risk: high
owners:
  - Codex
last_review: 2026-05-18
---

# TECH_SPEC - CO-512 governed review contract no-fallback packet

This mirror points to the canonical task spec at `tasks/specs/linear-991643b6-0043-49b8-9411-33b6de78e421.md`.

## Scope
- Create the CO-512 PRD, TECH_SPEC mirror, ACTION_PLAN, canonical task spec, and task checklist.
- Register the CO-512 packet in `tasks/index.json`.
- Preserve protected no-fallback wording around the governed review contract and review handoff acceptance criteria.
- Implement schema validation, review input bundles, contract telemetry, provider-worker handoff gating, proposal routing, and accepted-only memory export.
- Keep validation suites, workpad, Linear state, PR lifecycle, and wider registry/doc mirrors parent-owned.

## Non-Goals
- No unrelated source code changes outside governed review contract surfaces.
- No `.agent/task` mirror because that path is outside this child lane's declared file scope.
- No `docs/TASKS.md`, docs freshness registry, docs catalog, workpad, Linear, GitHub, or PR lifecycle changes.
- No waiver, bounded-success, stale-output, missing-telemetry, or fallback-review path accepted as satisfying the governed review contract.

## Acceptance Criteria
- The five scoped packet files exist and reference `linear-991643b6-0043-49b8-9411-33b6de78e421`.
- `tasks/index.json` registers `20260514-linear-991643b6-0043-49b8-9411-33b6de78e421` and keeps `paths.docs` as `docs/TECH_SPEC-linear-991643b6-0043-49b8-9411-33b6de78e421.md`.
- PRD, TECH_SPEC mirror, ACTION_PLAN, canonical task spec, and task checklist include acceptance criteria, non-goals, Not Done If, validation requirements, and the CO-382 fallback decision table.
- Protected-term scan finds `governed review contract`, `no fallback`, `no-fallback`, `clean governed review evidence`, and `review handoff`.
- Packet wording rejects fallback review evidence, missing telemetry, stale review output, and semantically dirty verdicts as handoff proof.
- JSON Schema rejects missing axes, malformed proposals, empty-clean ambiguity, stale/missing evidence refs, and invalid verdicts.
- Enforce mode uses `codex exec --output-schema schemas/review-contract.v1.output.schema.json --output-last-message review/contract.json -` as the primary no-fallback contract transport, with the full prompt delivered on stdin.
- Telemetry derives enforce-mode semantic verdicts from the authoritative `review/contract.json` last-message file only and records proposal counts separately.
- Code-change proposals and agent-loop proposals route separately; memory entries are emitted only for accepted validated agent-loop proposals.

## Validation Contract
- Child-lane validation is limited to:
  - scoped file existence
  - `tasks/index.json` JSON parse
  - protected-term scan across scoped files
  - `git diff --check`
- Parent validation requirements:
  - reconcile the source payload and live issue acceptance criteria
  - implement the governed review contract in the parent-owned surface, including the canonical contract schema and the structured-output transport schema
  - run focused tests for the touched implementation surface, proposal routing, and accepted-only memory export
  - capture governed review telemetry proving no fallback and a clean verdict before review handoff
  - run the parent-required validation and review gates before PR/Linear lifecycle movement

## CO-382 Fallback Decision Table
- Applies to fallback, compatibility, legacy, stale, cached, break-glass, or minor-seam behavior? Yes.
- Decision: `remove fallback` for any handoff path that treats fallback, missing, stale, or semantically dirty review evidence as accepted governed review proof.

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Governed review handoff | Fallback review evidence, missing telemetry, stale output, or semantically dirty verdict accepted as handoff proof | `remove fallback` | CO-512 parent lane | Parent attempts Human Review / In Review handoff without current clean governed review evidence | Unknown from child source; parent must confirm | 2026-05-14 | Not retained; remove before handoff | Handoff requires clean governed review evidence with no fallback, or the lane remains blocked/rework instead of advancing | Parent-owned focused tests plus manifest/review telemetry proof showing no fallback and clean verdict before handoff |

- Large-refactor decision: no extra broad refactor is required; the smallest governed path is one schema/module plus wrapper telemetry and provider-worker gate integration.
- Minor-seam decision: no retained fallback is allowed for this governed review handoff contract.
