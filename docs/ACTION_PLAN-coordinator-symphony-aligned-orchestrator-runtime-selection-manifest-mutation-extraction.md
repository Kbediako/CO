# ACTION_PLAN: Coordinator Symphony-Aligned Orchestrator Runtime Selection Manifest Mutation Extraction

## Docs-first

- [x] Create PRD / TECH_SPEC / ACTION_PLAN / findings / spec / checklist / .agent mirror for `1198`.
- [x] Update `tasks/index.json`, `docs/TASKS.md`, and `docs/docs-freshness-registry.json`.
- [x] Capture local deliberation plus docs-review approval or explicit override for the runtime-manifest mutation seam.

## Implementation

- [ ] Extract the shared runtime-manifest mutation pair from `orchestrator.ts`.
- [ ] Keep requested-mode mutation, selected-mode mutation, provider assignment, and fallback assignment exact.
- [ ] Preserve focused regressions around the shared mutation helper without widening into runtime-resolution logic.

## Closeout

- [ ] Run the standard gate bundle and capture closeout artifacts under `out/1198-coordinator-symphony-aligned-orchestrator-runtime-selection-manifest-mutation-extraction/manual/<timestamp>-closeout/`.
- [ ] Run an explicit elegance review.
- [ ] Record the next truthful seam after `1198`.
