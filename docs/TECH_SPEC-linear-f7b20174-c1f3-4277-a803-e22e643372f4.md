---
id: 20260402-linear-f7b20174-c1f3-4277-a803-e22e643372f4
title: 'CO: Scope external gitdir eval NODE_PATH roots to checkout'
relates_to: docs/PRD-linear-f7b20174-c1f3-4277-a803-e22e643372f4.md
risk: high
owners:
  - Codex
last_review: 2026-04-02
---

## Canonical Reference
- Canonical TECH_SPEC: `tasks/specs/linear-f7b20174-c1f3-4277-a803-e22e643372f4.md`
- PRD: `docs/PRD-linear-f7b20174-c1f3-4277-a803-e22e643372f4.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-f7b20174-c1f3-4277-a803-e22e643372f4.md`
- Task checklist: `tasks/tasks-linear-f7b20174-c1f3-4277-a803-e22e643372f4.md`

## Traceability
- Linear issue: `CO-71` / `f7b20174-c1f3-4277-a803-e22e643372f4`
- Linear URL: https://linear.app/asabeko/issue/CO-71/co-scope-external-gitdir-eval-node-path-roots-to-checkout

## Added by Bootstrap (refresh as needed)

## Summary
- Objective: scope external gitdir checkout handling in `evaluation/harness/env.ts` back to the current checkout root so non-ancestor `.git` file layouts cannot leak unrelated ancestor `node_modules` paths into `NODE_PATH`.
- Scope:
  - docs-first registration for the current Linear worker lane
  - focused regression coverage for the `git clone --separate-git-dir` style boundary leak
  - minimal repo-root detection repair in `evaluation/harness/env.ts`
  - required validation, review, and workpad updates for handoff
- Constraints:
  - keep the lane bounded to external gitdir checkout handling and direct regression coverage
  - preserve the existing nested worktree, symlinked-path, and submodule boundary behavior
  - file a follow-up instead of widening into broader repository-discovery work

## Technical Requirements
- Functional requirements:
  - reproduce the live external gitdir `NODE_PATH` leak in a deterministic harness regression
  - keep non-ancestor external gitdir layouts scoped to the current checkout root
  - preserve the existing worktree, symlinked-path, and submodule boundary behavior
  - validate with `npm run eval:test` on the repaired branch
- Non-functional requirements:
  - keep the diff reviewable and auditable
  - preserve deterministic `NODE_PATH` ordering semantics
  - keep docs, workpad, and validation evidence synchronized
- Interfaces / contracts:
  - repo-root detection contract: `evaluation/harness/env.ts`
  - harness regression contract: `evaluation/tests/harness.test.ts`
  - validation contract: `npm run eval:test`

## Architecture & Data
- Architecture / design adjustments:
  - keep the repair inside `.git` file repo-root classification rather than broadening `collectNodeModulePaths()` traversal logic unnecessarily
  - reuse the existing `isWithinAncestor()` and canonical path handling where it keeps the fix small and readable
  - prefer explicit ancestry checks before treating external `.git` directories as repo-root authorities
- Data model changes / migrations:
  - none expected
- External dependencies / integrations:
  - local Node/npm runtime used by the evaluation harness tests
  - provider-worker workspaces that may use external gitdir checkout layouts

## Validation Plan
- Tests / checks:
  - audited child `docs-review` stream after the docs packet exists
  - focused harness regression reruns for the new external gitdir case
  - `MCP_RUNNER_TASK_ID=linear-f7b20174-c1f3-4277-a803-e22e643372f4 npm run eval:test`
  - repo validation floor appropriate to the final diff, including `node scripts/delegation-guard.mjs`, `node scripts/spec-guard.mjs --dry-run`, `npm run build`, `npm run lint`, `npm run test`, `npm run docs:check`, `npm run docs:freshness`, `node scripts/diff-budget.mjs`, and wrapper-led `FORCE_CODEX_REVIEW=1 npm run review` for non-trivial diffs
- Rollout verification:
  - confirm the new external gitdir regression passes with no ancestor leak above the checkout root
  - confirm the existing worktree, symlinked-path, and submodule regressions remain green
  - confirm `npm run eval:test` exits cleanly in this workspace
- Monitoring / alerts:
  - keep the single Linear workpad current after docs, regression, implementation, and pre-handoff review

## Open Questions
- No unresolved design questions are expected if the fix stays inside ancestry checks for external gitdir `.git` file layouts.
- Remaining operational question: PR attach, quiet-window drain, and review-state handoff will be handled after validation succeeds.

## Approvals
- Reviewer: Pending docs-review, implementation validation, and pre-handoff review/elegance gates
- Date: 2026-04-02
