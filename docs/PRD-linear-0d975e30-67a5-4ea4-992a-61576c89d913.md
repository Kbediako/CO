# PRD - CO-524 Review Intent Matrix

## Immediate Traceability
- Linear issue: `CO-524` / `0d975e30-67a5-4ea4-992a-61576c89d913`
- Task id: `linear-0d975e30-67a5-4ea4-992a-61576c89d913`
- Canonical spec: `tasks/specs/linear-0d975e30-67a5-4ea4-992a-61576c89d913.md`
- TECH_SPEC mirror: `docs/TECH_SPEC-linear-0d975e30-67a5-4ea4-992a-61576c89d913.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-0d975e30-67a5-4ea4-992a-61576c89d913.md`
- Task checklist: `tasks/tasks-linear-0d975e30-67a5-4ea4-992a-61576c89d913.md`
- `.agent` mirror: `.agent/task/linear-0d975e30-67a5-4ea4-992a-61576c89d913.md`
- Source anchor: `ctx:sha256:6e71b8b5b90b781af5d2becb11953604969b8abe358cda6b629323af0a896360#chunk:c000001`
- Source object id: `sha256:6e71b8b5b90b781af5d2becb11953604969b8abe358cda6b629323af0a896360`
- Parent source payload pointer: `.runs/linear-0d975e30-67a5-4ea4-992a-61576c89d913-docs-packet-fileonly/cli/2026-05-31T17-06-09-706Z-a13c99ba/memory/source-0/source.txt`
- Child packet evidence key: `linear-0d975e30-67a5-4ea4-992a-61576c89d913-docs-packet`
- Docs-review evidence key: `linear-0d975e30-67a5-4ea4-992a-61576c89d913-docs-review`

## Summary
- Problem Statement: provider-worker review prompts and review telemetry currently depend on scattered prose, packet documents, coding standards, proposal notes, and wrapper fields. Without an explicit `review_intent_matrix`, the review wrapper can confuse the `original spec`, `PRD`, `TECH_SPEC`, `ACTION_PLAN`, `coding standards`, `code change proposals`, `agent loop change proposals`, `review_verdict`, and `review_outcome` when deciding whether a `provider-worker handoff` is clean.
- Desired Outcome: define a narrow `review_intent_matrix` contract that binds each review source and proposal class to the review question, evidence path, expected output channel, and handoff effect, while preserving `review_verdict` as the semantic gate, `review_outcome` as wrapper execution disposition, Codex CLI 0.130.0 compatibility, and `goals advisory-only`.

## User Request Translation
- User intent / needs: create the CO-524 docs-first packet for the `review_intent_matrix` feature only. The parent lane will implement and validate the matrix; this child lane must preserve exact review/handoff terminology and register the canonical provider issue task key without suffixes.
- Success criteria / acceptance:
  - packet explains how `review_intent_matrix.original_spec` uses canonical `PRD`, `TECH_SPEC`, and `ACTION_PLAN` evidence, and how `review_intent_matrix.coding_standards` records CO workflow standards review.
  - packet requires separate handling for `code change proposals` and `agent loop change proposals`.
  - packet preserves the difference between `review_verdict` and `review_outcome`.
  - packet makes clean `provider-worker handoff` depend on semantic clean review intent, not wrapper success alone.
  - packet preserves Codex CLI 0.130.0 compatibility and `goals advisory-only` authority boundaries.
  - packet rejects Linear, GitHub, PR, workpad, and implementation changes in this child lane.
- Constraints / non-goals:
  - no Linear/GitHub/PR/workpad actions in this child lane.
  - no implementation, test, script, or runtime file edits in this child lane.
  - no weakening of `review_verdict`, `review_outcome`, docs-review, implementation-gate, ready-review, or provider-worker handoff gates.
  - no treating goals, wrapper success, or stale review evidence as lifecycle authority.

## Issue Intent Checksum
- Exact user wording / phrases to preserve: `review_intent_matrix`, `original spec`, `PRD`, `TECH_SPEC`, `ACTION_PLAN`, `coding standards`, `code change proposals`, `agent loop change proposals`, `review_verdict`, `review_outcome`, `provider-worker handoff`, `Codex CLI 0.130.0`, `goals advisory-only`.
- Protected terms / exact artifact and surface names: `review_intent_matrix`, `original spec`, `PRD`, `TECH_SPEC`, `ACTION_PLAN`, `coding standards`, `code change proposals`, `agent loop change proposals`, `review_verdict`, `review_outcome`, `provider-worker handoff`, `Codex CLI 0.130.0`, `goals advisory-only`, `tasks/specs/linear-0d975e30-67a5-4ea4-992a-61576c89d913.md`, `tasks/tasks-linear-0d975e30-67a5-4ea4-992a-61576c89d913.md`, `.agent/task/linear-0d975e30-67a5-4ea4-992a-61576c89d913.md`, `tasks/index.json`, `docs/TASKS.md`, `docs/docs-freshness-registry.json`.
- Nearby wrong interpretations to reject: only adding a prose checklist; replacing the `original spec` with the PRD; treating `review_outcome: clean-success` or `review_outcome: bounded-success` as clean when `review_verdict` is findings, unknown, missing, or stale; treating `goals advisory-only` as lifecycle authority; rewriting all review-wrapper telemetry; changing Linear or PR lifecycle behavior from this child lane.

## Parity / Alignment Matrix

| Surface | Current truth | Reference truth | Target truth / intended delta | Explicitly out-of-scope differences |
| --- | --- | --- | --- | --- |
| Review source intent | Review prompts can reference the `original spec`, docs packet, coding standards, and local notes without one machine-readable intent map. | CO review gates require intent-scoped review, not generic approval. | Add `review_intent_matrix` rows that bind each source to the required review question and evidence path. | No broad prompt rewrite in this child lane. |
| Packet alignment | `PRD`, `TECH_SPEC`, and `ACTION_PLAN` describe intent, design, and sequencing separately. | Docs-first requires these documents to preserve protected terms, non-goals, and `Not Done If`. | Matrix consumes the docs packet as separate intent sources instead of collapsing them into one summary. | No docs-first process replacement. |
| Proposal handling | Reviewers can suggest code changes and creator-loop changes, but proposal classes can be conflated in prose. | Review workflow requires both `code change proposals` and `agent loop change proposals`. | Matrix records both proposal classes and keeps their handoff effect explicit. | No automatic implementation of proposals. |
| Telemetry semantics | `review_outcome` reports wrapper execution disposition, while `review_verdict` reports semantic findings state. | Provider-worker handoff should require semantic clean review evidence. | Matrix and validation keep `review_verdict` authoritative for review cleanliness; `review_outcome` cannot override findings or unknown verdicts. | No weakening of review telemetry parsing. |
| Runtime compatibility | Codex CLI 0.130.0-era evidence may have different wrapper/prompt behavior. | Compatibility states must be explicit and fail closed. | Matrix includes compatibility fixtures and visible missing/unsupported states. | No Codex version promotion. |
| Goals context | Goals can provide context but are advisory-only. | `goals advisory-only` cannot authorize lifecycle transitions. | Matrix may include goals context only as non-authoritative review context. | No lifecycle authority from goals. |

## Not Done If
- `review_intent_matrix` does not include the required axes `original_spec`, `coding_standards`, `code_change_proposals`, and `agent_loop_change_proposals`.
- `original_spec` is marked clean without canonical `PRD`, `TECH_SPEC`, and `ACTION_PLAN` evidence.
- `code change proposals` and `agent loop change proposals` are merged, dropped, or treated as the same handoff action.
- `review_outcome` can be used as a substitute for `review_verdict`.
- `provider-worker handoff` can proceed with `review_verdict` findings, unknown, missing, stale, or mismatched evidence.
- Codex CLI 0.130.0 compatibility is omitted or silently treated as current clean evidence.
- `goals advisory-only` becomes lifecycle authority.
- This child lane edits implementation, tests, Linear state, GitHub state, PR state, or the workpad.

## Goals
- Define the `review_intent_matrix` product contract and docs-first packet for CO-524.
- Preserve exact review source, proposal, telemetry, compatibility, and handoff terms.
- Make review intent traceable from source documents to semantic review output and provider-worker handoff.
- Keep parent-owned implementation and lifecycle gates explicit.

## Non-Goals
- No implementation, test, script, runtime, Linear, GitHub, PR, or workpad edits in this child lane.
- No generic review-wrapper redesign outside the `review_intent_matrix` contract.
- No replacement of docs-first, docs-review, implementation-gate, ready-review, or provider-worker handoff gates.
- No Codex version promotion.
- No goal-driven lifecycle authority.

## Metrics & Guardrails
- Primary success: all required axes are present, semantic `review_verdict` remains the clean-review gate, `review_outcome` remains wrapper disposition, and compatibility/advisory-goal states are explicit.
- Guardrail: fail closed on missing matrix axes, stale review evidence, unknown verdicts, findings, unsupported compatibility evidence, or any attempt to use goals as lifecycle authority.

## User Experience
- Provider worker and reviewer see whether `original_spec`, `coding_standards`, proposal axes, `review_verdict`, `review_outcome`, compatibility, and advisory goals are classified correctly before handoff.

## Technical Considerations
- Architectural Notes: implementation should add a structured matrix close to the review prompt/telemetry contract.
- Dependencies / Integrations: docs-first packet, review prompt assembly, review telemetry, provider-worker handoff checks, Codex CLI 0.130.0 compatibility fixtures, and advisory goal evidence.

## Fallback / Refactor Decision
- Applies to fallback, compatibility, legacy, stale, cached, break-glass, or minor-seam behavior? Yes.
- Decision: remove the implicit prose-only review intent seam and retain explicit fail-closed compatibility for Codex CLI 0.130.0 evidence and `goals advisory-only` context.

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Review intent classification | Review intent inferred from ad hoc prompt prose instead of a structured `review_intent_matrix`. | remove fallback | CO-524 | Provider-worker docs-review, implementation-gate, standalone review, or pre-handoff review prompt assembly. | 2026-05-31 | 2026-05-31 | This issue | `review_intent_matrix` axes are generated and validated for `original_spec`, `coding_standards`, `code_change_proposals`, and `agent_loop_change_proposals`; `original_spec` carries canonical `PRD`, `TECH_SPEC`, and `ACTION_PLAN` evidence. | Focused matrix generation, missing-axis, prompt transport, and provider-worker handoff tests. |
| Review telemetry compatibility | Codex CLI 0.130.0 or legacy review evidence can lack current intent matrix fields. | expire fallback | CO-524 | Review evidence comes from Codex CLI 0.130.0-era telemetry or lacks a current matrix binding. | 2026-05-31 | 2026-05-31 | 30 days, expires 2026-06-30 | Current review wrapper evidence always includes valid matrix metadata, or old evidence is explicitly rejected as stale/unsupported. | Codex CLI 0.130.0 fixture tests and fail-closed stale/unsupported evidence tests. |
| Goals context in review prompts | Goal evidence appears near review context and could be mistaken for lifecycle authority. | justify retaining fallback | CO-524 provider-worker handoff | Goals evidence is present in prompt, manifest, workpad, or status context. | 2026-05-31 | 2026-05-31 | non-expiring durable authority contract | A separately approved authority redesign replaces Linear/workpad/review authority and proves goal evidence can become authoritative. | Tests prove `goals advisory-only` cannot satisfy `review_verdict`, `review_outcome`, or provider-worker handoff gates. |

- Contract name: Review intent matrix with Linear-first and review-verdict-first provider-worker handoff.
- Owning surface: review prompt assembly, review telemetry interpretation, and provider-worker handoff checks.
- Steady-state proof: current handoff continues to require semantic review cleanliness; wrapper execution disposition and advisory goals remain context only.
- Tests/docs: this CO-524 packet, focused review intent matrix tests, review telemetry fixture tests, and provider-worker handoff tests.
- Non-expiring rationale: `goals advisory-only` is a durable authority boundary, not temporary fallback debt.
- Large refactor check: no broad review-wrapper replacement is selected for the docs packet. The implementation should make the smallest structured matrix addition that removes prose-only intent inference while preserving existing gates.
- Minor seam decision: no retained minor seam; missing or stale matrix evidence fails closed instead of adding another compatibility branch.
