# ACTION_PLAN: Coordinator Symphony-Aligned Standalone Review Command-Intent Classification Extraction

## Objective

Open the next truthful standalone-review follow-on after `1214` by extracting the deterministic command-intent classifier cluster still reused inside `review-execution-state`.

## Steps

1. Register `1215` docs-first with explicit evidence that the command-intent classifier cluster remains inline inside `scripts/lib/review-execution-state.ts`.
2. Extract the bounded command-intent helper while preserving stateful policy ownership in `scripts/lib/review-execution-state.ts`.
3. Add only the focused regressions needed to pin orchestration-command detection, direct validation runner detection, package-script resolution, and violation-label formatting.
4. Run the lane validations and record any truthful docs-review or bounded-review overrides.
5. Sync closeout mirrors and reassess whether the next truthful follow-on remains in review execution-state helper support or shifts elsewhere.
