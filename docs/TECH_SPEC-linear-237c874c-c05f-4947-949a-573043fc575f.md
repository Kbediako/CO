# TECH_SPEC - CO: restore Apr 15 docs:freshness historical packet baseline

## Added by Bootstrap 2026-04-15

## Summary
- Objective: restore `npm run docs:freshness` to green after the 2026-04-15 boundary by explicitly reviewing and refreshing the newly stale historical packet set that is outside the CO-175 rolling cohort.
- Scope:
  - docs-first packet and task mirrors for CO-184
  - classification note for all `173` blocking stale rows
  - registry `last_review` refresh for the classified historical rows
  - docs freshness cohort guide update documenting the April 15 handling decision
- Constraints:
  - do not change `scripts/docs-freshness.mjs`
  - do not increase `max_entries=300`
  - do not broaden eligible rolling classes or baseline cohorts
  - do not touch CO-183 Codex CLI posture or spark policy

## Current Failure
The preserved pre-fix `npm run docs:freshness` report at `out/linear-237c874c-c05f-4947-949a-573043fc575f/pre-fix-docs-freshness.json` fails with:

- `173` blocking stale docs
- `221` rolling freshness cohort entries
- `0` missing registry rows
- `0` missing-on-disk rows
- `0` invalid registry entries
- `0` uncatalogued docs

Blocking class breakdown:

| Class | Count |
| --- | ---: |
| Task Packet | 124 |
| Task Mirror | 25 |
| Report Only | 24 |

Blocking path-family breakdown:

| Path family | Count |
| --- | ---: |
| `.agent/task` | 25 |
| `docs/ACTION_PLAN-*` | 25 |
| `docs/findings` | 24 |
| `docs/PRD-*` | 25 |
| `docs/TECH_SPEC-*` | 25 |
| `tasks/specs` | 24 |
| `tasks/tasks-*` | 25 |

## Design Decision
Refresh the `173` blocking historical rows after this classification review.

Rationale:

- Adding them to rolling deferral alongside CO-175 would produce `394` rolling rows, exceeding `max_entries=300`.
- Archiving the packet set is broader than needed for restoring the baseline and risks losing near-term traceability for the March 15 extraction/review packet chain.
- Reclassification is not appropriate because the catalog classes already match the row types: Task Packet, Task Mirror, and Report Only.
- Refreshing the rows is the smallest explicit outcome that restores the gate without weakening freshness policy.

## Implementation Surface
- `docs/docs-freshness-registry.json`
  - update `last_review` to `2026-04-15` for the classified `173` blocking stale rows
  - add registry rows for the CO-184 docs-first packet and classification note
- `docs/guides/docs-freshness-cohorts.md`
  - add an April 15 handling note documenting why the new set was refreshed instead of rolled
- `docs/findings/linear-237c874c-c05f-4947-949a-573043fc575f-docs-freshness-classification.md`
  - preserve class, path-family, owner, sample, and outcome evidence
- `tasks/index.json`, `docs/TASKS.md`, `tasks/tasks-linear-237c874c-c05f-4947-949a-573043fc575f.md`, `.agent/task/linear-237c874c-c05f-4947-949a-573043fc575f.md`
  - register the task and mirror state for reviewers

## Protected Expectations
- CO-175 remains visible as the owner of `co-175-apr-14-march-14-tasks-1164-1195`.
- `rolling_freshness_cohorts.max_entries` remains `300`.
- Public and active documentation classes remain ineligible for rolling deferral.
- `docs:freshness` must still fail closed for missing, invalid, uncatalogued, public, active, or expired rows.

## Reject These Wrong Interpretations
- The April 15 cohort can be fixed by adding another baseline cohort without counting capacity.
- A registry refresh can happen without a review note.
- `docs:freshness` should be changed to warn for task packets.
- CO-175 rolling debt should be reassigned to CO-184.

## Validation Plan
- `npm run docs:freshness` reproduction before edits.
- `linear child-lane` same-turn classification lane evidence, rejected if the artifact is not useful.
- `linear child-stream --pipeline docs-review` for docs-first packet review when available.
- `npm run docs:check`.
- `npm run docs:freshness`.
- `node scripts/diff-budget.mjs`.
- Manifest-backed standalone review plus manual elegance pass before PR handoff if the final diff is non-trivial.

## Approvals
- Reviewer: docs-review child stream attempted and failed before review on generated task-id/provenance bootstrap; fallback review completed with bounded-success and no actionable findings after the evidence-path repair.
- Date: 2026-04-15
