# TECH SPEC Mirror - CO-426 Provider Validation Audit Isolation
Canonical spec: `tasks/specs/linear-f88846f3-24be-48b6-a983-087613fd8f82.md`.
## Summary / Behavior
Keep provider-worker validation from writing synthetic Linear CLI fixture audit rows into the live `CODEX_PROVIDER_LINEAR_AUDIT_PATH`. The implementation should isolate or mock the audit path for mutating `LinearCliShell.test.ts` cases, especially `create-follow-up` / `linear_follow_up_parity_matrix_missing` with fixture id `lin-issue-1`, and preserve real provider helper audit appends for actual issue ids. If `co-status` still sees mismatched fixture rows, classify or ignore them without hiding real provider failures.
## Protected Terms
`CODEX_PROVIDER_LINEAR_AUDIT_PATH`, `provider-linear-worker-linear-audit.jsonl`, `lin-issue-1`, `LinearCliShell.test.ts`, `create-follow-up`, `linear_follow_up_parity_matrix_missing`, `co-status`, live provider-worker audit.
## Validation
Required gates: delegation guard, spec guard, build, lint, focused `LinearCliShell` regressions, full test, docs:check, docs:freshness, repo:stewardship, diff budget, standalone/elegance review, ready-review, and pack:smoke.
