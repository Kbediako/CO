# ACTION_PLAN - CO: Umbrella Epic for Issue-Parent Orchestration Above Multiple Workers Per Issue

## Summary
- Goal: finish `CO-14` as a docs-first umbrella epic that defines a future issue-parent architecture above multiple same-issue workers without pretending the implementation is ready now.
- Scope: authoritative baseline audit, docs-first packet, staged architecture and activation gate, checklist and registry updates, delegated scout evidence, docs-review, and review-ready documentation handoff.
- Assumptions: `CO-13` remains the current bounded path, the `0.117/main` canary stays the latest CO recommendation, and current provider workers continue to own external issue lifecycle state through the existing control-host plus `provider-linear-worker` seam.
- Closeout rule: if the branch remains docs-only and GitHub `Core Lane` is green with zero actionable review threads, treat that as authoritative validation even if local `npm run test` reproduces only the known post-suite quiet tail without a failing test.

## Milestones & Sequencing
1. Draft and register the `CO-14` docs-first packet, baseline audit artifact, and checklist mirrors.
2. Compare current Symphony, `CO-13`, and canary evidence, then define the issue-parent authority, lineage, observability, workspace, and conflict rules plus explicit go or no-go triggers.
3. Run delegated scout evidence, docs-review, and the repo validation floor for the docs diff; then refresh the workpad and prepare PR handoff if clean.
4. Reconcile any stale blocked-state interpretation against live PR evidence, update the packet if the remaining seam is validation interpretation rather than a real regression, and close the PR only after the review and check quiet window stays clean.

## Dependencies
- `/Users/kbediako/Code/symphony/SPEC.md`
- `/Users/kbediako/Code/symphony/elixir/README.md`
- `/Users/kbediako/Code/symphony/elixir/WORKFLOW.md`
- `/Users/kbediako/.codex/reports/2026-03-27-codex-0.117.0-co-alignment.md`
- `AGENTS.md`
- `orchestrator/src/cli/controlHostCliShell.ts`
- `orchestrator/src/cli/control/providerIssueHandoff.ts`
- `orchestrator/src/cli/providerLinearWorkerRunner.ts`
- `orchestrator/src/cli/control/controlRuntime.ts`
- `orchestrator/src/cli/control/observabilityReadModel.ts`
- `docs/PRD-linear-488135bf-954e-4bd9-be7a-ad09d75f5f29.md`
- `docs/TECH_SPEC-linear-488135bf-954e-4bd9-be7a-ad09d75f5f29.md`
- `docs/TECH_SPEC-linear-b8a32c11-cfc6-4556-b783-f59b290fe67d.md`

## Validation
- Checks / tests:
  - delegated scout run with `MCP_RUNNER_TASK_ID=linear-62527426-2f74-4134-98e0-bb50c3da7913-<stream>`
  - docs-review before review handoff
  - `node scripts/delegation-guard.mjs --task linear-62527426-2f74-4134-98e0-bb50c3da7913`
  - `node scripts/spec-guard.mjs --dry-run`
  - `npm run build`
  - `npm run lint`
  - `npm run test`
  - `npm run docs:check`
  - `npm run docs:freshness`
  - `node scripts/diff-budget.mjs`
  - `npm run review -- --manifest <manifest>`
- Rollback plan:
  - revert the new docs-first packet and registry updates together if review or validation shows the framing overclaims current behavior or conflicts with the required baseline

## Risks & Mitigations
- Risk: the packet accidentally frames `CO-14` as Symphony parity work. Mitigation: keep the one-owner Symphony baseline explicit and state that issue-parent orchestration is a CO-specific extension.
- Risk: the packet treats stronger `0.117` substrate as permission to widen authority immediately. Mitigation: keep `exec` or `resume` supervision and remote app-server control surfaces explicitly deferred.
- Risk: the design overcommits to parallel coding lanes before conflict rules are proven. Mitigation: stage the epic so read-side lineage and parent-owned coordination come before any parallel write activation.

## Approvals
- Reviewer: Self-reviewed against the issue body and required baseline before docs-review
- Date: 2026-03-28
