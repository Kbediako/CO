# PRD - CO workflow: restore repo-wide spec/docs freshness baselines after 30-day rollover regression

## Added by Bootstrap 2026-04-12

## Traceability
- Linear issue: `CO-155` / `39a7ba03-ca72-4656-8941-f51250b09413`
- Linear URL: https://linear.app/asabeko/issue/CO-155/co-workflow-restore-repo-wide-specdocs-freshness-baselines-after-30
- Source issue: `CO-94` / `ce6f26d0-029a-40d9-8ffe-289cd40cde8d`
- Current local repro artifacts:
  - `out/linear-39a7ba03-ca72-4656-8941-f51250b09413/manual/20260412T003531Z-baseline-docs-freshness.log`
  - `out/linear-39a7ba03-ca72-4656-8941-f51250b09413/manual/20260412T003531Z-baseline-docs-freshness-report.json`
  - `out/linear-39a7ba03-ca72-4656-8941-f51250b09413/manual/workpad.md`
- Prior related repo-baseline lanes:
  - `CO-143` / `d3f1af87-57da-46ab-901d-75a6cc60420e`
  - `CO-142` / `710e37a2-a7bb-4976-859f-0378d6a7104f`

## Summary
- Problem Statement: `CO-155` was opened to restore repo-wide spec/docs freshness baselines after `CO-94` resumed validation. On the current workspace, the live blocker is narrower than the issue snapshot: `node scripts/spec-guard.mjs --dry-run` is already green, while `npm run docs:freshness` fails with `119` stale docs, `0` missing registry entries, `0` missing files, and `0` invalid entries. The stale set is one coherent March 12 cohort spanning packet ids `1121` through `1137`, with each stale row carrying `last_review=2026-03-12`, `cadence_days=30`, and `age_days=31`.
- Desired Outcome: restore a truthful green repo-wide docs/spec freshness baseline on the active tree so unrelated lanes, including `CO-94`, can hand off without repo-baseline fallback notes or policy waivers.

## User Request Translation (Context Anchor)
- User intent / needs (in your own words): keep this scoped to the repo-wide freshness-baseline debt surfaced while `CO-94` resumed validation, repair the live stale cohort without weakening `spec-guard` or `docs:freshness`, and leave evidence that future workers can cite directly.
- Success criteria / acceptance:
  - confirm the current live baseline truth on this workspace
  - repair the stale cohort without relaxing either guardrail
  - keep `CO-94` implementation/test health explicitly out of scope
  - record enough artifact-backed evidence that other lanes can cite the restored baseline
- Constraints / non-goals:
  - do not change `run memory controller` or consumer wiring from `CO-94`
  - do not reopen unrelated repo-test or review-wrapper work without new evidence
  - do not replace the fix with waivers, ignored failures, or reduced coverage

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `repo-wide spec/docs freshness baselines`
  - `CO-94`
  - `spec-guard`
  - `docs:freshness`
  - `manual fallback notes`
  - `without weakening policy`
- Protected terms / exact artifact and surface names:
  - `node scripts/spec-guard.mjs --dry-run`
  - `npm run docs:freshness`
  - `docs/docs-freshness-registry.json`
  - `tasks/index.json`
  - `docs/TASKS.md`
  - `out/linear-39a7ba03-ca72-4656-8941-f51250b09413/manual/20260412T003531Z-baseline-docs-freshness-report.json`
- Nearby wrong interpretations to reject:
  - `CO-94` still needs controller or consumer implementation work
  - the old `1093` through `1119` stale-spec cohort is still the live blocker on this workspace
  - the right fix is to relax or bypass freshness policy instead of repairing the active stale cohort
  - any remaining blocker here is the old repo-test seam from `CO-132`

## Parity / Alignment Matrix
- Current truth:
  - `spec-guard` is green on this workspace today
  - `docs:freshness` is red on one March 12 cohort spanning packet ids `1121` through `1137`
  - the stale split is `Task Packet=85`, `Task Mirror=17`, `Report Only=17`
- Reference truth:
  - issue-local implementation lanes should be able to rely on repo-baseline freshness checks being truthful and green when their own diffs are healthy
  - `CO-94` has already recorded green implementation/test health on the current worktree
- Target truth / intended delta:
  - the March 12 cohort is re-reviewed and refreshed consistently enough that `docs:freshness` returns green without weakening policy
  - `spec-guard` remains green after the repair
  - the closeout makes clear that this was repo-baseline drift, not renewed `CO-94` implementation risk
- Explicitly out-of-scope differences:
  - reopening `CO-94` code
  - reopening unrelated repo-test stabilization
  - redesigning the freshness system or archive policy

## Not Done If
- `node scripts/spec-guard.mjs --dry-run` regresses from green on the repaired tree.
- `npm run docs:freshness` still reports repo-baseline stale docs on the repaired tree.
- unrelated implementation lanes still need manual repo-baseline freshness fallback notes instead of issue-local notes.
- the lane cannot explain the current drift source concretely from saved artifacts.

## Goals
- Reproduce the current repo-baseline freshness truth on the active workspace.
- Keep the issue packet aligned to the live blocker, not the older issue snapshot.
- Refresh the March 12 stale cohort (`1121` through `1137`) truthfully and minimally.
- Leave auditable evidence that the repaired tree advances past the prior repo-baseline blocker.

## Non-Goals
- Reopening `CO-94` runtime/controller work.
- Relaxing or bypassing `spec-guard` or `docs:freshness`.
- Broad repo-wide docs churn outside the reproduced stale cohort.
- Reopening older spec-quarantine work unless new live evidence requires it.

## Stakeholders
- Product: CO operators and maintainers depending on truthful review handoffs
- Engineering: workers running `docs-review`, `implementation-gate`, and repo freshness validation
- Design: N/A

## Metrics & Guardrails
- Primary Success Metrics:
  - `node scripts/spec-guard.mjs --dry-run` remains green
  - `npm run docs:freshness` exits `0`
  - the stale cohort is explained concretely as `1121` through `1137` with March 12 review metadata
- Guardrails / Error Budgets:
  - keep the implementation bounded to the reproduced stale cohort plus the new `CO-155` packet
  - prefer explicit follow-up creation over scope expansion if a different blocker appears
  - preserve the truth that `CO-94` implementation/test health is already green here

## User Experience
- Personas:
  - provider worker preparing a truthful review handoff
  - reviewer auditing repo-baseline blockers versus issue-local defects
  - maintainer tracing why a previously green baseline rolled over again
- User Journeys:
  - a worker reruns `npm run docs:freshness` and sees a truthful green baseline
  - a reviewer can identify the exact packet cohort that aged out
  - a maintainer can distinguish repo freshness drift from new implementation regressions

## Technical Considerations
- Architectural Notes:
  - the current blocker is metadata freshness drift, not missing registry coverage or invalid entries
  - the repair should update task registry and packet review metadata consistently across the active cohort
  - `spec-guard` and `docs:freshness` must remain fail-closed
- Dependencies / Integrations:
  - `scripts/docs-freshness.mjs`
  - `docs/docs-freshness-registry.json`
  - `tasks/index.json`
  - the March 12 packet cohort `1121` through `1137`

## Open Questions
- Whether the post-fix proof should stop at the required validation floor or include an additional implementation-gate child stream if the diff surface stays purely doc/metadata scoped.

## Approvals
- Product: pending
- Engineering: pending audited `docs-review` child stream for the `CO-155` packet
- Design: N/A
