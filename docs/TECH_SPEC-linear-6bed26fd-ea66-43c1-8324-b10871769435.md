---
id: 20260403-linear-6bed26fd-ea66-43c1-8324-b10871769435
title: CO: Exclude historical non-provider in_progress manifests from live CO STATUS current activity/runtime
relates_to: docs/PRD-linear-6bed26fd-ea66-43c1-8324-b10871769435.md
risk: high
owners:
  - Codex
last_review: 2026-04-03
---

## Canonical Reference
- Canonical TECH_SPEC: `tasks/specs/linear-6bed26fd-ea66-43c1-8324-b10871769435.md`
- PRD: `docs/PRD-linear-6bed26fd-ea66-43c1-8324-b10871769435.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-6bed26fd-ea66-43c1-8324-b10871769435.md`
- Task checklist: `tasks/tasks-linear-6bed26fd-ea66-43c1-8324-b10871769435.md`

## Traceability
- Linear issue: `CO-73` / `6bed26fd-ea66-43c1-8324-b10871769435`
- Linear URL: https://linear.app/asabeko/issue/CO-73/co-exclude-historical-non-provider-in-progress-manifests-from-live-co
- Follow-up context: `CO-67` / `f3193657-a549-43a2-8cff-50c5284df986`

## Added by Bootstrap (refresh as needed)

## Summary
- Objective: stop historical non-provider raw `in_progress` manifests from appearing as live current activity in `CO STATUS` and `/api/v1/state`.
- Scope:
  - docs-first registration for `CO-73`
  - bounded runtime current-source selection changes in `controlRuntime.ts` while keeping `selectedRunProjection.ts` unchanged
  - focused runtime/API regressions plus terminal and attach-viewer screenshot proof
- Constraints:
  - preserve current operator surfaces
  - keep the lane narrower than `CO-67`
  - keep runtime totals aligned to the same filtered source set as the running table

## Issue-Shaping Contract
- User-request translation carried forward:
  - historical non-provider `in_progress` manifests must stop surfacing as current running work
  - running counts and aggregate runtime must use the same authoritative current source set
  - terminal and attach-viewer screenshots must be visible directly in Linear
- Protected terms / exact artifact and surface names:
  - `CO STATUS`
  - `/api/v1/state`
  - `provider_issue_released:not_active`
  - `selectedRunProjection.ts`
  - `controlRuntime.ts`
  - `orchestrator/tests/ControlRuntime.test.ts`
  - `orchestrator/tests/ControlServer.test.ts`
- Nearby wrong interpretations to reject:
  - raw historical `in_progress` status is enough to count as current activity
  - generic `taskId` / `runId` identity fallback is sufficient for historical current-running admission
  - runtime totals can still use a broader source set than the running table
  - proof can stop at logs or JSON
- Explicit non-goals carried forward:
  - no paused snapshot rework
  - no token or rate-limit follow-on work
  - no `/ui` redesign or new viewer command

## Parity / Alignment Matrix
- Not a broad parity lane; this is a bounded current-activity correctness fix.
- Current truth:
  - local intake state shows no active claims
  - live compatibility state still reports historical running rows and inflated runtime
  - null-provider historical manifests survive current-running admission through permissive fallback logic
- Reference truth:
  - only selected or active-claim-backed work should count as current activity
  - when no active current work exists, running/runtime should collapse to zero truthfully
- Target truth / intended delta:
  - null-provider historical manifests without an active claim no longer count as current running work
  - runtime totals align to the filtered running set
  - screenshots on this device prove the corrected live state
- Explicitly out-of-scope differences:
  - broader observability redesign
  - historical analytics or archive views
  - unrelated current-tree telemetry formatting work

## Readiness Gate
- Not done if:
  - live `CO STATUS` or `/api/v1/state` still shows stale historical running work with no active claims
  - runtime totals remain inflated after the running set is filtered
  - the regression test for the null-provider historical-manifest case is missing
  - proof is not embedded directly in Linear
- Pre-implementation issue-quality review evidence:
  - this issue is correctly narrower than `CO-67`: the accepted scope is current-source selection only, using the already-landed authoritative telemetry work as baseline rather than reopening it.
- Safeguard ownership split:
  - keep code changes in `controlRuntime.ts` and focused tests
  - file a follow-up if a broader selected-run identity contract problem appears

## Technical Requirements
- Functional requirements:
  - historical null-provider manifests without an active current claim must fail closed for current-running admission
  - the selected run and active intake-backed work must remain eligible as authoritative current activity
  - aggregate runtime must be derived from the same filtered current-running set
  - compatibility/API surfaces must present the filtered current-running truth consistently
  - focused tests must fail closed on the null-provider historical-manifest case
- Non-functional requirements:
  - keep existing filesystem discovery bounded
  - preserve existing selected-run semantics for the active run
  - avoid broadening the lane into unrelated telemetry seams
- Interfaces / contracts:
  - selected-run discovery remains a read-only input seam through `orchestrator/src/cli/control/selectedRunProjection.ts`
  - current-running/runtime filtering: `orchestrator/src/cli/control/controlRuntime.ts`
  - compatibility route projection coverage: `orchestrator/tests/ControlServer.test.ts`
  - runtime coverage: `orchestrator/tests/ControlRuntime.test.ts`

## Architecture & Data
- Architecture / design adjustments:
  - keep selected-run identity fallback behavior unchanged for this bounded lane; the selected manifest still needs that compatibility contract
  - change null-provider current-running admission to require explicit current authority rather than "not provider-bound", including the selected `local-mcp` fallback manifest
  - keep runtime totals derived from the same filtered issue index / running source collection
- Data model changes / migrations:
  - no external schema migrations
  - compatibility payload semantics become stricter about what counts as current running activity
- External dependencies / integrations:
  - local provider intake state under `.runs/local-mcp/cli/control-host/`
  - live host `/api/v1/state`
  - Linear-hosted screenshot uploads through the workpad refresh path

## Validation Plan
- Tests / checks:
  - audited `linear child-stream --pipeline docs-review` succeeded with `review_outcome: clean-success`
  - focused runtime and compatibility route regressions for the historical null-provider case
  - required repo validation floor after implementation
- Rollout verification:
  - verify live host/API truth on this device
  - capture terminal and attach-viewer screenshots and embed them in Linear
- Monitoring / alerts:
  - rely on focused regressions and live proof artifacts; no new monitoring surface is introduced

## Open Questions
- None.

## Approvals
- Reviewer: docs-review child stream succeeded at `.runs/linear-6bed26fd-ea66-43c1-8324-b10871769435-co-73-docs-review/cli/2026-04-03T01-19-03-347Z-5f8becc5/manifest.json`
- Date: 2026-04-03
