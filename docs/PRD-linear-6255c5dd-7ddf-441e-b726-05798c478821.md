# PRD - Fix missing CO-276 docs packet paths blocking docs:check

## Traceability
- Linear issue: `CO-370` / `6255c5dd-7ddf-441e-b726-05798c478821`
- Linear URL: https://linear.app/asabeko/issue/CO-370/fix-missing-co-276-docs-packet-paths-blocking-docscheck
- Source issue: `CO-354` / `bf27af4a-01b6-4006-a39b-a9c668be8548`
- Follow-up packet prefix: `linear-6255c5dd-7ddf-441e-b726-05798c478821`
- Source anchor: `ctx:sha256:f07f76e55455b4f08b706cb17da0724f6980b730e226641dd2fba1421931e7d1#chunk:c000001`
- Source object id: `sha256:f07f76e55455b4f08b706cb17da0724f6980b730e226641dd2fba1421931e7d1`
- Declared source payload: `.runs/linear-6255c5dd-7ddf-441e-b726-05798c478821-co370-docs-packet/cli/2026-04-25T12-22-20-829Z-83b9bda9/memory/source-0/source.txt`
- Origin manifest: `.runs/linear-6255c5dd-7ddf-441e-b726-05798c478821-co370-docs-packet/cli/2026-04-25T12-22-20-829Z-83b9bda9/manifest.json`
- Source payload note: the declared `.runs` payload was absent in the child checkout when the packet was drafted; it is present in the parent artifact tree. Exact issue wording was preserved through the read-only `linear issue-context` helper; no Linear mutation was performed by this child lane.

## Problem
`npm run docs:check` failed during `CO-354` pre-implementation docs-review on a stale workspace because existing `CO-276` / `linear-1c101ebc-4b86-4c1f-b04d-0455e50fbacb` task registry and `docs/TASKS.md` references pointed at docs/checklist files that were not present on disk.

Observed missing paths from `docs:check`:
- `docs/PRD-linear-1c101ebc-4b86-4c1f-b04d-0455e50fbacb.md`
- `docs/TECH_SPEC-linear-1c101ebc-4b86-4c1f-b04d-0455e50fbacb.md`
- `docs/ACTION_PLAN-linear-1c101ebc-4b86-4c1f-b04d-0455e50fbacb.md`
- `tasks/specs/linear-1c101ebc-4b86-4c1f-b04d-0455e50fbacb.md`
- `tasks/tasks-linear-1c101ebc-4b86-4c1f-b04d-0455e50fbacb.md`
- `.agent/task/linear-1c101ebc-4b86-4c1f-b04d-0455e50fbacb.md`

## Intent Checksum
- Exact terms to preserve: `CO-276`, `linear-1c101ebc-4b86-4c1f-b04d-0455e50fbacb`, `docs:check`, `backticked-path-missing`, `tasks/index.json`, `docs/docs-freshness-registry.json`, `docs/TASKS.md`.
- Protected issue boundary: reject the interpretation that `CO-354` should implement this unrelated packet repair.
- Nearby wrong interpretations to reject:
  - Changing `CO-354` doctor/default behavior.
  - Performing a broad docs freshness sweep.
  - Removing unrelated task history.
  - Weakening `docs:check` missing-path validation.
  - Treating this child lane as authority to reconcile `CO-276` directly.

## Goals
- Create the required `CO-370` docs-first packet before active implementation.
- Parent lane reconciles the `CO-276` / `linear-1c101ebc-4b86-4c1f-b04d-0455e50fbacb` docs packet references against current `origin/main`, where CO-332/CO-350 already restored the cited files, and records that all paths cited from `docs/TASKS.md`, `tasks/index.json`, and `docs/docs-freshness-registry.json` exist.
- Ensure `npm run docs:check` no longer fails on `backticked-path-missing` for `linear-1c101ebc-4b86-4c1f-b04d-0455e50fbacb`.
- Keep the implementation scope limited to the `CO-276` packet/registry mismatch.

## Non-Goals
- Do not change `CO-354` doctor/default behavior.
- Do not perform a broad docs freshness sweep.
- Do not remove unrelated task history.
- Do not weaken `docs:check` missing-path validation.
- Do not edit `CO-276` packet files, registry mirrors, code, tests, or docs hygiene logic from this child lane.

## Parity / Alignment Matrix
| Surface | Current truth | Target truth |
| --- | --- | --- |
| `docs/TASKS.md` | Stale workspace references were missing files; current `origin/main` already includes the CO-332/CO-350 restored packet paths. | Parent keeps the current-main references backed by real files and records the CO-370 closeout packet. |
| `tasks/index.json` | Current `origin/main` registers the `CO-276` packet against files that exist on disk. | Parent preserves that registry truth and registers the CO-370 packet. |
| `docs/docs-freshness-registry.json` | Current `origin/main` carries freshness coverage for the restored `CO-276` docs surfaces. | Parent keeps freshness coverage aligned with actual packet files and adds CO-370 coverage. |
| `docs:check` | Stale workspace failed with `backticked-path-missing`; current-main validation needs confirmation. | No missing-path failure for that task id, without weakening validation. |
| `CO-354` | Source issue that exposed the docs hygiene blocker. | Remains out of scope for this repair. |

## Not Done If
- `npm run docs:check` still reports `backticked-path-missing` for `linear-1c101ebc-4b86-4c1f-b04d-0455e50fbacb`.
- Registry entries still point at missing `CO-276` packet files without recorded rationale.
- The fix hides missing files by weakening docs hygiene checks.
- The diff changes `CO-354` doctor/default behavior or unrelated docs history.

## Acceptance Criteria
1. Reconcile the `CO-276` / `linear-1c101ebc-4b86-4c1f-b04d-0455e50fbacb` docs packet references so all paths cited from `docs/TASKS.md`, `tasks/index.json`, and `docs/docs-freshness-registry.json` exist or are intentionally removed with recorded rationale.
2. `npm run docs:check` no longer fails on those missing paths.
3. Scope remains limited to the `CO-276` packet/registry mismatch.
4. `CO-354` doctor/default behavior remains unchanged.
5. `docs:check` missing-path validation remains strict.

## Stakeholders
- Parent lane owning `CO-370` implementation, Linear state, workpad, PR lifecycle, and validation.
- Reviewers relying on `docs:check`, `docs/TASKS.md`, `tasks/index.json`, and `docs/docs-freshness-registry.json` as governed docs surfaces.
- Future agents resuming `CO-276` or `CO-354` without inheriting stale missing-path references.

## Technical Considerations
- Current `origin/main` already contains the six historical `CO-276` packet files through the CO-332/CO-350 repair lineage, so this lane should not duplicate restoration work.
- Registry and freshness updates are parent-owned because the child lane was limited to the six `CO-370` packet files.
- Validation must prove the current-main blocker is removed without weakening `docs:check` logic.
- The repo-supported `npm run docs:archive-tasks` helper owns `docs/TASKS.md` reserve restoration when the new snapshot would otherwise exceed the 440-line reserve target.

## Open Questions
- None blocking. Current `origin/main` already contains the restored `CO-276` packet files; parent records and validates that reconciliation.
