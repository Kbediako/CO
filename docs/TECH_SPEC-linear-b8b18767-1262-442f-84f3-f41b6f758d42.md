---
id: 20260513-linear-b8b18767-1262-442f-84f3-f41b6f758d42
title: CO-533 docs/TASKS archive reserve restoration
relates_to: docs/PRD-linear-b8b18767-1262-442f-84f3-f41b6f758d42.md
risk: medium
owners:
  - Codex
last_review: 2026-05-13
---

## Summary
- Objective: make the supported task archive helper restore the configured reserve target from current main.
- Scope: `scripts/tasks-archive.mjs`, `tests/tasks-archive.spec.ts`, verified historical task metadata in `tasks/index.json`, tracked archive output, and CO-533 packet mirrors.
- Constraints: keep archive and docs gates strict; preserve historical evidence; do not promote rows whose checklists still show handoff pending.

## Issue-Shaping Contract
- User-request translation carried forward: fix the root cause instead of patching over repeated `docs/TASKS.md` reserve failures.
- Protected terms / exact artifact and surface names: `docs/TASKS.md`, `docs/TASKS-archive-2026.md`, `tasks/index.json`, `tasks-archive-index`, `reserve_lines=10`, `target_lines=440`.
- Nearby wrong interpretations to reject: deleting rows, raising the hard max, weakening `docs:check`, or treating all terminal-looking prose as canonical completion.
- Explicit non-goals carried forward: CO-530 freshness cohort classification and Linear WIP/admission behavior.

## Parity / Alignment Matrix
- Current truth: current main at 448 lines fails archive dry-run; archive-index adjacency and nonterminal canonical statuses prevent sufficient candidates.
- Reference truth: CO-221 reserve policy expects proactive restoration to 440 lines.
- Target truth / intended delta: archive-index is ignored as task content, stale-completion blockers are diagnosable, and verified completed rows allow restoration to 434 lines before the CO-533 snapshot is added.
- Explicitly out-of-scope differences: unrelated docs freshness caps and nonterminal CO-451 handoff metadata.

## Readiness Gate
- Not done if: reserve target is not restored, archive truth is lost, or stale metadata blockers are hidden.
- Pre-implementation issue-quality review evidence: live dry-run reproduced `Unable to restore reserve target 440`; focused audit identified archive-index structural handling plus stale `tasks/index.json` status for 0933/0934.
- Safeguard ownership split: CO-533 owns archive reserve repair; CO-530 owns Apr 9-12 freshness cohort reclassification; CO-451 remains nonterminal.

## Technical Requirements
- Functional requirements:
  - `parseHeaderSections` must terminate a header-only snapshot before `tasks-archive-index`.
  - Archive failure messages must list terminal-looking snapshots blocked by nonterminal `tasks/index.json` status.
  - `tasks/index.json` may mark 0933 and 0934 completed because their mirrors show implementation and validation evidence.
  - Archive execution must write displaced snapshots into tracked `docs/TASKS-archive-2026.md`.
- Non-functional requirements (performance, reliability, security): deterministic local parsing, no network dependency, no destructive history loss.
- Interfaces / contracts: CLI output remains human-readable; error text gains blocker diagnostics.

## Fallback Expiry / Refactor Decision
- Applies to fallback, compatibility, legacy, stale, cached, break-glass, or minor-seam behavior? Yes.

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Task archive parser | Archive-index block parsed as adjacent header-only snapshot content | remove fallback | CO-533 | `tasks-archive-index` follows a completed header-only snapshot | historical | 2026-05-13 | removed in this lane | Parser stops before archive-index | `tests/tasks-archive.spec.ts` adjacent-index regression |
| Task index metadata | Terminal-looking prose conflicts with nonterminal canonical status | justify retaining fallback | CO-533 | Historical metadata drift remains possible | historical | 2026-05-13 | Non-expiring fail-closed diagnostic contract | Replace only with canonical lifecycle reconciliation | blocked-status diagnostic regression |

- Large-refactor check: a larger lifecycle reconciler is desirable but belongs to docs freshness prevention work; CO-533 keeps the archive command fail-closed and restores the reserve without broad registry churn.

## Architecture & Data
- Architecture / design adjustments: treat archive-index comments as file-level metadata during header section discovery.
- Data model changes / migrations: mark verified completed legacy rows 0933 and 0934 as `completed` with `completed_at=2026-01-05`.
- External dependencies / integrations: none.

## Validation Plan
- Tests / checks: focused `tasks-archive` tests, archive dry-run, archive execution, JSON parse, docs gates, spec guard, and review.
- Rollout verification: `docs/TASKS.md` remains under 440 after archive and under target after the CO-533 snapshot.
- Monitoring / alerts: existing `docs:check` and archive command output.

## Open Questions
- None blocking.

## Approvals
- Reviewer: pending.
- Date: 2026-05-13.
