# ACTION_PLAN: Coordinator Symphony-Aligned Orchestrator Resume Pre-Start Failure Persistence Callback Extraction

## Docs-first

- [x] Create PRD / TECH_SPEC / ACTION_PLAN / findings / spec / checklist / .agent mirror for `1200`.
- [x] Update `tasks/index.json`, `docs/TASKS.md`, and `docs/docs-freshness-registry.json`.
- [ ] Capture local deliberation plus docs-review approval or explicit override for the resume pre-start failure persistence callback seam.

## Implementation

- [ ] Extract the inline resume pre-start failure persistence callback from `orchestrator.ts`.
- [ ] Keep failed-status detail, forced persistence, and warning semantics exact.
- [ ] Preserve focused regressions around the callback contract without widening into broader control-plane lifecycle behavior.

## Closeout

- [ ] Run the standard gate bundle and capture closeout artifacts under `out/1200-coordinator-symphony-aligned-orchestrator-resume-pre-start-failure-persistence-callback-extraction/manual/<timestamp>-closeout/`.
- [ ] Run an explicit elegance review.
- [ ] Record the next truthful seam after `1200`.
