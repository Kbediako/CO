# PRD - CO: stabilize LockFile keepalive timing in full-suite runs

## Added by Docs Child Lane 2026-04-18

## Traceability
- Linear issue: `CO-234` / `80433961-2c2c-4199-b694-976b3fd67517`
- Linear URL: https://linear.app/asabeko/issue/CO-234/stabilize-lockfile-keepalive-timing-flake-in-full-npm-run-test
- MCP Task ID: `linear-80433961-2c2c-4199-b694-976b3fd67517`
- Canonical task ID: `20260418-linear-80433961-2c2c-4199-b694-976b3fd67517`
- Canonical spec: `tasks/specs/linear-80433961-2c2c-4199-b694-976b3fd67517.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-80433961-2c2c-4199-b694-976b3fd67517.md`
- Task checklist: `tasks/tasks-linear-80433961-2c2c-4199-b694-976b3fd67517.md`
- Docs packet child lane manifest: `.runs/linear-80433961-2c2c-4199-b694-976b3fd67517-docs-packet/cli/2026-04-18T16-15-30-031Z-5847f253/manifest.json`
- Source anchor: `ctx:sha256:2b498d1162b00fb3e2b9a5ab23a18ea8fba830298bd8865f8b7bcf08bce46163#chunk:c000001`
- Source payload note: the declared `source-0/source.txt` path is absent in this child checkout. This packet is anchored on the parent-provided `CO-234` intent, the shared source anchor, the current `LockFile` surfaces, and the existing split-out note in `tasks/tasks-linear-a0ef4f6c-bf78-4dd6-9d43-244c445b87cb.md`.

## Summary
- Problem Statement: ambient full-suite `npm run test` can intermittently fail in `orchestrator/tests/LockFile.test.ts` around the live-lock keepalive timing path even though the isolated `LockFile.test.ts` rerun can pass. The failure class is timing-sensitive under full-suite load, not a license to broaden into unrelated full-suite flakes.
- Desired Outcome: `orchestrator/tests/LockFile.test.ts` becomes stable under full `npm run test`, while `acquireLockWithRetry` still reclaims genuinely stale orphan locks, `createLockKeepalive` still keeps live locks fresh, and release still removes only the owned lock.

## User Request Translation (Context Anchor)
- User intent / needs (in your own words): create the docs-first packet for `CO-234` only so the parent lane can stabilize the ambient full-suite `LockFile.test.ts` keepalive timing flake without editing unrelated source, watering down lock coverage, or absorbing other ambient flakes such as `CO-235`.
- Success criteria / acceptance:
  - preserve the issue contract around `orchestrator/tests/LockFile.test.ts`, `acquireLockWithRetry`, `createLockKeepalive`, and full `npm run test`
  - reproduce or diagnose the ambient full-suite keepalive timing flake with current evidence, or record an explicit current-head non-reproduction before changing behavior
  - make the live-lock keepalive assertion deterministic enough that a waiting writer does not stale-take a live lock under full-suite load
  - preserve stale orphan lock recovery
  - preserve owner-token release behavior
  - finish with focused `LockFile.test.ts` coverage plus full `npm run test`
- Constraints / non-goals:
  - child lane owns only the declared docs packet and registry mirror files
  - parent lane owns implementation, source/test inspection, validation, Linear state, workpad, PR lifecycle, and merge
  - do not edit implementation or test files in this child lane
  - do not run full repo validation suites in this child lane

## Intent Checksum
- Exact user wording / phrases to preserve:
  - stabilize the ambient full-suite LockFile keepalive timing flake
  - `orchestrator/tests/LockFile.test.ts`
  - `acquireLockWithRetry`
  - `createLockKeepalive`
  - full `npm run test`
- Protected terms / exact artifact and surface names:
  - `orchestrator/tests/LockFile.test.ts`
  - `orchestrator/src/persistence/lockFile.ts`
  - `acquireLockWithRetry`
  - `createLockKeepalive`
  - `full npm run test`
  - `npm run test`
  - live lock
  - stale lock
  - keepalive timing
  - owner token
- Nearby wrong interpretations to reject:
  - do not fold this into `CO-235` or any `SelectedRunProjection` timeout drift
  - do not treat this as a generic Vitest timeout or full-suite performance lane
  - do not remove the live-lock freshness assertion to make the suite green
  - do not weaken stale orphan recovery or owner-token release behavior
  - do not make broad persistence lock consumer changes without evidence from the parent lane

## Parity / Alignment Matrix

| Surface | Current Truth | Reference Truth | Target Truth |
| --- | --- | --- | --- |
| Full `npm run test` | Ambient full-suite runs can expose a `LockFile.test.ts` keepalive timing flake while isolated reruns may pass. | Full-suite validation should be authoritative for timing-sensitive shared primitives. | Full `npm run test` completes without the `LockFile.test.ts` keepalive flake. |
| Live lock freshness | The live-lock test uses a small `staleMs` window and waits long enough for the keepalive to refresh the lock mtime. Under suite load, timer jitter can make this path flaky. | A live lock with an active keepalive should not be stale-taken by a waiting writer. | The test and/or keepalive contract is deterministic enough that live ownership is preserved under full-suite load. |
| Stale orphan recovery | `acquireLockWithRetry` can remove an old orphan lock and acquire a new owner token. | Stale orphan recovery must remain available for crashed writers. | Stale orphan recovery continues to pass and is not weakened to fix the live-lock case. |
| Owner-token release | Release closes the handle, stops keepalive, and removes the lock only if the file still contains the same owner token. | Release must not delete another writer's lock. | Owner-token release behavior remains unchanged or becomes more directly covered. |
| Adjacent flakes | `CO-227` split out `CO-234` for `LockFile.test.ts` keepalive timing and `CO-235` for `SelectedRunProjection.test.ts` timeout drift. | Ambient flakes should remain separately owned. | `CO-234` closes only the LockFile keepalive timing class. |

## Acceptance Criteria
- `CO-234` packet and registry mirrors exist before implementation.
- Parent implementation preserves `orchestrator/tests/LockFile.test.ts`, `acquireLockWithRetry`, `createLockKeepalive`, and full `npm run test` as protected surfaces.
- The ambient full-suite `LockFile.test.ts` keepalive timing flake is reproduced or explicitly diagnosed as non-repro on current head before any behavior change is claimed.
- A live lock remains fresh enough that a waiting writer cannot stale-take it during the covered keepalive window.
- A genuinely stale orphan lock remains reclaimable.
- Release still removes only the owned lock.
- Focused LockFile coverage and full `npm run test` pass after the parent fix.

## Non-Goals
- No `CO-235` / `SelectedRunProjection.test.ts` timeout drift work.
- No broad Vitest timeout increase as the primary fix.
- No deletion or weakening of the live-lock freshness assertion.
- No unrelated persistence consumer changes in `TaskStateStore`, `ExperienceStore`, provider worker state, metrics, or Linear budget state unless parent evidence proves the shared lock primitive requires it.
- No implementation or test edits in this docs child lane.

## Not Done If
- Full `npm run test` can still fail in `orchestrator/tests/LockFile.test.ts` due to the keepalive timing path.
- The fix only inflates sleeps, retries, or suite timeouts without making live-lock freshness deterministic.
- `acquireLockWithRetry` can stale-take a live lock that still has an active keepalive.
- `createLockKeepalive` stops refreshing too early or keeps refreshing after release.
- Stale orphan lock recovery regresses.
- The lane drifts into `CO-235`, generic full-suite timeout work, or unrelated persistence lock consumers.

## Stakeholders
- Product: CO operators and maintainers who rely on full `npm run test` as a release gate.
- Engineering: persistence lock, task-state, provider-worker, and validation maintainers.
- Design: N/A.

## Metrics & Guardrails
- Primary Success Metrics:
  - focused `orchestrator/tests/LockFile.test.ts` coverage passes
  - full `npm run test` passes on the parent lane after the fix
  - no stale orphan recovery regression
  - no live lock stale-takeover regression
- Guardrails / Error Budgets:
  - no global timeout inflation as the only remedy
  - preserve fail-safe behavior for orphaned lock files
  - preserve release ownership checks
  - keep adjacent ambient flakes separately tracked

## User Experience
- Personas:
  - maintainer waiting on full-suite validation before review handoff
  - parent provider worker diagnosing an ambient timing failure without wanting to rewrite unrelated test suites
- User Journeys:
  - maintainer runs full `npm run test` and receives terminal green status instead of an intermittent `LockFile.test.ts` failure
  - maintainer can still trust the lock primitive to reject stale-takeover of live writers while reclaiming orphaned locks

## Technical Considerations
- Architectural Notes:
  - `acquireLockWithRetry` creates the owner-token lock file with `open(..., 'wx+')`, starts `createLockKeepalive` when `staleMs > 0`, and retries on `EEXIST`
  - `createLockKeepalive` currently refreshes mtime on an interval derived from `staleMs`
  - `orchestrator/tests/LockFile.test.ts` covers stale orphan recovery and live-lock freshness using small millisecond windows
  - the smallest likely parent repair should improve deterministic timing or test isolation without hiding the live-lock contract
- Dependencies / Integrations:
  - `orchestrator/src/persistence/lockFile.ts`
  - `orchestrator/tests/LockFile.test.ts`
  - `npm run test:orchestrator -- orchestrator/tests/LockFile.test.ts`
  - `npm run test`

## Open Questions
- Does the parent reproduce the flake only under full-suite worker pressure, or can it be forced with a focused stress loop over `orchestrator/tests/LockFile.test.ts`?
- Is the smallest correct fix in the test timing envelope, in the keepalive refresh strategy, or both?
- Should parent add a bounded fake-clock or explicit instrumentation path, or would that overfit this primitive and obscure real timer behavior?

## Approvals
- Product: self-approved from the bounded `CO-234` issue wording carried into this packet.
- Engineering: pending parent docs-review / implementation handoff.
- Design: N/A.
