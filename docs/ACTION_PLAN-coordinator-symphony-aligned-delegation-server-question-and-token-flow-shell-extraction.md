# ACTION_PLAN: Coordinator Symphony-Aligned Delegation Server Question and Token Flow Shell Extraction

## Steps

1. Register `1231` docs-first artifacts, task mirrors, and registry/index entries.
2. Run the deterministic docs-first guard bundle (`spec-guard`, `docs:check`, `docs:freshness`).
3. Reinspect `delegationServer.ts` around question enqueue/poll, delegation-token resolution, await-state detection, and expiry fallback to confirm the exact extraction seam.
4. Extract the question/token flow shell without reopening the `1230` transport/tool-dispatch boundary.
5. Close with focused regression evidence and an explicit minimality review.

## Guardrails

- Do not widen into `delegationServerTransport.ts` or `delegationServerToolDispatchShell.ts`.
- Do not change delegate spawn/status/cancel behavior in this lane.
- Do not widen into dynamic-tool bridge policy, GitHub tool handling, or Telegram/Linear setup/auth work.
- Prefer one bounded extraction lane now; defer later delegation-server seams if the host file still retains truthful handler/policy ownership afterward.
