# ACTION PLAN - CO-328 accepted child-lane workspace artifacts and diff-budget scope

## Summary
- Goal: make `diff-budget` ignore accepted child-lane workspace copies only under the durable provider-artifact contract.
- Scope: docs-first packet, focused diff-budget implementation, focused regression coverage, validation, review, PR handoff.
- Assumptions: durable child-lane evidence can be detected from `.runs/**/provider-linear-child-lane.patch` plus proof/ledger metadata.

## Issue Readiness Gate
- Intent checksum / protected terms carried forward: `diff-budget`, `.child-lanes`, accepted same-issue child-lane, `provider-linear-child-lane.patch`, acceptance ledger, untracked budget surface.
- Not done if: accepted `.child-lanes/` copies still inflate budget after durable acceptance evidence exists, or unrelated untracked files are no longer counted.
- Pre-implementation issue-quality review: parent provider-worker reviewed the issue on 2026-04-23 and confirmed this is not a broad untracked-ignore or diff-budget-disablement request.

## Milestones & Sequencing
1. Create docs-first packet, task mirrors, registry entries, workpad, and parallelization evidence.
2. Run pre-implementation docs-review or record a truthful fallback if the wrapper is unavailable.
3. Inspect live child-lane artifacts and implement a fail-closed accepted-artifact predicate in `scripts/diff-budget.mjs`.
4. Integrate or reject the same-issue tests child-lane patch after parent review.
5. Run focused and required validation, standalone review, elegance review, PR attach, ready-review drain, and Linear review handoff.

## Dependencies
- Same-issue child lane `tests-child-lane-artifacts` owns `tests/diff-budget.spec.ts` until accepted, rejected, or invalidated.
- Parent owns docs, script implementation, validation, Linear state, and PR lifecycle.

## Validation
- Checks / tests:
  - `npx vitest run tests/diff-budget.spec.ts`
  - `node scripts/delegation-guard.mjs`
  - `node scripts/spec-guard.mjs --dry-run`
  - `npm run build`
  - `npm run lint`
  - `npm run test`
  - `npm run docs:check`
  - `npm run docs:freshness`
  - `npm run repo:stewardship`
  - `node scripts/diff-budget.mjs`
  - manifest-backed standalone review
  - explicit elegance review
- Rollback plan: revert the predicate and tests; local `.child-lanes/` pruning remains the operational fallback until a corrected rule lands.

## Risks & Mitigations
- Risk: predicate ignores pending child-lane output.
  - Mitigation: require durable patch plus accepted proof/ledger signal.
- Risk: evidence schema varies across older child lanes.
  - Mitigation: fail closed and count the workspace when evidence cannot be parsed or linked.
- Risk: docs packet pushes active task snapshot over line budget.
  - Mitigation: keep the snapshot concise and run docs gates.

## Approvals
- Reviewer: parent provider-worker.
- Date: 2026-04-23.
