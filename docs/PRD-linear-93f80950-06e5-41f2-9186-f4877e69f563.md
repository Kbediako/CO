# PRD - CO-471 build:all strict test typecheck debt

## Traceability
- Linear issue: `CO-471` / `93f80950-06e5-41f2-9186-f4877e69f563`
- Linear URL: https://linear.app/asabeko/issue/CO-471
- Task id: `linear-93f80950-06e5-41f2-9186-f4877e69f563`
- Canonical registry id: `20260506-linear-93f80950-06e5-41f2-9186-f4877e69f563`
- Canonical spec: `tasks/specs/linear-93f80950-06e5-41f2-9186-f4877e69f563.md`
- Packet prefix: `linear-93f80950-06e5-41f2-9186-f4877e69f563`
- Canonical owner key: `build-all-strict-test-typecheck-debt`
- Backlog hold target: `backlog_head_follow_up_traceability_pending`

## Summary
- Problem Statement: `npm run build` can pass through `tsconfig.build.json` while `npm run build:all` still fails the root `tsconfig.json` strict test type debt that protects the release/full-matrix signal.
- Desired Outcome: create the CO-471 docs-first traceability packet and registry mirrors so the Backlog hold can clear without pretending the build:all cleanup is implemented.

## User Request Translation
- User intent / needs:
  - create the packet and mirrors for Linear CO-471 before it leaves Backlog
  - preserve `npm run build:all` as a release/full-matrix signal
  - keep root `tsconfig.json` strict test typecheck coverage visible
  - keep `tsconfig.build.json` as the narrower production build path without using it as completion proof
  - make future implementation own the strict test type debt in `ProviderIssueHandoff.test.ts`, `ProviderLinearWorkerRunner.test.ts`, `ControlServerPublicLifecycle.test.ts`, `ProviderMergeCloseout.test.ts`, and `LinearCliShell.test.ts`
  - avoid broadening into CO-458 or production runtime behavior
- Success criteria / acceptance:
  - packet docs and mirrors exist for `linear-93f80950-06e5-41f2-9186-f4877e69f563`
  - `tasks/index.json` registers `20260506-linear-93f80950-06e5-41f2-9186-f4877e69f563`
  - `docs/docs-freshness-registry.json` has active rows for the packet and mirrors
  - `docs/TASKS.md` has an active task snapshot
  - packet text clearly says this clears only `backlog_head_follow_up_traceability_pending`
  - implementation remains future work and must prove `npm run build:all`
- Constraints / non-goals:
  - no CO-458 broadening
  - no production runtime behavior change
  - no blanket tsconfig exclusions as silent fallback
  - no weakening or deleting `build:all`
  - no marking done because `npm run build` passes
  - no source, test, Linear, GitHub, or issue-state changes in this packet lane

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `npm run build:all`
  - root `tsconfig.json`
  - strict test type debt
  - release/full-matrix signal
  - `tsconfig.build.json`
  - `build-all-strict-test-typecheck-debt`
  - `backlog_head_follow_up_traceability_pending`
- Protected artifacts and surfaces:
  - `ProviderIssueHandoff.test.ts`
  - `ProviderLinearWorkerRunner.test.ts`
  - `ControlServerPublicLifecycle.test.ts`
  - `ProviderMergeCloseout.test.ts`
  - `LinearCliShell.test.ts`
  - `tasks/index.json`
  - `docs/TASKS.md`
  - `docs/docs-freshness-registry.json`
- Nearby wrong interpretations to reject:
  - treating this packet as the build:all cleanup
  - claiming `npm run build` proves the release/full-matrix signal is clean
  - excluding broad test trees from root `tsconfig.json`
  - weakening or deleting `npm run build:all`
  - changing production runtime behavior to avoid test typechecking
  - absorbing CO-458 source-root freshness drift scope

## Parity / Alignment Matrix

| Surface | Current Truth | Reference Truth | Target Truth / Intended Delta | Explicitly Out Of Scope |
| --- | --- | --- | --- | --- |
| `npm run build` | Runs `tsc -p tsconfig.build.json` and can pass while tests remain outside the production build signal. | Production build success is useful but not a release/full-matrix substitute. | Packet states `npm run build` passing is not completion for CO-471. | Changing production runtime output or using production build pass as Done evidence. |
| `npm run build:all` | Runs root `tsconfig.json` typechecking plus pattern build and exposes strict test type debt. | Release/full-matrix checks should catch test typing regressions before release posture work lands. | Future implementation must make `npm run build:all` clean without weakening the signal. | Deleting, renaming, or weakening `build:all`. |
| Root `tsconfig.json` | Provides the broad strict typecheck surface that sees test debt. | Root strict coverage is the debt authority for CO-471. | Future implementation fixes or deliberately scopes the named test type debt with reviewable evidence. | Blanket exclusions or silent fallbacks. |
| `tsconfig.build.json` | Supports the narrower production build path. | It is not proof that full-matrix typing is clean. | Retain as production build contract while CO-471 owns root strict test debt separately. | Treating `tsconfig.build.json` as a workaround for build:all. |
| Named strict-test debt | Debt is anchored on `ProviderIssueHandoff.test.ts`, `ProviderLinearWorkerRunner.test.ts`, `ControlServerPublicLifecycle.test.ts`, `ProviderMergeCloseout.test.ts`, and `LinearCliShell.test.ts`. | The issue identifies concrete tests instead of generic TypeScript noise. | Future implementation fixes those surfaces or records exact reviewed alternatives. | Broad fixture cleanup outside the listed debt, CO-458, runtime changes. |

## Not Done If
- The packet is missing from any required mirror or registry.
- `backlog_head_follow_up_traceability_pending` remains untraceable in repo docs.
- The packet says the build:all cleanup is done.
- CO-471 is marked done because `npm run build` passes.
- `npm run build:all` is weakened, deleted, renamed away, or bypassed.
- Root `tsconfig.json` gets blanket test exclusions as a silent fallback.
- `tsconfig.build.json` is used to hide strict test type debt.
- CO-458 or production runtime behavior is absorbed.

## Goals
- Create the CO-471 docs-first traceability packet.
- Register the task and packet mirrors under `20260506-linear-93f80950-06e5-41f2-9186-f4877e69f563`.
- Preserve all protected terms and non-goals for the future implementation lane.
- Make the implementation acceptance bar explicit: future work must clean `npm run build:all`, not just `npm run build`.

## Non-Goals
- No build:all cleanup implementation.
- No changes to root `tsconfig.json` or `tsconfig.build.json`.
- No edits to `ProviderIssueHandoff.test.ts`, `ProviderLinearWorkerRunner.test.ts`, `ControlServerPublicLifecycle.test.ts`, `ProviderMergeCloseout.test.ts`, or `LinearCliShell.test.ts`.
- No CO-458 scope.
- No production runtime behavior changes.
- No Linear/GitHub state changes.

## Stakeholders
- Product: CO operators relying on Backlog traceability before issue promotion.
- Engineering: maintainers of TypeScript build signals, release validation, and the named test suites.
- Review: reviewers checking that packet setup is not confused with implementation completion.

## Metrics & Guardrails
- Primary Success Metrics:
  - all six packet/mirror files exist
  - task index registration exists for `20260506-linear-93f80950-06e5-41f2-9186-f4877e69f563`
  - freshness registry has six active rows
  - `docs/TASKS.md` includes a CO-471 snapshot
  - protected-term scan finds all required terms
  - JSON parse and docs gates pass, or unrelated debt is captured exactly
- Guardrails / Error Budgets:
  - zero source/test/runtime edits in this packet lane
  - zero `build:all` weakening
  - zero blanket root `tsconfig.json` exclusions
  - zero completion claims based on `npm run build`

## Technical Considerations
- Architectural Notes:
  - `npm run build:all` is the future implementation's release/full-matrix authority.
  - `npm run build` and `tsconfig.build.json` remain production-build evidence only.
  - The packet does not decide whether future source repair, fixture typing repair, or explicit config adjustment is best; it requires any such decision to preserve the strict signal.
- Dependencies / Integrations:
  - `package.json` build scripts
  - root `tsconfig.json`
  - `tsconfig.build.json`
  - named strict-test debt files under `orchestrator/tests/`

## Fallback / Refactor Decision
- Applies to fallback, compatibility, legacy, stale, cached, break-glass, or minor-seam behavior? Yes.
- Decision summary:
  - expire the Backlog traceability hold once packet and registry mirrors exist
  - remove the unsafe future fallback where `npm run build` or blanket exclusions substitute for `npm run build:all`
  - justify retaining `tsconfig.build.json` only as a supported production-build contract, not as a completion path for CO-471

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Backlog promotion traceability | `backlog_head_follow_up_traceability_pending` while packet files and mirrors are absent | expire fallback | CO-471 | Linear issue has no repo-side traceability packet | 2026-05-06 | 2026-05-06 | until packet PR lands | Packet files, task index entry, `docs/TASKS.md` snapshot, and freshness rows exist on main | JSON parse, protected-term scan, spec guard, docs checks |
| Release/full-matrix build signal | Treating `npm run build` or `tsconfig.build.json` success as enough while `npm run build:all` exposes strict test type debt | remove fallback | CO-471 | Future implementation tries to complete with only production-build proof | 2026-05-06 | 2026-05-06 | 0 days | CO-471 completion requires `npm run build:all` evidence or an explicit reviewed release/full-matrix replacement that is not weaker | `npm run build:all`, focused named-test type debt review, docs/review gates |
| Production build config | `tsconfig.build.json` is a narrower production-build contract alongside root strict typecheck | justify retaining fallback | CO-471 | Production build needs a runtime/package-focused TypeScript config | existing contract | 2026-05-06 | non-expiring supported path | Retained only while `npm run build:all` remains the release/full-matrix signal and root strict test debt is not hidden | `npm run build`, `npm run build:all` for release/full-matrix lanes, docs checks |

- Large-refactor check: the future implementation may need source/test type repair or a reviewed build-signal consolidation, but this packet does not authorize a broad refactor or silent staged replacement.

## Open Questions
- Future implementation must reproduce the current `npm run build:all` failures and identify the exact strict test type debt shape in each protected test file.
- Future implementation must decide whether each debt item is best fixed in test fixtures, helper typings, source signatures, or a reviewed config contract that preserves the release/full-matrix signal.

## Approvals
- Product: Linear issue `CO-471` accepted as the traceability contract.
- Engineering: packet setup only; implementation remains future work.
- Design: N/A
