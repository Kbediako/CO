# ACTION_PLAN: Coordinator Symphony-Aligned Orchestrator Cloud Environment Resolution Boundary Extraction

## Objective

Move the shared cloud environment resolution behavior out of `orchestratorCloudTargetExecutor.ts` without widening into prompt assembly, request shaping, or lifecycle handling.

## Steps

1. Register the shared env-resolution seam docs-first and capture the local review rationale.
2. Extract the cloud env-resolution helpers into one adjacent helper/service while preserving precedence exactly.
3. Update the executor, cloud-route shell, and auto-scout recorder to consume the shared helper and add focused coverage for shared resolution behavior.
4. Run the lane validations, record closeout artifacts, and sync checklist mirrors.
