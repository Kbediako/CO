# ACTION_PLAN - CO-363 CO STATUS active issue projection and current-host-unhealthy classification

## Added by Docs Child Lane 2026-04-25

## Summary
- Goal: create the docs-first packet and mirrors for CO-363, so the parent lane can implement a bounded status/read-model fix for active issue projection, active identifiers/counts, reopened metadata, and `current-host-unhealthy classification`.
- Scope: PRD, docs TECH_SPEC, canonical task spec, ACTION_PLAN, task checklist, `.agent` mirror, `tasks/index.json`, and `docs/TASKS.md` snapshot only.
- Assumptions: the parent-provided handoff text and source anchor are authoritative; the expected source payload is absent in this child checkout; parent owns source reconciliation, implementation, validation, Linear/workpad state, and PR lifecycle.

## Issue Readiness Gate
- Intent checksum / protected terms carried forward: `CO STATUS`, `control-host`, `co-status --format json`, `/ui/data.json`, `control_endpoint.json`, `provider-intake-state.json`, `active issue projection`, `active identifiers and counts`, `CO-345`, `CO-356`, `CO-330`, `reopened issue metadata`, `current-host-unhealthy classification`.
- User-request translation carried forward: recover stale or dead `CO STATUS` / `control-host` status reads without dropping active issue projection. The fix must keep host-health truth separate from issue lifecycle truth while making `co-status --format json`, `/ui/data.json`, and the terminal status surface agree on active identifiers/counts.
- Nearby wrong interpretations to reject: cosmetic terminal-renderer-only work; destructive local-state cleanup; treating `current-host-unhealthy classification` as permission to hide active issues; counting stale `provider-intake-state.json` terminal history as active proof by itself; implementing related-issue scope for `CO-345`, `CO-356`, or `CO-330`; and skipping parity checks because one surface currently looks healthy.
- Explicit non-goals: no broad control-host restart redesign, no frontend status UI redesign, no deletion of retained status artifacts, no CO-330 stale-owner reclaim fix unless proven as the only root cause, and no expansion into CO-345/CO-356/CO-330 implementation work.
- Not done if: active identifiers/counts disagree across status surfaces; current-host-unhealthy truth hides active projection; reopened metadata for `CO-345`, `CO-356`, or `CO-330` is omitted; stale provider-intake history can count active by itself; or the solution relies on deleting local state.
- Pre-implementation issue-quality review: self-approved for docs-only packet work. The issue is not eligible for a micro-task shortcut because correctness depends on exact protected surface names, protected issue identifiers, parity/alignment, and Not Done If conditions.

## Parity / Alignment Matrix
- Current truth: `co-status --format json` and attached `CO STATUS` can read through a stale or dead `/ui/data.json` endpoint from `control_endpoint.json`; `provider-intake-state.json` can still retain active issue evidence; reopened issue metadata can drift from live Linear truth; completed residue such as `CO-345` can be visible while active work like `CO-356` is omitted.
- Reference truth: endpoint health, host-health classification, provider-intake evidence, and issue lifecycle truth are separate status dimensions. They may be reconciled into one surface, but one dimension must not erase or impersonate another.
- Target truth: when the current endpoint rotates, the status path recovers against the current endpoint; when the current host is still unreachable, `current-host-unhealthy classification` is explicit; `CO STATUS`, `co-status --format json`, and `/ui/data.json` agree on active identifiers/counts; reopened `CO-330` metadata is truthful; completed `CO-345` history does not displace active `CO-356` / `CO-330` projection.
- Out-of-scope differences: renderer styling, broad supervisor restart mechanics, unrelated provider admission behavior, deleting retained intake/status files, and implementation ownership for the related issues.

## Milestones & Sequencing
1. Create the CO-363 PRD, docs TECH_SPEC, canonical task spec, ACTION_PLAN, task checklist, and `.agent` mirror.
2. Register the canonical task spec in `tasks/index.json` under `items[]` with `last_review: 2026-04-25`.
3. Add the CO-363 snapshot to `docs/TASKS.md`.
4. Self-review the packet for protected terms, current/reference/target parity, wrong interpretations, non-goals, Not Done If, and parent-owned implementation boundaries.
5. Leave parent-owned work pending for docs-review, source/test implementation, focused status fixtures, live issue verification, validation, review, PR lifecycle, Linear state, and workpad updates.

## Dependencies
- Parent-provided source anchor: `ctx:sha256:c98bd9a7b2ae5c5d558c5f43b9145ac007b6cd61e7aafe81a749db4eac4429e0#chunk:c000001`.
- Docs child-lane manifest path: `.runs/linear-16623322-4b8d-4e12-8f57-688aa5f71a23-docs-packet/cli/2026-04-25T01-30-33-657Z-a2ee411b/manifest.json`.
- Parent source payload path: `.runs/linear-16623322-4b8d-4e12-8f57-688aa5f71a23-docs-packet/cli/2026-04-25T01-30-33-657Z-a2ee411b/memory/source-0/source.txt`.
- Local status surfaces: `CO STATUS`, `co-status --format json`, `/ui/data.json`, `control_endpoint.json`, and `provider-intake-state.json`.
- Parent-owned live issue/context verification for `CO-345`, `CO-356`, and `CO-330`.

## Validation
- Child checks:
  - protected-term scan across the touched docs/checklist files
  - `node -e` JSON parse of `tasks/index.json`
  - `git diff --check --` scoped to the touched files
  - `git diff --name-only` scoped review to confirm docs-only file scope
- Parent checks:
  - focused active identifiers/counts parity across `CO STATUS`, `co-status --format json`, and `/ui/data.json`
  - focused `current-host-unhealthy classification` fixture that preserves active projection
  - focused reopened metadata fixture covering `CO-345`, `CO-356`, and `CO-330`
  - focused stale-intake-history fixture proving `provider-intake-state.json` terminal residue alone does not count active
  - docs-review before implementation acceptance
- Rollback plan: parent can discard or narrow the packet if live source reconciliation proves CO-363 is actually duplicate-owned by another issue. This child lane makes no runtime changes.

## Risks & Mitigations
- Risk: host-health truth is conflated with issue lifecycle truth.
  - Mitigation: packet requires explicit `current-host-unhealthy classification` and active projection parity.
- Risk: stale `provider-intake-state.json` history inflates active counts.
  - Mitigation: packet requires live or reopened corroboration before active counting.
- Risk: CO-345, CO-356, or CO-330 scope is accidentally absorbed into CO-363.
  - Mitigation: related identifiers are protected context only; implementation ownership stays parent-verified.
- Risk: child lane widens into implementation or validation.
  - Mitigation: action plan keeps implementation, tests, docs-review, Linear/workpad, and PR work parent-owned.

## Approvals
- Reviewer: docs child lane self-review complete for packet scope.
- Date: 2026-04-25
