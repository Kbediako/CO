# ACTION_PLAN: Coordinator Symphony-Aligned Orchestrator Cloud Branch Resolution Boundary Extraction

## Objective

Move the shared cloud branch-resolution behavior out of duplicated inline callers without widening into environment-id resolution, request shaping, or lifecycle handling.

## Steps

1. Register the shared branch-resolution seam docs-first and capture the local review rationale.
2. Extract the cloud branch-resolution helpers into one adjacent helper/service while preserving precedence exactly.
3. Update the cloud executor, cloud-route shell, and auto-scout recorder to consume the shared helper and add focused coverage for shared branch behavior.
4. Run the lane validations, record closeout artifacts, and sync checklist mirrors.
