# ACTION_PLAN: Coordinator Symphony-Aligned Standalone Review Inspection Target Parsing Pipeline Extraction

## Objective

Open the next truthful standalone-review follow-on after `1212` by extracting the deterministic inspection-target parsing pipeline still reused across `review-execution-state` analyzers.

## Steps

1. Register `1213` docs-first with explicit evidence that the inspection-target parsing cluster remains inline inside `scripts/lib/review-execution-state.ts`.
2. Extract the bounded parsing helper while preserving stateful policy ownership in `scripts/lib/review-execution-state.ts`.
3. Add only the focused regressions needed to pin touched-path prioritization, nested payload parsing, and generic fallback behavior.
4. Run the lane validations and record any truthful docs-review or bounded-review overrides.
5. Sync closeout mirrors and reassess whether the next truthful follow-on remains in review execution-state helper support or shifts elsewhere.
