---
id: 20260427-linear-5ef45396-4546-4699-9d4a-c69fb9179868
title: "CO-396 runtime-routing fallback expiry"
relates_to: docs/PRD-linear-5ef45396-4546-4699-9d4a-c69fb9179868.md
risk: high
owners:
  - Codex
last_review: 2026-04-27
---

# TECH_SPEC - CO-396 runtime-routing fallback expiry

This mirror points to the canonical task spec at `tasks/specs/linear-5ef45396-4546-4699-9d4a-c69fb9179868.md`.

## Implementation Summary
- Prepare the missing traceability packet for `CO-396` while the issue remains in Backlog.
- Parent implementation should classify runtime-routing contracts under CO-382 before touching `orchestrator/src/cli/runtime/provider.ts`, `orchestrator/src/cli/runtime/codexCommand.ts`, `scripts/runtime-mode-canary.mjs`, or cloud-mode docs.
- Temporary auto reroutes should carry `expire fallback` metadata.
- Strict unsupported combinations, policy aliases, runtime fallback metadata, and canary scenarios should be retained only with explicit durable compatibility, fail-fast, audit, or validation rationale.
- Keep `executionMode` and `runtimeMode` orthogonal and keep `runtime_fallback` separate from `cloud_fallback`.

## Protected Surfaces
- `orchestrator/src/cli/runtime/provider.ts`
- `orchestrator/src/cli/runtime/codexCommand.ts`
- `orchestrator/src/cli/runtime/fallbackPolicy.ts`
- `orchestrator/src/cli/runtime/types.ts`
- `scripts/runtime-mode-canary.mjs`
- cloud-mode docs
- `CODEX_ORCHESTRATOR_RUNTIME_FALLBACK`
- `RuntimeFallbackMetadata`
- `RuntimeSelectionFailure`
- `runtime_fallback`
- `cloud_fallback`
- `policy`
- `policy_source`
- `original_target`
- `fallback_target`
- `blocking_reason`
- `required cloud canary`
- `fallback cloud canary`

## Fallback Expiry Decisions

| Fallback / seam | Decision | Parent implementation guidance |
| --- | --- | --- |
| Appserver preflight failure auto reroutes to CLI | expire fallback | Retain only with CO-396 owner metadata, review date `2026-05-10`, maximum lifetime `2026-05-26`, removal condition, and focused activation/non-activation tests. |
| Explicit or strict cloud appserver runtime block | justify retaining fallback | Retain as durable fail-fast blocked metadata; do not allow explicit `executionMode=cloud` plus `runtimeMode=appserver` to launch a fallback target. |
| Implicit default appserver under cloud auto coerces to CLI | expire fallback | Retain only as a temporary bridge; later implementation should either select `cli` explicitly before runtime selection or fail fast with equivalent metadata. |
| `CODEX_ORCHESTRATOR_RUNTIME_FALLBACK` and `allowFallback` aliases | justify retaining fallback | Retain as durable compatibility only because all aliases normalize to explicit `auto` or `strict` with `policy_source`. |
| `RuntimeFallbackMetadata` for no-fallback, fallback, and blocked decisions | justify retaining fallback | Retain as durable audit schema for runtime decision traceability. |
| Runtime-mode canary fallback scenarios | justify retaining fallback | Retain as durable validation contract even if production fallback behavior narrows. |

## Validation Contract
- Prep lane:
  - JSON parse for edited registries
  - protected-term scan over packet files
  - `npm run docs:check`
  - `npm run docs:freshness`
  - scoped diff/status review
- Parent lane:
  - docs-review before implementation
  - focused runtime fallback policy/provider tests
  - focused route/manifest/status/proof projection tests for `runtime_fallback`
  - `node scripts/runtime-mode-canary.mjs` or exact blocker
  - required/fallback cloud canary commands or exact blockers when cloud-mode docs or cloud contracts are touched

## Not Done If
- `runtime_fallback` and `cloud_fallback` are merged or described as equivalent.
- Explicit cloud appserver runtime requests silently fall back.
- The fallback cloud canary satisfies required cloud execution.
- The prep lane edits runtime source, tests, scripts, workflows, Linear state, or PR lifecycle.
