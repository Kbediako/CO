# PRD - CO: restore Apr 15 docs:freshness historical packet baseline

## Added by Bootstrap 2026-04-15

## Traceability
- Linear issue: `CO-184` / `237c874c-c05f-4947-949a-573043fc575f`
- Linear URL: https://linear.app/asabeko/issue/CO-184/co-restore-apr-15-docsfreshness-historical-packet-baseline
- Source issue: `CO-183` / `df69fabe-63c2-4b98-a226-9c37892b4f9d`
- Pre-fix reproduction report: `out/linear-237c874c-c05f-4947-949a-573043fc575f/pre-fix-docs-freshness.json`
- Post-fix report: `out/linear-237c874c-c05f-4947-949a-573043fc575f/docs-freshness.json`
- Classification note: `docs/findings/linear-237c874c-c05f-4947-949a-573043fc575f-docs-freshness-classification.md`

## Summary
- Problem Statement: after the 2026-04-15 date boundary, `npm run docs:freshness` fails on `173` stale historical task/report packet rows outside the already declared CO-175 rolling cohort. The same report still shows `221` CO-175 rolling entries, so adding the new rows to rolling deferral would exceed `max_entries=300` and would hide unrelated freshness debt.
- Desired Outcome: restore a green repo-wide `docs:freshness` baseline by making an explicit, evidence-backed handling decision for the newly stale historical rows while preserving public/active docs fail-closed behavior and CO-175 provenance.

## User Request Translation (Context Anchor)
- User intent / needs (in your own words): classify the April 15 stale historical packet set, choose a bounded outcome, and restore the freshness gate without weakening the rolling-cohort policy or silently hiding drift.
- Success criteria / acceptance:
  - reproduce the failing `docs:freshness` report and keep the artifact
  - classify the blocking stale set by owner, path family, class, and outcome
  - make `npm run docs:freshness` green with zero blocking stale docs
  - leave the existing CO-175 rolling debt visible and within documented caps
  - run `npm run docs:check`, `npm run docs:freshness`, and `node scripts/diff-budget.mjs`
- Constraints / non-goals:
  - do not weaken `docs:freshness`
  - do not raise `max_entries=300`
  - do not move public docs, active guides, skills, templates, missing rows, invalid rows, or uncatalogued docs into rolling deferral
  - do not silently bump `last_review` dates without rationale
  - do not change CO-183 Codex CLI posture or spark file-search policy

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `npm run docs:freshness`
  - `docs/docs-freshness-registry.json`
  - `docs/docs-catalog.json`
  - `docs/guides/docs-freshness-cohorts.md`
  - CO-175
  - rolling freshness cohort
  - `max_entries=300`
  - `.agent/task/0951-delegation-rlm-quick-wins.md`
  - task cohort `1196+`
  - Task Packet
  - Task Mirror
  - Report Only
- Protected terms / exact artifact and surface names:
  - `out/linear-237c874c-c05f-4947-949a-573043fc575f/pre-fix-docs-freshness.json`
  - `docs/findings/linear-237c874c-c05f-4947-949a-573043fc575f-docs-freshness-classification.md`
  - `co-175-apr-14-march-14-tasks-1164-1195`
- Nearby wrong interpretations to reject:
  - rerunning CO-183 validation with `--warn` fixes the repo baseline
  - the rolling cap can be raised or broadened for unrelated historical rows
  - a green freshness gate means CO-175 rolling debt disappeared
  - registry dates may be bumped without a classification and review note

## Parity / Alignment Matrix
- Current truth:
  - the current report has `173` blocking stale docs, `221` rolling CO-175 entries, and no missing registry, missing-on-disk, invalid, or uncatalogued docs
  - all `173` blocking rows are owned by `Codex (top-level agent), Review agent` and have `status=active`
  - the stale blocking set is limited to Task Packet, Task Mirror, and Report Only rows
- Reference truth:
  - public docs, active guides, agent policy, skills, templates, missing rows, invalid rows, and uncatalogued docs stay fail-closed
  - CO-175 remains the owner for the April 14 `1164-1195` rolling cohort
  - rolling deferral must stay within `max_entries=300`
- Target truth / intended delta:
  - the new April 15 blocking rows are explicitly reviewed and refreshed instead of added to rolling deferral
  - `docs:freshness` exits green with zero blocking stale docs
  - CO-175 rolling debt remains visible as `221` rolling entries until its own owner resolves it
- Explicitly out-of-scope differences:
  - public doc cleanup outside this stale historical packet set
  - changing the CO-175 row cap, window, or eligible classes
  - Codex CLI or spark policy changes from CO-183

## Not Done If
- `npm run docs:freshness` still fails.
- CO-175 rolling debt is hidden, reassigned, or made invisible.
- The new stale rows are only hidden by broadening policy capacity.
- The final report does not include stale-count, rolling-count, and sample-path evidence.

## Goals
- Restore the April 15 repo-wide docs freshness baseline.
- Preserve a machine-checkable classification of the stale set.
- Keep the fix to registry and docs evidence, not freshness policy expansion.
- Leave CO-175 rolling cohort semantics unchanged.

## Non-Goals
- Archive or delete historical packet files in this lane.
- Reclassify documents whose catalog class is already correct.
- Change active public guidance semantics.
- Reopen CO-183 feature work.

## Stakeholders
- Product: CO operators relying on `docs:freshness` as a truthfulness gate
- Engineering: maintainers of docs catalog, freshness registry, and task packets
- Design: N/A

## Metrics & Guardrails
- Primary Success Metrics:
  - blocking stale entries: `0`
  - rolling cohort entries remain visible and owned by CO-175
  - `docs:check`, `docs:freshness`, and `diff-budget` pass
- Guardrails / Error Budgets:
  - no `max_entries` increase
  - no expanded eligible doc classes
  - no public/active docs included in deferral
  - no silent date bump without this packet and classification note

## User Experience
- Personas:
  - feature-lane worker blocked by unrelated historical freshness rows
  - reviewer checking that stale docs were handled explicitly
  - maintainer inspecting CO-175 rolling debt
- User Journeys:
  - a worker can run `npm run docs:freshness` and see a green gate plus any remaining CO-175 rolling ledger
  - a reviewer can follow the PRD, spec, classification note, and report artifact to see why the April 15 rows were refreshed

## Technical Considerations
- Architectural Notes:
  - the current `docs:freshness` policy already fails closed when a stale row is outside a declared rolling cohort
  - no script behavior change is required if the registry rows are truthfully refreshed after classification
- Dependencies / Integrations:
  - `docs/docs-freshness-registry.json`
  - `docs/docs-catalog.json`
  - `docs/guides/docs-freshness-cohorts.md`
  - `scripts/docs-freshness.mjs`

## Open Questions
- None for the active fix. Future large date-boundary cohorts should continue to use explicit owner issues instead of ad hoc policy expansion.

## Approvals
- Product: self-approved from Linear issue scope
- Engineering: docs-review child stream attempted and failed before review on generated task-id/provenance bootstrap; fallback accepted through manifest-backed standalone review bounded-success after the evidence-path repair. Final scoped validation passed `spec-guard`, `docs:check`, `docs:freshness`, and `diff-budget` with the documented CO-184 override.
- Design: N/A
