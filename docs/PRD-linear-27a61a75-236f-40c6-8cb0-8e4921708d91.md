# PRD - CO STATUS / intake: prefer Rework reset truth over stale rehydrated Merging state and closed PR attachment

## Traceability
- Linear issue: `CO-220` / `27a61a75-236f-40c6-8cb0-8e4921708d91`
- Task id: `linear-27a61a75-236f-40c6-8cb0-8e4921708d91`
- Canonical spec: `tasks/specs/linear-27a61a75-236f-40c6-8cb0-8e4921708d91.md`
- Source anchor: `ctx:sha256:9325920b59c29db35d719362685755d602b8fe90d0cbf966cf4f2dae8ba7f25e#chunk:c000001`
- Source payload: `../../.runs/linear-27a61a75-236f-40c6-8cb0-8e4921708d91/cli/2026-04-17T13-05-08-700Z-e749556a/memory/source-0/source.txt`

## Summary
`CO-220` fixes a reset-truth split after a retired provider-worker attempt. The reproduced issue shape is: a prior attempt has already been retired, its old PR attachment such as `#509` is closed, the live issue has already reset to `Rework`, and no new PR is attached yet. Even so, `co-status`, intake-derived projection, and `linear issue-context` can still project the retired attempt as current truth by showing stale rehydrated `running` / `Merging` state and surfacing the closed stale PR attachment as if it were still the current attached PR.

The repair must make the current reset attempt authoritative without hiding real active work, real live `Merging` issues, or real currently attached PRs. Historical evidence for the retired attempt must remain inspectable for audit and debug, but it must stop ranking as current truth.

## User Request Translation
- Preserve the exact issue checksum around `CO STATUS`, intake-derived projection, `issue-context`, reset `Rework` truth, stale rehydrated `Merging`, and the closed stale attachment `#509`.
- Reproduce the stale-reset shape in focused tests before implementation.
- Keep this lane separate from `CO-210` child-lane hydration/progress work, `CO-214` stale merged-closeout cleanup, and `CO-219` dead-proof running-count fixes.
- Treat the fix as a current-truth selection problem across read surfaces, not as historical artifact deletion.
- Keep real live `Merging` projection and real live attached PR truth intact.

## Intent Checksum
- Exact wording and protected surfaces to preserve:
  - `CO STATUS`
  - intake-derived projection
  - `issue-context`
  - `Rework`
  - stale rehydrated `Merging`
  - stale rehydrated `running`
  - closed PR attachment
  - `#509`
  - `no attached PR yet`
  - historical evidence remains inspectable
  - `CO-210`
  - `CO-211`
  - `CO-214`
  - `CO-219`
- Nearby wrong interpretations to reject:
  - this is only a `CO STATUS` presentation bug
  - this is only an `issue-context` attachment-filter bug
  - this should delete or hide retired historical artifacts entirely
  - every old attached PR should disappear once a lane resets
  - current `Merging` truth should be downgraded whenever a closed historical PR exists

## Parity / Alignment Matrix

| Surface | Current Truth | Target Truth |
| --- | --- | --- |
| Live issue state vs rehydrated claim | The live issue can already be `Rework`, while stale rehydrated claim/proof state still projects `running` / `Merging`. | Newer live `Rework` reset truth outranks stale rehydrated `running` / `Merging` state from the retired attempt. |
| `co-status` / intake-derived projection | Operator-facing current status can still look like the retired attempt is active or merging. | Operator-facing projection reflects the reset attempt as current and does not keep the retired attempt current. |
| `issue-context` PR truth | Closed stale attachment `#509` can still appear as the current attached PR even when no new PR exists. | `issue-context` makes `no attached PR yet` current truth and keeps stale attachment `#509` historical-only. |
| Historical evidence | The retired attempt remains visible, but current-vs-historical truth is not separated clearly enough. | Historical evidence stays inspectable for audit/debug but is not presented as current truth. |
| Healthy live `Merging` issues | A broad stale-state cleanup could accidentally suppress real live `Merging` or attached PR truth. | Real live `Merging` issues and real current attached PRs still project correctly. |

## Acceptance Criteria
- Reproduce the reset shape where a prior attempt is retired, its PR is closed, the issue is reset to `Rework`, and no new PR is attached yet.
- `co-status`, intake-derived projection, and `issue-context` stop reporting the retired attempt as current `running` / `Merging` state after the reset.
- Closed stale attachments from the retired attempt, such as `#509`, are not presented as the current attached PR once the reset attempt has no new PR yet.
- Current `Rework` truth and `no attached PR yet` are reflected consistently across the relevant status/projection surfaces.
- Historical evidence for the retired attempt remains inspectable for audit/debug purposes, but is not projected as current truth.
- Real live `Merging` issues and real currently attached PRs still project correctly.

## Non-Goals
- Do not broaden `CO-210` beyond its child-lane hydration/progress scope.
- Do not regress `CO-214` merge-closeout cleanup or `CO-219` dead-proof running-count fixes.
- Do not hide real active runs or real attached PR truth.
- Do not delete historical artifacts from the retired attempt.

## Not Done If
- A reset issue can still show stale rehydrated `Merging` / attached-PR truth from the retired attempt after the live issue is already `Rework` with no new PR.
- `issue-context` still surfaces closed stale attachment `#509` as the current attached PR for the reset attempt.
- The fix only changes one surface while other status/projection surfaces keep the stale truth split.

## Validation Contract
- Focused regressions reproduce the reset `Rework` shape and prove current-truth selection across projection and `issue-context`.
- Focused regressions preserve real live `Merging` and live attached PR projection.
- Parent validation runs the repo-required gate floor plus standalone review and an explicit elegance pass before handoff.
