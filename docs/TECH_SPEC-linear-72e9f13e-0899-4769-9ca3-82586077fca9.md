---
id: 20260413-linear-72e9f13e-0899-4769-9ca3-82586077fca9
title: CO workflow: refresh current-main docs:freshness stale cohorts 1138-1163 plus release docs
relates_to: docs/PRD-linear-72e9f13e-0899-4769-9ca3-82586077fca9.md
risk: medium
owners:
  - Codex
last_review: 2026-04-13
---

## Canonical Reference
- Canonical TECH_SPEC: `tasks/specs/linear-72e9f13e-0899-4769-9ca3-82586077fca9.md`
- PRD: `docs/PRD-linear-72e9f13e-0899-4769-9ca3-82586077fca9.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-72e9f13e-0899-4769-9ca3-82586077fca9.md`
- Task checklist: `tasks/tasks-linear-72e9f13e-0899-4769-9ca3-82586077fca9.md`

## Traceability
- Linear issue: `CO-167` / `72e9f13e-0899-4769-9ca3-82586077fca9`
- Linear URL: https://linear.app/asabeko/issue/CO-167/co-workflow-refresh-current-main-docsfreshness-stale-cohorts-1138-1163
- Source issue: `CO-166` / `6af47129-33cc-45fa-9a55-e49423536752`

## Summary
- Objective: restore a truthful green `docs:freshness` baseline on the current tree by refreshing only the two stale cohorts already proven by `CO-166`.
- Scope:
  - bootstrap the `CO-167` docs-first packet, workpad source, and registry mirrors
  - run an audited `docs-review` child stream before implementation
  - refresh the March 13 `1138` through `1163` cohort and the January release-doc cohort (`release.md`, `0942`, `0943`, `0944`)
  - rerun `docs:freshness` and the required validation floor, then prepare review handoff if the tree is clean
- Constraints:
  - keep `CO-165` code/test work out of scope
  - keep freshness policy and cadence semantics unchanged
  - avoid touching docs outside the proven stale cohorts unless a rerun produces new live drift

## Technical Requirements
- Functional requirements:
  - `CO-167` packet docs, workpad source, `tasks/index.json`, `docs/TASKS.md`, and `docs/docs-freshness-registry.json` must be registered before active stale-doc edits
  - `npm run docs:freshness` must pass on the repaired tree
  - stale entries must fall from `198` to `0` without introducing missing or invalid registry rows
  - before/after artifacts must link back to `CO-166` classification evidence
- Non-functional requirements:
  - keep the repair deterministic and metadata-only
  - keep the diff bounded to the stale cohorts plus the new issue packet
  - preserve a truthful validation story for unrelated lanes
- Interfaces / contracts:
  - `scripts/docs-freshness.mjs`
  - `docs/docs-freshness-registry.json`
  - `tasks/index.json`
  - `docs/TASKS.md`
  - packaged `linear child-stream --pipeline docs-review`

## Execution Update 2026-04-13
- Current repo truth before implementation:
  - `CO-166` already proved the failure is current-main baseline drift, not branch-local fallout
  - the saved baseline report shows `198` stale docs, `0` missing registry entries, `0` missing-on-disk rows, `0` invalid entries, and `0` uncatalogued docs
  - the stale cohorts are exactly:
    - March 13 packet cohort: task ids `1138` through `1163`, `182` docs total
    - January release-doc cohort: `.agent/SOPs/release.md` plus release packets `0942`, `0943`, and `0944`, `16` docs total
  - `docs/TASKS.md` is currently at the archive threshold (`450` lines), so packet registration may require the supported archive helper before `docs:check` can return clean
- Pre-implementation approval:
  - keep the lane bounded to packet/bootstrap work, one audited docs-review child stream, the two proven stale cohorts, and the standard validation/review closeout sequence
- Post-fix state:
  - `linear child-stream --pipeline docs-review` failed closed on missing provider control-host provenance, and the direct fallback docs-review run proved the packet only needed the supported `docs:archive-tasks` trim to bring `docs/TASKS.md` back from `451` to `450` lines
  - exact stale rows in `docs/docs-freshness-registry.json` were refreshed to `2026-04-13`, matching `tasks/index.json` review dates were updated where those item mirrors still exist, and stale `last_review` frontmatter was refreshed only in `tasks/specs/*` / `docs/TECH_SPEC-*` files that already carried it
  - deterministic validation is green through `delegation-guard`, `spec-guard`, `build`, `lint`, `test`, `docs:check`, `docs:freshness`, `repo:stewardship`, and `diff-budget` (with an explicit scope override)
  - forced standalone review reached the reviewer but ended as `review_outcome=failed-boundary` with `termination_boundary=startup-anchor`; packet-local manual review fallback and explicit elegance notes are recorded instead of claiming a clean wrapper verdict

## Architecture & Data
- Architecture / design adjustments:
  - no new freshness-system behavior is introduced
  - the repair updates freshness registry dates and mirrored packet metadata consistently for the stale cohorts
  - the workpad and issue packet stay aligned to the live stale baseline rather than the older snapshot alone
- Data model changes / migrations:
  - add one new `linear-72e9f13e-0899-4769-9ca3-82586077fca9` packet entry set
  - refresh `last_review` dates in `docs/docs-freshness-registry.json` for the exact stale cohort entries
  - refresh matching `last_review` metadata in `tasks/specs/*` and `docs/TECH_SPEC-*` where those docs already carry review frontmatter
- External dependencies / integrations:
  - audited `linear child-stream --pipeline docs-review`
  - saved `docs:freshness` artifacts under `out/linear-72e9f13e-0899-4769-9ca3-82586077fca9/manual/`

## Validation Plan
- Tests / checks:
  - `MCP_RUNNER_TASK_ID=linear-72e9f13e-0899-4769-9ca3-82586077fca9 "/opt/homebrew/Cellar/node/25.2.1/bin/node" "/Users/kbediako/Code/CO/bin/codex-orchestrator.js" linear child-stream --pipeline docs-review --stream co-167-docs-review --format json`
  - `node scripts/spec-guard.mjs --dry-run`
  - `npm run build`
  - `npm run lint`
  - `npm run test`
  - `npm run docs:check`
  - `npm run docs:freshness`
  - `npm run repo:stewardship`
  - `node scripts/diff-budget.mjs`
  - `FORCE_CODEX_REVIEW=1 npm run review -- --uncommitted`
- Rollout verification:
  - preserve the `CO-166` baseline evidence as the before state
  - save new rerun artifacts under the `CO-167` task id
  - refresh the workpad after docs-first bootstrap, after stale-cohort updates, and before any review stop point
- Monitoring / alerts:
  - ongoing signal remains the repo freshness gates and saved report artifacts; no new automation is introduced

## Open Questions
- None. The initial docs-review failure mode is resolved in the Execution Update: the audited child-stream stopped on the provider provenance boundary, and the direct fallback isolated the packet-only `docs/TASKS.md` threshold fix handled via `npm run docs:archive-tasks`.

## Approvals
- Reviewer: docs-review fallback plus standalone review manual fallback/elegance closeout recorded; PR handoff pending
- Status: in progress
- Date: 2026-04-13
