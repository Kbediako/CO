# PRD - CO: Fix CLI command-surface timeout and exec quoting blocker

## Added by Bootstrap 2026-04-10

## Traceability
- Linear issue: `CO-136` / `0878c751-6534-4352-ad6b-f18f67c239bb`
- Linear URL: https://linear.app/asabeko/issue/CO-136/co-fix-cli-command-surface-timeout-and-exec-quoting-blocker

## Summary
- Problem Statement: the CO-136 follow-up packet reports that clean validation now fails only in `tests/cli-command-surface.spec.ts`, where several raw source-entrypoint fast-fail cases sit too close to the existing `15_000ms` timeout budget and the quoted single-token `exec` path may still misparse escaped quotes. The issue is back in `Rework`, so the current `origin/main` truth has to be re-established before choosing between a product fix, a test-budget truthfulness fix, or both.
- Desired Outcome: make the affected command-surface cases deterministic and green on current `origin/main`, keeping the fix narrowly scoped to the CLI startup/parsing seam or truthful command-surface test coverage without reopening `CO-96` memory-provenance work.

## User Request Translation (Context Anchor)
- User intent / needs (in your own words): treat CO-136 as a narrow blocker that was discovered while finishing CO-96, reset the stale review handoff, and finish the issue end to end in the current workspace. The solution should restore truthful command-surface validation on fresh main and avoid widening into unrelated CLI, provider-worker, or observability work.
- Success criteria / acceptance:
  - `tests/cli-command-surface.spec.ts` passes in clean repo validation
  - the `rlm` conflict / missing-goal and `exec` missing-command cases either finish comfortably within the intended timeout budget or the tests are updated to a truthful budget for raw source-entrypoint startup
  - `codex-orchestrator.ts exec 'node -e "console.log(\"x y\")"' --json compact` preserves the intended argv and emits `x y`
  - the final diff stays scoped to the command-surface timing/parsing seam and does not modify `CO-96` memory observability behavior
- Constraints / non-goals:
  - do not reopen the memory provenance/outcome schema scope from `CO-96`
  - do not broaden into general test-harness performance work beyond the failing command-surface cases
  - do not rewrite unrelated CLI subcommands or provider-worker flows that are already green

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `Fix CLI command-surface timeout and exec quoting blocker`
  - `tests/cli-command-surface.spec.ts`
  - `15_000ms`
  - `node --loader ts-node/esm`
  - `quoted single-token exec`
  - `preserves backslashes`
  - `escaped quotes`
- Protected terms / exact artifact and surface names:
  - `CO-96`
  - `CO-136`
  - `tests/cli-command-surface.spec.ts`
  - `bin/codex-orchestrator.ts`
  - `exec`
  - `rlm`
  - `npm run test`
  - `codex-orchestrator.ts exec 'node -e "console.log(\"x y\")"' --json compact`
- Nearby wrong interpretations to reject:
  - "reopen or absorb the unrelated CO-96 memory-provenance scope"
  - "treat this as general Core Lane performance work again"
  - "weaken validation broadly instead of fixing the specific timeout/parsing seam"
  - "assume the prior unmerged attempt's current-main claims are still true without fresh reproduction"

## Parity / Alignment Matrix
- Current truth:
  - the issue packet reports five failing command-surface cases
  - three fast-fail raw source-entrypoint cases reportedly complete around `13s` to `14s`, too close to the existing `15_000ms` budget
  - the quoted single-token `exec` case reportedly mis-splits escaped quotes
  - the previous unmerged attempt claimed current main already fixed runtime parsing and only the test seam remained
- Reference truth:
  - command-surface coverage should stay truthful to the raw source entrypoint and deterministic under routine validation
  - the quoted single-token `exec` path should preserve intended argv when escaped quotes are present
- Target truth / intended delta:
  - reproduce fresh-main truth for the five cited cases
  - apply the smallest scoped change in either the test seam or CLI startup/parsing seam
  - keep `tests/cli-command-surface.spec.ts` green without broadening scope
- Explicitly out-of-scope differences:
  - general review-wrapper or non-command-surface performance cleanup
  - unrelated provider-worker behavior changes
  - any new CO-96 memory or manifest contract work

## Not Done If
- `tests/cli-command-surface.spec.ts` still fails in clean validation.
- The fast-fail raw source-entrypoint cases remain close enough to `15_000ms` that they time out under routine validation without a truthful test-budget update.
- Quoted single-token `exec` commands with escaped quotes still split argv incorrectly.
- The implementation reaches outside the command-surface timing/parsing seam and required traceability artifacts.

## Goals
- Re-establish current `origin/main` truth for the five cited command-surface cases on a fresh Rework branch.
- Restore deterministic command-surface validation with the smallest correct fix.
- Preserve raw source-entrypoint coverage truth.
- Complete the required docs/review/validation workflow for the new Rework attempt.

## Non-Goals
- Reopening `CO-96` memory provenance work.
- Broad test-harness optimization beyond the failing command-surface cases.
- Rewriting unrelated CLI or provider-worker flows.
- Assuming the stale r2 handoff is valid without fresh evidence.

## Stakeholders
- Product: reviewers/operators who need the CO-136 blocker cleared without widening scope
- Engineering: CLI command-surface and test-harness maintainers
- Design: N/A

## Metrics & Guardrails
- Primary Success Metrics:
  - the historical five-case focused subset passes on current main plus the smallest fix
  - the full `tests/cli-command-surface.spec.ts` file passes
  - direct quoted-argv and fast-fail probes align with the chosen fix path
- Guardrails / Error Budgets:
  - keep the diff bounded to the command-surface seam and required docs/evidence paths
  - preserve raw source-entrypoint truth rather than masking it with purely in-process substitutes
  - keep required repo validation and review gates honest in the workpad/checklist

## User Experience
- Personas:
  - an engineer waiting on `npm run test` after CO-96-era work
  - a reviewer verifying that command-surface coverage still reflects the real entrypoint
- User Journeys:
  - direct command probes and targeted tests agree on whether the problem is runtime parsing, timeout budget truthfulness, or both
  - the final command-surface suite fails only when the real contract is broken, not because the test budget misstates normal startup cost

## Technical Considerations
- Architectural Notes:
  - the failing seam lives where `tests/cli-command-surface.spec.ts` drives the raw source entrypoint through `node --loader ts-node/esm`
  - the minimal fix may be a truthful timeout-budget/test-path adjustment, a product parsing correction, or a small combination of both depending on fresh-main reproduction
  - because the issue is in `Rework`, the prior r2 conclusions are evidence to compare against, not current truth to assume
- Dependencies / Integrations:
  - `tests/cli-command-surface.spec.ts`
  - `bin/codex-orchestrator.ts`
  - any direct parser/argv helper the current CLI entrypoint uses
  - the docs-first and provider-worker workpad workflow

## Open Questions
- On fresh `origin/main`, is the quoted single-token `exec` bug still present, or was it already fixed and only the timeout budget remains untruthful?
- If the timing seam is still real, is the smallest honest fix a `tests/cli-command-surface.spec.ts` budget update, a lighter subprocess path, or another bounded correction?

## Approvals
- Product: self-approved from the Linear issue scope
- Engineering: pending audited `linear child-stream --pipeline docs-review` for the new Rework packet
- Design: N/A
