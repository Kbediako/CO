# PRD - CO: Fix CLI command-surface timeout and exec quoting blocker

## Added by Rework Reset 2026-04-10

## Traceability
- Linear issue: `CO-136` / `0878c751-6534-4352-ad6b-f18f67c239bb`
- Linear URL: https://linear.app/asabeko/issue/CO-136/co-fix-cli-command-surface-timeout-and-exec-quoting-blocker
- Source issue: `CO-96` / `99d7fc30-93ab-43fd-94c2-68d604b4f85c`

## Summary
- Problem Statement: the runtime parser seam from `CO-136` is already fixed on current main, but the five issue cases in `tests/cli-command-surface.spec.ts` had drifted away from the raw `bin/codex-orchestrator.ts` source-entrypoint path after the hot-suite optimization that introduced `runEntrypointLikeExec`. That made the suite green, but it stopped truthfully exercising the exact timeout/parsing path called out in the issue.
- Desired Outcome: preserve the current runtime behavior, restore only the CO-136 cases to raw source-entrypoint subprocess coverage with an explicit truthful timeout budget, and keep the rest of the command-surface suite on the existing faster path.

## User Request Translation (Context Anchor)
- User intent / needs (in your own words): treat `CO-136` as a scoped follow-up to `CO-96`, keep the lane focused on the command-surface seam, and do not widen into unrelated memory-observability or provider-worker work. If the current branch already satisfies the acceptance criteria, record that truth instead of manufacturing a new code fix.
- Success criteria / acceptance:
  - the five CO-136 regressions run through the raw `bin/codex-orchestrator.ts` subprocess path with a truthful timeout budget
  - `node --loader ts-node/esm bin/codex-orchestrator.ts exec 'node -e "console.log(\"x y\")"' --json compact` preserves the intended argv and emits `x y`
  - `tests/cli-command-surface.spec.ts` passes with those source-entrypoint cases enabled
  - the final diff stays scoped to the command-surface test seam and required traceability docs
- Constraints / non-goals:
  - do not reopen `CO-96`
  - do not broaden into general CLI startup optimization work
  - do not rewrite unrelated subcommands, provider-worker flows, or memory observability surfaces

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `Fix CLI command-surface timeout and exec quoting blocker`
  - `source-entrypoint command-surface`
  - `quoted single-token exec commands`
  - `preserve source-entrypoint behavior while making the validation result deterministic`
- Protected terms / exact artifact and surface names:
  - `tests/cli-command-surface.spec.ts`
  - `bin/codex-orchestrator.ts`
  - `normalizeExecCommandTokens(...)`
  - `splitShellLikeCommand(...)`
  - `runEntrypointLikeExec`
  - `CO-96`
- Nearby wrong interpretations to reject:
  - "reopen the parser fix even though current main already preserves the runtime behavior"
  - "treat this as part of the `CO-96` memory-provenance scope"
  - "leave the CO-136 cases on the in-process helper even though the issue is about the raw source-entrypoint seam"
  - "broaden this into general CLI startup optimization or a larger hot-suite redesign"

## Parity / Alignment Matrix
- Current truth:
  - `bin/codex-orchestrator.ts` already contains `normalizeExecCommandTokens(...)` and `splitShellLikeCommand(...)` for one-token `exec` normalization
  - direct raw source-entrypoint probes for `rlm` conflict, `rlm` missing goal, and `exec` missing command all fail fast in about `6.2s` to `6.4s`, well inside a `30s` source-entrypoint budget
  - `node --loader ts-node/esm bin/codex-orchestrator.ts exec 'node -e "console.log(\"x y\")"' --json compact` succeeds, preserves argv as `["node","-e","console.log(\"x y\")"]`, and emits `x y`
- Reference truth:
  - the issue description and prior review branch reflected a failing snapshot
  - `CO-114 shrink hot subprocess suites` changed `runCli(...)` in `tests/cli-command-surface.spec.ts` to use `runEntrypointLikeExec`, which made the file faster but stopped directly exercising the raw source-entrypoint path for these five issue cases
- Target truth / intended delta:
  - only the five CO-136 regressions move back onto a raw source-entrypoint subprocess helper with an explicit `30s` timeout
  - the broader hot-suite optimization remains intact
  - docs, workpad, and checklist mirrors explain that the runtime fix already existed and this lane restores truthful coverage for the issue seam
- Explicitly out-of-scope differences:
  - new CLI runtime or parser changes
  - general startup-speed work
  - `CO-96` behavior changes

## Not Done If
- `tests/cli-command-surface.spec.ts` stops passing during this fresh validation pass.
- the quoted single-token `exec` probe regresses from the current successful argv semantics.
- the packet misstates the live branch truth.
- the lane broadens into unrelated CLI performance or suite-architecture work.

## Goals
- Preserve the current quoted single-token `exec` argv behavior already present on `origin/main`.
- Restore truthful source-entrypoint coverage for the five CO-136 regressions.
- Keep the broader `tests/cli-command-surface.spec.ts` hot-suite optimization intact.
- Keep the final diff as small as possible.

## Non-Goals
- Reopening `CO-96` memory provenance or outcome-schema work.
- Re-implementing parser logic that current main already satisfies.
- Widening into unrelated CLI, provider-worker, or review-wrapper behavior.

## Stakeholders
- Product: operators who need the `CO-96` follow-up blocker cleared truthfully
- Engineering: CLI command-surface maintainers and validation owners
- Design: N/A

## Metrics & Guardrails
- Primary Success Metrics:
  - direct raw source-entrypoint probes remain green inside the explicit `30s` budget
  - the historical five-case subset passes on the raw source-entrypoint path
  - the full command-surface file remains green with the restored source-entrypoint coverage
- Guardrails / Error Budgets:
  - preserve the existing runtime behavior
  - limit code edits to `tests/cli-command-surface.spec.ts`
  - do not reopen unrelated hot-suite or CLI performance work

## User Experience
- Personas:
  - maintainer rerunning clean validation after `CO-96`
  - operator using `codex-orchestrator exec` with a quoted single-token command payload
- User Journeys:
  - the direct `node -e` probe keeps working on current main
  - repo validation again exercises the same raw source-entrypoint path that the issue used for reproduction
  - reviewers can see why this lane changes only the targeted test seam, not the runtime parser

## Technical Considerations
- Architectural Notes:
  - `bin/codex-orchestrator.ts` remains the canonical one-token `exec` normalization seam
  - `tests/helpers/inProcessEntrypoint.ts` stays in place for the broader hot-suite optimization
  - this lane adds a dedicated raw source-entrypoint subprocess helper only for the five CO-136 regressions
- Dependencies / Integrations:
  - `bin/codex-orchestrator.ts`
  - `tests/cli-command-surface.spec.ts`
  - `tests/helpers/inProcessEntrypoint.ts`

## Approvals
- Product: self-approved from the Linear issue scope
- Engineering: pending docs-review child stream for the fresh packet
- Design: N/A
