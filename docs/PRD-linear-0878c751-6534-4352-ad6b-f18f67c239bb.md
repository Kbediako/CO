# PRD - CO: Fix CLI command-surface timeout and exec quoting blocker

## Added by Bootstrap 2026-04-10

## Traceability
- Linear issue: `CO-136` / `0878c751-6534-4352-ad6b-f18f67c239bb`
- Linear URL: https://linear.app/asabeko/issue/CO-136/co-fix-cli-command-surface-timeout-and-exec-quoting-blocker
- Source issue: `CO-96` / `99d7fc30-93ab-43fd-94c2-68d604b4f85c`

## Summary
- Problem Statement: clean validation for `CO-96` now isolates a single unrelated CLI blocker in `tests/cli-command-surface.spec.ts`. Several source-entrypoint `rlm` / `exec` failure cases are functionally correct but take close to the current `15_000ms` Vitest budget, and the issue description attributed the remaining failure to quoted single-token `exec` parsing.
- Desired Outcome: make the affected command-surface cases deterministic and green without widening into unrelated CLI or provider-worker work, by preserving the quoted single-token `exec` behavior already present in the current workspace and using a truthful timeout budget for the specific source-entrypoint startup cases that currently pay full CLI bootstrap cost.

## User Request Translation (Context Anchor)
- User intent / needs (in your own words): treat `CO-136` as a scoped follow-up to `CO-96`, not as a continuation of the memory-observability work. Fix the real `exec` quoting bug, keep the change tightly focused on the command-surface seam, and avoid pretending that the current source-entrypoint startup cost fits the old `15_000ms` test budget if it does not.
- Success criteria / acceptance:
  - `tests/cli-command-surface.spec.ts` is green in clean validation
  - the `exec` command preserves `node -e "console.log(\"x y\")"` as `['node', '-e', 'console.log("x y")']` and emits `x y`
  - the slow `rlm` / `exec` source-entrypoint failure tests either run comfortably within the intended budget or use a truthful larger budget that matches current CLI bootstrap behavior
  - the diff stays scoped to CLI command-surface parsing and targeted tests
- Constraints / non-goals:
  - do not reopen `CO-96`
  - do not broaden into general CLI startup optimization or unrelated test-harness performance work
  - do not rewrite unrelated subcommands, provider-worker flows, or memory observability paths

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `Fix CLI command-surface timeout and exec quoting blocker`
  - `source-entrypoint command-surface`
  - `quoted single-token exec commands`
  - `preserve source-entrypoint behavior while making the validation result deterministic`
- Protected terms / exact artifact and surface names:
  - `tests/cli-command-surface.spec.ts`
  - `bin/codex-orchestrator.ts`
  - `orchestrator/src/cli/execCliShell.ts`
  - `orchestrator/src/cli/services/execRuntime.ts`
  - `runEntrypointLikeExec`
  - `CO-96`
- Nearby wrong interpretations to reject:
  - "fix this by folding unrelated CLI cleanup into `CO-96`"
  - "blame the memory provenance changes because the failure appeared during the same validation run"
  - "solve the timeout by weakening command-surface validation broadly"
  - "rewrite command execution semantics beyond the quoted single-token `exec` seam"

## Parity / Alignment Matrix
- Current truth:
  - `tests/cli-command-surface.spec.ts` exercises the source entrypoint in-process via `runEntrypointLikeExec`, so the per-test timeout is a Vitest wall-clock budget rather than a subprocess timeout
  - the `rlm` missing-goal and conflicting-collab cases, plus the `exec` missing-command case, are already functionally correct but run close to the `15_000ms` timeout because the source entrypoint loads the full CLI module tree
  - the current `bin/codex-orchestrator.ts` already normalizes quoted single-token `exec` payloads before they reach the exec runtime
- Reference truth:
  - direct source-entrypoint probes show the failing `rlm` / `exec` cases already produce the correct errors
  - direct source-entrypoint probes also show quoted backslashes can survive while escaped inner quotes still break
- Target truth / intended delta:
  - the quoted single-token `exec` path continues to use deterministic argv semantics that preserve escaped inner quotes and backslashes
  - the affected source-entrypoint tests use a truthful timeout budget that matches current CLI bootstrap behavior
- Explicitly out-of-scope differences:
  - general CLI cold-start optimization
  - provider-worker, review-wrapper, or memory-observability behavior changes

## Not Done If
- `tests/cli-command-surface.spec.ts` still fails in clean validation.
- fast-fail `rlm` / `exec` command-surface cases still time out under the test budget used for this lane.
- quoted single-token `exec` commands with escaped quotes still split the argv incorrectly.
- the fix broadens into unrelated CLI or `CO-96` behavior.

## Goals
- Preserve the existing quoted single-token `exec` behavior that already converts those commands into deterministic argv before execution.
- Keep backslash preservation intact for Windows-style path arguments passed through the same path.
- Make the source-entrypoint test timeout budget truthful for the specific `rlm` / `exec` cases that are already functionally correct.
- Keep the diff narrowly scoped to the command-surface seam and targeted tests.

## Non-Goals
- Reopening `CO-96` memory provenance or outcome-schema work.
- Generic CLI cold-start or module-graph performance work outside the failing cases.
- Rewriting unrelated CLI subcommands or provider-worker paths.

## Stakeholders
- Product: operators who need `CO-96` validation to clear without absorbing unrelated CLI drift
- Engineering: CLI command-surface maintainers and validation owners
- Design: N/A

## Metrics & Guardrails
- Primary Success Metrics:
  - `tests/cli-command-surface.spec.ts` passes
  - the escaped-quote `exec` case emits the expected argv and output
  - the affected startup-bound tests stop failing on timeout
- Guardrails / Error Budgets:
  - keep the fix additive and local to command parsing and command-surface tests
  - preserve existing `exec` behavior for already-green multi-token invocations
  - do not widen the timeout budget for unrelated tests

## User Experience
- Personas:
  - maintainer running clean repo validation after `CO-96`
  - operator using `codex-orchestrator exec` with a one-token quoted command payload
- User Journeys:
  - running `codex-orchestrator.ts exec 'node -e "console.log(\"x y\")"' --json compact` produces `x y` and reports the correct argv
  - running the command-surface suite no longer times out on functionally-correct source-entrypoint failure cases
  - reviewers can see that the timeout adjustment is truthful and limited to the affected boot-heavy cases

## Technical Considerations
- Architectural Notes:
  - keep the implementation delta limited to timeout truthfulness in `tests/cli-command-surface.spec.ts`; do not add a second single-token parser path when `bin/codex-orchestrator.ts` already owns that normalization
  - keep timeout truthfulness in `tests/cli-command-surface.spec.ts`, where the current source-entrypoint timing contract is actually enforced
- Dependencies / Integrations:
  - `bin/codex-orchestrator.ts`
  - `tests/cli-command-surface.spec.ts`

## Open Questions
- Did the issue description capture an older pre-fix snapshot of the quoted single-token `exec` path, given that the current workspace already contains the bin-level normalizer?
- Should the truthful timeout budget be documented as a per-case source-entrypoint boot budget to make future regressions easier to audit?

## Approvals
- Product: self-approved from the Linear issue scope
- Engineering: pending docs-review child stream for the new packet
- Design: N/A
