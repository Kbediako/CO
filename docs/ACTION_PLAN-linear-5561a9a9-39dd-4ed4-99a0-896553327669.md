# ACTION_PLAN - CO: Add a Codex 0.121 plugin marketplace distribution path for Codex Orchestrator

## Added by Bootstrap (refresh as needed)

## Summary
- Goal: ship the smallest truthful Codex marketplace distribution path for CO while keeping npm install unchanged and auditable.
- Scope: docs-first packet, repo-root marketplace metadata plus packaged plugin-directory metadata, tarball inclusion, marketplace-capability-gated temp-home smoke, downstream install docs, validation, review, and handoff.
- Assumptions: Codex CLI marketplace support is available in the local worker environment; the non-interactive smoke may need to prove discovery/registration via config/cache evidence rather than a dedicated `plugin install` CLI command if the CLI surface does not expose one.

## Issue Readiness Gate
- Intent checksum / protected terms carried forward: preserve `codex marketplace add`, `.agents/plugins/marketplace.json`, `plugins/codex-orchestrator/.codex-plugin/plugin.json`, `plugins/codex-orchestrator/.mcp.json`, `plugins/codex-orchestrator/launcher.mjs`, `codex-orchestrator mcp serve`, additive npm support, no secrets/hooks/private artifacts/workspace-only paths, and no authority expansion.
- Not done if: the tarball omits the descriptors, marketplace smoke lacks proof, or docs do not explain install plus removal/rollback clearly.
- Pre-implementation issue-quality review: current repo truth, official Codex plugin docs, and local marketplace help were checked first; the attempted same-turn docs child lane failed closed on provider provenance, so the parent lane owns the docs packet directly for this issue.

## Milestones & Sequencing
1) Finish the docs-first packet and register it in the task mirrors plus `docs/TASKS.md` using the supported archive path if needed to stay within the line budget.
2) Add the minimal repo-root marketplace source, packaged plugin descriptor, launcher, and package inclusion changes that reuse `codex-orchestrator mcp serve`.
3) Add the temp-home marketplace smoke and update downstream docs for npm vs marketplace install plus rollback/removal.
4) Run the required validation floor, standalone review, elegance review, then open/attach the PR and hold review handoff until the ready-review drain is clean.

## Dependencies
- Official Codex plugin/marketplace docs.
- Local `codex marketplace` help surfaces.
- Existing `codex-orchestrator mcp serve` CLI contract.
- `npm pack` / `pack:audit` / `pack:smoke` downstream packaging checks.

## Validation
- Checks / tests:
  - focused temp-home marketplace smoke
  - `node scripts/delegation-guard.mjs`
  - `node scripts/spec-guard.mjs --dry-run`
  - `npm run build`
  - `npm run lint`
  - `npm run test`
  - `npm run docs:check`
  - `npm run docs:freshness`
  - `npm run repo:stewardship`
  - `node scripts/diff-budget.mjs`
  - `FORCE_CODEX_REVIEW=1 npm run review -- --uncommitted`
  - `npm run pack:smoke`
- Rollback plan:
  - remove the new marketplace root and packaged plugin manifests together
  - restore the prior `package.json` `files` allowlist
  - revert downstream docs to npm-only instructions

## Risks & Mitigations
- Risk: Codex CLI exposes marketplace add but not a clean non-interactive plugin install/enable CLI.
  - Mitigation: use current CLI/documentation evidence to define the smoke boundary honestly and capture config/cache proof if that is the real non-interactive contract.
- Risk: descriptor paths accidentally depend on the author workspace.
  - Mitigation: keep all shipped paths repo-relative and reuse the existing CLI command contract.
- Risk: `docs/TASKS.md` line budget blocks packet registration.
  - Mitigation: use the supported `docs:archive-tasks` path before adding the new snapshot line.

## Approvals
- Reviewer: parent provider worker.
- Date: 2026-04-17
