# PRD - CO: rebaseline marketplace-dependent downstream smoke beyond Codex CLI 0.121.0

## Traceability
- Linear issue: `CO-275` / `09c996ec-47b3-4f55-928d-38e0b17c11df`
- Task id: `linear-09c996ec-47b3-4f55-928d-38e0b17c11df`
- Source issue: `CO-269`
- Lineage to preserve: `CO-196`, `CO-217`, and `CO-269`
- Initial docs child-lane manifest: `.runs/linear-09c996ec-47b3-4f55-928d-38e0b17c11df-docs-packet/cli/2026-04-21T04-32-03-882Z-56f78783/manifest.json`
- Prior CO-275 evidence artifact: `out/linear-09c996ec-47b3-4f55-928d-38e0b17c11df/manual/codex-marketplace-policy/marketplace-capability-compare.log`

## Summary
- Problem Statement: `CO-269` proved that `Codex CLI 0.122.0` / `@openai/codex@0.122.0` no longer satisfied the old `codex marketplace add` contract that `pack:smoke` depended on, while `Codex CLI 0.121.0` / `@openai/codex@0.121.0` still exposed that command.
- Current mainline truth: `CO-268` has since completed the replacement-contract path by proving the stable `0.122.0` command surface is `codex plugin marketplace add` / `codex plugin marketplace remove`, updating `scripts/pack-smoke.mjs`, `tests/pack-smoke.spec.ts`, `.github/workflows/core-lane.yml`, `.github/workflows/release.yml`, `.github/workflows/pack-smoke-backstop.yml`, and docs accordingly.
- Desired Outcome: close `CO-275` with one recorded post-`0.121.0` policy decision: CO replaces the old `codex marketplace add` steady-state contract with the evidence-backed `codex plugin marketplace add` contract on `@openai/codex@0.122.0`, without promoting the active CO compatibility target or weakening marketplace smoke coverage.

## Intent Checksum
- Preserve exact issue terms: `Codex CLI 0.122.0`, `Codex CLI 0.121.0`, `codex marketplace add`, `codex plugin marketplace add`, `pack:smoke`, `scripts/pack-smoke.mjs`, `tests/pack-smoke.spec.ts`, `.github/workflows/core-lane.yml`, `.github/workflows/release.yml`, `.github/workflows/pack-smoke-backstop.yml`, `@openai/codex@0.121.0`, `@openai/codex@0.122.0`, `CO-196`, `CO-217`, and `CO-269`.
- Reject treating this as a broad `0.122.0` promotion lane, a cloud-canary policy lane, a provider-worker runtime migration, or permission to make marketplace smoke optional.

## Parity / Alignment Matrix

| Surface | Current truth | Reference truth | Target truth |
| --- | --- | --- | --- |
| Command surface | `codex marketplace add` was the old `0.121.0` proof path; `0.122.0` rejects that old shape. | `CO-268` proved `codex plugin marketplace add --help` and `codex plugin marketplace remove --help` on `Codex CLI 0.122.0`. | `pack:smoke` uses the `codex plugin marketplace add` contract for current release-facing marketplace coverage. |
| Workflow pins | `CO-269` held release-facing smoke on `@openai/codex@0.121.0` because the script still used the old command. | Current `origin/main` after `CO-268` installs `@openai/codex@0.122.0` before release-facing `npm run pack:smoke`. | CO-275 records that post-`0.121.0` replacement as the long-term policy until a newer audited candidate changes the command surface again. |
| `pack:smoke` contract | Marketplace coverage remains mandatory by default, with explicit local non-coverage skip evidence only. | `CO-217` made missing marketplace coverage fail closed unless an explicit skip reason is provided. | Keep the fail-closed coverage posture while changing the command path from old `codex marketplace add` to `codex plugin marketplace add`. |
| Lineage | `CO-196` shipped the packaged marketplace architecture; `CO-217` hardened mandatory marketplace smoke; `CO-269` created this follow-up. | `CO-268` preserves the CO-196 architecture and posture lineage. | CO-275 closeout keeps all four issue links auditable and does not reopen packaging architecture. |

## Acceptance Criteria
1. Capture exact `0.121.0` versus `0.122.0` marketplace evidence with reproducible commands and task-scoped artifacts.
2. Record one decision for post-`0.121.0` marketplace-dependent downstream smoke.
3. If the requirement changes, keep `scripts/pack-smoke.mjs`, `tests/pack-smoke.spec.ts`, workflow pins, and docs aligned.
4. Preserve explicit linkage to `CO-196`, `CO-217`, and `CO-269`.

## Decision
CO should revise the marketplace-dependent `pack:smoke` contract for newer Codex CLIs. The accepted replacement contract is the `CO-268` mainline contract: `@openai/codex@0.122.0` plus `codex plugin marketplace add`, with `codex plugin marketplace remove` documented for rollback/removal. This supersedes the temporary `@openai/codex@0.121.0` release-facing smoke pin from `CO-269` while preserving the old evidence as historical lineage.

## Not Done If
- `scripts/pack-smoke.mjs`, `tests/pack-smoke.spec.ts`, and the protected workflows disagree about the marketplace command or Codex CLI pin.
- Marketplace coverage can silently skip without `PACK_SMOKE_ALLOW_MARKETPLACE_SKIP=1` and `PACK_SMOKE_MARKETPLACE_SKIP_REASON`.
- The final record drops `CO-196`, `CO-217`, or `CO-269`.
- The issue is framed as active-target promotion from `0.118.0` instead of a marketplace smoke command-surface rebaseline.

## Validation Contract
- Verify current mainline files contain the `codex plugin marketplace add` contract.
- Run focused pack-smoke workflow tests.
- Run required docs and repo gates before PR/review handoff when this branch carries tracked changes.
