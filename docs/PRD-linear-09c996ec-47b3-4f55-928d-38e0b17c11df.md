# PRD - CO: align marketplace pack:smoke with Codex CLI 0.122.0

## Added by Docs Child Lane 2026-04-21

## Traceability
- Linear issue: `CO-275` / `09c996ec-47b3-4f55-928d-38e0b17c11df`
- Linear URL: `https://linear.app/asabeko/issue/CO-275`
- Task id: `linear-09c996ec-47b3-4f55-928d-38e0b17c11df`
- Canonical spec: `tasks/specs/linear-09c996ec-47b3-4f55-928d-38e0b17c11df.md`
- Shared source 0 anchor: `ctx:sha256:0e9cc806c03afdcd2ad2aeccf80f57549868a86ca946747d78aee3d131e0c83f#chunk:c000001`
- Source object id: `sha256:0e9cc806c03afdcd2ad2aeccf80f57549868a86ca946747d78aee3d131e0c83f`
- Context dir: `.runs/linear-09c996ec-47b3-4f55-928d-38e0b17c11df-docs-packet/cli/2026-04-21T04-32-03-882Z-56f78783/memory/source-0`
- Source payload: `.runs/linear-09c996ec-47b3-4f55-928d-38e0b17c11df-docs-packet/cli/2026-04-21T04-32-03-882Z-56f78783/memory/source-0/source.txt`
- Origin manifest: `.runs/linear-09c996ec-47b3-4f55-928d-38e0b17c11df-docs-packet/cli/2026-04-21T04-32-03-882Z-56f78783/manifest.json`
- Source payload note: the declared `source-0/source.txt` is absent in this child checkout, so this packet preserves the bounded handoff wording plus current repo evidence for `pack:smoke`, workflow pins, and marketplace smoke posture.

## Summary
- Problem Statement: CO's release-facing marketplace smoke posture is still centered on the `Codex CLI 0.121.0` / `@openai/codex@0.121.0` proof line from `CO-196` and `CO-217`, while `CO-275` needs a docs-first packet for the `Codex CLI 0.122.0` / `@openai/codex@0.122.0` alignment lane before parent implementation edits touch `scripts/pack-smoke.mjs`, `tests/pack-smoke.spec.ts`, or release workflows.
- Desired Outcome: define the minimum parent-owned implementation and validation path that aligns marketplace `pack:smoke` proof with `Codex CLI 0.122.0` while preserving the `Codex CLI 0.121.0` compatibility lineage, `codex marketplace add` coverage, `CO-196` distribution proof, `CO-217` mandatory marketplace-smoke hardening, and `CO-269` as the 0.122.0 lineage anchor.

## User Request Translation (Context Anchor)
- User intent / needs (in your own words): create the docs-first packet and registry mirrors for `CO-275` only. Parent should later decide and implement the exact `@openai/codex@0.122.0` marketplace smoke alignment in `pack:smoke`, tests, and workflows without weakening existing `@openai/codex@0.121.0` proof or silently dropping marketplace coverage.
- Success criteria / acceptance:
  - docs-first packet and mirrors exist for `CO-275` before source, test, or workflow edits
  - parent implementation accounts for both `Codex CLI 0.122.0` and `Codex CLI 0.121.0` explicitly rather than treating the versions as interchangeable
  - `codex marketplace add` coverage remains mandatory inside `pack:smoke` unless an explicit documented non-coverage skip is used
  - `tests/pack-smoke.spec.ts` and the workflows `.github/workflows/core-lane.yml`, `.github/workflows/release.yml`, and `.github/workflows/pack-smoke-backstop.yml` no longer encode stale release-facing 0.121-only proof once the target is 0.122.0
  - `CO-196`, `CO-217`, and `CO-269` remain traceable as lineage, not replaced or blurred
- Constraints / non-goals:
  - child lane owns only docs/checklist/registry files
  - no edits to `scripts/pack-smoke.mjs`, `tests/pack-smoke.spec.ts`, or `.github/workflows/**` in this child lane
  - no Linear mutations from this child lane
  - no full repo validation from this child lane

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `Codex CLI 0.122.0`
  - `Codex CLI 0.121.0`
  - `codex marketplace add`
  - `pack:smoke`
  - `scripts/pack-smoke.mjs`
  - `tests/pack-smoke.spec.ts`
  - `.github/workflows/core-lane.yml`
  - `.github/workflows/release.yml`
  - `.github/workflows/pack-smoke-backstop.yml`
  - `@openai/codex@0.121.0`
  - `@openai/codex@0.122.0`
  - `CO-196`
  - `CO-217`
  - `CO-269`
- Protected terms / exact artifact and surface names:
  - `npm run pack:smoke`
  - `codex marketplace add`
  - `scripts/pack-smoke.mjs`
  - `tests/pack-smoke.spec.ts`
  - `.github/workflows/core-lane.yml`
  - `.github/workflows/release.yml`
  - `.github/workflows/pack-smoke-backstop.yml`
  - `@openai/codex@0.121.0`
  - `@openai/codex@0.122.0`
- Nearby wrong interpretations to reject:
  - treating this as a generic Codex CLI adoption or promotion lane
  - replacing the `CO-196` marketplace/plugin distribution proof
  - weakening the `CO-217` mandatory marketplace-smoke contract
  - editing workflows or tests from the docs child lane
  - accepting generic pack-smoke success when `codex marketplace add` coverage did not run
  - removing `Codex CLI 0.121.0` lineage instead of preserving it as the previous marketplace-capable baseline

## Parity / Alignment Matrix

| Surface | Current truth | Reference truth | Target truth |
| --- | --- | --- | --- |
| Marketplace smoke baseline | `tests/pack-smoke.spec.ts` and relevant workflows currently install `@openai/codex@0.121.0` before `npm run pack:smoke`. | `CO-196` added the marketplace/plugin distribution path and `CO-217` hardened `pack:smoke` so marketplace install/register coverage is mandatory by default. | Parent updates the release-facing proof line for `Codex CLI 0.122.0` / `@openai/codex@0.122.0` while preserving the 0.121.0 lineage as prior compatibility evidence. |
| `codex marketplace add` coverage | `scripts/pack-smoke.mjs` checks marketplace support and emits explicit skip/fail evidence. | Marketplace proof is not generic pack-smoke proof; it must prove `codex marketplace add` behavior or record a non-coverage skip. | `pack:smoke` keeps `codex marketplace add` coverage mandatory for marketplace-capable Codex versions, including 0.122.0. |
| Workflow posture | `.github/workflows/core-lane.yml`, `.github/workflows/release.yml`, and `.github/workflows/pack-smoke-backstop.yml` install `@openai/codex@0.121.0` before `npm run pack:smoke`. | `CO-217` requires relevant workflows to provision marketplace-capable Codex before blocking `pack:smoke`. | Workflows provision the parent-approved `@openai/codex@0.122.0` proof version, or explicitly document a deliberate dual-version/fallback posture. |
| Test posture | `tests/pack-smoke.spec.ts` currently names the dedicated `@openai/codex@0.121.0` install command and assertion text. | Tests must pin workflow proof so marketplace smoke cannot silently disappear. | Tests assert the intended 0.122.0 workflow proof and keep 0.121.0 references only where they are deliberate compatibility/fallback cases. |
| Lineage issues | `CO-196` and `CO-217` are present in repo docs; `CO-269` is referenced by this issue handoff as the 0.122.0 lineage anchor. | Existing issue lineage should remain auditable across the package, tests, workflows, and task mirrors. | Packet and parent implementation preserve `CO-196`, `CO-217`, and `CO-269` traceability in docs/checklists and implementation evidence. |

## Acceptance Criteria
1. The `CO-275` docs-first packet and registry mirrors are created before implementation edits.
2. The packet preserves all protected terms for `Codex CLI 0.122.0`, `Codex CLI 0.121.0`, `codex marketplace add`, `pack:smoke`, implementation surfaces, workflow surfaces, package pins, and lineage issues.
3. Parent implementation updates or explicitly dual-tracks release-facing marketplace smoke proof from `@openai/codex@0.121.0` to `@openai/codex@0.122.0`.
4. `codex marketplace add` coverage stays mandatory in `pack:smoke`; unsupported or skipped coverage remains visible non-coverage evidence.
5. `tests/pack-smoke.spec.ts` protects the workflow install order and version intent for the 0.122.0 lane.
6. `.github/workflows/core-lane.yml`, `.github/workflows/release.yml`, and `.github/workflows/pack-smoke-backstop.yml` cannot silently run release-facing `pack:smoke` without the intended marketplace-capable Codex install.
7. `CO-196`, `CO-217`, and `CO-269` stay explicitly traceable as lineage and boundaries.

## Non-Goals
- No implementation, test, package, or workflow edits in this docs child lane.
- No generic Codex CLI adoption decision beyond this marketplace smoke alignment packet.
- No broad release pipeline redesign.
- No weakening of marketplace smoke skip/fail behavior.
- No replacement of `CO-196` local proof or `CO-217` mandatory-marketplace-smoke hardening.
- No Linear state mutation or workpad update from this child lane.

## Not Done If
- The packet omits `Codex CLI 0.122.0`, `Codex CLI 0.121.0`, `@openai/codex@0.122.0`, or `@openai/codex@0.121.0`.
- The packet fails to name `codex marketplace add`, `pack:smoke`, `scripts/pack-smoke.mjs`, `tests/pack-smoke.spec.ts`, and all three protected workflow files.
- Parent implementation can leave workflows/tests proving only `@openai/codex@0.121.0` while claiming 0.122.0 readiness.
- `pack:smoke` can pass as marketplace proof without `codex marketplace add` coverage or explicit non-coverage evidence.
- `CO-196`, `CO-217`, or `CO-269` lineage is dropped or treated as interchangeable.

## Stakeholders
- Product: CO release operators who need marketplace smoke evidence to match the active Codex CLI posture.
- Engineering: pack-smoke, workflow, release, and downstream marketplace support maintainers.
- Design: N/A.

## Metrics & Guardrails
- Primary Success Metrics:
  - packet and registry mirrors exist for `CO-275`
  - parent `pack:smoke` output proves marketplace coverage under the intended Codex CLI proof version
  - workflow posture tests cover the intended `@openai/codex@0.122.0` install before `npm run pack:smoke`
  - 0.121.0 references are either preserved as prior lineage or intentionally dual-tracked
- Guardrails / Error Budgets:
  - keep marketplace non-coverage explicit
  - avoid widening into release orchestration or runtime policy
  - keep CO-196/CO-217 proof stronger, not weaker

## User Experience
- Release operators can see whether `pack:smoke` marketplace coverage ran under `Codex CLI 0.122.0`.
- Reviewers can distinguish active 0.122.0 proof from historical 0.121.0 lineage.
- Downstream users retain the `codex marketplace add` path and the mandatory marketplace smoke guarantees from `CO-196` and `CO-217`.

## Technical Considerations
- Architectural Notes:
  - parent should keep the change bounded to marketplace smoke prerequisites, workflow version pins, and the workflow-posture assertions in `tests/pack-smoke.spec.ts`
  - parent should update assertion text and constants so stale `Codex 0.121.0` wording does not masquerade as 0.122.0 proof
  - parent should preserve explicit skip reason behavior in `scripts/pack-smoke.mjs`
- Dependencies / Integrations:
  - `scripts/pack-smoke.mjs`
  - `tests/pack-smoke.spec.ts`
  - `.github/workflows/core-lane.yml`
  - `.github/workflows/release.yml`
  - `.github/workflows/pack-smoke-backstop.yml`
  - `@openai/codex@0.121.0`
  - `@openai/codex@0.122.0`
  - `CO-196`, `CO-217`, and `CO-269`

## Open Questions
- Should parent use a single `@openai/codex@0.122.0` proof version in all release-facing workflows, or keep a deliberate dual-version local fallback test for `@openai/codex@0.121.0`?
- Does `CO-269` require any additional non-marketplace `Codex CLI 0.122.0` evidence before parent moves the workflow proof pins?
- Should `tests/pack-smoke.spec.ts` encode 0.122.0 as the new constant directly, or make the expected marketplace-capable Codex version explicit in one shared test helper?

## Validation Contract
- Child lane:
  - parse `tasks/index.json` and `docs/docs-freshness-registry.json`
  - grep protected terms across the `CO-275` packet and mirrors
  - run `git diff --check` over declared docs scope only
- Parent lane:
  - focused `npx vitest run tests/pack-smoke.spec.ts`
  - `npm run pack:smoke`
  - workflow posture review for `.github/workflows/core-lane.yml`, `.github/workflows/release.yml`, and `.github/workflows/pack-smoke-backstop.yml`
  - parent-selected docs/spec gates and review before PR handoff

## Approvals
- Product: Linear issue `CO-275`
- Engineering: pending parent docs-review / implementation
- Design: N/A
