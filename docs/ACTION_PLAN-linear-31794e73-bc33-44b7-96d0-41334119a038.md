# ACTION_PLAN - Control host: reclaim Ready issues suppressed by plain released/not_active claims that retain completed blockers

## Added by Bootstrap 2026-04-17

## Summary
- Goal: close `CO-212` by reclaiming a live `Ready` / `unstarted` issue when the only retained blocker evidence on a plain `released` / `provider_issue_released:not_active` claim is already completed.
- Scope: this child lane creates the docs-first packet and registry mirrors only; the parent lane owns implementation, focused tests, validation, Linear/workpad, PR lifecycle, and final review.
- Assumptions:
  - the cached claim shape can retain `issue_state=Blocked`, stale `issue_updated_at`, and `issue_blocked_by=[CO-207 Done/completed]`
  - the live tracked issue can already be `Ready` / `unstarted` with newer `updated_at=2026-04-17T03:33:27.936Z` and no non-terminal blockers
  - completed blockers must remain preserved for audit after reclaim truth is corrected

## Issue Readiness Gate
- Intent checksum / protected terms carried forward:
  - preserve `providerIssueHandoff.ts`, `providerLinearWorkflowStates.ts`, `provider-intake-state.json`, `co-status --format json`, `state=released`, `reason=provider_issue_released:not_active`, cached `issue_state=Blocked`, stale `issue_updated_at`, `issue_blocked_by`, `issue_blocked_by=[CO-207 Done/completed]`, `Ready`, `unstarted`, and `updated_at=2026-04-17T03:33:27.936Z`
- Not done if:
  - completed-only blocker history still suppresses a live `Ready` / `unstarted` issue
  - reclaim requires manual worker start
  - completed blockers are deleted instead of preserved for audit
  - provider handoff and CO STATUS/runtime coverage do not exist for the issue shape
- Pre-implementation issue-quality review:
  - 2026-04-17: child-lane review confirms the issue is a narrow reclaim bug at the completed-blocker boundary, not a broad blocker-policy rewrite, not a status-only rendering bug, and not a cleanup request. Correctness depends on exact protected terms and exact seam ownership, so the micro-task path is ineligible.
  - 2026-04-19: parent review keeps the reopened work scoped to the CO-196 completed-blocker acceptance criteria. The same-issue docs child lane completed but was rejected because it drifted into unsupported CO-223/CO-238 wording.

## Milestones & Sequencing
1. Child lane drafts the `CO-212` PRD, TECH_SPEC, ACTION_PLAN, task spec, task checklist, `.agent` mirror, `tasks/index.json`, `docs/TASKS.md`, and docs-freshness registry entries inside the declared file scope.
2. Parent accepts or adjusts this packet and continues owning the authoritative issue workspace, Linear/workpad state, and implementation planning.
3. Parent inspects `providerIssueHandoff.ts` and `providerLinearWorkflowStates.ts` against the cached completed-blocker claim shape and current live issue truth.
4. Parent adds focused regression coverage for the cached CO-196-like claim, the retained terminal run with no `issue_updated_at`, and `co-status --format json` runtime/status projection.
5. Parent implements the minimal reclaim fix so deferred fresh discovery preserves eligible `provider_issue_released:not_active` rows, fresh Ready metadata persists, and reclaim/launch happens through the normal control-host path without manual worker start.
6. Parent validates with focused tests and the normal parent-owned review/validation flow before PR handoff.

## Dependencies
- `providerIssueHandoff.ts`
- `providerLinearWorkflowStates.ts`
- `provider-intake-state.json`
- `co-status --format json`
- cached `issue_state=Blocked`
- `issue_blocked_by=[CO-207 Done/completed]`
- live `Ready` / `unstarted` issue truth with `updated_at=2026-04-17T03:33:27.936Z`

## Validation
- Checks / tests:
  - child lane: scoped docs/JSON and diff sanity only
  - parent lane: focused provider handoff regressions for the cached CO-196-like claim and retained terminal run variant
  - parent lane: focused `co-status --format json` runtime/status projection coverage
  - parent lane: `node scripts/spec-guard.mjs --dry-run`
  - parent lane: manifest-backed docs-review or implementation gate selected by parent
- Rollback plan:
  - revert the reclaim/classification change if it regresses non-terminal blocker suppression or drops audit history
  - preserve retained `provider-intake-state.json` history and completed blocker evidence as rollback baseline
  - never roll back by deleting blocker history or manually starting a worker

## Risks & Mitigations
- Risk: completed blockers are treated the same as current non-terminal blockers during reclaim.
  - Mitigation: use the exact live issue truth and blocker-terminality distinction from the protected seams.
- Risk: reclaim truth and CO STATUS/runtime truth diverge.
  - Mitigation: keep focused runtime/status projection coverage in the same slice as provider handoff coverage.
- Risk: audit history is lost while making reclaim succeed.
  - Mitigation: preserve blocker history in `provider-intake-state.json` and treat it as audit evidence only.

## Approvals
- Reviewer: pending parent docs-review and implementation validation
- Date: 2026-04-17
