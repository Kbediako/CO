# PRD - CO-382 fallback-expiry and large-refactor policy

## Traceability
- Linear issue: `CO-382` / `88401e48-8112-4817-a7ee-dd4f23241a2b`
- Linear URL: https://linear.app/asabeko/issue/CO-382
- Task id: `linear-88401e48-8112-4817-a7ee-dd4f23241a2b`
- Canonical spec: `tasks/specs/linear-88401e48-8112-4817-a7ee-dd4f23241a2b.md`
- Parent manifest: `.runs/linear-88401e48-8112-4817-a7ee-dd4f23241a2b-docs-scaffold/cli/2026-04-26T13-28-53-386Z-40489599/manifest.json`
- Docs packet child source anchor: `ctx:sha256:f626c0060a758f54ce277c8a7d0cf2f3e1e1acea5ea6e244c8b83f55e3bcca44#chunk:c000001`
- Source payload path: `.runs/linear-88401e48-8112-4817-a7ee-dd4f23241a2b-docs-scaffold/cli/2026-04-26T13-28-53-386Z-40489599/memory/source-0/source.txt`
- Source payload note: the source payload path was not present in this child checkout, so this scaffold is anchored on the parent-provided issue title, acceptance criteria, not-done criteria, source anchor, and bounded file scope.

## Summary
- Problem Statement: high-churn CO control surfaces keep accumulating compatibility branches, runtime fallbacks, and small seam patches without a shared policy for `fallback expiry` or for choosing a `large refactor` over another `minor seam`.
- Desired Outcome: create a repo policy and docs-first prompts that force every retained fallback to carry an owner, trigger, introduced date, review date, removal condition, allowed maximum lifetime, and validation, and force agents to choose `remove fallback`, `expire fallback`, or `justify retaining fallback` for each touched fallback/seam.

## User Request Translation
- User intent / needs: scaffold the CO-382 docs-first packet only, preserving the exact policy mechanics and control surfaces before the parent lane implements any policy, template, checklist, follow-up, or validation change.
- Success criteria / acceptance:
  - Define a repo policy for `fallback expiry`: owner, trigger, introduced date, review date, removal condition, allowed maximum lifetime, and validation.
  - Define when a `large refactor` is preferred over another `minor seam`, especially for `provider workflow`, `review wrapper`, `runtime routing`, `docs freshness ownership`, and `control-host status surfaces`.
  - Add issue-template or docs-first checklist prompts that force agents to choose `remove fallback`, `expire fallback`, or `justify retaining fallback` for each touched fallback/seam.
  - Identify at least three existing fallback-heavy areas and create implementation follow-ups or attach them to existing issues.
  - Validate docs gates and relevant tests; this should change future posture, not weaken existing safety checks.
- Constraints / non-goals:
  - this child lane creates only the docs-first scaffold and registry/checklist mirrors
  - do not implement the policy itself
  - do not call Linear mutation helpers
  - do not open a PR or run full repo validation suites
  - parent owns authoritative Linear state, workpad, follow-up issue creation/attachment, implementation, validation, and PR lifecycle

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `CO: add fallback-expiry and large-refactor policy for high-churn control surfaces.`
  - `fallback expiry`
  - `large refactor`
  - `minor seam`
  - `provider workflow`
  - `review wrapper`
  - `runtime routing`
  - `docs freshness ownership`
  - `control-host status surfaces`
  - `remove fallback`
  - `expire fallback`
  - `justify retaining fallback`
- Protected terms / exact artifact and surface names:
  - `fallback expiry`
  - `large refactor`
  - `minor seam`
  - `provider workflow`
  - `review wrapper`
  - `runtime routing`
  - `docs freshness ownership`
  - `control-host status surfaces`
  - `remove fallback`
  - `expire fallback`
  - `justify retaining fallback`
  - `tasks/index.json`
  - `docs/TASKS.md`
  - `docs/docs-freshness-registry.json`
- Nearby wrong interpretations to reject:
  - "fallback policy only needs generic avoid-overengineering prose"
  - "agents can keep adding indefinite compatibility branches if tests pass"
  - "each high-churn surface should receive one more small seam by default"
  - "follow-up identification can be deferred until after implementation"
  - "docs gates can be weakened to make the policy easier to land"

## Parity / Alignment Matrix
- Not applicable. CO-382 is a new repo policy posture lane, not a parity/alignment lane with a concrete external reference target.

## Not Done If
- The policy only says "avoid overengineering" without concrete expiry mechanics.
- Agents can continue adding indefinite compatibility branches without an owner and removal condition.
- The docs-first prompts do not force `remove fallback`, `expire fallback`, or `justify retaining fallback`.
- The policy does not state when a `large refactor` is preferred over another `minor seam`.
- Fallback-heavy areas are not identified and mapped to parent-created follow-ups or existing issue attachments.
- Docs gates or relevant tests are weakened instead of validating the new posture.

## Goals
- Create the CO-382 docs-first packet and registry mirrors.
- Preserve exact fallback-expiry mechanics and large-refactor decision language.
- Keep the implementation scoped to future policy/checklist posture, not runtime behavior changes in this child lane.
- Name fallback-heavy areas that the parent must attach to existing issues or turn into follow-ups.

## Non-Goals
- No source implementation in this child lane.
- No Linear mutations, follow-up issue creation, PR updates, or workpad edits in this child lane.
- No full repo validation suites from this child lane.
- No weakening of existing docs, spec, review, or validation gates.
- No invented parity target.

## Candidate Fallback-Heavy Areas
- `provider workflow`: provider-id mapping and autopilot retained-claim fallback paths in `orchestrator/src/cli/control/providerIssueHandoff.ts`; follow-up `CO-394`.
- `review wrapper`: scoped prompt retry, synthesized title fallback, generated fallback notes, and bounded-review contracts in `scripts/lib/review-launch-attempt.ts`, `scripts/run-review.ts`, and `docs/standalone-review-guide.md`; follow-up `CO-395`.
- `runtime routing`: appserver-to-CLI and cloud fallback contracts in `orchestrator/src/cli/runtime/provider.ts`, `orchestrator/src/cli/runtime/codexCommand.ts`, `scripts/runtime-mode-canary.mjs`, and cloud-mode docs; follow-up `CO-396`.
- `docs freshness ownership`: owned rolling-debt and canonical owner fallback behavior in `scripts/docs-freshness-maintain.mjs` and `docs/guides/docs-freshness-cohorts.md`; follow-up `CO-397`.
- `control-host status surfaces`: legacy proof/status fallback projection paths in `orchestrator/src/cli/control/compatibilityIssuePresenter.ts`, `orchestrator/src/cli/control/providerIssueObservability.ts`, `orchestrator/src/cli/control/selectedRunProjection.ts`, and `orchestrator/src/cli/control/controlRuntime.ts`; follow-up `CO-398`.

## Stakeholders
- Product: CO maintainers and operators who need compatibility posture to stop accumulating indefinite fallbacks.
- Engineering: owners of provider workflow, review wrapper, runtime routing, docs freshness ownership, and control-host status surfaces.
- Design: N/A.

## Metrics & Guardrails
- Primary Success Metrics:
  - policy requires owner, trigger, introduced date, review date, removal condition, allowed maximum lifetime, and validation for every retained fallback
  - docs-first checklist/template prompts force `remove fallback`, `expire fallback`, or `justify retaining fallback`
  - at least three fallback-heavy areas are mapped to existing issues or parent-created follow-ups
  - docs gates and relevant tests validate the changed posture
- Guardrails / Error Budgets:
  - zero weakening of `spec-guard`, `docs:check`, `docs:freshness`, review gates, or runtime canary expectations
  - zero indefinite fallback branches without owner/removal metadata
  - zero child-lane edits outside the declared docs scaffold scope

## Technical Considerations
- Architectural Notes:
  - CO-382 should change governance posture and docs-first prompts before agents add more fallback branches.
  - The policy must distinguish temporary fallback expiry from durable compatibility contracts.
  - The large-refactor decision rule must be concrete enough to stop repeated minor seam accumulation on the named high-churn surfaces.
- Dependencies / Integrations:
  - docs-first templates/checklists
  - task registry and docs freshness registry
  - parent-owned follow-up issue creation or attachment
  - docs gates and focused tests chosen by the parent implementation

## Fallback / Refactor Decision
- Applies to fallback, compatibility, legacy, stale, cached, break-glass, or minor-seam behavior: `Yes`.
- CO-382 decision: create the shared policy and docs-first prompts that force future lanes to choose `remove fallback`, `expire fallback`, or `justify retaining fallback` for each touched fallback/seam.
- Retained temporary fallback fields: owner, trigger, introduced date, review date, allowed maximum lifetime, removal condition, and validation.
- Large-refactor check: named high-churn surfaces prefer larger consolidation when another fallback would split authority across live/cached/legacy state, span multiple lifecycle phases, or lack a clear removal condition.
- Policy guide: `docs/guides/fallback-expiry-and-refactor-policy.md`.

## Open Questions
- Follow-up Linear issue identifiers are `CO-394`, `CO-395`, `CO-396`, `CO-397`, and `CO-398`.
- Machine-readable fallback-expiry frontmatter is deferred; first implementation enforces through docs-first prompts, checklist evidence gates, and the policy guide.

## Approvals
- Product: parent CO-382 lane, pending
- Engineering: parent docs-review / implementation review, pending
- Design: N/A
