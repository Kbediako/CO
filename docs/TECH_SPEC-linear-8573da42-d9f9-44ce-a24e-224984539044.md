---
id: 20260513-linear-8573da42-d9f9-44ce-a24e-224984539044
title: CO-523 strict spec-guard stale last_review scope
relates_to: docs/PRD-linear-8573da42-d9f9-44ce-a24e-224984539044.md
risk: high
owners:
  - Codex
last_review: 2026-05-12
related_action_plan: docs/ACTION_PLAN-linear-8573da42-d9f9-44ce-a24e-224984539044.md
task_checklists:
  - tasks/tasks-linear-8573da42-d9f9-44ce-a24e-224984539044.md
---

## Canonical Reference
- Canonical task spec: `tasks/specs/linear-8573da42-d9f9-44ce-a24e-224984539044.md`
- PRD: `docs/PRD-linear-8573da42-d9f9-44ce-a24e-224984539044.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-8573da42-d9f9-44ce-a24e-224984539044.md`
- Task checklist: `tasks/tasks-linear-8573da42-d9f9-44ce-a24e-224984539044.md`
- `.agent` mirror: `.agent/task/linear-8573da42-d9f9-44ce-a24e-224984539044.md`
- Source anchor: `ctx:sha256:db68f6a7917841a63197e4457e7bcfe6bcb4009b7eba246a8880f864265816f7#chunk:c000001`
- Child lane manifest: `.runs/linear-8573da42-d9f9-44ce-a24e-224984539044-co523-docs-packet/cli/2026-05-12T18-52-17-405Z-7c08766d/manifest.json`

## Summary
- Objective: define the CO-523 docs-first contract for strict stale-baseline classification without weakening `node scripts/spec-guard.mjs`.
- Scope: six packet files only in this child lane; parent owns source-row repair, registry/index updates, validation, Linear state, GitHub state, and PR lifecycle.
- Constraints: preserve `strict spec-guard`, exact stale `last_review` scope, `Core Lane` evidence for `CO-522` / `PR #795`, `no blind bumps`, `no deletion`, and `no weakening`.

## Issue-Shaping Contract
- User-request translation carried forward: create the CO-523 packet only so the parent can classify stale baseline debt affecting `Core Lane` for `CO-522` / `PR #795` while leaving `node scripts/spec-guard.mjs` strict and avoiding broad updates to `docs/design/specs` or `tasks/specs`.
- Protected terms / exact artifact and surface names:
  - `strict spec-guard`
  - `node scripts/spec-guard.mjs`
  - `last_review`
  - `Core Lane`
  - `CO-522`
  - `PR #795`
  - `docs/design/specs`
  - `tasks/specs`
  - `stale baseline`
  - `no blind bumps`
  - `no deletion`
  - `no weakening`
- Nearby wrong interpretations to reject:
  - mass-refreshing stale `last_review` values
  - deleting stale packet/spec rows to make a gate pass
  - editing or bypassing `node scripts/spec-guard.mjs`
  - treating `CO-522` / `PR #795` implementation as owned by this child lane
  - touching `tasks/index.json`, `docs/TASKS.md`, `docs/docs-freshness-registry.json`, or existing stale source rows from this child lane
- Explicit non-goals carried forward:
  - no Linear or GitHub mutation
  - no source/test/product implementation changes
  - no existing stale source spec edits under `docs/design/specs` or `tasks/specs`
  - no full repo validation suite from this child lane

## Parity / Alignment Matrix

- Current truth:
  - parent-provided issue contract identifies stale `last_review` scope as the CO-523 subject
  - `Core Lane` for `CO-522` / `PR #795` is the named validation context
  - source payload path was unavailable in this child checkout
- Reference truth:
  - `node scripts/spec-guard.mjs` should remain strict for stale active specs
  - source rows under `docs/design/specs` and `tasks/specs` need path-level evidence before any date/status change
  - historical packet/spec evidence should not be deleted just because it is stale
- Target truth / intended delta:
  - parent has a packet that preserves the exact scope and non-goals
  - parent repairs only evidence-backed stale rows
  - `no blind bumps`, `no deletion`, and `no weakening` remain explicit acceptance gates
- Explicitly out-of-scope differences:
  - broad docs freshness cleanup
  - `CO-522` implementation changes
  - `PR #795` review or check manipulation
  - spec-guard behavior changes

## Readiness Gate
- Not done if:
  - any packet file is missing
  - any protected term is absent from the packet
  - the packet allows broad or blind `last_review` updates
  - the packet allows deletion of historical evidence
  - the packet allows weakening `node scripts/spec-guard.mjs`
  - this child lane edits existing stale source specs, registries, lifecycle state, or implementation files
- Pre-implementation issue-quality review evidence:
  - 2026-05-12: approved for packet-only bootstrap. The micro-task path is unavailable because correctness depends on exact protected wording, stale-baseline scope, and strict non-goals.
  - 2026-05-12: source payload path was absent in this child checkout, so the parent-provided prompt and source anchor remain the operative source.
- Safeguard ownership split:
  - child lane owns only the six scoped packet files
  - parent lane owns exact stale row discovery, source-row edits, registry/index/docs freshness updates, validation, Linear state, GitHub state, workpad, PR lifecycle, and patch integration

## Technical Requirements
1. Create the CO-523 PRD, TECH_SPEC mirror, canonical task spec, ACTION_PLAN, task checklist, and `.agent` mirror.
2. Preserve all protected terms exactly.
3. State that `node scripts/spec-guard.mjs` must remain strict and unchanged.
4. State that parent must classify the stale baseline before changing any `last_review` row.
5. State that existing rows under `docs/design/specs` and `tasks/specs` are parent-owned and must not be edited by this child lane.
6. State that `CO-522` / `PR #795` `Core Lane` evidence is parent-owned and must not become a child-lane GitHub workflow task.
7. Keep `no blind bumps`, `no deletion`, and `no weakening` as acceptance criteria.

## Fallback Expiry / Refactor Decision
- Applies to fallback, compatibility, legacy, stale, cached, break-glass, or minor-seam behavior? `Yes`, because CO-523 touches stale baseline handling.
- Required decision table:

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `strict spec-guard` stale `last_review` baseline | Allowing stale rows to persist as tolerated baseline debt or clearing them by blind bump/deletion. | `remove fallback` | CO-523 parent lane | `Core Lane` evidence for `CO-522` / `PR #795` needs stale-baseline classification. | Parent to derive from exact stale row evidence. | 2026-05-12 | 30 days maximum for stale docs/spec control surfaces. | Parent reviews, refreshes, or reclassifies the exact stale rows with evidence while leaving `node scripts/spec-guard.mjs` strict. | Parent-owned `node scripts/spec-guard.mjs --dry-run`, targeted source-row evidence, and review handoff. |

- Large-refactor check: not applicable to this child packet. A broad spec/freshness refactor is explicitly out of scope unless the parent relaunches with widened ownership.

## Architecture & Data
- Architecture / design adjustments: none.
- Data model changes / migrations: none.
- External dependencies / integrations:
  - `node scripts/spec-guard.mjs`
  - `Core Lane`
  - `CO-522`
  - `PR #795`
  - `docs/design/specs`
  - `tasks/specs`
  - parent-owned Linear/GitHub reads

## Validation Plan
- Child-lane checks:
  - protected-term scan across the six packet files
  - `git diff --check --` over the six packet files
  - changed-file review confirming only the declared six files changed
- Parent-owned checks:
  - exact stale row discovery under `docs/design/specs` and `tasks/specs`
  - `Core Lane` evidence for `CO-522` / `PR #795`
  - parent `node scripts/spec-guard.mjs --dry-run`
  - parent docs/review/validation gates after source-row repair

## Open Questions
- Which exact `docs/design/specs` or existing `tasks/specs` rows form the stale baseline?
- Does the parent classify each row as active review refresh or terminal/inactive reclassification?
- What specific `Core Lane` evidence on `CO-522` / `PR #795` distinguishes stale baseline debt from current implementation regression?

## Approvals
- Docs packet child lane: produced in this workspace for parent patch export.
- Parent source-row repair and lifecycle handling: pending parent integration.
- Date: 2026-05-12
