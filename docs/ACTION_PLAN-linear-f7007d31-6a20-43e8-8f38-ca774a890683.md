# ACTION_PLAN - CO-572 current-endpoint status-machine split

## Summary
- Goal: remove the dashboard-projection dependency from machine health so `co-status --format json` and supervision can report fresh zero-WIP health while preserving fail-closed behavior for real failures.
- Scope: docs-first packet, cheap machine status contract, co-status degraded current-endpoint behavior, supervisor probe decoupling, progress-field propagation, dashboard degraded classification, focused regression tests, validation, review, and PR handoff.
- Assumptions:
  - current shared root stays on latest `main`
  - no active GitHub PRs or started/review WIP lanes are currently competing for capacity
  - GPT Pro/local advisory evidence is design input; deterministic local tests and live artifacts remain authoritative

## Issue Readiness Gate
- Intent checksum / protected terms carried forward: `co-status --format json`, `/ui/data.json`, `same-endpoint current-endpoint timeout`, `probe_failed`, `probe_timeout`, `provider-intake-state.json`, `polling.restart_required=false`, `polling.stuck=false`, `active_claims=[]`, `progress_updated_at`, `machine status endpoint`, `readCompatibilityProjection()`.
- Not done if:
  - zero-WIP fresh provider polling plus same-endpoint dashboard timeout still exits nonzero
  - dashboard-only slowness can still trigger supervisor restart/quarantine
  - degraded status hides auth, stale source, dead endpoint, true stuck, active no-progress, or failed run evidence
  - dashboard partial/error output lacks explicit degraded metadata
- Pre-implementation issue-quality review:
  - CO-572 is the right owner because CO-571 is merged and fixed the provider progress-stall path, while the recurrence is now status/probe coupling to heavy `/ui/data.json`
  - CO-407/CO-376/CO-336 are historical adjacent owners but do not cover zero-WIP same-endpoint timeout with fresh polling
  - Linear labels and related issue links were verified after creation
- Fallback / refactor decision:
  - Yes, this touches fallback/seam behavior.
  - Remove fallback: `/ui/data.json` as machine health authority.
  - Remove fallback: active-claim-only degraded current-endpoint fallback.
  - Justify retaining fallback: source-labeled degraded machine status when cheap status proves freshness.
  - Remove fallback: supervisor dependence on heavy dashboard.
  - Expire fallback: unbounded dashboard response behavior if not fully fixed in first PR.
- Large-refactor check: the right refactor boundary is a status-machine split. A timeout bump or another minor branch is rejected. If the work must split, PR 1 must deliver machine status plus `co-status` plus supervisor decoupling; dashboard optimization may follow under CO-572.
- Minor-seam decision: acceptable only for the single source-labeled degraded machine status diagnostic contract; no timeout-only or dashboard-authority seam is retained.

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Machine health read | Full `/ui/data.json` dashboard projection is used as authoritative machine status. | remove fallback | CO-572 | Current same endpoint times out while provider polling is fresh. | Existing CO status architecture | 2026-05-21 | This issue | Cheap authenticated machine status endpoint/read model serves health without `readCompatibilityProjection()`. | Controller/presenter test proves no compatibility projection call; co-status zero-WIP timeout regression passes. |
| Degraded timeout fallback | Current fallback requires active provider-intake evidence and refuses zero-WIP fresh polling. | remove fallback | CO-572 | `active_claims=[]`, fresh progress, `restart_required=false`, same-endpoint dashboard timeout. | CO-407 fallback scope | 2026-05-21 | This issue | Zero-WIP fresh polling returns explicit degraded partial status. | `CoStatusCliShell` regression for zero active claims and fresh progress. |
| Local degraded partial status | Source-labeled local status explains dashboard timeout/reset without fabricating full UI data. | justify retaining fallback | CO-572 | Dashboard unavailable but cheap machine status proves auth/source/progress freshness. | Existing degraded-read contract refined by CO-572 | 2026-05-21 | Durable supported degraded mode | Replaced only by an equally cheap authoritative status endpoint used by all consumers. | Tests assert degraded metadata and fail-closed negative cases. |
| Supervisor probe | Supervisor depends on heavy dashboard via `co-status`. | remove fallback | CO-572 | `probe_failed` or `probe_timeout` from dashboard-only slowness under fresh zero-WIP health. | Existing supervision probe path | 2026-05-21 | This issue | Supervisor probes cheap machine status or bounded local diagnostic. | `ControlHostSupervision` regression proves no restart for dashboard-only timeout and restart for true stuck. |
| Dashboard response | `/ui/data.json` can reset/timeout without explicit dashboard-degraded response. | expire fallback | CO-572 | Dashboard projection exceeds budget. | 2026-05-21 | 2026-06-04 | 2026-06-20 | Dashboard completes within budget or returns explicit degraded/error metadata. | UI data controller timeout/reset test and local smoke trace. |

- Contract name: source-labeled degraded machine status.
- Owning surface: `co-status` local degraded machine-status read and `degraded_read` metadata.
- Steady-state proof: cheap machine status proves current auth, source, progress, provider-intake availability, no active claim stall, no polling error, and no failed-run evidence before degraded output is allowed.
- Tests/docs: `CoStatusCliShell`, `ControlHostSupervision`, `ControlMachineStatusContract`, `UiDataController`, and CO-572 packet mirrors.
- Non-expiring rationale: supported operator diagnostic mode, not a temporary compatibility bridge, because it is source-labeled, lower-authority, and fail-closed.

## Milestones & Sequencing
1. Docs-first and failing tests:
   - finalize PRD, TECH_SPEC, ACTION_PLAN, task checklist, task mirror, task index, registry, and docs/TASKS snapshot
   - add or identify failing tests for same-endpoint dashboard timeout/reset plus zero active claims plus fresh provider progress
   - add fail-closed negative tests before changing behavior
2. Machine status contract:
   - add cheap presenter/controller that excludes `readCompatibilityProjection()`
   - include bounded source, endpoint, polling, progress, provider-intake counts, and degraded metadata
   - verify auth, no-store, and bounded behavior
3. `co-status` and supervisor decoupling:
   - make `co-status --format json` use cheap machine status and report dashboard timeout/reset as explicit degraded read
   - make supervisor probe cheap status or equivalent local diagnostic
   - preserve true stuck, stale source, dead endpoint, auth, and failed run fail-closed outcomes
4. Dashboard degradation:
   - add explicit `/ui/data.json` dashboard degraded/error metadata for timeout/reset
   - avoid pretending partial dashboard output is complete
   - if split out, record expiry metadata and same-issue follow-up under CO-572
5. Validation, review, and handoff:
  - run required provider-worker validation floor
  - verify the machine-status route stays behind authenticated control routing and emits no-store caching headers
  - run manifest-backed standalone review with `gpt-5.5/xhigh`
   - open draft PR, trigger Codex review manually if auto review does not start, drain review/CI, then proceed through ready-review and merge closeout

## Dependencies
- Existing route/auth composition in the control-host app server.
- Existing `coStatusAttachCliShell.ts` fetch taxonomy and endpoint recovery.
- Existing provider polling health and CO-571 progress fields.
- Existing CO-407 direct JSON timeout tests and CO-571 provider issue handoff tests.
- Live control-host/provider-intake evidence for post-merge shared-root proof.

## Validation
- Checks / tests:
  - machine status endpoint excludes dashboard compatibility projection
  - `CoStatusCliShell.test.ts` zero-WIP same-endpoint timeout/reset returns explicit degraded partial status
  - negative cases for auth, stale source, dead endpoint, real stuck, active no-progress, and failed runs
  - `ControlHostSupervision` tests for dashboard-only timeout vs true stuck
  - `UiDataController` dashboard degraded/error response tests
  - progress-field round-trip test
  - `node scripts/delegation-guard.mjs`
  - `node scripts/spec-guard.mjs --dry-run`
  - `npm run build`
  - `npm run lint`
  - `npm run test`
  - `npm run docs:check`
  - `npm run docs:freshness`
  - `npm run repo:stewardship`
  - `node scripts/diff-budget.mjs`
  - manifest-backed review
  - `npm run pack:smoke` if CLI/package/review-wrapper surfaces require it
- Rollback plan:
  - revert CO-572 branch before merge if tests show false healthy classification
  - after merge, fail closed by preferring existing nonzero status behavior only if a blocking P0/P1 is found
  - do not delete provider-intake artifacts or relax WIP gates as rollback

## Risks & Mitigations
- Risk: cheap status omits a failure signal that dashboard previously surfaced. Mitigation: fail-closed negative cases for auth, stale source, dead endpoint, stuck, active no-progress, and failed runs.
- Risk: degraded status becomes a hidden fallback. Mitigation: source-labeled `degraded_read` metadata plus tests that partial output is not complete dashboard data.
- Risk: diff is too large. Mitigation: split under CO-572 with first PR delivering machine status, `co-status`, and supervisor decoupling.
- Risk: status paths drift after future dashboard changes. Mitigation: static/deterministic test that machine status does not call compatibility projection.

## Approvals
- Reviewer: pending docs review.
- Date: 2026-05-21
