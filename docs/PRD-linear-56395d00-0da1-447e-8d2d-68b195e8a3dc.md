# PRD - CO: Stabilize unrelated orchestrator/tests/Doctor.test.ts full-suite timeouts blocking CO-219 handoff

## Added by Provider Worker 2026-04-18

## Traceability
- Linear issue: `CO-226` / `56395d00-0da1-447e-8d2d-68b195e8a3dc`
- Source issue: `CO-219` / `9f101328-ca10-47b3-9dc9-b0984d339794`
- Task id: `20260418-linear-56395d00-0da1-447e-8d2d-68b195e8a3dc`
- Canonical TECH_SPEC: `tasks/specs/linear-56395d00-0da1-447e-8d2d-68b195e8a3dc.md`
- Prompt handoff referenced upstream origin manifest id `2026-04-17T21-37-49-533Z-7f05b822`, but that file is not materialized under this workspace `.runs/`; local evidence for this attempt comes from the manifests created during the active docs-review, child-lane, and review wrapper runs.
- Source anchor: `ctx:sha256:dc802d183aad52d63c661a623eea2dfb1a6331c8142f57e36c744ffe0706b069#chunk:c000001`
- Source payload note: the shared `source-0` payload in this workspace carries run metadata/provenance, not the issue body details; the protected issue wording from Linear is authoritative for the problem statement, evidence, and non-goals.

## Summary
- Problem Statement: repo-wide `npm run test` is still intermittently timing out inside `orchestrator/tests/Doctor.test.ts` on this host, even though the same Doctor coverage passed in standalone and in the targeted trio used during CO-219. That unrelated baseline instability blocks CO-219 from moving back to review because the required full-suite gate is not green.
- Desired Outcome: explain the difference between targeted Doctor success and full-suite timeout behavior, then land the smallest fix or validated test-runner adjustment that restores a green repo-wide `npm run test` without weakening Doctor coverage or reopening CO-219 scope.
- Latest status (2026-04-18): accepted child lane `doctor-targeted-tests` replaced one expensive real direct-dist readiness startup in `Doctor.test.ts` with a local fake direct-dist entrypoint, and follow-up commit `651e17b4a` tightened that stub so it replies only after the real `initialize` request and verifies the `delegate-server` argv. After merging current `origin/main`, including the CO-233 SelectedRunProjection fix, the latest exact repo-wide `npm run test` rerun is green with `345` files and `4246` tests; PR `#522` is ready for review handoff once remote checks and the `pr ready-review` drain are clean.

## User Request Translation (Context Anchor)
- User intent / needs (in your own words): isolate and fix the unrelated full-suite timeout behavior in `orchestrator/tests/Doctor.test.ts` so CO-219 does not have to absorb baseline test stabilization work, and keep the provider-worker workflow auditable with a docs-first packet, a single workpad, and bounded parallel child-lane evidence.
- Success criteria / acceptance:
  - reproduce and characterize the full-suite timeout story with the exact `npm run test` surface and the observed variability across reruns
  - determine whether the instability is primarily host load, Vitest scheduling, shared test state, or Doctor-specific performance
  - restore a green repo-wide `npm run test` with the smallest viable fix or runner/config adjustment
  - link the outcome back to CO-219 so its dead-proof projection fix can resume normal review handoff
- Constraints / non-goals:
  - do not reopen or widen the CO-219 dead-proof projection implementation
  - do not weaken Doctor assertions, delete coverage, or bypass the full-suite gate with targeted-only evidence
  - do not blindly increase timeouts without understanding the source of the instability
  - keep the same issue workpad current and use bounded same-issue child-lane evidence instead of blanket delegation overrides

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `npm run test`
  - `orchestrator/tests/Doctor.test.ts`
  - unrelated full-suite timeouts versus standalone pass
  - `npx vitest run orchestrator/tests/ControlRuntime.test.ts orchestrator/tests/ControlServer.test.ts orchestrator/tests/Doctor.test.ts`
  - CO-219 stays out of review until the repo-wide test gate is green
- Protected terms / exact artifact and surface names:
  - `CO-219`
  - `CO-226`
  - `orchestrator/tests/Doctor.test.ts`
  - `npm run test`
  - `npx vitest run orchestrator/tests/Doctor.test.ts`
  - `npx vitest run orchestrator/tests/ControlRuntime.test.ts orchestrator/tests/ControlServer.test.ts orchestrator/tests/Doctor.test.ts`
  - `prefers task roots over nested provider templates when both signals exist`
  - `resolves provider readiness from the repo root when doctor runs in a nested directory`
- Nearby wrong interpretations to reject:
  - CO-219 already proved a deterministic Doctor regression and this lane is just paperwork
  - targeted Doctor green is enough to waive the required full-suite test gate
  - the only acceptable fix is to raise Vitest or test-case timeouts blindly
  - the right fix is to weaken or remove Doctor coverage
  - any unrelated repo-baseline failure can be treated as “host flake” without evidence

## Parity / Alignment Matrix

| Surface | Current Truth | Reference Truth | Target Truth |
| --- | --- | --- | --- |
| Targeted Doctor run | `npx vitest run orchestrator/tests/Doctor.test.ts` passed earlier in the session. | Standalone Doctor coverage should be stable. | Standalone Doctor coverage remains green. |
| Targeted trio | `npx vitest run orchestrator/tests/ControlRuntime.test.ts orchestrator/tests/ControlServer.test.ts orchestrator/tests/Doctor.test.ts` passed with `243` tests after the CO-219 changes landed. | The trio shows the CO-219 issue-scoped surface is not enough to explain the failure. | The trio remains green and aligned with full-suite behavior. |
| Repo-wide suite | repeated `npm run test` reruns timed out in `Doctor.test.ts`, with 11 timeouts on one run and 1 timeout on later reruns in different cases. | The required repo-wide gate for CO-219 is a clean `npm run test`. | `npm run test` is green without special-casing or weakening Doctor coverage. |
| Review handoff | CO-219 is blocked from review because the repo-wide gate is not trustworthy. | Review handoff requires a green repo-wide test gate or an explicitly resolved blocker. | CO-219 can resume normal handoff after this lane restores a green suite or resolves the blocker explicitly. |

## Acceptance Criteria
- Capture a reproducible full-suite timeout story for the affected `Doctor.test.ts` cases, including exact command surface and observed variability.
- Determine whether the instability is caused by host load, Vitest scheduling, shared test state, or Doctor-specific performance.
- Land the smallest fix or validated runner/config adjustment that makes repo-wide `npm run test` green again without weakening coverage.
- Link the validation outcome back to CO-219 so the dead-proof projection fix can resume normal review handoff.

## Non-Goals
- Reopening or broadening the CO-219 dead-proof projection feature work.
- Raising `max_concurrent_agents` or changing `co-status` semantics.
- Weakening Doctor assertions or deleting coverage to hide the timeout symptom.
- Treating a standalone `Doctor.test.ts` pass as proof that the required full `npm run test` gate is green.

## Not Done If
- `npm run test` can still fail on `orchestrator/tests/Doctor.test.ts` timeouts under normal full-suite execution.
- The lane does not explain the difference between standalone Doctor success and full-suite timeout behavior.
- CO-219 is pushed into review again without a green repo-wide test gate or an explicit resolved blocker.

## Stakeholders
- Product: CO operators needing truthful review readiness for CO-219.
- Engineering: Doctor CLI/runtime maintainers, Vitest/test harness maintainers, provider-worker operators.
- Design: N/A.

## Metrics & Guardrails
- Primary Success Metrics:
  - repo-wide `npm run test` exits green on the current host after the fix
  - targeted Doctor and trio runs stay green
  - the timeout explanation is evidence-backed rather than speculative
- Guardrails / Error Budgets:
  - no weakened Doctor assertions or removed coverage
  - no hidden rerun-only waiver in place of a green required gate
  - no widening into unrelated CO-219 implementation work

## User Experience
- Personas: provider workers and reviewers deciding whether CO-219 is actually ready to hand off.
- User Journeys:
  - operator reruns `npm run test` and gets a green, stable full-suite outcome instead of intermittent Doctor timeouts
  - reviewer sees clear evidence for why targeted Doctor passed while the full suite used to fail
  - CO-219 can move forward without carrying unrelated Doctor stabilization debt

## Technical Considerations
- Architectural Notes:
  - this lane begins with a repo-baseline/testing diagnosis problem, not with a predetermined product-code seam
  - likely source or harness seams include `orchestrator/tests/Doctor.test.ts`, the shared test harness around Doctor temp directories and provider templates, and any Vitest runner/config surfaces that materially affect this suite under full load
  - targeted-only success means the cause could be scheduling/contention/shared-state, not necessarily a deterministic Doctor logic bug
- Dependencies / Integrations:
  - `orchestrator/tests/Doctor.test.ts`
  - `orchestrator/tests/ControlRuntime.test.ts`
  - `orchestrator/tests/ControlServer.test.ts`
  - package/Vitest command surfaces that define `npm run test`

## Open Questions
- Are the intermittent timeouts caused by per-test setup/teardown cost in `Doctor.test.ts`, or by cross-suite contention that only appears in the full run?
- Is there shared temporary-directory, filesystem, or process state that is safe in isolated runs but slow or contentious in the full suite?
- Does the smallest correct fix live in the Doctor tests themselves, in the shared test harness, or in the repo’s Vitest execution contract?

## Approvals
- Product: pending
- Engineering: pending
- Design: N/A
