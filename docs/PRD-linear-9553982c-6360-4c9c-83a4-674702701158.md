# PRD - CO: Restore baseline doctor and stage-set tests blocking review handoff

## Added by Bootstrap 2026-03-30

## Traceability
- Linear issue: `CO-47` / `9553982c-6360-4c9c-83a4-674702701158`
- Linear URL: https://linear.app/asabeko/issue/CO-47/co-restore-baseline-doctor-and-stage-set-tests-blocking-review-handoff

## Summary
- Problem Statement: provider-worker review handoff for `CO-44` is currently blocked by three pre-existing orchestrator test failures that reproduce on a clean `origin/main`-based checkout at `bdf8c648f5ec13e0068f6d592d1227924477e63c`. The failing suites are `orchestrator/tests/Doctor.test.ts`, `orchestrator/tests/PipelineResolverEnvOverrides.test.ts`, and `orchestrator/tests/UserConfigStageSets.test.ts`.
- Desired Outcome: restore a truthful clean-main baseline by reproducing those three failures in this workspace, landing the smallest fix or expectation correction needed, and proving the focused orchestrator command passes so provider-worker lanes can satisfy the required test gate before review handoff.

## User Request Translation (Context Anchor)
- User intent / needs (in your own words): complete Linear issue `CO-47` in this workspace by treating the clean-main failures as the task itself, not as collateral from `CO-44`, and repair the baseline doctor/stage-set seams without expanding into unrelated CO STATUS or provider-worker workflow changes.
- Success criteria / acceptance:
  - the failures in `orchestrator/tests/Doctor.test.ts`, `orchestrator/tests/PipelineResolverEnvOverrides.test.ts`, and `orchestrator/tests/UserConfigStageSets.test.ts` are reproduced on the clean-main-based workspace
  - the minimal code and/or test updates needed to restore truth are implemented
  - `npm run test:orchestrator -- Doctor.test.ts PipelineResolverEnvOverrides.test.ts UserConfigStageSets.test.ts` passes
  - any doctor cloud-preflight or stage-set expectation changes remain truthful to current runtime behavior
- Constraints / non-goals:
  - stay bounded to the three named suites and the code paths they directly exercise
  - do not widen into the unrelated `CO-44` terminal parity diff, generic full-suite cleanup, or unrelated review-handoff policy work
  - follow the repo docs-first path and use an audited child-stream docs-review before implementation

## Goals
- Reproduce the clean-main failures cited in the issue on this workspace branch.
- Identify whether the breakage is in runtime behavior, stale test expectations, or both.
- Restore truthful baseline behavior for doctor cloud-preflight and stage-set resolution.
- Revalidate the three targeted suites with the exact issue command.

## Non-Goals
- Fixing unrelated orchestrator or CLI tests outside the three named suites unless a direct shared seam is required for the minimal fix.
- Changing `CO-44` implementation scope beyond unblocking its review handoff gate.
- Broad refactors of doctor, pipeline resolver, or user-config loading.

## Stakeholders
- Product: CO operator waiting on provider-worker review handoff for `CO-44`
- Engineering: Codex
- Design: N/A

## Metrics & Guardrails
- Primary Success Metrics:
  - the three named test files pass together under `npm run test:orchestrator -- Doctor.test.ts PipelineResolverEnvOverrides.test.ts UserConfigStageSets.test.ts`
  - changed expectations match current implementation truth instead of masking drift
- Guardrails / Error Budgets:
  - preserve clean-main reproducibility and keep the fix minimal
  - do not fabricate broader validation success beyond the evidence actually rerun
  - stop coding if the issue reaches `In Review` or `Human Review`

## User Experience
- Personas: CO operators and provider workers who need the repo validation floor to be trustworthy before review handoff
- User Journeys:
  - a provider-worker lane reruns the required validation floor and no longer gets blocked by these unrelated baseline failures
  - an engineer inspecting doctor or stage-set expectations can trust the tests to reflect the current behavior

## Technical Considerations
- Architectural Notes:
  - `orchestrator/src/cli/doctor.ts` drives the doctor cloud-preflight expectations
  - `orchestrator/src/cli/services/pipelineResolver.ts` and related config-loading seams drive the pipeline env override expectations
  - `orchestrator/src/cli/config/userConfig.ts` and stage-set helpers drive `UserConfigStageSets` behavior
  - the current workspace starts from the same clean-main commit named in the issue, which keeps the reproduction scope aligned to the reported baseline
- Dependencies / Integrations:
  - `orchestrator/tests/Doctor.test.ts`
  - `orchestrator/tests/PipelineResolverEnvOverrides.test.ts`
  - `orchestrator/tests/UserConfigStageSets.test.ts`
  - the implementation files those suites currently exercise

## Open Questions
- Whether the failing expectations now lag intentional runtime changes, or whether a recent behavior change broke an older contract. The task should resolve this with direct reproduction and code inspection before any patch.

## Approvals
- Product: Self-approved from Linear issue scope and acceptance criteria
- Engineering: Pending docs-review and implementation validation
- Design: N/A
