# PRD - CO-503 May 6 active spec-guard stale review baseline

## Traceability
- Linear issue: `CO-503` / `1d32d63d-a4f1-4817-82e4-8dc90c26074d`
- Linear URL: https://linear.app/asabeko/issue/CO-503/co-refresh-may-6-active-spec-guard-stale-review-baseline
- Task id: `linear-1d32d63d-a4f1-4817-82e4-8dc90c26074d`
- Canonical spec: `tasks/specs/linear-1d32d63d-a4f1-4817-82e4-8dc90c26074d.md`
- TECH_SPEC mirror: `docs/TECH_SPEC-linear-1d32d63d-a4f1-4817-82e4-8dc90c26074d.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-1d32d63d-a4f1-4817-82e4-8dc90c26074d.md`
- Task checklist: `tasks/tasks-linear-1d32d63d-a4f1-4817-82e4-8dc90c26074d.md`
- `.agent` mirror: `.agent/task/linear-1d32d63d-a4f1-4817-82e4-8dc90c26074d.md`
- Triggering feature-lane evidence: PR `#779` for `CO-455`, Core Lane run `25408870938`, job `74525853122`, `BASE_SHA=f7a34fe4b7e0f11210daa2358d796d5d6ca1d166`
- Source anchor: `ctx:sha256:a94cf7cf9d9a394aa5c7f23c0ebf22d46760c2e4c1bb050d8ff1ff3a814a0296#chunk:c000001`

## Summary
- Problem Statement: `node scripts/spec-guard.mjs --dry-run` fails on current clean `origin/main` because seven active task specs still have `last_review=2026-04-05`, which is now 31 days old.
- Desired Outcome: repair the May 6 active-spec baseline with truthful review notes or terminal reclassification while preserving strict `spec-guard` behavior and keeping `CO-455` / PR `#779` out of scope.

## User Request Translation
- User intent / needs: own the repo-wide May 6 `spec-guard` stale active-spec baseline as a separate same-project issue, review the exact stale specs, preserve `last_review=2026-04-05` traceability, and clear the blocker for unrelated feature lanes.
- Success criteria / acceptance:
  - reproduce the stale active-spec baseline on current `origin/main`
  - classify each stale spec from live evidence instead of blind date updates
  - reclassify terminal source issues as inactive historical packets
  - refresh still-active specs only with current review notes
  - update task registry and docs freshness registry only for touched, reviewed surfaces
  - prove `spec-guard` passes after the repair
- Constraints / non-goals:
  - no `CO-455` implementation or test changes
  - no `spec-guard` weakening or bypass
  - no blind `last_review` bumps
  - no docs:freshness rolling-cohort hiding
  - no terminal-owner reuse as active capacity

## Intent Checksum
- Exact protected terms:
  - `spec-guard`
  - `active specs`
  - `last_review=2026-04-05`
  - `Core Lane`
  - PR `#779`
  - `CO-455`
  - stale active specs
  - `spec-guard:active-specs:last_review=2026-04-05`
- Nearby wrong interpretations to reject:
  - this is a `CO-455` source regression
  - `spec-guard` should be relaxed
  - stale rows can be hidden by docs:freshness rolling cohorts
  - terminal owners `CO-278`, `CO-428`, or `CO-498` are still active capacity
  - historical task packet evidence should be deleted to clear a gate

## Parity / Alignment Matrix

| Surface | Current | Reference | Target |
| --- | --- | --- | --- |
| PR `#779` / `CO-455` Core Lane | Fails `spec-guard` on the May 6 stale active-spec set. | Clean `origin/main` at `f7a34fe4b7` reproduces the same set. | `CO-455` no longer carries the unrelated baseline blocker. |
| Active owner truth | Prior related owners `CO-278`, `CO-428`, and `CO-498` are terminal. | Canonical owner reuse must not treat terminal owners as active capacity. | `CO-503` records the live May 6 owner and repair evidence. |
| Review metadata | Seven specs show `last_review=2026-04-05` and are 31 days old. | Active specs need real review notes; terminal specs need truthful inactive reclassification. | The active spec is refreshed with review evidence, and terminal source specs are inactive `done` historical metadata. |

## Not Done If
- PR `#779` or another unrelated feature lane still fails Core Lane on the same stale active-spec set.
- `node scripts/spec-guard.mjs` still fails on current `origin/main` for the May 6 cohort.
- Terminal owner issues remain the only documented ownership for this baseline.
- Touched specs lack truthful review notes/evidence for the May 6 disposition.
- `spec-guard` behavior is weakened, skipped, or bypassed.

## Goals
- Reproduce the baseline before edits.
- Create the CO-503 docs-first packet and registry coverage.
- Review the exact seven stale specs.
- Reclassify source specs whose live Linear issues are `Done`.
- Refresh the still-active RLM orchestrator spec with a real May 6 review note.
- Preserve PR `#779` as the trigger and keep `CO-455` out of implementation scope.

## Non-Goals
- No product code, tests, or `CO-455` branch changes.
- No guard policy changes.
- No broad docs:freshness maintenance outside the matching stale rows.
- No reopen or reuse of terminal owners as current capacity.

## Validation Contract
- `node scripts/spec-guard.mjs --dry-run` before and after the metadata repair.
- `npm run docs:freshness` before and after the registry repair.
- `jq empty tasks/index.json docs/docs-freshness-registry.json`.
- Required provider-worker validation floor before handoff.
- Manifest-backed standalone review plus explicit elegance review if final diff is non-trivial.

