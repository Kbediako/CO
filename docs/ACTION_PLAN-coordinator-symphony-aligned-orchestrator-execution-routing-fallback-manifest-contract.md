# ACTION PLAN: Coordinator Symphony-Aligned Orchestrator Execution-Routing Fallback Manifest Contract

1. Register `1170` docs-first and sync task mirrors/index/registry.
2. Narrow the remaining cloud-preflight failure contract inside `orchestratorExecutionRouter.ts` without widening the seam beyond router-local behavior.
3. Add or extend focused router tests for hard-fail manifest shaping, fallback manifest shaping, and recursive reroute input preservation.
4. Run the required validation bundle and close the lane with explicit override notes if review/docs-review wrapper noise recurs.
