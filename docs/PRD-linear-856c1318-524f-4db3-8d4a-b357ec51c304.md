# PRD - Archive Automation Policy Mismatch for Linked PRD/TECH_SPEC/ACTION_PLAN Docs

## Added by Bootstrap 2025-10-16

## Traceability
- Linear issue: `CO-2` / `856c1318-524f-4db3-8d4a-b357ec51c304`
- Linear URL: https://linear.app/asabeko/issue/CO-2/archive-automation-policy-mismatch-for-linked-prdtech-specaction-plan

## Summary
- Problem Statement: The implementation-docs archive policy already declares linked `docs/PRD-*`, `docs/TECH_SPEC-*`, and `docs/ACTION_PLAN-*` files in scope, but the current archiver misses those linked docs because plain `*` glob handling is inconsistent with the policy patterns, canonical `tasks/index.json` entries seed linked-doc discovery through explicit `paths.*` fields rather than only legacy `path`, and indexed paths need repo-boundary containment before they can be archived safely.
- Desired Outcome: Keep the broader archive scope explicit, fix the archiver so linked PRD/TECH_SPEC/ACTION_PLAN docs archive into payloads and stubs consistently with policy for both legacy and canonical task-index entries, keep explicit task packet paths tied to the active task instead of stray archiving, and lock the behavior with focused regressions.

## User Request Translation (Context Anchor)
- User intent / needs (in your own words): Complete Linear issue `CO-2` end to end by resolving the mismatch between the implementation-docs archive policy and the current archiver behavior, keeping the issue workpad current and handing off only after a PR is attached and the team review state is ready.
- Success criteria / acceptance:
  - intended archive scope is explicit and stays consistent between policy and implementation
  - linked `docs/PRD-*`, `docs/TECH_SPEC-*`, and `docs/ACTION_PLAN-*` docs are archived when policy eligibility says they should be
  - indexed task doc paths stay inside the repository boundary before the archiver reads or archives them
  - focused regression coverage proves plain `*` doc patterns archive linked implementation docs into payloads and stubs
  - validation is green enough to prepare a PR and stop coding at the actual team review state (`In Review` here)
- Constraints / non-goals:
  - do not narrow policy unless the existing docs/specs clearly require it
  - keep the change bounded to archive-pattern handling, targeted tests, and the docs-first packet for this issue
  - delegation must be recorded as an explicit override in this worker run because subagent spawning is unavailable in-session

## Goals
- Preserve the current policy intent that linked PRD/TECH_SPEC/ACTION_PLAN docs are archive-eligible.
- Fix the archive script so plain `*` patterns behave consistently across policy matching and linked-doc extraction, so canonical `tasks/index.json` `paths.docs` plus explicit task/spec/agent paths participate in linked-doc discovery, and so indexed candidates cannot escape the repo boundary.
- Add targeted regressions that fail if linked implementation docs stop archiving again or if explicit task packet paths regress into stray archival.

## Non-Goals
- Redesigning archive retention rules, archive branch handling, or the surrounding GitHub workflow.
- Broad refactors to the docs archive system beyond the broken glob handling and its direct regression coverage.
- Continuing to code after the issue reaches the team review state.

## Stakeholders
- Product: CO operator / Linear workflow owner
- Engineering: Codex
- Design: N/A

## Metrics & Guardrails
- Primary Success Metrics:
  - linked implementation docs match the existing policy scope during archive selection
  - focused archive test produces payloads and stubs for linked PRD/TECH_SPEC/ACTION_PLAN docs
  - no policy narrowing is needed to explain the implementation
- Guardrails / Error Budgets:
  - keep the fix minimal and auditable
  - preserve current checklist/spec/.agent triad behavior while widening no scope beyond existing policy
  - stop at PR handoff instead of continuing past the team review state

## User Experience
- Personas: CO operator reviewing archive automation correctness through Linear
- User Journeys:
  - operator sees a policy/implementation mismatch on archived implementation docs
  - worker confirms the intended scope, patches the mismatch, and proves it with a regression
  - operator receives a PR-linked issue ready for `In Review`

## Technical Considerations
- Architectural Notes:
  - the likely defect is in `scripts/implementation-docs-archive.mjs` where policy glob patterns are converted to regexes and indexed task paths are gathered before archive eligibility checks
  - focused regressions belong in `tests/implementation-docs-archive.spec.ts`
- Dependencies / Integrations:
  - `docs/implementation-docs-archive-policy.json`
  - `scripts/implementation-docs-archive.mjs`
  - `tests/implementation-docs-archive.spec.ts`
  - Linear issue `CO-2` / `856c1318-524f-4db3-8d4a-b357ec51c304`

## Open Questions
- None at registration time; the current policy and existing tech specs already point toward an implementation fix.

## Approvals
- Product: Self-approved from Linear issue scope and acceptance criteria
- Engineering: Pending docs-review + implementation validation
- Design: N/A

## Manifest Evidence
- Docs-review manifest: `.runs/linear-856c1318-524f-4db3-8d4a-b357ec51c304/cli/2026-03-22T12-45-37-444Z-4447037c/manifest.json`
- Validation summary: `out/linear-856c1318-524f-4db3-8d4a-b357ec51c304/manual/20260323T003457Z-provider-rework/validation-summary.md`
- Evidence summary: the docs-review manifest captures the approved docs-first packet for this issue, and the validation summary captures the linked-doc archive fix, review-driven hardening follow-up, and the focused regression evidence for this lane.
