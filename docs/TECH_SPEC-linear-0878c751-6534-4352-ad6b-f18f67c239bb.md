---
id: 20260410-linear-0878c751-6534-4352-ad6b-f18f67c239bb
title: CO: Fix CLI command-surface timeout and exec quoting blocker
relates_to: docs/PRD-linear-0878c751-6534-4352-ad6b-f18f67c239bb.md
risk: medium
owners:
  - Codex
last_review: 2026-04-10
---

## Canonical Reference
- Canonical TECH_SPEC: `tasks/specs/linear-0878c751-6534-4352-ad6b-f18f67c239bb.md`
- PRD: `docs/PRD-linear-0878c751-6534-4352-ad6b-f18f67c239bb.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-0878c751-6534-4352-ad6b-f18f67c239bb.md`
- Task checklist: `tasks/tasks-linear-0878c751-6534-4352-ad6b-f18f67c239bb.md`

## Traceability
- Linear issue: `CO-136` / `0878c751-6534-4352-ad6b-f18f67c239bb`
- Linear URL: https://linear.app/asabeko/issue/CO-136/co-fix-cli-command-surface-timeout-and-exec-quoting-blocker
- Source issue: `CO-96` / `99d7fc30-93ab-43fd-94c2-68d604b4f85c`

## Summary
- Objective: make the failing command-surface cases deterministic by preserving the current quoted single-token `exec` behavior and updating the affected source-entrypoint tests to use a truthful timeout budget.
- Scope:
  - confirm that the current bin-level one-token `exec` normalization already preserves backslashes and escaped inner quotes in this workspace
  - update only the affected `tests/cli-command-surface.spec.ts` cases to use the source-entrypoint boot budget they actually need
- Constraints:
  - no unrelated CLI startup refactor
  - no widening of timeout budgets outside the affected cases
  - no `CO-96` behavior changes

## Implementation Boundary
- Parsing seam:
  - keep the existing `bin/codex-orchestrator.ts` single-token `exec` normalization as the only parser path in this lane
  - do not change already-correct multi-token invocations
- Validation seam:
  - move the affected source-entrypoint tests onto `CLI_BOOT_TIMEOUT` or an equivalent clearly-named truthful budget
  - keep binary-shell timeout coverage and broader suite budgets unchanged

## Design
- Keep the existing bin-level `normalizeExecCommandTokens(...)` and `splitShellLikeCommand(...)` path as the source of truth for quoted single-token `exec` normalization.
- Limit the final code delta to the test-timeout budget, because the workspace already preserves the escaped-quote and backslash cases under the current command parser.
- Keep the timeout truth local to `tests/cli-command-surface.spec.ts`, since the issue is about test-budget truthfulness rather than runtime behavior.

## Validation
- `linear child-stream --pipeline docs-review`
- Focused regressions in `tests/cli-command-surface.spec.ts`
- Full repo validation floor before review handoff because this touches a downstream-facing CLI surface
- `npm run pack:smoke` remains in scope if the final diff still touches downstream CLI surfaces

## Approvals
- Reviewer: pending `codex-orchestrator docs-review` child stream for the new packet
- Date: 2026-04-10
