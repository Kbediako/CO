# ACTION_PLAN - CO: Add authoritative provider-worker progress and stall observability for self-repair and external debugging

## Traceability
- Linear issue: `CO-82` / `736c3631-341a-4250-875f-e8808ef2a31b`
- Source issue: `CO-80`

## Plan
1. Register the docs packet, task mirrors, branch, and single Linear workpad.
2. Run `docs-review` and keep the packet aligned with the approved boundary.
3. Add one shared provider progress and debug snapshot contract across worker proof, intake claims, merge-closeout data, and control-host read models.
4. Mirror semantic progress into the normal runner/event stream and add focused regressions for the `CO-77` / `CO-78` classification path.
5. Run the repo validation floor, standalone review, and elegance pass before PR or review handoff.

## Final Validation
- docs-review child stream approved
- delegation-guard and spec-guard green
- build, lint, test, docs:check, docs:freshness, diff-budget with explicit override, and pack:smoke green
- required standalone review command executed under `FORCE_CODEX_REVIEW=1`, but telemetry recorded a `failed-boundary` / `startup-anchor` wrapper boundary rather than a product finding; manual fallback correctness/regression review plus explicit elegance pass completed with no actionable findings
- focused regressions prove one-surface classification for active, waiting, stalled, merge-closeout, and completed cases

## Rollback
- Revert the shared progress and debug snapshot helper plus its projections together if STATUS/API payloads regress.
- Keep new progress telemetry fail-closed: stale or unreadable data should degrade to explicit unknown or unavailable classifications rather than optimistic progress claims.
