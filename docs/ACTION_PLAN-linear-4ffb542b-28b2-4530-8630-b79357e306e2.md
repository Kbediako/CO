# ACTION_PLAN - CO workflow: automate docs:freshness baseline upkeep and diff-local gating

## Added by Bootstrap 2026-04-15

## Summary
- Goal: finish CO-188 by adding autonomous docs freshness maintenance decisions, owner/action evidence, and provider-worker diff-local gating while preserving fail-closed `docs:freshness` and `spec-guard` behavior.
- Scope: docs-first packet, single Linear workpad, required parallelization decision and child lane, docs-review, maintenance command/workflow, provider-worker gate integration, recurrence tests/fixtures, documentation, validation, standalone review, elegance pass, PR handoff, and ready-review drain.
- Assumptions:
  - existing rolling cohort policy remains authoritative unless a reviewed owner/action decision changes it
  - parent lane owns implementation and docs, while child lane owns only declared test files until accepted/rejected
  - review handoff state for this team is `In Review`

## Issue Readiness Gate
- Intent checksum / protected terms carried forward:
  - automate docs freshness baseline upkeep
  - preserve `docs:freshness` and `spec-guard` fail-closed behavior
  - keep `docs/docs-freshness-registry.json`, `docs/docs-catalog.json`, `docs/guides/docs-freshness-cohorts.md`, rolling freshness cohorts, owner issue, `max_entries=300`, provider-worker handoff, `docs-review`, and `implementation-gate` semantics explicit
- Not done if:
  - unrelated lanes still manually diagnose rollover debt
  - provider-worker gates pass for real drift
  - owner/action evidence is missing or duplicated
  - CO-175/CO-184 recurrence shapes are untested
- Pre-implementation issue-quality review:
  - 2026-04-15: approved. The issue is intentionally broader than a date-bump and requires command/workflow, owner/action, provider gate, test, fixture, and docs changes.

## Milestones & Sequencing
1. Bootstrap workflow:
   - inspect Linear issue context and team states
   - move `Ready` to `In Progress`
   - create the single `## Codex Workpad`
   - record the required same-turn parallelization decision
   - launch one bounded child lane for focused test/fixture work
2. Docs-first:
   - create PRD, TECH_SPEC, ACTION_PLAN, task checklist, `.agent` mirror, task index entry, docs task snapshot, and freshness registry entries
   - run `linear child-stream --pipeline docs-review` and record manifest or truthful fallback
3. Implementation:
   - add maintenance decision command/workflow
   - add stable decision JSON schema and report output
   - classify hard blockers, owned rolling debt, policy expiry/capacity, and current-diff/task-packet blockers
   - add scheduled/preflight workflow behavior or command wiring
   - update provider-worker gate behavior to consume the decision
4. Tests and docs:
   - accept/reject child test patch, then finish focused tests
   - update cohort guide and provider-worker validation guidance
   - add recurrence fixtures/evals for CO-175 and CO-184 shapes
5. Validation and handoff:
   - run focused tests and required repo gates
   - run manifest-backed standalone review under `FORCE_CODEX_REVIEW=1`
   - run explicit elegance/minimality pass
   - open/update PR, attach it to Linear, merge latest `origin/main`, run `pr ready-review`, refresh workpad, then transition to `In Review` only if clean

## Dependencies
- Existing docs freshness report API in `scripts/docs-freshness.mjs`
- Existing spec freshness behavior in `scripts/spec-guard.mjs`
- Existing rolling cohort policy in `docs/docs-catalog.json`
- Existing cohort guidance in `docs/guides/docs-freshness-cohorts.md`
- Provider-worker validation/handoff code and `codex.orchestrator.json`
- Bounded-review command-intent classifier entries for new validation scripts
- Linear helper commands for workpad, child lane, child stream, owner/follow-up issue, and PR attachment

## Validation
- Checks / tests:
  - `linear child-stream --pipeline docs-review`
  - focused docs freshness/spec/provider-worker tests
  - changed CLI/workflow command tests
  - review command classifier tests for new validation script targets
  - `node scripts/delegation-guard.mjs`
  - `node scripts/spec-guard.mjs --dry-run`
  - `npm run build`
  - `npm run lint`
  - `npm run test`
  - `npm run docs:check`
  - `npm run docs:freshness`
  - `npm run repo:stewardship`
  - `node scripts/diff-budget.mjs`
  - `FORCE_CODEX_REVIEW=1 npm run review`
  - `npm run pack:smoke` if CLI/package/review-wrapper/downstream-facing surfaces are touched
- Rollback plan:
  - revert the maintenance command/workflow/provider-gate changes together if the decision contract cannot stay fail-closed
  - keep docs packet and owner issue notes as traceability if implementation is split into follow-ups

## Risks & Mitigations
- Risk: provider-worker gates accidentally allow real current-task drift.
  - Mitigation: classify current changed paths/task packet drift as hard blockers and cover with focused tests.
- Risk: owner issue automation creates duplicate baseline issues.
  - Mitigation: key owner paths by cohort/batch decision and test/update one-owner behavior.
- Risk: rolling policy capacity logic is bypassed.
  - Mitigation: explicitly emit and test `block_policy_over_budget` for the CO-184 over-cap shape.
- Risk: docs freshness and spec guard diverge.
  - Mitigation: reuse catalog policy inputs and keep both fail-closed for missing/invalid/current drift.

## Approvals
- Reviewer: docs-review child stream succeeded at `.runs/linear-4ffb542b-28b2-4530-8630-b79357e306e2-co-188-docs-review-r2/cli/2026-04-15T01-34-49-094Z-6efb48b4/manifest.json`.
- Date: 2026-04-15.
