# ACTION_PLAN: Coordinator Symphony-Aligned Control Server Lifecycle Orchestration Boundary Extraction

## Steps

1. Register `1234` docs-first artifacts, task mirrors, and registry/index entries.
2. Run the deterministic docs-first gates (`spec-guard`, `docs:check`, `docs:freshness`).
3. Reinspect the control-server lifecycle layer and confirm the exact startup/close orchestration seam.
4. Extract the lifecycle orchestration boundary without reopening request/action dispatch or broader control families.
5. Close with focused lifecycle sequencing evidence and an explicit minimality review.

## Guardrails

- Do not widen into route/action dispatch, Telegram, Linear, or oversight controller families.
- Keep startup/close sequencing and auth/readiness persistence timing as the primary regression checks.
- Prefer one bounded lifecycle lane now; defer broader control follow-ons if the host layer still retains truthful policy ownership afterward.
