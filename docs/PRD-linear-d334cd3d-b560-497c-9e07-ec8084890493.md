# PRD - CO: stabilize ControlServer read-only refresh temp cleanup ENOTEMPTY flake

## Added by Bootstrap 2026-04-19

## Traceability
- Linear issue: `CO-261` / `d334cd3d-b560-497c-9e07-ec8084890493`
- Linear URL: https://linear.app/asabeko/issue/CO-261
- Task id: `linear-d334cd3d-b560-497c-9e07-ec8084890493`
- Canonical spec: `tasks/specs/linear-d334cd3d-b560-497c-9e07-ec8084890493.md`
- Source anchor: `ctx:sha256:ce789eed64ab33a58fd40f1d77f4e6b2752cfaa7e0b79fefd4c6e638bc4f1128#chunk:c000001`
- Source object id: `sha256:ce789eed64ab33a58fd40f1d77f4e6b2752cfaa7e0b79fefd4c6e638bc4f1128`
- Source payload: `.runs/linear-d334cd3d-b560-497c-9e07-ec8084890493-docs-packet/cli/2026-04-19T09-43-09-262Z-5b353e4d/memory/source-0/source.txt`
- Origin manifest: `.runs/linear-d334cd3d-b560-497c-9e07-ec8084890493-docs-packet/cli/2026-04-19T09-43-09-262Z-5b353e4d/manifest.json`
- Reported Core Lane evidence: main Core Lane run `24623956000` on commit `c382730e0e6364cbfeb736a1999fc160c98b03cf`

## Summary
- Problem Statement: `ControlServer.test.ts` can flake in the read-only refresh case `acknowledges read-only refresh requests without mutating control state` when test cleanup tries to remove a temporary run directory and hits `ENOTEMPTY` under `/tmp/control-server-*/.runs/task-0940/cli/run-1`. The failure is a temp cleanup / async lifecycle issue around the read-only refresh test shape, not evidence that the read-only refresh contract should mutate control state.
- Desired Outcome: identify and land the smallest owning fix for the temp cleanup / async lifecycle race so the read-only refresh test remains behaviorally strict, focused and stress validation pass locally, full `npm run test` passes, and a later main Core Lane run can be classified as pass/non-repro for commit lineage after the fix.

## User Request Translation (Context Anchor)
- User intent / needs (in your own words): create the docs-first packet for `CO-261` only so the parent lane can investigate the `ENOTEMPTY` cleanup flake in `ControlServer.test.ts`, make the narrowest owning repair, prove it with focused and stress validation, then use full-suite and main Core Lane evidence to classify the failure.
- Success criteria / acceptance:
  - `ControlServer.test.ts` case `acknowledges read-only refresh requests without mutating control state` no longer fails cleanup with `ENOTEMPTY` at `/tmp/control-server-*/.runs/task-0940/cli/run-1`
  - the read-only refresh request still returns the expected acknowledgement and refresh payload without mutating `control_seq` or `latest_action`
  - any asynchronous resource, pending write, server lifecycle, or temp-root ownership issue causing the cleanup race is explicitly owned and closed; the fix is not just timeout inflation or blind cleanup retry masking
  - focused validation covers the failing test and adjacent read-only refresh behavior
  - stress/focused validation repeatedly exercises the failing test shape
  - full `npm run test` passes before handoff
  - a later main Core Lane run records pass/non-repro classification against the `24623956000` / `c382730e0e6364cbfeb736a1999fc160c98b03cf` failure lineage
- Constraints / non-goals:
  - do not edit source or tests from this docs-only child lane
  - do not call Linear mutation helpers from this child lane
  - do not run full repo validation suites from this child lane
  - `CO-181` is a non-goal scope; this lane does not reopen retained/released refresh fairness or fresh Ready admission
  - `CO-150` is a non-goal scope; this lane does not reopen ProviderIssueHandoff snapshot-only retry failures

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `ControlServer.test.ts`
  - `acknowledges read-only refresh requests without mutating control state`
  - `ENOTEMPTY`
  - `/tmp/control-server-*/.runs/task-0940/cli/run-1`
  - main Core Lane run `24623956000`
  - commit `c382730e0e6364cbfeb736a1999fc160c98b03cf`
  - `CO-181`
  - `CO-150`
- Protected terms / exact artifact and surface names:
  - `ControlServer.test.ts`
  - `acknowledges read-only refresh requests without mutating control state`
  - `ENOTEMPTY`
  - `/tmp/control-server-*/.runs/task-0940/cli/run-1`
  - `control_seq`
  - `latest_action`
  - `/api/v1/refresh`
  - `/api/v1/state`
  - `npm run test`
  - `Core Lane`
  - `24623956000`
  - `c382730e0e6364cbfeb736a1999fc160c98b03cf`
  - `CO-181`
  - `CO-150`
- Nearby wrong interpretations to reject:
  - "make read-only refresh mutate control state so cleanup changes"
  - "solve by deleting temp directories more aggressively without owning the async lifecycle"
  - "inflate test timeout or add broad retry loops as the primary fix"
  - "reopen `CO-181` fair admission / retained-released refresh behavior"
  - "reopen `CO-150` ProviderIssueHandoff retry investigation"
  - "skip full `npm run test` because the focused test passed once"

## Parity / Alignment Matrix

| Surface | Current truth | Reference truth | Target truth |
| --- | --- | --- | --- |
| Read-only refresh behavior | The test expects `/api/v1/refresh` to acknowledge and re-read runtime state while leaving `control_seq` and `latest_action` unchanged. | Read-only refresh must not be converted into a control-state mutation path. | Behavior remains unchanged and explicitly covered by the same test case. |
| Temp run cleanup | Reported Core Lane evidence shows `ENOTEMPTY` under `/tmp/control-server-*/.runs/task-0940/cli/run-1`. | After `await server.close()` and test cleanup, the temp run root should be removable without leftover async writers or handles. | Cleanup is deterministic for the read-only refresh shape without masking active lifecycle leaks. |
| Async lifecycle ownership | A pending write, server lifecycle task, provider refresh promise, or runtime refresh path may still touch the temp run directory as cleanup starts. | Async work spawned by a test must be awaited, closed, or isolated before temp directory removal. | The smallest owning seam closes or drains the relevant lifecycle before cleanup. |
| Core Lane classification | Main Core Lane run `24623956000` failed on commit `c382730e0e6364cbfeb736a1999fc160c98b03cf`. | CI evidence should be tied to a concrete commit/run lineage. | Parent records focused/stress/full-suite evidence and a later main Core Lane pass/non-repro classification. |
| Adjacent issue scopes | `CO-181` and `CO-150` are nearby but separate control/provider lanes. | This issue owns only the temp cleanup / async lifecycle flake. | No implementation or validation claim depends on changing `CO-181` or `CO-150` behavior. |

## Not Done If
- `ControlServer.test.ts` can still fail with `ENOTEMPTY` under `/tmp/control-server-*/.runs/task-0940/cli/run-1`.
- The read-only refresh acknowledgement is preserved only by changing or weakening the no-control-state-mutation assertion.
- The repair is a broad timeout increase, blind retry, or force-delete workaround without identifying the async lifecycle owner.
- Focused validation passes once but no stress/focused repeat covers the reported flake shape.
- Full `npm run test` is not run by the parent implementation lane before PR handoff.
- The final closeout lacks a main Core Lane pass/non-repro classification tied back to run `24623956000` and commit `c382730e0e6364cbfeb736a1999fc160c98b03cf`.
- The implementation widens into `CO-181` retained/released admission fairness or `CO-150` ProviderIssueHandoff retry behavior.

## Goals
- Preserve the read-only refresh contract in `ControlServer.test.ts`.
- Remove the temp cleanup / async lifecycle flake that produces `ENOTEMPTY`.
- Keep the owning fix minimal and localized to the relevant lifecycle or test harness seam.
- Prove the fix with focused, stress/focused, full-suite, and later Core Lane evidence.
- Keep `CO-181` and `CO-150` explicitly out of scope.

## Non-Goals
- No implementation or test edits from this docs-only child lane.
- No Linear state, workpad, PR, or issue mutation from this child lane.
- No broad ControlServer lifecycle rewrite unless the parent proves the smallest owning fix requires it.
- No scheduler, provider admission, retained/released refresh fairness, or ProviderIssueHandoff retry redesign.
- No acceptance based only on retrying `rm(..., { recursive: true, force: true })` after `ENOTEMPTY`.

## Stakeholders
- Product: CO operators and maintainers relying on stable Core Lane validation.
- Engineering: ControlServer lifecycle, test harness, and CI owners.
- Design: N/A.

## Metrics & Guardrails
- Primary Success Metrics:
  - focused `ControlServer.test.ts` read-only refresh case passes
  - stress/focused repeated validation does not reproduce `ENOTEMPTY`
  - full `npm run test` passes on the parent implementation head
  - main Core Lane rerun reaches pass or a documented non-repro classification for this failure lineage
- Guardrails / Error Budgets:
  - no weakening of `control_seq` / `latest_action` immutability assertions
  - no broad timeout inflation as the owning fix
  - no destructive cleanup that hides active async work
  - no expansion into `CO-181` or `CO-150`

## User Experience
- Maintainers can trust the read-only refresh test to fail only on product behavior regressions, not temp cleanup races.
- CI reviewers see a clear evidence chain from reported Core Lane failure to focused reproduction/stress/full-suite validation.
- Parent lanes can classify future `ENOTEMPTY` recurrence as the same failure only when it matches the protected path and test name.

## Technical Considerations
- Architectural Notes:
  - `orchestrator/tests/ControlServer.test.ts` owns the failing test shape and temporary root cleanup.
  - `ControlServer.close()` and the `/api/v1/refresh` read-only lifecycle are the owning inspection points for pending async work.
  - The fix closes the real writer/handle by draining active/queued provider refresh work and flushing provider polling health persistence before temp cleanup can race `provider-intake-state.json`.
  - Adjacent read-only refresh behavior without provider handoff should remain covered because it shares the runtime refresh shape.
- Dependencies / Integrations:
  - `orchestrator/tests/ControlServer.test.ts`
  - `orchestrator/src/cli/control/controlServer.ts`
  - temporary run roots under `/tmp/control-server-*`
  - GitHub Actions Core Lane

## Open Questions
- Answered: the leftover directory can be caused by an unawaited provider polling health persistence write to `provider-intake-state.json`.
- Answered: the smallest fix lives in the production lifecycle close path, not broad test cleanup retry logic.
- Current local evidence includes 20 focused read-only refresh iterations and full-suite validation; later main Core Lane classification remains required after PR/CI evidence is available.

## Approvals
- Product: Linear issue `CO-261`
- Engineering: parent implementation validation and bounded standalone review completed locally; later main Core Lane classification pending
- Design: N/A
