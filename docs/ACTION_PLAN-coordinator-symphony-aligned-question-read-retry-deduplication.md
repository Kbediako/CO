# ACTION_PLAN - Coordinator Symphony-Aligned Question-Read Retry Deduplication

## Steps

1. Register `1074` docs-first from the `1073` next-slice note and record the live duplicate-retry seam in findings.
2. Add the smallest shared helper for pre-expiry retry-candidate tracking.
3. Apply that helper to both the authenticated `/questions` route and Telegram oversight `readQuestions()`.
4. Add focused regressions for API and Telegram read surfaces.
5. Run the lane validation bundle, record explicit overrides if the known full-test or review-wrapper issues recur, then sync mirrors and queue the next bounded Symphony-aligned slice.
