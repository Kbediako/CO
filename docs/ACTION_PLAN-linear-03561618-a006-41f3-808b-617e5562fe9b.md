# ACTION_PLAN - CO-278 stale spec-guard baseline maintenance

## Summary
- Goal: create the CO-278 docs-first packet and registry mirrors for the stale spec-guard baseline maintenance lane.
- Scope: packet files, task mirrors, `tasks/index.json`, `docs/TASKS.md`, and docs freshness registry rows for the new CO-278 surfaces only.
- Assumptions:
  - the parent prompt carries the authoritative issue shape for this child lane
  - parent source-0 reconciliation found only run metadata/instruction hashes and no additional CO-278 scope
  - parent owns the stale task spec edits, Linear state, workpad, PR lifecycle, and full validation

## Issue Readiness Gate
- Intent checksum / protected terms carried forward:
  - `CO-278`
  - `stale spec-guard baseline maintenance lane`
  - `nine stale task specs`
  - `spec-guard`
  - `last_review: 2026-03-21`
  - `Core Lane`
  - `Spec guard`
  - `31 days old`
  - `must be ≤30 days`
- Not done if:
  - the docs packet omits any of the nine stale task specs
  - the child lane edits the stale task specs
  - registry mirrors omit the new CO-278 packet/checklist files
  - the plan weakens `spec-guard` or authorizes blind date bumps
- Pre-implementation issue-quality review:
  - 2026-04-21: parent enforced `node scripts/spec-guard.mjs` reproduced exactly nine stale active specs, all at `last_review: 2026-03-21`.
  - 2026-04-21: micro-task path is not appropriate because correctness depends on exact stale path preservation, protected non-goals, and registry mirror parity.

## Milestones & Sequencing
1. Confirm the source payload availability and record parent reconciliation.
2. Reproduce the stale spec-guard baseline with enforced `node scripts/spec-guard.mjs`.
3. Create the CO-278 PRD, canonical TECH_SPEC, TECH_SPEC mirror, ACTION_PLAN, task checklist, and `.agent` mirror.
4. Register the new canonical spec and checklist in `tasks/index.json`.
5. Add a current CO-278 snapshot to `docs/TASKS.md`.
6. Add docs freshness registry rows only for the new CO-278 packet/checklist surfaces.
7. Validate edited JSON registries and scoped git diff.
8. Leave changes uncommitted for parent patch export.

## Parent-Owned Follow-On Plan
1. Parent reconciles source-0 payload content before handoff.
2. Parent runs docs-review for the CO-278 packet.
3. Parent reviews the nine stale task specs and updates only those whose content is still current.
4. Parent records rationale for every `last_review` refresh.
5. Parent reruns enforced `node scripts/spec-guard.mjs` and the normal parent-owned validation floor.

## Dependencies
- Linear issue `CO-278`
- Source anchor `ctx:sha256:28e32e675229fa7e7a8c27c47b1e4f350222c304df75cd53153a5fe4679b88eb#chunk:c000001`
- `scripts/spec-guard.mjs`
- `tasks/index.json`
- `docs/docs-freshness-registry.json`

## Validation
- Checks / tests:
  - enforced `node scripts/spec-guard.mjs` for baseline and post-refresh evidence
  - JSON parse check for `tasks/index.json`
  - JSON parse check for `docs/docs-freshness-registry.json`
  - scoped `git diff --stat`
- Rollback plan:
  - revert only the CO-278 packet and registry mirror edits if parent source reconciliation changes the issue shape before implementation

## Risks & Mitigations
- Risk: source-0 payload contains additional issue wording.
  - Mitigation: parent reconciliation verified source-0 contains only run metadata/instruction hashes and no additional CO-278 scope.
- Risk: docs packet appears to authorize blind stale spec date bumps.
  - Mitigation: packet repeatedly requires reviewed refresh rationale for the exact nine paths.
- Risk: registry edits expand beyond the child-lane file scope.
  - Mitigation: registry rows are limited to the six new CO-278 docs/checklist surfaces.

## Approvals
- Docs-first packet: bounded same-issue child lane, 2026-04-21
- Parent docs-review / implementation approval: pending
