# PRD - CO-255: restore truthful control-host admission after retained active and handoff_failed claims flatten queue state

## Traceability
- Linear issue: `CO-255` / `b1835ea5-3d28-493a-934e-1b38048270ce`
- Task id: `linear-b1835ea5-3d28-493a-934e-1b38048270ce`
- Canonical spec: `tasks/specs/linear-b1835ea5-3d28-493a-934e-1b38048270ce.md`
- Source anchor: `ctx:sha256:de1e154c0eeab0ff773e815641b72013b7de2f3a9f39ddc170e520646a5c300a#chunk:c000001`

## Summary
- Problem Statement: after rebuilding and relaunching `control-host` from `co-248-refresh-retry-rehydrated`, `co-status --format json` still stayed flat with `running=2`, `issues=2`, `max_allowed=3`, top-level `tracked.linear.identifier=CO-1`, and retained `provider_intake` claims for `CO-212`, `CO-217`, and `CO-219`.
- Desired Outcome: a rebuilt/relaunched host should either admit current truthful `Ready` issues when capacity is available or expose a specific retained-state/projection/admission diagnostic that explains why admission is still blocked.

## User Request Translation
- User intent / needs: finish CO-255 as a control-host admission repair, not as a manual intake cleanup or another CO-248 reopening. Preserve the observed stale `tracked.linear=CO-1` top-level truth and retained active/handoff_failed claim trio as the issue checksum.
- Success criteria / acceptance: reproduce or model the post-relaunch flat state, identify the remaining failure seam, land the narrowest fix, and add regression coverage or durable diagnostics that distinguish CO-255 from adjacent stale-claim and stale-projection lanes.
- Constraints / non-goals: do not manually edit `provider-intake-state.json`, do not manually move issues into `In Progress`, do not switch the shared root to `main` under active root-bound processes, and do not reopen CO-248 unless its exact checksum is re-proven.

## Intent Checksum
- Exact user wording / phrases to preserve: `post-relaunch stale tracked CO-1`, `retained active/handoff_failed claims`, `provider_intake.active_issue_identifiers`, `provider_intake.running_issue_identifiers`, `selected_claim`, `provider_issue_merge_closeout_action_required`, `co-status --format json`, `CO-212`, `CO-217`, `CO-219`, `CO-216`, `CO-238`, `CO-41`, `CO-223`, `CO-237`, `CO-254`.
- Protected terms / exact artifact and surface names: `control-host`, `provider-intake-state.json`, `tracked.linear`, `provider_intake`, `co-status --format json`, `providerIssueHandoff.ts`, `providerIntakeState.ts`, `controlRuntime.ts`, `observabilityReadModel.ts`, `CO STATUS`.
- Nearby wrong interpretations to reject:
  - this is only the narrow CO-248 plain `released/not_active` cached Blocked checksum
  - this is only the CO-223 stale top-level `tracked.linear` fallback issue
  - this is only the CO-214 stale merged-closeout residue issue
  - this is only a CO STATUS presentation bug
  - the fix should manually prune intake JSON or force Linear workflow state by hand

## Parity / Alignment Matrix

| Surface | Current Truth | Reference Truth | Target Truth |
| --- | --- | --- | --- |
| Top-level tracked issue | `tracked.linear.identifier` can stay stale `CO-1` after rebuild/relaunch. | CO-223 owns stale top-level tracked fallback as a standalone projection issue. | CO-255 must prevent stale top-level tracked truth from masking current provider-intake/admission truth, or emit a specific diagnostic if it remains separate. |
| Retained active claims | `provider_intake.active_issue_identifiers` remains `CO-212`, `CO-217`, `CO-219`; `running_issue_identifiers` remains `CO-212`, `CO-217`; `selected_claim` stays pinned to `CO-212`. | CO-248 owns a narrower plain `released/not_active` stale Blocked claim; CO-219 owns accepted dead-proof running projection; CO-214 owns stale merged/Done closeout residue. | Retained active/handoff_failed rows must not flatten current queue projection or free-capacity admission when live issue truth is no longer actively occupying capacity. |
| Merge closeout residue | `CO-219` is retained as `handoff_failed` with `provider_issue_merge_closeout_action_required`. | Current `Merging` closeout still needs action; stale terminal closeout residue should not look active forever. | The host must distinguish current merge-closeout action from stale residue and keep that distinction visible in diagnostics/projection. |
| Admission | Fresh `Ready` issues are not admitted despite apparent capacity. | CO-41 covers refresh-lifecycle stalls; CO-248 covers a specific stale rehydrated lifecycle checksum. | Free capacity should admit current `Ready` work through the normal control-host path, or the blocking gate should be explicit and separable from CO-41/CO-248/CO-223. |

## Not Done If
- post-relaunch `co-status --format json` can still stay pinned to stale `tracked.linear=CO-1`
- retained active/handoff_failed claims can still flatten the projected queue to `CO-212`, `CO-217`, `CO-219`
- free capacity still fails to admit newly truthful `Ready` work after a clean rebuild and relaunch
- the resulting fix cannot distinguish this retained-state/projection seam from CO-248, CO-41, CO-214, CO-219, or CO-223

## Goals
- Reproduce or model the post-build and relaunch flat state from a clean checkout on the intended host branch behavior.
- Identify whether the remaining failure is top-level tracked projection, retained claim selection, merge-closeout residue interaction, or another admission gate.
- Patch the narrowest truthful source seam so fresh `Ready` admission resumes when capacity is really free.
- Add focused regression coverage or durable diagnostics for the stale tracked plus retained active/handoff_failed combination.

## Non-Goals
- No manual mutation of intake JSON or ad hoc claim pruning.
- No manual Linear state movement as a substitute for provider-worker ownership.
- No broad control-host scheduler redesign.
- No shared-root branch switch to clear merge closeout while active root-bound processes may still exist.
- No reopening CO-248 unless current evidence re-proves that exact checksum.

## Stakeholders
- Product: CO operators relying on truthful local admission and `CO STATUS`.
- Engineering: control-host, provider-worker, intake-state, and observability maintainers.
- Design: not applicable.

## Metrics & Guardrails
- Primary Success Metrics: focused regression proves retained active/handoff_failed stale rows do not block current `Ready` admission; status/projection output carries explicit truth for the stale combination.
- Guardrails / Error Budgets: preserve real active workers, real merge-closeout action-required rows, CO-125 admission capacity constraints, and audit history in `provider-intake-state.json`.

## User Experience
- Personas: local CO operator watching `co-status --format json` and expecting fresh `Ready` work to be admitted when capacity exists.
- User Journeys: rebuild/relaunch control-host, inspect status and provider intake, see either new admitted Ready work or an explicit retained-state diagnostic instead of a flat stale queue.

## Technical Considerations
- Architectural Notes: source inspection should start at provider claim summarization, selected-run/status projection, and provider handoff admission gates.
- Dependencies / Integrations: Linear live workflow state, local control-host artifacts, provider-worker proof sidecars, and existing focused Vitest suites.

## Open Questions
- Is the stale `tracked.linear=CO-1` value only a presentation fallback here, or does it still feed the admission selection path?
- Does `CO-219` merge-closeout residue consume active capacity because of claim state, selected-claim ordering, or projection-only logic?

## Approvals
- Product: CO-255 issue body and workpad.
- Engineering: parent provider worker.
- Design: not applicable.
