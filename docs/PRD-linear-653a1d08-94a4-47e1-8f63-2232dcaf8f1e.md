# PRD - CO: stabilize provider-worker validation and review gating for long-running suites

## Added by Bootstrap 2026-04-10

## Traceability
- Linear issue: `CO-134` / `653a1d08-94a4-47e1-8f63-2232dcaf8f1e`
- Linear URL: https://linear.app/asabeko/issue/CO-134/co-stabilize-provider-worker-validation-and-review-gating-for-long

## Summary
- Problem Statement: `CO-117` feature work is blocked by broader provider-worker validation instability outside the changed files. The issue already carries four concrete observations: an `implementation-gate` child stream died while `npm run test` was still in flight, `tests/cli-frontend-test.spec.ts` passes in isolation so the older full-suite timeout there was not reproduced as a feature regression, `orchestrator/tests/ProviderLinearWorkerRunner.test.ts` reproduces a wider timeout or failure cluster in isolation, and the bounded standalone review wrapper previously failed closed on a `failed-boundary` outcome because the reviewer crossed into launching direct validation commands.
- Desired Outcome: reproduce the blocker on the current tree, classify whether the real owner is a stage-timeout contract, a true suite regression, review-wrapper boundary drift, or a combination, then restore a truthful machine-checkable path so provider-worker lanes can finish `implementation-gate` and standalone-review handoff without mixing unrelated validation repair into feature diffs.

## User Request Translation (Context Anchor)
- User intent / needs (in your own words): keep this as a validation-stability lane, not a continuation of CO-117 feature work. The lane must explain why provider-worker validation and pre-handoff review are unstable, land the smallest truthful repair or narrowed contract, and leave later feature lanes able to prove clean review handoff without ad hoc narrative exceptions.
- Success criteria / acceptance:
  - the blocking validation behavior is reproduced with saved manifest and log evidence
  - the implementation-gate failure is classified truthfully as stage timeout, suite regression, review-wrapper boundary drift, or a combination
  - `npm run test` and `implementation-gate` once again pass or fail with actionable, machine-checkable causes
  - provider-worker lanes can capture bounded standalone-review evidence without silent or ambiguous wrapper failures
- Constraints / non-goals:
  - do not widen this into additional control-host supervision feature work
  - do not relitigate the CO-117 product contract
  - do not replace bounded standalone review with permanent manual fallback
  - keep the fix bounded to the current validation and review-owner seams unless new evidence proves a different minimal owner

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `stabilize provider-worker validation and review gating`
  - `validation-stability lane`
  - `implementation-gate`
  - `npm run test`
  - `ProviderLinearWorkerRunner.test.ts`
  - `failed-boundary`
  - `long-running suites`
  - `machine-checkable explanation`
- Protected terms / exact artifact and surface names:
  - `CO-117`
  - `CO-134`
  - `implementation-gate`
  - `tests/cli-frontend-test.spec.ts`
  - `orchestrator/tests/ProviderLinearWorkerRunner.test.ts`
  - `scripts/run-review.ts`
  - `scripts/lib/review-execution-telemetry.ts`
  - `orchestrator/src/cli/services/commandRunner.ts`
  - `review/telemetry.json`
- Nearby wrong interpretations to reject:
  - "Just rerun the same failing gate and hope it passes."
  - "Fold more control-host functionality into this lane."
  - "Treat manual review fallback as a permanent substitute for a stable bounded review path."
  - "Assume the older `cli-frontend-test` timeout is still the owner without rechecking the current tree."

## Parity / Alignment Matrix
- Not applicable; this is a bounded validation and review-stability lane, not a parity migration.
- Current truth:
  - `codex.orchestrator.json` still runs `npm run test` and then a forced `npm run review` inside `implementation-gate`
  - `tests/cli-frontend-test.spec.ts` already bootstraps a temporary package root for `frontendTestingRunner`, so the older frontend-test failure may already be stale on current `HEAD`
  - `orchestrator/tests/ProviderLinearWorkerRunner.test.ts` is still the named broad provider-worker regression surface for this blocker cluster
  - bounded review outcome classification still flows through `scripts/lib/review-execution-telemetry.ts` and `orchestrator/src/cli/services/commandRunner.ts`
- Reference truth:
  - provider-worker feature lanes should be able to finish `implementation-gate` and pre-handoff standalone review with truthful terminal evidence
- Target truth / intended delta:
  - the current blocker is re-established on current `HEAD` with saved artifacts
  - the smallest owner fix or narrowed contract restores truthful terminal validation and review evidence
  - later feature lanes do not need unrelated validation or wrapper debugging in their diffs
- Explicitly out-of-scope differences:
  - new control-host supervision product behavior
  - broad repo-wide CI cleanup that is not required by the reproduced blocker
  - weakening bounded review enforcement for genuinely heavy review commands

## Not Done If
- `implementation-gate` still terminates `npm run test` without a clear root cause and fix.
- `ProviderLinearWorkerRunner.test.ts` still fails or times out without being classified and addressed.
- provider-worker lanes still need ad hoc manual review fallback because bounded review trips avoidable wrapper boundary failures.

## Goals
- Reproduce the blocking validation and review behavior on the current tree with saved evidence.
- Determine whether the live blocker is stage timeout, real suite failure, review-boundary drift, or a combination.
- Land the smallest truthful fix or narrowed contract that restores provider-worker validation and review evidence.
- Keep CO-117 feature scope separate from test-harness and review-wrapper repair.

## Non-Goals
- Adding new control-host supervision features.
- Reopening queue-management, merge-policy, or child-runtime parity work.
- Replacing the review wrapper with a permanent manual-review-only path.
- Broad performance or CI cleanup unrelated to the reproduced blocker.

## Stakeholders
- Product: CO operators and authors who need feature lanes like CO-117 to hand off cleanly once the feature diff is ready
- Engineering: provider-worker, implementation-gate, standalone-review wrapper, and long-suite test-harness maintainers
- Design: N/A

## Metrics & Guardrails
- Primary Success Metrics:
  - one or more saved manifests or focused logs show the blocker clearly enough to classify its owner
  - the final implementation-gate or focused validation rerun ends with truthful pass or fail semantics
  - the final standalone-review rerun records an explicit telemetry outcome without ambiguous wrapper interpretation
- Guardrails / Error Budgets:
  - keep feature work separate from validation and review repair
  - keep bounded review enforcement for genuinely out-of-contract heavy commands
  - do not label a stage timeout as a suite regression without evidence
  - do not label a true suite regression as a wrapper problem without evidence

## User Experience
- Personas:
  - provider-worker author preparing a feature lane for review handoff
  - operator auditing a failed or slow `implementation-gate` run
  - reviewer interpreting `review/telemetry.json` during provider-worker closeout
- User Journeys:
  - a provider-worker lane hits a long-running validation surface and gets a truthful stage result instead of an unclassified timeout
  - a reviewer sees whether bounded review ended cleanly, bounded-successfully, or on a real explicit boundary
  - a feature lane like CO-117 can cite the repaired validation/review path instead of carrying unrelated debugging

## Technical Considerations
- Architectural Notes:
  - `implementation-gate` is still pipeline-owned via `codex.orchestrator.json`, so the lane must first prove whether the problem lives in stage timeout settings, the test suite itself, or post-stage evidence interpretation
  - `tests/cli-frontend-test.spec.ts` already does explicit temp-package-root preparation, which is a concrete reason to revalidate that older failure before assuming it is still the active owner
  - review wrapper outcome semantics are already explicit in `scripts/lib/review-execution-telemetry.ts`; the live defect may instead be in what the wrapper treats as an avoidable heavy-command boundary for provider-worker handoff
  - `orchestrator/src/cli/services/commandRunner.ts` is the current stage closeout and review-evidence consumer, so any mismatch between terminal command status and review telemetry will likely surface there
- Dependencies / Integrations:
  - `codex.orchestrator.json`
  - `orchestrator/tests/ProviderLinearWorkerRunner.test.ts`
  - `tests/cli-frontend-test.spec.ts`
  - `scripts/run-review.ts`
  - `scripts/lib/review-execution-telemetry.ts`
  - `scripts/lib/review-execution-state.ts`
  - `scripts/lib/review-command-intent-classification.ts`
  - `orchestrator/src/cli/services/commandRunner.ts`

## Open Questions
- Does the current-tree `implementation-gate` blocker still reproduce as a stage timeout once the previously cited `cli-frontend-test` surface is rechecked on `ff613b6ab0f83d286383591dadc90c9b1d632845`?
- Is the current `failed-boundary` outcome caused by a real review-command contract violation, or by provider-worker review expectations that now need a narrower allowlist or handoff contract?

## Approvals
- Product: self-approved from the Linear issue scope and explicit acceptance criteria
- Engineering: pending `linear child-stream --pipeline docs-review`
- Design: N/A
