# PRD - CO: Umbrella Epic for Issue-Parent Orchestration Above Multiple Workers Per Issue

## Added by Bootstrap 2026-03-28

## Traceability
- Linear issue: `CO-14` / `62527426-2f74-4134-98e0-bb50c3da7913`
- Linear URL: https://linear.app/asabeko/issue/CO-14/co-umbrella-epic-for-issue-parent-orchestration-above-multiple-workers
- Branch: `linear/co-14-issue-parent-orchestration-umbrella`

## Summary
- Problem Statement: CO now has two real enabling inputs that did not exist when `CO-14` was originally framed: `CO-13` added bounded provider-worker child streams without weakening delegation guard, and the `0.117.0` / upstream-main canary showed that multi-agent-v2 plus richer app-server session surfaces materially lowered substrate risk and made the follow-on design more credible, while still keeping provider-worker supervision on the existing `codex exec` / `codex exec resume` seam. At the same time, Symphony still models one authoritative owner and session per issue, current CO still serializes issue ownership through the control-host plus one `provider-linear-worker`, and current parity work is already split across bounded slices such as `1303`, `1312`, `1313`, `1314`, `1315`, and `1318`. That makes `CO-14` real, but it also makes it a CO-specific extension that must preserve existing single-owner truth instead of being treated as a parity blocker, a parity restart, or a greenfield rewrite.
- Desired Outcome: land a docs-first umbrella packet that compares an issue-parent architecture against `CO-13` and the canary findings, defines authority, lineage, observability, and conflict rules for multiple same-issue lanes under one external owner, and records an explicit no-go gate until evidence proves the bounded child-stream seam is insufficient.
- Current Closeout Reality: the live `CO-14` branch remains docs-only and PR `#317` is green in GitHub, but local `npm run test` can still enter a post-suite quiet tail after the visible suite finishes. This lane therefore needs an explicit validation interpretation: green `Core Lane` plus zero actionable review threads is authoritative closeout evidence for the docs-only diff unless a concrete failing test appears.

## User Request Translation (Context Anchor)
- User intent / needs (in your own words): complete Linear issue `CO-14` by turning it into a truthful umbrella epic for issue-parent orchestration above multiple workers per issue, grounded in the required Symphony and CO baseline, but keep actual implementation deferred until current bounded evidence says the existing single-worker seam is not enough.
- Success criteria / acceptance:
  - docs-first packet explicitly compares the issue-parent design against `CO-13` and the `0.117/main` canary findings
  - the design defines lineage, observability, authority boundaries, and conflict rules for multiple worker lanes under one issue owner
  - the packet explains why a broader per-issue supervisor would ever be preferable to bounded child streams inside the existing provider-worker model
  - the packet stays truthful about Symphony alignment: informed by upstream constraints, but not claimed as a direct parity requirement
  - the issue lands as an umbrella or consolidation epic with explicit activation gates instead of an isolated architecture spike
  - closeout evidence distinguishes a real failing validation from the known local post-suite quiet tail so the docs-only PR is not blocked by stale operator interpretation
- Constraints / non-goals:
  - preserve one authoritative issue-level claim and running owner
  - preserve one externally authoritative workspace, workpad, thread, and handoff state machine
  - serialize Linear lifecycle and state mutations through the issue-parent owner
  - preserve per-issue workspace confinement and manifest lineage
  - do not weaken `delegation-guard`
  - do not treat this as required for useful autonomous Linear work today
  - do not implement the multi-worker supervisor in this lane
  - do not widen the default authority model to remote app-server control surfaces such as `thread/shellCommand`

## Goals
- Re-audit the authoritative Symphony, CO seam, and `0.117/main` canary baseline for `CO-14`.
- Record the current CO truth: one control-host claim path, one provider-worker issue owner, bounded child streams, and parent-centric observability.
- Define a future issue-parent architecture that preserves the single external issue owner while allowing subordinate lanes.
- State the activation criteria that would justify moving beyond `CO-13`.
- Stage follow-on work as an umbrella epic instead of a speculative rewrite ticket.

## Non-Goals
- Replacing the current `provider-linear-worker` path in this issue.
- Reframing `CO-14` as direct Symphony parity work.
- Changing current review, merge, or workpad state semantics in the live workflow.
- Adopting remote app-server command or watcher surfaces by default.

## Stakeholders
- Product: CO operator deciding whether broader same-issue parallelism is worth the complexity
- Engineering: Codex / CO maintainers
- Design: N/A

## Metrics & Guardrails
- Primary Success Metrics:
  - `CO-14` has a source-backed docs-first packet and no longer depends on vague architecture-language
  - the packet makes the `CO-13` versus issue-parent boundary explicit
  - the packet defines a concrete no-go or go-gate for eventual implementation
- Guardrails / Error Budgets:
  - keep the diff documentation-only in this lane
  - do not claim capabilities that current Symphony or current CO do not actually have
  - preserve the current authority boundary where only one issue owner mutates Linear lifecycle state
  - do not invent a branch-specific code fix when the remaining evidence points at operator-surface ambiguity rather than a CO-14 regression

## User Experience
- Personas:
  - CO operator deciding whether current bounded child streams are enough
  - CO maintainer planning future parent or child observability/control seams
  - reviewer auditing whether `CO-14` is real evidence-driven work or speculative drift
- User Journeys:
  - operator reads the packet and understands why `CO-13` remains the current path and what specific evidence would justify escalation
  - maintainer reads the packet and sees the required authority, lineage, workspace, and conflict constraints before touching runtime code
  - reviewer can audit the design against Symphony and current CO files without guessing at the underlying truth

## Technical Considerations
- Architectural Notes:
  - current control-host launch defaults already route provider starts into `provider-linear-worker`
  - current provider handoff already serializes claim, retry, continuation, and workspace cleanup through one issue owner
  - current provider-worker instructions already enforce one active workpad and review or merge handoff discipline
  - current observability models are parent-centric and already exclude child-stream manifests from pretending to be scheduler-owned provider runs
  - current parity foundations already live in bounded CO slices for autonomous intake and handoff, same-session continuation, authoritative runtime snapshot, authoritative retry state, post-worker retry ownership, and current Linear operational parity; `CO-14` must inherit those truths rather than reopen them as architecture debate
  - `CO-13` already established the bounded child-stream substrate that this issue must compare against instead of replacing by assumption
- Dependencies / Integrations:
  - `/Users/kbediako/Code/symphony/SPEC.md`
  - `/Users/kbediako/Code/symphony/elixir/README.md`
  - `/Users/kbediako/Code/symphony/elixir/WORKFLOW.md`
  - `/Users/kbediako/.codex/reports/2026-03-27-codex-0.117.0-co-alignment.md`
  - `AGENTS.md`
  - `tasks/specs/1303-coordinator-symphony-parity-linear-autonomous-intake-and-run-handoff.md`
  - `tasks/specs/1312-coordinator-symphony-same-session-continuation-after-normal-success.md`
  - `tasks/specs/1313-coordinator-symphony-authoritative-runtime-snapshot-observability.md`
  - `tasks/specs/1314-coordinator-symphony-authoritative-retry-state-and-attempts.md`
  - `tasks/specs/1315-coordinator-symphony-post-worker-retry-queue-ownership.md`
  - `tasks/specs/1318-coordinator-symphony-current-linear-operational-parity.md`
  - `orchestrator/src/cli/controlHostCliShell.ts`
  - `orchestrator/src/cli/control/providerIssueHandoff.ts`
  - `orchestrator/src/cli/providerLinearWorkerRunner.ts`
  - `orchestrator/src/cli/control/controlRuntime.ts`
  - `orchestrator/src/cli/control/observabilityReadModel.ts`
  - `docs/PRD-linear-488135bf-954e-4bd9-be7a-ad09d75f5f29.md`
  - `docs/TECH_SPEC-linear-488135bf-954e-4bd9-be7a-ad09d75f5f29.md`
  - `docs/TECH_SPEC-linear-b8a32c11-cfc6-4556-b783-f59b290fe67d.md`

## Open Questions
- If current evidence ever justifies implementation, should the first parent-managed child lanes stay on the current `exec` or `resume` seam with additive lineage and observability, or is that the point where a guarded app-server control seam becomes necessary? This packet intentionally leaves that as a future go-gate rather than making a premature runtime commitment.

## Approvals
- Product: Self-approved from the Linear issue scope and acceptance criteria
- Engineering: Docs-review completed with recorded override evidence
- Design: N/A

## Manifest Evidence
- Baseline audit artifact: `out/linear-62527426-2f74-4134-98e0-bb50c3da7913/manual/20260328T020804Z-baseline-audit.md`
- Delegated scout manifest: `/Users/kbediako/Code/CO/.runs/linear-62527426-2f74-4134-98e0-bb50c3da7913-baseline-scout/cli/2026-03-28T02-13-51-881Z-a7822430/manifest.json`
- Docs-review manifest: `/Users/kbediako/Code/CO/.runs/linear-62527426-2f74-4134-98e0-bb50c3da7913/cli/2026-03-28T02-25-01-848Z-cbb7ed48/manifest.json`
- Docs-review override: `out/linear-62527426-2f74-4134-98e0-bb50c3da7913/manual/20260328T022948Z-docs-review-override.md`
- Validation reconciliation note: `out/linear-62527426-2f74-4134-98e0-bb50c3da7913/manual/20260329T013711Z-validation-reconciliation.md`
