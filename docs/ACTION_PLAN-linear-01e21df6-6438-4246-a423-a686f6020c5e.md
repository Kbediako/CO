# ACTION_PLAN - CO: Add retrieval-stage competitive scoring and anti-dominance normalization

## Added by Bootstrap (refresh as needed)

## Summary
- Goal: land a bounded retrieval-policy slice that turns raw reward-only experience selection into declared competitive scoring with inspectable anti-dominance normalization.
- Scope:
  - register the CO-95 docs packet, mirrors, registry entries, and workpad source
  - run audited docs-review before implementation
  - extend prompt-pack manifests/loader with retrieval policy
  - implement competitive selection plus diagnostics in the Experience Store and manifest bootstrap
  - add focused tests, then run the normal validation and review gates
- Assumptions:
  - the current prompt consumers can keep using the existing `experiences` string array if manifest bootstrap also persists structured retrieval metadata separately

## Issue Readiness Gate
- Intent checksum / protected terms carried forward:
  - `competitive scoring`, `anti-dominance normalization`, retrieval-stage, controller-consumable, inspectable, repeated-source suppression, ranking changes
- Not done if:
  - selection stays plain top-K reward or recency
  - source penalties or exclusions remain opaque
  - repeated-source suppression is untested
  - the slice broadens into reward persistence, telemetry, or runtime continuity work
- Pre-implementation issue-quality review:
  - Current repo truth is narrower than “memory policy” in general. The gap is the experience retrieval seam: controller config only exposes slot count, the store only does raw reward ranking, and manifests only expose final strings without policy or diagnostics.

## Milestones & Sequencing
1) Register the `linear-01e21df6-6438-4246-a423-a686f6020c5e` docs packet, task mirrors, registry entries, and workpad source, then run the audited `docs-review` child stream and fold back any packet-only fixes.
2) Extend prompt-pack manifests and loader types with a versioned retrieval-policy contract.
3) Implement Experience Store competitive selection, anti-dominance normalization, manifest-visible selection metadata, and bounded diagnostics artifacts.
4) Add focused regressions, run the validation floor, complete standalone review plus an explicit elegance pass, and refresh the workpad with final policy and diagnostics evidence.

## Dependencies
- `.agent/prompts/prompt-packs/implementation/manifest.json`
- `.agent/prompts/prompt-packs/diagnostics/manifest.json`
- `packages/orchestrator/src/instructions/promptPacks.ts`
- `packages/orchestrator/src/instructions/loader.ts`
- `orchestrator/src/persistence/ExperienceStore.ts`
- `orchestrator/src/cli/run/manifest.ts`
- `orchestrator/src/cli/exec/experience.ts`
- `packages/shared/manifest/types.ts`
- `orchestrator/src/cli/services/orchestratorCloudPromptBuilder.ts`

## Validation
- Checks / tests:
  - audited `linear child-stream --pipeline docs-review`
  - focused retrieval-policy and ranking regressions
  - `node scripts/delegation-guard.mjs`
  - `node scripts/spec-guard.mjs --dry-run`
  - `npm run build`
  - `npm run lint`
  - `npm run test`
  - `npm run docs:check`
  - `npm run docs:freshness`
  - `node scripts/diff-budget.mjs`
  - `FORCE_CODEX_REVIEW=1 npm run review`
  - `npm run pack:smoke`
- Rollback plan:
  - keep the change additive so rollback removes retrieval-policy parsing plus manifest diagnostics without affecting stored experience persistence or prompt-pack source loading

## Risks & Mitigations
- Risk: provenance grouping is too weak or too strong for repeated-source suppression.
  - Mitigation: make the grouping rule explicit in policy/diagnostics and pin repeated-source behavior in focused tests.
- Risk: richer snippet metadata bloats prompt strings.
  - Mitigation: keep string snippets bounded and move the full explanation into the diagnostics artifact plus manifest summary.
- Risk: prompt-pack manifest expansion breaks existing stamped packs.
  - Mitigation: make retrieval policy additive with strict defaults and update the two in-repo packs together.

## Approvals
- Reviewer: `codex-orchestrator docs-review (clean-success)` via `.runs/linear-01e21df6-6438-4246-a423-a686f6020c5e-co-95-docs-review/cli/2026-04-09T08-42-51-393Z-481f65db/manifest.json`
- Date: 2026-04-09
