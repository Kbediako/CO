# ACTION_PLAN - Coordinator Symphony-Aligned Telegram Dispatch Read Adapter Extraction

## Steps

1. Register `1077` docs-first from the `1076` next-slice note and the Symphony thin-shell heuristic.
2. Extract the remaining Telegram dispatch-read adapter assembly into one control-local helper.
3. Keep `controlServer.ts` thin while preserving existing dispatch evaluation, audit emission, and Telegram dispatch payload semantics.
4. Add focused regressions for the extracted helper seam.
5. Run the lane validation bundle, sync mirrors, and queue the next bounded Symphony-aligned seam.
