# ACTION_PLAN - Coordinator Symphony-Aligned Standalone Review Untouched Helper Classification Parity

1. Register `1113` across PRD / TECH_SPEC / ACTION_PLAN / findings / task checklist / `.agent` mirror / `tasks/index.json` / `docs/TASKS.md` / docs freshness registry.
2. Extend `scripts/lib/review-execution-state.ts` so untouched adjacent review-owned helpers receive the same bounded classification parity as the existing standalone-review support surfaces.
3. Add focused `tests/review-execution-state.spec.ts` coverage for untouched helper parity and touched-helper preservation.
4. Add `tests/run-review.spec.ts` coverage only if needed to prove the live review behavior at the wrapper surface.
5. Run the docs-first guard bundle and sync the registration evidence.
