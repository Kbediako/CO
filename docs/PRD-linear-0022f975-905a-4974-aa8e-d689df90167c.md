# PRD - CO: reconcile Done issue mirrors and validation-only closeout provenance

## Traceability
- Linear issue: `CO-178` / `0022f975-905a-4974-aa8e-d689df90167c`
- Canonical task id: `20260414-linear-0022f975-905a-4974-aa8e-d689df90167c`
- Follow-up packet prefix: `linear-0022f975-905a-4974-aa8e-d689df90167c`
- Source issue: `CO-92` / `9eaac719-eecf-4e49-8d4e-4d793d9ff958`
- Parent workpad: `out/linear-0022f975-905a-4974-aa8e-d689df90167c/manual/workpad.md`

## Summary
- Problem Statement: Done Linear issues can leave local `docs/TASKS.md`, task mirror, `.agent/task` mirror, and closeout artifacts that still imply implementation, review, PR attachment, or validation is pending.
- Desired Outcome: sampled Done issues are classified against Linear, GitHub, and local artifact evidence, stale mirrors are reconciled without reopening completed functional work, no-PR validation-only closeouts have local provenance pointers, and a read-only checker prevents future silent pending terminal rows unless an explicit stale-mirror waiver exists.

## User Request Translation
- User intent / needs: audit terminal Done closeout provenance for completed CO issues and normalize local mirror truth so stale pending rows do not survive as ambiguous implementation gaps.
- Success criteria / acceptance: enumerate Done issues with stale terminal rows; classify sampled issues as stale mirror only, no-PR validation-only provenance gap, or true follow-up needed; reconcile sampled issues including `CO-92`, `CO-94`, `CO-78`, `CO-89`, `CO-43`, and `CO-62`; define and backfill no-PR validation-only local provenance for weak cases such as `CO-170`; add a read-only checker; document future terminal mirror closeout behavior.
- Constraints / non-goals: do not reopen completed implementation work without a fresh functional failure; do not duplicate `CO-172` manifest-summary truthfulness work; do not delete useful historical audit evidence; do not require every validation-only Done issue to have a PR; do not weaken docs freshness or archive policy.

## Intent Checksum
- Exact user wording / phrases to preserve: `Linear Done`, `docs/TASKS.md`, `task mirrors`, `.agent/task mirrors`, `tasks/index.json`, `Linear workpad`, `PR attachment`, `merged PR evidence`, `no-PR validation-only closeout`, `out/<linear-id>/manual/workpad.md`, `stale mirror waiver`.
- Protected artifacts / surfaces: Done state evidence from Linear, merged green PR evidence from GitHub, task and docs mirror checklist rows, `.agent/task` mirrors, local `out/` closeout pointers, and repo-local checker output.
- Nearby wrong interpretations to reject: only cleaning `CO-92` and `CO-94`; treating stale mirror debt as proof of failed implementation; reopening completed implementation scope without a fresh repro; hiding stale mirrors by weakening docs policies.

## Parity / Alignment Matrix

| Surface | Current truth | Reference truth | Target truth / intended delta |
| --- | --- | --- | --- |
| Done implementation issues | Sampled Done issues can have merged PRs while local mirrors still contain pending implementation, review, PR attachment, or validation rows. | Linear Done plus merged PR evidence is terminal closeout evidence unless a fresh functional failure proves otherwise. | Local mirrors either mark terminal closeout complete with evidence or carry an explicit stale-mirror waiver that the checker can audit. |
| Validation-only Done issues | Some Done issues legitimately have no PR or canonical task packet, leaving weak local provenance such as Linear-only comments. | Validation-only closeout can be terminal without a PR, but it still needs a local closeout pointer. | No-PR validation-only Done issues have local `out/<linear-id>/manual/workpad.md` or equivalent pointer evidence recorded in the provenance manifest. |
| Stale mirror prevention | Agents can miss pending terminal rows because task mirrors are historical docs. | Terminal closeout rows must not silently remain pending after Done. | A read-only checker reports or fails on pending terminal closeout rows without explicit waiver. |
| Future provider-worker closeout | Merge closeout instructions already record shared checkout sync and Linear transitions. | Future agents should update terminal mirrors after merge without duplicating manifest-summary truthfulness checks. | Documentation names the mirror/provenance update step and keeps `CO-172` manifest summary handling out of scope. |

## Not Done If
- Done issues can still have local task/docs mirrors saying implementation, review, PR attachment, or validation remains pending without an explicit waiver.
- `CO-92`, `CO-94`, `CO-78`, `CO-89`, `CO-43`, and `CO-62` are not used as fixtures or manual verification cases.
- No-PR validation-only Done issues remain dependent on Linear-only comments with no local closeout pointer.
- The solution treats stale mirror debt as proof of failed implementation without checking PR or Linear evidence.
- The prevention checker cannot run read-only in CI or by agents.

## Goals
- Build a sampled Done issue provenance inventory.
- Reconcile or waive stale local terminal rows with evidence.
- Establish a local provenance requirement for no-PR validation-only Done issues.
- Add a read-only checker and focused tests that future agents can run.
- Document the terminal mirror update contract for provider-worker merge closeout.

## Non-Goals
- Do not reopen completed implementation scope without a fresh functional failure.
- Do not duplicate `CO-172` manifest-summary truthfulness work.
- Do not delete historical audit evidence.
- Do not require a GitHub PR for every validation-only Done issue.
- Do not weaken docs freshness, task archive, or implementation-doc archive policies.

## Stakeholders
- Product: CO operator and Linear issue owner.
- Engineering: provider-worker, docs/task mirror, GitHub PR closeout, and repo validation maintainers.
- Review: docs-review, implementation-gate, and PR reviewers.

## Metrics & Guardrails
- Primary Success Metrics: checker reports zero unwaived pending terminal rows for sampled Done issues; no-PR validation-only samples have local closeout pointers; reconciled mirrors preserve evidence rather than deleting history.
- Guardrails / Error Budgets: zero functional reopenings without repro evidence, zero live Linear-only closeouts for validation-only samples, zero policy weakening, and zero destructive archival of useful audit records.

## User Experience
- Personas: provider worker closing a merged issue, reviewer auditing terminal evidence, operator investigating stale Done provenance, and future agent running the checker before handoff.
- User Journeys: an agent runs the checker, sees issue-level classifications and evidence pointers, reconciles stale rows or adds an explicit waiver, and records terminal closeout truth in the relevant task mirrors before review handoff.

## Technical Considerations
- Architectural Notes: use a checked-in provenance manifest for sampled Done issues and waivers, a read-only checker that scans local markdown mirrors for terminal pending rows, and focused tests with fixture mirrors.
- Dependencies / Integrations: local filesystem, optional GitHub/Linear evidence gathered during implementation, existing task/docs mirror conventions, and normal repo validation gates.

## Open Questions
- Resolved 2026-04-14: sampled implementation Done issues `CO-92`, `CO-94`, `CO-78`, `CO-89`, `CO-43`, and `CO-62` are stale mirror only based on Linear Done plus merged PR/check evidence, and their pending rows are covered by explicit hash-bound stale-mirror waivers.
- Resolved 2026-04-14: `CO-170` has enough validation-only no-PR closeout evidence to backfill a local closeout pointer without reopening functional scope.

## Approvals
- Product: self-approved from CO-178 issue text and provider-worker handoff.
- Engineering: docs-review r4 clean before implementation; manifest-backed implementation review clean after P2 fixes; explicit elegance/minimality pass complete.
- Design: not applicable.
