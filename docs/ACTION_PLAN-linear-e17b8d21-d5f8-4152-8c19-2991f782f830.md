# ACTION_PLAN - CO-317 fresh-discovery admission backfill after duplicate or terminal cleanup frees capacity

## Summary
- Goal: close `CO-317` by making cleanup-freed provider capacity visible to bounded `fresh_discovery` without requiring unrelated relaunch or queue churn.
- Scope: docs-first packet and `tasks/index.json` linkage in this child lane; parent-owned diagnosis, implementation, focused tests, validation, workpad, and PR lifecycle.
- Assumptions:
  - duplicate-cleanup and terminal-cleanup generation/execution are already owned by adjacent lanes
  - the remaining bug is in provider-intake/admission refresh after truthful cleanup frees a slot
  - `selected_claim` may be either a participant in or a symptom of the stale admission state

## Issue Readiness Gate
- Intent checksum / protected terms carried forward:
  - preserve `provider-intake-state.json`, `provider_intake`, `selected_claim`, `fresh_discovery`, `admission-budget`, `duplicate_cleanup`, `terminal_cleanup`, `providerIssueHandoff.ts`, `providerIntakeState.ts`, and `co-status --format json`
- Not done if:
  - cleanup frees real capacity but discovery still does not backfill the slot
  - the packet drifts into cleanup redesign or generic queue redesign
  - the only answer remains relaunch or manual state cleanup
- Pre-implementation issue-quality review:
  - accepted scope is the cleanup-to-admission backfill seam only: provider intake, fresh discovery, and admission-budget or `selected_claim` refresh after cleanup frees capacity

## Milestones & Sequencing
1. Child lane creates the `CO-317` PRD, canonical TECH_SPEC, TECH_SPEC mirror, ACTION_PLAN, task checklist, and `tasks/index.json` entry within the declared file scope.
2. Parent reviews the packet against the authoritative issue workspace and confirms whether the missing backfill is an admission-budget refresh seam, a `selected_claim` refresh seam, or a shared invalidation seam.
3. Parent reproduces or models one cleanup-freed-capacity case for duplicate cleanup and one for terminal cleanup, or proves a shared predicate covers both.
4. Parent patches the smallest provider-intake/fresh-discovery seam so the newly freed slot is visible to bounded discovery without over-admission.
5. Parent adds focused regression coverage for cleanup-freed capacity plus any required intake/read-model alignment coverage.
6. Parent runs scoped validation first, then the normal review/elegance/PR flow in the authoritative workspace.

## Dependencies
- `orchestrator/src/cli/control/providerIssueHandoff.ts`
- `orchestrator/src/cli/control/providerIntakeState.ts`
- `orchestrator/src/cli/control/observabilityReadModel.ts`
- `provider-intake-state.json`
- `co-status --format json`

## Validation
- Checks / tests:
  - child lane: scoped docs/JSON and diff checks only
  - parent lane: focused Vitest for provider handoff cleanup-freed-capacity behavior
  - parent lane: focused intake/read-model tests if `selected_claim` or summary refresh changes
  - parent lane: `node scripts/spec-guard.mjs --dry-run`
  - parent lane: docs-review and required repo validation after patch import
- Rollback plan:
  - revert the cleanup-to-admission refresh change if it over-admits or reintroduces cleaned issues as active occupancy
  - preserve existing cleanup generation/execution semantics as rollback baseline

## Risks & Mitigations
- Risk: freed-slot backfill over-admits while real queued or active work still exists.
  - Mitigation: require live occupancy proof to remain authoritative over cleaned summary state.
- Risk: the fix only updates `selected_claim` wording and leaves discovery behavior unchanged.
  - Mitigation: require focused regression proof that cleanup-freed capacity actually triggers backfill.
- Risk: the lane broadens into cleanup-feature or queue-policy redesign.
  - Mitigation: keep the implementation seam inside provider intake, slot accounting, and bounded `fresh_discovery`.

## Approvals
- Reviewer: pending parent docs-review and implementation validation
- Date: 2026-04-24
