# PRD - Control host: reclaim Ready issues suppressed by plain released/not_active claims that retain completed blockers

## Added by Bootstrap 2026-04-17

## Traceability
- Linear issue: `CO-212` / `31794e73-bc33-44b7-96d0-41334119a038`
- Linear URL: https://linear.app/asabeko/issue/CO-212
- Task id: `20260417-linear-31794e73-bc33-44b7-96d0-41334119a038`
- Canonical spec: `tasks/specs/linear-31794e73-bc33-44b7-96d0-41334119a038.md`
- Shared source 0 anchor: `ctx:sha256:0273f7bcf74c254d8b96f059f16eff76e5f6c36b6d2de134eee0e6abe85db191#chunk:c000001`
- Source payload: `.runs/linear-31794e73-bc33-44b7-96d0-41334119a038-co212-docs-packet/cli/2026-04-17T04-02-55-450Z-76be388e/memory/source-0/source.txt`
- Origin manifest: `.runs/linear-31794e73-bc33-44b7-96d0-41334119a038-co212-docs-packet/cli/2026-04-17T04-02-55-450Z-76be388e/manifest.json`
- Parent issue packet: `out/linear-31794e73-bc33-44b7-96d0-41334119a038/manual/workpad.md`

## Summary
`CO-212` closes the remaining Ready reclaim gap for plain `released` / `provider_issue_released:not_active` claims that still retain completed blocker history. A cached CO-196-like claim can keep `state=released`, `reason=provider_issue_released:not_active`, cached `issue_state=Blocked`, stale `issue_updated_at`, and `issue_blocked_by=[CO-207 Done/completed]` even after the live tracked issue is `Ready` / `unstarted` with newer `updated_at=2026-04-17T03:33:27.936Z` and no non-terminal blockers. The desired outcome is a minimal reclaim fix through the normal control-host path: completed blockers stay preserved for audit, but they no longer suppress eligibility once current live issue truth says the issue is runnable again.

## User Request Translation
- Add a focused regression for the cached CO-196-like blocked-claim shape: `state=released`, `reason=provider_issue_released:not_active`, cached `issue_state=Blocked`, stale `issue_updated_at`, and `issue_blocked_by=[CO-207 Done/completed]`.
- Treat live tracked issue truth as authoritative when it is `Ready` / `unstarted`, has newer `updated_at=2026-04-17T03:33:27.936Z`, and has no non-terminal blockers.
- Make refresh/fresh discovery persist fresh Ready metadata and reclaim/launch through the normal control-host path without manual worker start.
- Preserve completed blockers in audit history while preventing them from suppressing eligibility.
- Keep focused tests on provider handoff plus `co-status --format json` runtime/status projection.

## Intent Checksum
- Protected terms and surfaces:
  - `providerIssueHandoff.ts`
  - `providerLinearWorkflowStates.ts`
  - `provider-intake-state.json`
  - `co-status --format json`
  - `state=released`
  - `reason=provider_issue_released:not_active`
  - cached `issue_state=Blocked`
  - stale `issue_updated_at`
  - `issue_blocked_by`
  - `issue_blocked_by=[CO-207 Done/completed]`
  - `Ready`
  - `unstarted`
  - `updated_at=2026-04-17T03:33:27.936Z`
  - `fresh_discovery`
  - `CO STATUS`
- Nearby wrong interpretations to reject:
  - deleting completed blocker history from `provider-intake-state.json` or `issue_blocked_by`
  - treating this as a generic non-terminal blocker policy rewrite
  - fixing only `co-status --format json` display while reclaim stays suppressed
  - manually starting a worker instead of reclaiming through the normal control-host path
  - broad scheduler, polling, or request-burn changes
  - broad workflow-state redesign outside the exact completed-blocker distinction

## Parity / Alignment Matrix

| Surface | Current | Target |
| --- | --- | --- |
| Cached plain released/not_active claim | A row can stay `released` / `provider_issue_released:not_active` with cached `issue_state=Blocked`, stale `issue_updated_at`, and completed blocker history. | The row remains auditable, but completed-only blocker history no longer blocks reclaim once live issue truth is newer and runnable. |
| Live tracked issue truth | The issue can already be `Ready` / `unstarted` with no non-terminal blockers. | Live Ready/unstarted truth becomes the reclaim signal through the existing control-host path. |
| Completed blocker evidence | `issue_blocked_by=[CO-207 Done/completed]` can still suppress eligibility indirectly through stale cached state. | Completed blockers remain visible for audit but do not veto eligibility on their own. |
| Control-host reclaim | Refresh/fresh discovery can stay fail-closed on stale blocked cache. | Refresh/fresh discovery persists fresh Ready metadata and reclaims/launches without manual worker start. |
| CO STATUS/runtime coverage | Tests can miss the reclaim/status interaction after the metadata refresh. | Focused provider handoff and runtime/status projection coverage keep reclaim truth and status truth aligned. |

## Acceptance Criteria
- Add a regression with cached CO-196-like claim: `state=released`, `reason=provider_issue_released:not_active`, cached `issue_state=Blocked`, stale `issue_updated_at`, and `issue_blocked_by=[CO-207 Done/completed]`.
- Live tracked issue is `Ready` / `unstarted`, newer `updated_at=2026-04-17T03:33:27.936Z`, and has no non-terminal blockers.
- Refresh/fresh discovery persists fresh Ready metadata and launches/reclaims through the normal control-host path without manual worker start.
- Completed blockers remain preserved for audit but do not suppress eligibility.
- Focused tests cover provider handoff and CO STATUS/runtime projection.

## Non-Goals
- No manual worker start as the fix.
- No deletion of `provider-intake-state.json` history or `issue_blocked_by` audit evidence.
- No broad rewrite of blocker semantics, workflow-state naming, or scheduler behavior.
- No status-only/UI-only repair that leaves reclaim suppressed.
- No child-lane code or test edits.

## Not Done If
- A cached plain `released` / `provider_issue_released:not_active` claim still suppresses a live `Ready` / `unstarted` issue only because its retained blockers are already completed.
- Live issue truth is newer and blocker-free, but refresh/fresh discovery still does not reclaim through the normal control-host path.
- Completed blockers disappear from audit history instead of remaining preserved.
- The fix depends on manual worker start, manual state cleanup, or broad workflow-state changes.
- Focused provider handoff and CO STATUS/runtime projection coverage do not exist for the issue shape.

## Metrics & Guardrails
- Primary success metrics:
  - the cached completed-blocker claim shape is reclaimable through normal control-host flow once live issue truth is Ready/unstarted and newer
  - completed blockers remain available in audit surfaces
  - focused provider handoff and runtime/status projection tests cover the shape
- Guardrails:
  - non-terminal blockers must remain blocking
  - audit history must not be deleted or silently rewritten away
  - no manual worker launch is required to satisfy the fix
  - status coverage must stay consistent with handoff/reclaim truth

## User Experience
- Operators can move an issue back to `Ready` and expect normal control-host reclaim when the only remaining blocker evidence is already completed.
- Audit readers can still see which blocker previously held the issue without treating that history as current suppression.
- Parent maintainers can validate reclaim truth and `CO STATUS` truth from the same focused regression slice.

## Technical Considerations
- The minimal fix must stay within the protected seams: `providerIssueHandoff.ts` for reclaim/persist logic, `providerLinearWorkflowStates.ts` for workflow/blocker classification, `provider-intake-state.json` for retained audit history, and `co-status --format json` runtime/status projection coverage.
- Completed blocker history should be treated as audit evidence after live issue truth is refreshed, not as an eligibility veto by itself.
- Fresh Ready metadata should be persisted before reclaim/launch so later status/debug surfaces reflect the corrected truth.
- The runtime/status coverage should prove the reclaim shape without widening into a general workflow-state redesign.

## Open Questions
- Should the parent implementation classify completed-only blocker lists directly in `providerIssueHandoff.ts`, or should it thread a narrower helper from `providerLinearWorkflowStates.ts` so reclaim and runtime/status tests share the same blocker truth?
- Does the runtime/status projection need any additional debug marker for preserved completed blockers, or is unchanged audit history enough for this slice?

## Approvals
- Product: Linear issue `CO-212`
- Engineering: pending parent docs-review and implementation validation
- Design: N/A
