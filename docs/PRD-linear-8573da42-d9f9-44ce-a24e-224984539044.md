# PRD - CO-523 strict spec-guard stale last_review scope

## Traceability
- Linear issue: `CO-523` / `8573da42-d9f9-44ce-a24e-224984539044`
- Linear URL: https://linear.app/asabeko/issue/CO-523
- Task id: `linear-8573da42-d9f9-44ce-a24e-224984539044`
- Canonical spec: `tasks/specs/linear-8573da42-d9f9-44ce-a24e-224984539044.md`
- TECH_SPEC mirror: `docs/TECH_SPEC-linear-8573da42-d9f9-44ce-a24e-224984539044.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-8573da42-d9f9-44ce-a24e-224984539044.md`
- Task checklist: `tasks/tasks-linear-8573da42-d9f9-44ce-a24e-224984539044.md`
- `.agent` mirror: `.agent/task/linear-8573da42-d9f9-44ce-a24e-224984539044.md`
- Source anchor: `ctx:sha256:db68f6a7917841a63197e4457e7bcfe6bcb4009b7eba246a8880f864265816f7#chunk:c000001`
- Source payload note: the referenced `.runs/.../memory/source-0/source.txt` path was absent in this child checkout, so this packet uses the parent-provided CO-523 contract, source anchor, protected terms, and child-lane instructions as the authoritative source.
- Child lane manifest: `.runs/linear-8573da42-d9f9-44ce-a24e-224984539044-co523-docs-packet/cli/2026-05-12T18-52-17-405Z-7c08766d/manifest.json`

## Summary
- Problem Statement: `CO-523` must keep `strict spec-guard` behavior intact while isolating stale `last_review` debt that can affect the `Core Lane` signal for `CO-522` / `PR #795`. The parent lane must not solve that signal by weakening `node scripts/spec-guard.mjs`, blindly bumping dates, deleting historical packet evidence, or widening into unrelated `docs/design/specs` or `tasks/specs` rows.
- Desired Outcome: create a docs-first packet that preserves the exact stale baseline scope and non-goals so the parent can repair only the evidence-backed rows, keep `node scripts/spec-guard.mjs` fail-closed, and hand off `CO-523` without hiding or deleting historical evidence.

## User Request Translation
- User intent / needs: bootstrap the CO-523 docs packet only, preserving the parent issue contract around `strict spec-guard`, stale `last_review` rows, `Core Lane` evidence on `CO-522` / `PR #795`, and the distinction between source spec repair and validation-policy weakening.
- Success criteria / acceptance:
  - the six scoped packet files exist and are internally consistent
  - protected terms appear across the packet: `strict spec-guard`, `node scripts/spec-guard.mjs`, `last_review`, `Core Lane`, `CO-522`, `PR #795`, `docs/design/specs`, `tasks/specs`, `stale baseline`, `no blind bumps`, `no deletion`, and `no weakening`
  - the packet says parent-owned repair must use exact row evidence before any `last_review` or status change
  - the packet keeps `docs/design/specs`, `tasks/specs`, `tasks/index.json`, `docs/TASKS.md`, `docs/docs-freshness-registry.json`, `scripts/spec-guard.mjs`, Linear state, GitHub state, PR lifecycle, and workpad lifecycle outside this child lane
  - validation is scoped to protected-term and diff hygiene checks over the six touched files
- Constraints / non-goals:
  - do not call Linear or GitHub
  - do not edit `tasks/index.json`, `docs/TASKS.md`, `docs/docs-freshness-registry.json`, `scripts/spec-guard.mjs`, `docs/design/specs`, `tasks/specs` stale source rows outside this new canonical CO-523 packet, or any implementation/test files
  - do not run full repo validation suites from this child lane
  - do not broaden from packet creation into source-row repair, registry repair, or PR feedback handling

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `strict spec-guard`
  - `node scripts/spec-guard.mjs`
  - `last_review`
  - `Core Lane`
  - `CO-522`
  - `PR #795`
  - `docs/design/specs`
  - `tasks/specs`
  - `stale baseline`
  - `no blind bumps`
  - `no deletion`
  - `no weakening`
- Protected terms / exact artifact and surface names:
  - `strict spec-guard`
  - `node scripts/spec-guard.mjs`
  - `last_review`
  - `Core Lane`
  - `CO-522`
  - `PR #795`
  - `docs/design/specs`
  - `tasks/specs`
  - `stale baseline`
- Nearby wrong interpretations to reject:
  - a broad date refresh across stale specs is acceptable
  - stale `last_review` debt can be fixed with `no blind bumps`
  - deleting packet/spec rows is acceptable because the rows are old
  - weakening or bypassing `node scripts/spec-guard.mjs` is acceptable to make `Core Lane` green
  - the child lane should update registries, source specs, Linear, GitHub, or PR state
  - `CO-522` / `PR #795` implementation work should be changed from this docs packet lane

## Parity / Alignment Matrix

| Surface | Current truth | Reference truth | Target truth | Explicitly out-of-scope differences |
| --- | --- | --- | --- | --- |
| `node scripts/spec-guard.mjs` | Parent-provided issue contract says the lane is about `strict spec-guard` stale `last_review` scope. | `spec-guard` is a fail-closed validation gate for stale active specs. | Parent keeps `node scripts/spec-guard.mjs` unchanged and repairs only evidence-backed stale rows. | No `scripts/spec-guard.mjs` edits from this child lane. |
| `Core Lane` for `CO-522` / `PR #795` | Parent-provided context ties the stale baseline concern to `Core Lane` evidence on `CO-522` / `PR #795`. | `Core Lane` should distinguish current PR regressions from pre-existing stale baseline debt. | Parent records whether the blocker is exact stale baseline debt and handles only that scope. | No GitHub check reads, PR edits, or PR lifecycle handling by this child lane. |
| `docs/design/specs` and `tasks/specs` | These are the source families named by the issue contract. | Source spec rows are reviewed or reclassified with row-level evidence, not mass-updated. | Parent changes only the exact stale rows after evidence, with `no blind bumps`, `no deletion`, and `no weakening`. | This child lane adds only the new CO-523 canonical packet under `tasks/specs`; it does not edit existing stale source rows. |
| Historical packet evidence | Old packet/spec rows may be stale but still carry lineage. | Historical evidence stays available unless a separate archive/reclassification contract authorizes a change. | Parent preserves lineage while fixing the stale baseline truthfully. | No deletion, archive sweep, or docs freshness registry rewrite in this child lane. |

## Not Done If
- The CO-523 packet files are missing or incomplete.
- The packet omits `strict spec-guard`, `node scripts/spec-guard.mjs`, `last_review`, `Core Lane`, `CO-522`, `PR #795`, `docs/design/specs`, `tasks/specs`, or `stale baseline`.
- The packet permits `no blind bumps` to be ignored, allows `no deletion` to be bypassed, or accepts `no weakening` of `node scripts/spec-guard.mjs`.
- The packet implies this child lane should edit `tasks/index.json`, `docs/TASKS.md`, `docs/docs-freshness-registry.json`, `scripts/spec-guard.mjs`, stale source rows under `docs/design/specs` or `tasks/specs`, Linear, GitHub, or PR state.
- Parent cannot tell which work is packet-only versus source-row/validation/lifecycle work.

## Goals
- Create the six CO-523 docs-first packet files.
- Preserve the protected terms and wrong-interpretation guardrails.
- Make the parent-owned repair path explicit: exact stale row evidence first, then targeted update or reclassification.
- Keep `strict spec-guard` and `node scripts/spec-guard.mjs` fail-closed.

## Non-Goals
- No `node scripts/spec-guard.mjs` weakening, bypass, skip, or policy edit.
- No blind `last_review` bumps.
- No deletion of historical docs, packet files, specs, or registry lineage.
- No source-row edits in `docs/design/specs` or existing `tasks/specs`.
- No changes to `CO-522` / `PR #795` implementation or GitHub lifecycle.
- No Linear mutation, GitHub mutation, workpad update, PR creation, or PR review handling.
- No full validation suite from this child lane.

## Metrics & Guardrails
- Primary Success Metrics:
  - all six scoped packet files exist
  - protected-term scan finds every required term across the packet
  - scoped diff contains no files outside the declared child-lane file scope
  - whitespace check passes for the six touched files
- Guardrails / Error Budgets:
  - zero edits outside the six declared files
  - zero source-row edits outside the new CO-523 canonical packet
  - zero Linear or GitHub mutations
  - zero guard policy changes
  - zero deletion or blind date bump guidance

## Technical Considerations
- Architectural Notes:
  - CO-523 is a docs-first packet and source-scope discipline lane, not a guard-policy lane
  - parent must preserve the distinction between stale baseline classification and `CO-522` / `PR #795` implementation correctness
  - parent must use exact path-level evidence before changing any stale `last_review` row
- Dependencies / Integrations:
  - `node scripts/spec-guard.mjs`
  - `Core Lane`
  - `CO-522`
  - `PR #795`
  - `docs/design/specs`
  - `tasks/specs`
  - parent-owned Linear and GitHub evidence collection

## Fallback / Refactor Decision
- Applies to fallback, compatibility, legacy, stale, cached, break-glass, or minor-seam behavior? `Yes`, because the lane is explicitly about stale baseline handling.

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `strict spec-guard` stale `last_review` baseline | Treating stale `docs/design/specs` or `tasks/specs` rows as a tolerated baseline or clearing them by blind refresh/deletion. | `remove fallback` | CO-523 parent lane | `Core Lane` for `CO-522` / `PR #795` needs classification against stale baseline debt. | Parent to derive from exact stale row evidence. | 2026-05-12 | 30 days maximum for docs freshness/control-host adjacent stale-baseline handling. | Exact stale rows are reviewed, refreshed, or reclassified with evidence; `node scripts/spec-guard.mjs` remains strict. | Parent-owned `node scripts/spec-guard.mjs --dry-run`, scoped source-row evidence, and review handoff. |

## Open Questions
- Parent must supply the exact stale `docs/design/specs` and `tasks/specs` row list before implementation.
- Parent must record the `Core Lane` evidence that ties the stale baseline to `CO-522` / `PR #795`.
- Parent must decide, row by row, whether the correct action is active review refresh or terminal/inactive reclassification.

## Approvals
- Docs packet child lane: produced in this workspace for parent patch export.
- Parent evidence, source-row repair, validation, Linear/GitHub lifecycle, and PR handling: parent-owned.
- Date: 2026-05-12
