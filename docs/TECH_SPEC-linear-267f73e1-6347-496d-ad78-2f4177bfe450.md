---
id: 20260424-linear-267f73e1-6347-496d-ad78-2f4177bfe450
title: CO-351 Codex CLI 0.125.0 App-Server Control Seam Validation
relates_to: docs/PRD-linear-267f73e1-6347-496d-ad78-2f4177bfe450.md
risk: high
owners:
  - Codex
last_review: 2026-04-24
---

## Canonical Reference
- Canonical task spec: `tasks/specs/linear-267f73e1-6347-496d-ad78-2f4177bfe450-codex-0125-appserver-control-seam.md`
- PRD: `docs/PRD-linear-267f73e1-6347-496d-ad78-2f4177bfe450.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-267f73e1-6347-496d-ad78-2f4177bfe450.md`
- Task checklist: `tasks/tasks-linear-267f73e1-6347-496d-ad78-2f4177bfe450.md`
- Agent mirror: `.agent/task/linear-267f73e1-6347-496d-ad78-2f4177bfe450.md`

## Summary
- Objective: register CO-351 as the Codex CLI 0.125.0 app-server control seam validation lane, with canary-backed hold/adopt criteria.
- Scope: docs-first packet, registry entry, task checklist, `.agent` mirror, parent canary findings, and version-policy hold decision.
- Constraints: do not adopt the 0.125 app-server control seam for provider workers until sticky environment and resume/fork turn-pagination evidence is clean in a configured lane.

## Issue-Shaping Contract
- User-request translation carried forward: CO-351 is not a release-note summary or blind Codex CLI `0.125.0` upgrade. It is a validation lane for whether the candidate app-server control seam can safely support CO after focused canaries.
- Protected terms / exact artifact and surface names: Codex CLI 0.125.0, app-server control seam, `codex app-server --listen unix://...`, `app-server proxy --sock`, schema generation, remote thread store/config, sticky environments, resume/fork pagination, permission-profile round-trip, explicit untrusted project config, bursty WebSocket tool-output handling, `codex exec / codex exec resume`, official-doc/local-help mismatch for unix://, `CO-351`, `linear-267f73e1-6347-496d-ad78-2f4177bfe450`.
- Nearby wrong interpretations to reject: release-note-only summarization, immediate version-policy promotion, replacing `codex exec / codex exec resume` before canaries pass, assuming `unix://` behavior without reconciling docs and local help, and expanding this child lane into implementation, validation artifacts, findings docs, Linear state, workpad updates, or PR work.
- Explicit non-goals carried forward: no code changes, no provider-runtime adoption, no removal of `codex exec / codex exec resume`, no blind Codex CLI `0.125.0` promotion, no Linear mutation from docs-only child lanes, and no broad app-server migration.

## Parity / Alignment Matrix
- Current truth: CO provider workflows remain grounded in `codex exec / codex exec resume`; the app-server control seam is not adopted for provider-worker authority.
- Reference truth: prior CO control-seam work requires guarded authority, truthful continuity, and no overclaiming of runtime behavior. The CO-351 parent scope identifies candidate Codex CLI `0.125.0` surfaces: `codex app-server --listen unix://...`, `app-server proxy --sock`, schema generation, remote thread store/config, sticky environments, resume/fork pagination, permission-profile round-trip, explicit untrusted project config, bursty WebSocket tool-output handling, and the official-doc/local-help mismatch for unix://.
- Target truth / intended delta: parent records canary evidence and a hold/adopt decision for Codex CLI `0.125.0`; current decision is HOLD because sticky environment and resume/fork turn-pagination checks were blocked in this worker canary. Any future adoption must preserve fallback to `codex exec / codex exec resume` and document mismatches or blockers precisely.
- Explicitly out-of-scope differences: implementation code, version-policy updates, findings docs, generated canary artifacts, review execution, PR lifecycle, Linear state, and workpad mutation.

## Readiness Gate
- Not done if: any protected term is missing; acceptance can be satisfied by release-note summarization; the packet implies adoption before canaries; `unix://` mismatch is omitted; or parent-owned validation/review/PR work is pulled into this child lane.
- Pre-implementation issue-quality review evidence: self-review approves the issue shape for docs-only packet drafting because the request is bounded, all protected terms are explicit, and acceptance depends on exact canary surfaces rather than broad implementation.
- Safeguard ownership split: child lane owns only the declared docs-phase files. Parent owns canaries, version-policy edits, findings docs, validation, review, PR handoff, Linear/workpad state, and final adoption decision.

## Technical Requirements
- Functional requirements:
  1. Create the PRD, docs TECH_SPEC, canonical task spec, ACTION_PLAN, task checklist, and `.agent` mirror for CO-351.
  2. Register the canonical task spec in `tasks/index.json` under `items[]`.
  3. Add a `docs/TASKS.md` snapshot that tells future agents this is a canary-driven app-server control seam validation lane.
  4. Preserve every protected term exactly enough for targeted scans.
  5. Require parent-owned canaries for `codex app-server --listen unix://...`, `app-server proxy --sock`, schema generation, remote thread store/config, sticky environments, resume/fork pagination, permission-profile round-trip, explicit untrusted project config, and bursty WebSocket tool-output handling.
  6. Require a final parent-owned hold/adopt decision that explicitly keeps or changes current `codex exec / codex exec resume` posture.
- Non-functional requirements (performance, reliability, security): docs must be machine-checkable, avoid unverified claims from unavailable source payloads, and preserve authority/security boundaries until canaries prove otherwise.
- Interfaces / contracts: local Codex CLI `0.125.0` help/command surface, official docs for `unix://`, app-server socket/proxy control path, schema generation output, remote thread store/config behavior, sticky environment and resume/fork pagination behavior, permission profile and untrusted config behavior, WebSocket tool-output transport, and existing `codex exec / codex exec resume` fallback.

## Architecture & Data
- Architecture / design adjustments: none in runtime code. Version-policy now records 0.125 app-server as a candidate control substrate with a HOLD decision, not an adopted provider seam.
- Data model changes / migrations: none in this child lane.
- External dependencies / integrations: parent-owned Codex CLI `0.125.0`, official OpenAI/Codex documentation, local app-server canary harnesses, and existing CO runtime/version-policy docs.

## Validation Plan
- Child-lane checks:
  - protected-term scan over the declared packet/checklist files
  - JSON parse check for `tasks/index.json`
  - `git diff --check --` scoped to the touched files
- Parent-lane canaries:
  - verify local `codex app-server --listen unix://...` behavior and reconcile the official-doc/local-help mismatch for unix://
  - verify `app-server proxy --sock` connectivity and authority boundaries
  - verify schema generation compatibility with CO expectations
  - verify remote thread store/config persistence and failure behavior
  - verify sticky environments across app-server operations
  - verify resume/fork pagination behavior with enough records to exercise pagination
  - verify permission-profile round-trip behavior
  - verify explicit untrusted project config handling
  - stress bursty WebSocket tool-output handling
  - verify fallback behavior through `codex exec / codex exec resume`
- Rollout verification: parent records a hold/adopt decision with evidence paths; release-note summarization alone is not sufficient. Current evidence is `out/linear-267f73e1-6347-496d-ad78-2f4177bfe450/manual/codex-0125-appserver-canary/runtime-canary-summary.json` and `docs/findings/linear-267f73e1-6347-496d-ad78-2f4177bfe450-codex-0125-appserver-control-seam.md`.
- Monitoring / alerts: no new monitoring from this child lane.

## Open Questions
- Which configured environment should exercise sticky environments with a real known environment id?
- Which persisted turn-backed rollout should exercise resume/fork turn pagination beyond the isolated worker's no-turn thread start?
- Should a follow-up convert the local canary harness into a reusable script after the configured-environment blockers are resolved?

## Approvals
- Reviewer: docs child lane self-review; parent docs-review reran after current-main reconciliation and passed with `review_outcome=clean-success` at `.runs/linear-267f73e1-6347-496d-ad78-2f4177bfe450-docs-review/cli/2026-04-24T21-41-26-157Z-b91030b3/review/telemetry.json`.
- Date: 2026-04-24
