# PRD - CO workflow: add explicit remediation for historical shared git identity leakage

## Added by Bootstrap 2026-04-11

## Traceability
- Linear issue: `CO-148` / `11faa458-6864-4317-8f28-3fcf606c55a2`
- Linear URL: https://linear.app/asabeko/issue/CO-148/co-workflow-add-explicit-remediation-for-historical-shared-git
- Related surfaces:
  - `CO-120` / `cc5c55d9-9145-4fa8-93d9-551a3fff7702`
  - `.github/workflows/archive-automation-base.yml`
  - `scripts/worktree-git-identity.mjs`
  - shared checkout root `/Users/kbediako/Code/CO`

## Summary
- Problem Statement: `CO-120` stopped future linked-worktree helper leakage, but the repo still has no explicit repo-root operator path to detect or intentionally clear a historical shared repo-local git identity override already present in `/Users/kbediako/Code/CO/.git/config`. That leaves `user.name` / `user.email` pollution as silent local state.
- Desired Outcome: operators get an exact shared-checkout command path that detects historical shared repo-local `user.name` / `user.email` overrides, an explicit opt-in remediation path that clears only those shared repo-local overrides from the main checkout, and repo docs/helper text that clearly separate `future leak prevention` from `historical cleanup`.

## User Request Translation (Context Anchor)
- User intent / needs (in your own words): keep the CO-120 preventive helper contract intact, but add an operator-safe follow-up path for pre-existing shared repo-local git identity overrides so cleanup happens intentionally from the shared checkout rather than automatically from a linked-worktree helper flow.
- Success criteria / acceptance:
  - a deterministic repo-root detection path reports whether `/Users/kbediako/Code/CO/.git/config` contains shared repo-local `user.name` / `user.email`
  - an explicit, opt-in remediation path clears those shared repo-local overrides only when the operator intentionally runs it from the shared checkout
  - temporary linked-worktree helper flows stay free of automatic shared `user.*` mutation
  - docs and closeout guidance distinguish `future leak prevention` from `historical cleanup`
- Constraints / non-goals:
  - do not rewrite historical commits or contributor attribution
  - do not change user-global git config
  - do not make the linked-worktree helper auto-clear shared repo config
  - do not reopen or broaden the CO-120 preventive helper/workflow contract

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `shared repo-local git identity override`
  - `user.name`
  - `user.email`
  - .git/config
  - `linked-worktree helper flow`
  - `future leak prevention`
  - `historical cleanup`
- Protected terms / exact artifact and surface names:
  - `/Users/kbediako/Code/CO/.git/config`
  - `/Users/kbediako/Code/CO`
  - `scripts/worktree-git-identity.mjs`
  - `.github/workflows/archive-automation-base.yml`
  - `git config --worktree`
- Nearby wrong interpretations to reject:
  - "rewrite old commits or contributor attribution"
  - "change the operator's global git config"
  - "make the worktree helper auto-clear shared repo config on every run"
  - "treat CO-120 as incomplete because it does not rewrite already-leaked shared config during temporary worktree operations"

## Parity / Alignment Matrix
- Current truth:
  - `CO-120` already provides future leak prevention via a safe linked-worktree helper path
  - the repo still lacks a repo-owned shared-checkout command path for historical shared repo-local `user.name` / `user.email` detection and cleanup
  - existing operator guidance does not make the boundary between preventive helper behavior and intentional historical cleanup explicit
- Reference truth:
  - linked-worktree helpers prevent new shared repo-local identity leakage
  - operators can intentionally inspect and clear historical shared repo-local overrides from the shared checkout root
  - the preventive helper never mutates shared `user.*` settings automatically
- Target truth / intended delta:
  - a dedicated shared-checkout helper detects and optionally clears shared repo-local `user.name` / `user.email` in the shared checkout config file at `/Users/kbediako/Code/CO/.git/config`
  - the helper fails closed when invoked from a linked worktree or non-root path
  - repo docs/helper messaging point historical cleanup to the new explicit repo-root path rather than blurring it with worktree-local prevention
- Explicitly out-of-scope differences:
  - history rewriting
  - global git policy changes
  - changing preventive worktree-helper semantics
  - unrelated provider/runtime changes

## Not Done If
- Operators still lack an exact repo-root command path to detect pre-existing shared repo-local `user.name` / `user.email` overrides.
- The remediation story still depends on temporary linked-worktree helpers mutating the shared checkout config file at `/Users/kbediako/Code/CO/.git/config`.
- Docs still blur the line between preventing future leakage and intentionally cleaning up historical local pollution.

## Goals
- Add a deterministic shared-checkout detection path for historical shared repo-local `user.name` / `user.email` overrides.
- Add an explicit, opt-in shared-checkout remediation path that clears only the historical shared repo-local override.
- Keep the preventive linked-worktree helper contract unchanged apart from clearer scope wording.
- Provide focused regression proof and exact rerunnable commands for reviewers and operators.

## Non-Goals
- Rewriting historical commits or GitHub contributor attribution.
- Changing user-global git config.
- Auto-clearing shared repo config during temporary linked-worktree helper runs.
- Expanding CO-148 into new workflow/authorship theories unrelated to the shared local config file at `/Users/kbediako/Code/CO/.git/config`.

## Stakeholders
- Product: CO operators who need explicit cleanup steps for already-polluted local state
- Engineering: maintainers of linked-worktree helper flows and local repo operations
- Design: N/A

## Metrics & Guardrails
- Primary Success Metrics:
  - the repo exposes a deterministic shared-checkout detection command for historical shared repo-local identity overrides
  - the repo exposes a separate opt-in cleanup command that clears only shared repo-local `user.name` / `user.email`
  - preventive linked-worktree helper behavior stays isolated to future leak prevention
- Guardrails / Error Budgets:
  - do not introduce any automatic shared `user.*` mutation into temporary linked-worktree helpers
  - do not clear shared config from non-root linked-worktree paths
  - keep the diff bounded to the historical-cleanup seam plus explicit documentation

## User Experience
- Personas:
  - operator inspecting the shared CO checkout after prior helper leakage
  - reviewer verifying why CO-148 is distinct from CO-120
- User Journeys:
  - operator runs one exact repo-root command and sees whether `/Users/kbediako/Code/CO/.git/config` holds a shared repo-local `user.name` / `user.email` override
  - operator runs one explicit cleanup command from the shared checkout to clear that override intentionally
  - reviewer reads helper/doc text and sees that worktree prevention and historical cleanup are separate contracts

## Technical Considerations
- Architectural Notes:
  - historical cleanup belongs at the shared checkout root, not inside a temporary linked-worktree helper path
  - the implementation should keep the existing preventive helper minimal and add a separate shared-checkout helper rather than overloading the worktree helper with automatic cleanup behavior
  - focused tests should prove both the status path and the opt-in clear path
- Dependencies / Integrations:
  - local `git`
  - `scripts/worktree-git-identity.mjs`
  - a new shared-checkout remediation helper under `scripts/`
  - focused Vitest coverage under `tests/`

## Open Questions
- Resolved for bootstrap: the cleanest boundary is a separate shared-checkout helper plus explicit worktree-helper wording, not an automatic extension of the CO-120 preventive helper.

## Approvals
- Product: self-approved from the Linear issue scope and protected wording
- Engineering: pending audited `docs-review` evidence before implementation
- Design: N/A
