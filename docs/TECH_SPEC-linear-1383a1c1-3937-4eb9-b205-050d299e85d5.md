---
id: linear-1383a1c1-3937-4eb9-b205-050d299e85d5
title: CO-391 installed control-host timeout and stale terminal suppression
relates_to: docs/PRD-linear-1383a1c1-3937-4eb9-b205-050d299e85d5.md
risk: high
owners:
  - Codex
last_review: 2026-04-26
---

## Summary
Fix the installed `CO STATUS` hot path by making terminal or merge-closeout action-required provider-intake residue inactive and by bounding compatibility discovery when retained Linear provider history is large.

## Contract
- Preserve: `5cc7be319`, `co-status --format json`, `degraded_read.reason=ui_request_timeout`, `active_worker_probe_timeout_quarantine`, `active_issue_identifiers`, `running_issue_identifiers`, `provider_issue_merge_closeout_action_required`, `provider_debug_snapshot`.
- Reject: CO-376 reopen without post-install source-regression proof, unrelated status UI changes, broad provider workflow rewrite.
- Parity target: installed LaunchAgent and current source agree that live terminal Linear truth suppresses stale non-retry handoff residue while queued retries and active non-terminal handoff failures remain active.

## Requirements
Provider intake excludes terminal non-retry `handoff_failed` claims and merge-closeout action-required residue from active/running identifiers, compatibility projection prunes those terminal rows, and high-volume discovery scans active/retry claim dirs, non-Linear task dirs, plus a bounded recent active-claim-derived synthetic local/null-provider set.
LaunchAgent must use the installed runtime containing the source fix.

## Validation Plan
Run focused `ProviderIntakeState`, `ControlRuntime`, and `SelectedRunProjection` tests; full build/lint/test/docs/stewardship/pack-smoke gates; standalone review; elegance pass; and installed-runtime proof with direct `co-status` timing plus two supervisor health windows.
