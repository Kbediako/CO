# PRD - CO Run Codex 0.117.x-to-main Canary for Multi-Agent-v2 and App-Server Adoption
- Linear issue: `CO-22` / `b8a32c11-cfc6-4556-b783-f59b290fe67d`
- Problem: CO is already running `codex-cli 0.117.0` against an upstream-aligned Codex fork, but active CO policy/docs/runtime assumptions still reflect the older `0.115.x` collab and app-server model. The most visible gaps are stale `max_spawn_depth` guidance, thread-id-only collab lineage assumptions, and provider-worker supervision that still treats app-server as a passive runtime choice instead of evaluating it as a richer control substrate candidate.
- Outcome: run a bounded adoption canary that re-audits the live upstream baseline, corrects stale active guidance/defaults where evidence is already clear, records explicit deferrals where current exec/app-server history still does not expose richer agent-path lineage, and lands a concrete recommendation for provider-worker supervision plus `CO-13` / `CO-14` sequencing.
- Acceptance: docs-first packet exists, multi-agent-v2 and app-server canary evidence is captured under CO-owned artifacts, stale active guidance/defaults are corrected or explicitly deferred with reasons, and the final recommendation makes the app-server adoption posture and `CO-13` / `CO-14` impact explicit.
- Constraints: keep delegation guard intact, do not widen issue-level authority boundaries during the canary, treat `thread/shellCommand` as a sensitive unsandboxed surface, and avoid a speculative provider-worker architecture rewrite in this lane.
