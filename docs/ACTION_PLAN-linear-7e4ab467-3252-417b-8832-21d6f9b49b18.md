# ACTION_PLAN - CO-495 targeted issue-by-id recovery ignores stale Backlog intake

## Summary
- Goal: make explicit `control-host recover --issue-id` use `dispatch_source_issue_by_id` for the selected issue while stale Backlog ignored provider-intake claims remain audit-only and tracked-issues discovery stays constrained.
- Scope: targeted control-host recovery dispatch/source selection, stale provider-intake authority classification for the targeted path, focused regressions, docs packet, validation, and parent-owned handoff.
- Assumptions:
  - The declared source payload is absent from this child checkout, so the parent-provided protected terms and issue-shaping contract are authoritative for this docs packet.
  - Targeted issue-by-id recovery is narrower than broad tracked-issues discovery and must not become normal Backlog reclaim.
  - Parent recovery must still respect Linear budget, admission caps, provider-worker launch safety, and provider-intake audit preservation.

## Issue Readiness Gate
- Intent checksum / protected terms carried forward: `control-host recover --issue-id`, `targeted issue-by-id`, `dispatch_source_issue_by_id`, `tracked-issues discovery constrained`, `stale Backlog ignored provider-intake claim`, `provider-intake-state.json`, `provider-linear-worker`, `control-host recovery`, `Linear budget`.
- Not done if:
  - targeted recovery still misses the selected issue because tracked-issues discovery is constrained
  - stale Backlog ignored provider-intake claim data remains authoritative over the targeted issue-by-id path
  - normal Backlog reclaim, Rework metadata recovery, rate-limit policy removal, Linear budget bypass, or manual provider-intake editing enters scope
  - provider-intake audit evidence is deleted or hidden
- Pre-implementation issue-quality review:
  - CO-495 is implementation-ready from the parent-provided issue contract: correctness depends on exact source naming, targeted issue-by-id authority, and preserved adjacent-lane boundaries.
  - The micro-task path is unavailable because stale/cached provider-intake behavior, fallback authority, and protected dispatch source names define correctness.
- Fallback / refactor decision:
  - `remove fallback`: stale Backlog provider-intake evidence must stop being sufficient to block or replace explicit issue-by-id recovery.
  - `justify retaining fallback`: tracked-issues discovery constraints remain a durable budget/safety contract.
- Large-refactor decision: not required unless source inspection proves targeted issue-by-id authority is split across multiple incompatible recovery/status surfaces.
- Minor-seam decision: acceptable only as stale-authority removal for the targeted path; do not add another broad discovery path.

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Targeted recovery source selection | `control-host recover --issue-id` can depend on stale provider-intake or constrained tracked discovery instead of exact issue-by-id truth. | `remove fallback` | CO-495 | Explicit recover is invoked with `--issue-id` and local intake has stale Backlog ignored evidence. | observed 2026-05-05 | 2026-05-05 | This issue | Targeted recover uses `dispatch_source_issue_by_id` and stale Backlog intake cannot block the selected issue. | Focused targeted recovery regression. |
| Tracked-issues discovery constraint | Broad tracked discovery remains constrained for budget and queue safety. | `justify retaining fallback` | control-host recovery / Linear dispatch | Non-targeted discovery, recovery sweeps, or queue refreshes need bounded Linear reads. | Existing provider-worker safety behavior | 2026-05-05 | Durable safety contract | Separate issue-quality review changes Linear budget and discovery policy. | Regression proves targeted issue-by-id works without broadening tracked discovery. |

- Contract name: Constrained tracked-issues discovery safety.
- Owning surface: control-host recovery and Linear dispatch.
- Steady-state proof: explicit issue-by-id recovery can succeed while broad tracked-issues discovery remains constrained.
- Tests/docs: focused CO-495 targeted recovery regressions and this packet.
- Non-expiring rationale: discovery constraints protect Linear budget and queue safety.

Issue boundaries remain: use targeted issue-by-id for explicit recovery, ignore stale Backlog intake only for that targeted decision, keep discovery constrained, keep budget/admission/provider-worker checks intact, and avoid manual intake edits.

## Milestones & Sequencing
1. Parent accepts this docs packet and reconciles any missing source-payload details from the authoritative issue workspace.
2. Inspect the `control-host recover --issue-id` path, dispatch-source selection, provider-intake stale-claim classification, and targeted recovery proof surfaces.
3. Add focused regression coverage for stale Backlog ignored provider-intake evidence under constrained tracked-issues discovery.
4. Add preservation coverage for non-targeted tracked discovery constraints, Linear budget failure, and provider-intake audit retention.
5. Implement the smallest targeted issue-by-id dispatch-source correction.
6. Verify recovery proof/status surfaces expose `dispatch_source_issue_by_id` or equivalent targeted provenance.
7. Run focused checks, then the parent-required validation/review chain.
8. Complete PR lifecycle and Linear handoff under parent ownership.

## Dependencies
- Existing control-host recovery command and provider-worker recovery path.
- Existing Linear issue-by-id dispatch helper and tracked-issues discovery constraints.
- Existing provider-intake state/audit persistence.
- Parent-owned live Linear issue truth, workpad, docs-review, implementation, validation, and PR lifecycle.
- Adjacent boundaries:
  - CO-281 normal Backlog reclaim remains separate
  - CO-493 Rework metadata recovery remains separate
  - rate-limit and Linear budget policy remain separate

## Validation
- Checks / tests:
  - child docs-lane protected-term grep over packet files
  - child docs-lane JSON parse for registry mirrors
  - child docs-lane `git diff --check` over declared touched files
  - focused targeted `control-host recover --issue-id` regression under constrained tracked discovery
  - focused stale Backlog ignored provider-intake claim regression
  - focused Linear budget fail-closed preservation check
  - focused non-targeted tracked discovery constraint preservation check
  - provider-intake audit retention assertion
  - `node scripts/delegation-guard.mjs`
  - `node scripts/spec-guard.mjs --dry-run`
  - `npm run build`
  - `npm run lint`
  - focused tests, then `npm run test` when parent expands validation
  - `npm run docs:check`
  - `npm run docs:freshness`
  - `npm run repo:stewardship`
  - `node scripts/diff-budget.mjs`
  - manifest-backed review via `TASK=linear-7e4ab467-3252-417b-8832-21d6f9b49b18 NOTES="Goal: CO-495 explicit recover uses targeted issue-by-id under constrained tracked discovery. | Summary: Docs packet plus minimal provider recovery detail propagation and focused ProviderIssueHandoff regressions. | Risks: Do not bypass Linear budget reserves, broaden tracked discovery, mutate provider-intake manually, or regress CO-281/CO-493 adjacent recovery paths." codex-orchestrator review --manifest ../../.runs/linear-7e4ab467-3252-417b-8832-21d6f9b49b18/cli/2026-05-05T01-07-51-635Z-d4ef14da/manifest.json`
  - elegance/minimality review
  - verify unresolved actionable review threads are zero before merge; if any remain, resolve each thread or record an explicit waiver in task checklist notes
  - if Codex review is unavailable due to quota, record required checks green, quota evidence, and an explicit waiver in task checklist notes before merge
  - `npm run pack:smoke` if required by touched CLI/package surface
- Rollback plan:
  - Revert the targeted dispatch-source/stale-intake classification change and focused tests if normal Backlog reclaim, Rework metadata recovery, budget controls, or provider-worker safety regress.
  - If source inspection shows this cannot stay at the targeted recovery boundary, stop and relaunch with widened ownership.
  - Do not merge with unresolved actionable review threads unless the task checklist records a dated waiver, owner, reason, and evidence.

## Risks & Mitigations
- Keep exact protected-source assertions, non-targeted discovery preservation tests, budget fail-closed tests, and provider-intake audit-retention checks so the targeted repair does not become a broad scheduler or policy rewrite.

Approval: provider-worker parent lane, 2026-05-05.
