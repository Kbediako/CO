---
id: 20260413-linear-6af47129-33cc-45fa-9a55-e49423536752
title: CO validation: classify current-main docs:freshness stale-doc baseline after CO-165 rebase
relates_to: docs/PRD-linear-6af47129-33cc-45fa-9a55-e49423536752.md
risk: medium
owners:
  - Codex
last_review: 2026-04-13
---

## Canonical Reference
- Canonical TECH_SPEC: `tasks/specs/linear-6af47129-33cc-45fa-9a55-e49423536752.md`
- PRD: `docs/PRD-linear-6af47129-33cc-45fa-9a55-e49423536752.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-6af47129-33cc-45fa-9a55-e49423536752.md`
- Task checklist: `tasks/tasks-linear-6af47129-33cc-45fa-9a55-e49423536752.md`

## Traceability
- Linear issue: `CO-166` / `6af47129-33cc-45fa-9a55-e49423536752`
- Linear URL: https://linear.app/asabeko/issue/CO-166/co-validation-classify-current-main-docsfreshness-stale-doc-baseline
- Source issue: `CO-165` / `09012cec-1a78-408c-a7a7-a53e2fce5612`
- Follow-up issue: `CO-167` / `72e9f13e-0899-4769-9ca3-82586077fca9`

## Summary
- Objective: classify whether the post-`CO-165` `docs:freshness` failure is now a current-main baseline, identify the exact stale-doc sources, and either restore a green truthful baseline or document the exact remaining blocker path.
- Scope:
  - bootstrap the `CO-166` docs-first packet, registry mirrors, and single workpad source
  - reproduce `docs:freshness` on the latest current mainline with saved artifacts
  - classify the exact stale entries and their drift source
  - land only the smallest bounded fix required by the repro, or document the blocker truthfully if no safe bounded fix exists
- Constraints:
  - keep `CO-165` provider-worker runtime/test scope out of this lane
  - do not weaken `docs:freshness`
  - keep any fix limited to the proven stale source plus the new `CO-166` packet

## Technical Requirements
- Functional requirements:
  - save a fresh `docs:freshness` report for the current workspace
  - classify stale entries by class and exact path/age source
  - reconcile the live result against the issue snapshot and pre-rebase evidence
  - either restore `docs:freshness` to green or record the exact blocker and approved handling path
- Non-functional requirements:
  - keep the diff bounded and auditable
  - avoid speculative broad refreshes
  - keep saved evidence under `out/linear-6af47129-33cc-45fa-9a55-e49423536752/manual/`
- Interfaces / contracts:
  - `scripts/docs-freshness.mjs`
  - `docs/docs-freshness-registry.json`
  - `tasks/index.json`
  - `docs/TASKS.md`
  - task/spec review metadata fields

## Execution Update 2026-04-13
- Current repo truth after local repro:
  - a clean workspace fast-forwarded to `origin/main` reproduced `docs:freshness FAILED - 3725 docs, 3728 registry entries`
  - exact stale totals match the issue snapshot: `198` overall, `Task Packet=142`, `Task Mirror=29`, `Report Only=26`, `Active Guide=1`
  - there are no missing registry entries, missing files, invalid entries, or uncatalogued docs
  - the stale set splits into two cohorts:
    - March 13 / 30-day cohort: `182` docs across packet ids `1138` through `1163`, with seven stale surfaces per packet (`.agent/task`, `docs/findings`, `docs/ACTION_PLAN-*`, `docs/PRD-*`, `docs/TECH_SPEC-*`, `tasks/specs/*`, `tasks/tasks-*`)
    - January 12 / 90-day cohort: `16` docs spanning `.agent/SOPs/release.md` plus the release-plan packets `0942`, `0943`, and `0944`
- Pre-implementation approval:
  - register the `CO-166` packet first, publish the initial workpad, run an audited docs-review child stream, then classify the live stale set before any repair
- Post-fix state:
  - `linear child-stream --pipeline docs-review` failed closed on `provider_worker_child_stream_provenance_invalid` because the parent provider-worker manifest has no control-host provenance
  - direct fallback run `linear-6af47129-33cc-45fa-9a55-e49423536752-co-166-docs-review` first failed only because the new `CO-166` snapshot pushed `docs/TASKS.md` to `451/450`
  - `npm run docs:archive-tasks` archived one snapshot and restored `docs/TASKS.md` to `450/450`
  - direct fallback rerun `linear-6af47129-33cc-45fa-9a55-e49423536752-co-166-docs-review-rerun` then passed `spec-guard` and `docs:check` and failed only at `docs:freshness`
  - `CO-167` now owns the actual stale-cohort refresh because this lane’s truthful outcome is classification plus explicit blocker routing, not a silent 198-doc metadata sweep

## Architecture & Data
- Architecture / design adjustments:
  - none are planned unless the repro proves a bounded metadata repair is required
  - the primary product of this lane is a truthful baseline classification plus any minimal metadata refresh needed by that evidence
- Data model changes / migrations:
  - add the `linear-6af47129-33cc-45fa-9a55-e49423536752` docs packet and registry entries
  - refresh existing metadata only if the repro identifies a live stale cohort
- External dependencies / integrations:
  - audited `linear child-stream --pipeline docs-review`
  - saved `docs:freshness` report artifacts under `out/linear-6af47129-33cc-45fa-9a55-e49423536752/manual/`

## Validation Plan
- Tests / checks:
  - audited `linear child-stream --pipeline docs-review`
  - `npm run docs:freshness`
  - targeted follow-on validation only after the repro identifies the true blocker
- Rollout verification:
  - capture baseline `docs:freshness` output before any repair
  - preserve a path-by-path stale-entry report for the lane
  - refresh the workpad after docs-first, after repro/classification, and before any handoff
- Monitoring / alerts:
  - rely on saved repro artifacts and reruns; no new automation is introduced here

## Open Questions
- Whether the docs-only `CO-166` packet should be handed off through the normal PR path or treated as a local investigation packet now that `CO-167` owns the substantive repair.

## Approvals
- Reviewer: docs-review fallback recorded; rerun manifest proves `spec-guard` and `docs:check` are green while the remaining failure is current-main `docs:freshness`
- Status: in progress
- Date: 2026-04-13
