# ACTION_PLAN - Coordinator Symphony-Aligned Question Queue Child-Resolution Adapter Extraction

## Phase 1 - Docs and review handoff

- Register `1068` across PRD / TECH_SPEC / ACTION_PLAN / findings / spec mirror / checklist mirror / `tasks/index.json` / `docs/TASKS.md` / docs freshness registry.
- Capture delegated or local read-only approval that the next smallest Symphony-aligned seam is the question/delegation child-resolution helper cluster, not another standalone-review lane or a wider cross-subsystem unification.
- Capture docs-review evidence or an explicit override if the review wrapper again drifts without new bounded findings.

## Phase 2 - Bounded extraction

- Add a dedicated question child-resolution adapter module under `orchestrator/src/cli/control/`.
- Move delegation/header/manifest/child-resolution helper ownership out of `controlServer.ts`.
- Keep `questionQueueController.ts` and `authenticatedRouteComposition.ts` explicit and behavior-preserving.

## Phase 3 - Validation and closeout

- Run focused question/delegation child-resolution regressions plus the standard guard bundle.
- Run standalone review and elegance review on the final tree.
- Record any remaining broader control/delegation sharing opportunities as a later slice instead of widening `1068`.
