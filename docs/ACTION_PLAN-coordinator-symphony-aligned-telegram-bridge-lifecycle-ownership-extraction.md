# ACTION_PLAN - Coordinator Symphony-Aligned Telegram Bridge Lifecycle Ownership Extraction

## Steps

1. Register `1081` docs-first from the `1080` next-slice note and the bounded Telegram bridge lifecycle scout recommendation.
2. Extract the remaining Telegram bridge lifecycle ownership into one control-local helper near `controlServerBootstrapLifecycle.ts`.
3. Keep `persist -> expiry -> bridge` ordering unchanged while delegating only the Telegram bridge runtime ownership.
4. Add focused regressions for lazy adapter creation, bridge attachment, and bridge shutdown behavior.
5. Run the lane validation bundle, sync mirrors, and queue the next bounded Symphony-aligned seam.
