# PRD - CO-559 normalize Linear Markdown before create-follow-up post-create verification

## Traceability
- Linear issue: `CO-559` / `33117eac-b4d3-4eb0-889c-4527c9b79352`; task id: `20260519-linear-33117eac-b4d3-4eb0-889c-4527c9b79352`.
- Source anchor: `ctx:sha256:ad91393daad62beac2ab532666452455c2e36af37bdfeef26ac820459b867dd0#chunk:c000001`; parent manifest: `.runs/linear-33117eac-b4d3-4eb0-889c-4527c9b79352/cli/2026-05-19T11-14-18-330Z-47b25076/manifest.json`.

## Summary
- Problem: `codex-orchestrator linear create-follow-up` can create or reuse the intended Linear issue, then report a non-retryable failure because strict post-create description comparison treats Linear Markdown normalization as a semantic mismatch.
- Desired outcome: compare descriptions through a Linear-compatible normalized Markdown representation while keeping created issue id, identifier, URL, canonical marker, labels, and relations as authoritative success evidence.

## User Request Translation
- User intent: stop successful follow-up creation from looking failed solely because Linear persisted equivalent Markdown with normalized bullets or spacing.
- Acceptance: normalize expected and observed Markdown before mismatch classification; cover ordinary and `canonical-owner-key` follow-ups; keep missing sections, canonical marker, labels, or related links fail-closed.
- Non-goals: do not change Linear rendering, bypass canonical owner reuse, remove required sections or labels, or create duplicate issues when a prior helper attempt already succeeded.

## Intent Checksum
- Protected terms: `create-follow-up`, `Linear Markdown normalization`, `post-create verification`, `canonical-owner-key`, `non-retryable failure`, `codex-orchestrator:canonical-owner-key=...`, follow-up labels, related Linear issue link.
- Reject nearby wrong interpretations: do not weaken issue creation contracts, stop post-create verification, retry a successful create, or treat missing structural evidence as Markdown-only drift.

## Parity / Alignment Matrix

| Surface | Current truth | Reference truth | Target truth | Explicitly out-of-scope differences |
| --- | --- | --- | --- | --- |
| Description verification | Strict byte comparison can reject equivalent Markdown after Linear normalizes bullets or spacing. | Linear persisted Markdown may use equivalent bullet and spacing forms. | The helper compares expected and observed descriptions through a normalized Markdown representation before reporting mismatch. | Changing Linear rendering or stored descriptions. |
| Success authority | A created issue can have id/identifier/url but still return failed on Markdown-only drift. | Created issue identity plus canonical marker, labels, and relations are authoritative structural evidence. | Markdown-only normalized equivalence reports success when structural evidence is complete. | Ignoring missing markers, labels, relations, or sections. |
| Duplicate prevention | Re-running after a false failure can create retry confusion or duplicate risk. | Successful helper-created follow-ups should not be retried as if creation failed. | Markdown-only drift no longer causes non-retryable helper failure after successful create/reuse. | Broad retry-policy redesign. |

## Not Done If
- A successfully created issue can still cause helper failure solely due to Linear Markdown rendering differences.
- Re-running after a Markdown-only false failure can still risk duplicate issue creation or retry suppression confusion.
- Missing required sections, canonical marker, labels, or related links are accepted as normalized text drift.

## Goals
- Add normalized Markdown description comparison matching known Linear persistence behavior around bullets and spacing.
- Preserve hard structural verification for follow-up identity, canonical marker, labels, relations, and required issue-shaping content.
- Cover ordinary create and canonical-owner-key paths with focused regressions.

## Fallback / Refactor Decision
- Applies to fallback, compatibility, legacy, stale, cached, break-glass, or minor-seam behavior? `Yes`.
- Large-refactor decision: not required; the existing create-follow-up verifier remains the single authority and only needs bounded normalization before semantic mismatch classification.

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Description verification | Byte-exact Markdown comparison treats Linear-equivalent bullet/spacing normalization as failure. | `remove fallback` | CO-559 | Post-create or post-reuse description verification. | Existing pre-CO-559 behavior | 2026-05-19 | This issue | Verification compares normalized expected and observed Markdown before failing on text mismatch. | Focused facade regressions for bullet/spacing normalization and canonical-owner-key creation. |

## Validation
- Focused facade tests prove accepted Linear-equivalent bullet/spacing normalization and rejected non-equivalent content drift.
- Required provider-worker validation, standalone review, elegance pass, PR attachment, and ready-review drain must pass before review handoff.
