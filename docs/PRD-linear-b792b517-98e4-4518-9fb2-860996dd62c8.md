# PRD - CO validation: stabilize unrelated CLI/runtime test timeouts that block provider-worker full-suite evidence

## Added by Bootstrap 2026-04-12

## Traceability
- Linear issue: `CO-130` / `b792b517-98e4-4518-9fb2-860996dd62c8`
- Linear URL: https://linear.app/asabeko/issue/CO-130/co-validation-stabilize-unrelated-cliruntime-test-timeouts-that-block
- Source issue: `CO-120` / `cc5c55d9-9145-4fa8-93d9-551a3fff7702`

## Summary
- Problem Statement: provider-worker closeout previously lacked truthful repo-wide test evidence because an earlier `npm run test` run in the CO workspace did not reach a clean terminal result. This follow-up lane was opened to confirm whether the unrelated CLI/runtime timeout report was still live on the current tree or whether the repo had already recovered.
- Desired Outcome: keep provider-worker evidence truthful by proving the current-tree result with exact commands, classifying the named suites accurately, and only landing runtime/test changes if the blocker still reproduces.

## User Request Translation (Context Anchor)
- User intent / needs (in your own words): complete CO-130 in the current provider-worker workspace by treating the listed CLI/runtime timeout suites as the suspected blocker, reproducing and classifying them with exact commands, and restoring truthful provider-worker evidence for repo-wide validation without blaming CO-120 or inventing a broad redesign.
- Success criteria / acceptance:
  - reproduce the exact `npm run test` path plus the named suites and classify whether they are a real regression, a harness timeout, or an existing long-run but terminal-green suite
  - restore a truthful repo-wide validation path for provider-worker closeout, even if the truthful outcome is that the blocker no longer reproduces on the current tree
  - keep workpad and handoff guidance explicit about the difference between change-local failures and unrelated repo-wide blockers
- Constraints / non-goals:
  - do not reopen the linked-worktree git identity implementation from CO-120 unless new evidence directly ties that change to the failures
  - do not pretend a killed or partial full-suite run is good enough for review handoff
  - do not permanently skip repo-wide validation for provider-worker lanes

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `full-suite validation blocker`
  - `unrelated CLI/runtime Vitest timeouts`
  - `provider-worker evidence should stay truthful`
- Protected terms / exact artifact and surface names:
  - `npm run test`
  - `orchestrator/tests/CodexOrchestratorCli.test.ts`
  - `orchestrator/tests/LearningCrystalizerCliClient.test.ts`
  - `orchestrator/tests/CliExecRuntime.test.ts`
  - `tests/run-review.spec.ts`
  - `tests/cli-command-surface.spec.ts`
  - `orchestrator/tests/ProviderLinearWorkerRunner.test.ts`
- Nearby wrong interpretations to reject:
  - `CO-120`'s worktree helper caused these timeout failures
  - a killed or partial full-suite run is good enough for review handoff
  - the fix is to permanently skip repo-wide validation for provider-worker lanes

## Parity / Alignment Matrix
- Current truth:
  - focused CO-120 validation is already green on its own surface
  - exact current-tree reproductions no longer reproduce the earlier full-suite blocker: plain `npm run test` finished cleanly in `135.22s`, and a stage-owned `CODEX_VITEST_PROGRESS=1 npm run test` run also finished cleanly in `138.78s`
  - the reported timeout-heavy suites are outside the CO-120 change surface, but on the current tree they are green individually and inside the full suite
- Reference truth:
  - provider-worker handoff requires truthful validation evidence rather than partial or implied success
  - the issue already preserves the exact suites, rejection rules, and non-goals that prevent this lane from drifting into CO-120 blame or broad harness redesign
- Target truth / intended delta:
  - current-tree evidence shows `npm run test` reaches a clean terminal result again for this repo state
  - provider-worker workpad and handoff content stay explicit that the earlier blocker report is historical evidence, not a currently reproducing failure
- Explicitly out-of-scope differences:
  - linked-worktree git identity implementation changes from CO-120
  - a permanent repo-wide validation skip
  - a broad unrelated test-harness redesign beyond the named timeout lanes

## Not Done If
- `npm run test` still cannot produce a trustworthy terminal result for provider-worker closeout.
- The same timeout-heavy CLI/runtime suites still fail without a root-cause explanation or scoped remediation.
- Provider-worker closeouts still have to choose between stalling indefinitely and misreporting a partial suite as green.

## Goals
- Reproduce and classify the exact timeout-heavy suites named in the issue.
- Restore a truthful repo-wide validation posture for provider-worker closeout.
- Keep the final change bounded to the minimum supported by the reproduced evidence, including the possibility of a no-code resolution when the blocker no longer reproduces.

## Non-Goals
- Reopening the linked-worktree identity lane from CO-120 without new causal evidence.
- Treating an interrupted full-suite run as acceptable review evidence.
- Expanding the task into a general Vitest or runtime redesign outside the named blocker surfaces.

## Stakeholders
- Product: CO operators who rely on provider-worker closeout evidence before review handoff.
- Engineering: CLI/runtime, review-wrapper, and test-harness maintainers.
- Design: Not applicable.

## Metrics & Guardrails
- Primary Success Metrics:
  - the exact blocker suites are reproduced and classified with machine-checkable evidence
  - provider-worker closeout regains a truthful repo-wide validation story
  - the final diff stays bounded to the evidence-backed outcome instead of speculative runtime/test changes
- Guardrails / Error Budgets:
  - no false-green full-suite claims
  - no blame shift onto CO-120 without new evidence
  - no permanent provider-worker validation downgrade

## User Experience
- Personas:
  - provider-worker operators who need trustworthy closeout evidence
  - maintainers diagnosing whether these suites are regressions, harness limits, or existing repo-wide instability
- User Journeys:
  - a worker reruns `npm run test` and gets a terminal result, or a truthful scoped blocker report
  - handoff notes clearly separate historical blocker reports from current change-local validation outcomes

## Technical Considerations
- Architectural Notes:
  - prefer the smallest shared timeout/harness fix if the failing suites still share one seam
  - if the blocker no longer reproduces, prefer a no-code closeout with exact evidence over speculative timeout tuning
- Dependencies / Integrations:
  - `package.json`
  - Vitest config and runtime harnesses
  - `orchestrator/tests/CodexOrchestratorCli.test.ts`
  - `orchestrator/tests/LearningCrystalizerCliClient.test.ts`
  - `orchestrator/tests/CliExecRuntime.test.ts`
  - `tests/run-review.spec.ts`
  - `tests/cli-command-surface.spec.ts`
  - `orchestrator/tests/ProviderLinearWorkerRunner.test.ts`
  - provider-worker workpad and review-handoff surfaces

## Open Questions
- None for current closeout. If provider-worker timeout instability returns later, a follow-up can revisit whether worker-cap signals should align more closely with provider-worker env markers.

## Approvals
- Product: Self-approved for docs-first investigation start against the Linear issue contract.
- Engineering: Audited docs-review child stream succeeded; final validation floor, standalone review, and elegance review still pending before handoff.
- Design: Not applicable.
