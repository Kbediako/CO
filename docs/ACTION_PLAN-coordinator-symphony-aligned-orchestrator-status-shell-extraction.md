# ACTION_PLAN: Coordinator Symphony-Aligned Orchestrator Status Shell Extraction

## Docs-first

- [x] Create PRD / TECH_SPEC / ACTION_PLAN / findings / spec / checklist / .agent mirror for `1196`.
- [x] Update `tasks/index.json`, `docs/TASKS.md`, and `docs/docs-freshness-registry.json`.
- [x] Capture local deliberation plus docs-review approval or explicit override for the status-shell seam.

## Implementation

- [ ] Extract the `status()` command cluster from `orchestrator.ts`.
- [ ] Keep JSON payload shape, text rendering, and manifest/activity lookup behavior exact.
- [ ] Preserve focused regressions around the status shell without widening into `plan()` or lifecycle flows.

## Closeout

- [ ] Run the standard gate bundle and capture closeout artifacts under `out/1196-coordinator-symphony-aligned-orchestrator-status-shell-extraction/manual/<timestamp>-closeout/`.
- [ ] Run an explicit elegance review.
- [ ] Record the next truthful seam after `1196`.
