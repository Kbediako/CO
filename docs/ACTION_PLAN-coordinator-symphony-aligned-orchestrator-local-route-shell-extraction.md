# ACTION_PLAN: Coordinator Symphony-Aligned Orchestrator Local Route Shell Extraction

## Docs-first

- [x] Create PRD / TECH_SPEC / ACTION_PLAN / findings / spec / checklist / .agent mirror for `1182`.
- [x] Update `tasks/index.json`, `docs/TASKS.md`, and `docs/docs-freshness-registry.json`.
- [x] Capture local deliberation plus docs-review approval or explicit override for the local-route shell seam.

## Implementation

- [x] Extract one bounded local-route shell around `runLocalExecutionLifecycleShell(...)`.
- [x] Keep `routeOrchestratorExecution(...)` as the router-local failure and branch boundary while delegating runtime-fallback summary append, auto-scout env forwarding, local execution dispatch, and guardrail recommendation append.
- [x] Add focused regression coverage for runtime-fallback summary behavior, auto-scout env propagation, local execution dispatch, and guardrail recommendation append without reopening route-state or cloud-route seams.

## Closeout

- [x] Run the standard gate bundle and capture closeout artifacts under `out/1182-coordinator-symphony-aligned-orchestrator-local-route-shell-extraction/manual/20260314T075314Z-closeout/`.
- [x] Run an explicit elegance review.
- [x] Record the next truthful seam after `1182`.
