# ACTION_PLAN: Coordinator Symphony-Aligned Orchestrator Local Route Shell Extraction

## Docs-first

- [x] Create PRD / TECH_SPEC / ACTION_PLAN / findings / spec / checklist / .agent mirror for `1182`.
- [x] Update `tasks/index.json`, `docs/TASKS.md`, and `docs/docs-freshness-registry.json`.
- [x] Capture local deliberation plus docs-review approval or explicit override for the local-route shell seam.

## Implementation

- [ ] Extract one bounded local-route shell around `runLocalExecutionLifecycleShell(...)`.
- [ ] Keep `routeOrchestratorExecution(...)` as the router-local failure and branch boundary while delegating runtime-fallback summary append, auto-scout env forwarding, local execution dispatch, and guardrail recommendation append.
- [ ] Add focused regression coverage for runtime-fallback summary behavior, auto-scout env propagation, local execution dispatch, and guardrail recommendation append without reopening route-state or cloud-route seams.

## Closeout

- [ ] Run the standard gate bundle and capture closeout artifacts under `out/1182-coordinator-symphony-aligned-orchestrator-local-route-shell-extraction/manual/<timestamp>-closeout/`.
- [ ] Run an explicit elegance review.
- [ ] Record the next truthful seam after `1182`.
