# ACTION_PLAN - Coordinator Symphony-Aligned Core Compatibility Response Builders

1. Register `1031` docs-first artifacts, checklist mirrors, task index entries, and freshness coverage.
2. Capture the real-Symphony controller/presenter comparison and bounded next-slice deliberation.
3. Extract the remaining core compatibility response builders from `controlServer.ts` into `observabilitySurface.ts`.
4. Keep `/api/v1/dispatch` explicitly separate as a CO-only extension seam.
5. Update regression/manual evidence to prove unchanged core-route behavior plus preserved extension separation.
6. Run the ordered validation lane, elegance review, and closeout sync.
