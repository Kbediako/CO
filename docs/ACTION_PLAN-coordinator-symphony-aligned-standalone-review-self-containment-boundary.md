# ACTION_PLAN - Coordinator Symphony-Aligned Standalone Review Self-Containment Boundary

## Phase 1 - Docs and registration

- Register `1094` across PRD / TECH_SPEC / ACTION_PLAN / task checklist / `.agent` mirror / `tasks/index.json` / `docs/TASKS.md` / docs freshness registry.
- Capture the post-`1093` residual drift evidence from the final live review log and the delegated slice-shaping note.

## Phase 2 - Bounded implementation

- Extend runtime classification for review-system-adjacent surfaces.
- Add the sustained diff-mode self-containment boundary without disturbing the `1093` `diff`/`audit` contract.
- Keep audit-mode allowances explicit and narrow.

## Phase 3 - Validation and closeout

- Add focused regression fixtures in `tests/run-review.spec.ts`.
- Run the standard validation lane plus pack-smoke.
- Re-run the live wrapper and record whether the reviewer now converges on the bounded diff surface before resuming Symphony controller extraction.
