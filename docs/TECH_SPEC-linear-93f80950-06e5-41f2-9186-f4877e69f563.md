---
id: 20260506-linear-93f80950-06e5-41f2-9186-f4877e69f563
title: "CO-471 build:all strict test typecheck debt"
relates_to: docs/PRD-linear-93f80950-06e5-41f2-9186-f4877e69f563.md
risk: high
owners:
  - Codex
last_review: 2026-05-06
---

# TECH_SPEC Mirror - CO-471 build:all strict test typecheck debt

Canonical spec: `tasks/specs/linear-93f80950-06e5-41f2-9186-f4877e69f563.md`; PRD: `docs/PRD-linear-93f80950-06e5-41f2-9186-f4877e69f563.md`; action plan: `docs/ACTION_PLAN-linear-93f80950-06e5-41f2-9186-f4877e69f563.md`.

## Implementation Summary
- Create the CO-471 packet and registry mirrors only.
- Clear `backlog_head_follow_up_traceability_pending` by registering the traceability surfaces.
- Preserve `npm run build:all` as the release/full-matrix signal.
- Preserve root `tsconfig.json` as the strict test type debt authority.
- Preserve `tsconfig.build.json` as production-build evidence only.
- Future implementation owns the strict test type debt in `ProviderIssueHandoff.test.ts`, `ProviderLinearWorkerRunner.test.ts`, `ControlServerPublicLifecycle.test.ts`, `ProviderMergeCloseout.test.ts`, and `LinearCliShell.test.ts`.
- Do not implement the build:all cleanup in this packet lane.

## Issue-Shaping Contract
- User-request translation carried forward:
  - The issue must not leave Backlog without a docs-first packet and registry mirrors.
  - This packet is not a claim that `npm run build:all` passes.
  - Future implementation must fix the root `tsconfig.json` strict test type debt without weakening the release/full-matrix signal.
- Protected terms / exact artifact and surface names:
  - `npm run build:all`
  - root `tsconfig.json`
  - strict test type debt
  - release/full-matrix signal
  - `tsconfig.build.json`
  - `ProviderIssueHandoff.test.ts`
  - `ProviderLinearWorkerRunner.test.ts`
  - `ControlServerPublicLifecycle.test.ts`
  - `ProviderMergeCloseout.test.ts`
  - `LinearCliShell.test.ts`
  - `build-all-strict-test-typecheck-debt`
  - `backlog_head_follow_up_traceability_pending`
- Nearby wrong interpretations to reject:
  - docs-only packet means implementation is done
  - `npm run build` is enough proof
  - broad root `tsconfig.json` exclusions are acceptable
  - `build:all` can be weakened or deleted
  - CO-458 source-root freshness drift is part of this issue
  - production runtime behavior should change to avoid test typechecking
- Explicit non-goals carried forward:
  - no CO-458 broadening
  - no production runtime behavior change
  - no blanket tsconfig exclusions as silent fallback
  - no weakening/deleting `build:all`
  - no marking done because `npm run build` passes

## Parity / Alignment Matrix

| Surface | Current Truth | Reference Truth | Target Truth / Intended Delta | Explicitly Out Of Scope |
| --- | --- | --- | --- | --- |
| Build command surface | `npm run build` uses `tsconfig.build.json`; `npm run build:all` uses root `tsconfig.json`. | Release/full-matrix posture depends on the broader command. | Packet records that future completion must address `npm run build:all`. | Changing package scripts in this lane. |
| TypeScript config surface | Root `tsconfig.json` sees strict test type debt that production build config can miss. | The debt is a full-matrix signal, not production runtime behavior. | Future implementation keeps the strict signal and fixes or explicitly reviews the named debt. | Blanket exclusions, silent bypasses, runtime changes. |
| Named tests | The debt is anchored on five exact test files. | Issue traceability should preserve exact file names. | Future implementation inspects and fixes each named surface or records a precise reviewed exception. | Generic test-suite cleanup outside CO-471. |
| Backlog traceability | Missing packet blocks promotion through `backlog_head_follow_up_traceability_pending`. | Provider workflow requires repo-side packet evidence. | This packet supplies the traceability evidence only. | Linear/GitHub transitions from this worker. |

## Readiness Gate
- Not done if:
  - packet and mirrors are missing
  - `backlog_head_follow_up_traceability_pending` is not mentioned in packet surfaces
  - future implementation can complete without `npm run build:all` or equivalent non-weaker release/full-matrix proof
  - `npm run build` pass is represented as full completion
  - root `tsconfig.json` excludes broad test debt silently
- Pre-implementation issue-quality review evidence:
  - 2026-05-06: CO-471 is a broad release/full-matrix debt lane; the micro-task path is unavailable.
  - 2026-05-06: Packet setup is sufficient only to clear traceability; implementation remains future work.
  - 2026-05-06: Future implementation must preserve `npm run build:all`, root `tsconfig.json`, and the named strict-test surfaces.
- Safeguard ownership split:
  - This worker owns packet and registry files only.
  - Future implementation owns source/test/config changes and `npm run build:all` validation.
  - Parent/provider workflow owns Linear/GitHub lifecycle.

## Technical Requirements
- Functional requirements:
  1. Create PRD, TECH_SPEC mirror, ACTION_PLAN, canonical task spec, task checklist, and agent mirror for CO-471.
  2. Register `20260506-linear-93f80950-06e5-41f2-9186-f4877e69f563` in `tasks/index.json`.
  3. Add a `docs/TASKS.md` snapshot near active task snapshots.
  4. Add six active rows to `docs/docs-freshness-registry.json`.
  5. Preserve every protected term and non-goal.
  6. Make clear that only traceability is complete.
  7. Future implementation must reproduce and fix the `npm run build:all` strict test type debt.
- Non-functional requirements:
  - packet diff stays inside the declared owned files
  - JSON remains parseable
  - docs gates stay strict
  - no production runtime behavior changes
- Interfaces / contracts:
  - registry id: `20260506-linear-93f80950-06e5-41f2-9186-f4877e69f563`
  - canonical owner marker: `build-all-strict-test-typecheck-debt`
  - release/full-matrix command: `npm run build:all`
  - production build command: `npm run build`

## Fallback Expiry / Refactor Decision
- Applies to fallback, compatibility, legacy, stale, cached, break-glass, or minor-seam behavior? Yes.

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Backlog promotion traceability | `backlog_head_follow_up_traceability_pending` while packet files and mirrors are absent | expire fallback | CO-471 | Linear issue has no repo-side traceability packet | 2026-05-06 | 2026-05-06 | until packet PR lands | Packet files, task index entry, `docs/TASKS.md` snapshot, and freshness rows exist on main | JSON parse, protected-term scan, spec guard, docs checks |
| Release/full-matrix build signal | Treating `npm run build` or `tsconfig.build.json` success as enough while `npm run build:all` exposes strict test type debt | remove fallback | CO-471 | Future implementation tries to complete with only production-build proof | 2026-05-06 | 2026-05-06 | 0 days | CO-471 completion requires `npm run build:all` evidence or an explicit reviewed release/full-matrix replacement that is not weaker | `npm run build:all`, focused named-test type debt review, docs/review gates |
| Production build config | `tsconfig.build.json` remains a narrower production-build TypeScript config | justify retaining fallback | CO-471 | Production build needs runtime/package-focused compile coverage | existing contract | 2026-05-06 | non-expiring supported path | Retained only while `npm run build:all` remains the strict release/full-matrix signal | `npm run build`, `npm run build:all` for release/full-matrix lanes, docs checks |

- Durable retention evidence: `tsconfig.build.json` is retained as the supported production build config named by `npm run build`; it is not retained as a bypass for root `tsconfig.json` strict test type debt.
- Large-refactor check: future implementation may choose a larger type/config consolidation if local evidence shows the same strict debt spans multiple lifecycle phases, but this packet authorizes no source, test, runtime, or package-script refactor.

## Architecture & Data
- Architecture / design adjustments:
  - None in this packet lane.
  - Future implementation must decide the smallest safe repair after reproducing the build:all failure.
- Data model changes / migrations:
  - None.
- External dependencies / integrations:
  - None.

## Validation Plan
- Packet setup:
  - JSON parse for `tasks/index.json`
  - JSON parse for `docs/docs-freshness-registry.json`
  - targeted scan for `CO-471` and `93f80950-06e5-41f2-9186-f4877e69f563`
  - targeted protected-term scan
  - `node scripts/spec-guard.mjs --dry-run`
  - `npm run docs:check`
  - `npm run docs:freshness`
- Future implementation:
  - reproduce the current `npm run build:all` strict test type debt
  - fix or explicitly review the debt in each named test file
  - run `npm run build:all` before completion

## Open Questions
- Future implementation must capture the exact TypeScript diagnostics for each protected test file.
- Future implementation must decide whether fixture typing, helper API typing, or source contract changes best removes each debt item.

## Approvals
- Reviewer: CO operator / provider-worker parent lane.
- Date: 2026-05-06
