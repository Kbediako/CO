---
id: 20260506-linear-93f80950-06e5-41f2-9186-f4877e69f563
title: "CO-471 build:all strict test typecheck debt"
relates_to: docs/PRD-linear-93f80950-06e5-41f2-9186-f4877e69f563.md
risk: high
owners:
  - Codex
last_review: 2026-05-07
---

# TECH_SPEC Mirror - CO-471 build:all strict test typecheck debt

Canonical spec: `tasks/specs/linear-93f80950-06e5-41f2-9186-f4877e69f563.md`; PRD: `docs/PRD-linear-93f80950-06e5-41f2-9186-f4877e69f563.md`; action plan: `docs/ACTION_PLAN-linear-93f80950-06e5-41f2-9186-f4877e69f563.md`.

## Implementation Summary
- Keep the CO-471 packet and registry mirrors current after the traceability packet PR.
- Clear the active `npm run build:all` failure by replacing the unbounded root `tsconfig.json` failure with an explicit root strict typecheck baseline guard.
- Preserve `npm run build:all` as the release/full-matrix signal.
- Preserve root `tsconfig.json` as the strict test type debt authority.
- Preserve `tsconfig.build.json` as production-build evidence only.
- Keep the strict test type debt in `ProviderIssueHandoff.test.ts`, `ProviderLinearWorkerRunner.test.ts`, `ControlServerPublicLifecycle.test.ts`, `ProviderMergeCloseout.test.ts`, and `LinearCliShell.test.ts` visible through `npm run typecheck:root` and checked through `tasks/baselines/build-all-strict-typecheck-debt.json`.

## Issue-Shaping Contract
- User-request translation carried forward:
  - The issue must not leave Backlog without a docs-first packet and registry mirrors.
  - `npm run build:all` must pass without deleting the root strict typecheck signal.
  - `npm run typecheck:root` must still reproduce root `tsconfig.json` strict test type debt until the baseline is burned down.
- Protected terms / exact artifact and surface names:
  - `npm run build:all`
  - root `tsconfig.json`
  - strict test type debt
  - release/full-matrix signal
  - `tsconfig.build.json`
  - `ProviderIssueHandoff.test.ts`
  - `ProviderLinearWorkerRunner.test.ts`
  - `ControlServerPublicLifecycle.test.ts`
  - `ProviderMergeCloseout.test.ts`
  - `LinearCliShell.test.ts`
  - `build-all-strict-test-typecheck-debt`
  - `backlog_head_follow_up_traceability_pending`
- Nearby wrong interpretations to reject:
  - docs-only packet means implementation is done
  - `npm run build` is enough proof
  - broad root `tsconfig.json` exclusions are acceptable
  - `build:all` can be weakened or deleted
  - CO-458 source-root freshness drift is part of this issue
  - production runtime behavior should change to avoid test typechecking
- Explicit non-goals carried forward:
  - no CO-458 broadening
  - no production runtime behavior change
  - no blanket tsconfig exclusions as silent fallback
  - no weakening/deleting `build:all`
  - no marking done because `npm run build` passes

## Parity / Alignment Matrix

| Surface | Current Truth | Reference Truth | Target Truth / Intended Delta | Explicitly Out Of Scope |
| --- | --- | --- | --- | --- |
| Build command surface | `npm run build` uses `tsconfig.build.json`; `npm run build:all` runs build, patterns, and the root strict typecheck baseline guard. | Release/full-matrix posture depends on the broader command. | `build:all` passes only when raw diagnostics match the checked baseline or reach zero. | Weakening or deleting `build:all`. |
| TypeScript config surface | Root `tsconfig.json` sees strict test type debt that production build config can miss. | The debt is a full-matrix signal, not production runtime behavior. | `npm run typecheck:root` reproduces raw debt; `npm run typecheck:root-baseline` enforces exact diagnostic counts and repo-root-normalized diagnostic identities. | Blanket exclusions, silent bypasses, runtime changes. |
| Named tests | The debt is anchored on five exact test files. | Issue traceability should preserve exact file names. | Baseline records per-file diagnostic counts so future cleanup is deliberate and reviewable. | Generic runtime behavior or CO-458 scope. |
| Backlog traceability | Missing packet blocks promotion through `backlog_head_follow_up_traceability_pending`. | Provider workflow requires repo-side packet evidence. | This packet supplies the traceability evidence only. | Linear/GitHub transitions from this worker. |

## Readiness Gate
- Not done if:
  - packet and mirrors are missing
  - `backlog_head_follow_up_traceability_pending` is not mentioned in packet surfaces
  - `npm run build:all` still fails from known strict test type debt
  - `npm run build` pass is represented as full completion
  - root `tsconfig.json` excludes broad test debt silently
- Pre-implementation issue-quality review evidence:
  - 2026-05-06: CO-471 is a broad release/full-matrix debt lane; the micro-task path is unavailable.
  - 2026-05-07: Implementation chooses the staged baseline guard; root strict debt remains reproducible and cannot drift silently.
  - 2026-05-07: `npm run build:all` must preserve `npm run build`, `npm run build:patterns`, root `tsconfig.json`, and the named strict-test surfaces.
- Safeguard ownership split:
  - Parent owns package scripts, the baseline guard, docs packet updates, validation, and PR lifecycle.
  - Same-issue child lanes may own explicitly delegated test files only.
  - Parent/provider workflow owns Linear/GitHub lifecycle.

## Technical Requirements
- Functional requirements:
  1. Keep PRD, TECH_SPEC mirror, ACTION_PLAN, canonical task spec, task checklist, and agent mirror current for CO-471.
  2. Preserve `20260506-linear-93f80950-06e5-41f2-9186-f4877e69f563` in `tasks/index.json`.
  3. Update `npm run build:all` to run `npm run build`, `npm run build:patterns`, and the root strict typecheck baseline guard.
  4. Add `npm run typecheck:root` as the raw root strict debt reproduction command.
  5. Add `scripts/build-all-strict-typecheck.mjs` and `tasks/baselines/build-all-strict-typecheck-debt.json` so diagnostic drift fails closed.
  6. Preserve every protected term and non-goal.
- Non-functional requirements:
  - JSON remains parseable
  - docs gates stay strict
  - no production runtime behavior changes
- Interfaces / contracts:
  - registry id: `20260506-linear-93f80950-06e5-41f2-9186-f4877e69f563`
  - canonical owner marker: `build-all-strict-test-typecheck-debt`
  - release/full-matrix command: `npm run build:all`
  - production build command: `npm run build`
  - raw strict typecheck command: `npm run typecheck:root`
  - baseline guard command: `npm run typecheck:root-baseline`

## Fallback Expiry / Refactor Decision
- Applies to fallback, compatibility, legacy, stale, cached, break-glass, or minor-seam behavior? Yes.

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Backlog promotion traceability | `backlog_head_follow_up_traceability_pending` while packet files and mirrors are absent | expire fallback | CO-471 | Linear issue has no repo-side traceability packet | 2026-05-06 | 2026-05-06 | until packet PR lands | Packet files, task index entry, `docs/TASKS.md` snapshot, and freshness rows exist on main | JSON parse, protected-term scan, spec guard, docs checks |
| Release/full-matrix build signal | Treating `npm run build` or `tsconfig.build.json` success as enough while raw `npm run typecheck:root` exposes strict test type debt | remove fallback | CO-471 | Active lanes keep waiving `build:all` as noisy pre-existing debt | 2026-05-06 | 2026-05-07 | 0 days | `npm run build:all` runs production build, pattern build, and exact root diagnostic baseline guard | `npm run build:all`, `npm run typecheck:root-baseline`, docs/review gates |
| Production build config | `tsconfig.build.json` remains a narrower production-build TypeScript config | justify retaining fallback | CO-471 | Production build needs runtime/package-focused compile coverage | existing contract | 2026-05-06 | non-expiring supported path | Retained only while `npm run build:all` remains the strict release/full-matrix signal | `npm run build`, `npm run build:all` for release/full-matrix lanes, docs checks |

- Durable retention evidence: `tsconfig.build.json` is retained as the supported production build config named by `npm run build`; it is not retained as a bypass because `npm run typecheck:root-baseline` still runs the full root `tsconfig.json` check.
- Large-refactor check: exact diagnostic baselining is smaller than a broad source/test refactor and keeps the debt owner explicit without touching production runtime behavior.

## Architecture & Data
- Architecture / design adjustments:
  - `build:all` now composes production build, pattern build, and strict diagnostic baseline validation.
  - The raw root `tsconfig.json` command remains separately available for debt burn-down.
- Data model changes / migrations:
  - None.
- External dependencies / integrations:
  - None.

## Validation Plan
- Packet setup:
  - JSON parse for `tasks/index.json`
  - JSON parse for `docs/docs-freshness-registry.json`
  - targeted scan for `CO-471` and `93f80950-06e5-41f2-9186-f4877e69f563`
  - targeted protected-term scan
  - `node scripts/spec-guard.mjs --dry-run`
  - `npm run docs:check`
  - `npm run docs:freshness`
- Implementation:
  - reproduce the current raw root strict typecheck debt
  - update the checked diagnostic baseline after reviewing the current clusters
  - run `npm run typecheck:root-baseline`
  - run `npm run build:all` before completion

## Open Questions
- Future cleanup must update `tasks/baselines/build-all-strict-typecheck-debt.json` whenever it intentionally changes strict diagnostic counts.

## Approvals
- Reviewer: CO operator / provider-worker parent lane.
- Date: 2026-05-06
