# ACTION_PLAN - Coordinator Symphony-Aligned Control Server Seed Loading Extraction

## Steps

1. Register `1086` docs-first from the `1085` next-slice note plus bounded read-only seam inspection.
2. Extract the five JSON seed reads from `ControlServer.start()` into one control-local helper.
3. Keep token generation, seeded runtime assembly, request shell creation, bootstrap assembly, startup sequencing, and shutdown ordering unchanged.
4. Add focused regressions for missing-file tolerance and preserved loaded seed payload shape.
5. Run the validation bundle, sync mirrors, and queue the next bounded Symphony-aligned seam.
