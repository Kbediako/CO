# PRD - CO: route docs-freshness owner reuse by canonical cohort without broadening CO-320

## Traceability
- Linear issue: `CO-323` / `80d4473e-e9c7-4d4d-9340-bc1193103a7d`
- Linear URL: https://linear.app/asabeko/issue/CO-323/co-route-docs-freshness-owner-reuse-by-canonical-cohort-without
- Source issue: `CO-319` / `7b3de1c1-f420-4203-bb8c-494dadecaa88`
- Stamped live owner issue: `CO-320`
- Terminal historical owner metadata: `CO-300`
- Source anchor: `ctx:sha256:6ac09dabeeaf032143a1979732d6482c8b15295f84f26bdd31105334dccb8dcf#chunk:c000001`
- Source payload path from parent prompt: `../../.runs/linear-80d4473e-e9c7-4d4d-9340-bc1193103a7d/cli/2026-04-23T03-19-56-969Z-7c4795cc/memory/source-0/source.txt`

## Summary
- Problem Statement: `docs:freshness:maintain` still depends on the single global `docs/docs-catalog.json` field `policies.rolling_freshness_cohorts.owner_issue`. Re-pointing that field from terminal `CO-300` to cohort-specific `CO-320` makes unrelated stale cohorts inherit `CO-320`, including older 90-day packet/mirror/report rows and adjacent Mar 23 packet families outside the exact `tasks/tasks-*` canonical key.
- Desired Outcome: add a truthful owner-resolution path that can reuse stamped live owners by exact `canonical_owner_key` while keeping `CO-320` cohort-specific and preventing duplicate follow-up owners for the same cohort.

## User Request Translation
- Keep `CO-319` narrow to owner recreation and packet/mirror truthfulness.
- Do not solve the terminal-owner blocker by repointing `policies.rolling_freshness_cohorts.owner_issue` to `CO-320`.
- Let future provider lanes surface `CO-320` only for the exact canonical owner key `docs_freshness_candidate|doc_class:task_packet|path_family:tasks/tasks-*|last_review:2026-03-23|cadence_days:30`.
- Preserve duplicate-prevention semantics by routing stamped live owners through exact `canonical_owner_key` matching.

## Intent Checksum
- Exact problem to preserve: creating the stamped live owner issue is not the same as safely re-pointing the single global rolling-freshness owner field.
- Protected terms / exact artifact and surface names: `docs/docs-catalog.json`, `policies.rolling_freshness_cohorts.owner_issue`, `docs:freshness:maintain`, `owner_issue`, `owner_issue_action`, `canonical_owner_key`, `docs_freshness_candidate|doc_class:task_packet|path_family:tasks/tasks-*|last_review:2026-03-23|cadence_days:30`, `CO-320`, `CO-300`, `block_policy_over_budget`, `configured_owner_terminal`.
- Nearby wrong interpretations to reject: reopening `CO-300`, broadening `CO-320` into the owner for every stale historical cohort, weakening `docs:freshness` or `docs:freshness:maintain`, and rolling the full historical Mar 23 refresh back into `CO-319`.

## Parity / Alignment Matrix

| Surface | Current Truth | Target Truth |
| --- | --- | --- |
| Global rolling owner field | `policies.rolling_freshness_cohorts.owner_issue` is a single global owner pointer and can make unrelated cohorts inherit one issue. | The global field is not repointed to cohort-specific `CO-320`; terminal owner metadata remains evidence only. |
| Exact Mar 23 `tasks/tasks-*` cohort | `CO-320` exists as the stamped live owner for the exact canonical key. | `docs:freshness:maintain` can surface `CO-320` for that exact key. |
| Unrelated stale cohorts | A policy re-home attempt showed unrelated Jan 22 and adjacent Mar 23 cohorts under `owner_issue=CO-320`. | Unrelated cohorts do not report `CO-320` unless their exact canonical owner key matches the stamped owner. |
| Duplicate prevention | Canonical owner markers exist, but live stamped owner reuse is not the safe owner-routing source for maintenance output. | Future lanes reuse stamped owner issues by exact `canonical_owner_key` instead of creating duplicates. |

## Scope
- Create the CO-323 docs-first packet and registry mirrors.
- Specify the owner-resolution contract for parent-owned implementation.
- Preserve issue wording for protected terms, non-goals, Not Done If, and acceptance criteria.
- Keep this child lane docs-only with no source, test, package, or `docs/docs-catalog.json` edits.

## Acceptance Criteria
- `docs:freshness:maintain` can truthfully reuse a live stamped owner issue for an exact canonical owner key without routing unrelated candidate cohorts to that same issue.
- `CO-320` remains cohort-specific to `docs_freshness_candidate|doc_class:task_packet|path_family:tasks/tasks-*|last_review:2026-03-23|cadence_days:30`.
- Future lanes reuse stamped owner issues by canonical owner key instead of creating duplicates for the same cohort.
- Terminal owner metadata like `CO-300` remains evidence only and cannot be reused as the live owner path.

## Not Done If
- Future provider lanes still need to broaden `CO-320` through the single global `owner_issue` field just to avoid `configured_owner_terminal`.
- `docs:freshness:maintain` still presents unrelated candidate cohorts as owned by `CO-320`.
- The exact canonical owner key for the Mar 23 `tasks/tasks-*` cohort cannot be reused deterministically without creating duplicates.
- The fix depends on reopening or reusing terminal `CO-300`.

## Non-Goals
- Do not solve this by setting the single global `rolling_freshness_cohorts.owner_issue` field to `CO-320` or any other cohort-specific issue.
- Do not weaken docs freshness policy, owner verification, or duplicate-prevention behavior.
- Do not broaden `CO-320` beyond the exact Mar 23 `tasks/tasks-*` cohort it was created to own.
- Do not reopen terminal `CO-300` as the live owner path.

## Stakeholders
- Product: CO operator workflow.
- Engineering: Codex Orchestrator docs freshness and provider Linear maintainers.
- Design: Not applicable.

## Metrics & Guardrails
- Primary Success Metrics: maintenance output routes `CO-320` only for the exact canonical owner key and does not create duplicate owners for that cohort.
- Guardrails / Error Budgets: fail closed on terminal configured owners, keep unrelated cohorts unowned by `CO-320`, and preserve existing docs freshness enforcement.

## Technical Considerations
- Architectural Notes: owner resolution should route live stamped owners by exact `canonical_owner_key`, not by repointing a global policy owner.
- Dependencies / Integrations: `docs:freshness:maintain` candidate cohort output, canonical owner markers, provider Linear follow-up duplicate-prevention behavior, and `docs/docs-catalog.json` policy metadata.

## Open Questions
- None blocking for docs-first. Parent implementation should decide the smallest source seam that resolves owner routing without policy repointing.

## Approvals
- Product: parent provider worker, 2026-04-23.
- Engineering: parent provider worker, 2026-04-23.
- Design: Not applicable.
