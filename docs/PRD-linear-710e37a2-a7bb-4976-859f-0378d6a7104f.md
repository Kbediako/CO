# PRD - CO: clear repo-wide stale spec-review dates that are blocking unrelated merge-ready PRs at spec-guard

## Added by Bootstrap 2026-04-10

## Traceability
- Linear issue: `CO-142` / `710e37a2-a7bb-4976-859f-0378d6a7104f`
- Linear URL: https://linear.app/asabeko/issue/CO-142/co-clear-repo-wide-stale-spec-review-dates-that-are-blocking-unrelated
- Representative blocked PRs:
  - `#395`
  - `#397`
  - `#399`
  - `#400`
  - `#401`

## Summary
- Problem Statement: `node scripts/spec-guard.mjs` currently fails unrelated merge-ready PRs on the same completed March 10 packet cohort because the packet specs from `tasks/specs/1093-coordinator-symphony-aligned-standalone-review-diff-audit-surface-split.md` through `tasks/specs/1109-coordinator-symphony-aligned-standalone-review-audit-exported-env-startup-anchor-propagation.md` still exist as active spec files with stale `last_review` dates. Those lanes are already marked `status: completed`, but the current guard still treats every Markdown file under `tasks/specs/` as an active freshness target.
- Desired Outcome: completed packet docs for `1093` through `1109` are archived or otherwise quarantined truthfully, archived spec stubs no longer participate in active-spec freshness gating, and representative blocked PRs are no longer stopped on this repo-wide stale cohort before their own validation runs.

## User Request Translation (Context Anchor)
- User intent / needs (in your own words): clear the repo-wide stale-spec blocker without papering over it, keep the fix truthful to packet status, and avoid broadening this lane into generic docs-freshness cleanup or conflict recovery.
- Success criteria / acceptance:
  - `node scripts/spec-guard.mjs` no longer fails on the `1093` through `1109` cohort
  - the fix reflects repo truth: either still-active packets are re-reviewed with matching evidence or completed packets are archived/quarantined
  - representative blocked PRs have concrete evidence that the repo-wide stale cohort is no longer their Core Lane blocker
  - `CO-102` remains scoped to the separate archive-selection / `docs:freshness` seam
- Constraints / non-goals:
  - do not waive the failures manually while leaving the stale cohort in place
  - do not reopen generic docs-freshness cleanup
  - do not broaden into merge-conflict recovery or unrelated UI/status work

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `clear repo-wide stale spec-review dates`
  - `blocking unrelated merge-ready PRs`
  - `spec-guard`
  - `1093`-`1109`
  - `CO-102`
- Protected terms / exact artifact and surface names:
  - `node scripts/spec-guard.mjs`
  - `tasks/specs/1093-coordinator-symphony-aligned-standalone-review-diff-audit-surface-split.md`
  - `tasks/specs/1109-coordinator-symphony-aligned-standalone-review-audit-exported-env-startup-anchor-propagation.md`
  - `docs/implementation-docs-archive-policy.json`
  - `docs/docs-freshness-registry.json`
  - `tasks/index.json`
  - `scripts/implementation-docs-archive.mjs`
- Nearby wrong interpretations to reject:
  - this is only a `docs:freshness` baseline problem so `CO-102` should absorb it
  - the truthful fix is just refreshing dates on completed packets without any archival or quarantine decision
  - manually rerunning or waiving the blocked PRs is sufficient
  - archived spec stubs can remain part of active-spec freshness gating

## Parity / Alignment Matrix
- Not a parity lane, but repo truth still needs a current/reference/target check:
  - Current truth:
    - the completed packet specs from `tasks/specs/1093-coordinator-symphony-aligned-standalone-review-diff-audit-surface-split.md` through `tasks/specs/1109-coordinator-symphony-aligned-standalone-review-audit-exported-env-startup-anchor-propagation.md` all carry `last_review: 2026-03-10`
    - `tasks/index.json` still carries the same packet rows as active registry entries with March 10 `last_review`
    - implementation-doc archiving already emits `<!-- docs-archive:stub -->` stubs and archived registry entries, but `scripts/spec-guard.mjs` currently scans all spec Markdown files without skipping archived stubs
  - Reference truth:
    - completed packets should be archived or quarantined truthfully
    - only active specs should participate in active-spec freshness gating
  - Target truth / intended delta:
    - the completed `1093` through `1109` packet cohort is archived with matching registry/task metadata
    - `spec-guard` ignores archived spec stubs so unrelated PRs stop failing on archived packets
  - Explicitly out-of-scope differences:
    - broader docs-freshness baseline cleanup
    - archive-selection policy redesign
    - unrelated PR conflict recovery

## Not Done If
- Only the currently affected PRs are manually rerun or waived while the stale `1093` through `1109` cohort still trips `spec-guard` for unrelated work.
- The cohort remains active in the repo with stale `last_review` dates and no archival or quarantine decision.
- `CO-102` is reopened or blurred instead of giving this blocker its own explicit owner.

## Goals
- Make the `1093` through `1109` packet treatment match their completed status.
- Keep active-spec freshness focused on active specs rather than archived packet stubs.
- Prove the fix against the representative merge-ready PR cohort already cited in the issue.

## Non-Goals
- Generic docs-freshness cleanup across the repo.
- Manual waiver workflows for unrelated PRs.
- Reopening the archive-selection policy work already owned by `CO-102`.

## Stakeholders
- Product: CO operators and authors waiting on merge-ready PRs blocked by repo baseline guard failures
- Engineering: maintainers of `spec-guard`, task docs, and implementation-doc archive flows
- Design: N/A

## Metrics & Guardrails
- Primary Success Metrics:
  - `spec-guard` passes without reporting the `1093` through `1109` cohort
  - the archived cohort is reflected consistently in packet docs and registry metadata
  - representative blocked PR evidence no longer points at this stale cohort as the failure owner
- Guardrails / Error Budgets:
  - keep the implementation bounded to the stale completed packet cohort and active-spec freshness semantics
  - avoid broad repo-wide archive churn outside the targeted cohort
  - preserve `CO-102` ownership boundaries explicitly

## User Experience
- Personas:
  - author of a merge-ready PR blocked by repo baseline `spec-guard`
  - maintainer auditing why a completed packet cohort is still participating in active freshness
  - reviewer checking that the archival treatment is truthful and bounded
- User Journeys:
  - a merge-ready PR reruns Core Lane and does not stop on the March 10 stale packet cohort
  - a reviewer inspecting the archived docs sees a clear stub plus archive metadata
  - future completed-packet archives do not silently reintroduce the same stale-spec blocker

## Technical Considerations
- Architectural Notes:
  - the narrow seam is the interaction between implementation-doc archival stubs and `spec-guard` freshness scanning
  - the completed cohort should follow the same archive conventions already used elsewhere in the repo
- Dependencies / Integrations:
  - `scripts/spec-guard.mjs`
  - `docs/implementation-docs-archive-policy.json`
  - `docs/docs-freshness-registry.json`
  - `tasks/index.json`
  - representative PR metadata for `#395`, `#397`, `#399`, `#400`, and `#401`

## Open Questions
- Whether representative unblock evidence is best captured from the PR metadata alone or from a repo-local Core Lane rerun against one or more representative heads after the baseline fix lands.

## Approvals
- Product: pending
- Engineering: pending docs-review
- Design: N/A
