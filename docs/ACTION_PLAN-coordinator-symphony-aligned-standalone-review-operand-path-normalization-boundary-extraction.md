# ACTION_PLAN: Coordinator Symphony-Aligned Standalone Review Operand Path Normalization Boundary Extraction

## Objective

Open the next truthful standalone-review follow-on after `1211` by extracting the shared operand/path normalization helpers still reused across `review-execution-state` analyzers.

## Steps

1. Register `1212` docs-first with explicit evidence that the operand/path normalization cluster remains inline inside `scripts/lib/review-execution-state.ts`.
2. Extract the bounded normalization helper while preserving policy ownership in `scripts/lib/review-execution-state.ts`.
3. Add only the focused regressions needed to pin operand expansion and audit-anchor path normalization behavior.
4. Run the lane validations and record any truthful review overrides.
5. Sync closeout mirrors and reassess whether the next truthful follow-on remains in operand/meta-surface support logic or shifts to a different review-behavior family.
