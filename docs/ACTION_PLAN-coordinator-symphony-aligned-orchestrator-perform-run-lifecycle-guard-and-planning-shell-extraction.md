# ACTION_PLAN: Coordinator Symphony-Aligned Orchestrator Perform-Run-Lifecycle Guard-and-Planning Shell Extraction

## Prep

- [ ] Capture local deliberation plus docs-review approval or explicit override for the guard-and-planning seam.
- [ ] Keep the docs/index/task mirrors synchronized for `1163`.

## Implementation

- [ ] Introduce one bounded class-local guard-and-planning helper adjacent to `performRunLifecycle(...)`.
- [ ] Rewire `performRunLifecycle(...)` to delegate guard execution and scheduler planning through that helper without changing surrounding lifecycle ownership.
- [ ] Keep focused ordering / forwarding / short-circuit regressions green.

## Validation

- [ ] Run the deterministic gate bundle for the final tree.
- [ ] Run a bounded review pass plus an explicit elegance review.
- [ ] Capture manual/mock guard-and-planning evidence and the next-slice note before closeout.
