# ACTION_PLAN: Coordinator Symphony-Aligned Standalone Review Shell Command Parser Review-Support Classification

## Objective

Finish the next truthful post-`1217` standalone-review parity lane by classifying the extracted shell-command parser family as `review-support` wherever bounded review already recognizes adjacent helper families.

## Steps

1. Register the lane docs-first with explicit evidence that the shell-command parser family is currently omitted from `review-support` classification and touched-family parity.
2. Update `scripts/lib/review-meta-surface-normalization.ts` so the parser helper source and built pair inherit the same `review-support` and touched-family behavior as adjacent extracted helpers.
3. Add only the focused normalization and review-state regressions needed to prove the new helper family inherits the existing bounded helper-family contract.
4. Run the lane validations and record the resulting review or explicit override evidence.
5. Sync closeout mirrors and reassess the next truthful standalone-review seam only after the parser-family parity gap is resolved.
