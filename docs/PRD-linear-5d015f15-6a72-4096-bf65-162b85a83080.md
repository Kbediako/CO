# PRD - CO-367 current-host-unhealthy matcher wording drift

## Traceability
- Linear issue: `CO-367` / `5d015f15-6a72-4096-bf65-162b85a83080`
- MCP task id: `linear-5d015f15-6a72-4096-bf65-162b85a83080`
- Canonical task id: `20260425-linear-5d015f15-6a72-4096-bf65-162b85a83080`
- Canonical TECH_SPEC: `tasks/specs/linear-5d015f15-6a72-4096-bf65-162b85a83080.md`
- Docs TECH_SPEC: `docs/TECH_SPEC-linear-5d015f15-6a72-4096-bf65-162b85a83080.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-5d015f15-6a72-4096-bf65-162b85a83080.md`
- Task checklist: `tasks/tasks-linear-5d015f15-6a72-4096-bf65-162b85a83080.md`
- Agent-task mirror: `.agent/task/linear-5d015f15-6a72-4096-bf65-162b85a83080.md`
- Source anchor: `ctx:sha256:e84202fb7f21807b41c7ad7277361061de8c5c4a7253dc560d87722d58ead25c#chunk:c000001`

## Summary
- Problem Statement: `isCurrentHostUnhealthyErrorMessage` in `orchestrator/src/cli/coStatusCliShell.ts` still carried a fallback substring whose word order drifted from the current attach-shell network failure message. The canonical `current-host-unhealthy` marker is active, but non-canonical or legacy messages can silently lose matcher coverage when the fallback is stale.
- Desired Outcome: the matcher recognizes the canonical `current-host-unhealthy` marker, the current invariant attach-shell unhealthy-host wording, and intentionally supported legacy wording, with focused regression coverage for current and legacy message variants and no change to CO-363 active issue projection semantics.

## User Request Translation
- User intent: finish CO-367 as a narrow hardening follow-up from PR #644 CodeRabbit feedback, not as another CO STATUS projection redesign.
- Acceptance:
  - matcher uses `current-host-unhealthy` and/or invariant current attach-shell wording
  - legacy wording remains covered if intentionally supported
  - focused regression covers current and legacy unhealthy-host wording
  - CO-363 active issue projection semantics remain unchanged
- Constraints:
  - preserve exact surfaces `isCurrentHostUnhealthyErrorMessage`, `current-host-unhealthy`, `control_endpoint.json`, `co-status --format json`, and `orchestrator/src/cli/coStatusCliShell.ts`
  - do not alter producer wording unless source inspection proves it is required
  - do not expand into active issue projection, provider-intake, endpoint rotation, or dashboard rendering behavior

## Intent Checksum
- Protected terms / exact artifact and surface names:
  - `CO-367`
  - `CO-363`
  - `PR #644`
  - `CodeRabbit`
  - `isCurrentHostUnhealthyErrorMessage`
  - `current-host-unhealthy`
  - `control_endpoint.json`
  - `co-status --format json`
  - `orchestrator/src/cli/coStatusCliShell.ts`
  - `orchestrator/src/cli/coStatusAttachCliShell.ts`
  - `orchestrator/tests/CoStatusCliShell.test.ts`
- Nearby wrong interpretations to reject:
  - treating this as a broad CO STATUS projection or provider-intake lane
  - removing legacy fallback coverage without an explicit decision
  - relying only on the canonical marker while leaving current non-canonical wording uncovered
  - changing attach-shell producer text when matcher hardening is sufficient
  - weakening CO-363 degraded read or active projection behavior

## Parity / Alignment Matrix
| Surface | Current truth | Reference truth | Target truth / intended delta | Explicitly out of scope |
| --- | --- | --- | --- | --- |
| Attach-shell producer message | Network failures now include the canonical marker and current stale-endpoint wording with `stale endpoint after control-host restart`. | Producer text can evolve, but current invariant phrases should stay matchable. | Matcher recognizes the marker and current invariant fallback wording. | Rewriting producer message format. |
| Legacy stale-endpoint wording | Older fallback wording placed `control_endpoint.json has not rotated` directly after `control-host unavailable`. | Legacy/replayed errors may still carry the old form. | Legacy wording remains intentionally covered. | Supporting arbitrary unrelated network errors. |
| `co-status --format json` degraded read | CO-363 added degraded read behavior when the current host is unhealthy. | Host-health classification must remain separate from active issue projection semantics. | Matcher hardening changes only unhealthy-host classification detection. | Changing active issue projection, provider-intake overlays, or CO-363 semantics. |

## Not Done If
- Current attach-shell unhealthy-host wording without relying on the canonical marker is not matched.
- Legacy unhealthy-host wording is dropped without an explicit non-support decision.
- Focused tests do not cover both current and legacy matcher wording.
- The implementation changes CO-363 active issue projection, provider-intake truth overlays, or dashboard semantics.

## Goals
- Harden matcher wording against current producer drift.
- Preserve intentional legacy fallback coverage.
- Add focused regression coverage for current and legacy unhealthy-host wording.
- Keep the implementation small and auditable.

## Non-Goals
- No broad CO STATUS redesign.
- No provider-intake or active issue projection change.
- No endpoint rotation retry redesign.
- No Linear workflow or PR feedback automation change.

## Approvals
- Reviewer: parent provider worker issue-quality self-review.
- Date: 2026-04-25
