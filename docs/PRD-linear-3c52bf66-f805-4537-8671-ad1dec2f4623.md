# PRD - CO: maintain docs freshness after terminal CO-300 owner

## Traceability
- Linear issue: `CO-324` / `3c52bf66-f805-4537-8671-ad1dec2f4623`
- Linear URL: https://linear.app/asabeko/issue/CO-324/co-maintain-docs-freshness-after-terminal-co-300-owner
- Task id: `linear-3c52bf66-f805-4537-8671-ad1dec2f4623`
- Canonical spec: `tasks/specs/linear-3c52bf66-f805-4537-8671-ad1dec2f4623.md`
- Canonical owner key: `docs:freshness:maintain`
- Canonical owner marker: `codex-orchestrator:canonical-owner-key=docs:freshness:maintain`

## Summary
- Problem Statement: current-main `docs:freshness:maintain` fails with `block_unowned_repo_debt` because the configured owner `CO-300` is terminal (`Done`) and cannot remain the live owner for recurring docs-freshness maintenance.
- Desired Outcome: make `CO-324` the live same-project owner for `docs:freshness:maintain`, preserve `CO-300` terminal-owner evidence as historical context, and review/refresh the remaining current-main stale docs without weakening freshness gates or broadening `CO-321`.

## User Request Translation
- User intent: complete the provider-worker lane by replacing terminal-owner reuse with a live same-project owner for the current docs freshness maintenance baseline.
- Success criteria:
  - `docs:freshness:maintain` no longer reports `block_unowned_repo_debt` solely because `CO-300` is terminal.
  - `CO-324` is recorded as the live owner for canonical key `docs:freshness:maintain`.
  - current-main stale rows from the baseline are refreshed or dispositioned with evidence.
- Constraints:
  - do not reopen, reuse, or hide terminal `CO-300`.
  - do not weaken `docs:freshness`, `docs:freshness:maintain`, owner-terminal checks, registry checks, or stale cadence policy.
  - do not broaden `CO-321` or mutate its refreshed `tasks/specs` cohort.

## Intent Checksum
- Protected terms / exact surfaces:
  - `docs:freshness:maintain`
  - `block_unowned_repo_debt`
  - `CO-300`
  - `Done`
  - `terminal owner`
  - `blocking_changed_paths=[]`
  - `.agent/task`
  - `docs/ACTION_PLAN-*`
  - `docs/PRD-*`
  - `docs/TECH_SPEC-*`
  - `tasks/tasks-*`
  - `docs/guides/collab-vs-mcp.md`
  - `docs/guides/evaluation-playbook.md`
  - `docs/reference/metrics-collab-context-rot.md`
  - `docs/release-notes-template-addendum.md`
- Nearby wrong interpretations to reject:
  - reopening `CO-321`
  - modifying the CO-321 `tasks/specs` cohort
  - weakening freshness or maintenance gates
  - keeping terminal `CO-300` as live owner
  - silently archiving or reclassifying historical rows without review evidence

## Parity / Alignment Matrix

| Surface | Current Truth | Reference Truth | Target Truth | Explicitly Out Of Scope |
| --- | --- | --- | --- | --- |
| Maintenance owner | `docs:freshness:maintain` verifies `CO-300` as `Done`, `is_terminal=true`, `usable=false`. | Terminal owner metadata is historical evidence only. | `CO-324` is the live owner for `docs:freshness:maintain`; `CO-300` remains historical context. | Reopening `CO-300` or bypassing owner verification. |
| Freshness debt | Current-main baseline has 31 stale rows, including 27 eligible task/report rows and 4 hard-stale Active Guide/reference rows. | Stale rows must be reviewed, archived, reclassified, or explicitly owned. | Reviewed stale rows are refreshed to 2026-04-23 with classification evidence. | Broad stale-cadence or rolling-cap changes. |
| CO-321 boundary | CO-321 removed its exact tasks/specs last_review 2026-03-23 candidate. | Remaining repo debt is separate baseline work. | CO-324 handles current-main owner and stale-row disposition without changing CO-321. | Expanding CO-321 or its packet. |

## Not Done If
- `docs:freshness:maintain` still reports `block_unowned_repo_debt` because `CO-300` is terminal and no live same-project owner exists.
- Current stale rows are hidden by policy weakening instead of being reviewed and refreshed, dispositioned, or explicitly owned.
- `CO-321` is broadened into unrelated `.agent/task`, `docs/*`, `tasks/tasks-*`, or hard-stale guide/reference rows.
- Future provider lanes cannot deterministically reuse the `docs:freshness:maintain` owner marker.

## Goals
- Re-home the docs freshness maintenance owner to live `CO-324`.
- Preserve machine-readable terminal-owner evidence for `CO-300`.
- Review and refresh the current-main stale docs surfaced by the CO-324 baseline.
- Register the CO-324 docs packet and task mirrors.

## Non-Goals
- No changes to docs freshness enforcement semantics.
- No rolling-window, cap, or eligible-class expansion.
- No CO-321 mutation.
- No broad historical archive or task-doc cleanup outside the surfaced baseline rows.

## Technical Considerations
- Update `docs/docs-catalog.json` owner metadata from terminal `CO-300` to live `CO-324`.
- Update `docs/guides/docs-freshness-cohorts.md` with the Apr 23 owner reset and reviewed disposition.
- Add `docs/findings/linear-3c52bf66-f805-4537-8671-ad1dec2f4623-docs-freshness-classification.md` as the review evidence for stale-row refresh.
- Update `docs/docs-freshness-registry.json`, `tasks/index.json`, and `docs/TASKS.md`.

## Validation
- `npm run docs:freshness`
- `npm run docs:freshness:maintain`
- `node scripts/spec-guard.mjs --dry-run`
- `npm run docs:check`
- `npm run repo:stewardship`
- `node scripts/diff-budget.mjs`
- standalone review plus explicit elegance review before review handoff if the final diff remains non-trivial.

## Open Questions
- None. The current-main baseline reproduced the issue-provided terminal-owner blocker and narrowed the remaining stale rows.

## Approvals
- Product: Codex provider worker, 2026-04-23
- Engineering: standalone review clean-success plus explicit elegance review, 2026-04-23
- Design: N/A
