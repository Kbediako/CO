---
id: 20260425-linear-5d015f15-6a72-4096-bf65-162b85a83080
title: CO-367 current-host-unhealthy matcher wording drift
relates_to: docs/PRD-linear-5d015f15-6a72-4096-bf65-162b85a83080.md
risk: low
owners:
  - Codex
last_review: 2026-04-25
---

# TECH_SPEC - CO-367 current-host-unhealthy matcher wording drift

## Canonical Reference
- Canonical task spec: `tasks/specs/linear-5d015f15-6a72-4096-bf65-162b85a83080.md`
- PRD: `docs/PRD-linear-5d015f15-6a72-4096-bf65-162b85a83080.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-5d015f15-6a72-4096-bf65-162b85a83080.md`
- Task checklist: `tasks/tasks-linear-5d015f15-6a72-4096-bf65-162b85a83080.md`
- Agent-task mirror: `.agent/task/linear-5d015f15-6a72-4096-bf65-162b85a83080.md`

## Summary
- Objective: harden `isCurrentHostUnhealthyErrorMessage` so current and legacy unhealthy-host wording remain classified as `current_host_unhealthy`.
- Scope: matcher constants/logic and focused `CoStatusCliShell` regressions.
- Constraints: no change to attach-shell producer wording, degraded-read payload shape, provider-intake overlay, or CO-363 active issue projection semantics.

## Issue-Shaping Contract
- User-request translation carried forward: this is a narrow PR #644 follow-up for wording drift in a fallback matcher, not a new status projection feature.
- Protected terms / exact artifact and surface names: `isCurrentHostUnhealthyErrorMessage`, `current-host-unhealthy`, `control_endpoint.json`, `co-status --format json`, `orchestrator/src/cli/coStatusCliShell.ts`, `orchestrator/src/cli/coStatusAttachCliShell.ts`, `orchestrator/tests/CoStatusCliShell.test.ts`, `CO-363`, `PR #644`, `CodeRabbit`.
- Nearby wrong interpretations to reject: dropping legacy coverage silently, changing producer wording unnecessarily, widening into endpoint rotation or active issue projection, or treating this as a CO-363 semantic change.
- Explicit non-goals carried forward: no dashboard redesign, no provider-intake changes, no Linear workflow changes, no endpoint retry policy changes.

## Parity / Alignment Matrix
- Current truth: producer messages include `current-host-unhealthy` and current stale-endpoint text, while the matcher fallback still carried old adjacent wording.
- Reference truth: canonical marker should match first; fallback fragments should be invariant current wording plus explicit legacy support.
- Target truth / intended delta: matcher recognizes marker, current stale-endpoint fallback, legacy stale-endpoint fallback, and rotated-endpoint fallback without changing degraded read behavior.
- Explicitly out-of-scope differences: producer message format, active issue projection, provider-intake overlays, and UI rendering.

## Readiness Gate
- Not done if:
  - current non-canonical attach-shell wording is not recognized
  - legacy wording loses coverage without an explicit removal decision
  - focused regressions do not cover current and legacy wording
  - CO-363 projection semantics change
- Pre-implementation issue-quality review evidence: 2026-04-25 parent review found the lane narrow and actionable after fetching current `origin/main`, where CO-363 introduced the matcher and current producer text.
- Safeguard ownership split: parent owns source matcher and docs; child lane owns focused tests only when usable.

## Technical Requirements
1. Define stable matcher fragments for the canonical marker, current stale-endpoint fallback, legacy stale-endpoint fallback, and rotated-endpoint fallback.
2. Keep `resolveLocalDegradedReadReason` returning `current_host_unhealthy` only for those unhealthy-host message families.
3. Exercise matcher behavior through public `runCoStatusCliShell` / degraded read flow or an equivalent focused test surface.
4. Preserve CO-363 active projection behavior and degraded-read payload semantics.

## Architecture & Data
- Architecture / design adjustments: keep matching local to `coStatusCliShell.ts`; do not add a new exported API unless tests require it.
- Data model changes / migrations: none.
- External dependencies / integrations: none.

## Validation Plan
- Focused test: `npx vitest run orchestrator/tests/CoStatusCliShell.test.ts`.
- Guardrails: `node scripts/spec-guard.mjs --dry-run`, `npm run build`, `npm run lint`, `npm run test`, `npm run docs:check`, `npm run docs:freshness`, `npm run repo:stewardship`, `node scripts/diff-budget.mjs`.
- Review: manifest-backed standalone review plus explicit elegance/minimality pass before handoff.

## Open Questions
- None known.

## Approvals
- Reviewer: parent provider worker.
- Date: 2026-04-25
