---
id: 20260513-linear-34ca427e-fb62-430e-9d8f-949a07690542
title: CO-489 external agent session import resume continuity
relates_to: docs/PRD-linear-34ca427e-fb62-430e-9d8f-949a07690542.md
related_action_plan: docs/ACTION_PLAN-linear-34ca427e-fb62-430e-9d8f-949a07690542.md
risk: medium
owners:
  - Codex
last_review: 2026-05-13
---

## Summary
- Objective: evaluate whether Codex external agent session import should become part of CO resume continuity, diagnostics, or migration workflows.
- Scope:
  - six CO-489 packet/mirror files
  - `tasks/index.json`
  - `docs/TASKS.md`
  - `docs/docs-freshness-registry.json`
  - future provider-worker canary using isolated config/session fixtures
- Constraints:
  - no persisted goal integration
  - no plugin hook/import governance
  - no broad historical session migration
  - no mutation of real user config or live provider-worker state

## Issue-Shaping Contract
- User-request translation:
  - Queue holds should be fixed at root cause, not patched over.
  - CO-489 needs packet and registry evidence before active work.
  - The implementation must validate current Codex behavior, not blindly rely on stale 0.128 evidence.
- Protected terms / exact artifact and surface names:
  - external agent session import
  - background imports
  - imported-session title handling
  - resume continuity
  - provider-worker manifests
  - hook continuation
  - Linear workpads
  - external_migration
  - `codex-cli-0128:external-session-import-continuity`
  - `codex-orchestrator:canonical-owner-key=codex-cli-0128:external-session-import-continuity`
- Nearby wrong interpretations to reject:
  - persisted goal workflow adoption
  - plugin hook/import governance
  - imported sessions replacing provider-worker manifests
  - broad historical migration before a canary proves value
- Explicit non-goals:
  - no Linear state authority changes
  - no real home config import
  - no live provider-worker mutation
  - no source/test implementation in this packet branch

## Parity / Alignment Matrix

| Surface | Current Truth | Reference Truth | Target Truth | Explicitly Out Of Scope |
| --- | --- | --- | --- | --- |
| Traceability | CO-489 has Linear description and workpad evidence, but no repo packet. | Backlog follow-ups need packet and registry mirrors. | Packet and mirrors exist before admission. | Transitioning CO-489 from the packet branch. |
| Current CLI evidence | Triage workpad captured 0.128 protocol evidence. | User expects latest CLI/model posture and no stale worker branch assumptions. | Worker records current installed Codex version, feature posture, and safe import behavior before deciding. | Treating 0.128 evidence as complete if current CLI differs. |
| CO continuity | Provider-worker manifests, hook continuation, and Linear workpads are current authority. | Imported sessions may help diagnostics only if provenance survives import. | Use-case matrix classifies adopt/defer/reject and preserves authority boundaries. | Replacing provider-intake or workpads. |
| Follow-up creation | Implementation follow-up is optional. | New work should exist only when evidence proves benefit. | Create follow-up only for canary-backed work with exact surfaces and non-goals. | Speculative migration machinery. |

## Technical Requirements
- Functional requirements:
  1. Preserve the six CO-489 packet/mirror files.
  2. Register the task in `tasks/index.json`.
  3. Update `docs/TASKS.md` and `docs/docs-freshness-registry.json`.
  4. Define a future canary using temp `CODEX_HOME`, fixture cwd, fixture external session/config data, and no live provider-worker state.
  5. Capture current CLI/app-server version, feature flag posture, import method names, item types, session fields, and imported-session title behavior.
  6. Classify CO use cases as adopt, defer, or reject.
  7. Preserve provider-worker manifests, hook continuation, Linear workpads, and provider-intake authority as canonical lifecycle surfaces.
- Non-functional requirements:
  - JSON remains parseable.
  - Packet stays docs/registry only.
  - Future validation must use `gpt-5.5` / `xhigh` review posture unless an explicit access-failure waiver is recorded.
  - The worker must not fall back to `gpt-5.4` for implementation/review by default.

## Fallback Expiry / Refactor Decision
- Applies to fallback, compatibility, legacy, stale, cached, break-glass, or minor-seam behavior? Yes.

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Resume authority | Imported session state could replace provider-worker manifests or Linear workpads. | remove fallback | CO-489 | Import evidence is considered for lifecycle or recovery authority. | 2026-05-03 | 2026-05-13 | N/A for authoritative use | Imported sessions stay advisory unless a follow-up governs authority boundaries. | Fixture canary plus model-backed review. |
| Stale import evidence | Historical 0.128 protocol findings could stand in for current CLI behavior. | expire fallback | CO-489 | Evaluation cites old release-intake notes without current probe or upstream evidence. | 2026-05-03 | 2026-05-13 | 2026-06-12 | Current CLI/app-server probe or explicit defer/reject decision replaces the stale evidence. | Versioned canary artifact or current source citation. |

- Large-refactor check: If adoption needs imported session state to participate in provider-intake, hook recovery, review handoff, or Linear closeout, choose a larger continuity design rather than adding a narrow import branch.
- Minor-seam decision: Packet work is a bounded traceability repair. Future implementation can remain bounded only while import evidence is advisory and isolated.

## Architecture & Data
- Architecture / design adjustments: External import evaluation reads CLI/app-server metadata and fixture import outputs as evidence. It does not alter provider-intake state, workpads, run manifests, or hook continuation contracts.
- Data model changes / migrations: None in packet setup. Future follow-up may add manifest evidence fields only if canary proves useful provenance.
- External dependencies / integrations: Codex CLI external_migration feature, app-server JSON schema, `codex resume`, `codex exec resume`, provider-worker manifests, Linear issue context/workpads.

## Validation Plan
- Tests / checks:
  - JSON parse for `tasks/index.json` and `docs/docs-freshness-registry.json`.
  - Protected-term scan for CO-489 and external import terms.
  - `git diff --check`.
  - `node scripts/spec-guard.mjs --dry-run`.
  - `npm run docs:check`.
  - `npm run docs:freshness`, with inherited baseline classification if the failure is repo-wide and `blocking_changed_paths=[]`.
  - `node scripts/diff-budget.mjs`.
- Future provider-worker canary:
  - Record `codex --version`, `codex features list`, and app-server schema/protocol evidence.
  - Use temp `CODEX_HOME` and fixture session/config inputs only.
  - Verify imported sessions preserve or fail to preserve cwd, title, path, timestamp/provenance, and diagnostic linkage.
  - Write canary artifacts under `out/linear-34ca427e-fb62-430e-9d8f-949a07690542/manual/`.
- Rollout verification: Open a packet PR, attach to CO-489, wait for checks/reviews, and keep the issue in Backlog until the traceability PR is merged or explicitly waived.
- Monitoring / alerts: If Codex auto-review does not add the expected eyes reaction after PR creation, manually trigger at most one `@codex` ping per head SHA.

## Open Questions
- Should issue creation helpers automatically scaffold packet files and labels for release-intake follow-ups?
- Should imported sessions be exposed only as diagnostics evidence even if adoption for continuity authority is rejected?

## Approvals
- Reviewer: CO parent orchestrator.
- Date: 2026-05-13.
