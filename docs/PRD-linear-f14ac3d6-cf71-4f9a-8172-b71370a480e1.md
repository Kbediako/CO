# PRD - CO: re-home March 28 docs freshness cohort owner

## Traceability
- Linear issue: `CO-420` / `f14ac3d6-cf71-4f9a-8172-b71370a480e1`
- Linear URL: https://linear.app/asabeko/issue/CO-420
- Task id: `linear-f14ac3d6-cf71-4f9a-8172-b71370a480e1`
- Canonical spec: `tasks/specs/linear-f14ac3d6-cf71-4f9a-8172-b71370a480e1.md`
- Canonical owner key: `docs:freshness:maintain`
- Canonical owner marker: `codex-orchestrator:canonical-owner-key=docs:freshness:maintain`
- Shared source anchor: `ctx:sha256:6d4d5e3d19965f60dc9e00809159fdd254719ca5621a2feb717e04808157390e#chunk:c000001`

## Summary
- Problem Statement: current docs freshness maintenance is blocked by stale March 28 task packet and mirror ownership evidence tied to terminal `CO-401`, causing `docs:freshness:maintain` to surface `block_unowned_repo_debt` with `configured_owner_terminal` rather than a live same-project owner path.
- Desired Outcome: make live `CO-420` the same-project docs maintenance owner for the March 28 Task Packet / Task Mirror cohort while preserving gate semantics, keeping stale rows machine-visible, and registering the CO-420 packet/mirrors.

## User Request Translation
- User intent: re-home the March 28 task packet/mirror cohort under a live owner without weakening `docs:freshness` or `docs:freshness:maintain`.
- Success criteria:
  - The CO-420 docs packet captures the exact owner-alignment problem, protected terms, non-goals, wrong interpretations to reject, `Not Done If`, acceptance criteria, validation plan, and parity matrix.
  - `CO-420` is established as the live same-project owner for the `docs:freshness:maintain canonical owner key` alignment work.
  - `docs/docs-catalog.json` declares the exact March 28 baseline cohort without weakening caps, classes, or windows.
  - `docs/docs-freshness-registry.json`, `tasks/index.json`, and `docs/TASKS.md` register the CO-420 packet and evidence.
- Constraints:
  - Do not modify `CO-415` timeout logic.
  - Do not weaken `docs:freshness`, `docs:freshness:maintain`, owner-terminal checks, registry checks, or stale cadence policy.
  - Do not edit `docs/docs-catalog.json`, `docs/docs-freshness-registry.json`, `tasks/index.json`, or `docs/TASKS.md` outside the scoped owner re-home and packet registration.

## Intent Checksum
- Protected terms / exact artifact and surface names:
  - `docs:freshness`
  - `docs:freshness:maintain`
  - `docs/docs-catalog.json`
  - `docs/docs-freshness-registry.json`
  - `CO-401`
  - `block_unowned_repo_debt`
  - `configured_owner_terminal`
  - `blocking_changed_paths=[]`
  - `March 28 task packet/mirror cohort`
  - `March 28 Task Packet / Task Mirror cohort`
  - `docs:freshness:maintain canonical owner key`
- Nearby wrong interpretations to reject:
  - Treating this as a `CO-415` timeout or core validation repair lane.
  - Weakening freshness gates so stale or terminal-owner evidence no longer fails closed.
  - Editing `docs/docs-catalog.json`, `docs/docs-freshness-registry.json`, `tasks/index.json`, or `docs/TASKS.md` beyond the scoped owner re-home and packet registration.
  - Reusing terminal `CO-401` as the live owner without re-home evidence.
  - Expanding into broad docs cleanup outside the March 28 task packet/mirror cohort.
  - Treating `blocking_changed_paths=[]` as permission to ignore owner alignment.

## Parity / Alignment Matrix

| Surface | Current Truth | Reference Truth | Target Truth | Explicitly Out Of Scope |
| --- | --- | --- | --- | --- |
| Maintenance owner | `docs:freshness:maintain` classifies terminal `CO-401` as `block_unowned_repo_debt` with `configured_owner_terminal` and `blocking_changed_paths=[]`. | A live same-project owner should carry `docs:freshness:maintain canonical owner key` work when the configured owner is terminal. | `CO-420` is the live same-project owner for re-homing the March 28 task packet/mirror cohort. | Reopening terminal owner issues or changing classifier semantics. |
| Catalog owner metadata | `docs/docs-catalog.json` points rolling owner metadata at terminal `CO-401` and lacks the March 28 baseline declaration. | `docs/docs-catalog.json` should point recurring freshness ownership at a usable owner issue and declare only intentional rolling cohorts. | Owner metadata points at `CO-420`; the exact March 28 Task Packet / Task Mirror cohort is declared under existing policy caps. | Changing freshness caps, window, eligible classes, or unrelated cohorts. |
| Freshness registry rows | March 28 rows stay machine-visible stale entries; CO-420 packet registry rows are absent. | Registry refresh must preserve stale cadence and owner-terminal checks unless rows are directly reviewed/refreshed. | March 28 rows remain visible as rolling debt; CO-420 packet rows are registered with current review evidence. | Hiding, deleting, or blindly date-bumping stale rows. |
| CO-415 boundary | CO-415 is separate timeout/core validation repair work. | Docs freshness owner re-home should not change timeout logic. | CO-420 remains docs-only owner alignment and cohort refresh/re-home work. | CO-415 runtime, timeout, or validation logic changes. |

## Not Done If
- The packet omits any required protected term or fails to describe the March 28 task packet/mirror cohort owner-alignment target.
- `docs:freshness:maintain` still reports the March 28 cohort as `block_unowned_repo_debt` or `configured_owner_terminal`.
- `docs:freshness` or `docs:freshness:maintain` would be weakened, bypassed, or reframed as optional.
- CO-420 is confused with `CO-415` timeout logic.
- Terminal `CO-401` or any other terminal owner remains the intended live owner without an explicit re-home.
- The fix hides stale rows instead of preserving rolling evidence or direct reviewed refresh evidence.

## Goals
- Create the CO-420 docs-first packet files.
- Preserve the issue-shaping contract around the March 28 task packet/mirror cohort.
- Re-home rolling owner metadata from terminal `CO-401` to live `CO-420`.
- Declare the exact March 28 Task Packet / Task Mirror cohort under the existing rolling freshness policy.
- Register CO-420 packet rows and task mirrors.

## Non-Goals
- No catalog/registry edits outside the scoped owner re-home and packet registration.
- No unrelated Linear issue mutation or PR lifecycle expansion.
- No `CO-415` timeout/core validation repair.
- No freshness gate weakening, cadence change, owner-terminal bypass, or broad stale-docs cleanup.

## Acceptance Criteria
- PRD, TECH_SPEC mirror, ACTION_PLAN, canonical spec, task checklist, and `.agent/task` mirror exist for `linear-f14ac3d6-cf71-4f9a-8172-b71370a480e1`.
- Each packet surface names `CO-420`, the `docs:freshness:maintain canonical owner key`, the March 28 task packet/mirror cohort, and the parent-owned catalog/registry validation surfaces.
- The packet includes protected terms, nearby wrong interpretations to reject, non-goals, `Not Done If`, validation plan, and a current/reference/target parity matrix.
- The packet states that parent owns owner re-home, registry validation, Linear state, and PR lifecycle.
- Baseline machine reports preserve `configured_owner_terminal`, terminal `CO-401`, `block_unowned_repo_debt`, and `blocking_changed_paths=[]` evidence.
- `docs:freshness:maintain` no longer reports `configured_owner_terminal` / `block_unowned_repo_debt` for the March 28 cohort after the re-home.
- `docs:freshness` and `docs:check` pass without weakening freshness policy.

## Technical Considerations
- Implementation updates `docs/docs-catalog.json` and `docs/docs-freshness-registry.json` only for the CO-420 owner re-home and packet registration.
- Parent validation should include machine-readable `docs:freshness:maintain` output proving the owner decision no longer blocks solely on terminal owner state.
- The accepted child lane only proved the six-file packet creation path; parent validation owns catalog, registry, task-list archive movement, Linear workpad, review, and PR handoff.

## Validation
- `git apply --check` plus accepted `docs-packet` child lane.
- Baseline `npm run docs:freshness`: `out/linear-f14ac3d6-cf71-4f9a-8172-b71370a480e1/before/docs-freshness.json`
- Baseline `docs:freshness:maintain -- --format json`: `out/linear-f14ac3d6-cf71-4f9a-8172-b71370a480e1/before/docs-freshness-maintenance.json`
- Post-fix `npm run docs:freshness`: `out/linear-f14ac3d6-cf71-4f9a-8172-b71370a480e1/after/docs-freshness.json`
- Post-fix `npm run docs:freshness:maintain -- --format json`: `out/linear-f14ac3d6-cf71-4f9a-8172-b71370a480e1/after/docs-freshness-maintenance.json`
- `node scripts/spec-guard.mjs --dry-run`: `out/linear-f14ac3d6-cf71-4f9a-8172-b71370a480e1/after/spec-guard-dry-run.log`
- `npm run docs:check`: `out/linear-f14ac3d6-cf71-4f9a-8172-b71370a480e1/after/docs-check.log`
- registry/catalog validation tied to `docs/docs-catalog.json` and `docs/docs-freshness-registry.json`

## Open Questions
- None.

## Approvals
- Product: parent CO-420 lane
- Engineering: parent CO-420 lane after applying docs packet and running owner/registry validation
- Design: N/A
