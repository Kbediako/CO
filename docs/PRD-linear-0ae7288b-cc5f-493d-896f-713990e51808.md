# PRD - CO: stabilize run-review fake Codex subprocess timeout flake

## Added by Bootstrap 2026-04-18

## Traceability
- Linear issue: `CO-251` / `0ae7288b-cc5f-493d-896f-713990e51808`
- Follow-up packet prefix: `linear-0ae7288b-cc5f-493d-896f-713990e51808`
- Canonical task id: `20260418-linear-0ae7288b-cc5f-493d-896f-713990e51808` (`tasks/index.json` lookup key)
- Canonical task spec: `tasks/specs/linear-0ae7288b-cc5f-493d-896f-713990e51808.md`
- TECH_SPEC mirror: `docs/TECH_SPEC-linear-0ae7288b-cc5f-493d-896f-713990e51808.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-0ae7288b-cc5f-493d-896f-713990e51808.md`
- Task checklist: `tasks/tasks-linear-0ae7288b-cc5f-493d-896f-713990e51808.md`
- `.agent` mirror: `.agent/task/linear-0ae7288b-cc5f-493d-896f-713990e51808.md`
- Shared source anchor: `ctx:sha256:7bc40b569a41bfbaead86699849abdfea03d40ad923c988770e2bfb98ef981bc#chunk:c000001`
- Source manifest: `.runs/linear-0ae7288b-cc5f-493d-896f-713990e51808-docs-packet/cli/2026-04-18T16-47-04-916Z-1d67c18a/manifest.json`
- Source payload note: this child checkout has no local `.runs/` directory, so the packet is anchored on the provided source pointer and protected lane wording.

## Summary
- Problem Statement: `tests/run-review.spec.ts` has a fake Codex subprocess timeout cleanup path that can flake under the full `npm run test` suite. The protected case creates a hanging fake Codex review process, writes `hang-started.txt`, kills the wrapper subprocess through a harness timeout, and verifies `findRunReviewMockPids(...)` returns to the pre-test baseline. The current issue is the full-suite reliability of that cleanup contract, not a request to remove or weaken the subprocess cleanup assertion.
- Desired Outcome: parent implementation stabilizes the full `npm run test` behavior for the `run-review` fake Codex subprocess timeout path while preserving the coverage that proves hanging fake review children are reaped and real review processes remain untouched.

## User Request Translation (Context Anchor)
- User intent / needs (in your own words): create the docs-first packet for `CO-251` only, preserving the exact `run-review` subprocess-timeout flake surface so the parent can reproduce, root-cause, fix, and validate without drifting into unrelated full-suite flakes or review-wrapper redesign.
- Success criteria / acceptance:
  - the packet names `tests/run-review.spec.ts`, `scripts/run-review.ts`, `fake Codex subprocess`, `hang-started.txt`, `findRunReviewMockPids`, and `full npm run test`
  - parent-owned work distinguishes isolated test behavior from full-suite interaction
  - any fix keeps subprocess cleanup coverage at least as strong as the current assertion
  - parent-owned validation includes focused `run-review` evidence and full-suite `npm run test` evidence before handoff
- Constraints / non-goals:
  - no implementation or test edits in this child lane
  - no `CO-234` `LockFile` work in this lane
  - no generic review-wrapper redesign unless parent evidence proves it is required
  - no skipping, weakening, or deleting fake Codex subprocess cleanup coverage

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `stabilize tests/run-review.spec.ts fake Codex subprocess timeout flake under full npm run test`
  - `tests/run-review.spec.ts`
  - `scripts/run-review.ts`
  - `fake Codex subprocess`
  - `hang-started.txt`
  - `findRunReviewMockPids`
  - `full npm run test`
  - `no CO-234 LockFile work`
  - `no generic review-wrapper redesign`
  - `no skipping/weakening subprocess cleanup coverage`
- Protected terms / exact artifact and surface names:
  - `tests/run-review.spec.ts`
  - `scripts/run-review.ts`
  - `runReviewCommandSubprocess`
  - `RUN_REVIEW_MODE=hang`
  - `RUN_REVIEW_HANG_MARKER`
  - `CODEX_REVIEW_TIMEOUT_SECONDS=0`
  - `CODEX_REVIEW_STALL_TIMEOUT_SECONDS=0`
  - `hang-started.txt`
  - `findRunReviewMockPids`
  - `fake Codex subprocess`
  - `npm run test`
  - `CO-234`
  - `LockFile`
- Nearby wrong interpretations to reject:
  - treating this as `CO-234` `LockFile` keepalive timing work
  - deleting or skipping the hanging fake Codex subprocess regression to make the suite green
  - weakening `findRunReviewMockPids(...)` so leaked processes are hidden instead of reaped
  - broadening into a review-wrapper redesign before parent evidence proves the current cleanup contract cannot be stabilized locally
  - assuming focused pass/fail behavior explains the full-suite flake without a full `npm run test` reproduction or equivalent artifact

## Parity / Alignment Matrix

| Surface | Current / Reference Truth | Target Truth |
| --- | --- | --- |
| Focused fake Codex timeout cleanup | `tests/run-review.spec.ts` owns a regression that runs the fake Codex in `RUN_REVIEW_MODE=hang`, waits for `hang-started.txt`, kills the wrapper subprocess, and checks `findRunReviewMockPids(...)` against the pre-test baseline. | The focused regression remains present and still proves no new fake Codex subprocess leaks from the test harness. |
| Full-suite behavior | The issue reports a flake under full `npm run test`, where isolated `run-review` behavior may not be enough to explain suite-level timing, cleanup, or process ownership interactions. | Parent captures full-suite evidence, fixes the smallest proven ownership/timing gap, and reruns `npm run test` successfully before handoff. |
| Adjacent flakes | `CO-234` owns `LockFile` timing work and is not this lane. | Parent keeps any `LockFile` or unrelated ambient suite flakes out of `CO-251` unless it stops and widens scope explicitly. |
| Review-wrapper design | `scripts/run-review.ts` and nearby test helpers already carry timeout, stall, telemetry, and subprocess cleanup contracts. | Parent changes only the specific cleanup/harness/runtime contract required by evidence, with no generic redesign as a substitute for root cause. |

## Acceptance Criteria
1. The docs packet and registry mirrors exist for `CO-251` with exact protected terms carried forward.
2. Parent captures whether the named `run-review` fake Codex subprocess cleanup case passes in isolation.
3. Parent captures or otherwise explains the full `npm run test` flake with machine-checkable evidence.
4. Parent-owned implementation preserves the cleanup guarantee: newly-created fake Codex subprocesses are terminated or reaped after harness timeout.
5. Parent-owned implementation does not kill real review processes, hide leaked fake processes, or weaken `findRunReviewMockPids(...)` into a false-green check.
6. Parent-owned validation includes a focused `tests/run-review.spec.ts` rerun and full `npm run test` before claiming completion.
7. `CO-234` `LockFile` work and unrelated full-suite flakes stay out of scope.

## Goals
- Stabilize the full-suite `run-review` fake Codex subprocess timeout path.
- Preserve the existing subprocess cleanup coverage and process-ownership safety.
- Give the parent a narrow reproduction and validation contract before implementation.
- Keep adjacent full-suite flakes and broad wrapper redesign out of this issue.

## Non-Goals
- No docs-only reinterpretation of the flake as solved.
- No `CO-234` `LockFile` changes.
- No removal, skip, blanket timeout inflation, or weakening of the fake Codex subprocess cleanup assertion. A narrow harness timeout adjustment is acceptable only when paired with root-cause classification and full-suite evidence.
- No broad review-wrapper redesign unless parent evidence proves the local cleanup surface cannot be stabilized.
- No Linear, workpad, PR, code, or test mutation by this child lane.

## Not Done If
- `tests/run-review.spec.ts` still flakes under full `npm run test`.
- The fix removes or skips the fake Codex subprocess timeout coverage.
- `findRunReviewMockPids(...)` is weakened so leaked fake subprocesses are no longer observed.
- Real review processes can be terminated by the cleanup path.
- Evidence only covers the focused test while the issue was specifically about full `npm run test`.
- The lane drifts into `CO-234` `LockFile` or unrelated suite failures without an explicit parent relaunch.

## Stakeholders
- Product: CO maintainers who need a stable review-wrapper validation floor.
- Engineering: parent implementer and reviewers responsible for root cause, code/test changes, and validation.
- Operations / Review: agents relying on `npm run test` and `run-review` subprocess cleanup as a trustworthy gate.

## Metrics & Guardrails
- Primary Success Metrics:
  - focused `run-review` subprocess-timeout coverage remains green
  - full `npm run test` completes without the reported flake
  - post-test process checks, when used, show no new fake Codex subprocess leak from the lane
- Guardrails / Error Budgets:
  - fail closed on process leaks
  - preserve exact protected surfaces
  - keep unrelated full-suite flakes classified separately

## User Experience
- Personas:
  - parent worker reproducing and fixing the flake
  - reviewer checking that cleanup coverage was preserved
  - maintainer auditing why full-suite stability improved without weakening tests
- User Journeys:
  - parent starts from this packet, runs focused and full-suite evidence, and edits only the proven surface
  - reviewer sees the fake subprocess cleanup contract still enforced
  - maintainer can distinguish `CO-251` from `CO-234` and earlier `run-review` cleanup work

## Technical Considerations
- Architectural Notes:
  - the protected regression uses a fake Codex binary plus environment-controlled hang behavior, not a real Codex review process
  - process matching must stay scoped to the test sandbox / fake Codex command path so real review processes are not touched
  - full-suite behavior may expose ordering, cleanup, timeout, or ambient-process interactions that isolated reruns do not show
- Dependencies / Integrations:
  - `tests/run-review.spec.ts`
  - `scripts/run-review.ts`
  - `npm run test`
  - fake Codex subprocess harness helpers
  - `hang-started.txt`
  - `findRunReviewMockPids`

## Open Questions
- Does the fake Codex timeout case fail in isolation on the current head, or only under full `npm run test` ordering?
- Is the flake caused by process cleanup timing, process-group handling, test sandbox matching, ambient fake-Codex processes, or another full-suite interaction?
- Can the parent fix stay test-harness-local, or does evidence require a narrow `scripts/run-review.ts` runtime cleanup adjustment?

## Approvals
- Product: pending parent reproduction and implementation decision
- Engineering: pending parent root cause and validation
- Operations / Review: pending full-suite proof
