# PRD - CO: Fix non-interactive npm run test quiet-tail / MessagePort hang

## Added by Bootstrap 2026-04-05

## Summary
- Problem Statement: the current provider-worker non-interactive `npm run test` path does reach a truthful terminal exit on this head, but it goes quiet for several minutes while `tests/run-review.spec.ts` and `tests/cli-command-surface.spec.ts` continue running. Mid-run `MessagePort` samples from that quiet tail can look like a hang even though the suite is still live.
- Desired Outcome: prove the current non-interactive behavior deterministically, reclassify the quiet tail truthfully, and make long-running final specs observable in worker/non-interactive lanes without reducing coverage or masking failures.

## User Request Translation (Context Anchor)
- User intent / needs (in your own words): treat the fresh CO-80 follow-up evidence as a current-tree blocker, validate whether the worker lane is truly hung or just silent, classify the `MessagePort` samples truthfully, and land the smallest fix that keeps non-interactive `npm run test` observable and review-safe.
- Success criteria / acceptance:
  - deterministic artifact-backed evidence exists for the current non-interactive quiet tail
  - the sampled `MessagePort` handles are classified truthfully as in-flight or leaked
  - `npm run test` exits terminally in the affected worker lane on the current head
  - long-running quiet-tail specs emit bounded progress in worker/non-interactive runs
  - focused regression proof or equivalent evidence prevents silent recurrence
- Constraints / non-goals:
  - do not reduce validation coverage
  - do not close the issue by declaring visible late-suite output "good enough"
  - do not hide the symptom with blanket force-exit behavior unless evidence proves that is the only truthful fix

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `non-interactive npm run test`
  - `quiet-tail`
  - `MessagePort`
  - `truthful terminal exit`
  - `worker/non-interactive environments`
- Protected terms / exact artifact and surface names:
  - `npm run test`
  - `tests/cli-frontend-test.spec.ts`
  - `orchestrator/tests/CodexOrchestratorCli.test.ts`
  - `provider-worker`
  - `In Review`
- Nearby wrong interpretations to reject:
  - late visible green output means the validation is effectively done
  - the earlier patience-first CO-69 classification is sufficient proof that this issue is already solved
  - a manual interruption or force-exit timer is acceptable normal behavior for worker validation

## Parity / Alignment Matrix
- Not applicable.
- Current truth:
  - earlier CO lanes proved a long quiet tail exists in the suite
  - fresh current-head reproduction shows `npm run test` exits `0` in the provider-worker env after `tests/run-review.spec.ts` and `tests/cli-command-surface.spec.ts` finish
  - the sampled ref'd `MessagePort`s belong to the active Vitest worker pool during that quiet tail, not to a reproduced post-suite leak on this head
- Reference truth:
  - provider-worker review handoff requires a machine-checkable full-suite terminal result
- Target truth / intended delta:
  - the current non-interactive worker lane finishes with a truthful terminal success or failure
  - the long quiet tail is observable via bounded progress output in worker lanes
  - any sampled `MessagePort` owner is fixed or explicitly classified with durable proof
  - the issue packet and workpad record the exact reproduction and validation path for future lanes
- Explicitly out-of-scope differences:
  - unrelated control-host or merge-closeout behavior
  - broad Vitest or CLI refactors not required by the leaking owner
  - coverage reductions that only make the suite terminate faster

## Not Done If
- `npm run test` still reaches visible late-suite success and remains non-terminal in non-interactive worker runs.
- The worker lane can still go silent long enough to look hung without emitting bounded progress for the long final specs.
- The result depends on manual interruption, a shell-specific workaround, or an unverifiable timeout.
- The root cause classification and fix are still only narrative and not backed by reproducible evidence.

## Goals
- Reproduce the current non-interactive quiet tail with machine-checkable artifacts.
- Classify the sampled `MessagePort` handles against the current head's real terminal behavior.
- Land the minimum fix that preserves full-suite coverage and truthful failure semantics while making the quiet tail observable.
- Keep the Linear workpad, docs packet, and validation evidence synchronized through handoff.

## Non-Goals
- Reopening unrelated CO-80 implementation scope.
- Broad test-suite cleanup outside the leaking owner seam.
- Switching provider-worker validation away from the full suite.

## Stakeholders
- Product: CO issue owners who need truthful worker review handoff.
- Engineering: CO CLI, test harness, and provider-worker maintainers.
- Design: not applicable.

## Metrics & Guardrails
- Primary Success Metrics:
  - non-interactive `npm run test` exits terminally on green runs in this workspace
  - failure cases still exit non-zero without being reclassified
  - the final packet contains reproducible commands, artifacts, and validation evidence
- Guardrails / Error Budgets:
  - no dropped suite coverage
  - no blanket cleanup that can hide real failures
  - no speculative refactors unless the narrow owner proves insufficient

## User Experience
- Personas:
  - provider-worker agents running review-gated validation
  - reviewers reading manifests and workpad evidence
- User Journeys:
  - worker runs the full suite non-interactively
  - suite either fails truthfully or exits terminally on green
  - review handoff proceeds without a stuck post-pass tail

## Technical Considerations
- Architectural Notes:
  - start from the non-interactive worker command surface and current-tree evidence rather than assuming older late-tail conclusions still apply
  - direct current-head reproduction shows the suite remains alive inside `tests/run-review.spec.ts` and `tests/cli-command-surface.spec.ts` after the late visible passes
  - prioritize a bounded observability fix on the existing Vitest progress reporter over broader exit-policy changes because the current head already exits cleanly
- Dependencies / Integrations:
  - `package.json` test scripts
  - `vitest.config.core.ts`
  - the implicated tests/helpers keeping worker-thread or channel resources alive
  - provider-worker validation and review wrappers

## Open Questions
- Should non-interactive progress reporting remain scoped to worker-style env flags only, or expand to other quiet non-TTY lanes in follow-up work?

## Approvals
- Product: pending
- Engineering: pending
- Design: not applicable
