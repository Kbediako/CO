# ACTION_PLAN - Coordinator Symphony-Aligned Control Server Request Body Helper Extraction

1. Register `1089` docs-first from the `1088` next-slice note plus bounded `controlServer.ts` inspection.
2. Add one bounded helper module for raw-body / JSON-body request parsing.
3. Repoint `controlServer.ts` to use that helper for Linear webhook ingress and authenticated-route request parsing.
4. Refresh focused request-body regressions.
5. Run the full validation + closeout bundle, capture the elegance review, and sync the task mirrors.
