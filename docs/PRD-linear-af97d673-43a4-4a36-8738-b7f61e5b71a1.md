# PRD - CO-41 post-PR #547 provider refresh stall recurrence

## Added by Bootstrap 2026-03-30

## Refreshed 2026-04-18

## Refreshed 2026-04-19

## Traceability
- Linear issue: `CO-41` / `af97d673-43a4-4a36-8738-b7f61e5b71a1`
- Linear URL: https://linear.app/asabeko/issue/CO-41/co-investigate-control-host-provider-refresh-stall-that-stops-new
- March reference baseline: `PR #324` / commit `330581458` (`CO-41: Fix stuck control-host refresh lifecycle reporting`)
- Apr 18 repair baseline: `PR #547` / commit `0484cd803` (repeated active-worker restart quarantine repair and `counts.max_allowed` supervision diagnostic preservation)
- Apr 18 source anchor: `ctx:sha256:32d01f29a223917c3e47b5f30f7753d44d23e7cfafd3dc6fb8977eb8a2d0c633#chunk:c000001`
- Apr 18 origin manifest: `.runs/linear-af97d673-43a4-4a36-8738-b7f61e5b71a1-docs-refresh/cli/2026-04-18T21-09-55-260Z-b69a5e6c/manifest.json`
- Apr 19 reopened source anchor: `ctx:sha256:1bc7b3ac282ad1ebac36e250c2a063346e8497500fbf4b407ca3ea84ae327f35#chunk:c000001`
- Apr 19 origin manifest: `.runs/linear-af97d673-43a4-4a36-8738-b7f61e5b71a1-docs-apr19-refresh/cli/2026-04-19T02-56-26-118Z-98ad9a6f/manifest.json`
- Related issue boundaries / evidence terms to preserve: `CO-252`, `CO-217`, `CO-211`, `CO-214`, `CO-248`

## Summary
- Problem Statement: `CO-41` has now recurred after two baselines. March `PR #324` fixed the original stuck polling / refresh watchdog class, and Apr 18 `PR #547` fixed the free-capacity repeated active-worker restart quarantine seam. Linear comments on Apr 19 reopened the issue again after `PR #547` merged because operators still saw post-#547 `restart_required` recurrences, stale `released:not_active` / `provider_issue_released:not_active` rows, `active_worker_proof_missing`, a freshness-gauge stale verdict, only partial restart recovery, and spare-capacity no-admission.
- Desired Outcome: Treat Apr 19 as the current unresolved `CO-41` attempt. Preserve March `PR #324` and Apr 18 `PR #547` as baselines, add the new recurrence evidence, and require the parent lane to identify and validate the post-#547 blocker without broadening into unrelated issue families.

## User Request Translation (Context Anchor)
- User intent / needs (in your own words): Refresh the `CO-41` docs/task packet for the Apr 19 reopened recurrence after `PR #547` merged. The packet must make clear that `PR #324` and `PR #547` are historical baselines, not current completion proof, and must preserve the Linear-comment evidence terms for the new recurrence.
- Success criteria / acceptance:
  - `CO-41` docs clearly separate the March `PR #324` baseline, the Apr 18 `PR #547` baseline, and the Apr 19 unresolved recurrence.
  - The packet preserves exact artifact and state names: `provider-intake-state.json`, `provider_refresh_lifecycle_stuck`, `refresh:claim_reconcile`, `Ready`, `In Progress`, `restart_required`, `stuck`, `released:not_active`, `provider_issue_released:not_active`, `active_worker_proof_missing`, `freshness-gauge stale`, and `spare-capacity no-admission`.
  - The packet preserves adjacent issue IDs `CO-252`, `CO-217`, `CO-211`, `CO-214`, and `CO-248` without widening this lane into those issue scopes.
  - Acceptance criteria stay aligned to the Linear issue: a wedged provider refresh lifecycle must not silently block new `Ready` issue pickup, and recovery or restart-required behavior must be explicit and machine-checkable.
  - Apr 19 implementation, tests, review, Linear state, workpad, and PR lifecycle remain parent-owned and are not marked done by this docs child lane.
- Constraints / non-goals:
  - This child lane is docs-only and must not edit source or tests.
  - Implementation stays limited to the post-#547 control-host/provider refresh recurrence once the parent identifies the current seam.
  - Do not relabel the recurrence as capacity exhaustion: the new evidence includes spare-capacity no-admission.
  - Do not collapse this issue into `CO-39`, `CO-40`, `CO-33`, `CO-211`, `CO-214`, `CO-248`, `CO-217`, or `CO-252`; preserve them as boundaries and evidence handles.

## Protected Terms
- `provider-intake-state.json`
- `provider_refresh_lifecycle_stuck`
- `refresh:claim_reconcile`
- `Ready`
- `In Progress`
- `restart_required`
- `stuck`
- `released:not_active`
- `provider_issue_released:not_active`
- `active_worker_proof_missing`
- `freshness-gauge stale`
- `partial restart recovery`
- `spare-capacity no-admission`
- `CO-41`
- `CO-252`
- `CO-217`
- `CO-211`
- `CO-214`
- `CO-248`

## Nearby Wrong Interpretations To Reject
- `PR #324` remains the March reference fix, not proof that the Apr 19 post-#547 recurrence is irrelevant.
- `PR #547` remains the Apr 18 baseline for the repeated active-worker restart quarantine seam, but the Apr 19 Linear comments show a fresh unresolved recurrence after that merge.
- Spare-capacity no-admission is part of the Apr 19 evidence, so this is not a simple capacity-full condition.
- Local control-host provider refresh/admission truth remains central; this is not only Linear workflow-state drift.
- `CO-41` still needs parent source investigation, implementation, focused tests, review, and PR lifecycle; docs refresh alone is not closeout.
- This is not an invitation to widen into `CO-39`, `CO-40`, `CO-33`, `CO-211`, `CO-214`, `CO-248`, `CO-217`, or `CO-252`.

## Current / Reference / Target Parity Matrix

| Surface | Reference: March `PR #324` | Reference: Apr 18 `PR #547` | Current: Apr 19 recurrence | Target |
| --- | --- | --- | --- | --- |
| Refresh-health classification | Original stuck polling / refresh watchdog fix landed for `CO-41`. | Repeated active-worker restart quarantine was narrowed using `counts.max_allowed`. | Post-#547 `restart_required` recurrences still appear. | The parent identifies the new seam and preserves truthful unhealthy/restart-required evidence. |
| Refresh/admission evidence | March incident centered on stale live instance and stuck refresh reporting. | Apr 18 recurrence centered on `refresh:claim_reconcile` with free capacity. | Linear comments add stale `released:not_active` rows, `provider_issue_released:not_active`, and `active_worker_proof_missing`. | Current evidence distinguishes stale release rows, missing active-worker proof, and genuine refresh-stuck truth. |
| Freshness / recovery | Restart released stale truth and let `Ready` issues move to `In Progress`. | Apr 18 repair allowed supervision to avoid quarantining free-capacity stuck series. | Freshness-gauge stale verdict and partial restart recovery mean restart alone is not completion proof. | Freshness and recovery state are machine-checkable before any closeout. |
| Capacity and admission | Restart historically allowed stuck `Ready` issues to advance. | `running=2`, `max_allowed=3` was the free-capacity no-admission baseline. | Apr 19 preserves spare-capacity no-admission after `PR #547`. | `Ready` admission proceeds when spare capacity exists, or the host emits explicit blocking evidence. |
| Adjacent issue boundaries | March `CO-41` is the lifecycle-stall lineage anchor. | `CO-217`, `CO-211`, `CO-214`, `CO-248`, and `CO-252` were preserved as boundaries/evidence handles. | Do not broaden into `CO-39`, `CO-40`, `CO-33`, or unrelated issue families. | Implementation preserves neighboring contracts unless parent evidence proves a coordinated change is required. |

## Goals
- Refresh the `CO-41` packet around the Apr 19 reopened recurrence after `PR #547`.
- Preserve March `PR #324` and Apr 18 `PR #547` as baselines only.
- Preserve the exact Apr 19 evidence terms from Linear comments: post-#547 `restart_required` recurrence, stale `released:not_active` rows, `active_worker_proof_missing`, freshness-gauge stale verdict, partial restart recovery, and spare-capacity no-admission.
- Keep the implementation scope focused on the current post-#547 blocking seam once the parent identifies it.
- Require machine-checkable recovery or restart-required evidence for the current recurrence.

## Non-Goals
- No source, test, Linear, workpad, or PR lifecycle changes in this child lane.
- No package, provider issue handoff, merge closeout, Linear workflow, or provider-worker admission redesign without parent-owned source evidence.
- No broad control-plane rewrite or scheduling redesign.
- No replacement of adjacent lanes `CO-39`, `CO-40`, `CO-33`, `CO-211`, `CO-214`, `CO-248`, `CO-217`, or `CO-252`.

## Not Done If
- The packet still reads as if March `PR #324` or Apr 18 `PR #547` is the final answer for the Apr 19 recurrence.
- The packet omits post-#547 `restart_required` recurrence, stale `released:not_active` rows, `active_worker_proof_missing`, freshness-gauge stale verdict, partial restart recovery, or spare-capacity no-admission.
- The packet implies spare-capacity no-admission was caused by no provider capacity.
- Apr 19 implementation, tests, review, or PR lifecycle are marked complete by this docs-only lane.
- Adjacent issue IDs are used as a substitute for current `CO-41` root-cause analysis.

## Stakeholders
- Product: CO operators depending on local control-host intake for issue pickup
- Engineering: CO maintainers responsible for provider refresh lifecycle correctness and diagnosability
- Design: N/A

## Metrics & Guardrails
- Primary Success Metrics:
  - a `Ready` issue is not silently starved when provider capacity is available
  - `provider-intake-state.json` preserves actionable `stuck`, `restart_required`, `provider_refresh_lifecycle_stuck`, phase, stale-release, and freshness evidence
  - closeout identifies the Apr 19 post-#547 blocking seam rather than only citing restart as the workaround
- Guardrails / Error Budgets:
  - preserve March `PR #324` and Apr 18 `PR #547` behavior as reference baselines
  - fail closed into explicit restart-required evidence if safe recovery is not proven
  - keep adjacent issue boundaries intact unless implementation evidence requires updating them

## User Experience
- Personas: local control-host operator, provider-worker issue owner, reviewer diagnosing intake stalls
- User Journeys:
  - an operator inspecting `provider-intake-state.json` can tell whether the host is genuinely stuck, stale due to release rows, missing active-worker proof, or partially recovered after restart
  - the focused regression simulates the Apr 19 post-#547 shape before closeout
  - a reviewer can see why March `PR #324` and Apr 18 `PR #547` are historical references, not claims that the Apr 19 recurrence is fixed

## Technical Considerations
- Architectural Notes:
  - March evidence and memory identify the provider refresh lifecycle as the first hypothesis for Ready-but-unclaimed stalls.
  - Apr 18 evidence narrowed one recurrence to `refresh:claim_reconcile` with `stuck=true`, `restart_required=true`, and free capacity, then `PR #547` repaired the repeated active-worker restart quarantine seam.
  - Apr 19 evidence reopens the issue after `PR #547` with stale release rows, missing active-worker proof, stale freshness, partial restart recovery, and spare-capacity no-admission.
  - The current source seam is not proven by this docs child lane; the parent lane owns source investigation and implementation.
- Dependencies / Integrations:
  - Apr 19 Linear-comment recurrence evidence
  - `PR #324` / commit `330581458` as the historical `CO-41` March baseline
  - `PR #547` / commit `0484cd803` as the Apr 18 repair baseline
  - adjacent issue boundaries: `CO-252`, `CO-217`, `CO-211`, `CO-214`, `CO-248`

## Open Questions
- Which post-#547 source seam explains stale `released:not_active` rows plus `active_worker_proof_missing`?
- Why did restart only partially recover the recurrence?
- Which focused regression should become the parent implementation gate for spare-capacity no-admission after `PR #547`?

## Approvals
- Product: Self-approved from reopened Linear issue scope
- Engineering: Pending Apr 19 parent implementation, validation, review, and PR handoff
- Design: N/A
