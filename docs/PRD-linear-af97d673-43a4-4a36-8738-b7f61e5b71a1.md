# PRD - CO-41 reopened provider refresh stall recurrence

## Added by Bootstrap 2026-03-30

## Refreshed 2026-04-18

## Traceability
- Linear issue: `CO-41` / `af97d673-43a4-4a36-8738-b7f61e5b71a1`
- Linear URL: https://linear.app/asabeko/issue/CO-41/co-investigate-control-host-provider-refresh-stall-that-stops-new
- March reference fix: `PR #324` / commit `330581458` (`CO-41: Fix stuck control-host refresh lifecycle reporting`)
- Apr 18 recurrence source anchor: `ctx:sha256:32d01f29a223917c3e47b5f30f7753d44d23e7cfafd3dc6fb8977eb8a2d0c633#chunk:c000001`
- Apr 18 origin manifest: `.runs/linear-af97d673-43a4-4a36-8738-b7f61e5b71a1-docs-refresh/cli/2026-04-18T21-09-55-260Z-b69a5e6c/manifest.json`
- Related issue boundaries / evidence terms to preserve: `CO-252`, `CO-217`, `CO-211`, `CO-214`, `CO-248`

## Summary
- Problem Statement: The March `CO-41` fix in `PR #324` landed the original stuck polling / refresh watchdog repair, but `CO-41` was reopened on 2026-04-18 because the same failure family recurred. The Apr 18 `provider-intake-state.json` evidence showed `stuck=true`, `restart_required=true`, `last_error=provider_refresh_lifecycle_stuck`, `refresh_phase=refresh:claim_reconcile`, and free capacity (`running=2`, `max_allowed=3`) while a `Ready` issue, preserved here as `CO-252`, was not admitted until restart.
- Desired Outcome: Treat the Apr 18 recurrence as the current `CO-41` scope, identify the current blocking seam, preserve the March reference behavior, and land the smallest repair that keeps `provider_refresh_lifecycle_stuck`, `restart_required`, `stuck`, and `refresh:claim_reconcile` truth machine-checkable.

## User Request Translation (Context Anchor)
- User intent / needs (in your own words): Complete `CO-41` for the Apr 18 reopened recurrence. The lane must acknowledge that `PR #324` already fixed the original March stuck refresh watchdog class, preserve the Apr 18 `refresh:claim_reconcile` evidence with free capacity, and repair the current blocker without broadening into neighboring issues.
- Success criteria / acceptance:
  - `CO-41` docs clearly separate the March reference fix from the Apr 18 reopened recurrence.
  - The packet preserves exact artifact and state names: `provider-intake-state.json`, `provider_refresh_lifecycle_stuck`, `refresh:claim_reconcile`, `Ready`, `In Progress`, `restart_required`, and `stuck`.
  - The packet preserves adjacent issue IDs `CO-252`, `CO-217`, `CO-211`, `CO-214`, and `CO-248` without widening this lane into those issue scopes.
  - Acceptance criteria stay aligned to the Linear issue: a wedged provider refresh lifecycle must not silently block new `Ready` issue pickup, and recovery or restart-required behavior must be explicit and machine-checkable.
- The repair identifies the current source seam: repeated active-worker restart quarantine must not suppress recovery when `running + retrying < max_allowed`, and the supervision diagnostic must preserve `counts.max_allowed` from the existing `co-status --format json` operator dataset.
- Constraints / non-goals:
  - Implementation stays limited to control-host supervision health evaluation and focused regression coverage.
  - Do not relabel the recurrence as capacity exhaustion: the Apr 18 evidence has free capacity (`running=2`, `max_allowed=3`).
  - Do not collapse this issue into `CO-211`, `CO-214`, `CO-248`, `CO-217`, or `CO-252`; preserve them as boundaries and evidence handles.

## Protected Terms
- `provider-intake-state.json`
- `provider_refresh_lifecycle_stuck`
- `refresh:claim_reconcile`
- `Ready`
- `In Progress`
- `restart_required`
- `stuck`
- `CO-41`
- `CO-252`
- `CO-217`
- `CO-211`
- `CO-214`
- `CO-248`

## Nearby Wrong Interpretations To Reject
- This is not proof that `PR #324` was irrelevant; `PR #324` is the March reference fix, while Apr 18 is a reopened recurrence requiring fresh source investigation.
- This is not a simple capacity-full condition because the Apr 18 snapshot preserved free capacity: `running=2`, `max_allowed=3`.
- This is not only a Linear workflow-state problem; a `Ready` issue was not admitted until restart while the local provider refresh state reported `provider_refresh_lifecycle_stuck`.
- This is not the `CO-211` full-capacity active-worker churn case; `CO-41` adds the free-capacity admission blocker where `running + retrying < max_allowed`.
- This is not a docs-only closeout for `CO-41`; the reopened issue needs source repair and validation.

## Current / Reference / Target Parity Matrix
| Surface | Reference: March `PR #324` | Current: Apr 18 recurrence | Target |
| --- | --- | --- | --- |
| Refresh-health classification | Original stuck polling / refresh watchdog fix landed for `CO-41`. | `provider-intake-state.json` again reports `stuck=true`, `restart_required=true`, and `last_error=provider_refresh_lifecycle_stuck`. | The current seam is identified and either recovers safely or emits truthful restart-required evidence without silently freezing intake. |
| Refresh phase | March incident centered on stale live instance and stuck refresh reporting. | Apr 18 recurrence is at `refresh_phase=refresh:claim_reconcile`. | Phase-specific evidence remains preserved so `refresh:claim_reconcile` does not collapse into generic timeout wording. |
| Capacity and admission | Restart released stale truth and let `Ready` issues move to `In Progress`. | Free capacity exists (`running=2`, `max_allowed=3`) while a `Ready` issue (`CO-252`) is not admitted until restart. | `Ready` admission proceeds when capacity is available, or the host emits an explicit `restart_required` / `stuck` condition explaining why it cannot. |
| Adjacent issue boundaries | March `CO-41` is the lifecycle-stall lineage anchor. | `CO-217`, `CO-211`, `CO-214`, and `CO-248` are nearby but not automatically this issue's fix. | Implementation preserves neighboring contracts unless source evidence requires a coordinated change. |

## Goals
- Refresh the `CO-41` packet around the Apr 18 reopened recurrence, not the already-landed March fix alone.
- Preserve the exact `provider-intake-state.json` evidence fields and the `refresh:claim_reconcile` phase.
- Keep the implementation focused on the current blocking seam that prevents a `Ready` issue from moving to `In Progress` despite free capacity.
- Carry `max_allowed` into supervision diagnostics, then keep repeated refresh-stuck states unhealthy when capacity remains open.
- Require machine-checkable recovery or restart-required evidence for the current recurrence.

## Non-Goals
- No package, provider issue handoff, merge closeout, Linear workflow, or provider-worker admission redesign.
- No broad control-plane rewrite or scheduling redesign.
- No replacement of adjacent lanes `CO-211`, `CO-214`, `CO-248`, `CO-217`, or `CO-252`.

## Not Done If
- The packet still reads as if the March `PR #324` implementation is the final answer for the Apr 18 recurrence.
- The packet omits `provider-intake-state.json`, `provider_refresh_lifecycle_stuck`, `refresh:claim_reconcile`, `restart_required`, `stuck`, or the free-capacity evidence.
- The packet implies `CO-252` was blocked because no provider capacity was available.
- The repair leaves repeated refresh-stuck quarantine healthy even when `running + retrying < max_allowed`.
- Adjacent issue IDs `CO-217`, `CO-211`, `CO-214`, or `CO-248` are used as a substitute for current `CO-41` root-cause analysis.

## Stakeholders
- Product: CO operators depending on local control-host intake for issue pickup
- Engineering: CO maintainers responsible for provider refresh lifecycle correctness and diagnosability
- Design: N/A

## Metrics & Guardrails
- Primary Success Metrics:
  - a `Ready` issue is not silently starved when provider capacity is available
  - `provider-intake-state.json` preserves actionable `stuck`, `restart_required`, `provider_refresh_lifecycle_stuck`, and phase evidence
  - closeout identifies the Apr 18 blocking seam rather than only citing restart as the workaround
- Guardrails / Error Budgets:
  - preserve March `PR #324` behavior as the reference baseline
  - fail closed into explicit restart-required evidence if safe recovery is not proven
  - keep adjacent issue boundaries intact unless implementation evidence requires updating them

## User Experience
- Personas: local control-host operator, provider-worker issue owner, reviewer diagnosing intake stalls
- User Journeys:
  - an operator inspecting `provider-intake-state.json` can tell that `refresh:claim_reconcile` is stuck and restart-required, not merely slow
  - the focused regression simulates the Apr 18 free-capacity / no-admission shape
  - a reviewer can see why March `PR #324` is historical reference evidence, not a claim that the current recurrence is fixed

## Technical Considerations
- Architectural Notes:
  - March evidence and memory identify the provider refresh lifecycle as the first hypothesis for Ready-but-unclaimed stalls.
  - Apr 18 evidence narrows the current recurrence to `refresh:claim_reconcile` with `stuck=true`, `restart_required=true`, and free capacity.
  - The current blocking seam is in `controlHostSupervision.ts`: repeated active-worker restart quarantine ignored `counts.max_allowed`.
  - The `co-status --format json` operator dataset already exposes `counts.max_allowed`; the missing source behavior was preserving that signal inside supervision diagnostics and the restart-quarantine decision.
- Dependencies / Integrations:
  - Apr 18 `provider-intake-state.json` recurrence evidence
  - `PR #324` / commit `330581458` as the historical `CO-41` reference fix
  - adjacent issue boundaries: `CO-252`, `CO-217`, `CO-211`, `CO-214`, `CO-248`

## Open Questions
- Does broader validation expose any downstream reliance on `ControlHostSupervisionHealthDiagnostic.counts` containing only `running` and `retrying`?
- Do PR checks or review feedback require widening the no-regression matrix beyond supervision health evaluation?

## Approvals
- Product: Self-approved from reopened Linear issue scope
- Engineering: Pending full validation, review, and PR handoff
- Design: N/A
