# ACTION_PLAN - CO-472 Ready released-pending-reopen no-run explicit recovery

## Summary
- Goal: prepare parent implementation to recover Ready released-pending-reopen no-run claims through explicit control-host recover/relaunch/nudge while preserving adjacent provider-intake safety contracts.
- Scope: docs-first packet, parent-owned source inspection, parent-owned focused recovery regressions, and parent-owned validation/handoff.
- Assumptions:
  - source payload path was absent in the child checkout, so the parent-provided child-lane brief is the issue-shaping contract for this packet
  - the relevant claim shape is `provider-intake state=released`, `reason=provider_issue_released_pending_reopen:provider_issue_released:not_active`, `run_id=null`, `run_manifest_path=null`, and no launch token
  - parent owns all source, tests, lifecycle state, Linear/workpad state, PR lifecycle, and patch integration

## Issue Readiness Gate
- Intent checksum / protected terms carried forward:
  - `Ready released-pending-reopen no-run claims`
  - `provider-intake state=released`
  - `reason=provider_issue_released_pending_reopen:provider_issue_released:not_active`
  - `run_id=null`
  - `run_manifest_path=null`
  - `no launch token`
  - `explicit control-host recover/relaunch/nudge`
  - `preserve CO-468 accepted pending-revalidation behavior, CO-193 Ready reclaim safety, CO-392 live-started refresh behavior, and CO-443 completed-claim suppression`
- Not done if:
  - explicit control-host recover/relaunch/nudge cannot recover or truthfully refuse the exact Ready released-pending-reopen no-run claim
  - accepted pending-revalidation behavior changes
  - CO-193 Ready reclaim safety, CO-392 live-started refresh behavior, or CO-443 completed-claim suppression regresses
  - the implementation uses direct provider-worker starts or manual `provider-intake-state.json` edits
- Pre-implementation issue-quality review:
  - The issue is broader than display-only status cleanup and narrower than a provider-intake rewrite.
  - The issue is not CO-193 automatic Ready reclaim, not CO-392 live-started refresh, not CO-468 accepted pending-revalidation, and not CO-443 completed-claim suppression.
  - The micro-task path is unavailable because exact naming, fallback/seam decisions, and adjacent invariant preservation are central to correctness.
- Fallback / refactor decision:
  - `remove fallback`: Ready released-pending-reopen no-run claims suppressing explicit control-host recover/relaunch/nudge.
  - `justify retaining fallback`: CO-468 accepted pending-revalidation behavior as an adjacent supported state family.
  - `justify retaining fallback`: CO-193 Ready reclaim safety as automatic reclaim behavior.
  - `justify retaining fallback`: CO-392 live-started refresh behavior as started-truth recovery.
  - `justify retaining fallback`: CO-443 completed-claim suppression as duplicate-safety behavior.
- Durable retention evidence:
  - Retained released-pending-reopen rows remain audit evidence, but no-run Ready rows must not suppress explicit recovery forever.
  - Accepted pending-revalidation, automatic Ready reclaim, live-started refresh, and completed-claim suppression remain separate non-expiring contracts unless their owner issues replace them.
- Large-refactor check:
  - Parent may keep this narrow only if one existing recovery decision point can distinguish the exact released no-run shape. If the check must be duplicated across multiple control surfaces, parent should stop and route a larger consolidation lane.

## Milestones & Sequencing
1. Child lane creates docs packet and registry mirrors only.
2. Parent imports child patch and records docs-first acceptance in the workpad.
3. Parent inspects released-pending-reopen no-run suppression under explicit recover/relaunch/nudge.
4. Parent implements the smallest supported control-host recovery change for the exact Ready released no-run claim.
5. Parent adds focused recover, relaunch, and nudge regressions for the Ready released-pending-reopen no-run claim.
6. Parent reruns adjacent preservation checks for CO-468, CO-193, CO-392, and CO-443.
7. Parent runs normal validation/review gates, opens or updates PR, handles feedback, drains ready-review, and performs Linear handoff.

## Dependencies
- Root control-host recover/relaunch/nudge path.
- Provider-intake retained claim classification.
- Provider-worker admission and duplicate-launch guardrails.
- Adjacent CO-468, CO-193, CO-392, and CO-443 regression coverage or equivalent focused preservation proof.

## Validation
- Checks / tests:
  - child-lane JSON parse for `tasks/index.json`
  - child-lane scoped `git diff --check`
  - child-lane protected-term scan
  - parent-owned docs-review or equivalent packet review
  - parent-owned focused tests for recover/relaunch/nudge explicit recovery
  - parent-owned adjacent preservation checks for CO-468, CO-193, CO-392, and CO-443
  - parent-owned normal provider-worker validation floor and review
- Rollback plan:
  - If implementation cannot preserve adjacent contracts, revert the source change and keep the docs packet as evidence for a widened owner issue.
  - If source inspection shows split recovery authority, stop and relaunch with widened ownership rather than adding a second ad hoc branch.

## Risks & Mitigations
- Risk: released no-run recovery weakens duplicate-launch safety.
  - Mitigation: parent must preserve live worker, launch token, unreadable occupancy, and active run guardrails before recovery.
- Risk: this lane accidentally changes accepted pending-revalidation behavior.
  - Mitigation: treat accepted pending-revalidation as out of scope except for preservation checks under CO-468.
- Risk: the implementation duplicates Ready reclaim logic.
  - Mitigation: distinguish explicit control-host recover/relaunch/nudge from automatic CO-193 fresh-discovery reclaim.
- Risk: operator evidence remains ambiguous.
  - Mitigation: require a concrete recovery or safe refusal reason that names this released-pending-reopen no-run shape.

## Approvals
- Reviewer: bounded same-issue child lane.
- Date: 2026-05-01.
