# ACTION PLAN - CO-523 strict spec-guard stale last_review scope

## Summary
- Goal: create the CO-523 docs-first packet that preserves `strict spec-guard`, stale `last_review` scope, `Core Lane` evidence boundaries for `CO-522` / `PR #795`, and the non-goals `no blind bumps`, `no deletion`, and `no weakening`.
- Scope: this child lane edits only the six declared packet files.
- Assumptions:
  - the parent-provided source anchor and child-lane instructions are authoritative
  - the referenced source payload path is unavailable in this child checkout
  - parent owns exact stale row discovery, source-row repair, registry/index updates, validation, Linear/GitHub lifecycle, workpad, PR, and patch integration

## Issue Readiness Gate
- Intent checksum / protected terms carried forward:
  - `strict spec-guard`
  - `node scripts/spec-guard.mjs`
  - `last_review`
  - `Core Lane`
  - `CO-522`
  - `PR #795`
  - `docs/design/specs`
  - `tasks/specs`
  - `stale baseline`
  - `no blind bumps`
  - `no deletion`
  - `no weakening`
- Not done if:
  - any CO-523 packet/mirror file is missing
  - the plan permits blind `last_review` bumps
  - the plan permits deletion of historical packet/spec evidence
  - the plan permits weakening or bypassing `node scripts/spec-guard.mjs`
  - the child lane edits existing stale rows in `docs/design/specs` or `tasks/specs`
  - the child lane edits `tasks/index.json`, `docs/TASKS.md`, `docs/docs-freshness-registry.json`, `scripts/spec-guard.mjs`, Linear, GitHub, or PR state
- Pre-implementation issue-quality review:
  - 2026-05-13: approved for packet-only bootstrap. The lane is not suitable for the micro-task path because correctness depends on exact protected terms, exact surfaces, stale-baseline scope, and non-goals.
- Fallback / refactor decision: applies because the task touches stale baseline handling. Decision is `remove fallback`: do not tolerate stale rows as standing debt and do not clear them via blind refresh/deletion; parent must repair only exact evidence-backed rows while leaving `node scripts/spec-guard.mjs` strict.
- Durable retention evidence: no retained fallback is introduced by this child lane.
- Large-refactor check: a larger freshness/spec refactor is out of scope for this child packet and requires parent relaunch with widened ownership.

## Milestones & Sequencing
1. Create the CO-523 PRD, TECH_SPEC mirror, canonical task spec, ACTION_PLAN, task checklist, and `.agent` mirror.
2. Preserve the protected terms and rejected interpretations across the packet.
3. Keep source-row repair out of this child lane.
4. Parent identifies the exact stale baseline rows under `docs/design/specs` and `tasks/specs`.
5. Parent records `Core Lane` evidence for `CO-522` / `PR #795`.
6. Parent decides row by row whether each stale source row needs active review refresh or terminal/inactive reclassification.
7. Parent changes only evidence-backed rows and records why the update is not a blind `last_review` bump.
8. Parent reruns `node scripts/spec-guard.mjs --dry-run` and the relevant handoff gates.
9. Parent keeps `no deletion` and `no weakening` visible in workpad, checklist, PR, and review handoff.

## Dependencies
- Source anchor `ctx:sha256:db68f6a7917841a63197e4457e7bcfe6bcb4009b7eba246a8880f864265816f7#chunk:c000001`.
- Child lane manifest `.runs/linear-8573da42-d9f9-44ce-a24e-224984539044-co523-docs-packet/cli/2026-05-12T18-52-17-405Z-7c08766d/manifest.json`.
- `node scripts/spec-guard.mjs`.
- `Core Lane` evidence for `CO-522` / `PR #795`.
- Parent-owned evidence for stale rows under `docs/design/specs` and `tasks/specs`.

## Validation
- Checks / tests:
  - protected-term scan across the six CO-523 packet files
  - scoped `git diff --check --` over the six CO-523 packet files
  - `git status --short` and `git diff --name-only` to confirm declared file scope only
- Parent-owned validation:
  - exact stale-row evidence before any source edit
  - `node scripts/spec-guard.mjs --dry-run`
  - focused docs/review gates and any required `Core Lane` follow-up after parent source-row repair
- Rollback plan: remove only the six CO-523 packet files from the child patch; no implementation, registry, guard, Linear, or GitHub state is changed by this child lane.

## Risks & Mitigations
- Risk: stale `last_review` rows are mass-bumped.
  - Mitigation: `no blind bumps` is an explicit Not Done If and parent must attach row-level evidence.
- Risk: stale evidence is deleted to make validation pass.
  - Mitigation: `no deletion` is explicit; parent must preserve historical packet/spec lineage.
- Risk: `node scripts/spec-guard.mjs` is weakened to make `Core Lane` green.
  - Mitigation: `no weakening` is explicit; guard behavior is not an implementation surface for this lane.
- Risk: `CO-522` / `PR #795` implementation work bleeds into the packet lane.
  - Mitigation: the packet treats `Core Lane` and PR evidence as parent-owned context only.

## Approvals
- Docs packet child lane: produced in this workspace for parent patch export.
- Parent source-row repair, validation, Linear/GitHub lifecycle, and review handoff: parent-owned.
- Date: 2026-05-13
