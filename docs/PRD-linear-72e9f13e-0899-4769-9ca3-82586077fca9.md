# PRD - CO workflow: refresh current-main docs:freshness stale cohorts 1138-1163 plus release docs

## Added by Bootstrap 2026-04-13

## Traceability
- Linear issue: `CO-167` / `72e9f13e-0899-4769-9ca3-82586077fca9`
- Linear URL: https://linear.app/asabeko/issue/CO-167/co-workflow-refresh-current-main-docsfreshness-stale-cohorts-1138-1163
- Source issue: `CO-166` / `6af47129-33cc-45fa-9a55-e49423536752`
- Source evidence:
  - `out/linear-6af47129-33cc-45fa-9a55-e49423536752/manual/00-classification.md`
  - `out/linear-6af47129-33cc-45fa-9a55-e49423536752/manual/baseline-docs-freshness.log`
  - `out/linear-6af47129-33cc-45fa-9a55-e49423536752/manual/baseline-docs-freshness-report.json`
  - `out/linear-6af47129-33cc-45fa-9a55-e49423536752/manual/01-docs-review-fallback.md`
- Current workpad source:
  - `out/linear-72e9f13e-0899-4769-9ca3-82586077fca9/manual/workpad.md`

## Summary
- Problem Statement: `CO-166` proved the red `docs:freshness` result on current `origin/main` is real repo-baseline drift, not branch-local fallout. The live stale set is exactly two cohorts: a March 13 packet cohort covering task ids `1138` through `1163` (`182` stale docs total across `.agent/task`, `docs/findings`, `docs/ACTION_PLAN-*`, `docs/PRD-*`, `docs/TECH_SPEC-*`, `tasks/specs/*`, and `tasks/tasks-*`) and a January release-doc cohort covering `.agent/SOPs/release.md` plus release packets `0942`, `0943`, and `0944` (`16` stale docs total).
- Desired Outcome: refresh only those two proven stale cohorts, preserve the truthful validation story from `CO-166`, and restore a green `npm run docs:freshness` baseline without waivers or policy changes.

## User Request Translation (Context Anchor)
- User intent / needs (in your own words): register a docs-first packet for `CO-167`, verify the live stale-doc scope against the saved `CO-166` evidence, refresh the March 13 and January release cohorts consistently, and leave machine-checkable before/after proof that unrelated lanes can cite directly.
- Success criteria / acceptance:
  - only the `1138` through `1163` cohort plus `release.md` / `0942` / `0943` / `0944` are refreshed unless a fresh rerun proves new live drift
  - `npm run docs:freshness` turns green with zero missing registry entries, missing files, invalid entries, or uncatalogued docs
  - the lane keeps `CO-165` runtime/test work and freshness-policy changes explicitly out of scope
  - closeout cites exact before/after stale counts and saved rerun artifacts
- Constraints / non-goals:
  - do not reopen `CO-165` provider-worker code or tests
  - do not weaken `docs:freshness`, archive policy, or review cadence rules
  - do not bulk-refresh unrelated docs beyond the two proven stale cohorts

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `docs:freshness`
  - `current-main stale-doc baseline`
  - `1138` through `1163`
  - `release.md`
  - `0942`
  - `0943`
  - `0944`
  - `truthful validation story`
- Protected terms / exact artifact and surface names:
  - `out/linear-6af47129-33cc-45fa-9a55-e49423536752/manual/00-classification.md`
  - `out/linear-6af47129-33cc-45fa-9a55-e49423536752/manual/baseline-docs-freshness-report.json`
  - `docs/docs-freshness-registry.json`
  - `tasks/index.json`
  - `docs/TASKS.md`
  - `.agent/SOPs/release.md`
- Nearby wrong interpretations to reject:
  - this lane should reopen `CO-165` runtime/test repairs
  - the right fix is to waive or relax `docs:freshness`
  - any unrelated stale doc can be refreshed opportunistically while this lane is open

## Parity / Alignment Matrix
- Current truth:
  - current `main` reproduces `198` stale docs with no missing registry coverage, no missing files on disk, no invalid entries, and no uncatalogued docs
  - one March 13 cohort spans task ids `1138` through `1163`
  - one January cohort spans `.agent/SOPs/release.md` plus release packets `0942`, `0943`, and `0944`
- Reference truth:
  - unrelated implementation and provider-worker lanes should be able to cite a truthful green `docs:freshness` baseline rather than carry repo-baseline fallback notes
  - `CO-166` already owns the baseline-classification story and should remain separate from the actual refresh work
- Target truth / intended delta:
  - refresh only the two proven stale cohorts so `npm run docs:freshness` returns green again
  - preserve the truthful claim that the failure was current-main baseline drift, not branch-local fallout
  - keep policy and cadence semantics unchanged
- Explicitly out-of-scope differences:
  - `CO-165` runtime/test fixes
  - `docs:freshness` policy or cadence changes
  - unrelated stale-doc cleanup outside the proven cohorts

## Not Done If
- `npm run docs:freshness` is still red on the repaired tree.
- either proven stale cohort remains only partially refreshed across its registered surfaces.
- closeout cannot cite exact before/after stale counts and saved rerun artifacts.
- the lane blurs the boundary between `CO-166` classification work and the actual stale-doc refresh.

## Goals
- Bootstrap the `CO-167` packet, workpad source, and registry mirrors before implementation.
- Run a truthful docs-review child stream or record a baseline-only fallback if the child stops on the known repo baseline.
- Refresh the March 13 `1138` through `1163` packet cohort consistently and minimally.
- Refresh the January release-doc cohort consistently and minimally.
- Leave auditable before/after evidence tied back to `CO-166`.

## Non-Goals
- Reopening `CO-165` code or tests.
- Changing freshness or archive policy.
- Refreshing docs outside the two proven stale cohorts without fresh evidence.
- Broad repo cleanup that is not required for the stale-cohort repair.

## Stakeholders
- Product: CO maintainers who need truthful review and release handoffs
- Engineering: workers and reviewers depending on a green repo-baseline `docs:freshness` check
- Release / Operations: maintainers relying on current release SOP docs

## Metrics & Guardrails
- Primary Success Metrics:
  - `npm run docs:freshness` exits `0`
  - stale entries reduce from `198` to `0`
  - no new missing registry coverage, missing-on-disk entries, invalid entries, or uncatalogued docs appear
- Guardrails / Error Budgets:
  - keep the diff bounded to the new `CO-167` packet plus the two proven stale cohorts
  - prefer explicit follow-up creation over scope creep if a new blocker appears
  - preserve the truthful split between baseline-classification evidence (`CO-166`) and refresh evidence (`CO-167`)

## User Experience
- Personas:
  - provider worker repairing repo-baseline docs drift
  - reviewer verifying the refresh stayed bounded to the proven stale cohorts
  - maintainer tracing release-doc freshness without reopening release implementation
- User Journeys:
  - a worker reruns `npm run docs:freshness` and sees a truthful green repo baseline
  - a reviewer can audit exactly which stale cohorts were refreshed and why
  - a maintainer can follow the evidence from `CO-166` classification through `CO-167` repair

## Technical Considerations
- Architectural Notes:
  - `scripts/docs-freshness.mjs` trusts `docs/docs-freshness-registry.json` for freshness dates, so registry updates must stay aligned with any mirrored frontmatter or checklist metadata we keep current
  - the stale repair is a metadata-refresh lane, not a freshness-system redesign
  - `docs/TASKS.md` is already at the archive policy threshold and may need the supported archive helper after the new packet snapshot is added
- Dependencies / Integrations:
  - `scripts/docs-freshness.mjs`
  - `docs/docs-freshness-registry.json`
  - `tasks/index.json`
  - the stale packet docs and release docs listed in `out/linear-6af47129-33cc-45fa-9a55-e49423536752/manual/baseline-docs-freshness-report.json`

## Open Questions
- Whether the pre-implementation `docs-review` child stream will stop only on the known repo baseline or surface an additional packet-local docs defect that needs to be fixed before the stale refresh itself.

## Approvals
- Product: pending
- Engineering: pending audited `docs-review` child stream or truthful fallback
- Release / Operations: pending
