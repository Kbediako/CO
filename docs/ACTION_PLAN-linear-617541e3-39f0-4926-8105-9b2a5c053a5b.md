# ACTION PLAN - CO-520 Provider-Worker Codex Goals

## Summary
- Goal: adopt Codex goals as a deterministic provider-worker planning/operator-continuity surface for Linear issues while preserving Linear/workpad authority.
- Scope: docs-first packet, deterministic spec/workpad-plan goal identity/checksum, objective shaping, create/reuse/supersede behavior, advisory `goal_evidence`, `co-status --format json` projection, compatibility states, and focused tests.
- Assumptions:
  - parent lane owns live Linear/workpad/PR lifecycle and imports this docs patch.
  - Codex CLI `0.135.0+` reports `goals stable true`; older `0.130.0` compatibility must stay explicit.
  - provider issue task key remains `linear-617541e3-39f0-4926-8105-9b2a5c053a5b` without suffixes.

## Issue Readiness Gate
- Intent checksum / protected terms carried forward: CO-520, provider-worker goals, Codex goals, Linear specs, `goal_evidence`, `advisory_only`, `provider-worker-goals:linear-spec-derived-status-tracking`, `co-status --format json`, `## Codex Workpad`, `linear parallelization`, `goals stable true`, Codex CLI `0.135.0+`, older `0.130.0` compatibility.
- Not done if:
  - multiple overlapping goals can be created for the same issue/spec/workpad-plan checksum.
  - goals are created before specs, workpad, and checksum evidence.
  - status projection treats goal state as lifecycle authority or hides missing/unsupported states.
  - stale or mismatched-thread evidence can look current.
  - scope broadens into release promotion, generic goal UI, or Linear replacement.
- Pre-implementation issue-quality review: 2026-05-31 packet review classifies the issue as the full provider-worker goals workflow, not just status display; micro-task path is unavailable because correctness depends on exact naming, protected terms, authority boundaries, and parity/state coverage.
- Fallback / refactor decision: this task touches compatibility/seam behavior. Retain explicit older `0.130.0`/unavailable goal states and the CO-492 Linear-first authority contract; do not replace lifecycle authority.
- Durable retention evidence: retain the single CO-382 decision below.

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Provider-worker goals compatibility and authority | Older or unavailable goal evidence remains visible while `goal_evidence` stays advisory-only beside Linear/workpad lifecycle authority. | justify retaining fallback | CO-520 provider-worker workflow | Codex goals are missing, disabled, stale, thread-mismatched, or captured beside Linear/workpad authority. | 2026-05-31 | 2026-05-31 | non-expiring durable compatibility and authority contract | A separate approved authority redesign replaces Linear/workpad authority and proves old goal evidence no longer needs interpretation. | Focused unavailable, stale, mismatched-thread, advisory-only, and status projection tests. |

- Contract name: Linear-first provider-worker lifecycle authority with deterministic advisory Codex goals.
- Owning surface: CO provider-worker workflow and status projection.
- Steady-state proof: current Linear/workpad/PR/review authority remains unchanged; goal states are rendered as `advisory_only`.
- Tests/docs: this CO-520 packet, focused provider-worker goal tests, focused status projection tests, and CO-486/CO-492 advisory evidence docs.
- Non-expiring rationale: older runtime, unavailable, stale, and mismatched-thread goal evidence must remain interpretable as advisory evidence until a separate approved authority redesign replaces it.
- Large refactor: no broad authority refactor is selected; CO-520 adds deterministic advisory continuity inside the existing provider-worker authority surface.
- Minor seam: bounded additive compatibility/status seam is justified because missing, unavailable, stale, and mismatched-thread goals must remain visible without becoming lifecycle authority.

## Milestones & Sequencing
1. [x] Refresh docs-first packet and register the task key.
   - Files: PRD, canonical task spec, TECH_SPEC mirror, ACTION_PLAN, task checklist, `.agent` mirror, `tasks/index.json`, `docs/TASKS.md`.
   - Acceptance: protected terms, wrong interpretations, non-goals, `Not Done If`, advisory boundary, compatibility decision, and validation plan are explicit.
2. [ ] Parent docs-review before implementation.
   - Evidence key: `linear-617541e3-39f0-4926-8105-9b2a5c053a5b-docs-review`.
   - Acceptance: docs-review accepts the packet or parent records and fixes findings.
3. [ ] Implement deterministic goal preparation after docs-first/spec/workpad creation.
   - Acceptance: goal is not created before specs, active `## Codex Workpad`, and checksum evidence exist.
4. [ ] Implement create/reuse/supersede behavior.
   - Acceptance: same spec/workpad-plan checksum reuses/updates; changed checksum supersedes old evidence without overlapping active goals.
5. [ ] Normalize `goal_evidence` and workpad rendering.
   - Acceptance: `authority: advisory_only`, `linear_authority_preserved: true`, and not-authorized surfaces are preserved.
6. [ ] Extend `co-status --format json` advisory projection.
   - Acceptance: missing, unavailable, stale, mismatched-thread, active, and complete states render compactly.
7. [ ] Add focused tests and run scoped validation.
   - Acceptance: duplicate prevention, objective shaping, status rendering, stale/mismatched, resume, idempotency, and authority-boundary tests pass.
8. [ ] Parent runs final repo gates, review, PR, feedback drain, and Linear handoff.

## Dependencies
- Current provider-worker manifest/workpad proof hydration.
- Codex goal tooling availability on Codex CLI `0.135.0+`.
- Existing `goal_evidence` advisory-only contract from CO-486/CO-492.
- Parent-owned docs-review and implementation-gate workflows.

## Validation
- Checks / tests:
  - scoped docs packet checks in this child lane
  - parent-owned docs-review before implementation
  - focused provider-worker goal tests
  - focused `co-status --format json` projection tests
  - focused resume/idempotency tests
  - parent-owned required validation floor after implementation
- Rollback plan: remove the CO-520 goal integration and keep the docs packet as issue evidence; existing Linear/workpad lifecycle authority remains unchanged.

## Risks & Mitigations
- Risk: goals become accidental lifecycle authority.
  - Mitigation: preserve `advisory_only`, `linear_authority_preserved`, and not-authorized lists in manifest/workpad/status tests.
- Risk: retries create duplicate active goals.
  - Mitigation: checksum identity and same-checksum reuse tests are mandatory.
- Risk: unsupported or stale goal state disappears.
  - Mitigation: explicit missing/unavailable/stale/mismatched status projection states are required.
- Risk: status refresh becomes expensive.
  - Mitigation: prefer local spec/workpad/checksum evidence before heavy Linear/GitHub reads.

## Approvals
- Reviewer: parent lane.
- Date: 2026-05-31.
