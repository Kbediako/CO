# PRD - CO-307 child-lane trusted-project pollution

## Traceability
- Linear: `CO-307` / `b7838857-953c-48d1-97fd-ef75aaf31221`
- Task: `linear-b7838857-953c-48d1-97fd-ef75aaf31221`
- Canonical spec: `tasks/specs/linear-b7838857-953c-48d1-97fd-ef75aaf31221.md`
- Docs child lane manifest: `.runs/linear-b7838857-953c-48d1-97fd-ef75aaf31221-docs-packet/cli/2026-04-22T10-39-35-018Z-9f277b0c/manifest.json`
- Source anchor: `ctx:sha256:5592e921272085385fb1db3d450715f2978bddf72fb4d9e61f25ea945e79bafd#chunk:c000001`

## Goal
Stop ephemeral same-issue child-lane workspaces under `.child-lanes/<stream>-<run>` from creating durable trusted-project residue in `~/.codex/config.toml` when a stable CO ancestor is already trusted.

## User Request Translation
- Keep the current parent-owned child-lane model and patch acceptance flow.
- Fix only the launch/home/config seam behind `trusted-project pollution`, `child-lane paths`, and `global ~/.codex/config.toml growth`.
- Do not weaken trust boundaries or silently trust unrelated roots.

## Protected Terms And Surfaces
- Terms: `trusted-project pollution`, `child-lane paths`, `global ~/.codex/config.toml growth`, `do not weaken trust boundaries`
- Surfaces: `.child-lanes/<stream>-<run>`, `lane_workspace_path`, `CODEX_HOME`, `~/.codex/config.toml`, `resolveCodexHome`, `provider-linear-child-lane-proof.json`
- Files: `orchestrator/src/cli/providerLinearChildLaneRunner.ts`, `orchestrator/src/cli/providerLinearChildLaneShell.ts`, `orchestrator/src/cli/providerLinearWorkerRunner.ts`

## Non-Goals
- Generic global Codex config redesign
- Auth/profile cleanup
- Removing parent-owned child-lane boundaries

## Success / Not Done If
- Success:
  - new child-lane runs do not grow global trusted-project state unboundedly when a trusted ancestor already exists
  - the fix remains bounded and fail-closed
- Not done if:
  - each new child-lane path can still append durable trust residue
  - the mitigation is only manual operator cleanup
  - trust expands beyond the intended project root
