# PRD - CO-307 child-lane trusted-project pollution (`CO-307` / `b7838857-953c-48d1-97fd-ef75aaf31221`; task: `linear-b7838857-953c-48d1-97fd-ef75aaf31221`; spec: `tasks/specs/linear-b7838857-953c-48d1-97fd-ef75aaf31221.md`)
- Stop ephemeral same-issue child-lane workspaces under `.child-lanes/<stream>-<run>` from creating durable trusted-project residue in `~/.codex/config.toml` when a stable CO ancestor is already trusted; keep the current parent-owned child-lane model and patch acceptance flow, fix only the launch/home/config seam, and do not weaken trust boundaries.
- Terms / surfaces / non-goals: `trusted-project pollution`, `child-lane paths`, `global ~/.codex/config.toml growth`, `do not weaken trust boundaries`, `.child-lanes/<stream>-<run>`, `lane_workspace_path`, `CODEX_HOME`, `~/.codex/config.toml`, `resolveCodexHome`, `provider-linear-child-lane-proof.json`, disabling trust checks, trusting all `.child-lanes/*`, collapsing lanes into the parent workspace, generic global Codex config redesign, auth/profile cleanup, and removing parent-owned child-lane boundaries.
- Success / not done if: child-lane runs stop growing trusted-project state unboundedly when a trusted ancestor already exists and the fix stays bounded and fail-closed; not done if any new lane can still append durable trust residue, the mitigation is only manual cleanup, or trust expands beyond the intended project root.
