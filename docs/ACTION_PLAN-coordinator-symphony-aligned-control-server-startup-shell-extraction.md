# ACTION_PLAN - Coordinator Symphony-Aligned Control Server Startup Shell Extraction

## Steps

1. Register `1083` docs-first from the `1082` next-slice note and the bounded startup-shell scout.
2. Extract bind/listen plus final bootstrap start ownership from `ControlServer.start()` into one control-local helper.
3. Keep request-context/store seeding and collaborator assembly in `ControlServer.start()`.
4. Add focused regressions for startup success and close-on-failure behavior.
5. Run the validation bundle, sync mirrors, and queue the next bounded Symphony-aligned seam.
