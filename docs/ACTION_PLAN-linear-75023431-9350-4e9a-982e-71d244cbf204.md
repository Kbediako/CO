# ACTION_PLAN - CO-494 provider child-lane accept current issue snapshot

## Summary
- Goal: make `linear child-lane --action accept` use live current issue truth, leave unavailable or backwards current snapshots retryable with an explicit diagnostic, and preserve stale invalidation for parent HEAD movement, live forward `updated_at`, and issue state changes.
- Scope: provider child-lane accept stale-decision logic, accept-time current issue snapshot evidence, focused regressions, docs packet, validation, and parent-owned handoff.
- Assumptions:
  - The source payload contains issue/run metadata only, so the parent-provided child-lane purpose is the issue-shaping authority for this docs packet.
  - Parent run-manifest `issue_updated_at` can be older than the child snapshot and must not be presented as current issue truth.
  - Parent acceptance must still fail closed on parent HEAD movement, live forward `updated_at`, semantic issue drift, patch/proof/scope violations, dirty parent overlap, and missing artifacts.

## Issue Readiness Gate
- Intent checksum / protected terms carried forward: `provider-linear-child-lane`, `linear child-lane --action accept`, `current issue snapshot`, `parent_snapshot`, `issue_updated_at`, `issue_state`, `issue_state_type`, `provider_worker_child_lane_stale`, `provider-linear-child-lane.patch`, `provider-linear-child-lane-proof.json`, `same-issue child lane`, `provider-worker:child-lane-accept-current-issue-snapshot`.
- Not done if:
  - backwards current issue inversion still invalidates child output with `provider_worker_child_lane_stale`
  - current issue snapshot evidence is not recorded or exposed with the accept decision
  - semantic issue state/type drift or live forward `updated_at` movement is allowed through
  - patch/proof/scope/dirty/base-SHA guards are weakened
- Pre-implementation issue-quality review:
  - CO-494 is specific and implementation-ready: correctness hinges on live/current `issue_updated_at` truth, explicit unavailable/backwards diagnostics, and preserved true stale rejection.
  - The micro-task path is unavailable because stale/cached issue snapshot behavior and protected field names define correctness.
- Fallback / refactor decision:
  - `remove fallback`: parent run-manifest `issue_updated_at` must stop being sufficient to classify accept-time current issue truth.
  - `justify retaining fallback`: semantic issue drift remains a durable fail-closed safety contract.
- Large-refactor decision: not required unless source inspection finds the accept snapshot must be coordinated across multiple proof/ledger/status authorities.
- Minor-seam decision: acceptable only as stale-authority removal; do not add a second long-lived current-state branch.

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Parent run-manifest `issue_updated_at` as current accept truth | Accept can fall back to stale run-manifest metadata and compare backwards against the child snapshot. | `remove fallback` | CO-494 | Accept-time live/current issue lookup is unavailable or older than the child snapshot. | observed 2026-05-02 | 2026-05-02 | This issue | Accept uses live current issue truth only; unavailable or backward timestamps fail retryably while forward live movement remains stale. | Focused child-lane accept regression. |
| Semantic issue drift fail-closed behavior | Issue state/type or identity changes between launch and accept. | `justify retaining fallback` | provider child-lane accept helper | Current issue snapshot no longer matches the launched issue contract. | Existing child-lane safety behavior | 2026-05-02 | Durable safety contract | Separate issue-quality review changes parent acceptance trust rules. | Focused stale rejection regression. |

- Contract name: Semantic child-lane issue drift fail-closed.
- Owning surface: provider child-lane accept helper.
- Steady-state proof: real issue state/type or identity changes still return `provider_worker_child_lane_stale`.
- Tests/docs: focused `ProviderLinearChildLaneShell` acceptance tests and the CO-494 packet.
- Non-expiring rationale: semantic issue drift remains durable parent-acceptance safety behavior.

Issue boundaries remain: preserve launch `parent_snapshot`, use live accept-time current issue truth, keep `provider_worker_child_lane_stale` for real drift, and leave patch/proof/scope checks unchanged.

## Milestones & Sequencing
1. Parent accepts this docs packet and records the current issue/workpad state.
2. Inspect `providerLinearChildLaneShell` accept-time snapshot and stale-reason logic.
3. Add focused regressions for CO-486 and CO-493 backwards timestamp inversion, expecting retryable current-issue diagnostics.
4. Add focused regressions where live `updated_at` moves forward or issue state differs, expecting `provider_worker_child_lane_stale`.
5. Implement the smallest accept-time stale-decision change and current issue snapshot evidence field(s).
6. Verify proof/ledger/status evidence remains additive and does not overwrite launch `parent_snapshot`.
7. Run focused checks, then the parent-required validation/review chain.
8. Complete PR lifecycle and Linear handoff under parent ownership.

## Dependencies
- Existing provider child-lane accept helper and tests.
- Existing proof/ledger schema for child-lane records.
- Parent-owned live Linear issue truth and workpad.
- Adjacent boundaries:
  - parent HEAD movement remains stale
  - patch/proof/scope/dirty checks remain unchanged
  - child lanes still do not own Linear/workpad/PR lifecycle

## Validation
- Checks / tests:
  - child docs-lane protected-term grep over packet files
  - child docs-lane JSON parse for registry mirrors
  - child docs-lane `git diff --check` over declared touched files
  - focused accept-time backwards timestamp inversion regression
  - focused live forward timestamp and semantic issue drift rejection regressions
  - focused accept-time current issue snapshot source/unavailable reason evidence assertion
  - `node scripts/delegation-guard.mjs`
  - `node scripts/spec-guard.mjs --dry-run`
  - `npm run build`
  - `npm run lint`
  - `npm run test`
  - `npm run docs:check`
  - `npm run docs:freshness`
  - `npm run repo:stewardship`
  - `node scripts/diff-budget.mjs`
  - manifest-backed review via `TASK=<task-id> NOTES="..." MANIFEST=<path> codex-orchestrator review --manifest <path>` or equivalent wrapper evidence
  - elegance/minimality review
  - `npm run pack:smoke` if required by touched CLI/package surface
- Rollback plan:
  - Revert the accept-time stale-decision/evidence change and focused tests if semantic issue drift, parent HEAD drift, or patch/proof/scope fail-closed behavior regresses.
  - If source inspection shows current issue snapshot evidence cannot stay local to child-lane accept, stop and relaunch with widened ownership.

## Risks & Mitigations
- Keep live forward timestamp/state drift regressions, additive current snapshot evidence, immutable `parent_snapshot`, and child-lane accept-only scope to avoid accepting true stale output or widening provider-worker lifecycle behavior.

Approval: provider-worker parent lane, 2026-05-02.
