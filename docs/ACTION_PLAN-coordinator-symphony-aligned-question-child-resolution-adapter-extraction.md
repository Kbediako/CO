# ACTION_PLAN - Coordinator Symphony-Aligned Question Child-Resolution Adapter Extraction

## Steps

1. Confirm the current seam in `controlServer.ts` and capture the bounded ownership in the findings note.
2. Extract the child-resolution adapter assembly plus fallback event wiring into a dedicated control-local module.
3. Add focused unit/integration coverage for the extracted seam.
4. Run the lane validation bundle and capture explicit overrides if the known full-test/review wrapper issues recur.
5. Sync checklist mirrors and queue the next bounded Symphony-aligned seam.
