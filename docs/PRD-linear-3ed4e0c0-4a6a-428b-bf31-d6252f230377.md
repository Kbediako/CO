# PRD - CO-443 completed intake claim live-truth recovery

## Traceability
- Linear issue: `CO-443`
- Linear URL: https://linear.app/asabeko/issue/CO-443
- Task id: `linear-3ed4e0c0-4a6a-428b-bf31-d6252f230377`
- Canonical spec: `tasks/specs/linear-3ed4e0c0-4a6a-428b-bf31-d6252f230377.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-3ed4e0c0-4a6a-428b-bf31-d6252f230377.md`
- Task checklist: `tasks/tasks-linear-3ed4e0c0-4a6a-428b-bf31-d6252f230377.md`
- Child lane manifest: `.runs/linear-3ed4e0c0-4a6a-428b-bf31-d6252f230377-docs-packet/cli/2026-04-30T06-34-58-998Z-34bebedf/manifest.json`
- Source anchor: `ctx:sha256:cf913ea2dcd62e7ac2df8f89fd2725b8eb43ade503dd23e7c55ae65d81ad7cc7#chunk:c000001`
- Source payload path from handoff: `.runs/linear-3ed4e0c0-4a6a-428b-bf31-d6252f230377-docs-packet/cli/2026-04-30T06-34-58-998Z-34bebedf/memory/source-0/source.txt`
- Source payload note: the referenced payload path is absent in this child checkout; this packet uses the parent-provided issue-shaping prompt and source anchor only.

## Summary
- Problem Statement: a completed provider-intake claim with `provider_issue_run_already_completed` can suppress supported `control-host recover|relaunch|nudge` even after live Linear truth becomes active/fresher again, including when polling has already copied the latest active timestamp into the completed claim. That makes a stale completed-run cache act as permanent authority over current issue state.
- Desired Outcome: completed-run provider-intake evidence remains auditable, but supported control-host recovery actions revalidate against live Linear truth. When the issue is active/fresher again, `control-host recover|relaunch|nudge` is not suppressed solely by `provider_issue_run_already_completed`.

## User Request Translation
- User intent / needs: create the CO-443 docs-first packet for a narrow parent implementation lane that restores supported control-host actions when stale completed provider-intake state disagrees with newer active Linear truth.
- Success criteria / acceptance:
  - `provider_issue_run_already_completed` does not suppress `control-host recover|relaunch|nudge` when live Linear truth is active/fresher again, including after poll reconciliation has consumed the active timestamp.
  - unchanged or older live truth can still be treated as already completed so equal/non-increasing replays do not duplicate provider-worker runs.
  - completed provider-intake rows remain audit-visible and are revalidated or superseded by code, not hand-edited.
  - supported control-host actions keep CO-393 provenance, launch-token, issue-workspace, and direct-start guard contracts.
  - focused regressions cover stale completed claim plus active/fresher Linear truth for recover/relaunch/nudge, and unchanged non-recovery duplicate truth still no-ops.
- Constraints / non-goals:
  - no implementation, test, package, Linear, GitHub, workpad, PR, or lifecycle edits in this child lane
  - do not widen into `CO-330` stale-owner recovery
  - do not widen into `CO-393` command plumbing
  - do not widen into `CO-404` timeout acknowledgement
  - do not widen into `CO-406` no-run capacity
  - do not widen into `CO-392` released-pending-reopen
  - do not add direct `provider-linear-worker` starts
  - do not repair by manual `provider-intake-state.json` edits

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `completed provider-intake claims`
  - `provider_issue_run_already_completed`
  - `control-host recover|relaunch|nudge`
  - `live Linear truth`
  - `active/fresher again`
  - `supported`
- Protected terms / exact artifact and surface names:
  - `provider-intake-state.json`
  - `control-host recover|relaunch|nudge`
  - `provider_issue_run_already_completed`
  - `provider-linear-worker`
  - `CO-330 stale-owner recovery`
  - `CO-393 command plumbing`
  - `CO-404 timeout acknowledgement`
  - `CO-406 no-run capacity`
  - `CO-392 released-pending-reopen`
- Nearby wrong interpretations to reject:
  - treating `provider_issue_run_already_completed` as permanent authority after live Linear state becomes newer and active
  - deleting or manually editing `provider-intake-state.json` to make the issue runnable
  - direct-starting `provider-linear-worker` instead of going through root control-host recover/relaunch/nudge
  - reimplementing CO-393 control-host command plumbing
  - changing CO-404 acknowledgement timeout or CO-406 capacity accounting
  - solving CO-330 stale control-host owner recovery or CO-392 released-pending-reopen behavior

## Parity / Alignment Matrix

| Surface | Current truth | Reference truth | Target truth | Explicitly out of scope |
| --- | --- | --- | --- | --- |
| Completed provider-intake claim | `provider-intake-state.json` can retain a completed row with `reason=provider_issue_run_already_completed`. | Completed rows are useful audit evidence for unchanged/equal replays. | A completed row no longer suppresses supported control-host action when live Linear truth is active/fresher again. | Deleting or hand-editing intake state. |
| Live Linear revalidation | Explicit control-host actions can need to recover an issue after state changes. | Live Linear truth is authoritative when it is newer than cached provider-intake truth. | Recover/relaunch/nudge revalidates the completed claim before deciding no-op versus supported action. | CO-330 stale-owner recovery or broader polling/reconcile loops. |
| Control-host action path | CO-393 owns command plumbing and direct-start protection. | Supported operations go through root control-host authority. | CO-443 reuses supported `control-host recover/relaunch/nudge` without adding direct provider starts or changing CLI shape. | New command plumbing or direct `provider-linear-worker` starts. |
| Adjacent residue families | CO-404, CO-406, and CO-392 each own separate timeout, capacity, and released-pending-reopen behavior. | Each residue family stays in its owner lane. | CO-443 fixes only completed-run suppression when live Linear truth is active/fresher. | Timeout acknowledgement, no-run capacity, or released-pending-reopen semantics. |

## Not Done If
- `control-host recover|relaunch|nudge` can still no-op solely because of `provider_issue_run_already_completed` after live Linear truth is active/fresher again.
- Equal or older unchanged replays now duplicate completed provider-worker runs.
- The repair depends on manual `provider-intake-state.json` edits, deleting audit rows, or direct `provider-linear-worker` starts.
- The implementation changes CO-330, CO-393, CO-404, CO-406, or CO-392 behavior outside the exact stale-completed-claim boundary.
- Focused regressions do not prove both the active/fresher recovery path and the unchanged duplicate no-op path.

## Goals
- Preserve a precise CO-443 implementation contract before parent source edits.
- Restore supported control-host recovery actions when stale completed intake state is older than active live Linear truth.
- Keep completed-run provider-intake evidence audit-visible and duplicate-safe for unchanged replays.
- Preserve direct-start guard, launch provenance, and adjacent issue boundaries.

## Non-Goals
- No source, test, package, Linear, GitHub, PR, or workpad changes in this child lane.
- No broad provider-worker lifecycle, polling, stale-owner, timeout, capacity, or released-state redesign.
- No direct `provider-linear-worker` launch path.
- No manual provider-intake-state cleanup.
- No docs-freshness or review-wrapper work.

## Stakeholders
- Product: CO operators using control-host recover/relaunch/nudge to restore provider-worker progress from current Linear truth.
- Engineering: provider-intake, control-host recovery, provider-worker admission, and lifecycle maintainers.
- Design: N/A.

## Metrics & Guardrails
- Primary Success Metrics:
  - focused regression proves active/fresher live Linear truth is not suppressed by `provider_issue_run_already_completed`
  - unchanged/equal completed replays remain duplicate-safe
  - control-host recovery path retains CO-393 provenance and guardrails
- Guardrails / Error Budgets:
  - zero manual provider-intake-state edits as the fix
  - zero direct `provider-linear-worker` starts
  - zero behavior changes to CO-330, CO-393, CO-404, CO-406, or CO-392 outside the stated boundary

## Technical Considerations
- Architectural Notes:
  - The parent should prefer a small stale-completed-claim revalidation path near the existing provider-intake/control-host recovery authority.
  - The completed claim should remain valid for equal or non-increasing issue truth, matching prior duplicate-run prevention.
  - Live Linear active state with a fresher update timestamp should supersede stale completed-claim suppression for supported control-host actions.
- Dependencies / Integrations:
  - `provider-intake-state.json`
  - `provider_issue_run_already_completed`
  - `control-host recover|relaunch|nudge`
  - live Linear issue state and `issue_updated_at`
  - CO-393 root control-host provenance and direct-start guard contracts

## Fallback / Refactor Decision
- Applies to fallback, compatibility, legacy, stale, cached, break-glass, or minor-seam behavior? `Yes`.

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Completed-claim suppression | `provider_issue_run_already_completed` suppresses supported action even after fresher active Linear truth | `remove fallback` | CO-443 parent implementation lane | Control-host recover/relaunch/nudge sees a completed intake claim whose cached truth is older than live active Linear truth. | observed 2026-04-30 | N/A after removal | N/A after removal | Completed claim is revalidated and no longer suppresses fresher active truth. | Focused regression for stale completed claim plus fresher active Linear truth. |
| Completed-run audit row | Retain completed provider-intake evidence for unchanged/equal replays | `justify retaining fallback` | CO-443 parent implementation lane | Provider-intake has completed run evidence and live truth is not newer active truth. | existing provider-intake duplicate-safety contract | 2026-04-30 | Non-expiring audit and duplicate-safety contract | Replace only with a stronger completed-run freshness contract that preserves duplicate-run prevention and auditability. | Regression proves unchanged/equal truth still no-ops and does not duplicate runs. |
| Direct-start guard | Root control-host must own supported recovery starts | `justify retaining fallback` | CO-393 control-host recovery contract / CO-443 parent lane | Operator attempts recovery for a specific Linear issue. | CO-393 | 2026-04-30 | Non-expiring launch-authority contract | Remove only if provider-worker launch authority is replaced by an equivalent or stronger root-control contract. | Focused recovery tests preserve control-host provenance and reject direct starts. |

- Large-refactor check: a large refactor is not required for this packet. CO-443 should stay narrower than provider workflow reconciliation and reuse existing control-host action plumbing unless parent source inspection proves completed-claim freshness authority is duplicated across incompatible lifecycle phases.

## Open Questions
- Parent source inspection should confirm the exact state and timestamp fields used to compare cached completed truth with live Linear active truth.

## Approvals
- Docs-first packet: bounded same-issue child lane, 2026-04-30.
