# ACTION_PLAN - Coordinator Symphony-Aligned Telegram Question-Read Adapter Assembly Extraction

## Steps

1. Register `1076` docs-first from the `1075` next-slice note and the real-Symphony shell-versus-core heuristic.
2. Extract the remaining Telegram question-read adapter assembly into one control-local helper.
3. Keep `controlServer.ts` thin while preserving existing question-read and child-resolution semantics.
4. Add focused regressions for the extracted helper seam.
5. Run the lane validation bundle, sync mirrors, and queue the next bounded Symphony-aligned seam.
