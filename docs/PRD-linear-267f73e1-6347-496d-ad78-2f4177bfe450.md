# PRD - CO-351 Codex CLI 0.125.0 App-Server Control Seam Validation

## Added by Bootstrap 2026-04-24

## Traceability
- Linear issue: `CO-351` / `267f73e1-6347-496d-ad78-2f4177bfe450`
- MCP task id: `linear-267f73e1-6347-496d-ad78-2f4177bfe450`
- Source anchor: `ctx:sha256:57e3788049ac55221b4dd437196096c5dd036790ab40746609e075f7b776202f#chunk:c000001`
- Source payload declared by parent: `.runs/linear-267f73e1-6347-496d-ad78-2f4177bfe450/cli/2026-04-24T20-42-05-147Z-c64686a5/memory/source-0/source.txt`
- Child-lane note: the docs child lane created the initial packet from the parent-provided issue text and protected terms; the parent lane later reconciled the source anchor and added canary evidence.

## Summary
- Problem Statement: CO needs a docs-first decision frame for Codex CLI 0.125.0 app-server control seam validation. The repo already treats `codex exec / codex exec resume` as the supervised provider-worker fallback path until a guarded app-server control seam is proven. Codex CLI 0.125.0 exposes candidate app-server surfaces that matter to CO, but adoption cannot be based on release-note summarization. The parent lane needs a packet that turns those surfaces into CO-relevant canaries, captures the official-doc/local-help mismatch for unix://, and records the exact adoption boundary after evidence.
- Desired Outcome: parent validates Codex CLI `0.125.0` against the app-server control seam using focused local canaries, adopts the proven app-server surfaces for explicit control-host/proof usage, and leaves existing `codex exec / codex exec resume` supervision available as rollback until sticky-environment, real turn-backed pagination, runtime-mode, and cloud evidence support broader provider-supervision migration.

## User Request Translation (Context Anchor)
- User intent / needs (in your own words): create the CO-351 docs-first packet only. The issue should be framed as a Codex CLI 0.125.0 app-server control seam validation lane, not a general version upgrade, implementation sprint, release-note summary, or direct mutation of Linear/workpad/PR state. The packet must preserve the exact candidate surfaces and define evidence gates that prove whether the new app-server path can safely support CO before any version-policy or runtime adoption change.
- Success criteria / acceptance:
  - PRD, TECH_SPEC, ACTION_PLAN, task spec, task checklist, `.agent` mirror, `tasks/index.json`, and `docs/TASKS.md` are created or refreshed for CO-351 only.
  - The packet preserves the exact terms: Codex CLI 0.125.0, app-server control seam, `codex app-server --listen unix://...`, `app-server proxy --sock`, schema generation, remote thread store/config, sticky environments, resume/fork pagination, permission-profile round-trip, explicit untrusted project config, bursty WebSocket tool-output handling, `codex exec / codex exec resume`, and official-doc/local-help mismatch for unix://.
  - Acceptance criteria require CO-relevant canaries and an adoption-boundary decision, not release-note summarization.
  - Parent-owned work is clearly separated: canary execution, version-policy edits, findings docs, implementation validation, review, PR lifecycle, Linear state, and workpad updates stay outside this child lane.
  - The target TECH_SPEC is linked in `tasks/index.json` under `items[]`.
- Constraints / non-goals:
  - no Linear mutation helpers
  - no code, test, script, package metadata, generated output, findings-doc, version-policy, validation-suite, review, or PR-state edits in this child lane
  - no full repo validation from this child lane
  - no blind adoption of Codex CLI `0.125.0` beyond the surfaces parent canaries actually prove

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `Codex CLI 0.125.0`
  - `app-server control seam`
  - `codex app-server --listen unix://...`
  - `app-server proxy --sock`
  - `schema generation`
  - `remote thread store/config`
  - `sticky environments`
  - `resume/fork pagination`
  - `permission-profile round-trip`
  - `explicit untrusted project config`
  - `bursty WebSocket tool-output handling`
  - `codex exec / codex exec resume`
  - `official-doc/local-help mismatch for unix://`
- Protected terms / exact artifact and surface names:
  - `CO-351`
  - `linear-267f73e1-6347-496d-ad78-2f4177bfe450`
  - Codex CLI `0.125.0`
  - `codex app-server --listen unix://...`
  - `app-server proxy --sock`
  - `codex exec`
  - `codex exec resume`
  - `docs/guides/codex-version-policy.md`
  - `.runs/linear-267f73e1-6347-496d-ad78-2f4177bfe450-docs-packet/cli/2026-04-24T20-46-46-959Z-a7f3b702/manifest.json`
  - `ctx:sha256:57e3788049ac55221b4dd437196096c5dd036790ab40746609e075f7b776202f#chunk:c000001`
  - `out/linear-267f73e1-6347-496d-ad78-2f4177bfe450/manual/codex-0125-appserver-canary/runtime-canary-summary.json`
  - `docs/findings/linear-267f73e1-6347-496d-ad78-2f4177bfe450-codex-0125-appserver-control-seam.md`
- Nearby wrong interpretations to reject:
  - treating CO-351 as a release-note summarization issue
  - blindly promoting Codex CLI `0.125.0` because a version or changelog exists
  - removing `codex exec / codex exec resume` provider-supervision fallback before sticky environment and real turn-backed pagination canaries pass
  - treating `unix://` support as proven when official docs and local help disagree
  - using this child lane to run canaries, edit version policy, create findings docs, mutate Linear/workpad state, or open/update a PR
  - broadening into general app-server migration, remote-control authority redesign, marketplace rebaseline, or unrelated Codex CLI feature adoption

## Parity / Alignment Matrix
| Surface | Current truth | Reference truth | Target truth / intended delta |
| --- | --- | --- | --- |
| Provider runtime control | CO still keeps `codex exec / codex exec resume` as the trusted provider-worker supervision fallback. | Prior CO app-server seam work requires explicit guarded authority before provider work moves fully to a resident app-server path. | Parent validates whether Codex CLI 0.125.0 can support guarded app-server control without weakening the existing exec/resume rollback path. |
| App-server socket launch | No adopted CO policy yet says `codex app-server --listen unix://...` is ready for guarded CO control-host/proof use. | Candidate 0.125 surface includes `codex app-server --listen unix://...`, while the issue flags an official-doc/local-help mismatch for unix://. | Parent proves the real local command/help behavior and adopts the socket path for explicit control-host/proof usage with documented caveats. |
| Proxy/control client | CO has no adopted control seam through `app-server proxy --sock`. | Candidate surface names `app-server proxy --sock` as the local control path to validate. | Parent runs canaries that prove proxy connectivity, authority boundaries, and failure behavior before bounded control-host adoption. |
| Schema and thread/config state | CO cannot rely on new app-server contracts until schema generation and remote thread store/config behavior are proven. | Candidate validation must include schema generation and remote thread store/config. | Parent records whether generated schemas and remote thread/config persistence are adequate for CO runtime and review surfaces. |
| Sticky/resume/fork behavior | Existing exec/resume flows preserve continuity through current provider-worker contracts only. | Candidate app-server control seam must prove sticky environments plus resume/fork pagination. | Parent validates sticky environments, resume/fork pagination, and continuity behavior without regressing existing session lineage expectations. |
| Permission and trust posture | CO must keep permission profiles and untrusted project config explicit. | Candidate validation requires permission-profile round-trip and explicit untrusted project config. | Parent proves permission-profile round-trip behavior and untrusted project config handling before any adoption. |
| Tool-output transport | Current CLI supervision does not depend on a new WebSocket tool-output path. | Candidate seam must tolerate bursty WebSocket tool-output handling. | Parent canary stresses bursty WebSocket tool-output handling and records adoption-boundary evidence. |

Explicitly out-of-scope differences: implementation code, version-policy edits, findings docs, validation artifacts, review execution, PR handoff, Linear mutation, and broad runtime migration.

## Not Done If
- The packet does not mention Codex CLI 0.125.0 and the app-server control seam as the central validation subject.
- Any protected term from the intent checksum is absent or renamed.
- Acceptance criteria are only "read release notes" or "summarize 0.125.0" instead of requiring CO-relevant canaries.
- The issue can be mistaken for immediate adoption, a version-policy edit, or a replacement of `codex exec / codex exec resume` before evidence.
- The official-doc/local-help mismatch for unix:// is omitted.
- Parent-owned canary execution, findings docs, validation, review, PR handoff, or Linear/workpad mutation is pulled into this child lane.

## Goals
- Create the CO-351 docs-first packet and registry/checklist mirrors inside the declared file scope.
- Preserve the exact app-server control seam surfaces and evidence gates the parent must validate.
- Make the adoption-boundary criteria explicit before any Codex CLI 0.125.0 posture or runtime-seam change.
- Keep fallback behavior grounded in the current `codex exec / codex exec resume` provider-worker path while adopting only the app-server surfaces this lane proves.

## Non-Goals
- No canary execution in this child lane.
- No implementation, package, script, test, generated output, version-policy, findings-doc, validation-suite, review, PR, Linear, or workpad mutation.
- No blanket promotion of Codex CLI `0.125.0` beyond the proven app-server control surfaces.
- No broad app-server migration or remote-control redesign.
- No release-note-only closeout.

## Stakeholders
- Product: CO operators deciding whether a new app-server seam is safe to adopt.
- Engineering: CO runtime, provider-worker, review-wrapper, and version-policy maintainers.
- Design: Not applicable.

## Metrics & Guardrails
- Primary Success Metrics:
  - packet captures all protected terms and wrong interpretations
  - registry mirrors point to the canonical TECH_SPEC under `tasks/specs/**`
  - parent has canary-driven acceptance criteria and a bounded adoption decision frame
- Guardrails / Error Budgets:
  - do not weaken `codex exec / codex exec resume` fallback posture
  - do not claim `unix://` support without local help and command evidence
  - do not remove provider-supervision fallback without schema, permission, thread-store/config, sticky environment, pagination, and WebSocket burst evidence

## User Experience
- Personas:
  - CO maintainer auditing whether Codex CLI `0.125.0` app-server behavior is safe enough for provider workflows
  - operator who needs the existing exec/resume path to stay available while a new seam is adopted incrementally
  - reviewer verifying that adoption is backed by canaries rather than release-note prose
- User Journeys:
  - parent implementer opens the packet and sees exactly which app-server canaries must run
  - reviewer checks that the official-doc/local-help mismatch for unix:// is addressed before any adoption claim
  - operator sees a clear adoption boundary and fallback story after parent validation

## Technical Considerations
- Architectural Notes:
  - the parent lane should treat `codex app-server --listen unix://...` and `app-server proxy --sock` as candidate control surfaces that must be proven under CO's authority model
  - schema generation, remote thread store/config, sticky environments, resume/fork pagination, permission-profile round-trip, explicit untrusted project config, and bursty WebSocket tool-output handling are minimum acceptance surfaces
  - current `codex exec / codex exec resume` should remain the fallback path while parent evidence justifies only bounded app-server usage
- Dependencies / Integrations:
  - parent-owned local Codex CLI `0.125.0` install/help evidence
  - parent-owned official documentation check for `unix://`
  - parent-owned app-server canary artifacts
  - `docs/guides/codex-version-policy.md` for later parent-managed policy edits, if any

## Open Questions
- Does local Codex CLI `0.125.0` help accept `codex app-server --listen unix://...`, and how does that compare to official docs?
- Can `app-server proxy --sock` drive the required CO control operations without expanding authority or bypassing permission profile checks?
- Do schema generation, remote thread store/config, sticky environments, resume/fork pagination, permission-profile round-trip, explicit untrusted project config, and bursty WebSocket tool-output handling all pass in one coherent app-server canary set?
- Which configured lane should prove sticky environments and real turn-backed pagination before provider-supervision fallback constraints are relaxed?

## Approvals
- Product: CO-351 issue scope provided by parent lane.
- Engineering: docs child lane self-reviewed; parent docs-review reran after current-main reconciliation and passed with `review_outcome=clean-success`; parent owns terminal validation, Linear/workpad state, review, and PR lifecycle.
- Design: Not applicable.
