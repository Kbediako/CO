# TECH_SPEC - CO validation: stabilize unrelated CLI/runtime test timeouts that block provider-worker full-suite evidence

## Added by Bootstrap 2026-04-12

## Summary
- Objective: reproduce and classify the named CLI/runtime timeout blockers, then restore a truthful provider-worker repo-wide validation path with the smallest evidence-backed outcome.
- Scope: docs-first packet, workpad, audited docs-review delegation, focused reproductions for the named suites, exact repo-wide `npm run test` evidence, and the required validation/review gates before any handoff.
- Constraints:
  - preserve the issue’s exact blocker surfaces and reject false attribution to CO-120
  - do not treat a partial `npm run test` run as acceptable evidence
  - avoid broad unrelated harness redesign beyond the named failing lanes

## Issue-Shaping Contract
- User-request translation carried forward:
  - restore truthful provider-worker closeout evidence for repo-wide tests by reproducing and classifying the listed timeout suites rather than speculating about CO-120
  - keep the final outcome auditable: either `npm run test` reaches terminal status again, or the blocker-handling mechanism stays explicit and bounded
- Protected terms / exact artifact and surface names:
  - `npm run test`
  - `orchestrator/tests/CodexOrchestratorCli.test.ts`
  - `orchestrator/tests/LearningCrystalizerCliClient.test.ts`
  - `orchestrator/tests/CliExecRuntime.test.ts`
  - `tests/run-review.spec.ts`
  - `tests/cli-command-surface.spec.ts`
  - `orchestrator/tests/ProviderLinearWorkerRunner.test.ts`
- Nearby wrong interpretations to reject:
  - blame `CO-120` without new evidence
  - call a killed or partial full-suite run good enough
  - permanently skip repo-wide validation for provider-worker closeout
- Explicit non-goals carried forward:
  - reopening the linked-worktree identity implementation
  - broad unrelated Vitest/runtime redesign
  - weakening the truthfulness requirement for provider-worker handoff

## Parity / Alignment Matrix
- Current truth:
  - change-local CO-120 validation is green
  - current-tree reproductions do not show a live provider-worker full-suite blocker: plain `npm run test` passed in `135.22s`, and the stage-owned `CODEX_VITEST_PROGRESS=1 npm run test` run passed in `138.78s`
  - the named suites are outside the CO-120 change surface and are currently green both in isolation and in the repo-wide run
- Reference truth:
  - provider-worker review handoff expects truthful repo-wide validation evidence
  - the issue packet already names the exact suites and rejected shortcuts to preserve
- Target truth / intended delta:
  - exact reproduction/classification for the listed suites
  - a clean terminal `npm run test` result for the current tree, recorded with exact timing and suite evidence
  - workpad/handoff guidance that distinguishes change-local failures from historical unrelated repo-wide blocker reports
- Explicitly out-of-scope differences:
  - changing CO-120 implementation behavior
  - repo-wide validation bypass as a standing policy
  - unrelated CLI/runtime modernization beyond the blocker seam

## Readiness Gate
- Not done if:
  - `npm run test` still cannot produce a trustworthy provider-worker closeout result
  - the listed timeout-heavy suites remain unexplained or unclassified
  - provider-worker guidance still collapses unrelated blockers into false-green handoff language
- Pre-implementation issue-quality review evidence:
  - 2026-04-12: the issue is already properly shaped around the named suite surfaces, explicit non-goals, and truthful-handoff contract. The missing input is reproduced root-cause evidence, not more scope.
- Safeguard ownership split:
  - parent: docs packet, workpad, branch, reproductions, remediation/policy selection, validation, final review/handoff
  - delegated stream: audited `linear child-stream --pipeline docs-review` after packet creation

## Technical Requirements
- Functional requirements:
  - reproduce the timeout behavior for each named suite with exact commands
  - classify each suite as regression, harness timeout, or existing long-run instability
  - restore a truthful provider-worker validation path for repo-wide tests
  - preserve distinct reporting for change-local failures versus unrelated repo-wide blockers
- Non-functional requirements (performance, reliability, security):
  - no false-green validation claims
  - bounded, auditable change surface
  - fail closed when the suite still cannot reach a trustworthy terminal result
- Interfaces / contracts:
  - `npm run test`
  - suite-local `npx vitest run ...` reproductions
  - provider-worker workpad and review-handoff wording

## Architecture & Data
- Architecture / design adjustments:
  - prefer one shared timeout/process-management fix if the named suites share a root cause and still fail
  - otherwise prefer a no-code closeout when exact current-tree evidence already restores truthful validation, instead of speculative timeout inflation
- Data model changes / migrations:
  - none expected unless the chosen blocker-handling mechanism needs bounded review/workpad metadata updates
- External dependencies / integrations:
  - `package.json`
  - Vitest runner/configuration
  - the named CLI/runtime test files
  - Linear workpad and provider-worker review-handoff surfaces

## Validation Plan
- Tests / checks:
  - audited `docs-review` child stream
  - focused reproduction commands for the named suites
  - repo-wide `npm run test` rerun with terminal-status capture
  - standard validation floor for the final non-trivial diff
  - standalone review followed by explicit elegance review before handoff
- Rollout verification:
  - refresh the same workpad after docs, reproduction, remediation, and final validation
- Monitoring / alerts:
  - record exact termination mode and blocker attribution when the suite does not finish
  - keep unrelated CI/review/merge blockers separate from review-wrapper or validation-surface issues

## Current Evidence
- Audited docs-review child stream `co-130-docs-review-r2` succeeded with manifest `.runs/linear-b792b517-98e4-4518-9fb2-860996dd62c8-co-130-docs-review-r2/cli/2026-04-12T13-32-44-649Z-053ed626/manifest.json` and review telemetry `status: succeeded`, `review_outcome: clean-success`.
- Focused isolated runs all passed:
  - `orchestrator/tests/CodexOrchestratorCli.test.ts`: `24.75s` real / `24.13s` Vitest duration
  - `orchestrator/tests/LearningCrystalizerCliClient.test.ts`: `1.17s` real / `828ms` Vitest duration
  - `orchestrator/tests/CliExecRuntime.test.ts`: `1.17s` real / `882ms` Vitest duration
- Stage-owned repo-wide run passed: `env CODEX_VITEST_PROGRESS=1 npm run test` completed in `138.78s` real time with `333` files and `3641` tests passing.
- Exact repo-wide run passed: plain `npm run test` completed in `135.22s` real time with `333` files and `3641` tests passing.
- Long-running suites are currently terminal-green rather than stalled:
  - `tests/run-review.spec.ts`: `132644ms` in the plain run, `135329ms` in the stage-owned run
  - `tests/cli-command-surface.spec.ts`: `71157ms` in the plain run, `101226ms` in the stage-owned run
  - `orchestrator/tests/ProviderLinearWorkerRunner.test.ts`: `3801ms` in the plain run, `3879ms` in the stage-owned run

## Open Questions
- None for current closeout. If timeout instability returns in a later run, re-open classification from the exact commands captured under `out/linear-b792b517-98e4-4518-9fb2-860996dd62c8/manual/repro/`.

## Approvals
- Reviewer: Self-approved for implementation start after this packet and the pre-implementation issue-quality review above. Audited docs-review child stream is complete; final validation floor, standalone review, and elegance review remain before handoff.
- Date: 2026-04-12
