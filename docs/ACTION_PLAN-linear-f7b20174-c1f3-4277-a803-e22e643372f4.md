# ACTION_PLAN - CO: Scope external gitdir eval NODE_PATH roots to checkout

## Added by Bootstrap (refresh as needed)

## Traceability
- Linear issue: `CO-71` / `f7b20174-c1f3-4277-a803-e22e643372f4`
- Linear URL: https://linear.app/asabeko/issue/CO-71/co-scope-external-gitdir-eval-node-path-roots-to-checkout

## Summary
- Goal: finish `CO-71` by reproducing and repairing the external gitdir checkout `NODE_PATH` boundary leak in `evaluation/harness/env.ts`.
- Scope: docs-first packet, child docs-review evidence, focused harness reproduction, minimal boundary fix, required validation, and review-ready workpad refreshes.
- Assumptions:
  - the current missing case is limited to non-ancestor external gitdir layouts such as `git clone --separate-git-dir <gitdir>`
  - the repaired worktree, symlinked-path, and submodule cases should remain unchanged
  - a narrow ancestry-based repair is preferable to any broad repository-discovery redesign
- Current status:
  - issue moved from `Ready` to `In Progress`
  - workspace switched from detached `HEAD` to `linear/co-71-scope-external-gitdir-node-path-roots`
  - docs packet is being registered before the focused reproduction

## Issue Readiness Gate
- Intent checksum / protected terms carried forward:
  - `git clone --separate-git-dir`
  - `external gitdir checkout`
  - `current checkout root`
  - `NODE_PATH boundary leak`
  - `evaluation/harness/env.ts`
- Not done if:
  - the external gitdir boundary leak is not reproduced deterministically
  - the repair still permits ancestor `node_modules` entries above the checkout root
  - the change regresses the repaired worktree, symlinked-path, or submodule coverage
- Pre-implementation issue-quality review:
  - the issue is scoped correctly as a distinct follow-up to `CO-58`; implementation should stay on the external gitdir checkout seam and use a follow-up issue if another same-family edge case appears

## Milestones & Sequencing
1. Register the `CO-71` docs-first packet, update `tasks/index.json`, refresh `docs/TASKS.md`, refresh `docs/docs-freshness-registry.json`, and mirror the checklist under `.agent/task/`.
2. Create the single required Linear workpad comment and launch the audited child `docs-review` stream for the new packet.
3. Add or refresh the focused harness regression that reproduces the external gitdir boundary leak.
4. Implement the smallest truthful repair in `evaluation/harness/env.ts` and rerun focused regressions until the checkout boundary is preserved.
5. Run the required validation floor for the final diff, then standalone review followed by an explicit elegance/minimality pass.
6. Refresh the workpad with the repair, validation, and review status, then open or update the PR and move toward review handoff only once all prerequisites are satisfied.

## Dependencies
- `evaluation/harness/env.ts`
- `evaluation/tests/harness.test.ts`
- `skills/linear/SKILL.md`

## Validation
- Checks / tests:
  - `MCP_RUNNER_TASK_ID=linear-f7b20174-c1f3-4277-a803-e22e643372f4 node "/Users/kbediako/Code/CO/dist/bin/codex-orchestrator.js" linear child-stream --pipeline docs-review --stream co-71-docs-review --format json`
  - focused harness regression reruns covering the external gitdir layout
  - `MCP_RUNNER_TASK_ID=linear-f7b20174-c1f3-4277-a803-e22e643372f4 npm run eval:test`
  - `MCP_RUNNER_TASK_ID=linear-f7b20174-c1f3-4277-a803-e22e643372f4 node scripts/delegation-guard.mjs`
  - `MCP_RUNNER_TASK_ID=linear-f7b20174-c1f3-4277-a803-e22e643372f4 node scripts/spec-guard.mjs --dry-run`
  - `MCP_RUNNER_TASK_ID=linear-f7b20174-c1f3-4277-a803-e22e643372f4 npm run build`
  - `MCP_RUNNER_TASK_ID=linear-f7b20174-c1f3-4277-a803-e22e643372f4 npm run lint`
  - `MCP_RUNNER_TASK_ID=linear-f7b20174-c1f3-4277-a803-e22e643372f4 npm run test`
  - `MCP_RUNNER_TASK_ID=linear-f7b20174-c1f3-4277-a803-e22e643372f4 npm run docs:check`
  - `MCP_RUNNER_TASK_ID=linear-f7b20174-c1f3-4277-a803-e22e643372f4 npm run docs:freshness`
  - `MCP_RUNNER_TASK_ID=linear-f7b20174-c1f3-4277-a803-e22e643372f4 node scripts/diff-budget.mjs`
  - `MCP_RUNNER_TASK_ID=linear-f7b20174-c1f3-4277-a803-e22e643372f4 FORCE_CODEX_REVIEW=1 npm run review`
- Rollback plan:
  - revert any change that widens repo-root detection incorrectly or weakens existing boundary behavior
  - keep the issue active and record blocker ownership instead of forcing a misleading green report

## Risks & Mitigations
- Risk: the missing boundary case is subtler than the issue description implies.
  - Mitigation: start by encoding the exact non-ancestor external gitdir layout in a focused harness regression.
- Risk: a seemingly small fix could regress existing worktree, symlinked-path, or submodule behavior.
  - Mitigation: keep those regressions in the focused validation loop before broader suite runs.
- Risk: docs-review or review tooling may fail for reasons unrelated to the new packet.
  - Mitigation: keep truthful fallback notes with explicit manifests rather than stalling or inventing a clean result.

## Approvals
- Reviewer: Pending docs-review, implementation validation, and pre-handoff review/elegance gates
- Date: 2026-04-02
