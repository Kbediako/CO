# ACTION_PLAN - Coordinator Symphony-Aligned Standalone Review Startup-Anchor Boundary

1. Confirm from the fresh `1106` review trace and a direct local probe that shell-payload classification already works, so the remaining defect is startup ordering before the first touched-path anchor.
2. Add startup-anchor tracking and a small pre-anchor meta-surface budget in `review-execution-state.ts`.
3. Add diff-mode prompt guidance in `run-review.ts` plus focused regressions covering triggering and non-triggering startup flows.
4. Run the bounded review reliability validation lane and sync task/docs mirrors.
