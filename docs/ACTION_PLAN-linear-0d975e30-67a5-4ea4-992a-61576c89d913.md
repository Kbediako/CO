# ACTION PLAN - CO-524 Review Intent Matrix

## Summary
- Goal: define and implement a structured `review_intent_matrix` so review prompts, review telemetry, and provider-worker handoff decisions share one explicit intent map.
- Scope: docs-first packet, matrix schema/contract, required axes, proposal axis separation, review telemetry semantics, Codex CLI 0.130.0 compatibility, `goals advisory-only` authority boundary, and focused tests.
- Assumptions:
  - parent lane owns live Linear state, workpad, docs-review, implementation, validation, PR lifecycle, and final handoff.
  - provider issue task key remains `linear-0d975e30-67a5-4ea4-992a-61576c89d913` without issue-title, retry, or freshness suffixes.
  - this child lane only creates the docs-first packet and registry mirrors.

## Issue Readiness Gate
- Intent checksum / protected terms carried forward: `review_intent_matrix`, `original spec`, `PRD`, `TECH_SPEC`, `ACTION_PLAN`, `coding standards`, `code change proposals`, `agent loop change proposals`, `review_verdict`, `review_outcome`, `provider-worker handoff`, `Codex CLI 0.130.0`, `goals advisory-only`.
- Not done if:
  - the matrix omits any required axis.
  - `original_spec` is clean without canonical `PRD`, `TECH_SPEC`, and `ACTION_PLAN` evidence.
  - `code change proposals` and `agent loop change proposals` are conflated.
  - `review_outcome` can override `review_verdict`.
  - provider-worker handoff can proceed with findings, unknown, missing, stale, or mismatched semantic review evidence.
  - Codex CLI 0.130.0 compatibility silently passes without valid matrix evidence.
  - `goals advisory-only` becomes lifecycle or review authority.
- Pre-implementation issue-quality review: 2026-05-31 packet review classifies CO-524 as a parity/alignment and exact-terminology lane for review intent, telemetry, proposals, compatibility, and handoff authority. The micro-task path is unavailable.
- Fallback / refactor decision: this task touches compatibility/seam behavior. Remove prose-only review intent inference, expire Codex CLI 0.130.0 missing-matrix compatibility, and retain the durable `goals advisory-only` authority boundary.
- Durable retention evidence: retain only the `goals advisory-only` authority boundary as non-expiring; it is not lifecycle authority.

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Review intent classification | Review intent inferred from ad hoc prompt prose instead of a structured `review_intent_matrix`. | remove fallback | CO-524 | Provider-worker docs-review, implementation-gate, standalone review, or pre-handoff review prompt assembly. | 2026-05-31 | 2026-05-31 | This issue | `review_intent_matrix` axes are generated and validated for `original_spec`, `coding_standards`, `code_change_proposals`, and `agent_loop_change_proposals`; `original_spec` carries canonical `PRD`, `TECH_SPEC`, and `ACTION_PLAN` evidence. | Focused matrix generation, missing-axis, prompt transport, and provider-worker handoff tests. |
| Review telemetry compatibility | Codex CLI 0.130.0 or legacy review evidence can lack current intent matrix fields. | expire fallback | CO-524 | Review evidence comes from Codex CLI 0.130.0-era telemetry or lacks a current matrix binding. | 2026-05-31 | 2026-05-31 | 30 days, expires 2026-06-30 | Current review wrapper evidence always includes valid matrix metadata, or old evidence is explicitly rejected as stale/unsupported. | Codex CLI 0.130.0 fixture tests and fail-closed stale/unsupported evidence tests. |
| Goals context in review prompts | Goal evidence appears near review context and could be mistaken for lifecycle authority. | justify retaining fallback | CO-524 provider-worker handoff | Goals evidence is present in prompt, manifest, workpad, or status context. | 2026-05-31 | 2026-05-31 | non-expiring durable authority contract | A separately approved authority redesign replaces Linear/workpad/review authority and proves goal evidence can become authoritative. | Tests prove `goals advisory-only` cannot satisfy `review_verdict`, `review_outcome`, or provider-worker handoff gates. |

- Contract name: Review intent matrix with Linear-first and review-verdict-first provider-worker handoff.
- Owning surface: review prompt assembly, review telemetry interpretation, and provider-worker handoff checks.
- Steady-state proof: current handoff continues to require semantic review cleanliness; wrapper execution disposition and advisory goals remain context only.
- Tests/docs: this CO-524 packet, focused review intent matrix tests, review telemetry fixture tests, and provider-worker handoff tests.
- Non-expiring rationale: `goals advisory-only` is a durable authority boundary, not temporary fallback debt.
- Large refactor check: no broad review-wrapper replacement is selected; CO-524 adds the smallest structured matrix that removes prose-only intent inference while preserving existing gates.
- Minor seam decision: no retained minor seam; missing or stale matrix evidence fails closed instead of adding another compatibility branch.

## Milestones & Sequencing
1. [x] Create docs-first packet and register the task key.
   - Files: PRD, canonical task spec, TECH_SPEC mirror, ACTION_PLAN, task checklist, `.agent` mirror, `tasks/index.json`, `docs/TASKS.md`, `docs/docs-freshness-registry.json`.
   - Acceptance: protected terms, wrong interpretations, non-goals, `Not Done If`, parity/alignment matrix, fallback/seam decision, and validation plan are explicit.
2. [ ] Parent docs-review before implementation.
   - Evidence key: `linear-0d975e30-67a5-4ea4-992a-61576c89d913-docs-review`.
   - Acceptance: docs-review accepts the packet or parent records and fixes findings.
3. [ ] Implement the `review_intent_matrix` schema and axis generation.
   - Acceptance: matrix contains required axes and canonical original-spec evidence.
4. [ ] Implement proposal axis separation.
   - Acceptance: `code change proposals` and `agent loop change proposals` stay separate in prompt output and handoff evidence.
5. [ ] Implement telemetry/handoff interpretation.
   - Acceptance: `review_verdict` governs semantic cleanliness; `review_outcome` reports wrapper disposition and cannot override findings or unknown verdicts.
6. [ ] Add Codex CLI 0.130.0 and `goals advisory-only` compatibility coverage.
   - Acceptance: old or missing matrix evidence fails closed; advisory goals cannot satisfy review/handoff gates.
7. [ ] Parent runs focused tests, required validation floor, review, PR lifecycle, feedback drain, and Linear handoff.

## Dependencies
- Review prompt assembly and telemetry parsing surfaces.
- Provider-worker handoff checks.
- Existing docs-first packet paths and task registry.
- Codex CLI 0.130.0 fixture or compatibility evidence.
- Existing goal evidence rendering, treated as `goals advisory-only`.

## Validation
- Checks / tests:
  - scoped docs packet checks in this child lane
  - parent-owned docs-review before implementation
  - focused `review_intent_matrix` schema/source-row tests
  - focused missing-axis fail-closed tests
  - focused code and agent-loop proposal separation tests
  - focused telemetry tests for `review_verdict` vs `review_outcome`
  - focused provider-worker handoff blocker tests
  - focused Codex CLI 0.130.0 compatibility tests
  - focused `goals advisory-only` authority-boundary tests
- Rollback plan: remove the matrix integration and keep existing review gates fail-closed; do not weaken provider-worker handoff.

## Risks & Mitigations
- Risk: wrapper execution success is mistaken for semantic review cleanliness.
  - Mitigation: make `review_verdict` the review cleanliness gate and test contradictory telemetry.
- Risk: reviewer proposals are routed to the wrong action surface.
  - Mitigation: keep `code change proposals` and `agent loop change proposals` as separate matrix row classes.
- Risk: legacy Codex CLI 0.130.0 evidence silently passes.
  - Mitigation: fixture-backed compatibility path fails closed when current matrix metadata is missing.
- Risk: goals context is promoted to authority.
  - Mitigation: assert `goals advisory-only` in docs, telemetry, and handoff tests.

## Approvals
- Reviewer: parent lane.
- Date: 2026-05-31.
