# ACTION_PLAN - CO validation: stabilize unrelated CLI/runtime test timeouts that block provider-worker full-suite evidence

## Added by Bootstrap 2026-04-12

## Summary
- Goal: restore truthful provider-worker full-suite validation evidence for CO-130 by reproducing and classifying the named timeout-heavy suites, then taking the smallest evidence-backed outcome.
- Scope: docs-first packet, workpad, docs-review child stream, focused reproductions, exact repo-wide validation evidence, and full pre-handoff validation/review gates.
- Assumptions:
  - the issue’s named suites and rejected interpretations remain the canonical scope
  - the current blocker may resolve either as a shared timeout/process-management seam, a narrower auditable validation-path mechanism, or a no-code closeout if the blocker no longer reproduces
  - provider-worker handoff truthfulness is non-negotiable even if the eventual fix is policy/mechanism rather than direct runtime code

## Issue Readiness Gate
- Intent checksum / protected terms carried forward: `full-suite validation blocker`, `unrelated CLI/runtime Vitest timeouts`, `provider-worker evidence should stay truthful`, `npm run test`, `orchestrator/tests/CodexOrchestratorCli.test.ts`, `orchestrator/tests/LearningCrystalizerCliClient.test.ts`, `orchestrator/tests/CliExecRuntime.test.ts`, `tests/run-review.spec.ts`, `tests/cli-command-surface.spec.ts`, `orchestrator/tests/ProviderLinearWorkerRunner.test.ts`
- Not done if:
  - the named timeout-heavy suites remain unexplained
  - `npm run test` still lacks a trustworthy provider-worker closeout result
  - the final handoff path still collapses unrelated blockers into a false-green full-suite claim
- Pre-implementation issue-quality review:
  - the issue is already narrower than a repo-wide test redesign and already rejects the common shortcuts, so the next step is concrete reproduction evidence rather than more scope expansion

## Milestones & Sequencing
1. Completed: rechecked live issue context, moved the issue from `Ready` to `In Progress`, recorded the required same-turn `stay_serial` / `overlapping_scope` decision, switched onto branch `linear/co-130-provider-worker-validation-timeouts`, and staged the single workpad source.
2. Completed: drafted the docs-first packet, updated registry mirrors, created the single workpad comment, and refreshed it after docs-review plus reproduction milestones.
3. Completed: audited `docs-review` child stream `co-130-docs-review-r2` succeeded with manifest-backed evidence.
4. Completed: reproduced the named suites plus the exact repo-wide `npm run test` path. Current-tree result is green rather than blocked: focused isolated runs passed, plain `npm run test` finished in `135.22s`, and a stage-owned `CODEX_VITEST_PROGRESS=1 npm run test` run finished in `138.78s`.
5. In progress: finish the standard validation floor, standalone review, and explicit elegance review, then prepare the no-code closeout/handoff if those gates stay clean.

## Dependencies
- `package.json`
- Vitest runner/configuration
- `orchestrator/tests/CodexOrchestratorCli.test.ts`
- `orchestrator/tests/LearningCrystalizerCliClient.test.ts`
- `orchestrator/tests/CliExecRuntime.test.ts`
- `tests/run-review.spec.ts`
- `tests/cli-command-surface.spec.ts`
- `orchestrator/tests/ProviderLinearWorkerRunner.test.ts`
- `skills/linear/SKILL.md`

## Validation
- Checks / tests:
  - audited `linear child-stream --pipeline docs-review`
  - focused `npx vitest run ...` reproductions for the named suites
  - repo-wide `npm run test`
  - standard validation floor for any final non-trivial diff
  - standalone review followed by explicit elegance review before handoff
- Rollback plan:
  - revert the chosen timeout/mechanism changes and keep the reproduction evidence if validation shows the remediation widens scope, weakens truthfulness, or fails to improve terminal validation behavior

## Risks & Mitigations
- Risk: the earlier timeout report no longer reproduces and tempts a speculative runtime/test fix anyway.
  - Mitigation: prefer exact current-tree evidence over hypothetical remediation and keep the final diff docs-only unless a live blocker reappears.
- Risk: provider-worker closeout language drifts into vague blocker reporting.
  - Mitigation: keep workpad/handoff wording tied to the exact named suites and explicit terminal-result truth.
- Risk: long-running suites consume time without proving whether they are real stalls.
  - Mitigation: capture exact commands, timings, and terminal outcomes so the result is auditable even when the blocker no longer reproduces.

## Approvals
- Reviewer: Self-approved for docs-first execution; audited docs-review child stream and later standalone/elegance review still required before handoff.
- Date: 2026-04-12
