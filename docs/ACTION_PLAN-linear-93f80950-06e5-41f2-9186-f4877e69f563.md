# ACTION_PLAN - CO-471 build:all strict test typecheck debt

## Summary
- Goal: create the CO-471 docs-first packet and traceability mirrors so `backlog_head_follow_up_traceability_pending` can clear before the issue leaves Backlog.
- Scope: packet docs, canonical task spec, task mirrors, `tasks/index.json`, `docs/TASKS.md`, and `docs/docs-freshness-registry.json`.
- Assumptions:
  - `npm run build` is production-build proof through `tsconfig.build.json`, not release/full-matrix proof.
  - `npm run build:all` remains the release/full-matrix signal through root `tsconfig.json`.
  - Future implementation owns strict test type debt cleanup; this packet lane does not.

## Issue Readiness Gate
- Intent checksum / protected terms carried forward:
  - `npm run build:all`
  - root `tsconfig.json`
  - strict test type debt
  - release/full-matrix signal
  - `tsconfig.build.json`
  - `ProviderIssueHandoff.test.ts`
  - `ProviderLinearWorkerRunner.test.ts`
  - `ControlServerPublicLifecycle.test.ts`
  - `ProviderMergeCloseout.test.ts`
  - `LinearCliShell.test.ts`
  - `build-all-strict-test-typecheck-debt`
  - `backlog_head_follow_up_traceability_pending`
- Not done if:
  - packet registration or mirrors are missing
  - the packet claims build:all cleanup is implemented
  - `npm run build` pass is used as completion proof
  - `build:all` is weakened or deleted
  - root `tsconfig.json` gets blanket exclusions as silent fallback
  - CO-458 or production runtime behavior is absorbed
- Pre-implementation issue-quality review:
  - 2026-05-06: CO-471 is broad release/full-matrix debt and the micro-task path is unavailable.
  - 2026-05-06: The correct immediate objective is traceability only.
  - 2026-05-06: Future implementation must preserve root `tsconfig.json`, `tsconfig.build.json`, and `npm run build:all` boundaries.
- Linear parallelization decision: `stay_serial_packet_only`; this worker owns only packet/registry files, while future implementation can separately decompose named test debt after reproducing `npm run build:all`.
- Fallback / refactor decision: expire the Backlog traceability hold, remove any future fallback that treats `npm run build` as full-matrix proof, and retain `tsconfig.build.json` only as a supported production build contract.

## Milestones & Sequencing
1. Read nearby Backlog traceability packet patterns.
2. Create PRD, TECH_SPEC mirror, ACTION_PLAN, canonical task spec, task checklist, and agent mirror for CO-471.
3. Register `20260506-linear-93f80950-06e5-41f2-9186-f4877e69f563` in `tasks/index.json`.
4. Add a `docs/TASKS.md` snapshot.
5. Add six active rows to `docs/docs-freshness-registry.json`.
6. Record that packet setup clears only `backlog_head_follow_up_traceability_pending`.
7. Run scoped packet validation: JSON parse, protected-term scans, spec guard, docs checks, and freshness where feasible.
8. Leave build:all cleanup, source/test/config work, Linear state, GitHub lifecycle, and issue transitions to future parent-owned work.

## Dependencies
- Linear issue `CO-471` / `93f80950-06e5-41f2-9186-f4877e69f563`.
- `package.json` scripts for `npm run build` and `npm run build:all`.
- Root `tsconfig.json`.
- `tsconfig.build.json`.
- Named strict-test debt files:
  - `orchestrator/tests/ProviderIssueHandoff.test.ts`
  - `orchestrator/tests/ProviderLinearWorkerRunner.test.ts`
  - `orchestrator/tests/ControlServerPublicLifecycle.test.ts`
  - `orchestrator/tests/ProviderMergeCloseout.test.ts`
  - `orchestrator/tests/LinearCliShell.test.ts`

## Validation
- Checks / tests:
  - JSON parse for `tasks/index.json`
  - JSON parse for `docs/docs-freshness-registry.json`
  - targeted scan for `CO-471` and `93f80950-06e5-41f2-9186-f4877e69f563`
  - targeted protected-term scan for all protected terms
  - `node scripts/spec-guard.mjs --dry-run`
  - `npm run docs:check`
  - `npm run docs:freshness`
- Rollback plan:
  - revert only the CO-471 packet files, task index item, docs snapshot, and freshness rows if the packet is rejected.

## Risks & Mitigations
- Risk: packet setup is mistaken for build:all cleanup.
  - Mitigation: every packet surface states implementation remains future work.
- Risk: future work hides the debt by weakening `npm run build:all` or root `tsconfig.json`.
  - Mitigation: protected terms, Not Done If, and fallback table reject silent bypasses.
- Risk: `npm run build` passing is treated as release/full-matrix proof.
  - Mitigation: packet separates `tsconfig.build.json` production build from root strict typecheck.
- Risk: issue drifts into CO-458 or runtime behavior.
  - Mitigation: non-goals reject CO-458 broadening and production runtime behavior changes.

## Fallback Metadata

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Backlog promotion traceability | `backlog_head_follow_up_traceability_pending` while packet files and mirrors are absent | expire fallback | CO-471 | Linear issue has no repo-side traceability packet | 2026-05-06 | 2026-05-06 | until packet PR lands | Packet files, task index entry, `docs/TASKS.md` snapshot, and freshness rows exist on main | JSON parse, protected-term scan, spec guard, docs checks |
| Release/full-matrix build signal | Treating `npm run build` or `tsconfig.build.json` success as enough while `npm run build:all` exposes strict test type debt | remove fallback | CO-471 | Future implementation tries to complete with only production-build proof | 2026-05-06 | 2026-05-06 | 0 days | CO-471 completion requires `npm run build:all` evidence or an explicit reviewed release/full-matrix replacement that is not weaker | `npm run build:all`, focused named-test type debt review, docs/review gates |
| Production build config | `tsconfig.build.json` remains a narrower production-build TypeScript config | justify retaining fallback | CO-471 | Production build needs runtime/package-focused compile coverage | existing contract | 2026-05-06 | non-expiring supported path | Retained only while `npm run build:all` remains the strict release/full-matrix signal | `npm run build`, `npm run build:all` for release/full-matrix lanes, docs checks |

## Approvals
- Reviewer: CO operator / provider-worker parent lane.
- Date: 2026-05-06
