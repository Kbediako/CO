# ACTION_PLAN - CO-471 build:all strict test typecheck debt

## Summary
- Goal: restore `npm run build:all` as a release/full-matrix command that can run green while the broad root `tsconfig.json` strict test type debt remains explicit, owned, and drift-blocking.
- Scope: package build/typecheck scripts, a root strict typecheck baseline guard, checked diagnostic baseline data, release SOP/docs updates, and the CO-471 packet/checklist mirrors.
- Assumptions:
  - `npm run build` remains production-build proof through `tsconfig.build.json`, not release/full-matrix proof by itself.
  - `npm run build:all` remains the release/full-matrix signal.
  - `npm run typecheck:root` is the raw root `tsconfig.json` strict debt reproduction command and may fail until the checked baseline is burned down to zero.
  - `npm run typecheck:root-baseline` must run the full root typecheck and fail closed on diagnostic drift from `tasks/baselines/build-all-strict-typecheck-debt.json`.

## Issue Readiness Gate
- Intent checksum / protected terms carried forward:
  - `npm run build:all`
  - root `tsconfig.json`
  - strict test type debt
  - release/full-matrix signal
  - `tsconfig.build.json`
  - `ProviderIssueHandoff.test.ts`
  - `ProviderLinearWorkerRunner.test.ts`
  - `ControlServerPublicLifecycle.test.ts`
  - `ProviderMergeCloseout.test.ts`
  - `LinearCliShell.test.ts`
  - `build-all-strict-test-typecheck-debt`
  - `backlog_head_follow_up_traceability_pending`
- Not done if:
  - `npm run build:all` still fails from the known strict test type debt
  - `npm run typecheck:root` no longer reproduces the raw root `tsconfig.json` debt
  - `npm run typecheck:root-baseline` accepts new or changed diagnostics without a baseline update
  - `npm run build` pass is used as completion proof
  - `build:all` is weakened or deleted
  - root `tsconfig.json` gets blanket exclusions as silent fallback
  - CO-458 or production runtime behavior is absorbed
- Pre-implementation issue-quality review:
  - 2026-05-06: CO-471 is broad release/full-matrix debt and the micro-task path is unavailable.
  - 2026-05-07: The smallest implementation path is an exact diagnostic baseline guard, not a broad source/test refactor inside one lane.
  - 2026-05-07: The staged guard preserves root `tsconfig.json` as debt authority and keeps all named strict-test surfaces visible.
- Linear parallelization decision: `parallelize_now`; parent owns strategy/package/docs/baseline while same-issue child lane `provider-linear-runner-types` owned only `orchestrator/tests/ProviderLinearWorkerRunner.test.ts` and was accepted after clearing that file's strict diagnostics.
- Fallback / refactor decision: remove the unsafe fallback where a raw `npm run build:all` failure is waived as generic baseline noise; justify retaining `tsconfig.build.json` only as the production-build contract; expire the checked baseline when raw root strict diagnostics reach zero.

## Milestones & Sequencing
1. Read live Linear context and refresh the single workpad.
2. Sweep attached PR #783 feedback before implementation.
3. Record the pre-turn child-lane matrix and parallelization decision.
4. Reproduce current root strict typecheck debt and record dominant clusters.
5. Add `scripts/build-all-strict-typecheck.mjs` to run root `tsconfig.json` with `--noEmit`, parse diagnostics, and compare exact counts plus repo-root-normalized diagnostic identities.
6. Add `tasks/baselines/build-all-strict-typecheck-debt.json` from the current diagnostic set.
7. Update `package.json` so `npm run build:all` runs `npm run build`, `npm run build:patterns`, and `npm run typecheck:root-baseline`; add `npm run typecheck:root` for raw reproduction.
8. Update release SOP and CO-471 packet/checklist mirrors with the command contract.
9. Run required validation, standalone review, elegance review, PR update, and ready-review drain before handoff.

## Dependencies
- Linear issue `CO-471` / `93f80950-06e5-41f2-9186-f4877e69f563`.
- `package.json` scripts for `npm run build`, `npm run build:all`, and the new typecheck commands.
- Root `tsconfig.json`.
- `tsconfig.build.json`.
- Named strict-test debt files:
  - `orchestrator/tests/ProviderIssueHandoff.test.ts`
  - `orchestrator/tests/ProviderLinearWorkerRunner.test.ts`
  - `orchestrator/tests/ControlServerPublicLifecycle.test.ts`
  - `orchestrator/tests/ProviderMergeCloseout.test.ts`
  - `orchestrator/tests/LinearCliShell.test.ts`

## Validation
- Checks / tests:
  - `npm run typecheck:root` reproduces raw root strict debt and records current clusters.
  - `npm run typecheck:root-baseline` passes when diagnostics match the checked baseline.
  - `npm run build:all` passes and therefore proves production build, pattern build, and baseline guard.
  - `npm run build`
  - `npm run lint`
  - `npm run test`
  - `node scripts/spec-guard.mjs --dry-run`
  - `npm run docs:check`
  - `npm run docs:freshness`
  - `npm run repo:stewardship`
  - `node scripts/diff-budget.mjs`
  - manifest-backed standalone review
  - explicit elegance/minimality review
- Rollback plan:
  - revert only the CO-471 package script, baseline guard, baseline JSON, release SOP, and packet/checklist updates if reviewers reject the staged guard.

## Risks & Mitigations
- Risk: the baseline becomes a silent blanket exclusion.
  - Mitigation: the guard runs the full root `tsconfig.json` check and fails on exact diagnostic drift.
- Risk: future work reduces or changes diagnostics without updating the owner evidence.
  - Mitigation: exact per-file and per-code counts fail until the baseline is updated.
- Risk: `npm run build` passing is treated as release/full-matrix proof.
  - Mitigation: release SOP and packet split `npm run build`, `npm run build:all`, `npm run typecheck:root`, and `npm run typecheck:root-baseline`.
- Risk: issue drifts into CO-458 or runtime behavior.
  - Mitigation: non-goals continue to reject CO-458 broadening and production runtime behavior changes.

## Fallback Metadata

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Backlog promotion traceability | `backlog_head_follow_up_traceability_pending` while packet files and mirrors are absent | expire fallback | CO-471 | Linear issue has no repo-side traceability packet | 2026-05-06 | 2026-05-07 | until packet PR lands | Packet files, task index entry, `docs/TASKS.md` snapshot, and freshness rows exist on main | JSON parse, protected-term scan, spec guard, docs checks |
| Release/full-matrix build signal | Treating `npm run build` or `tsconfig.build.json` success as enough while raw `npm run typecheck:root` exposes strict test type debt | remove fallback | CO-471 | Active lanes keep waiving `build:all` as noisy pre-existing debt | 2026-05-06 | 2026-05-07 | 0 days | `npm run build:all` runs production build, pattern build, and exact root diagnostic baseline guard | `npm run build:all`, `npm run typecheck:root-baseline`, docs/review gates |
| Root strict diagnostic baseline | Checked baseline for current `npm run typecheck:root` errors | expire fallback | CO-471 | Root strict debt currently spans broad tests/adapters/evaluation/scripts | 2026-05-07 | 2026-05-07 | until total_errors reaches 0 | Remove baseline once raw root strict typecheck is clean | `npm run typecheck:root`, `npm run typecheck:root-baseline` |
| Production build config | `tsconfig.build.json` remains a narrower production-build config | justify retaining fallback | CO-471 | Production build needs runtime/package-focused compile coverage | existing contract | 2026-05-07 | non-expiring supported path | Retained only while `npm run build:all` also runs the root strict baseline guard | `npm run build`, `npm run build:all` |

## Approvals
- Reviewer: CO operator / provider-worker parent lane.
- Date: 2026-05-07
