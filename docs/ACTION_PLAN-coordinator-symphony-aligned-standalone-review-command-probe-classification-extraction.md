# ACTION_PLAN: Coordinator Symphony-Aligned Standalone Review Command-Probe Classification Extraction

## Objective

Open the next truthful standalone-review follow-on after `1213` by extracting the deterministic shell-probe and heavy-command classifier cluster still reused inside `review-execution-state`.

## Steps

1. Register `1214` docs-first with explicit evidence that the shell-probe/heavy-command classifier cluster remains inline inside `scripts/lib/review-execution-state.ts`.
2. Extract the bounded classifier helper while preserving stateful policy ownership in `scripts/lib/review-execution-state.ts`.
3. Add only the focused regressions needed to pin nested payload probe detection, `grep` option parsing, env-var probe heuristics, and heavy-command behavior.
4. Run the lane validations and record any truthful docs-review or bounded-review overrides.
5. Sync closeout mirrors and reassess whether the next truthful follow-on remains in review execution-state helper support or shifts elsewhere.
