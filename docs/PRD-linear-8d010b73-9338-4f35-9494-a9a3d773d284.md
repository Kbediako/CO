# PRD - CO: follow up recurring control-host provider-worker stdin bootstrap exits after CO-274

## Added by Bootstrap 2026-04-23

## Traceability
- Linear issue: `CO-329` / `8d010b73-9338-4f35-9494-a9a3d773d284`
- Linear URL: https://linear.app/asabeko/issue/CO-329/co-follow-up-recurring-control-host-provider-worker-exits-at-reading
- Canonical owner key: `provider-worker-stdin-bootstrap-failure`
- Canonical owner marker: `codex-orchestrator:canonical-owner-key=provider-worker-stdin-bootstrap-failure`

## Summary
- Problem Statement: control-host-launched `provider-linear-worker` runs can still fail with `stderr | Reading additional input from stdin...` after `CO-274`, and current proof artifacts can classify the failure as generic `provider_runtime` while retry/resumable handling keeps the issue circulating.
- Desired Outcome: the exact stdin bootstrap failure shape has truthful, operator-facing classification and recovery guidance, including whether the worker died before meaningful issue execution or after live work began.

## User Request Translation (Context Anchor)
- User intent / needs (in your own words): re-own the post-`CO-274` recurrence as its own canonical lane, verify current live evidence from `CO-314` and `CO-328`, and restore classification/retry truth without muting provider failures or folding the work into issue-content lanes.
- Success criteria / acceptance:
  - a non-terminal canonical owner exists for the recurrence
  - current live proofs distinguish `CO-328` pre-work bootstrap failure from `CO-314` retry/recovery progress
  - future `Reading additional input from stdin...` provider exits are no longer only generic `provider_runtime`
  - regression coverage proves the classification and retry evidence surfaces
- Constraints / non-goals:
  - do not weaken fail-closed provider behavior
  - do not mute retries as a substitute for classification
  - do not fold this into issue-specific `CO-314` or `CO-328` content work
  - do not broaden into unrelated control-host refresh lifecycle work without fresh proof

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `provider-linear-worker`
  - `control-host`
  - `stderr | Reading additional input from stdin...`
  - `provider_runtime`
  - `provider-linear-worker-proof.json`
  - `manifest.json`
  - `linear_audit.attempted_count`
  - `retry/resumable queue behavior`
  - `codex_exit_1`
- Protected terms / exact artifact and surface names:
  - `provider-worker-stdin-bootstrap-failure`
  - `failure_diagnosis`
  - `diagnostic_category`
  - `retry_error`
  - `owner_status`
  - `end_reason`
- Nearby wrong interpretations to reject:
  - this is already handled because `CO-274` is closed
  - this is stale dashboard projection only
  - this belongs inside issue-specific work for `CO-314` or `CO-328`
  - suppressing retries is equivalent to restoring classification truth

## Parity / Alignment Matrix
- Current truth:
  - `CO-328` proof records `stderr | Reading additional input from stdin...`, `diagnostic_category: provider_runtime`, `end_reason: codex_exit_1`, and `linear_audit.attempted_count: 0`
  - `CO-314` has recovered into active/review work in its latest proof, while stale failed-run summaries still show retry churn
  - the current runner no longer exposes the CO-274-specific stdin bootstrap diagnostic category
- Reference truth:
  - `CO-274` introduced a dedicated stdin bootstrap classification, so operators can identify Codex stdin bootstrap death separately from issue-content and generic runtime faults
  - `classifyProviderWorkerFailureDiagnosis` keeps stronger diagnostics ahead of stdin bootstrap: structured diagnostic categories and guardian signals first, then `cloud_connector_auth_drift`, `cloud_denial`, `auth_mismatch`, `quota_rate_limit`, `env_config`, `provider_stdin_bootstrap`, `provider_runtime`, and `unknown`
- Target truth / intended delta:
  - future stdin bootstrap proofs classify as a dedicated provider stdin bootstrap failure with concrete recovery guidance
  - handoff retry evidence carries the specific diagnosis instead of only `codex_exit_1` or generic `provider_runtime`
  - current proof interpretation distinguishes pre-work bootstrap failure from in-progress recovered work
- Explicitly out-of-scope differences:
  - generic control-host ownership redesign
  - queue retry muting
  - issue-content remediation for `CO-314` or `CO-328`

## Not Done If
- control-host launched provider-worker runs can still hit `Reading additional input from stdin...` with only generic `provider_runtime` truth
- operators cannot tell whether the failing attempt died before meaningful issue execution
- retry/resumable queue behavior still makes this seam look like ordinary issue progress

## Goals
- Restore a dedicated provider stdin bootstrap diagnostic category and guidance.
- Preserve stronger root-cause categories when they coexist with stdin preamble text.
- Surface failure diagnosis in retry/resumable handoff evidence.
- Add deterministic regression coverage for classification and meaningful-work boundary evidence.

## Non-Goals
- Weakening fail-closed provider exits.
- Solving unrelated issue content, Linear state, or refresh lifecycle problems.
- Rewriting the provider worker or control-host retry model.

## Stakeholders
- Product: CO operators triaging control-host throughput
- Engineering: provider worker, control-host, and Linear handoff maintainers
- Design: N/A

## Metrics & Guardrails
- Primary Success Metrics:
  - dedicated stdin bootstrap category appears in new proof artifacts
  - focused tests cover stdout/stderr signal paths and stronger-category preservation
  - retry handoff records include the specific failure diagnosis
- Guardrails / Error Budgets:
  - fail closed on provider errors
  - keep the change bounded to classification, proof, retry evidence, and docs
  - avoid scope expansion into unrelated queue or issue-content work

## User Experience
- Personas:
  - operator watching control-host issue throughput
  - reviewer validating proof artifact and retry evidence semantics
- User Journeys:
  - operator sees a pre-work stdin bootstrap failure called out explicitly
  - operator can separate recovered issue work from stale failed-run retry evidence
  - reviewer verifies exact protected phrases in tests and docs

## Technical Considerations
- Architectural Notes:
  - `providerLinearWorkerRunner.ts` owns proof parsing and `failure_diagnosis`
  - `providerIssueHandoff.ts` owns retry/resumable claim evidence and retry error messages
  - regression tests should not depend on live Linear state; live artifacts are used as context, deterministic fixtures are used as proof
- Dependencies / Integrations:
  - `orchestrator/src/cli/providerLinearWorkerRunner.ts`
  - `orchestrator/src/cli/control/providerIssueHandoff.ts`
  - `orchestrator/tests/ProviderLinearWorkerRunner.test.ts`
  - `orchestrator/tests/ProviderIssueHandoff.test.ts`

## Open Questions
- None blocking. If new evidence shows the stdin bootstrap signature is emitted by a separate control-host seam, file a follow-up instead of broadening this lane.

## Approvals
- Product: Self-approved from Linear issue `CO-329`
- Engineering: Pending docs-review / implementation validation
- Design: N/A
