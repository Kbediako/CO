# ACTION_PLAN: Coordinator Symphony-Aligned Frontend-Test CLI Request Shell Extraction

## Steps

1. Extract the remaining frontend-test request shaping into a dedicated helper under `orchestrator/src/cli/`.
2. Reduce `handleFrontendTest(...)` to parse/help plus thin handoff ownership.
3. Add focused helper parity and preserve existing frontend-test CLI coverage.
4. Run the full lane validation bundle and close the slice with explicit carry-forwards.
