# PRD - CO validation: classify current-main docs:freshness stale-doc baseline after CO-165 rebase

## Added by Bootstrap 2026-04-13

## Traceability
- Linear issue: `CO-166` / `6af47129-33cc-45fa-9a55-e49423536752`
- Linear URL: https://linear.app/asabeko/issue/CO-166/co-validation-classify-current-main-docsfreshness-stale-doc-baseline
- Source issue: `CO-165` / `09012cec-1a78-408c-a7a7-a53e2fce5612`
- Follow-up issue: `CO-167` / `72e9f13e-0899-4769-9ca3-82586077fca9`
- Follow-up packet prefix: `linear-6af47129-33cc-45fa-9a55-e49423536752`
- Current workpad source: `out/linear-6af47129-33cc-45fa-9a55-e49423536752/manual/workpad.md`
- Current local repro artifacts:
  - `out/linear-6af47129-33cc-45fa-9a55-e49423536752/manual/baseline-docs-freshness.log`
  - `out/linear-6af47129-33cc-45fa-9a55-e49423536752/manual/baseline-docs-freshness-report.json`
  - `out/linear-6af47129-33cc-45fa-9a55-e49423536752/manual/00-classification.md`
  - `out/linear-6af47129-33cc-45fa-9a55-e49423536752/manual/01-docs-review-fallback.md`

## Summary
- Problem Statement: after the `CO-165` rebase, the issue snapshot reports that current `origin/main` now fails `npm run docs:freshness` even though the provider-worker validation fixes themselves remain green. The latest observed evidence attached to the issue reports `198` stale docs split across `Task Packet=142`, `Task Mirror=29`, `Report Only=26`, and `Active Guide=1`, while the same workspace was previously green before the rebase.
- Desired Outcome: re-check the live mainline truth in the current workspace, classify the exact stale-doc sources responsible for the drift, and either land the smallest bounded repair or record the exact residual blocker and truthful handling path without reopening `CO-165` runtime/test scope.

## User Request Translation (Context Anchor)
- User intent / needs (in your own words): prove whether the stale-doc failure is a current-main baseline or something local to this branch/workspace, identify the concrete stale packet or registry rows behind it, and restore a truthful `docs:freshness` story that future workers can cite directly.
- Success criteria / acceptance:
  - reproduce `npm run docs:freshness` on the latest relevant mainline and save the exact counts
  - classify the stale set by concrete path and drift source, not just totals
  - either fix the baseline minimally or document the exact residual blocker with evidence-backed next handling
  - keep the lane explicitly separate from `CO-165` provider-worker runtime/test fixes
- Constraints / non-goals:
  - do not change `CO-165` code just to force this lane green
  - do not weaken or silently waive `docs:freshness`
  - do not bulk-edit unrelated task packets before the actual stale source is proven

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `docs:freshness`
  - `current-main stale-doc baseline`
  - `Task Packet`
  - `Task Mirror`
  - `Report Only`
  - `truthful validation story`
- Protected terms / exact artifact and surface names:
  - `npm run docs:freshness`
  - `docs/docs-freshness-registry.json`
  - `tasks/index.json`
  - `docs/TASKS.md`
  - `CO-165`
  - `CO-166`
- Nearby wrong interpretations to reject:
  - this lane should reopen `CO-165` runtime/test fixes
  - the right move is to add a quiet waiver or `--warn`
  - unrelated stale task packets should be bulk-refreshed before the live culprit is classified

## Parity / Alignment Matrix
- Current truth:
  - the issue snapshot reports that current `origin/main` fails `docs:freshness` after the `CO-165` rebase with `198` stale docs
  - this run starts from a clean checkout fast-forwarded to `origin/main` and an issue branch created from that point
  - the live current-workspace repro still needs to be captured in this run before implementation
- Reference truth:
  - the same workspace reportedly had `docs:freshness OK - 3719 docs, 3722 registry entries` before the rebase
  - future provider-worker lanes need a truthful repo-baseline answer they can cite without conflating repo drift with issue-local regressions
- Target truth / intended delta:
  - capture a fresh mainline repro, classify the exact stale sources, and leave the repo in one of two truthful states:
    - `docs:freshness` is green after the smallest bounded repair, or
    - the exact residual blocker is documented with approved handling and evidence
- Explicitly out-of-scope differences:
  - reopening `CO-165` code or runtime/test assertions
  - redesigning freshness policy
  - unrelated large-scale docs cleanup without direct evidence from the repro

## Not Done If
- the lane still cannot say whether the failure is current-main baseline drift or branch-local drift
- the exact stale sources remain unidentified
- the closeout is only a vague waiver with no saved evidence or follow-up path

## Goals
- Reproduce the live `docs:freshness` result on the latest current mainline.
- Classify the stale-doc set by exact path, class, and cadence/age source.
- Keep the lane bounded to truthfulness repair for the baseline surfaced after the `CO-165` rebase.
- Leave artifacts and docs future workers can cite directly.

## Non-Goals
- Reopening `CO-165` provider-worker runtime/test changes.
- Weakening `docs:freshness` policy or converting the failure to a warning.
- Broad unrelated task-packet refresh without repro evidence.

## Stakeholders
- Product: CO operators relying on truthful validation handoff
- Engineering: provider workers and reviewers running `docs:freshness` against current main
- Design: N/A

## Metrics & Guardrails
- Primary Success Metrics:
  - the live repro is saved with exact class totals and stale paths
  - the lane can explain the drift source concretely
  - `docs:freshness` is either restored to green or the remaining blocker is explicitly documented
- Guardrails / Error Budgets:
  - keep the implementation bounded to the proven stale source plus the new `CO-166` packet
  - preserve explicit separation from `CO-165` runtime/test scope
  - prefer a same-project follow-up over scope expansion if a second independent blocker appears

## User Experience
- Personas:
  - provider worker validating a current-main baseline
  - reviewer auditing whether a red docs gate is baseline drift or issue-local regression
  - maintainer tracing why post-rebase validation truth changed
- User Journeys:
  - rerun `npm run docs:freshness` on the branch and see a truthful baseline result
  - inspect the saved report and identify the exact stale-doc cohort
  - cite a bounded fix or blocker note without reopening `CO-165`

## Technical Considerations
- Architectural Notes:
  - the lane is first an evidence-classification problem, then a bounded metadata repair only if the repro confirms a live stale cohort
  - `scripts/docs-freshness.mjs` already emits class-separated failures and exact stale entry metadata; this lane should rely on that output instead of inventing a parallel classifier
  - registry/task/doc packet surfaces must stay aligned if a fix is required
- Dependencies / Integrations:
  - `scripts/docs-freshness.mjs`
  - `docs/docs-freshness-registry.json`
  - `tasks/index.json`
  - `docs/TASKS.md`
  - audited `linear child-stream --pipeline docs-review`

## Execution Update 2026-04-13
- The live repro confirmed the issue snapshot exactly on a clean workspace fast-forwarded to `origin/main`: `docs:freshness FAILED - 3725 docs, 3728 registry entries`, `stale docs: 198`, `Task Packet=142`, `Task Mirror=29`, `Report Only=26`, `Active Guide=1`.
- The stale baseline is split across two cohorts:
  - March 13 / 30-day cohort: `182` stale docs across packet ids `1138` through `1163`, with seven stale surfaces per packet (`.agent/task`, `docs/findings`, `docs/ACTION_PLAN-*`, `docs/PRD-*`, `docs/TECH_SPEC-*`, `tasks/specs/*`, `tasks/tasks-*`).
  - January 12 / 90-day cohort: `16` stale docs across `.agent/SOPs/release.md` and the release-plan packets `0942`, `0943`, and `0944`.
- The audited `linear child-stream --pipeline docs-review` path failed closed on `provider_worker_child_stream_provenance_invalid` because the parent provider-worker manifest lacks control-host provenance. The truthful direct fallback first failed on a packet-local `docs/TASKS.md` line-budget overflow (`451/450`), `npm run docs:archive-tasks` repaired that local defect, and the rerun then passed `spec-guard` plus `docs:check` and failed only on `docs:freshness`.
- Because the actual repair would require truthfully refreshing `198` stale docs across two cohorts, `CO-166` now closes as a classification and validation-story lane rather than silently bulk-refreshing the stale rows. Follow-up `CO-167` owns the real stale-cohort refresh.

## Open Questions
- Whether the docs-only `CO-166` packet should be handed off through the normal PR path or treated as a local investigation packet only, now that the substantive repair has been split into `CO-167`.

## Approvals
- Product: pending
- Engineering: live repro captured, docs-review fallback recorded, and `CO-167` created for the actual stale-cohort refresh
- Design: N/A
