# ACTION_PLAN: Coordinator Symphony-Aligned Flow CLI Request Shell Extraction

## Steps

1. Extract the remaining `flow` request-shaping logic from `handleFlow(...)` into a dedicated CLI helper.
2. Add focused unit coverage for the new helper and keep existing `flow` command-surface parity green.
3. Run the required validation bundle, close the slice honestly, and register the next truthful follow-on lane.
