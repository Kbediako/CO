---
id: 20260425-linear-16623322-4b8d-4e12-8f57-688aa5f71a23
title: CO-363 CO STATUS active issue projection and current-host-unhealthy classification
relates_to: docs/PRD-linear-16623322-4b8d-4e12-8f57-688aa5f71a23.md
risk: high
owners:
  - Codex
last_review: 2026-04-25
---

# TECH_SPEC - CO-363 CO STATUS active issue projection and current-host-unhealthy classification

## Canonical Reference
- Canonical task spec: `tasks/specs/linear-16623322-4b8d-4e12-8f57-688aa5f71a23.md`
- PRD: `docs/PRD-linear-16623322-4b8d-4e12-8f57-688aa5f71a23.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-16623322-4b8d-4e12-8f57-688aa5f71a23.md`
- Task checklist: `tasks/tasks-linear-16623322-4b8d-4e12-8f57-688aa5f71a23.md`
- Agent-task mirror: `.agent/task/linear-16623322-4b8d-4e12-8f57-688aa5f71a23.md`

## Traceability
- Linear issue: `CO-363` / `16623322-4b8d-4e12-8f57-688aa5f71a23`
- Source anchor: `ctx:sha256:c98bd9a7b2ae5c5d558c5f43b9145ac007b6cd61e7aafe81a749db4eac4429e0#chunk:c000001`
- Docs packet child lane manifest: `.runs/linear-16623322-4b8d-4e12-8f57-688aa5f71a23-docs-packet/cli/2026-04-25T01-30-33-657Z-a2ee411b/manifest.json`
- Shared source note: the child checkout has no `.runs` tree, so this packet is anchored on the parent handoff and protected terms only.

## Summary
- Objective: give the parent lane a bounded contract for aligning `CO STATUS`, `co-status --format json`, and `/ui/data.json` active issue projection while separately surfacing `current-host-unhealthy classification`.
- Scope:
  - docs-first packet and registry/checklist mirrors for `CO-363`
  - active issue projection, active identifiers and counts, reopened issue metadata, and current-host health classification
  - protected related identifiers `CO-345`, `CO-356`, and `CO-330`
- Constraints:
  - child lane remains docs-only
  - parent owns implementation, focused tests, docs-review, validation, Linear/workpad reconciliation, PR lifecycle, and merge
  - no broad dashboard redesign, host restart redesign, or destructive intake-state cleanup

## Issue-Shaping Contract
- User-request translation carried forward: preserve the status/control-host contract named by the parent handoff. `CO STATUS`, `control-host`, `co-status --format json`, `/ui/data.json`, `control_endpoint.json`, and `provider-intake-state.json` must align around active issue projection, active identifiers/counts, reopened issue metadata, and explicit `current-host-unhealthy classification`.
- Protected terms / exact artifact and surface names:
  - `CO STATUS`
  - `control-host`
  - `co-status --format json`
  - `/ui/data.json`
  - `control_endpoint.json`
  - `provider-intake-state.json`
  - `active issue projection`
  - `active identifiers and counts`
  - `CO-345`
  - `CO-356`
  - `CO-330`
  - `reopened issue metadata`
  - `current-host-unhealthy classification`
- Nearby wrong interpretations to reject:
  - cosmetic `CO STATUS` renderer-only work
  - host-health failures as a reason to drop active issue identifiers/counts
  - stale provider-intake history as enough to count a terminal issue active
  - destructive cleanup of local state as the product fix
  - folding CO-363 into CO-345, CO-356, or CO-330
  - Linear/workpad/PR mutations from this child lane
- Explicit non-goals carried forward:
  - no code/test edits in this child lane
  - no full repo validation from this child lane
  - no direct implementation of CO-345, CO-356, or CO-330
  - no broad `control-host` restart or endpoint-rotation redesign

## Parity / Alignment Matrix
- Current truth:
  - `co-status --format json` reaches the local status dataset through `control_endpoint.json` and `/ui/data.json`
  - `/ui/data.json` is the control-host dataset consumed by status surfaces
  - `provider-intake-state.json` can retain active and historical provider claims
  - active issue identifiers/counts can be misread when current host health is unhealthy or when reopened metadata is flattened
- Reference truth:
  - host-health classification and issue-lifecycle projection are related but not identical
  - `current-host-unhealthy classification` should be explicit and machine-checkable
  - active issue projection should preserve active identifiers/counts and reopened issue metadata
  - retained intake state should be audit evidence, not a destructive cleanup target
- Target truth / intended delta:
  - `CO STATUS`, `co-status --format json`, and `/ui/data.json` agree on active identifiers/counts for the same snapshot
  - current host health is surfaced separately as `current-host-unhealthy classification`
  - reopened metadata for `CO-345`, `CO-356`, and `CO-330` remains visible enough to distinguish live reopened work from stale terminal history
  - stale provider-intake rows do not inflate active projection without live/reopened corroboration
- Explicitly out-of-scope differences:
  - terminal UI redesign
  - Linear mutation or issue-state repair
  - CO-345, CO-356, or CO-330 implementation
  - destructive cleanup of `provider-intake-state.json`, `control_endpoint.json`, or `.runs` artifacts

## Readiness Gate
- Not done if:
  - `CO STATUS`, `co-status --format json`, or `/ui/data.json` disagree on active identifiers or counts
  - `current-host-unhealthy classification` is missing, renamed, or conflated with active issue lifecycle
  - reopened issue metadata for `CO-345`, `CO-356`, or `CO-330` is absent from the issue-projection contract
  - stale `provider-intake-state.json` history alone can create active rows
  - implementation deletes retained local artifacts instead of fixing projection semantics
- Pre-implementation issue-quality review evidence:
  - 2026-04-25: self-review approves the issue shape for docs-only packet drafting because the handoff supplies exact protected surfaces, issue identifiers, and acceptance boundaries.
  - 2026-04-25: the micro-task path is ineligible because correctness depends on exact naming, active/reference/target parity, reopened metadata, and host-health versus issue-lifecycle separation.
- Safeguard ownership split:
  - child lane owns only the packet files and registry/checklist mirrors
  - parent lane owns source/test inspection, implementation, docs-review, focused validation, Linear/workpad reconciliation, PR lifecycle, and merge

## Technical Requirements
1. Create the CO-363 docs-first packet and registry/checklist mirrors.
2. Preserve every protected term from the issue-shaping contract.
3. Define active issue projection as a shared contract across `CO STATUS`, `co-status --format json`, and `/ui/data.json`.
4. Preserve `active identifiers and counts` as machine-checkable status truth.
5. Preserve `reopened issue metadata` for `CO-345`, `CO-356`, and `CO-330`.
6. Surface `current-host-unhealthy classification` separately from issue lifecycle state.
7. Keep `provider-intake-state.json` as retained evidence and prevent stale history from inflating active counts without corroboration.
8. Leave implementation, test, validation, review, Linear/workpad, and PR work parent-owned.

## Architecture & Data
- Architecture / design adjustments:
  - likely parent seams include `coStatusCliShell.ts`, `coStatusAttachCliShell.ts`, `uiDataController.ts`, `observabilityReadModel.ts`, `observabilitySurface.ts`, `selectedRunProjection.ts`, and `providerIssueHandoff.ts`
  - host-health classification should be additive to the status dataset, not a replacement for issue projection
  - active issue projection should be computed once or reconciled through one shared helper so terminal and JSON surfaces cannot drift
- Data model / artifact constraints:
  - retain `provider-intake-state.json` as audit evidence
  - retain `control_endpoint.json` as endpoint metadata, while classifying current-host unhealthy conditions separately
  - represent reopened metadata in a way that can be checked in focused tests and read by operators
- External dependencies / integrations:
  - parent-owned live issue/context verification for `CO-345`, `CO-356`, and `CO-330`
  - local control-host status artifacts
  - existing observability/read-model tests

## Validation Plan
- Child-lane checks:
  - protected-term scan over the declared packet/checklist files
  - JSON parse check for `tasks/index.json`
  - `git diff --check --` scoped to the touched files
  - no full repo validation suites
- Parent-lane checks:
  - focused status/read-model tests proving `CO STATUS`, `co-status --format json`, and `/ui/data.json` active identifiers/counts agree
  - focused fixture for `current-host-unhealthy classification` that does not erase active issue projection
  - focused reopened-metadata fixture for `CO-345`, `CO-356`, and `CO-330`
  - regression proving stale `provider-intake-state.json` terminal history does not create active rows without live or reopened corroboration
  - parent docs-review before implementation acceptance
- Rollout verification:
  - parent records the exact implementation seams, focused tests, and status payload examples in the task checklist/workpad before review handoff
- Monitoring / alerts:
  - no new monitoring from this child lane

## Open Questions
- Which exact status payload field should carry `current-host-unhealthy classification`?
- Should reopened metadata live on each active issue row, on a projection-level summary, or both?
- What live issue-context evidence should the parent require before treating `CO-345`, `CO-356`, or `CO-330` as active/reopened in fixtures?

## Approvals
- Reviewer: docs child lane self-review for packet shape and issue boundaries.
- Date: 2026-04-25
