# PRD - Doctor validation: classify and restore current Doctor readiness stability for CO-220 handoff

## Added by Provider Worker Parent Lane 2026-04-18

## Traceability
- Linear issue: `CO-231` / `91749283-6dc8-4df8-aee3-5c9127c1200c`
- Packet prefix: `linear-91749283-6dc8-4df8-aee3-5c9127c1200c`
- Task id: `20260418-linear-91749283-6dc8-4df8-aee3-5c9127c1200c`
- Source anchor: `ctx:sha256:2e68cd2fda454db25cb1007f4066d1660e095ddaaf609630cabcc4697c11670e#chunk:c000001`
- Parent-provided source payload reference: `.runs/linear-91749283-6dc8-4df8-aee3-5c9127c1200c/cli/2026-04-18T02-10-26-787Z-56ab47e0/memory/source-0/source.txt`
- Initial worker manifest: `.runs/linear-91749283-6dc8-4df8-aee3-5c9127c1200c/cli/2026-04-18T02-10-26-787Z-56ab47e0/manifest.json`
- Successful child-lane evidence: `.runs/linear-91749283-6dc8-4df8-aee3-5c9127c1200c-classification-note/cli/2026-04-18T02-34-58-947Z-42530f5e/manifest.json`
- Advisory classification artifact: `out/linear-91749283-6dc8-4df8-aee3-5c9127c1200c/manual/20260418T023500Z-child-lane-doctor-classification.md`
- Current workpad artifact: `out/linear-91749283-6dc8-4df8-aee3-5c9127c1200c/manual/workpad-latest.md`
- Traceability note: two same-issue docs child lanes stalled under the appserver path and one CLI-fallback docs lane drifted into unrelated packet content, so the authoritative docs-first packet was completed in the parent workspace after the successful `classification-note` lane preserved the comparison buckets.

## Summary
- Problem Statement: `CO-220` finished the current-truth projection and stale-attachment repair lane, but its handoff validation exposed a separate `Doctor.test` stability problem. Preserved evidence showed a clean `origin/main` isolated failure on `reports direct-dist delegation readiness and initialize latency`, while the `CO-220` branch carried additional branch-only `Doctor.test` failures in both the isolated rerun and the full-suite path.
- Desired Outcome: preserve the exact `current branch` vs `origin/main` vs `CO-220` split, land the smallest truthful `Doctor.test` fix that restores current-head stability in both `isolated rerun` and `full-suite` validation, and keep the issue strictly separate from `CO-220` stale-attachment logic.
- Current Target-Head Result: after the narrow fixture repair in `orchestrator/tests/Doctor.test.ts` and the current `origin/main` merge, `npm run build` passes, `npx vitest run --config vitest.config.core.ts orchestrator/tests/Doctor.test.ts` passes `47/47`, and `npm test` passes `345` files / `4311` tests with `Doctor.test` green inside the full-suite run.

## User Request Translation
- Complete `CO-231` only; do not reopen `CO-220` stale `Rework` / stale attachment projection logic.
- Preserve the exact `Doctor.test`, `full-suite`, `isolated rerun`, `provider readiness`, and `direct-dist delegation readiness and initialize latency` surfaces and classify the current branch vs clean-main split truthfully.
- Restore Doctor readiness stability without weakening the readiness truth, hiding failures, deleting tests, or muting red outcomes.
- Keep any explanation of the result machine-checkable and auditable from manifests, commands, and saved artifacts.

## Intent Checksum
- Protected terms and surfaces:
  - `Doctor.test`
  - `full-suite`
  - `isolated rerun`
  - `provider readiness`
  - `direct-dist delegation readiness and initialize latency`
  - `keeps Telegram incomplete when transport is allowed but mutations are disabled`
  - `falls back to legacy collab feature key when canonical key is absent`
  - `current branch`
  - `origin/main`
  - `CO-220`
- Nearby wrong interpretations to reject:
  - reopen `CO-220` stale `Rework` / stale attachment projection logic
  - weaken validation by ignoring, muting, or hiding red `Doctor.test` outcomes
  - solve this by deleting failing tests or changing expected truth without root-cause evidence
  - collapse the problem into generic `full-suite` flakiness without preserving the exact current branch vs clean-main evidence split

## Parity / Alignment Matrix

| Surface | Current / Reference Truth | Target Truth |
| --- | --- | --- |
| Current target head `Doctor.test` `isolated rerun` | After the current fix, `npx vitest run --config vitest.config.core.ts orchestrator/tests/Doctor.test.ts` passes `47/47`. | Keep the current target head green on the isolated Doctor surface. |
| Current target head `full-suite` | After the current fix and current `origin/main` merge, `npm test` passes `345` files / `4311` tests and includes `orchestrator/tests/Doctor.test.ts` green inside the suite. | Keep the review-handoff validation path green on the Doctor surface. |
| Preserved clean `origin/main` comparison | The issue packet and bounded classification note preserve a clean-main isolated failure on `reports direct-dist delegation readiness and initialize latency`; the preserved clean-main `full-suite` bucket was non-Doctor. | Keep the baseline-only `origin/main` Doctor failure distinct from branch-only `CO-220` failures and from the repaired current head. |
| Preserved `CO-220` current branch `isolated rerun` | The `CO-220` branch preserved branch-only failures `keeps Telegram incomplete when transport is allowed but mutations are disabled` and `falls back to legacy collab feature key when canonical key is absent`. | Keep these branch-only failures separate from clean-main debt and from the repaired current head. |
| Preserved `CO-220` current branch `full-suite` | The `CO-220` branch preserved branch-only `Doctor.test` failures on `reports devtools readiness when config and skill exist`, `reports direct-dist delegation readiness and initialize latency`, `keeps overall doctor status at warning when providers are incomplete`, `reports provider readiness when the repo is seeded and env is configured`, and `resolves provider readiness from the repo root when doctor runs in a nested directory`. | Keep the full-suite Doctor failure cluster traceable to `CO-220` and out of the repaired current head. |
| Issue ownership boundary | `CO-231` owns Doctor validation stability for the handoff floor; `CO-220` owns stale-attachment / truth-projection logic. | Preserve the issue split and do not claim `CO-220` is solved by this lane. |

## Acceptance Criteria
1. Reproduce and record the exact current-branch vs clean-main failure split for `Doctor.test` and the surrounding `full-suite` validation floor with machine-checkable artifacts.
2. Classify each preserved Doctor case as baseline-only, branch-only, shared, or non-repro on the current target head.
3. Land the smallest truthful fix or approved validation contract that restores stable Doctor validation semantics without weakening the underlying readiness truth.
4. The chosen target head no longer fails the named Doctor cases in both isolated Doctor reruns and the repo validation path used for review handoff, or the packet records an explicit approved fallback with exact residual risk.

## Non-Goals
- No return to `co-status`, intake projection, `issue-context`, or stale PR attachment truth logic from `CO-220`.
- No change to live provider/readiness semantics unless a Doctor expectation is proven wrong.
- No hiding of failing tests behind retries, blanket timeout inflation, or skipped validation.
- No reclassification of unrelated clean-main suites as if they were solved by this lane without fresh evidence.

## Not Done If
- The current target head still fails `Doctor.test` in either isolated or full-suite validation and the exact failing cases are not classified.
- Clean `origin/main` and current-head Doctor outcomes are still conflated without explicit comparison evidence.
- The lane changes only one execution mode while the other remains red on the same Doctor surface.
- The packet implies `CO-220` is closed by this work rather than keeping the Doctor validation debt separate and traceable.

## Guardrails
- Keep the fix bounded to the Doctor validation seam.
- Preserve the exact issue wording and comparison buckets in the docs packet, workpad, and validation evidence.
- Treat child-lane output as advisory only unless it produces a valid scoped patch or manifest-backed proof the parent can accept.

## User Experience
- Reviewers should be able to see exactly why `CO-231` exists separately from `CO-220`.
- The Doctor validation floor should be green on the current target head without requiring local build state knowledge outside the test itself.
- Future handoff lanes should be able to audit the clean-main, `CO-220`, and repaired-current-head buckets from saved artifacts rather than re-deriving them from narrative only.
