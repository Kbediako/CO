# ACTION_PLAN - Coordinator Symphony-Aligned Bootstrap Metadata Persistence Extraction

## Steps

1. Register `1080` docs-first from the `1079` next-slice note and the bounded lifecycle-scout recommendation.
2. Extract the bootstrap metadata persistence block into one control-local helper near `controlServerBootstrapLifecycle.ts`.
3. Keep lifecycle ordering and bridge attach behavior unchanged while delegating only the persistence phase.
4. Add focused regressions for payload persistence, chmod hardening, and startup ordering.
5. Run the lane validation bundle, sync mirrors, and queue the next bounded Symphony-aligned seam.
