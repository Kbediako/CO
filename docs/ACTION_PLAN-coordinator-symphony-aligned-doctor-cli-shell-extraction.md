# ACTION_PLAN: Coordinator Symphony-Aligned Doctor CLI Shell Extraction

1. Extract the inline doctor command shell into a dedicated module while keeping parse/flag normalization in the top-level CLI wrapper.
2. Preserve doctor command-surface behavior across advisory output, JSON mode, and `--apply`.
3. Add or move focused parity coverage if the seam needs direct helper tests.
4. Run the required validation/review lane and close the task with explicit overrides if unrelated repo issues persist.
