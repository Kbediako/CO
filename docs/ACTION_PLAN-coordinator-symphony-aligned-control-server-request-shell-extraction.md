# ACTION_PLAN - Coordinator Symphony-Aligned Control Server Request Shell Extraction

## Steps

1. Register `1085` docs-first from the `1084` next-slice note plus bounded read-only seam inspection.
2. Extract the inline `http.createServer(...)` request shell from `ControlServer.start()` into one control-local helper.
3. Keep `handleRequest(...)`, seeded runtime assembly, bootstrap assembly, startup sequencing, and shutdown ordering unchanged.
4. Add focused regressions for pre-instance `503`, live request-context assembly, and top-level JSON error mapping.
5. Run the validation bundle, sync mirrors, and queue the next bounded Symphony-aligned seam.
