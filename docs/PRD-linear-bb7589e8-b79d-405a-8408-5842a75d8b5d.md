# PRD - CO-584 docs freshness registry lifecycle integrity hard block

## Added by Bootstrap 2026-05-25

## Summary
- Problem Statement: `docs:freshness:maintain --check` currently stops at `block_missing_or_invalid_registry` because 35 registry rows express contradictory lifecycle state: `status: archived` and `lifecycle_state: active`. Those rows came from the CO-580 lifecycle resolver migration path that tried to preserve terminal source evidence while linked local checklist obligations remain open.
- Desired Outcome: Repair the registry integrity hard block without weakening `docs:freshness`, without blind `last_review` bumps, and without broadening exact owner routes. After repair, invalid registry entries are zero and any remaining cohort/capacity/pre-expiry debt remains visible as the next explicit blocker.

## User Request Translation (Context Anchor)
- User intent / needs (in your own words): The user wants recurring docs-freshness failures fixed at the root cause, not patched over. This issue addresses the malformed lifecycle data that prevents trustworthy owner routing and capacity decisions.
- Success criteria / acceptance: Current archived/active contradictions are repaired into consistent lifecycle states, validation rejects any recurrence, and exact owner precedence for CO-579, CO-581, and CO-569 remains intact.
- Constraints / non-goals: Do not delete historical docs to pass gates, do not raise cohort caps, do not suppress `block_missing_or_invalid_registry`, do not turn CO-579 into a bucket for all docs-freshness debt, and do not broaden CO-569 or CO-581 beyond their exact cohorts.

## Intent Checksum
- Exact user wording / phrases to preserve: "we shouldn't just patch then over, we should dive deep get to the root cause and fix it"; "never face docs freshness issues again"; "issues are labelled properly"; "related issues are linked".
- Protected terms / exact artifact and surface names: `docs:freshness`, `docs:freshness:maintain`, `block_missing_or_invalid_registry`, `docs/docs-freshness-registry.json`, `status: archived`, `lifecycle_state: active`, `terminal_pending_archive`, `preserved_historical_stub`, `canonical_owner_key`, `CO-579`, `CO-580`, `CO-581`, `CO-569`, `CO-429`.
- Nearby wrong interpretations to reject: This is not a date-bump task, not a historical packet deletion task, not a new owner-swap task, not a cap expansion, not a reason to weaken docs or spec gates, and not a reason to hide remaining capacity or pre-expiry debt after registry integrity is fixed.

## Parity / Alignment Matrix
- Current truth: `docs:freshness:maintain --check` reports `block_missing_or_invalid_registry`, 35 invalid registry entries, `current_entries=105`, `current_cohorts=5`, `max_cohorts=2`, and active owners CO-569, CO-579, and CO-581.
- Reference truth: A registry row must express one effective lifecycle state consistently. Archived rows are terminal/local-archive-ready. Active rows can preserve terminal source evidence only through explicit active lifecycle fields, not by overloading `status: archived`.
- Target truth / intended delta: Invalid archived/active rows are converted to a consistent active or terminal-pending representation with audit evidence preserved. Validation prevents new contradictory rows. Capacity and owner routing still run after integrity repair and may surface follow-on blockers.
- Explicitly out-of-scope differences: The May 19 and May 20 rolling cohorts remain CO-581 and CO-569 responsibilities. CO-429 residue and 2026-06-01 pre-expiry work are follow-on routes unless this registry integrity fix directly exposes them.

## Not Done If
- `docs:freshness:maintain --check` can still report `block_missing_or_invalid_registry` for the archived/active contradiction.
- Any fix changes `last_review` without real review evidence.
- Capacity over-budget or pre-expiry diagnostics disappear instead of moving to the next blocker after registry repair.
- CO-569 or CO-581 receives debt outside its exact canonical cohort key.
- A future migration/finalizer can emit `status: archived` with `lifecycle_state: active` without a failing test.

## Goals
- Preserve audit lineage while repairing malformed lifecycle state in `docs/docs-freshness-registry.json`.
- Add focused regression coverage for registry validation and maintenance decision ordering.
- Keep owner routing deterministic and exact-key first.
- Leave downstream docs-freshness blockers visible for separate owner lanes.

## Non-Goals
- No broad historical packet deletion.
- No blind date bumps.
- No weakening `docs:freshness`, `docs:freshness:maintain`, `docs:check`, or `spec-guard`.
- No cohort cap increase.
- No control-host status-plane implementation; CO-583 owns that route.

## Stakeholders
- Product: CO operators relying on docs freshness gates as truthful routing signals.
- Engineering: Codex orchestrator maintainers and provider-worker implementers.
- Design: Not applicable.

## Metrics & Guardrails
- Primary Success Metrics: Invalid registry entry count for this contradiction is zero; focused tests fail before and pass after the fix; `docs:freshness:maintain --check` advances beyond registry integrity to the next truthful blocker or pass state.
- Guardrails / Error Budgets: No more than one active implementation issue is consumed for this lane; no extra subagents remain running after bounded research; no Linear labels or related issue links are omitted.

## User Experience
- Personas: CO operator watching issue queue, PR review drain, and docs-freshness gate truth.
- User Journeys: Operator sees CO-584 as the exact registry integrity owner. Once fixed, the next docs-freshness blocker is actionable rather than hidden behind malformed registry state.

## Technical Considerations
- Architectural Notes: Treat lifecycle status as a typed invariant. `status: archived` means archived/local terminal readiness; local active obligations must use active lifecycle state and explicit terminal source evidence fields or notes.
- Dependencies / Integrations: `scripts/docs-freshness.mjs`, `scripts/docs-freshness-maintain.mjs`, `scripts/lib/docs-freshness-lifecycle.js`, `tests/docs-freshness.spec.ts`, `tests/docs-freshness-maintain.spec.ts`, and `docs/docs-freshness-registry.json`.

## Fallback / Refactor Decision
- Applies to fallback, compatibility, legacy, stale, cached, break-glass, or minor-seam behavior? Yes.
- Decision: remove the stale/legacy lifecycle encoding that allowed archived registry rows to remain active via notes or migration residue.
- Large-refactor check: CO-580 already introduced the broader lifecycle resolver boundary. CO-584 is the focused integrity repair and regression layer on that boundary, not another independent seam.

## Open Questions
- None blocking. Remaining capacity and pre-expiry routes should become separate issues only after registry integrity no longer masks them.

## Approvals
- Product: Codex operator on 2026-05-25.
- Engineering: Pending implementation review.
- Design: Not applicable.
