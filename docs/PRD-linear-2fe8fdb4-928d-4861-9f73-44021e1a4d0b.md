# PRD - CO-551 operator-confirmed quota-hygiene stale-process remediation

## Immediate Traceability
- Linear issue: `CO-551` / `2fe8fdb4-928d-4861-9f73-44021e1a4d0b`
- Linear URL: https://linear.app/asabeko/issue/CO-551
- Task id: `linear-2fe8fdb4-928d-4861-9f73-44021e1a4d0b`
- Canonical task spec: `tasks/specs/linear-2fe8fdb4-928d-4861-9f73-44021e1a4d0b.md`
- TECH_SPEC mirror: `docs/TECH_SPEC-linear-2fe8fdb4-928d-4861-9f73-44021e1a4d0b.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-2fe8fdb4-928d-4861-9f73-44021e1a4d0b.md`
- Task checklist: `tasks/tasks-linear-2fe8fdb4-928d-4861-9f73-44021e1a4d0b.md`
- `.agent` mirror: `.agent/task/linear-2fe8fdb4-928d-4861-9f73-44021e1a4d0b.md`
- Canonical owner key: `quota-hygiene:operator-confirmed-stale-process-remediation:v1`
- Canonical owner marker: `codex-orchestrator:canonical-owner-key=quota-hygiene:operator-confirmed-stale-process-remediation:v1`
- Source anchor: `ctx:sha256:7d0b516fef794941c1aec9537d1ecb0a177327a9c0ce84e01c46d2d1efa423e4#chunk:c000001`
- Source object id: `sha256:7d0b516fef794941c1aec9537d1ecb0a177327a9c0ce84e01c46d2d1efa423e4`
- Parent manifest pointer: `.runs/linear-2fe8fdb4-928d-4861-9f73-44021e1a4d0b-docs-packet/cli/2026-05-21T22-26-14-450Z-12568c4c/manifest.json`
- Source payload pointer: `.runs/linear-2fe8fdb4-928d-4861-9f73-44021e1a4d0b-docs-packet/cli/2026-05-21T22-26-14-450Z-12568c4c/memory/source-0/source.txt`

## Summary
- Problem Statement: CO-542 gives operators a zero-model quota-hygiene detector/reporting surface, but an operator-confirmed path is still needed for stale local process remediation. Operators need a governed way to act on current-audit PID evidence without broad process matching, automatic kills, restarts, model calls, provider-intake mutation, or app-server/control-host restarts.
- Desired Outcome: define the CO-551 docs-first packet for remediation only. Parent implementation should require explicit operator confirmation, accept only scoped targets that are eligible in the current quota-hygiene audit by PID, emit durable per-PID audit output, and preserve CO-542 as detection/reporting only.

## User Request Translation
- User intent / needs: create a bounded remediation contract for stale quota-hygiene process cleanup after an operator has inspected the current audit and explicitly confirmed the exact PID targets.
- Success criteria / acceptance:
  - canonical owner key `quota-hygiene:operator-confirmed-stale-process-remediation:v1` is preserved in packet and task mirrors
  - current detector owner CO-542 remains detection/reporting only
  - remediation is separate from detection and requires explicit operator confirmation
  - only scoped targets from the current audit can be considered
  - current-audit PID eligibility is required before any remediation action
  - durable per-PID audit output records eligibility, confirmation, attempted action, result, and refusal reason when skipped
  - no automatic kill/restart, no app-server/control-host restarts, no provider-intake mutation, no model calls, and no kill-by-name behavior
- Constraints / non-goals:
  - this child lane edits docs-first packet and task mirror files only
  - no implementation or test files
  - no Linear mutation helpers, workpad updates, PR lifecycle work, model-backed review, or full repo validation
  - no weakening or widening of CO-542 detector semantics

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `quota-hygiene:operator-confirmed-stale-process-remediation:v1`
  - `current detector owner CO-542 remains detection/reporting only`
  - `remediation only`
  - `explicit operator confirmation`
  - `scoped targets`
  - `current-audit PID eligibility`
  - `durable per-PID audit output`
  - `no automatic kill/restart`
  - `no app-server/control-host restarts`
  - `no provider-intake mutation`
  - `no model calls`
  - `no kill-by-name behavior`
- Protected terms / exact artifact and surface names:
  - `CO-551`
  - `CO-542`
  - `quota-hygiene`
  - `codex-orchestrator hygiene quota`
  - `provider-intake-state.json`
  - `app-server`
  - `control-host`
  - `PID`
  - `per-PID audit output`
  - `operator confirmation`
- Nearby wrong interpretations to reject:
  - turning CO-542 into an automatic cleanup tool
  - killing processes by name, command substring, owner, cwd, or stale label without exact PID eligibility
  - restarting app-server, control-host, launchd, or provider-worker infrastructure as remediation
  - editing `provider-intake-state.json` or queue state to make quota symptoms disappear
  - launching model-backed agents or reviews as part of the remediation path
  - adding broad background cleanup, daemon behavior, or retry loops

## Parity / Alignment Matrix

| Surface | Current truth | Reference truth | Target truth / intended delta | Explicitly out-of-scope differences |
| --- | --- | --- | --- | --- |
| Quota-hygiene detection | CO-542 owns current audit and reporting. | Detection/reporting should remain read-only and zero-model. | CO-551 consumes current-audit evidence for a separate operator-confirmed remediation flow. | Changing CO-542 into a mutating cleanup tool. |
| Target selection | Stale process reports can mention processes, commands, or roles. | Destructive process actions require exact PID-scoped evidence. | Only scoped PIDs that are eligible in the current audit can be remediated. | Kill-by-name, fuzzy process matching, cwd-wide cleanup, or stale snapshot-only targeting. |
| Operator control | Operators currently must reason manually from detector output. | Remediation should be explicit and auditable. | Each action requires explicit confirmation tied to the current audit and target list. | Automatic kill/restart, background sweeps, or confirmation bypass. |
| Audit evidence | Current reporting can explain suspected stale quota consumers. | Remediation needs durable result evidence for every PID. | Output records per-PID eligibility, action, result, timestamps, and refusal reasons. | Silent cleanup, top-level summary only, or unstructured terminal prose. |
| Runtime authority | App-server/control-host/provider-intake are governed control-plane surfaces. | Stale process remediation should not mutate those authorities. | CO-551 does not restart app-server/control-host and does not mutate provider-intake state. | Provider-intake edits, control-host restarts, app-server restarts, or queue mutations. |

## Not Done If
- CO-542 becomes a mutating detector or no longer remains detection/reporting only.
- Remediation can run without explicit operator confirmation.
- Targets can be selected by process name, command substring, role, cwd, or stale label without current-audit PID eligibility.
- Any automatic kill/restart path exists.
- The implementation restarts app-server, control-host, launchd, or provider-worker infrastructure.
- The implementation mutates `provider-intake-state.json`, Linear state, queue state, model settings, or Codex defaults.
- The remediation flow spends model calls.
- Per-PID output is missing, non-durable, or omits skipped/refused PID reasons.
- The docs packet or parent implementation widens into broad quota policy, provider-intake repair, or control-host lifecycle recovery.

## Goals
- Create the CO-551 docs-first packet and task mirrors.
- Preserve the exact canonical owner key and protected remediation boundaries.
- Make the parent implementation contract explicit before source or test edits.
- Keep current audit detection and operator-confirmed remediation separate.
- Require durable per-PID audit output for every scoped target.

## Non-Goals
- No source implementation or tests in this child lane.
- No Linear/workpad/PR lifecycle changes in this child lane.
- No automatic process cleanup.
- No app-server/control-host restarts.
- No provider-intake mutation.
- No kill-by-name behavior.
- No model-backed analysis or review as a normal remediation dependency.
- No CO-542 detector redesign.

## Acceptance Criteria
- Packet/checklist files exist with CO-551 traceability and source anchor.
- Canonical owner key `quota-hygiene:operator-confirmed-stale-process-remediation:v1` appears in the PRD, specs, action plan, checklist, and task registry entry.
- PRD and TECH_SPEC state that current detector owner CO-542 remains detection/reporting only.
- PRD, TECH_SPEC, ACTION_PLAN, and checklist preserve explicit operator confirmation, scoped targets, current-audit PID eligibility, and durable per-PID audit output.
- `Not Done If` rejects automatic kill/restart, app-server/control-host restart, provider-intake mutation, model calls, and kill-by-name behavior.
- Parent-owned validation requirements are listed before implementation starts.

## Metrics & Guardrails
- Primary Success Metrics:
  - docs packet changes stay inside the declared docs phase scope
  - protected-term scan finds all protected terms across the packet and mirrors
  - parent implementation cannot remediate a PID absent from the current audit
  - parent implementation emits one durable audit record per requested PID
  - parent implementation refuses unconfirmed or ineligible targets without side effects
- Guardrails / Error Budgets:
  - zero source/test changes from this child lane
  - zero Linear mutations from this child lane
  - zero model calls in remediation flow
  - zero provider-intake mutations
  - zero app-server/control-host restarts
  - zero kill-by-name behavior

## Fallback / Refactor Decision
- Applies to fallback, compatibility, legacy, stale, cached, break-glass, or minor-seam behavior? `Yes`.
- Decision: `remove fallback` for the unsafe operator workaround where stale quota consumers are cleaned up manually or by broad process matching outside a governed, current-audit PID-scoped remediation path.
- Owner: `CO-551`.
- Trigger: a current quota-hygiene audit reports stale local process evidence and an operator chooses exact PID targets for remediation.
- Introduced date: observed by CO-551 on 2026-05-21.
- Review date: 2026-05-21.
- Maximum lifetime: this issue.
- Removal condition: parent implementation provides an operator-confirmed, PID-scoped remediation path with durable per-PID audit output and refusal semantics.
- Validation: parent-owned focused remediation tests for confirmation, PID eligibility, audit output, no kill-by-name, no restarts, no provider-intake mutation, and no model calls.
- Large-refactor check: a bounded remediation command is acceptable because CO-551 does not split detection authority or add a competing provider-intake/control-host lifecycle source. Escalate if implementation requires changing CO-542 detector authority, provider-intake lifecycle, or control-host restart policy.

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Stale process remediation | Operators can be tempted to kill stale quota consumers manually or by broad process name. | `remove fallback` | CO-551 | Current audit reports stale local process evidence and operator requests cleanup. | 2026-05-21 | 2026-05-21 | This issue | Remediation requires explicit operator confirmation and current-audit PID eligibility. | Focused tests prove unconfirmed/ineligible targets refuse without action. |
| CO-542 detector boundary | Contract name: CO-542 read-only quota-hygiene detector boundary. | `justify retaining fallback` | Owning surface: `codex-orchestrator hygiene quota` detector/reporting contract plus CO-551 remediation boundary. | Steady-state proof: detector output remains zero-model and read-only, while remediation is available only through explicit `--apply --yes --only` confirmation and current-audit PID eligibility. | CO-542 detector contract | 2026-05-21 | Non-expiring durable retention only with rationale. | Non-expiring rationale: the detector/remediator authority split is a supported safety contract, not temporary compatibility debt; remove only through a separate approved issue that preserves current-audit evidence, explicit confirmation, and read-only detector semantics. | Tests/docs: focused detector smoke, remediation command contract, and false-positive tests prove detector remains read-only while remediation is opt-in. |
| Per-PID audit evidence | Cleanup can be summarized without durable per-target proof. | `remove fallback` | CO-551 | Operator confirms more than one PID or a PID is refused. | 2026-05-21 | 2026-05-21 | This issue | Every requested PID has durable action/refusal output. | Snapshot/schema test for per-PID audit records. |

Durable retained-boundary evidence:
- Contract name: CO-542 read-only quota-hygiene detector boundary.
- Owning surface: `codex-orchestrator hygiene quota` detector/reporting contract plus CO-551 remediation boundary.
- Steady-state proof: detector output remains zero-model and read-only, while remediation is available only through explicit `--apply --yes --only` confirmation and current-audit PID eligibility.
- Tests/docs: focused detector smoke, remediation command contract, and false-positive tests prove detector remains read-only while remediation is opt-in.
- Non-expiring rationale: the detector/remediator authority split is a supported safety contract, not temporary compatibility debt; remove only through a separate approved issue that preserves current-audit evidence, explicit confirmation, and read-only detector semantics.

## Technical Considerations
- Architectural Notes:
  - Parent implementation should consume the current quota-hygiene audit as evidence, not stale memory or previous reports.
  - Confirmation should bind to the exact PID list and current-audit evidence so a stale confirmation cannot apply to later PIDs.
  - PID eligibility should fail closed when a process disappeared, changed identity, is not stale, belongs to protected infrastructure, or lacks current-audit evidence.
  - Remediation output should be structured and durable enough for workpad, PR, and post-run audit trails.
- Dependencies / Integrations:
  - CO-542 `codex-orchestrator hygiene quota` detector/reporting output
  - process inventory / PID identity checks
  - parent-owned command surface selection
  - parent-owned validation and PR review workflow

## Open Questions
- Parent implementation must choose the exact command name and confirmation UX.
- Parent implementation must decide the durable audit path and schema.
- Parent implementation must define which current-audit evidence fields are sufficient PID identity guards.

## Approvals
- Product: parent CO-551 lane owns issue/workpad/Linear/PR reconciliation.
- Engineering: bounded same-issue docs child lane produced the docs-first packet for parent import.
- Design: N/A.
