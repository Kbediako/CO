# PRD - CO-576 reconcile terminal promoted-PR intake residue

## Traceability
- Linear issue: `CO-576` / `6c71f385-db09-470b-84cb-61013987a2a1`
- Task id: `linear-6c71f385-db09-470b-84cb-61013987a2a1`
- Registry id: `20260525-linear-6c71f385-db09-470b-84cb-61013987a2a1`
- Source issue: `CO-516` / `089348e9-a67f-4cf0-a9f5-a55bd885f0aa`
- Canonical owner key: `provider-intake:terminal-merge-closeout-truth:post-co516-recurrence-v1`
- Canonical owner marker: `codex-orchestrator:canonical-owner-key=provider-intake:terminal-merge-closeout-truth:post-co516-recurrence-v1`

## Summary
- Problem Statement: after CO-516, retained completed `provider_issue_review_promotion_promoted` claims in `provider-intake-state.json` can still project stale current truth such as `issue_state=Merging`, `review_pr_state OPEN`, and `merge_closeout=null` even when GitHub says the PR is merged and Linear says the issue is `Done`.
- Desired Outcome: completed promoted-review audit rows retain their historical evidence but current projections, active counts, queue blockers, retry paths, and stale alarms use live terminal truth or explicitly source-labeled stale/degraded truth.

## User Request Translation
- Reproduce the post-CO-516 recurrence for `CO-514`, `CO-518`, `CO-552`, `CO-565`, `CO-566`, and `CO-573`.
- Fix provider-intake projection and accounting so merged PR plus Linear `Done` cannot leave retained completed claims acting like current `Merging` or open review work.
- Preserve audit history and live-truth separation; do not hand-edit `provider-intake-state.json`.

## Intent Checksum
- Protected terms / exact artifact and surface names: `provider-intake-state.json`, `completed claim`, `provider_issue_review_promotion_promoted`, `issue_state=Merging`, `review_pr_state OPEN`, `merge_closeout=null`, `merged PR`, `Linear Done`, `CO-514`, `CO-518`, `CO-552`, `CO-565`, `CO-566`, `CO-573`.
- Nearby wrong interpretations to reject: deleting audit history, manual provider-intake cleanup, counting stale retained rows as active WIP, reopening completed issues without fresh live evidence, changing PR merge policy, weakening review gates, broad CO STATUS redesign, worker kill, and control-host restart as the fix.

## Current Truth
- Live provider-intake evidence on 2026-05-25 still shows stale completed/handoff residue extending beyond the original issue list, including completed rows for recently merged issues.
- Open GitHub PR audit found no current open PRs for `Kbediako/CO`, so stale promoted-review rows must not be treated as active PR readiness work.
- CO-557 is currently the only running provider-worker claim; CO-576 must not collide with that active lane.

## Parity / Alignment Matrix

| Surface | Current / Reference Truth | Target Truth | Explicitly Out Of Scope |
| --- | --- | --- | --- |
| Provider-intake retained claim | Completed promoted-review claims preserve historical audit fields. | Historical fields remain retained, while current projection is source-labeled terminal/stale/degraded truth. | Manual JSON cleanup or audit deletion. |
| GitHub PR state | Listed PRs are merged, while cached review state can look open. | Merged PR truth wins for current closeout projection when live verification is available. | Changing PR merge policy or review gates. |
| Linear issue state | Listed source issues are terminal `Done` unless fresh live evidence says otherwise. | Linear `Done` excludes retained completed rows from active WIP, retry, blocker, and stale alarm paths. | Reopening completed issues as a cleanup mechanism. |
| Queue accounting | Retained stale rows can pollute status and retry surfaces. | Only accepted/running/starting/claimed work consumes active capacity; terminal retained rows are audit-only. | Broad CO STATUS redesign. |

## Acceptance Criteria
1. Reproduce the May 22 post-CO-516 retained promoted-review residue for the listed merged PRs.
2. Reconcile or project completed promoted-review claims with live merged PR and terminal Linear truth while preserving audit evidence.
3. Exclude terminal retained promoted-review rows from active WIP, retry, blocker, and stale alarm paths unless fresh live evidence says the issue reopened.
4. Add focused regressions for completed `provider_issue_review_promotion_promoted` rows with merged PR plus Linear `Done` and `merge_closeout=null`.
5. Document the relationship to CO-516 and why this is recurrence follow-up work, not manual state cleanup.

## Not Done If
- A merged PR plus live Linear `Done` can leave a retained completed provider-intake claim projecting `issue_state=Merging`, `review_pr_state OPEN`, or `merge_closeout=null` without source-labeled terminal or stale/degraded truth.
- Active counts, queue blockers, retry routing, or status projections treat terminal retained rows as current work.
- The implementation deletes retained audit history, hand-edits provider-intake state, restarts the control host as the fix, or reopens terminal issues without fresh live evidence.

## Goals
- Preserve strict live truth for queue and status consumers.
- Keep retained audit history inspectable.
- Make stale terminal residue harmless to WIP, retry, blocker, and PR readiness surfaces.

## Non-Goals
- No manual `provider-intake-state.json` cleanup.
- No change to PR merge policy, review gates, or retained audit history.
- No broad CO STATUS redesign.
- No worker kill, control-host restart, or Linear terminal issue reopening as the fix.

## Metrics & Guardrails
- Primary success metric: terminal promoted-review rows no longer count as active work or current open PR/review blockers.
- Guardrail: retained audit rows remain present with explicit current-truth provenance.
- Guardrail: provider-intake active capacity remains based on real accepted/running/starting/claimed work.

## CO-382 Fallback Decision Table
- Applies to fallback, compatibility, legacy, stale, cached, break-glass, or minor-seam behavior? `Yes`.
- Large-refactor decision: no large refactor is warranted because CO-576 removes one stale current-projection branch inside the existing provider-intake completed-run rehydrate authority; it does not add a new source of truth or split queue/status/PR lifecycle ownership.
- Minor-seam decision: remove the stale current-projection seam now; retain only the existing completed-claim audit record as a durable audit contract.

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `provider-intake current projection` | Completed promoted-review rows can project cached `Merging` / open review truth after PR merge and Linear terminal closeout. | `remove fallback` | CO-576 | Post-CO-516 recurrence for merged PRs with retained completed claims. | 2026-05-22 | 2026-05-25 | Removed in this issue | Current projection uses fresh Linear `Done` truth or explicit stale/degraded provenance, and terminal rows do not consume active work paths. | `orchestrator/tests/ProviderIssueHandoff.test.ts` focused stale promoted-review rehydrate regression plus full provider handoff suite. |
| `provider-intake completed-claim audit retention` | Completed claim history remains in `provider-intake-state.json` for audit and diagnosis. | `justify retaining fallback` | CO-576 | Historical provider-worker claims are intentionally retained after closeout. | 2026-05-22 | 2026-05-25 | Non-expiring durable audit retention only with rationale | Retention stays audit-only and never overrides live Linear/GitHub closeout truth. | Regression asserts the stale standalone promoted-review residue is cleared from current projection while released merge-closeout audit retention remains intact. |

Durable retention evidence:
- Contract name: `provider-intake completed-claim audit retention`.
- Owning surface: provider-intake state and status projection.
- Steady-state proof: retained completed rows remain readable for diagnostics while current projection comes from fresh Linear/GitHub truth or source-labeled degraded reads.
- Tests/docs: `orchestrator/tests/ProviderIssueHandoff.test.ts` stale promoted-review regression plus existing released merge-closeout audit-retention coverage; this CO-576 PRD, TECH_SPEC, ACTION_PLAN, and task checklist.
- Non-expiring rationale: completed-claim retention is the audit artifact, not an active fallback; only stale current projection is removed.

## Open Questions
- Whether the implementation should cover newer residue beyond the original CO-576 issue list in the same fix or classify it as follow-up depends on source seam size after investigation.
