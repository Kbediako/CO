# PRD - CO-386 Codex CLI release-intake supersession workflow

## Traceability
- Linear issue: `CO-386` / `67e2ffd2-a653-4d69-9241-5f32c459a682`
- Linear URL: https://linear.app/asabeko/issue/CO-386
- Task id: `linear-67e2ffd2-a653-4d69-9241-5f32c459a682`
- Canonical spec: `tasks/specs/linear-67e2ffd2-a653-4d69-9241-5f32c459a682.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-67e2ffd2-a653-4d69-9241-5f32c459a682.md`
- Parent manifest: `.runs/linear-67e2ffd2-a653-4d69-9241-5f32c459a682-docs-packet/cli/2026-04-26T02-01-05-570Z-cf7b5e23/manifest.json`
- Docs packet child source anchor: `ctx:sha256:aad8a7b6913c77cb5d07cb83c0b1766d268691fd3e688457d7f9b8bfd48a1742#chunk:c000001`
- Parent source payload: `.runs/linear-67e2ffd2-a653-4d69-9241-5f32c459a682-docs-packet/cli/2026-04-26T02-01-05-570Z-cf7b5e23/memory/source-0/source.txt`
- Source payload note: source-0 contains run metadata, issue id/identifier/updated_at, instruction hashes, and artifact paths only; it does not include additional CO-386 issue body text. This packet therefore uses the parent-provided CO-386 protected terms and issue-shaping instructions as the authoritative docs-first contract.

## Summary
- Problem Statement: Codex CLI release adoption has been handled by issue-specific posture lanes that can scatter release notes, local CLI evidence, package/downstream smoke evidence, cloud-canary evidence, workflow pins, model posture decisions, and docs surfaces across separate task packets. CO-386 needs a bounded docs-first packet for a Codex CLI release-intake supersession workflow so future release candidates can be classified consistently before parent implementation.
- Desired Outcome: create the CO-386 docs packet and task mirrors that define a parent-owned release-intake workflow with a supersedes/holds matrix, archive-only classification, and evidence gates for local CLI, package/downstream smoke, cloud-canary, workflow pins, model posture, docs surfaces, and release notes.

## User Request Translation
- User intent / needs: prepare the docs-first packet for CO-386 without implementation changes, preserving the exact protected terms that make this issue about Codex CLI release-intake, not another ad hoc version bump or model default change.
- Success criteria / acceptance:
  - PRD, canonical TECH_SPEC, ACTION_PLAN, task checklist, `.agent` mirror, `tasks/index.json` entry, and `docs/TASKS.md` snapshot exist for `linear-67e2ffd2-a653-4d69-9241-5f32c459a682`
  - packet preserves `Codex CLI release-intake`, `supersession workflow`, `local CLI`, `package/downstream smoke`, `cloud-canary`, `workflow pins`, `model posture`, `docs surfaces`, `release notes`, `supersedes/holds matrix`, and `archive-only classification`
  - parent implementation is framed as evidence-first and posture-first, not as blind pin promotion
  - parent-owned validation distinguishes local CLI evidence, package/downstream smoke evidence, cloud-canary evidence, workflow pins, model posture, and docs surfaces
  - older release-intake/posture evidence can be held as archive-only when superseded, without deleting useful history or confusing current policy
- Constraints / non-goals:
  - this child lane is docs-only and must not touch implementation, template, workflow, package, test, or Linear mutation surfaces
  - do not run full repo validation suites from this child lane
  - do not call Linear mutation helpers
  - do not decide the final Codex CLI version, workflow pin, or model posture here

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `CO-386`
  - `Codex CLI release-intake`
  - `supersession workflow`
  - `local CLI`
  - `package/downstream smoke`
  - `cloud-canary`
  - `workflow pins`
  - `model posture`
  - `docs surfaces`
  - `release notes`
  - `supersedes/holds matrix`
  - `archive-only classification`
- Nearby wrong interpretations to reject:
  - "CO-386 should promote the newest Codex CLI because release notes exist"
  - "local CLI success alone is enough to update workflow pins"
  - "package/downstream smoke can be skipped when model posture looks current"
  - "cloud-canary failure can be hidden as archive-only classification"
  - "docs surfaces should be updated before the supersedes/holds matrix is explicit"
  - "older posture packets should be deleted instead of retained as archive-only evidence"
  - "this child lane should edit workflow, package, test, template, or implementation files"

## Parity / Alignment Matrix

| Surface | Current Truth | Reference Truth | Target Truth | Explicitly Out Of Scope |
| --- | --- | --- | --- | --- |
| Codex CLI release-intake | Prior lanes can capture candidate-specific evidence, but release notes, local CLI, package/downstream smoke, cloud-canary, workflow pins, model posture, and docs surfaces are not yet governed by one intake matrix. | CO release posture is evidence-gated; local success is not enough without downstream smoke and cloud-canary classification. | CO-386 defines a reusable release-intake workflow that records every candidate's evidence and final hold/promote/archive-only decision. | Promoting a specific release from this child lane. |
| Supersession workflow | Older posture packets and workflow-pin decisions remain useful but can become hard to distinguish from current policy. | Superseded evidence should remain auditable while current docs point at the active posture. | A supersedes/holds matrix says which prior evidence is superseded, which blockers hold, and which records become archive-only classification. | Deleting historical posture docs or changing archive policy here. |
| Local CLI and package/downstream smoke | Local CLI checks and package/downstream smoke can produce different adoption truth. | Downstream-facing package behavior must remain independently validated. | Parent implementation requires separate rows for local CLI and package/downstream smoke before any release-facing workflow pins move. | Running `npm run pack:smoke` or modifying package smoke tests here. |
| Cloud-canary and workflow pins | Cloud-canary and workflow pins may intentionally diverge from local posture when cloud evidence or marketplace compatibility is blocked. | Required/fallback cloud-canary results must be explicit and reproducible. | The matrix records cloud-canary status and workflow pins as first-class gates, including hold reasons. | Editing `.github/workflows/**` here. |
| Model posture and docs surfaces | Model posture and docs surfaces can drift when Codex CLI adoption work also touches default model guidance. | Model posture is a governed policy surface, not an incidental release-note side effect. | Parent implementation carries model posture through the same release-intake matrix and updates docs surfaces only after the posture decision. | Changing current model defaults or docs content outside this packet. |

## Not Done If
- The packet omits any protected term listed in the issue-shaping instructions.
- Codex CLI release-intake is framed as a version bump rather than an evidence workflow.
- The supersession workflow lacks a supersedes/holds matrix.
- Archive-only classification is used to hide unresolved blockers instead of classifying superseded evidence.
- Local CLI, package/downstream smoke, cloud-canary, workflow pins, model posture, docs surfaces, and release notes are not separately visible.
- Parent cannot tell which implementation, template, workflow, package, test, and Linear surfaces remain parent-owned.

## Goals
- Create the CO-386 docs-first packet and mirrors inside the declared docs scope.
- Preserve the protected CO-386 release-intake and supersession terms exactly.
- Define the parent-owned release-intake matrix and evidence gates before implementation.
- Make archive-only classification safe, auditable, and distinct from unresolved holds.

## Non-Goals
- No implementation, workflow, template, package, test, or model default changes in this child lane.
- No Codex CLI version promotion decision in this child lane.
- No Linear state, workpad, or PR lifecycle mutations.
- No broad docs freshness or archive sweep.
- No weakening of package/downstream smoke, cloud-canary, or workflow pin gates.

## Stakeholders
- Product: CO operators who need current Codex CLI adoption truth without losing historical posture context.
- Engineering: release-intake, workflow pin, package/downstream smoke, cloud-canary, model posture, and docs surface maintainers.
- Design: N/A.

## Metrics & Guardrails
- Primary Success Metrics:
  - all seven declared docs packet paths are created or updated
  - `tasks/index.json` remains valid JSON
  - protected-term scan covers the CO-386 packet files
  - child-lane patch touches only declared file-scope paths
- Guardrails / Error Budgets:
  - zero edits outside declared docs packet scope
  - zero Linear mutations
  - zero release, workflow, package, template, or test edits
  - no wording that treats archive-only classification as success for a live blocker

## Technical Considerations
- Architectural Notes:
  - CO-386 is a governance and docs-first issue: it creates the decision frame before the parent implementation chooses exact surfaces.
  - The supersedes/holds matrix should be machine-checkable enough for reviewers to distinguish current posture, held blockers, superseded evidence, and archive-only records.
  - Parent implementation should keep local CLI, package/downstream smoke, cloud-canary, workflow pins, model posture, docs surfaces, and release notes as separate rows or fields.
- Dependencies / Integrations:
  - `docs/guides/codex-version-policy.md`
  - workflow pin surfaces under `.github/workflows/**`
  - package/downstream smoke validation
  - cloud-canary evidence
  - local Codex CLI command evidence
  - docs surfaces that publish current model posture and release posture

## Open Questions
- Which parent-owned document or script will become the canonical release-intake surface: an existing version-policy doc, a new intake template, or an orchestrator-generated artifact?
- Should archive-only classification be stored in the same matrix as supersedes/holds, or as a derived view for docs freshness and release posture reporting?
- Which existing posture packets should the parent list as initial superseded or held examples for CO-386?

## Approvals
- Product: parent CO-386 lane, pending
- Engineering: parent docs-review / implementation review, pending
- Design: N/A
