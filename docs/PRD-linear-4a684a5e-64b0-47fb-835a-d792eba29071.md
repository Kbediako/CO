# PRD - CO Codex CLI 0.124.0 and GPT-5.5 Posture

## Summary
Codex CLI moved to `0.124.0` while current main documents the active adoption target as Codex CLI `0.123.0` and model posture as `gpt-5.4`. CO-341 turns the new local `codex-cli 0.124.0` and `gpt-5.5` availability into an evidence-gated posture lane instead of a blind promotion.

## User Request Translation
- Capture official `rust-v0.124.0` release facts, OpenAI Codex model guidance, hooks guidance, and config reference facts with source URLs and dates.
- Record local auth/provider state with `codex --version`, `npm view @openai/codex version`, and `codex login status`.
- Probe `gpt-5.5` with `model_reasoning_effort = "xhigh"` across local `codex exec`, delegated subagent, direct review model, provider-worker/app-server, provider-worker `codex exec` / `codex exec resume` seam where safe, and cloud-canary or exact blocker evidence.
- Decide promote, hold, or no-op buckets for Codex CLI `0.124.0`, `gpt-5.5`, `codex_hooks`, `hooks.json`, `legacy_notify`, stale config warnings, marketplace/plugin command changes, and 0.124 release deltas.
- Land repo docs/tests/configuration only after the gated evidence supports the posture.

## Intent Checksum
Protected terms: Codex CLI `0.124.0`, `rust-v0.124.0`, `gpt-5.5`, `model_reasoning_effort = "xhigh"`, `review_model`, `codex_hooks`, `hooks.json`, `legacy_notify`, `after_agent hook failed`, `remote_connections`, `skills`, `voice_transcription`, `Fast service tier`, `wait_agent`, `app-server multiple environments`, `codex exec`, `codex exec resume`, `codex review`, `provider-linear-worker`, `pack:smoke`, `cloud-canary`, `CODEX_CLOUD_ENV_ID`.

Wrong interpretations to reject: blind promotion from one OK-only smoke, treating ChatGPT-auth local availability as Codex Cloud or API-key availability, weakening cloud/runtime canary requirements, ignoring config/hook warnings, changing marketplace smoke policy without validating the current `plugin marketplace` command, or touching the dirty shared CO-282/CO-278 checkout.

## Parity / Alignment Matrix
- Current truth: repo policy says active target Codex CLI `0.123.0`; model posture `gpt-5.4`; release-facing smoke and cloud-canary workflows pin `@openai/codex@0.123.0`; marketplace smoke uses the `codex plugin marketplace add` contract.
- Reference truth: official Codex docs recommend `gpt-5.5` for most Codex tasks when available, mark `gpt-5.5` usable in Codex CLI/SDK and unavailable in Codex Cloud/API-key auth, and `rust-v0.124.0` release notes add stable hooks and marketplace/plugin/runtime changes.
- Target truth: promote only surfaces with current passing evidence; hold or explicitly block surfaces where `CODEX_CLOUD_ENV_ID`, Codex Cloud model choice, post-build canaries, or command-surface compatibility remain unproven.
- Explicitly out-of-scope differences: unrelated CO-282 dirty checkout work, CO-337/CO-340 historical cleanup beyond superseding posture notes, CO-338 release publication, broad runtime/control-host rewrites, and user-level config mutation outside evidence capture.

## Not Done If
- CO docs still say only `0.123.0` / `gpt-5.4` after evidence proves a surface should promote.
- CO promotes `0.124.0` or `gpt-5.5` without runtime, delegated/review, provider-worker, and cloud blocker evidence.
- The `remote_connections`, `skills`, `voice_transcription`, or `legacy_notify` warnings remain untracked.
- Marketplace/pack-smoke policy changes land without validating the current `plugin marketplace` command surface.

## Goals
- Produce a single evidence pack for CO-341 under `.runs/linear-4a684a5e-64b0-47fb-835a-d792eba29071/` and `out/linear-4a684a5e-64b0-47fb-835a-d792eba29071/manual/`.
- Make one dated posture decision for Codex CLI `0.124.0`, `gpt-5.5`, and 0.124 hook/config adoption.
- Update active repo truth surfaces and tests consistently with the final decision.
- Keep CO provider intake within the active-issue cap while preserving the dirty shared checkout.

## Non-Goals
- No direct edits to `/Users/kbediako/Code/CO` shared-root dirty work.
- No weakening of cloud canary, pack-smoke, provider-worker, or review-wrapper gates.
- No broad plugin marketplace redesign beyond the command-surface compatibility required for current `0.124.0`.
- No user-level `~/.codex/config.toml` edits unless explicitly requested; local config/hook state is evidence only.

## Metrics & Guardrails
- `gpt-5.5` probes must record command, exit, output, and blocker status.
- Runtime/cloud gates must pass after build or record exact blocker evidence.
- `docs:check`, `docs:freshness`, and focused tests must reflect the new posture.
- Full validation floor and `pack:smoke` are required if CLI/package/skill/review-wrapper/downstream smoke surfaces change.

## Approvals
- Product: Linear issue CO-341.
- Engineering: docs-review before implementation, full validation floor, standalone review, elegance review, PR ready-review drain, and Linear workpad closeout.
- Design: Not applicable.
