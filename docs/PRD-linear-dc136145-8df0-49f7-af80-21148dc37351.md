# PRD - CO-453 provider child-lane tracker real-run reconciliation

## Traceability
- Linear issue: `CO-453` / `dc136145-8df0-49f7-af80-21148dc37351`
- Task registry id: `20260503-linear-dc136145-8df0-49f7-af80-21148dc37351`
- MCP Task ID: `linear-dc136145-8df0-49f7-af80-21148dc37351`
- Canonical owner key: `provider-worker:child-lane-tracker-real-run-reconciliation`
- Canonical owner marker: `codex-orchestrator:canonical-owner-key=provider-worker:child-lane-tracker-real-run-reconciliation`
- Source anchor: `ctx:sha256:10210d74b1aeda6ff0cb3656c91e02bc36497974d4817a4a180e3739db3aa5a9#chunk:c000001`
- Source manifest: `.runs/linear-dc136145-8df0-49f7-af80-21148dc37351-docs-packet/cli/2026-05-03T12-07-21-112Z-a02619a1/manifest.json`
- Source payload: `.runs/linear-dc136145-8df0-49f7-af80-21148dc37351-docs-packet/cli/2026-05-03T12-07-21-112Z-a02619a1/memory/source-0/source.txt`
- Source payload note: the declared source payload path is not present inside this child lane checkout. This packet is anchored on the parent-provided issue contract, source anchor, and protected terms; the parent lane owns live Linear `issue-context`, workpad, docs-review, implementation, PR lifecycle, and final handoff.

## Summary
- Problem Statement: provider child-lane tracking can keep `provider-linear-worker-child-lanes.json` pinned to a synthetic `launching-*` reservation after the real child run has started and written a trusted manifest. Parent refresh/status logic can then read nonexistent placeholder paths and hide healthy child progress behind tracker drift.
- Desired Outcome: parent implementation reconciles the current child-lane ledger row from `launching-*` reservation metadata to the real child run id and artifact paths once startup succeeds. Launch context remains audit-visible through child-lane proof, decision lineage, and accepted patch artifacts, while CO-449 intake and generic `refresh request timeout` recovery stay out of scope.

## User Request Translation
- User intent / needs:
  - create the CO-453 docs-first packet and checklist/index mirrors only
  - preserve protected terms: `provider-linear-worker-child-lanes.json`, `launching-*`, real child run id/artifact paths, parent refresh/status logic, `refresh request timeout`, real child run startup reconciliation, and CO-449 as a related observed lane only
  - define a parent-owned implementation contract where successful child startup reconciles the current ledger row to real child run metadata
  - keep CO-449 release intake and refresh-timeout diagnosis out of implementation scope unless the parent relaunches with widened ownership
- Success criteria / acceptance:
  - PRD, TECH_SPEC mirror, canonical task spec, ACTION_PLAN, task checklist, `tasks/index.json`, and `docs/TASKS.md` carry the same issue-shaping contract
  - parent implementation reconciles `provider-linear-worker-child-lanes.json` current rows from `launching-*` reservation metadata to real child run id and artifact paths
  - parent refresh/status logic resolves the real child manifest after launch instead of the nonexistent placeholder path
  - launch provenance remains available through proof, decision lineage, and accepted patch artifacts
  - parent implementation leaves CO-449 intake, broad recovery/freshness, and generic `refresh request timeout` remediation out of scope
- Constraints / non-goals:
  - child lane owns docs packet, checklist, `tasks/index.json`, and `docs/TASKS.md` only
  - no source edits, tests, Linear mutation, GitHub mutation, workpad edits, PR lifecycle work, `.agent` mirror edits, or docs-freshness registry edits in this child lane
  - parent owns live source inspection, docs-review, implementation, validation, workpad, PR lifecycle, and final patch integration

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `provider-linear-worker-child-lanes.json`
  - `launching-*`
  - real child run id/artifact paths
  - parent refresh/status logic
  - `refresh request timeout`
  - real child run startup reconciliation
  - CO-449 as related observed lane only
  - no CO-449 intake changes
  - no broad recovery/freshness work
  - no leaving current ledger rows pinned to `launching-*` after a trusted real child manifest exists
- Protected terms / exact artifact and surface names:
  - `provider-linear-worker-child-lanes.json`
  - `provider-linear-child-lane.patch`
  - `provider-linear-child-lane-proof.json`
  - `provider-linear-child-lane`
  - `parent_run_id`
  - `run_manifest_path`
  - `issue_updated_at`
  - `updated_at`
  - `real child run startup reconciliation`
  - `refresh request timeout`
  - `CO-449`
  - `provider-worker:child-lane-tracker-real-run-reconciliation`
- Nearby wrong interpretations to reject:
  - CO-449 release-intake implementation or posture changes
  - generic control-host recovery or docs freshness repair
  - treating every `refresh request timeout` as CO-453
  - requiring manual patch handling instead of tracker reconciliation after trusted child startup
  - dropping child-lane proof or decision evidence to hide stale tracker drift
  - changing same-issue child-lane launch, reject, cap, or child-side mutation authority

## Parity / Alignment Matrix

| Surface | Current truth | Reference truth | Target truth | Explicitly out-of-scope differences |
| --- | --- | --- | --- | --- |
| Child-lane ledger | `provider-linear-worker-child-lanes.json` can record a temporary `launching-*` reservation before the child manifest exists. | A trusted child manifest is the current run authority after startup succeeds. | Current ledger rows reconcile to the real child run id, manifest path, artifact root, and log path once discovered. | Dropping proof/decision/patch evidence or rewriting unrelated rows. |
| Child run evidence | Real child run id/artifact paths can exist after the initial `launching-*` reservation. | Real child artifacts remain the basis for patch/proof review. | Parent refresh/status logic resolves real child run evidence instead of placeholder paths. | Accepting proof-mismatched, out-of-scope, or missing patch artifacts. |
| Parent refresh/status logic | Status can continue to read placeholder manifest paths after real startup. | Parent owns current child-run truth and durable status projection. | Status/refresh reports the real child run and does not treat the placeholder as current once a trusted manifest exists. | Broad status UI redesign or provider workflow rewrite. |
| Refresh timeout diagnosis | `refresh request timeout` can appear in adjacent provider-worker recovery lanes. | Timeout diagnosis is related evidence, not the CO-453 fix itself. | CO-453 keeps timeout wording protected while leaving generic recovery/freshness work to owners. | CO-449 intake changes or broad recovery/freshness work. |
| Related observed lane | CO-449 may expose the symptom while release-intake work is active. | CO-449 owns intake/posture, not child-lane tracker repair. | CO-449 is cited only as related observed context. | CO-449 implementation or lifecycle changes. |

## Not Done If
- `provider-linear-worker-child-lanes.json` remains pinned to `launching-*` after a trusted real child manifest exists.
- Parent refresh/status logic still resolves a nonexistent placeholder manifest path after real child startup.
- Real child run id/artifact paths are lost, overwritten, or treated as the same thing as the `launching-*` reservation id.
- The fix changes CO-449 intake behavior or broad release posture.
- The fix becomes generic `refresh request timeout`, control-host recovery, or docs freshness work.
- Launch proof, decision lineage, or accepted patch evidence is deleted to hide tracker drift.

## Goals
- Create the CO-453 docs-first packet and checklist/index mirrors within the child lane file scope.
- Define a narrow parent-owned implementation contract for placeholder-to-real-run tracker reconciliation after child startup.
- Preserve child-lane proof, decision, and patch evidence while making the current ledger row point at the real run.
- Keep parent refresh/status truth aligned with real child manifests.
- Keep CO-449 intake and refresh-timeout recovery/freshness work out of scope.

## Non-Goals
- No CO-449 intake changes.
- No broad recovery/freshness work.
- No generic `refresh request timeout` remediation.
- No leaving stale placeholder paths as current run authority after real startup.
- No source or test edits in this docs-only child lane.
- No Linear, GitHub, workpad, PR lifecycle, `.agent`, or docs-freshness registry mutation in this child lane.
- No changes to same-issue child-lane launch, reject, invalidate, cap, or child-side lifecycle authority unless parent relaunches with widened ownership.

## Stakeholders
- Product: CO operators who need provider-worker status to distinguish child startup progress from placeholder tracker drift.
- Engineering: parent CO-453 provider worker implementing child-lane tracker reconciliation and focused regressions.
- Review: parent lane and reviewers validating that CO-449 and refresh-timeout boundaries remain separate.

## Metrics & Guardrails
- Primary Success Metrics:
  - protected terms appear in the PRD, TECH_SPEC mirror, canonical task spec, ACTION_PLAN, and task checklist
  - `tasks/index.json` parses after registration
  - parent focused tests cover placeholder-to-real-run reconciliation and preserved real child run id/artifact paths
- Guardrails:
  - no CO-449 intake changes
  - no broad recovery/freshness work
  - no stale placeholder manifest path after trusted child startup
  - no loss of child-lane proof, decision, or patch evidence

## Technical Considerations
- Architectural Notes:
  - parent implementation should inspect provider child-lane tracking, accept/reject/invalidate decision recording, and parent status/refresh projection of child lanes
  - reconcile `launching-*` reservation rows to real child run id/artifact paths for current status once a trusted manifest exists
  - keep launch context in child-lane proof, decision lineage, and accepted patch artifacts
  - keep `refresh request timeout` as adjacent diagnostic wording unless parent source inspection proves it is directly in this tracker seam
- Dependencies / Integrations:
  - `provider-linear-worker-child-lanes.json`
  - `provider-linear-child-lane.patch`
  - `provider-linear-child-lane-proof.json`
  - parent refresh/status logic
  - real child run manifest and artifact paths
  - CO-449 observed intake context

## Fallback / Refactor Decision
- Applies to fallback, compatibility, legacy, stale, cached, break-glass, or minor-seam behavior? `Yes`.
- Decision summary: remove stale placeholder authority after real child startup; keep child-lane proof, decision lineage, and accepted patch artifacts as durable audit truth; keep refresh-timeout diagnosis as an adjacent non-goal unless source inspection proves it is the same seam.

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Parent child-lane status/refresh projection | Status can read a placeholder manifest path after the real child run exists. | `remove fallback` | CO-453 | Trusted child manifest exists after startup. | observed 2026-05-03 | 2026-05-03 | This issue | Parent status/refresh resolves the real child manifest instead of the placeholder path. | Focused child-lane tracker/status regression. |
| Child-lane ledger current identity | `provider-linear-worker-child-lanes.json` can retain `launching-*` as current identity after a real child manifest exists. | `remove fallback` | provider worker child-lane tracker | Operators need real run identity and patch/proof provenance once startup succeeds. | Existing child-lane runtime | 2026-05-03 | This issue | Current ledger rows reconcile to the real child run id once a trusted manifest exists. | Focused tests prove placeholder-to-real-run ledger reconciliation and patch/proof path preservation. |
| Refresh timeout diagnosis | `refresh request timeout` appears near the observed symptom. | `justify retaining fallback` | control-host recovery owners, not CO-453 | Control-host refresh request exceeds its budget. | Existing recovery behavior | 2026-05-03 | Durable adjacent diagnostic | Dedicated recovery issue changes timeout contract. | Boundary tests or no-touch proof keep timeout remediation out of CO-453. |

- Durable retention evidence: child-lane proof, decision lineage, and accepted patch artifacts preserve launch context; the current ledger row must not stay pinned to a placeholder once the real run exists. Refresh timeout text remains protected as adjacent evidence, not a CO-453 implementation target.
- Large-refactor check: a broad provider-worker recovery/freshness refactor is not justified for this packet. Parent should keep the implementation at the child-lane tracker/status seam unless source inspection proves current child truth is split across incompatible ledger, proof, and status authorities.

## Open Questions
- Which parent-owned status surface should present the reconciled real child run first: the child-lane ledger, proof/status projection, or both?
- Should launch reservation metadata be duplicated into a historical field, or is proof/decision lineage sufficient for audit context?

## Approvals
- Product: CO-453 child-lane prompt, pending parent acceptance
- Engineering: bounded docs-only child lane
- Design: N/A
