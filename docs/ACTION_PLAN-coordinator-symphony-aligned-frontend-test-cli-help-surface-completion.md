# ACTION_PLAN: Coordinator Symphony-Aligned Frontend-Test CLI Help Surface Completion

## Steps

1. Add bounded frontend-test help handling in the binary wrapper.
2. Preserve the extracted request shell and lower frontend-test pipeline behavior.
3. Add focused CLI parity for `frontend-test --help` / `frontend-test help`.
4. Run the lane validation bundle and close the slice with explicit carry-forwards.
