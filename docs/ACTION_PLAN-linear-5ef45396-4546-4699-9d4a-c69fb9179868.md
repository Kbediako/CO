# ACTION_PLAN - CO-396 runtime-routing fallback expiry

## Summary
- Goal: create the CO-396 docs-first traceability packet and registry mirrors before the issue leaves Backlog.
- Scope: packet files, task mirrors, `tasks/index.json`, `docs/TASKS.md`, and docs freshness registry rows only.
- Assumptions:
  - the parent-provided issue context is authoritative for this prep stream
  - CO-382 fallback-expiry policy is authoritative for runtime-routing fallback classification
  - parent owns Linear state, workpad, implementation, full validation, PR lifecycle, merge, and any large-refactor decision

## Issue Readiness Gate
- Intent checksum / protected terms carried forward:
  - `CO-396`
  - `CO: expire runtime-routing fallback contracts`
  - `Immediate Traceability`
  - `runtime-routing fallback behavior`
  - `executionMode/runtimeMode separation`
  - `required cloud canary semantics`
  - `remove fallback`
  - `expire fallback`
  - `justify retaining fallback`
  - `durable compatibility`
  - `fail fast`
- Protected surfaces:
  - `orchestrator/src/cli/runtime/provider.ts`
  - `orchestrator/src/cli/runtime/codexCommand.ts`
  - `scripts/runtime-mode-canary.mjs`
  - cloud-mode docs
  - `runtime_fallback`
  - `cloud_fallback`
  - `original_target`
  - `fallback_target`
  - `blocking_reason`
- Not done if:
  - runtime fallback and cloud fallback are collapsed
  - explicit cloud appserver runtime requests can silently reroute
  - fallback cloud canary evidence is treated as required cloud execution
  - this prep stream edits runtime source or tests
- Pre-implementation issue-quality review:
  - 2026-04-27: approved for packet creation only. The micro-task path is inappropriate because correctness depends on exact protected wording, runtime/cloud contract separation, and CO-382 fallback decision tables.

## Milestones & Sequencing
1. Create the CO-396 PRD, canonical TECH_SPEC, TECH_SPEC mirror, ACTION_PLAN, task checklist, and `.agent` mirror.
2. Register the new canonical spec and checklist in `tasks/index.json`.
3. Add a current CO-396 snapshot to `docs/TASKS.md`.
4. Add docs freshness registry rows only for the six new CO-396 packet/checklist surfaces.
5. Run scoped prep validation:
   - JSON parse checks for edited registries
   - protected-term scan over packet files
   - `npm run docs:check`
   - `npm run docs:freshness`
   - scoped `git status --short` / `git diff --name-only`
6. Commit the packet locally.
7. Push and open a draft PR titled `CO-396 traceability packet` only if validation passes and auth works non-interactively.

## Parent-Owned Follow-On Plan
1. Reconcile live CO-396 issue text before leaving Backlog.
2. Run docs-review on the packet.
3. Inspect runtime-routing surfaces around appserver preflight fallback, strict blocked metadata, cloud/appserver unsupported behavior, runtime fallback policy aliases, and canary checks.
4. Decide whether each expiring fallback is removed or retained with refreshed expiry evidence before implementation handoff.
5. Keep `executionMode` and `runtimeMode` orthogonal.
6. Keep `runtime_fallback` and `cloud_fallback` separate.
7. Preserve required/fallback cloud canary semantics and record exact blockers when cloud evidence cannot run.
8. Run parent validation floor, standalone review, elegance pass, PR lifecycle, and Linear state handling.

## Dependencies
- `docs/guides/fallback-expiry-and-refactor-policy.md`
- `orchestrator/src/cli/runtime/provider.ts`
- `orchestrator/src/cli/runtime/codexCommand.ts`
- `orchestrator/src/cli/runtime/fallbackPolicy.ts`
- `orchestrator/src/cli/runtime/types.ts`
- `scripts/runtime-mode-canary.mjs`
- `scripts/cloud-canary-ci.mjs`
- cloud-mode docs

## Validation
- Checks / tests:
  - `node -e "JSON.parse(...)"` for `tasks/index.json` and `docs/docs-freshness-registry.json`
  - protected-term scan over the CO-396 packet files
  - `npm run docs:check`
  - `npm run docs:freshness`
  - scoped diff/status review
- Rollback plan:
  - revert only the CO-396 packet and registry mirror edits if parent issue reconciliation changes scope before implementation

## Risks & Mitigations
- Risk: CO-396 is read as permission to remove all runtime fallback support.
  - Mitigation: packet separates expiring auto reroutes from durable compatibility adapters, fail-fast metadata, audit schema, and validation canary contracts.
- Risk: cloud and runtime fallback semantics blur.
  - Mitigation: packet protects `runtime_fallback`, `cloud_fallback`, required cloud canary, and fallback cloud canary as distinct terms.
- Risk: implementation starts before traceability exists.
  - Mitigation: this packet is the Backlog-exit prerequisite and explicitly prohibits prep-lane runtime code changes.

## Approvals
- Docs-first packet: pending validation in this prep stream
- Parent implementation: pending
- Parent review: pending
