# ACTION_PLAN - CO workflow: restore repo-wide spec/docs freshness baselines after 30-day rollover regression

## Added by Bootstrap 2026-04-12

## Summary
- Goal: restore the live repo-wide freshness baseline by refreshing the March 12 stale cohort that has now rolled past the 30-day review cadence.
- Scope:
  - register the `CO-155` docs-first packet, registry mirrors, and single workpad source
  - run an audited `docs-review` child stream before implementation
  - refresh the March 12 stale cohort (`1121` through `1137`) across task metadata and registry rows
  - rerun `docs:freshness` and the required validation floor
- Assumptions:
  - the current repo blocker is the March 12 docs-freshness cohort, not a renewed stale-spec baseline
  - a bounded metadata refresh is the smallest truthful fix for the active tree

## Issue Readiness Gate
- Intent checksum / protected terms carried forward:
  - `repo-wide spec/docs freshness baselines`
  - `CO-94`
  - `spec-guard`
  - `docs:freshness`
  - `manual fallback notes`
- Not done if:
  - `spec-guard` regresses from green
  - `docs:freshness` still fails on the repaired tree
  - the closeout cannot explain the live stale cohort concretely
- Pre-implementation issue-quality review:
  - live repro shows one coherent March 12 stale cohort with no missing registry coverage and no invalid entries, so the lane stays bounded and auditable.

## Milestones & Sequencing
1. Register the `CO-155` docs-first packet, update `tasks/index.json`, `docs/TASKS.md`, `docs/docs-freshness-registry.json`, mirror the checklist, and stage the single workpad source.
2. Run `linear child-stream --pipeline docs-review` for the registered packet and record the manifest or truthful fallback if the child stops only on the existing repo baseline.
3. Refresh the March 12 stale cohort (`1121` through `1137`) consistently across task index rows, task specs, tech specs, and freshness-registry rows.
4. Rerun `node scripts/spec-guard.mjs --dry-run` and `npm run docs:freshness`, save post-fix artifacts, and update the workpad/checklist.
5. Run standalone review and an explicit elegance pass before any review handoff if the final diff remains non-trivial.

## Dependencies
- `scripts/docs-freshness.mjs`
- `docs/docs-freshness-registry.json`
- `tasks/index.json`
- the March 12 packet cohort `1121` through `1137`

## Validation
- Checks / tests:
  - `linear child-stream --pipeline docs-review`
  - `node scripts/spec-guard.mjs --dry-run`
  - `npm run docs:freshness`
  - repo validation floor if the diff remains non-trivial
- Rollback plan:
  - revert the metadata refresh if the rerun reveals a different stale cohort or if the repair introduces a new guardrail regression

## Risks & Mitigations
- Risk: the live stale set differs from the older issue snapshot.
  - Mitigation: use the saved April 12 report as source of truth and reject older cohorts unless they reappear in current artifacts.
- Risk: refreshing only one metadata surface leaves the cohort internally inconsistent.
  - Mitigation: update task index rows, spec frontmatter, and freshness-registry rows together for the active cohort.
- Risk: the lane drifts back into `CO-94` implementation work.
  - Mitigation: keep the packet and workpad explicit that `CO-94` code/test health is already green here.

## Approvals
- Reviewer: pending audited docs-review
- Date: 2026-04-12
