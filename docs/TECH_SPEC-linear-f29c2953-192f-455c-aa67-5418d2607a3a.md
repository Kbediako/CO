---
id: 20260418-linear-f29c2953-192f-455c-aa67-5418d2607a3a
title: Control host stabilize Doctor.test.ts full-suite timeout cluster under suite load
relates_to: docs/PRD-linear-f29c2953-192f-455c-aa67-5418d2607a3a.md
risk: high
owners:
  - Codex
last_review: 2026-04-18
---

## Canonical Reference
- Canonical TECH_SPEC: `tasks/specs/linear-f29c2953-192f-455c-aa67-5418d2607a3a.md`
- TECH_SPEC mirror: `docs/TECH_SPEC-linear-f29c2953-192f-455c-aa67-5418d2607a3a.md`
- PRD: `docs/PRD-linear-f29c2953-192f-455c-aa67-5418d2607a3a.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-f29c2953-192f-455c-aa67-5418d2607a3a.md`
- Task checklist: `tasks/tasks-linear-f29c2953-192f-455c-aa67-5418d2607a3a.md`

## Traceability
- Linear issue: `CO-229` / `f29c2953-192f-455c-aa67-5418d2607a3a`
- Source issue: `CO-216` / `8578d3d3-adc7-4391-8d69-0a0fa5e6e378`
- Source anchor: `ctx:sha256:ce8d79c8861c039782f62f47ed8c1e50aaa5e0c89390d66072caab9ae13d8df4#chunk:c000001`
- Parent-provided source payload reference: `.runs/linear-f29c2953-192f-455c-aa67-5418d2607a3a-docs-packet/cli/2026-04-18T00-31-50-715Z-81077b6b/memory/source-0/source.txt`
- Shared source note: the source object in this lane contains only run metadata; the exact issue description and checksum were copied from the live read-only `CO-229` issue body.
- Docs packet child lane manifest: `.runs/linear-f29c2953-192f-455c-aa67-5418d2607a3a-docs-packet/cli/2026-04-18T00-31-50-715Z-81077b6b/manifest.json`

## Summary
- Objective: isolate and fix the `Doctor.test.ts` full-suite timeout cluster under suite load so provider-worker validation stops failing for unrelated reasons.
- Scope:
  - docs-first packet and bounded registry/checklist mirrors for `CO-229`
  - explicit preservation of the five named `Doctor.test.ts` timeout cases
  - parent-owned reproduction of the suite-load timeout cluster under `npm run test`
  - parent-owned bounded fix that keeps isolated doctor coverage green
- Constraints:
  - child lane remains docs-only
  - no implementation, test, Linear, workpad, or PR mutations in this patch
  - keep the follow-up scoped to doctor-suite validation behavior rather than `CO-216` autopilot logic

## Issue-Shaping Contract
- User-request translation carried forward: this follow-up exists because full-suite validation for `CO-216` still fails in `orchestrator/tests/Doctor.test.ts` under suite load even after the operator-autopilot fix is stable, while the same file passes in isolation. The parent lane must isolate and fix that suite-load timeout behavior without reopening `CO-216` backlog-promotion or manual-demotion logic.
- Protected terms / exact artifact and surface names:
  - `Doctor.test.ts`
  - `full-suite`
  - `suite load`
  - `passes in isolation`
  - `timeouts`
  - `orchestrator/tests/Doctor.test.ts`
  - `npm run test`
  - `npx vitest run --config vitest.config.core.ts orchestrator/tests/Doctor.test.ts`
  - `reports direct-dist delegation readiness and initialize latency`
  - `keeps top-level doctor status ok when delegation is advisory but the rest of the install is healthy`
  - `keeps overall doctor status at warning when providers are incomplete`
  - `reports provider readiness when the repo is seeded and env is configured`
  - `resolves provider readiness from the repo root when doctor runs in a nested directory`
- Nearby wrong interpretations to reject:
  - this is another `CO-216` operator-autopilot bug
  - isolated `Doctor.test.ts` is failing on the same diff
  - this follow-up should reopen backlog-promotion or manual-demotion logic
  - this is a generic docs-packet bug rather than a doctor suite-load timeout issue
- Explicit non-goals carried forward:
  - do not reopen `CO-216` backlog-promotion or manual-demotion policy logic
  - do not broaden into generic doctor feature work unrelated to the timeout cluster
  - do not weaken validation by deleting the failing doctor coverage without replacing its signal
  - do not treat isolated doctor-file success as proof that the full-suite timeout cluster is acceptable

## Parity / Alignment Matrix
- Current truth:
  - `npm run test` on the settled `CO-216` diff failed with 5 `Doctor.test.ts` timeouts under suite load
  - the affected cases are:
    - `reports direct-dist delegation readiness and initialize latency`
    - `keeps top-level doctor status ok when delegation is advisory but the rest of the install is healthy`
    - `keeps overall doctor status at warning when providers are incomplete`
    - `reports provider readiness when the repo is seeded and env is configured`
    - `resolves provider readiness from the repo root when doctor runs in a nested directory`
  - the same diff passed `npx vitest run --config vitest.config.core.ts orchestrator/tests/Doctor.test.ts` in isolation with `47 passed`, duration about `39s`
- Reference truth:
  - full-suite validation should not fail for unrelated provider-worker lanes because of load-sensitive doctor timeouts
  - isolated doctor coverage should remain green
  - `CO-216` operator-autopilot logic should remain closed over its own scope
- Target truth / intended delta:
  - parent reproduces the timeout cluster and identifies the concrete suite-load interaction
  - a bounded fix removes the full-suite timeout cluster without masking or deleting the signal
  - isolated doctor coverage still passes after the fix
- Explicitly out-of-scope differences:
  - reopening `CO-216` backlog-promotion or manual-demotion logic
  - generic doctor feature work unrelated to the timeout cluster
  - docs-only reinterpretation as the completion path

## Readiness Gate
- Not done if:
  - `npm run test` still times out in the same `Doctor.test.ts` readiness cases under full-suite load
  - the investigation cannot explain why isolated doctor coverage passes while the full suite times out
  - the fix relies only on raising global timeouts without understanding or containing the suite-load behavior
  - the follow-up leaves provider-worker validation blocked in the same way for unrelated issue lanes
- Pre-implementation issue-quality review evidence:
  - 2026-04-18: the live `CO-229` issue body makes this lane narrower than `CO-216` operator-autopilot logic and narrower than generic doctor/docs work. The packet must preserve the exact contrast between `npm run test` suite-load timeouts and isolated `Doctor.test.ts` success, plus all five named cases. The micro-task path is ineligible because correctness depends on exact test names, exact command surfaces, and exact negative boundaries.
- Safeguard ownership split:
  - child lane owns only the packet files and listed registry/checklist mirrors
  - parent lane owns implementation, focused tests, docs-review, validation, workpad reconciliation, PR lifecycle, and patch integration

## Technical Requirements
1. Create the docs-first packet and bounded registry/checklist mirrors for `CO-229`.
2. Reproduce the `Doctor.test.ts` timeout cluster under `npm run test` with concrete evidence from the current suite.
3. Prove whether the root cause is load-sensitive doctor work, environment-sensitive probing, or another suite interaction.
4. Land a bounded fix so `npm run test` no longer times out in the affected doctor cases.
5. Preserve `npx vitest run --config vitest.config.core.ts orchestrator/tests/Doctor.test.ts` passing in isolation after the fix.
6. Keep the follow-up scoped to doctor-suite validation behavior rather than `CO-216` autopilot logic.

## Architecture & Data
- Architecture / design adjustments:
  - keep the investigation centered on the suite-load behavior exercised by the five named doctor readiness cases rather than reinterpreting the issue as control-host/autopilot policy logic
  - prefer one bounded stabilization of doctor readiness probing or doctor test harness behavior over blunt global timeout increases
  - preserve the existing doctor signal surface; the fix must not simply delete or bypass the slow cases
- Expected implementation surfaces:
  - `orchestrator/tests/Doctor.test.ts`
  - `orchestrator/src/cli/doctor.ts`
  - `orchestrator/src/cli/utils/delegationMcpHealth.ts`
  - `orchestrator/src/cli/utils/codexCli.ts`
  - `orchestrator/src/cli/utils/optionalDeps.ts`
  - `orchestrator/src/cli/control/linearDispatchSource.ts`
  - touch only the helper seams proven necessary by reproduction evidence
- Data model / artifact constraints:
  - preserve evidence of both failing full-suite behavior and passing isolated behavior until the root cause is explained
  - keep provider-worker validation meaningful for unrelated lanes after the fix

## Validation Plan
- Child-lane checks:
  - `jq empty tasks/index.json docs/docs-freshness-registry.json`
  - `git diff --check -- docs/PRD-linear-f29c2953-192f-455c-aa67-5418d2607a3a.md docs/TECH_SPEC-linear-f29c2953-192f-455c-aa67-5418d2607a3a.md docs/ACTION_PLAN-linear-f29c2953-192f-455c-aa67-5418d2607a3a.md tasks/specs/linear-f29c2953-192f-455c-aa67-5418d2607a3a.md tasks/tasks-linear-f29c2953-192f-455c-aa67-5418d2607a3a.md .agent/task/linear-f29c2953-192f-455c-aa67-5418d2607a3a.md tasks/index.json docs/TASKS.md docs/docs-freshness-registry.json`
  - `rg -n "Doctor.test.ts|full-suite|suite load|passes in isolation|timeouts|orchestrator/tests/Doctor.test.ts|npm run test|npx vitest run --config vitest.config.core.ts orchestrator/tests/Doctor.test.ts|reports direct-dist delegation readiness and initialize latency|keeps top-level doctor status ok when delegation is advisory but the rest of the install is healthy|keeps overall doctor status at warning when providers are incomplete|reports provider readiness when the repo is seeded and env is configured|resolves provider readiness from the repo root when doctor runs in a nested directory|CO-216" docs/PRD-linear-f29c2953-192f-455c-aa67-5418d2607a3a.md docs/TECH_SPEC-linear-f29c2953-192f-455c-aa67-5418d2607a3a.md docs/ACTION_PLAN-linear-f29c2953-192f-455c-aa67-5418d2607a3a.md tasks/specs/linear-f29c2953-192f-455c-aa67-5418d2607a3a.md tasks/tasks-linear-f29c2953-192f-455c-aa67-5418d2607a3a.md .agent/task/linear-f29c2953-192f-455c-aa67-5418d2607a3a.md`
- Parent-lane checks:
  - `npm run test`
  - `npx vitest run --config vitest.config.core.ts orchestrator/tests/Doctor.test.ts`
  - focused reruns for the five named `Doctor.test.ts` cases after the fix
  - parent-owned docs-review before implementation
- Rollback plan:
  - revert the bounded doctor/test stabilization if it masks real failures or broadens beyond the suite-load timeout cluster

## Open Questions
- Which probe or readiness path makes the five named `Doctor.test.ts` cases time out only under suite load while isolated file execution stays green?
- Does the smallest truthful fix live in doctor command code, shared readiness helpers, or the doctor test harness setup/teardown contract?
- How can the parent keep the timeout cluster reproducible enough to validate the fix without normalizing a broad global timeout increase?

## Approvals
- Reviewer: docs child lane self-review for packet shape and issue boundaries.
- Date: 2026-04-18
