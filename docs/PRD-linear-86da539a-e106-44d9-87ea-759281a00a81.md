# PRD - CO-395 review-wrapper fallback expiry

## Traceability
- Linear issue: `CO-395` / `86da539a-e106-44d9-87ea-759281a00a81`
- Linear URL: https://linear.app/asabeko/issue/CO-395
- Task id: `linear-86da539a-e106-44d9-87ea-759281a00a81`
- Canonical spec: `tasks/specs/linear-86da539a-e106-44d9-87ea-759281a00a81.md`
- Child lane manifest: `.runs/linear-86da539a-e106-44d9-87ea-759281a00a81-docs-packet-v2/cli/2026-04-26T20-13-09-221Z-1315cc57/manifest.json`
- Original source anchor: `ctx:sha256:2ad735bd7603aa57616c6fa318ee89a796462b103bed9568d6fd16928618bd1e#chunk:c000001`
- Docs packet child source anchor: `ctx:sha256:77c98bad7ca4cfe8e5367dc6eb6e20c1d9eff50fc4338300073a8c74303bb730#chunk:c000001`
- Source payload path from handoff: `../../.runs/linear-86da539a-e106-44d9-87ea-759281a00a81/cli/2026-04-26T20-07-39-220Z-8ba45809/memory/source-0/source.txt`
- Source payload note: the child lane used the parent-provided issue-shaping prompt; the parent branch was then rebased onto current `origin/main` and reconciled against the live CO-382 policy file.
- Policy note: `docs/guides/fallback-expiry-and-refactor-policy.md` is present on current `origin/main` and is authoritative for the required fallback expiry decision table.

## Summary
- Problem Statement: the `review wrapper` carries several compatibility and safety fallbacks around scoped prompt retry, synthesized title fallback, generated fallback notes, bounded-review fallback contracts, and review telemetry classification. CO-395 exists to decide which fallback paths should now be removed, expired, or justified before parent implementation touches the wrapper.
- Desired Outcome: create a docs-first packet that gives the parent lane an implementation-ready fallback expiry contract: remove any stale retry-without-scope fallback that cannot preserve explicit scope, expire temporary synthesized-title/artifact-only scoped prompt fallback metadata if retained, and justify retaining only durable command-intent bounded retry and telemetry classification contracts with focused validation.

## User Request Translation
- User intent / needs: prepare CO-395 docs-first artifacts for a strict docs phase, preserve the exact fallback-expiry wording and protected review-wrapper surfaces, and leave Linear/GitHub lifecycle plus implementation to the parent lane.
- Success criteria / acceptance:
  - PRD, canonical TECH_SPEC, TECH_SPEC mirror, ACTION_PLAN, task checklist, and `.agent` mirror exist for `linear-86da539a-e106-44d9-87ea-759281a00a81`
  - `tasks/index.json` links the canonical spec and checklist
  - `docs/TASKS.md` records a current CO-395 snapshot
  - `docs/docs-freshness-registry.json` covers the new CO-395 docs/checklist surfaces
  - the packet includes the fallback-expiry decision table required by policy
  - implementation validation is focused on docs/spec guard plus review-wrapper direct-success and retry/fallback test coverage
- Constraints / non-goals:
  - no Linear mutation helpers, workpad updates, GitHub PR commands, issue lifecycle commands, or PR lifecycle work in this child lane
  - no full repo validation suites from this child lane
  - do not weaken manifest-backed standalone review
  - do not weaken `FORCE_CODEX_REVIEW` provider handoff
  - do not change unrelated PR ready-review behavior
  - do not weaken telemetry semantics

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `review wrapper`
  - `fallback expiry`
  - `large refactor`
  - `minor seam`
  - `remove fallback`
  - `expire fallback`
  - `justify retaining fallback`
- Protected surfaces:
  - scoped prompt retry
  - synthesized title fallback
  - generated fallback notes
  - bounded-review fallback contracts
  - review telemetry classification
- Nearby wrong interpretations to reject:
  - "retry without explicit scope is acceptable because the saved artifact still has context"
  - "fallback expiry means removing command-intent protection"
  - "generated fallback notes can silently replace caller-supplied `NOTES`"
  - "telemetry can flatten artifact-only, scoped-title, and inline prompt delivery into one success bucket"
  - "CO-395 should become a large refactor of standalone review"

## Fallback Expiry Decision Table

| Review-wrapper fallback | Classification | Initial decision | Rationale | Validation expectation |
| --- | --- | --- | --- | --- |
| Retry without explicit scope after scoped prompt transport fails | Stale compatibility fallback | remove fallback | A retry that drops `--base`, `--commit`, or `--uncommitted` cannot preserve the requested review surface; saved artifacts are not enough to make the live review scoped. | Focused launch tests must fail if retry/fallback drops explicit scope. |
| Synthesized title fallback and artifact-only scoped prompt fallback metadata | Temporary compatibility fallback | expire fallback if retained | Bounded `--title` plus artifact-only metadata was a bridge for current Codex CLI transport limits. If implementation still needs it, metadata must carry an explicit expiry rationale rather than becoming permanent drift. | Focused launch and telemetry tests must prove scope is preserved and `reviewer_visible_context_transport` is truthful. |
| Generated fallback notes | minor seam safety fallback | justify retaining fallback | Empty `NOTES` should still produce bounded, explicitly marked context, but generated fallback notes must never override caller intent or pretend to be user-provided notes. | Prompt-context tests must cover absent `NOTES`, explicit `NOTES`, and title truncation behavior. |
| Command-intent bounded retry after validation-command boundary | Durable safety fallback | justify retaining fallback | The bounded retry preserves the original scope, adds reviewer-visible no-validation guidance, and keeps a second validation command as a fail-closed boundary. Removing it would weaken bounded-review fallback contracts. | Direct-success and command-intent retry/fallback tests must show bounded-success telemetry preserves the termination boundary and scope. |
| Review telemetry classification for launch context, fallback transport, and bounded outcomes | Durable audit contract | justify retaining fallback | Telemetry is the audit surface that distinguishes clean success, bounded success, failed boundary, inline prompt, scoped title, and artifact-only context. It is not optional fallback clutter. | Telemetry tests must preserve `review_outcome`, `termination_boundary`, `prompt_delivery`, and `reviewer_visible_context_transport` semantics. |

## CO-382 Fallback Metadata

| Fallback | Decision | Owner | Trigger | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Scope-dropping retry after scoped transport failure | remove fallback | Review wrapper / CO-395 | Codex rejects an explicit scoped review launch. | 2026-04-27 | None; remove in this lane. | No retry path can relaunch by omitting `--base`, `--commit`, or `--uncommitted`. | Focused launch tests assert explicit scope rejection fails closed. |
| Synthesized scoped-title fallback to artifact-only context | expire fallback | Review wrapper / CO-395 | Codex rejects synthesized scoped `--title` transport. | 2026-05-10 | 2026-05-26 | Audited Codex target accepts synthesized scoped titles for `--base`, `--commit`, and `--uncommitted`, or a larger review-wrapper prompt-transport refactor replaces the seam. | Launch and run-review tests cover same-scope retry, artifact-only telemetry, and direct success. |
| Generated fallback notes | justify retaining fallback | Review prompt context | `NOTES` is absent in a non-authoritative review. | 2026-05-26 | Non-expiring durable no-empty-context guard. | Remove only if all wrapper entrypoints require explicit `NOTES` or break-glass waiver evidence. | Prompt-context tests cover absent notes, explicit notes, waiver behavior, and title truncation. |
| Command-intent bounded retry | justify retaining fallback | Review execution boundary | First scoped bounded review attempts a validation or orchestration command. | 2026-05-26 | Non-expiring durable bounded-review safety contract. | Remove only when a replacement closeout contract preserves scope, no-validation guidance, read-only retry posture, and boundary telemetry. | Launch and run-review tests cover direct success, command-intent retry, and preserved bounded-success telemetry. |
| Review telemetry classification | justify retaining fallback | Review telemetry | Every review closeout. | 2026-05-26 | Non-expiring audit contract. | Remove only with a replacement telemetry schema that preserves clean success, bounded success, failed boundary, failed other, prompt delivery, and reviewer-visible context semantics. | Telemetry tests cover `review_outcome`, `termination_boundary`, `prompt_delivery`, and reviewer-visible context transport. |

## Parity / Alignment Matrix

| Surface | Current Truth | Reference Truth | Target Truth | Explicitly Out Of Scope |
| --- | --- | --- | --- | --- |
| Scoped prompt retry | Current wrapper has explicit scoped review compatibility paths and artifact/title fallback behavior. | Explicit scope is authoritative; fallback must not silently broaden the review. | Any retry/fallback keeps the exact requested scope or is removed. | Dropping scope to get a review verdict. |
| Synthesized title fallback | Bounded title context and artifact-only fallback metadata can preserve reviewer-visible context under scoped runs. | Temporary compatibility fallbacks need expiry or retention rationale. | Retained metadata is explicitly classified, expiry-reviewed, and truthful in telemetry. | Reintroducing inline prompt under explicit scope flags. |
| Generated fallback notes | Missing `NOTES` can trigger generated fallback notes. | Fallback notes are only a no-empty-context guard. | Keep only with explicit generated marker and focused tests. | Treating generated notes as user intent. |
| Bounded-review fallback contracts | Command-intent retry gives the reviewer one no-validation chance while preserving fail-closed behavior. | Manifest-backed standalone review must remain strict and auditable. | Retain as durable safety contract with direct-success and retry/fallback coverage. | Weakening manifest-backed review, `FORCE_CODEX_REVIEW`, or provider handoff. |
| Review telemetry classification | Launch context and outcomes are the audit trail. | Telemetry must explain what happened without hiding fallback paths. | Preserve exact classification semantics for prompt delivery, reviewer-visible context, and bounded outcomes. | Flattening telemetry or changing unrelated ready-review behavior. |

## Not Done If
- The packet omits the fallback expiry decision table.
- The packet fails to preserve `review wrapper`, `fallback expiry`, `large refactor`, `minor seam`, `remove fallback`, `expire fallback`, or `justify retaining fallback`.
- The parent cannot tell which fallbacks should be removed, expired, or justified.
- The plan allows a scoped review retry that drops explicit scope.
- The plan weakens manifest-backed standalone review, `FORCE_CODEX_REVIEW` provider handoff, unrelated PR ready-review behavior, or telemetry semantics.
- The child lane performs Linear/GitHub lifecycle work or full repo validation.

## Goals
- Create the CO-395 docs-first packet and mirrors.
- Preserve the protected review-wrapper surfaces and exact fallback-expiry wording.
- Provide implementation-ready fallback decisions for the parent lane.
- Register the packet in task and docs freshness mirrors.

## Non-Goals
- No code changes in this child lane.
- No Linear state, workpad, PR, issue-context, or lifecycle mutations.
- No broad standalone-review large refactor.
- No changes to unrelated ready-review or provider handoff behavior.
- No telemetry schema weakening or outcome flattening.

## Stakeholders
- Product: CO operators who rely on manifest-backed review evidence.
- Engineering: maintainers of `scripts/run-review.ts`, `scripts/lib/review-launch-attempt.ts`, prompt context, and review telemetry.
- Design: N/A.

## Metrics & Guardrails
- Primary Success Metrics:
  - packet and mirrors exist under the exact CO-395 task id
  - fallback expiry decisions are present and conservative
  - edited JSON registries parse
  - patch touches only declared docs/task registry files
- Guardrails / Error Budgets:
  - zero Linear/GitHub lifecycle commands
  - zero scope-dropping review fallback recommendations
  - zero full-suite validation from this child lane

## Technical Considerations
- Architectural Notes:
  - CO-395 is a docs-first implementation contract for the review wrapper, not a large refactor request.
  - The parent implementation is expected to inspect the current wrapper seams in `scripts/run-review.ts`, `scripts/lib/review-launch-attempt.ts`, `scripts/lib/review-prompt-context.ts`, and `scripts/lib/review-execution-telemetry.ts`.
  - The parent should keep changes at the smallest minor seam that satisfies fallback expiry decisions.
- Dependencies / Integrations:
  - `scripts/run-review.ts`
  - `scripts/lib/review-launch-attempt.ts`
  - `scripts/lib/review-prompt-context.ts`
  - `scripts/lib/review-execution-telemetry.ts`
  - `tests/review-launch-attempt.spec.ts`
  - `tests/review-prompt-context.spec.ts`
  - `tests/review-execution-telemetry.spec.ts`
  - `tests/run-review.spec.ts`

## Open Questions
- None. Parent reconciliation found the CO-382 fallback-expiry policy on current `origin/main`, and this packet treats it as authoritative before implementation.

## Approvals
- Product: parent CO-395 lane, pending
- Engineering: parent docs-review / implementation review, pending
- Design: N/A
