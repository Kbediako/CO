# ACTION_PLAN - CO-445 provider closeout live-owner guard

## Scope
Prevent `provider merge closeout` from moving a merged issue to `Done` while it is still the live `docs:freshness:maintain` `owner_issue` or cohort canonical owner. CO-431 remains related `canonical-owner automation`; this task owns `providerMergeCloseout.ts`.

## Plan
1. Use completed docs child-lane evidence and keep Linear/workpad synchronized.
2. Capture CO-444-shaped evidence: `pass_with_owned_rolling_debt`, `owner_issue=CO-444`, `blocking_changed_paths=[]`, live verification, `configured_owner_terminal`, and `block_unowned_repo_debt`.
3. After merge/shared-root reconciliation and before `Done`, resolve `docs:freshness:maintain -- --format json`, including `candidate_cohorts`.
4. If current issue is still owner-bearing, return action-required evidence and move/hold `Blocked`; otherwise preserve normal terminal closeout.
5. Project action-required closeout as stalled/watching merge, then validate, review, drain PR feedback, and hand off Linear.

## Constraints
No `docs:freshness` / `docs:freshness:maintain` weakening, direct owner re-home, docs-only final fix, provider merge validation weakening, ready-review weakening, shared-root closeout weakening, or silent CO-431 scope expansion.

## Validation
Focused provider closeout/observability tests; `git diff --check`; `node scripts/spec-guard.mjs --dry-run`; `npm run build`; `npm run lint`; `npm run test`; `npm run docs:check`; `npm run docs:freshness`; `npm run docs:freshness:maintain -- --format json`; `npm run repo:stewardship`; `node scripts/diff-budget.mjs`; standalone review; elegance; PR checks; `pr ready-review`.
