# ACTION_PLAN - CO-445 provider merge closeout live docs:freshness owner terminal guard

## Summary
- Goal: prevent provider merge closeout from completing a live `docs:freshness:maintain` owner issue to `Done` after a metadata PR merge.
- Scope: docs packet, focused `providerMergeCloseout.ts` guard, observability projection adjustment, focused regressions, validation, review, PR, and Linear handoff.
- Assumptions:
  - `providerMergeCloseout.ts` owns the terminal transition after merge/shared-root reconciliation
  - `docs:freshness:maintain -- --format json` is the machine-readable owner evidence surface
  - CO-431 remains related canonical-owner automation, not this provider closeout lifecycle fix

## Issue Readiness Gate
- Intent checksum / protected terms carried forward:
  - `provider merge closeout`
  - `metadata PR merge`
  - `docs:freshness:maintain`
  - `owner_issue`
  - `CO-444`
  - `configured_owner_terminal`
  - `block_unowned_repo_debt`
  - `blocking_changed_paths=[]`
  - `pass_with_owned_rolling_debt`
  - `live owner`
  - `canonical-owner automation`
  - `provider closeout lifecycle`
  - `origin/main`
  - `providerMergeCloseout.ts`
- Not done if:
  - closeout can still move a live `docs:freshness:maintain` owner to `Done`
  - real terminal-owner evidence is hidden or weakened
  - CO-431 is silently widened into this provider closeout lifecycle patch
  - the fix is only docs metadata or direct owner re-home
- Pre-implementation issue-quality review:
  - 2026-04-30: this issue is about terminal closeout behavior after merge, not metadata PR merge eligibility by itself.
  - 2026-04-30: micro-task path is unavailable because correctness depends on exact owner evidence, closeout lifecycle state, and the CO-431 boundary.
- Fallback / refactor decision:
  - Add a bounded terminal-transition guard while retaining docs freshness fail-closed owner checks.
  - Keep implementation inside provider merge closeout unless source inspection proves the terminal transition is elsewhere.

## Milestones & Sequencing
1. Reconcile live issue context and move Ready to In Progress.
2. Record parallelization decision and launch a bounded docs-packet child lane.
3. Import the child docs packet, then parent-correct its scope to the live-owner terminal guard after source inspection.
4. Capture current owner evidence with `docs:freshness:maintain -- --format json`.
5. Add live owner evidence resolution in `providerMergeCloseout.ts` before the `Done` transition.
6. When the current issue is still the live owner, return action-required evidence and move/hold the issue in `Blocked`.
7. When ownership is re-homed or retired, preserve normal `Done` closeout.
8. Adjust observability so merged action-required closeout is not projected as completed.
9. Add focused regressions for live-owner hold, no-longer-owner Done, and observability projection.
10. Run focused tests, normal validation gates, standalone review, elegance pass, PR lifecycle, ready-review drain, and Linear review handoff.

## Dependencies
- Source anchor: `ctx:sha256:e3cf9354b49bf4da338a134a6f2b07abc004119c12fa362d5cc4b7a2e44dbb0d#chunk:c000001`
- Child manifest: `.runs/linear-345cd50c-4962-4376-a7ca-1982ba311699-co445-docs-packet-r2/cli/2026-04-30T09-00-50-146Z-4d8a155f/manifest.json`
- Source surfaces: `orchestrator/src/cli/control/providerMergeCloseout.ts`, `orchestrator/src/cli/control/providerIssueObservability.ts`
- Test surfaces: `orchestrator/tests/ProviderMergeCloseout.test.ts`, `orchestrator/tests/ProviderIssueObservability.test.ts`

## Validation
- Focused checks:
  - `npm run test:core -- orchestrator/tests/ProviderMergeCloseout.test.ts --testTimeout=30000`
  - `npm run test:core -- orchestrator/tests/ProviderIssueObservability.test.ts --testTimeout=30000`
  - combined provider closeout/observability focused suite before handoff
- Required repo gates:
  - `git diff --check`
  - `node scripts/spec-guard.mjs --dry-run`
  - `npm run build`
  - `npm run lint`
  - `npm run test`
  - `npm run docs:check`
  - `npm run docs:freshness`
  - `npm run docs:freshness:maintain -- --format json`
  - `npm run repo:stewardship`
  - `node scripts/diff-budget.mjs`
  - manifest-backed `codex-orchestrator review` / `npm run review` under `FORCE_CODEX_REVIEW=1`
  - explicit elegance/minimality pass
- Rollback plan:
  - revert the provider closeout guard, observability projection change, focused tests, and CO-445 packet if validation proves the terminal transition is owned by a different lifecycle seam

## Risks & Mitigations
- Risk: live-owner action-required closeout is mistaken for merge failure.
  - Mitigation: evidence names `docs_freshness_owner`, `terminal_transition_blocked`, and the owner decision reason.
- Risk: normal non-owner closeout regresses.
  - Mitigation: focused no-longer-owner regression preserves `Done`.
- Risk: observability hides the action-required state.
  - Mitigation: projection regression keeps merged action-required closeout in `watching_merge` stalled state.
- Risk: adjacent CO-431 automation is widened.
  - Mitigation: packet and tests keep CO-431 as boundary context only.

## Approvals
- Docs-first packet: bounded same-issue child lane completed 2026-04-30; parent imported and corrected scope after implementation source inspection.
