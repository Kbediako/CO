# PRD - CO-391 installed control-host timeout and stale terminal suppression

## Summary
Installed `codex-orchestrator` / LaunchAgent still timed out after CO-376 because the global runtime was stale and local provider-intake residue could still count terminal merge-closeout handoff failures as active work.

Desired outcome: install a runtime containing CO-376 merge `5cc7be319` plus CO-391 stale-terminal suppression, then prove `co-status --format json` returns inside the default attach timeout with no `degraded_read.reason=ui_request_timeout`.

## Issue Contract
- User request: complete CO-391 by proving installed runtime provenance, fixing stale terminal active-status behavior, updating the LaunchAgent runtime, and capturing before/after timing, supervisor, package, active-ID, and Linear evidence.
- Protected terms: `CO STATUS: installed control-host still times out after CO-376 source fix`, `active_worker_probe_timeout_quarantine`, `provider_issue_merge_closeout_action_required`, `active_issue_identifiers`, `running_issue_identifiers`, `CO-381`.
- Reject: treating stale npm `0.2.1` as CO-376 source regression; broad status UI redesign; counting Done/completed local intake history as active capacity.

## Parity Matrix
- Current truth: installed `0.2.1` can take about 54s, report `ui_request_timeout`, leave supervision quarantined, and surface CO-381-style stale handoff residue.
- Reference truth: current source includes CO-376 and live Linear terminal state is authoritative over retained local history.
- Target truth: installed LaunchAgent uses current source behavior; terminal non-retry handoff failures and merge-closeout action-required residue are inactive/pruned; queued retries and active non-terminal handoff failures remain visible.
- Non-goal delta: no package publish/tag/release, no unrelated status UI or provider workflow rewrite.

## Not Done If
`CO-381` or any live Done/completed issue still appears as active/running/debug status work, `co-status --format json` still times out or reports `degraded_read.reason=ui_request_timeout`, or supervision remains in `active_worker_probe_timeout_quarantine`.

## Validation
Capture before/after package provenance, LaunchAgent path, direct `co-status` timing, active identifiers, supervisor status, focused tests, and live Linear cross-checks when request budget allows.
