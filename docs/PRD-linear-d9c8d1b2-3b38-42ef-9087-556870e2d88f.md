# PRD - CO: define preserved historical task-stub status semantics without triggering freshness or archive automation

## Traceability
- Linear issue: `CO-311` / `d9c8d1b2-3b38-42ef-9087-556870e2d88f`
- Linear URL: https://linear.app/asabeko/issue/CO-311/co-define-preserved-historical-task-stub-status-semantics-without
- Task id: `linear-d9c8d1b2-3b38-42ef-9087-556870e2d88f`
- Canonical spec: `tasks/specs/linear-d9c8d1b2-3b38-42ef-9087-556870e2d88f.md`
- Docs packet child manifest: `.runs/linear-d9c8d1b2-3b38-42ef-9087-556870e2d88f-docs-packet/cli/2026-04-22T12-20-49-557Z-ce9e900e/manifest.json`
- Docs packet child source anchor: `ctx:sha256:74b4c465c76042caa48680e8f6d1a67f1d95f056492747a6a11231781f41381a#chunk:c000001`
- Source payload: `.runs/linear-d9c8d1b2-3b38-42ef-9087-556870e2d88f-docs-packet/cli/2026-04-22T12-20-49-557Z-ce9e900e/memory/source-0/source.txt`
- Source payload note: source-0 contains run metadata plus issue id/identifier/updated_at only; the live issue description was reconciled read-only from Linear on 2026-04-22 so this packet preserves the current wording rather than guessing.

## Summary
- Problem Statement: while fixing `CO-308`, the repo preserved `tasks/tasks-linear-1c101ebc-4b86-4c1f-b04d-0455e50fbacb.md` as a historical task-key stub so current repo tooling can still resolve the canonical `linear-1c101ebc-4b86-4c1f-b04d-0455e50fbacb` key. The current status model is not truthful for that kind of preserved stub: leaving it `active` eventually turns it into ordinary docs-freshness debt, while marking it `archived` is not safe because implementation-doc archiving treats `archived` as immediate archive eligibility even though the stub still serves current tooling.
- Desired Outcome: define the truthful representation for preserved historical task-key stubs and document the bounded tooling/docs contract so future lanes do not have to choose between false active freshness debt and premature archive eligibility.

## User Request Translation
- User intent / needs: create the full docs-first packet for the CO-311 follow-up, preserve the exact protected wording from the live issue description, and hand the parent lane a bounded implementation contract for preserved historical task-stub semantics.
- Success criteria / acceptance:
  - PRD, canonical TECH_SPEC, TECH_SPEC mirror, ACTION_PLAN, task checklist, and `.agent` mirror exist for `linear-d9c8d1b2-3b38-42ef-9087-556870e2d88f`
  - `tasks/index.json` links the new canonical spec and checklist
  - `docs/TASKS.md` records a current CO-311 snapshot
  - `docs/docs-freshness-registry.json` covers only the new CO-311 packet/checklist surfaces
  - the packet preserves the `tasks/tasks-linear-1c101ebc-4b86-4c1f-b04d-0455e50fbacb.md` example, the `docs:freshness:maintain` canonical owner reference, and the exact non-goals / not-done-if wording from the issue
  - the packet stays bounded to preserved historical task-stub semantics and does not recreate missing historical packet files or reopen CO-308
- Constraints / non-goals:
  - do not reopen the CO-308 `docs:check` blocker fix
  - do not recreate the missing Apr 21 PRD / TECH_SPEC / ACTION_PLAN / spec / mirror files for `linear-1c101ebc-4b86-4c1f-b04d-0455e50fbacb`
  - do not silently archive the preserved historical stub just because it looks historical
  - do not weaken docs-freshness or archive automation broadly without a bounded preserved-stub contract
  - do not mutate Linear state from this child lane
  - do not run full repo validation suites from this child lane

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `tasks/tasks-linear-1c101ebc-4b86-4c1f-b04d-0455e50fbacb.md`
  - `docs/docs-freshness-registry.json`
  - `scripts/docs-freshness.mjs`
  - `scripts/implementation-docs-archive.mjs`
  - `active`
  - `archived`
  - `canonical task key`
  - `historical stub`
  - `docs:freshness:maintain`
  - `codex-orchestrator:canonical-owner-key=docs:freshness:maintain`
- Protected terms / exact artifact and surface names:
  - `CO-308`
  - `CO-311`
  - `preserved historical task-key stubs`
  - `false active freshness debt`
  - `immediate archive eligibility`
  - `Create before active work`
  - `Update registry mirrors before the issue leaves Backlog`
- Nearby wrong interpretations to reject:
  - "reopen the CO-308 `docs:check` blocker fix"
  - "recreate the missing Apr 21 PRD / TECH_SPEC / ACTION_PLAN / spec / mirror files"
  - "archive the preserved historical stub because it looks historical"
  - "broaden docs-freshness or archive automation beyond preserved stub handling"
  - "replace the bounded preserved-stub contract with a blanket archive-status migration"

## Parity / Alignment Matrix

| Surface | Current Truth | Reference Truth | Target Truth | Explicitly Out Of Scope |
| --- | --- | --- | --- | --- |
| Preserved historical task-key stub status | The live CO-311 description says the preserved stub example exists to keep the canonical `linear-1c101ebc-4b86-4c1f-b04d-0455e50fbacb` key resolvable, but `active` turns that stub into ordinary freshness debt. | A preserved historical stub that is still authoritative for current repo tooling should not be forced into a misleading ordinary-active status. | Define an explicit truthful representation for preserved historical task-key stubs, including when they remain authoritative and when they become archive-eligible. | Ad hoc per-lane status choices with no shared contract. |
| Docs freshness behavior | CO-311 cites `scripts/docs-freshness.mjs:556-576` as aging `active` and `deprecated` rows into stale-doc debt. | Freshness logic should treat preserved historical stubs consistently with their actual semantics rather than as normal active debt. | Update docs/tooling guidance so preserved stubs no longer become false active freshness debt by default. | Broad `docs:freshness` redesign unrelated to preserved historical stubs. |
| Implementation-doc archive behavior | CO-311 cites `scripts/implementation-docs-archive.mjs:617-689` as treating `archived` or `deprecated` rows as archive-eligible, which is too aggressive for a preserved stub that is still authoritative. | Archive automation should only archive artifacts once they are no longer authoritative for current tooling. | Preserve a bounded non-archive-eligible representation for authoritative historical stubs until explicit criteria are met. | Blanket archive-status migration for unrelated task packets or mirrors. |
| Historical packet lineage | The follow-up explicitly says not to recreate the missing Apr 21 packet files for `linear-1c101ebc-4b86-4c1f-b04d-0455e50fbacb`. | Preserved-stub semantics should let the repo remain truthful without reconstructing missing historical packet files. | A preserved-stub example like `tasks/tasks-linear-1c101ebc-4b86-4c1f-b04d-0455e50fbacb.md` can be represented truthfully without reopening CO-308 or recreating the missing packet. | Reconstructing the missing historical packet or turning CO-311 into a CO-308 follow-on repair. |

## Not Done If
- Preserved historical task-key stubs still have only two bad choices: ordinary active freshness debt or immediate archive eligibility.
- Repo tooling remains ambiguous about whether a preserved historical stub should be treated as active current evidence, an archive stub, or a third explicit class.
- A future lane preserving a task-key stub still has to hand-roll an ad hoc status choice without docs/tooling guidance.

## Goals
- Create the CO-311 docs-first packet and mirrors.
- Preserve the live issue wording around preserved historical task-key stubs, freshness debt, archive eligibility, and the example `tasks/tasks-linear-1c101ebc-4b86-4c1f-b04d-0455e50fbacb.md`.
- Bound parent implementation to preserved historical stub semantics across docs freshness and implementation-doc archiving.
- Keep CO-308 reopening and historical packet reconstruction explicitly out of scope.

## Non-Goals
- No reopening of CO-308 implementation scope.
- No broad docs-freshness policy redesign beyond preserved historical stub handling.
- No reconstruction of the missing historical packet files for `linear-1c101ebc-4b86-4c1f-b04d-0455e50fbacb`.
- No blanket archive-status migration for unrelated task packets or mirrors.
- No script or test edits in this child lane.

## Stakeholders
- Product: CO maintainers who need a truthful preserved-stub contract instead of repeated ad hoc status decisions.
- Engineering: docs/tooling owners for `scripts/docs-freshness.mjs`, `scripts/implementation-docs-archive.mjs`, `tasks/index.json`, and `docs/docs-freshness-registry.json`.
- Design: N/A.

## Metrics & Guardrails
- Primary Success Metrics:
  - CO-311 packet and mirrors exist under the exact task id
  - protected terms, non-goals, and not-done-if language from the live issue are preserved verbatim or near-verbatim where needed
  - `tasks/index.json` parses as JSON
  - `docs/docs-freshness-registry.json` parses as JSON
  - child-lane patch touches only declared files
- Guardrails / Error Budgets:
  - zero Linear mutations from this child lane
  - zero recreated Apr 21 historical packet files
  - zero scope creep into broad docs-freshness or archive-policy redesign
  - zero script/test edits in this child lane

## Technical Considerations
- Architectural Notes:
  - CO-311 is a status-model and tooling-alignment follow-up, not a packet reconstruction lane.
  - The live issue description explicitly cites `scripts/docs-freshness.mjs:556-576` and `scripts/implementation-docs-archive.mjs:617-689` as the surfaces exposing the current gap.
  - The packet must preserve `docs:freshness:maintain` as the canonical owner reference, not replace it with a new owner scheme.
  - The child lane supplies docs-first packet and machine-readable mirrors only; the parent owns implementation, review, and validation.
- Dependencies / Integrations:
  - `tasks/tasks-linear-1c101ebc-4b86-4c1f-b04d-0455e50fbacb.md` as the preserved-stub example named by CO-311
  - `docs/docs-freshness-registry.json`
  - `scripts/docs-freshness.mjs`
  - `scripts/implementation-docs-archive.mjs`
  - `tasks/index.json`
  - `docs/TASKS.md`

## Open Questions
- Parent implementation must choose the smallest truthful representation for preserved historical task-key stubs: an explicit third status, a bounded metadata flag, or an equivalent contract that both freshness and archive tooling consume consistently.

## Approvals
- Product: parent CO-311 lane, pending
- Engineering: parent docs-review / implementation review, pending
- Design: N/A
