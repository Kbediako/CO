# PRD - CO-364 RLM GPT-5.5 Route Posture

## Traceability
- Linear issue: `CO-364` / `linear-7f6b0776-51b8-4f54-b330-b4e359afe829`
- Branch: `linear/co-364-rlm-gpt55-posture`

## Summary
- Problem Statement: `orchestrator/src/cli/rlm/alignment.ts` still defaults `sentinel_model`, `high_reasoning_model`, and `arbitration_model` to `gpt-5.4` even after CO-352 established `gpt-5.5` / `xhigh` as the validated CO-local posture for eligible ChatGPT-auth/appserver surfaces.
- Desired Outcome: the RLM alignment route defaults use `gpt-5.5` where applicable, focused tests prove default and override behavior, and adjacent spark/file-search exceptions remain untouched.

## User Request Translation
- Implement the CO-364 posture correction in the requested dedicated worktree from latest `origin/main`.
- Keep the code change small and scoped to RLM route defaults plus nearby regression coverage.
- Preserve env/config route overrides and do not change `explorer_fast`, spark, provider-worker, appserver, or workflow surfaces owned by other active lanes.

## Intent Checksum
- Exact wording to preserve:
  - `sentinel_model`
  - `high_reasoning_model`
  - `arbitration_model`
  - `gpt-5.5`
  - `xhigh`
  - `explorer_fast`
  - `spark exceptions`
- Protected surfaces:
  - `orchestrator/src/cli/rlm/alignment.ts`
  - `orchestrator/tests/RlmAlignment.test.ts`
  - `DEFAULT_ALIGNMENT_POLICY.route`
  - route `policy` override merging
- Nearby wrong interpretations to reject:
  - changing shipped downstream templates or generic packaged defaults
  - changing `explorer_fast` / spark behavior
  - weakening route override behavior
  - broadening into CO-356 workflow files or CO-360 provider-worker/appserver files

## Parity / Alignment Matrix
- Current truth: CO-352 has merged and validates `gpt-5.5` / `xhigh` for CO-local eligible surfaces, but RLM route defaults still name `gpt-5.4`.
- Reference truth: RLM alignment route defaults should reflect the current CO-local high-reasoning posture while allowing explicit route policy overrides.
- Target truth / intended delta: `sentinel_model`, `high_reasoning_model`, and `arbitration_model` default to `gpt-5.5`; `high_reasoning_available` remains true; override merging remains additive.
- Explicitly out-of-scope differences: generic downstream templates, `explorer_fast`, spark/file-search exceptions, cloud/release posture, provider-worker runtime promotion, and appserver control seam changes.

## Not Done If
- Any RLM route default remains `gpt-5.4`.
- Tests do not prove both default posture and explicit route overrides.
- The change touches unrelated CO-356 or CO-360 surfaces.
- Docs/registry/checklist mirrors for CO-364 are missing.

## Goals
- Align RLM route defaults with the merged CO-local `gpt-5.5` posture.
- Preserve explicit policy override semantics.
- Validate through focused RLM tests and required repo/docs gates.

## Non-Goals
- Broader model catalog policy changes.
- Generic downstream default promotion.
- Runtime-mode, provider-worker, cloud, or spark role changes.

## Stakeholders
- Product: CO maintainers relying on truthful model posture.
- Engineering: RLM/alignment route maintainers and review agents.
- Design: Not applicable.

## Metrics & Guardrails
- Primary Success Metrics: focused RLM tests pass, full build/test gates pass or blockers are reported with evidence, docs gates accept the packet.
- Guardrails / Error Budgets: no unrelated surface edits; route overrides remain deterministic.

## Technical Considerations
- Architectural Notes: the existing `resolvePolicy` merge can keep override behavior unchanged if the default constants are updated directly.
- Dependencies / Integrations: Vitest focused coverage in `orchestrator/tests/RlmAlignment.test.ts`.

## Open Questions
- None for this bounded lane.

## Approvals
- Product: encoded by Linear issue CO-364 and user task.
- Engineering: parent worker self-review plus local validation.
- Design: Not applicable.
