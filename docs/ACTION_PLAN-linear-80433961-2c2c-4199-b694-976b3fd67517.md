# ACTION_PLAN - CO: stabilize LockFile keepalive timing in full-suite runs

## Summary
- Goal: give the parent lane a bounded implementation plan for the ambient full-suite `LockFile.test.ts` keepalive timing flake while preserving `acquireLockWithRetry`, `createLockKeepalive`, stale orphan recovery, and full `npm run test` as the required proof.
- Scope: docs-first packet, registry/checklist mirrors, parent-owned LockFile timing diagnosis, parent-owned minimal test or primitive stabilization, focused LockFile validation, and parent-owned full `npm run test`.
- Assumptions:
  - the source payload is absent in this child checkout, so the parent prompt and current LockFile surfaces are the authoritative checksum available to this lane
  - the flake is ambient/full-suite timing-sensitive because the split-out note says isolated reruns passed while full `npm run test` eventually completed cleanly in a separate lane
  - the smallest correct fix should keep lock semantics intact rather than hiding the timing contract

## Issue Readiness Gate
- Intent checksum / protected terms carried forward:
  - `orchestrator/tests/LockFile.test.ts`
  - `acquireLockWithRetry`
  - `createLockKeepalive`
  - full `npm run test`
  - live lock
  - stale lock
  - keepalive timing
  - owner token
- Not done if:
  - full `npm run test` can still fail in `orchestrator/tests/LockFile.test.ts` due to keepalive timing
  - the fix only inflates sleeps, retries, or suite timeouts without making live-lock freshness deterministic
  - `acquireLockWithRetry` can stale-take a live lock that still has an active keepalive
  - `createLockKeepalive` stops refreshing too early or keeps refreshing after release
  - stale orphan recovery regresses
  - the lane drifts into `CO-235`, generic full-suite timeout work, or unrelated persistence lock consumers
- Pre-implementation issue-quality review:
  - 2026-04-18: the issue is not plausibly narrower than the LockFile keepalive timing contract because the protected surfaces explicitly include the test, acquisition helper, keepalive helper, and full-suite proof command.

## Milestones & Sequencing
1. Create the docs-first packet and mirrors for `CO-234` within the declared docs scope.
2. Parent confirms the current LockFile failure mode from the previous full-suite evidence or reruns a focused/full-suite reproduction on current head.
3. Parent inspects `orchestrator/tests/LockFile.test.ts` and `orchestrator/src/persistence/lockFile.ts` for the smallest deterministic timing repair.
4. Parent preserves stale orphan recovery and owner-token release behavior while stabilizing live-lock freshness.
5. Parent runs focused `npm run test:orchestrator -- orchestrator/tests/LockFile.test.ts`.
6. Parent runs full `npm run test` and records the outcome as the closeout proof.
7. Parent carries the packet into docs-review, normal validation, review, and PR lifecycle as required by the authoritative issue workspace.

## Dependencies
- Shared source anchor: `ctx:sha256:2b498d1162b00fb3e2b9a5ab23a18ea8fba830298bd8865f8b7bcf08bce46163#chunk:c000001`
- Origin manifest: `.runs/linear-80433961-2c2c-4199-b694-976b3fd67517-docs-packet/cli/2026-04-18T16-15-30-031Z-5847f253/manifest.json`
- Split-out evidence: `tasks/tasks-linear-a0ef4f6c-bf78-4dd6-9d43-244c445b87cb.md`
- Parent implementation seams:
  - `orchestrator/tests/LockFile.test.ts`
  - `orchestrator/src/persistence/lockFile.ts`
  - `acquireLockWithRetry`
  - `createLockKeepalive`

## Validation
- Child lane only:
  - `node -e "JSON.parse(require('node:fs').readFileSync('tasks/index.json','utf8')); JSON.parse(require('node:fs').readFileSync('docs/docs-freshness-registry.json','utf8'));"`
  - `rg -n "orchestrator/tests/LockFile.test.ts|acquireLockWithRetry|createLockKeepalive|full npm run test|npm run test|keepalive timing|CO-235|SelectedRunProjection" docs/PRD-linear-80433961-2c2c-4199-b694-976b3fd67517.md docs/TECH_SPEC-linear-80433961-2c2c-4199-b694-976b3fd67517.md docs/ACTION_PLAN-linear-80433961-2c2c-4199-b694-976b3fd67517.md tasks/specs/linear-80433961-2c2c-4199-b694-976b3fd67517.md tasks/tasks-linear-80433961-2c2c-4199-b694-976b3fd67517.md .agent/task/linear-80433961-2c2c-4199-b694-976b3fd67517.md`
  - `git diff --check -- docs/PRD-linear-80433961-2c2c-4199-b694-976b3fd67517.md docs/TECH_SPEC-linear-80433961-2c2c-4199-b694-976b3fd67517.md docs/ACTION_PLAN-linear-80433961-2c2c-4199-b694-976b3fd67517.md tasks/specs/linear-80433961-2c2c-4199-b694-976b3fd67517.md tasks/tasks-linear-80433961-2c2c-4199-b694-976b3fd67517.md .agent/task/linear-80433961-2c2c-4199-b694-976b3fd67517.md tasks/index.json docs/TASKS.md docs/docs-freshness-registry.json`
- Parent implementation lane:
  - `npm run test:orchestrator -- orchestrator/tests/LockFile.test.ts`
  - repeated or loaded focused LockFile rerun if needed to prove timing stability
  - full `npm run test`
  - `node scripts/spec-guard.mjs --dry-run`
  - parent-selected docs/review/PR validation floor
- Rollback plan:
  - revert the timing or primitive change if it weakens stale orphan recovery, permits live lock stale-takeover, or turns owner-token release into best-effort deletion

## Risks & Mitigations
- Risk: the fix overfits a sleep value and remains flaky under full-suite load.
  - Mitigation: require full `npm run test` as the acceptance proof and reject timeout-only explanations.
- Risk: stabilizing the live-lock test weakens stale orphan recovery.
  - Mitigation: keep stale orphan recovery as a protected acceptance criterion and focused regression.
- Risk: parent broadens into `CO-235` because both were split out from ambient full-suite behavior.
  - Mitigation: keep `CO-235` and `SelectedRunProjection` explicit in non-goals.
- Risk: source payload absence causes issue drift.
  - Mitigation: preserve the parent prompt's protected surfaces exactly and record the absence in traceability.

## Approvals
- Docs packet child lane: `.runs/linear-80433961-2c2c-4199-b694-976b3fd67517-docs-packet/cli/2026-04-18T16-15-30-031Z-5847f253/manifest.json`
- Parent docs-review: pending parent acceptance
- Parent implementation/review/PR lifecycle: pending parent lane
