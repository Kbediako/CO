# PRD - CO-363 CO STATUS active issue projection and current-host-unhealthy classification

## Added by Docs Child Lane 2026-04-25

## Traceability
- Linear issue: `CO-363` / `16623322-4b8d-4e12-8f57-688aa5f71a23`
- MCP Task ID: `linear-16623322-4b8d-4e12-8f57-688aa5f71a23`
- Canonical task ID: `20260425-linear-16623322-4b8d-4e12-8f57-688aa5f71a23`
- Canonical TECH_SPEC: `tasks/specs/linear-16623322-4b8d-4e12-8f57-688aa5f71a23.md`
- Docs TECH_SPEC: `docs/TECH_SPEC-linear-16623322-4b8d-4e12-8f57-688aa5f71a23.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-16623322-4b8d-4e12-8f57-688aa5f71a23.md`
- Task checklist: `tasks/tasks-linear-16623322-4b8d-4e12-8f57-688aa5f71a23.md`
- Agent-task mirror: `.agent/task/linear-16623322-4b8d-4e12-8f57-688aa5f71a23.md`
- Source anchor: `ctx:sha256:c98bd9a7b2ae5c5d558c5f43b9145ac007b6cd61e7aafe81a749db4eac4429e0#chunk:c000001`
- Docs packet child lane manifest: `.runs/linear-16623322-4b8d-4e12-8f57-688aa5f71a23-docs-packet/cli/2026-04-25T01-30-33-657Z-a2ee411b/manifest.json`
- Source payload declared by parent: `.runs/linear-16623322-4b8d-4e12-8f57-688aa5f71a23-docs-packet/cli/2026-04-25T01-30-33-657Z-a2ee411b/memory/source-0/source.txt`
- Source payload note: the expected shared `source-0` payload is not present in this child checkout because the lane workspace has no `.runs` tree. This packet is anchored on the parent-provided handoff text, protected terms, and nearby repo observability/status contracts only.

## Summary
- Problem Statement: `CO STATUS`, `co-status --format json`, and `/ui/data.json` need a truthful contract for active issue projection when the local `control-host` or `control_endpoint.json` path is unhealthy. The active issue projection must preserve active identifiers and counts, including `CO-345`, `CO-356`, and `CO-330`, and must carry reopened issue metadata without confusing host-health classification with issue lifecycle truth.
- Desired Outcome: parent implementation makes `CO STATUS`, `co-status --format json`, and `/ui/data.json` agree on active issue identifiers/counts and reopened metadata while surfacing `current-host-unhealthy` as an explicit host-health classification. `provider-intake-state.json` remains retained evidence, not a destructive cleanup target, and related issue references stay scoped instead of absorbing CO-363 into CO-345, CO-356, or CO-330.

## User Request Translation (Context Anchor)
- User intent / needs (in your own words): create the docs-first packet and task mirrors for `CO-363` only before parent implementation. Preserve the exact status/control-host surfaces and issue identifiers named by the handoff, especially `CO STATUS`, `control-host`, `co-status --format json`, `/ui/data.json`, `control_endpoint.json`, `provider-intake-state.json`, `active issue projection`, `active identifiers and counts`, `CO-345`, `CO-356`, `CO-330`, `reopened issue metadata`, and `current-host-unhealthy classification`.
- Success criteria / acceptance:
  - docs-first packet and mirrors are present under the declared CO-363 task id
  - active identifiers and counts are preserved as first-class acceptance criteria for `CO STATUS`, `co-status --format json`, and `/ui/data.json`
  - `current-host-unhealthy classification` is recorded as host-health truth, not as permission to drop or fabricate active issue projection
  - reopened issue metadata is explicitly preserved for `CO-345`, `CO-356`, `CO-330`, and any sibling active rows the parent validates
  - `provider-intake-state.json` remains an evidence source and audit ledger rather than a file to delete or flatten
  - parent-owned implementation, focused tests, docs-review, validation, Linear/workpad state, and PR lifecycle stay outside this child lane
- Constraints / non-goals:
  - child lane owns only the declared docs-phase files
  - no implementation, test, generated-artifact, Linear mutation, workpad mutation, or PR lifecycle edits
  - no full repo validation suites in this child lane
  - no claim that CO-345, CO-356, or CO-330 is completed, reopened, or superseded without parent-owned live issue evidence

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `CO STATUS`
  - `control-host`
  - `co-status --format json`
  - `/ui/data.json`
  - `control_endpoint.json`
  - `provider-intake-state.json`
  - `active issue projection`
  - `active identifiers and counts`
  - `CO-345`
  - `CO-356`
  - `CO-330`
  - `reopened issue metadata`
  - `current-host-unhealthy classification`
- Protected terms / exact artifact and surface names:
  - `CO-363`
  - `linear-16623322-4b8d-4e12-8f57-688aa5f71a23`
  - `CO STATUS`
  - `control-host`
  - `co-status --format json`
  - `/ui/data.json`
  - `control_endpoint.json`
  - `provider-intake-state.json`
  - `active issue projection`
  - `active identifiers and counts`
  - `reopened issue metadata`
  - `current-host-unhealthy classification`
  - `CO-345`
  - `CO-356`
  - `CO-330`
  - `ctx:sha256:c98bd9a7b2ae5c5d558c5f43b9145ac007b6cd61e7aafe81a749db4eac4429e0#chunk:c000001`
- Nearby wrong interpretations to reject:
  - treating CO-363 as a terminal-dashboard cosmetic change only
  - letting a `current-host-unhealthy classification` erase active issue identifiers or counts
  - counting stale terminal rows as active only because old `provider-intake-state.json` history exists
  - destructively deleting `provider-intake-state.json`, `.runs` artifacts, or endpoint files as the fix
  - using CO-363 to implement CO-345 docs changes, CO-356 unknown sibling work, or CO-330 stale control-host owner recovery
  - treating `co-status` local output alone as authoritative Linear truth without parent-owned issue/context verification
  - mutating Linear state, workpads, PRs, or implementation files from this docs-only child lane

## Parity / Alignment Matrix

| Surface | Current truth | Reference truth | Target truth / intended delta | Explicitly out-of-scope differences |
| --- | --- | --- | --- | --- |
| `CO STATUS` terminal view | The operator view can depend on local control-host/read-model state and may become ambiguous when the current host is unhealthy. | `CO STATUS` should show current work truth and separate host-health classification from issue lifecycle truth. | Parent keeps active issue projection stable and visible while surfacing `current-host-unhealthy classification` as a distinct status dimension. | No terminal renderer redesign beyond the projection/classification contract. |
| `co-status --format json` | JSON reads through `control_endpoint.json` and `/ui/data.json`; endpoint or host health issues can be mistaken for issue-count truth. | Machine JSON should preserve `active identifiers and counts` and expose host-health failures explicitly. | `co-status --format json` and `/ui/data.json` agree on active issue identifiers/counts and host-health metadata. | No new public API family or broad attach recovery redesign unless parent proves it is necessary. |
| `/ui/data.json` | The control-host in-process dataset is the upstream JSON source for status surfaces. | `/ui/data.json` should be the stable projection source when `ui.controlEnabled` is active and the host is reachable. | Parent validates `/ui/data.json` carries the same active projection, reopened issue metadata, and current-host-unhealthy classification expected by `co-status`. | No frontend UI redesign. |
| `provider-intake-state.json` | Retained provider claims can contain active, released, retry, or historical state. | Intake state is evidence for status projection, not a destructive cleanup target. | Parent uses intake state plus live/reopened issue metadata to classify active rows without deleting history. | No local artifact pruning as the product fix. |
| Reopened issue metadata | Reopened or reactivated issues can be misread as stale terminal residue or as generic active rows without reopened context. | Reopened metadata must remain visible enough to distinguish live reopened work from stale completed rows. | Parent preserves reopened metadata for `CO-345`, `CO-356`, `CO-330`, and validated sibling active rows in active issue projection. | No unsupported Linear mutation or issue-state claims from this child lane. |
| Related issue boundaries | CO-345, CO-356, and CO-330 are named context and identifiers that must not be lost. | Related issues sharpen requirements but do not replace CO-363 unless parent evidence proves duplicate ownership. | CO-363 remains the status-projection/current-host-health lane and references related identifiers only to preserve active projection semantics. | No implementation of CO-345, CO-356, or CO-330 scope inside this packet. |

## Not Done If
- `CO STATUS`, `co-status --format json`, or `/ui/data.json` can still disagree on active issue identifiers or counts for the same control-host snapshot.
- `current-host-unhealthy classification` is missing, renamed, or used to hide active issue projection.
- reopened issue metadata for `CO-345`, `CO-356`, or `CO-330` is dropped, flattened into stale terminal history, or treated as proof of a different issue's completion.
- stale `provider-intake-state.json` history can inflate active identifiers/counts without live or reopened issue corroboration.
- the fix deletes local intake/endpoint history instead of correcting projection and classification semantics.
- the lane broadens into CO-345 docs work, CO-356 sibling implementation, CO-330 stale-owner recovery, Linear mutation, or PR lifecycle work.

## Goals
- Create the CO-363 docs-first packet and task mirrors inside the declared file scope.
- Preserve the exact status/control-host surface names, related issue identifiers, parity matrix, and Not Done If conditions.
- Give the parent lane a bounded implementation contract for active issue projection, active identifiers/counts, reopened metadata, and current-host-unhealthy classification.
- Keep `provider-intake-state.json` as auditable evidence and avoid destructive local-state cleanup as the solution.

## Non-Goals
- No implementation, test, script, generated output, Linear, workpad, PR, or validation-suite edits in this child lane.
- No broad `CO STATUS` renderer or UI redesign.
- No destructive cleanup of `provider-intake-state.json`, `control_endpoint.json`, or `.runs` artifacts as the product fix.
- No direct implementation of CO-345, CO-356, or CO-330.
- No claim that a local unhealthy host alone proves issue lifecycle state.

## Stakeholders
- Product: CO operators relying on `CO STATUS` and JSON status to understand current work.
- Engineering: control-host, status projection, provider-intake, and issue lifecycle/read-model maintainers.
- Design: Not applicable.

## Metrics & Guardrails
- Primary Success Metrics:
  - active identifiers/counts match across `CO STATUS`, `co-status --format json`, and `/ui/data.json`
  - `current-host-unhealthy classification` is explicit and machine-checkable
  - reopened issue metadata remains visible for the protected related issue identifiers
  - stale provider-intake history does not inflate active counts without corroboration
- Guardrails / Error Budgets:
  - preserve audit evidence in `provider-intake-state.json`
  - keep host-health and issue-lifecycle truth separate
  - fail closed when host health or Linear issue truth cannot be verified
  - avoid broad status/dashboard scope expansion

## User Experience
- Personas:
  - operator reading `CO STATUS` during a local host-health incident
  - maintainer comparing `co-status --format json`, `/ui/data.json`, `control_endpoint.json`, and `provider-intake-state.json`
  - reviewer checking that reopened issue metadata did not disappear from active projection
- User Journeys:
  - operator sees current active issue identifiers/counts and separately sees whether the current host is unhealthy
  - maintainer can trace an active row back to provider-intake and reopened issue metadata without deleting local state
  - reviewer can verify that CO-345, CO-356, and CO-330 are preserved as projection identifiers/context rather than accidentally absorbed into another lane

## Technical Considerations
- Architectural Notes:
  - `co-status --format json` reads the local control-host through `control_endpoint.json` and `/ui/data.json`
  - `/ui/data.json` is served by the in-process control server and should carry the status dataset used by `CO STATUS`
  - `provider-intake-state.json` should remain retained state that informs projection alongside live issue metadata and host health
  - the likely parent seams are status/read-model projection, selected-run/provider-intake reconciliation, and co-status attach/read behavior
  - current-host health must be a separate classification dimension from active issue lifecycle state
- Dependencies / Integrations:
  - `orchestrator/src/cli/coStatusCliShell.ts`
  - `orchestrator/src/cli/coStatusAttachCliShell.ts`
  - `orchestrator/src/cli/control/uiDataController.ts`
  - `orchestrator/src/cli/control/observabilityReadModel.ts`
  - `orchestrator/src/cli/control/observabilitySurface.ts`
  - `orchestrator/src/cli/control/selectedRunProjection.ts`
  - `orchestrator/src/cli/control/providerIssueHandoff.ts`
  - parent-owned live issue/context verification for `CO-345`, `CO-356`, and `CO-330`

## Open Questions
- Which exact read-model field should carry `current-host-unhealthy classification` so terminal and JSON consumers do not diverge?
- Should reopened issue metadata appear on every active row, only rows with reopened evidence, or as a projection-level annotation?
- What minimum parent-owned live issue evidence is required before `CO-345`, `CO-356`, or `CO-330` is treated as active/reopened rather than historical context?

## Approvals
- Product: CO-363 issue scope provided by parent lane.
- Engineering: docs child lane self-review for packet shape and issue boundaries; parent owns docs-review, implementation, validation, Linear/workpad state, and PR lifecycle.
- Design: Not applicable.
