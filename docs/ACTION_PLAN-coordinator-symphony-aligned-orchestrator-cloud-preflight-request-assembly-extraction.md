# ACTION PLAN: Coordinator Symphony-Aligned Orchestrator Cloud-Preflight Request Assembly Extraction

1. Register `1171` docs-first and sync task mirrors/index/registry.
2. Narrow the remaining cloud-preflight request assembly inside `orchestratorExecutionRouter.ts` without widening the seam beyond router-local behavior.
3. Add or extend focused router tests for the preflight-request contract only if the extracted seam becomes independently meaningful.
4. Run the required validation bundle and close the lane with explicit override notes if the recurring full-test quiet-tail or review-wrapper drift recurs.

