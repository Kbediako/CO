# PRD - CO-169 Provider Worker Issue Worktree Root Authority
Problem/goal/acceptance: provider-worker helpers may run in `.workspaces/linear-<issue-id>` while inherited env points at `/Users/kbediako/Code/CO`; make the issue worktree authoritative for child streams/review wrappers, preserve `provider_worker_child_stream_context_missing` traceability and workspace-scoped evidence, reject guard weakening/CO-93 rework/review policy/shared-root `Merging` changes, and finish only when helpers resolve the active worktree, review sees the issue diff, and regressions cover the mismatch.
