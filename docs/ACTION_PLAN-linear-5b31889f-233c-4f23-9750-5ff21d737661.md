# ACTION_PLAN - CO child-lane decisions repair stale pending ledger after post-startup launch-shell miss

## Summary
- Goal: repair stale pending `launching-*` ledger rows in parent-owned child-lane decision flows without widening `CO-210` or weakening child-lane provenance checks.
- Scope: docs-first packet and mirrors, bounded same-issue child-lane retry, parent-owned implementation in `providerLinearChildLaneShell`, focused regression coverage, parent-owned review/handoff.
- Assumptions: the shared source-0 payload for this provider-worker run is metadata/provenance only; the current issue body was read via the packaged `linear issue-context` helper; STATUS/progress hydration is already handled separately by `CO-210`.

## Issue Readiness Gate
- Intent checksum / protected terms carried forward:
  - `provider-linear-worker-child-lanes.json`
  - `provider-linear-child-lane`
  - `providerLinearChildLaneShell`
  - `launching-*`
  - `accept` / `reject` / `invalidate`
  - child-lane manifest
  - child-lane proof
  - `parent_run_id`
  - `issue_id`
  - `issue_identifier`
  - `pipeline_id=provider-linear-child-lane`
- Not done if:
  - parent decisions still fail on a stale `launching-*` row when stronger same-lineage child proof/manifest evidence already exists
  - reconciliation can claim unrelated child runs by stream name, mtime, or artifact recency
  - the fix changes stale `updated_at` invalidation, child-lane authority, or `CO-210` STATUS/progress hydration
  - the fix only changes STATUS display while leaving the decision ledger unusable
- Pre-implementation issue-quality review:
  - 2026-04-17: parent inspection confirms this lane is a narrow decision-ledger repair, not a STATUS/progress hydration change and not a scheduler redesign. Correctness depends on protected terms, exact artifact surfaces, and fail-closed lineage checks, so the micro-task path is ineligible.

## Milestones & Sequencing
1. Create the docs-first packet and scoped registry/checklist mirrors for `CO-218`.
2. Refresh the single Linear workpad with the turn decomposition matrix, current blocker, and docs packet evidence.
3. Retry a bounded same-issue child lane after the nested Codex usage-limit window resets so the active-turn `parallelize_now` contract can complete cleanly.
4. Run manifest-backed `docs-review` for the new packet before implementation continues.
5. Inspect `providerLinearChildLaneShell` and related proof/record helpers to define the narrowest stale-placeholder repair seam.
6. Implement repair or upgrade of a stale `launching-*` reservation row using stronger same-parent, same-issue child proof/manifest evidence.
7. Preserve fail-closed behavior when proof lineage, artifact root, child task id, run id, or issue metadata do not match.
8. Preserve truthful non-acceptance for zero-byte or missing-patch child lanes after reconciliation.
9. Add focused regressions for repaired `accept` / `reject` / `invalidate` behavior and fail-closed mismatches.
10. Run targeted validation, manifest-backed standalone review, explicit elegance review, PR/update handoff, and ready-review drain before any review-state transition.

## Dependencies
- Linear issue `CO-218` and workpad comment `866d1ca8-c36b-48b4-bfc3-60cfe2837418`
- Failed docs child-lane manifests:
  - `.runs/linear-5b31889f-233c-4f23-9750-5ff21d737661-docs-packet/cli/2026-04-17T09-40-30-654Z-0dfcba23/manifest.json`
  - `.runs/linear-5b31889f-233c-4f23-9750-5ff21d737661-docs-packet/cli/2026-04-17T09-42-56-585Z-65ed2abd/manifest.json`
- Parent inspection seams:
  - `orchestrator/src/cli/providerLinearChildLaneShell.ts`
  - `orchestrator/src/cli/providerLinearChildLaneRunner.ts`
  - `orchestrator/src/cli/providerLinearWorkerRunner.ts`
  - `orchestrator/tests/ProviderLinearChildLaneShell.test.ts`
- Adjacent scope boundaries:
  - `CO-210` current-progress hydration
  - stale Linear `updated_at` invalidation
  - child-lane admission and scheduler policy

## Validation
- Docs packet and mirrors:
  - `jq empty tasks/index.json docs/docs-freshness-registry.json`
  - `git diff --check -- docs/PRD-linear-5b31889f-233c-4f23-9750-5ff21d737661.md tasks/specs/linear-5b31889f-233c-4f23-9750-5ff21d737661.md docs/TECH_SPEC-linear-5b31889f-233c-4f23-9750-5ff21d737661.md docs/ACTION_PLAN-linear-5b31889f-233c-4f23-9750-5ff21d737661.md tasks/tasks-linear-5b31889f-233c-4f23-9750-5ff21d737661.md .agent/task/linear-5b31889f-233c-4f23-9750-5ff21d737661.md tasks/index.json docs/TASKS.md docs/docs-freshness-registry.json`
- Parent implementation lane:
  - focused `ProviderLinearChildLaneShell` regressions for stale placeholder repair and fail-closed mismatches
  - manifest-backed `docs-review`
  - `node scripts/delegation-guard.mjs`
  - `node scripts/spec-guard.mjs --dry-run`
  - `npm run build`
  - `npm run lint`
  - `npm run test`
  - `npm run docs:check`
  - `npm run docs:freshness`
  - `npm run repo:stewardship`
  - `node scripts/diff-budget.mjs`
  - manifest-backed standalone review and explicit elegance review
- Rollback plan:
  - revert the focused stale-placeholder repair if it weakens lineage checks, artifact-root safety, or truthful non-acceptance for patchless child lanes
  - retain this docs packet as the issue-shaping record even if implementation changes are rolled back

## Risks & Mitigations
- Risk: the lane drifts into `CO-210` STATUS/progress hydration.
  - Mitigation: keep reconciliation in the decision-ledger seam and preserve the explicit out-of-scope boundary in docs, tests, and workpad updates.
- Risk: repair logic claims the wrong child run because stream names overlap.
  - Mitigation: require same `parent_run_id`, `issue_id`, `issue_identifier`, `pipeline_id`, child task id, run id, and artifact-root anchoring before mutation.
- Risk: placeholder repair accidentally auto-accepts a patchless or malformed child lane.
  - Mitigation: keep the existing proof, patch-path, and patch-bytes gates authoritative after repair.
- Risk: the active-turn `parallelize_now` contract fails because nested Codex usage is still blocked.
  - Mitigation: retry a bounded same-issue child lane as soon as the usage-limit window resets and keep the blocker evidence current in the workpad if another retry is required.

## Approvals
- Parent docs packet: local authoring under active-turn usage-limit blocker; successful same-turn child-lane retry still pending.
- Parent docs-review: pending.
- Parent implementation/review/PR lifecycle: pending.
