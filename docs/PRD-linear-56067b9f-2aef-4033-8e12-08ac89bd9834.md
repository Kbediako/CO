# PRD - CO: Restore unrelated full-suite baseline blocking CO-43 handoff

## Added by Bootstrap 2026-04-01

## Traceability
- Linear issue: `CO-46` / `56067b9f-2aef-4033-8e12-08ac89bd9834`
- Linear URL: https://linear.app/asabeko/issue/CO-46/co-restore-unrelated-full-suite-baseline-blocking-co-43-handoff
- Related source issue: `CO-43`

## Summary
- Problem Statement: `CO-43` is ready for scoped-review handoff, but provider-worker closeout cannot move to `In Review` because the current branch baseline still has unrelated red suites outside the scoped standalone-review wrapper seam. The issue names three observed failing suites from 2026-03-30: `orchestrator/tests/Doctor.test.ts`, `orchestrator/tests/UserConfigStageSets.test.ts`, and `tests/cli-frontend-test.spec.ts`.
- Desired Outcome: the current branch baseline becomes truthful again for review handoff. That means either `npm run test` returns green on this workspace branch, or every remaining failure is explicitly owned by a follow-up issue or waiver so `CO-43` no longer has to absorb unrelated baseline repair work.

## User Request Translation (Context Anchor)
- User intent / needs (in your own words): finish `CO-46` in the current provider-worker workspace by treating the full-suite failures as a separate baseline-repair lane from `CO-43`, staying docs-first, and leaving concrete evidence that the named failures are fixed or explicitly owned elsewhere.
- Success criteria / acceptance:
  - `npm run test` is green again on the current CO branch baseline, or each remaining failure has an explicit issue or waiver owner
  - `orchestrator/tests/Doctor.test.ts`, `orchestrator/tests/UserConfigStageSets.test.ts`, and `tests/cli-frontend-test.spec.ts` are either fixed or conclusively triaged as out of scope with blocking ownership recorded
  - `CO-43` or the succeeding review-handoff lane can cite `CO-46` as the blocker instead of carrying unrelated repair work
- Constraints / non-goals:
  - do not widen into `CO-43` feature behavior unless a direct blocker proves inseparable
  - keep the lane bounded to the named suites, their direct implementation seams, and explicit blocker ownership if a remaining failure is not repaired here
  - do not mask failures with narrower scoped-review evidence; restore or truthfully classify the branch baseline itself

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `Restore unrelated full-suite baseline blocking CO-43 handoff`
  - `orchestrator/tests/Doctor.test.ts`
  - `orchestrator/tests/UserConfigStageSets.test.ts`
  - `tests/cli-frontend-test.spec.ts`
  - `npm run test`
- Protected terms / exact artifact and surface names:
  - full-suite baseline
  - scoped standalone-review wrapper seam
  - review handoff
  - blocker ownership
- Nearby wrong interpretations to reject:
  - "finish CO-43 by waiving the unrelated red suites"
  - "only make the standalone review wrapper green"
  - "reopen unrelated provider-worker workflow refactors instead of fixing or owning the named baseline failures"

## Parity / Alignment Matrix
- Required for parity/alignment lanes; otherwise state `Not applicable`.
- Current truth: focused reproduction on this workspace showed `orchestrator/tests/Doctor.test.ts` and `orchestrator/tests/UserConfigStageSets.test.ts` already green, while `tests/cli-frontend-test.spec.ts` still failed because the shipped `frontend-testing` command expects a built `dist/orchestrator/src/cli/frontendTestingRunner.js`, and a source-only worktree run may not have that build artifact under the current checkout root. After the fixture fix landed, the named suites were green again under the shipped `.js` entrypoint, with the fixture compiling the current branch into a temp package root before launching the shipped runner path, and the remaining full-suite blocker narrowed to the separate nested `npm run test` quiet-tail hang already owned by `CO-57`.
- Reference truth: unrelated branch-baseline failures should be repaired or explicitly owned in their own lane so review handoff for nearby scoped work does not inherit ambiguous repo health.
- Target truth / intended delta: the named suites are repaired on this branch baseline and any remaining full-suite non-terminal blocker is owned explicitly enough that `CO-43` can cite `CO-46` plus the surviving owner lane instead of carrying the repair scope itself.
- Explicitly out-of-scope differences: new `CO-43` feature work, broad provider-worker process redesign, and unrelated full-suite churn not connected to the named failures.

## Not Done If
- `npm run test` is still red and the remaining failures do not have explicit issue or waiver ownership.
- any of the three named suites remain ambiguous, untriaged, or hidden behind narrower scoped-review evidence.
- the resulting evidence still forces `CO-43` to explain or absorb unrelated baseline repair work itself.

## Goals
- Reproduce the current failure shape for the three named suites on this workspace branch.
- Identify the smallest truthful repair or expectation correction needed for each failing seam.
- Restore or explicitly classify the full-suite baseline so review handoff can cite a separate blocker lane instead of mixing scopes.
- Keep the docs, checklists, and workpad current enough that a succeeding handoff lane can audit the baseline status quickly.

## Non-Goals
- Expanding into `CO-43` scoped transport or review-wrapper feature changes.
- Refactoring broad validation infrastructure if a narrower doctor, config, or frontend-test fix is sufficient.
- Creating blanket waivers for the full suite without reproducing and classifying the named failures first.

## Stakeholders
- Product: CO operators and reviewers unblocking `CO-43` handoff
- Engineering: Codex / validation maintainers / provider-worker owners
- Design: N/A

## Metrics & Guardrails
- Primary Success Metrics:
  - the three named suites no longer block the branch baseline without ownership
  - final `npm run test` either passes or fails only with explicit tracked ownership
  - `CO-43` can cite `CO-46` as the unrelated baseline blocker lane
- Guardrails / Error Budgets:
  - keep the patch reviewable and bounded to the observed baseline blockers
  - preserve truthful doctor, stage-set, and frontend-test behavior
  - file a same-project follow-up instead of widening scope when a meaningful new defect is discovered

## User Experience
- Personas: provider-worker owner, reviewer resuming `CO-43`, maintainer verifying repo baseline
- User Journeys:
  - a worker reproduces the named baseline failures without guessing whether they belong to `CO-43`
  - the smallest truthful fix lands and the full suite returns green, or the remaining blocker is clearly owned elsewhere
  - a succeeding review-handoff lane can cite `CO-46` and its artifacts instead of re-triaging the same unrelated failures

## Technical Considerations
- Architectural Notes:
  - `orchestrator/tests/Doctor.test.ts` pins doctor baseline expectations and often reflects shared config/runtime contract drift
  - `orchestrator/tests/UserConfigStageSets.test.ts` covers user-config stage-set resolution and has recently been adjacent to provider-env and hermeticity follow-ups
  - `tests/cli-frontend-test.spec.ts` exercises the real `frontend-test` CLI path and can fail from fixture/package-root contract drift
  - the current branch may already contain nearby fixes from other lanes, so this issue must reproduce current truth in this workspace before assuming which seam is still broken
- Dependencies / Integrations:
  - `orchestrator/src/cli/doctor.ts`
  - `orchestrator/src/cli/config/userConfig.ts`
  - `orchestrator/src/cli/frontendTestCliRequestShell.ts`
  - `orchestrator/tests/Doctor.test.ts`
  - `orchestrator/tests/UserConfigStageSets.test.ts`
  - `tests/cli-frontend-test.spec.ts`

## Open Questions
- Focused reproduction answered the main question: the March 30 report was stale for `Doctor.test.ts` and `UserConfigStageSets.test.ts`, while `cli-frontend-test.spec.ts` still needed a real fix. The remaining open question for final handoff is whether the separate quiet-tail full-suite stall is fully resolved in `CO-57` before this lane moves beyond blocker documentation.

## Approvals
- Product: Self-approved from the Linear issue scope
- Engineering: Pending docs-review, reproduction, validation, and pre-handoff review/elegance gates
- Design: N/A
