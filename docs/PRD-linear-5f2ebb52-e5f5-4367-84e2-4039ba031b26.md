# PRD - Control host: stale plain released/not_active Blocked claim can still strand refresh at refresh:rehydrated and starve Ready admission

## Added by Docs Child Lane 2026-04-18

## Traceability
- Linear issue: `CO-248` / `5f2ebb52-e5f5-4367-84e2-4039ba031b26`
- Linear URL: https://linear.app/asabeko/issue/CO-248/control-host-stale-plain-releasednot-active-blocked-claim-can-still
- Task id: `linear-5f2ebb52-e5f5-4367-84e2-4039ba031b26`
- Canonical spec: `tasks/specs/linear-5f2ebb52-e5f5-4367-84e2-4039ba031b26.md`
- Shared source 0 anchor: `ctx:sha256:90f181707d191588a350b88e2378b3a174ef9814ccc956d90dd6d782f7c437a9#chunk:c000001`
- Source object id: `sha256:90f181707d191588a350b88e2378b3a174ef9814ccc956d90dd6d782f7c437a9`
- Context dir: `.runs/linear-5f2ebb52-e5f5-4367-84e2-4039ba031b26-docs-packet/cli/2026-04-18T13-17-17-407Z-341c0733/memory/source-0`
- Source payload: `.runs/linear-5f2ebb52-e5f5-4367-84e2-4039ba031b26-docs-packet/cli/2026-04-18T13-17-17-407Z-341c0733/memory/source-0/source.txt`
- Origin manifest: `.runs/linear-5f2ebb52-e5f5-4367-84e2-4039ba031b26-docs-packet/cli/2026-04-18T13-17-17-407Z-341c0733/manifest.json`
- Current issue-context source: `.runs/linear-5f2ebb52-e5f5-4367-84e2-4039ba031b26/cli/2026-04-18T13-10-39-584Z-a63bc089/provider-linear-issue-context-cache.json`
- Source payload note: the shared `source-0` payload is run metadata and provenance only; the exact issue wording below is preserved from the current read-only issue-context cache.

## Summary
- Problem Statement: on April 18, 2026, control-host still failed to re-admit `CO-233` even though live dispatch was already recommending it as `Ready` and free capacity remained. The host accepted `POST /api/v1/refresh`, but the acknowledgement still reported `stuck=true`, `restart_required=true`, and `reason=provider_refresh_lifecycle_stuck`, while later snapshots remained stranded at `refresh:rehydrated` with `claims_scanned=0` and `fresh_discovery_runs=0`.
- Desired Outcome: prove whether the stale plain `released/not_active` cached `Blocked` claim can wedge the control-host refresh lifecycle itself rather than merely suppress reclaim eligibility, then give the parent lane a bounded path to restore normal `Ready` admission without collapsing into manual launch or broad restart behavior.

## User Request Translation
- Preserve the exact issue framing around the control-host refresh lifecycle, `refresh:rehydrated`, `provider_refresh_lifecycle_stuck`, stale plain `released/not_active` claim state, cached `Blocked` issue truth, Ready admission starvation, and `POST /api/v1/refresh`.
- Keep the problem broader than reclaim-only: the incident proof is that refresh accepts the POST but remains stuck before claim scanning or fresh discovery resumes.
- Keep the fix narrower than a general restart policy rewrite: the packet should support a targeted recovery or diagnostic seam first.
- Preserve the explicit adjacent-lane boundaries from the issue body: `CO-202` is related but narrower, `CO-212` is the completed-blocker variant, `CO-238` is the reopened merge-closeout variant, `CO-41` is the lifecycle-stall lineage anchor, and `CO-211` is restart churn rather than this exact starvation seam.

## Intent Checksum
- Exact user wording / phrases to preserve:
  - control-host refresh lifecycle
  - `refresh:rehydrated`
  - `provider_refresh_lifecycle_stuck`
  - stale plain `released/not_active` claim
  - cached `Blocked` issue truth
  - Ready admission starvation
  - `POST /api/v1/refresh`
- Protected terms / exact artifact and surface names:
  - control-host refresh lifecycle
  - `refresh:rehydrated`
  - `provider_refresh_lifecycle_stuck`
  - stale plain `released/not_active` claim
  - `provider_issue_released:not_active`
  - cached `Blocked` issue truth
  - Ready admission starvation
  - `POST /api/v1/refresh`
  - `claims_scanned=0`
  - `fresh_discovery_runs=0`
  - `CO-233`
  - `CO-202`
  - `CO-212`
  - `CO-238`
  - `CO-41`
  - `CO-211`
- Nearby wrong interpretations to reject:
  - reclaim-only or generic stale-eligibility-only framing
  - the completed-blocker variant from `CO-212`
  - the reopened merge-closeout variant from `CO-238`
  - `manual-launch` as the fix
  - broad restart or host reboot as the only acceptable steady-state behavior
  - weakening reclaim guardrails or stale-claim validation to hide the lifecycle wedge

## Parity / Alignment Matrix
| Surface | Current Truth | Target Truth |
| --- | --- | --- |
| Refresh lifecycle | `POST /api/v1/refresh` is accepted, but later snapshots still show `refresh_phase=refresh:rehydrated`, `claims_scanned=0`, `fresh_discovery_runs=0`, `stuck=true`, and `restart_required=true`. | Parent reproduces why refresh can remain stranded before claim scanning or discovery and restores forward progress beyond `refresh:rehydrated`. |
| Retained stale claim | The retained intake row still carries plain `provider_issue_released:not_active`, cached `issue_state=Blocked`, and old run id `2026-04-18T11-53-57-793Z-060c77dc` while live dispatch already wants `CO-233`. | The stale claim remains auditable but cannot keep the lifecycle wedged or suppress an actually eligible `Ready` issue. |
| Admission outcome | A 60 second watch still does not re-admit `CO-233`, and the active set stays `CO-196` / `CO-245`. | A newly free slot plus live `Ready` truth becomes sufficient for normal control-host re-admission without manual launch. |
| Adjacent scope | `CO-202`, `CO-212`, `CO-238`, `CO-41`, and `CO-211` are nearby and easy to conflate. | The fix remains explicitly distinct from reclaim-only, completed-blocker, reopened merge-closeout, and broad restart-churn lanes. |

## Acceptance Criteria
- Reproduce the case where live dispatch recommends a `Ready` issue but refresh remains stranded at `refresh:rehydrated` with `provider_refresh_lifecycle_stuck` and no fresh discovery.
- Prove whether stale plain `released/not_active` retained claims can keep the refresh lifecycle wedged instead of merely suppressing reclaim eligibility.
- Land the narrowest fix that lets control-host consume a newly free slot and re-admit the `Ready` issue without requiring a broad restart.
- Add regression coverage or durable diagnostics so future incidents distinguish reclaim-shape suppression from lifecycle-stuck starvation.
- Preserve truthful issue-state modeling for neighboring variants already owned by `CO-202`, `CO-212`, and `CO-238`.

## Non-Goals
- No weakening of reclaim guardrails or stale-claim validation.
- No broad control-host restart policy changes unless fix evidence shows no narrower recovery path exists.
- No reopening the separate test-timeout scope owned by `CO-233`.
- No duplication or silent collapse of `CO-41`, `CO-202`, `CO-212`, or `CO-238`.
- No child-lane implementation or test edits in this docs-only slice.

## Not Done If
- Refresh can still sit at `refresh:rehydrated` with `claims_scanned=0` and `fresh_discovery_runs=0` while live dispatch recommends a `Ready` issue.
- The resulting explanation or fix is reclaim-only and never answers the lifecycle-stuck starvation proof.
- The parent solution depends on `manual-launch` or broad restart as the normal answer.
- The packet blurs or replaces `CO-212`, `CO-238`, or the `CO-41` lineage anchor.

## Metrics & Guardrails
- Primary Success Metrics:
  - the exact starvation shape is reproduced or simulated from the incident bundle
  - parent evidence proves whether stale plain `released/not_active` retained claims can wedge the lifecycle
  - a narrow recovery path advances beyond `refresh:rehydrated` and re-admits the `Ready` issue
  - future incidents can distinguish reclaim suppression from lifecycle-stuck starvation with machine-checkable evidence
- Guardrails / Error Budgets:
  - preserve truthful `provider_refresh_lifecycle_stuck` / `restart_required` diagnostics for genuine lifecycle stalls
  - preserve stale-claim auditability
  - preserve adjacent `CO-202`, `CO-212`, `CO-238`, `CO-41`, and `CO-211` boundaries
  - do not make manual launch or broad restart the steady-state contract

## User Experience
- Operators should be able to trust that a `Ready` issue with live dispatch backing and free capacity will be reconsidered through the normal control-host path rather than requiring manual launch.
- Operators should be able to distinguish a reclaim-shape stale claim from a refresh lifecycle that is genuinely wedged before claim scanning or fresh discovery.
- Incident artifacts should remain inspectable without forcing operators to infer behavior from a single stale snapshot.

## Technical Considerations
- The issue-context/workpad sharpen the seam beyond the issue body: after refresh acknowledgement, the watch window still shows `claims_total=128`, `claims_scanned=0`, `fresh_discovery_runs=0`, and `restart_required=true`.
- The parent lane needs to answer whether stale retained claim handling itself wedges progress, or whether a fail-closed lifecycle latch stops progress before claims are scanned.
- The issue bundle under `out/co-233-admission-nudge-20260418T124529Z/` is the machine-checkable source of truth for before/after dispatch, state, claim, refresh ack, and watch-window snapshots.

## Open Questions
- Does the narrowest parent fix live in the step that rehydrates retained claim state, or in the fail-closed lifecycle latch that currently stops progress before claims are scanned?
- Should the parent expose a dedicated diagnostic for the blocking retained-claim class at the `refresh:rehydrated` boundary?
- If the stale plain `released/not_active` claim is only an indirect amplifier, what is the smallest additional guard that keeps refresh progress truthful without reopening broader restart policy?

## Approvals
- Product: Linear issue `CO-248`
- Engineering: pending parent lane acceptance, docs-review, and implementation validation
- Design: N/A
