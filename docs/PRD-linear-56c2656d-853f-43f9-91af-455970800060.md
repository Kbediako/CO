# PRD - CO-356 archive automation Core Lane checks

## Traceability
- Linear issue: `CO-356` / `56c2656d-853f-43f9-91af-455970800060`
- Linear URL: https://linear.app/asabeko/issue/CO-356/co-make-archive-automation-prs-produce-required-core-lane-checks
- Task id: `linear-56c2656d-853f-43f9-91af-455970800060`
- Canonical spec: `tasks/specs/linear-56c2656d-853f-43f9-91af-455970800060-archive-core-lane-checks.md`

## Summary
- Problem Statement: archive automation PR `#637` updates `automation/tasks-archive` through the workflow `GITHUB_TOKEN`, leaving required `Core Lane` runs in `action_required` with zero jobs and no mergeable required check result.
- Desired Outcome: archive automation PRs produce a required `Core Lane` check on the archive PR head without bypassing branch protection, and the existing PR is not merged until the required check exists and is green.

## User Request Translation
- User intent / needs: diagnose the missing required-check state for `automation/tasks-archive`, make the recurring automation path produce the required `Core Lane` status or a safe rerun path, and avoid a one-off manual merge of PR `#637`.
- Success criteria / acceptance:
  - the root cause is recorded with live PR and Actions evidence
  - the archive PR path produces a `Core Lane` check for workflow-created PR heads
  - branch protection remains authoritative
  - PR `#637` is closed or merged only after required checks are green
  - the next archive automation PR does not reproduce the missing-check state
- Constraints / non-goals:
  - no branch-protection bypass
  - no broad archive policy rewrite
  - no unrelated cleanup of docs archive selection or task snapshot content
  - no secret rotation inside the repo diff

## Intent Checksum
- Exact user wording / phrases to preserve: `automation/tasks-archive`, `Core Lane`, `action_required`, `no-job state`, `PR #637`, `preserve branch protection`, `do not bypass required checks`.
- Protected terms / exact artifact and surface names:
  - `.github/workflows/archive-automation-base.yml`
  - `.github/workflows/tasks-archive-automation.yml`
  - `.github/workflows/implementation-docs-archive-automation.yml`
  - `.github/workflows/core-lane.yml`
  - `automation/tasks-archive`
  - `ARCHIVE_AUTOMERGE_TOKEN`
  - `Core Lane`
- Nearby wrong interpretations to reject:
  - manually merging PR `#637` without a required `Core Lane` result
  - closing only the current PR while leaving the next automation PR broken
  - disabling branch protection or removing the required check
  - treating Cloud Canary as the required branch-protection context
  - broadening into archive eligibility or docs freshness policy changes

## Parity / Alignment Matrix
- Required for parity/alignment lanes; otherwise state `Not applicable`.
- Current truth: workflow-created archive PRs produce approval-required `pull_request` workflow runs and no required `Core Lane` result on the PR head.
- Reference truth: GitHub permits `workflow_dispatch` runs triggered from workflows using `GITHUB_TOKEN`, while normal `pull_request` and `push` recursion remains blocked or approval-gated.
- Target truth / intended delta: after archive PR creation/update, archive automation explicitly dispatches `Core Lane` on the archive PR branch so the required check is reported on the PR head.
- Explicitly out-of-scope differences: replacing archive automation tokens, changing branch protection, or redesigning archive PR authoring.

## Not Done If
- PR `#637` is merged without a required `Core Lane` result.
- The fix only handles PR `#637` manually while future archive PRs still lack required checks.
- Archive automation still leaves `automation/tasks-archive` PR heads with only `action_required` / zero-job required workflows.
- The implementation weakens branch protection, deletes the required check, or silently ignores dispatch failure.

## Goals
- Add a durable required-check path for archive automation PRs.
- Preserve existing archive PR behavior and auto-merge intent where credentials allow it.
- Capture the current `ARCHIVE_AUTOMERGE_TOKEN` bad-credentials evidence without depending on secret mutation in this repo diff.

## Non-Goals
- Changing archive content policies.
- Updating archived docs payload branches beyond the existing automation behavior.
- Fixing unrelated GitHub Actions Node runtime deprecation warnings.
- Rotating GitHub secrets.

## Validation
- PR feedback sweep for PR `#637`: top-level comments, inline comments, review summaries, unresolved review threads, checks.
- Workflow source inspection for `archive-automation-base.yml` and `core-lane.yml`.
- Focused workflow contract tests for the Core Lane dispatch path.
- Repo gates appropriate to workflow/docs/test changes.
- Standalone review and explicit elegance pass before review handoff.

## Approvals
- Product: Linear CO-356
- Engineering: parent provider worker
