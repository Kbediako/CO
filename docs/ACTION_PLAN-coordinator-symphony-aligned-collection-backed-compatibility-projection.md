# ACTION_PLAN - Coordinator Symphony-Aligned Collection-Backed Compatibility Projection

1. Register `1032` docs-first artifacts, checklist mirrors, task index entries, and freshness coverage.
2. Capture the bounded deliberation showing why the next highest-value gap is the selected-run-backed compatibility read model rather than another small helper extraction.
3. Add a collection-backed compatibility projection builder derived from the current selected-run runtime snapshot.
4. Update `controlRuntime.ts` and `observabilitySurface.ts` so the compatibility state/issue routes consume that projection.
5. Keep `/api/v1/dispatch`, `/ui/data.json`, and Telegram/UI selected-run consumers on their current seams.
6. Run the ordered validation lane, elegance review, and closeout sync.
