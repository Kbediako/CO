# ACTION_PLAN - Coordinator Symphony-Aligned Telegram Bridge Bootstrap Handoff Extraction

## Steps

1. Register `1079` docs-first from the `1078` next-slice note and the Symphony thin-shell heuristic.
2. Extract the remaining Telegram bootstrap handoff assembly into one control-local helper.
3. Keep `controlServer.ts` thin while preserving Telegram bridge startup ordering and subscription behavior.
4. Add focused regressions for the extracted bootstrap seam.
5. Run the lane validation bundle, sync mirrors, and queue the next bounded Symphony-aligned seam.
