# PRD - CO STATUS / observability: stop direct `co-status --format json` fetch refusals on healthy control-hosts

## Added by Docs Child Lane 2026-04-18

## Traceability
- Linear issue: `CO-246` / `4a34011b-104b-4334-871b-a0a014883548`
- Linear URL: https://linear.app/asabeko/issue/CO-246/co-status-observability-stop-direct-co-status-format-json-fetch
- MCP Task ID: `linear-4a34011b-104b-4334-871b-a0a014883548`
- Canonical task ID: `20260418-linear-4a34011b-104b-4334-871b-a0a014883548`
- Canonical spec: `tasks/specs/linear-4a34011b-104b-4334-871b-a0a014883548.md`
- Docs packet child lane manifest: `.runs/linear-4a34011b-104b-4334-871b-a0a014883548-co-246-docs-packet/cli/2026-04-18T08-43-23-951Z-861dc439/manifest.json`
- Source anchor: `ctx:sha256:ed8d7cee28dd1ae41d954a39ca3c8ec761012ade0011d04e9e673f3abb5fa86a#chunk:c000001`
- Issue body evidence: `.runs/linear-4a34011b-104b-4334-871b-a0a014883548/cli/2026-04-18T08-35-23-027Z-c520a178/provider-linear-issue-context-cache.json`
- Source payload note: the shared `source-0` payload attached to this child lane is run metadata only. The exact protected wording below is therefore preserved from the parent worker's cached live `CO-246` issue body plus the current repo seams named in the parent workpad plan.

## Summary
- Problem Statement: Direct `node dist/bin/codex-orchestrator.js co-status --format json` can intermittently fail with `control-host unavailable ... fetch failed (ECONNREFUSED)` even while the underlying control-host read model is still healthy and the provider-intake ledger keeps advancing.
- Desired Outcome: the direct JSON snapshot path truthfully recovers from stale or dead control-host snapshot endpoints through endpoint re-resolution or equivalent bounded retry behavior, preserves existing attach-style recovery behavior, and remains explicitly distinct from top-level truth fixes or genuine refresh-stuck / restart-required host-failure classes.

## User Request Translation (Context Anchor)
- User intent / needs (in your own words): create the missing docs-first packet for `CO-246` only so the parent lane can implement the narrowest truthful recovery for intermittent direct `co-status --format json` fetch refusal on a healthy control-host without drifting into `provider_intake`, `tracked.linear`, or generic host-failure work.
- Success criteria / acceptance:
  - reproduce a direct `co-status --format json` fetch refusal while the control-host ledger/read model remains healthy
  - add direct JSON recovery that re-resolves the control endpoint or otherwise handles stale/dead-port snapshot reads truthfully
  - preserve attach-mode recovery behavior and keep the direct JSON path consistent with it where appropriate
  - add regression coverage for stale-endpoint / dead-port direct JSON reads in addition to the existing happy-path tests
  - keep closeout evidence explicit that this failure class is distinct from `CO-243`, `CO-223`, and refresh-stuck host failures
- Constraints / non-goals:
  - child lane owns only the declared docs packet, checklist mirror, and `tasks/index.json`
  - parent lane owns implementation, focused validation, Linear state, workpad, PR lifecycle, and merge
  - do not edit implementation or test files in this child lane
  - do not run full repo validation suites in this child lane

## Intent Checksum
- Exact user wording / phrases to preserve:
  - Direct `node dist/bin/codex-orchestrator.js co-status --format json` can intermittently fail with `control-host unavailable ... fetch failed (ECONNREFUSED)` even while the underlying control-host read model is still healthy and the provider-intake ledger keeps advancing.
  - `co-status --format json`
  - `ECONNREFUSED`
  - healthy control-host
  - direct JSON snapshot path
  - endpoint re-resolution
  - attach-style recovery
  - intermittent dashboard-read refusal
- Protected terms / exact artifact and surface names:
  - `co-status --format json`
  - `ECONNREFUSED`
  - healthy control-host
  - direct JSON snapshot path
  - endpoint re-resolution
  - attach-style recovery
  - intermittent dashboard-read refusal
  - `.runs/local-mcp/cli/control-host/provider-intake-state.json`
  - `polling.stuck=false`
  - `polling.restart_required=false`
  - `coStatusCliShell.ts`
  - `coStatusAttachCliShell.ts`
  - `readUiDatasetWithEndpointRecovery`
  - `resolveAttachTarget`
  - `fetchUiDataset`
- Nearby wrong interpretations to reject:
  - do not fold this into `provider_intake` truth semantics
  - do not fold this into `tracked.linear` truth semantics
  - do not treat this as proof that the control-host itself is unhealthy every time
  - do not solve it by suppressing fetch errors without retry/re-resolution behavior
  - do not widen this into the generic refresh-stuck / restart-loop failure class

## Parity / Alignment Matrix

| Surface | Current Truth | Reference Truth | Target Truth |
| --- | --- | --- | --- |
| Direct `co-status --format json` | succeeds in operator-dashboard mode, then can intermittently fail with `control-host unavailable at http://127.0.0.1:<port>/ui/data.json: fetch failed (ECONNREFUSED)` | direct JSON consumers should recover truthfully from stale or rotated endpoints instead of failing on the first dead-port read | direct JSON path re-resolves or otherwise recovers from stale/dead-port reads without hiding genuine failure |
| Healthy control-host evidence | the same live window still had a healthy advancing intake ledger in `.runs/local-mcp/cli/control-host/provider-intake-state.json`; the same live probes showed `polling.stuck=false` and `polling.restart_required=false` | healthy host evidence should stay authoritative and should not be misclassified as a generic host outage | direct JSON fetch refusal is separated from genuine stuck / restart-required host failure classes |
| Attach recovery | attach mode already has endpoint re-resolution/retry behavior after network failure | the direct JSON path should stay consistent with attach-style recovery where appropriate | direct JSON snapshot path gains equivalent bounded recovery rather than remaining single-shot |
| `CO-243` boundary | `CO-243` is about top-level `provider_intake` truth under concurrent running claims | adjacent issue boundaries should stay explicit | this lane stays about direct dashboard-read fetch refusal, not `provider_intake` semantics |
| `CO-223` boundary | `CO-223` is about stale top-level `tracked.linear` fallback truth | adjacent issue boundaries should stay explicit | this lane stays about direct `co-status --format json` recovery, not `tracked.linear` truth |
| `CO-41` / `CO-194` / `CO-211` boundary | those issues own genuine stuck/restart-required/probe-timeout host failure classes | healthy-host intermittent refusal should not be conflated with genuine host failure | this lane stays narrowly on dashboard-surface fetch refusal when host-health signals are not tripped |

## Acceptance Criteria
- The docs packet and checklist mirror exist for the declared `CO-246` files only.
- The packet preserves the exact checksum `co-status --format json`, `ECONNREFUSED`, healthy control-host, direct JSON snapshot path, endpoint re-resolution, attach-style recovery, and intermittent dashboard-read refusal.
- Parent implementation can truthfully recover the direct JSON path from stale or dead-port reads without suppressing genuine failures.
- The packet keeps this lane explicitly distinct from `CO-243`, `CO-223`, `CO-41`, `CO-194`, and `CO-211`.

## Non-Goals
- No redesign of top-level `provider_intake` payload semantics.
- No redesign of top-level `tracked.linear` payload semantics.
- No weakening of genuine host-health alarms for stuck/restart-required states.
- No requirement that attach mode become the only supported recovery path for JSON consumers.
- No implementation or test edits in this docs child lane.

## Not Done If
- Direct `co-status --format json` still fails on the first dead-port snapshot read while healthy control-host evidence remains available.
- The proposed repair is described as `provider_intake` truth, `tracked.linear` truth, or generic refresh-stuck host failure work.
- The packet fails to preserve the exact incident checksum or the current repo seams.

## Stakeholders
- Product: CO operators relying on truthful direct `co-status --format json` snapshots.
- Engineering: CLI shell, attach recovery, control-host endpoint, and observability maintainers.
- Design: N/A.

## Metrics & Guardrails
- Primary Success Metrics:
  - direct `co-status --format json` no longer fails on a stale/dead control endpoint when the host is otherwise healthy
  - attach-style recovery behavior remains intact
  - the fix remains explicitly separate from `CO-243`, `CO-223`, `CO-41`, `CO-194`, and `CO-211`
- Guardrails / Error Budgets:
  - preserve fail-closed behavior when the refreshed endpoint is still unreadable
  - preserve genuine stuck/restart-required host-failure alarms
  - preserve truthful network/auth/timeout messaging rather than silently swallowing errors

## User Experience
- Personas:
  - operator consuming direct JSON snapshots from `co-status --format json`
  - maintainer debugging intermittent dashboard-read refusal without wanting to attach interactively
- User Journeys:
  - operator runs `co-status --format json` during endpoint rotation and receives a truthful recovered snapshot instead of an immediate dead-port failure
  - maintainer can still distinguish stale-endpoint recovery from a genuinely unhealthy control-host

## Technical Considerations
- Architectural Notes:
  - `coStatusCliShell.ts` currently resolves the attach target once and calls the UI dataset fetch once for `--format json`
  - `coStatusAttachCliShell.ts` already routes interactive attach reads through `readUiDatasetWithEndpointRecovery`, which re-resolves `control_endpoint.json` and retries when the endpoint rotates
  - the smallest likely repair is to keep direct JSON path behavior aligned with the bounded endpoint-recovery contract already present on the attach path
- Dependencies / Integrations:
  - `orchestrator/src/cli/coStatusCliShell.ts`
  - `orchestrator/src/cli/coStatusAttachCliShell.ts`
  - `orchestrator/tests/CoStatusCliShell.test.ts`
  - `orchestrator/tests/CoStatusAttachCliShell.test.ts`

## Open Questions
- Should the parent factor the direct JSON path through `readUiDatasetWithEndpointRecovery` directly, or add an equivalent bounded recovery helper that preserves current JSON-only output behavior?
- Is one retry after endpoint re-resolution sufficient for the direct JSON path, matching attach behavior closely enough for this bounded seam?
- Does the parent need any additive proof in the error path to make stale-endpoint recovery versus genuine host unavailability more obvious in closeout evidence?

## Approvals
- Product: self-approved from the bounded `CO-246` issue wording carried into this packet.
- Engineering: pending parent docs-review / implementation handoff.
- Design: N/A.
