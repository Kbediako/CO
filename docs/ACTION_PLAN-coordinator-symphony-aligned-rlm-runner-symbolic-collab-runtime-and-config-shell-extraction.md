# ACTION_PLAN: Coordinator Symphony-Aligned RLM Runner Symbolic Collab Runtime And Config Shell Extraction

## Steps

1. Register `1238` docs-first artifacts, task mirrors, registry entries, and index updates.
2. Run the deterministic docs-first gates (`spec-guard`, `docs:check`, `docs:freshness`).
3. Extract the remaining symbolic collab-runtime invocation and shell-owned config surface from `rlmRunner.ts` into `rlmCodexRuntimeShell.ts`.
4. Update focused RLM tests so shell-owned contracts import from the runtime shell directly where appropriate.
5. Run the bounded implementation closeout lane and record summary, overrides, and the next-slice note.

## Guardrails

- Keep the lane bounded to the symbolic collab-runtime invocation and shell-owned config seam.
- Do not widen into the iterative or symbolic loop cores or into the broader top-level CLI and env parsing block.
- Keep symbolic handoff, runtime-shell lifecycle validation, and role-policy behavior explicit in the evidence.
