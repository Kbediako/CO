# ACTION_PLAN - linear-70e83230-4f52-4850-b494-41fd0ec32f13

## Summary
- Goal: turn the existing worker-lifetime same-session substrate into an explicit guarded resident restart seam that can drain, restart, and resume the same logical resident thread/session lineage at safe completed-turn boundaries.
- Scope: docs-first packet, docs-review child stream, resident continuity envelope through control-host start, guarded first-turn resume in `provider-linear-worker`, proof/read-model surfacing, focused regressions, and the normal validation/review handoff flow.
- Assumptions:
  - current `1312` thread reuse inside one provider-worker lifetime remains correct and becomes the inner substrate for this lane
  - the first safe restart boundary to support is a completed-turn boundary proven by provider proof, not a mid-turn crash or partial state

## Issue Readiness Gate
- Intent checksum / protected terms carried forward: preserve `guarded resident app-server control seam`, `drain/restart/resume continuity`, `stable logical thread/session continuity`, `provider-linear-worker`, `thread_id`, `latest_session_id`, and the no-hot-upgrade boundary.
- Not done if: restart continuity is still implicit, a restarted worker cannot truthfully resume the same logical resident session at a safe boundary, or the docs overclaim BEAM-like hot upgrade.
- Pre-implementation issue-quality review: approved. The concrete gap is the outer runtime/control seam above `1312`, not the inner worker-lifetime continuation loop itself.

## Milestones & Sequencing
1. Create the CO-89 docs packet, checklist mirrors, local workpad source, and registry entries, then run audited `linear child-stream --pipeline docs-review`.
2. Extend the provider launch seam with additive resident continuity metadata derived from prior fresh proof at restart-safe completed-turn boundaries.
3. Update `provider-linear-worker` so a guarded restart can resume the prior resident thread on its first turn, keep stable logical continuity metadata, and fail closed on unsafe boundaries.
4. Surface the new resident continuity contract in proof/read-model state and add focused regressions around restart-safe continuity plus unsafe-boundary fallback.
5. Run the required validation floor, standalone review, explicit elegance review, and workpad refresh before any PR or review handoff.

## Dependencies
- `orchestrator/src/cli/control/providerIssueHandoff.ts`
- `orchestrator/src/cli/controlHostCliShell.ts`
- `orchestrator/src/cli/providerLinearWorkerRunner.ts`
- `orchestrator/src/cli/control/providerIssueObservability.ts`
- `orchestrator/tests/ProviderIssueHandoff.test.ts`
- `orchestrator/tests/ProviderLinearWorkerRunner.test.ts`
- `orchestrator/tests/ControlHostCliShell.test.ts`

## Validation
- Checks / tests:
  - `MCP_RUNNER_TASK_ID=linear-70e83230-4f52-4850-b494-41fd0ec32f13 node "/Users/kbediako/Code/CO/dist/bin/codex-orchestrator.js" linear child-stream --pipeline docs-review --stream co-89-docs-review --format json`
  - focused continuity regressions for safe completed-turn drain/restart/resume and unsafe-boundary fallback
  - required repo validation floor after implementation
- Rollback plan:
  - revert the resident continuity envelope, guarded first-turn resume behavior, and observability updates together so the system returns to the pre-CO-89 fresh-worker restart contract without mixed proof semantics

## Risks & Mitigations
- Risk: the lane accidentally relabels fresh-start retry behavior as resident continuity.
  - Mitigation: continuity is only derived from explicit fresh proof at allowed completed-turn boundaries and is surfaced distinctly in proof/read-model state.
- Risk: a restarted worker attempts to resume a thread after an unsafe boundary.
  - Mitigation: keep the initial allowlist narrow and fail closed when the prior boundary is not restart-safe.
- Risk: docs overclaim hot reload or in-memory continuity.
  - Mitigation: spell out that policy/config applies on restart boundaries only and that mid-turn hot upgrade remains unsupported.

## Approvals
- Reviewer: pending `codex-orchestrator docs-review` child stream for the new packet
- Date: 2026-04-09
