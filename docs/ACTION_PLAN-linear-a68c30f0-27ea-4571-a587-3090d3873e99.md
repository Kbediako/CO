# ACTION_PLAN - CO: restore missing docs/TASKS packet references for linear-1c101

## Added by Parent Lane 2026-04-24

## Summary
- Goal: restore truthful docs packet references for `linear-1c101ebc-4b86-4c1f-b04d-0455e50fbacb` so `docs:check` no longer fails on six missing paths discovered during CO-346 docs-review.
- Scope:
  - current CO-350 docs-first registration packet
  - old `linear-1c101...` packet file restoration or explicit stale-reference correction
  - registry and freshness mirror consistency
  - `docs:check` reproduction and final validation
- Assumptions:
  - `docs:check` behavior is correct and should remain strict
  - the existing `tasks/index.json` and freshness rows are intended active packet truth unless evidence proves the snapshot should be corrected instead
  - current `origin/main` now contains the old packet restoration from CO-332, so CO-350's final diff should not duplicate those files
  - CO-346 behavior remains out of scope

## Issue Readiness Gate
- Intent checksum / protected terms carried forward:
  - all six exact `docs/TASKS.md: backticked-path-missing` errors for `linear-1c101...`
  - `docs/TASKS.md`, `tasks/index.json`, `docs/docs-freshness-registry.json`
  - CO-346 docs-review manifest `.runs/linear-a66fa065-3c6c-4063-b2ba-1121bf71f74f-co346-docs-review/cli/2026-04-24T06-34-29-445Z-c594301b/manifest.json`
- Not done if:
  - any of the six missing-path errors remain
  - the snapshot, index, and freshness registry disagree
  - missing-path errors are hidden by policy changes instead of fixed data
- Pre-implementation issue-quality review:
  - fresh local `docs:check` reproduced exactly the six named errors
  - the lane is not a micro-task because it depends on exact packet paths and cross-registry alignment

## Milestones & Sequencing
1. Inspect live Linear issue context, move `Ready` to `In Progress`, create the single workpad, and record same-turn parallelization.
2. Launch a bounded same-issue child lane for the old `linear-1c101...` packet files while the parent owns this CO-350 packet and registry mirrors.
3. Capture pre-fix `npm run docs:check` evidence.
4. Create the CO-350 docs-first packet and mirror it into `tasks/index.json`, `docs/TASKS.md`, and `docs/docs-freshness-registry.json`.
5. Integrate, reject, or supersede the child lane output for the six old packet files; after current-main rebase, supersede the accepted stale-base child patch with upstream CO-332 restoration and verify all old references resolve.
6. Run `npm run docs:check` and targeted consistency checks.
7. Run review and elegance gates before PR/review handoff.

## Dependencies
- Linear issue `CO-350`.
- CO-346 docs-review failure evidence from the issue description.
- Existing snapshot/index/freshness rows for `linear-1c101ebc-4b86-4c1f-b04d-0455e50fbacb`.

## Validation
- Checks / tests:
  - `npm run docs:check` before fix, stored at `out/linear-a68c30f0-27ea-4571-a587-3090d3873e99/docs-check-before.log`
  - `npm run docs:check` after fix
  - targeted path existence check for all six `linear-1c101...` files
  - registry consistency inspection for `docs/TASKS.md`, `tasks/index.json`, and `docs/docs-freshness-registry.json`
  - standalone review and elegance pass before handoff
- Rollback plan:
  - revert the docs packet/registry repair if final validation shows the restored packet misrepresents the active task truth

## Risks & Mitigations
- Risk: reconstructed old packet content overstates implementation truth.
  - Mitigation: current `origin/main` already supplies the restored old files, so the final CO-350 diff does not reconstruct them again.
- Risk: registry edits introduce unrelated churn.
  - Mitigation: only add CO-350 mirrors and repair `linear-1c101...` existence/consistency.
- Risk: child-lane output overlaps parent registry edits.
  - Mitigation: child scope was limited to the six old packet files; after current-main rebase, parent keeps only CO-350 packet and registry ownership.

## Approvals
- Reviewer: parent provider worker
- Date: 2026-04-24
