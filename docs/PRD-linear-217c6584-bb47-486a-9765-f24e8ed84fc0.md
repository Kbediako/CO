# PRD - CO-481 removed feature defaults after Codex CLI 0.128

## Traceability
- Linear issue: `CO-481` / `217c6584-bb47-486a-9765-f24e8ed84fc0`
- Linear URL: https://linear.app/asabeko/issue/CO-481
- Task registry id: `20260505-linear-217c6584-bb47-486a-9765-f24e8ed84fc0`
- MCP Task ID: `linear-217c6584-bb47-486a-9765-f24e8ed84fc0`
- Canonical spec: `tasks/specs/linear-217c6584-bb47-486a-9765-f24e8ed84fc0.md`
- TECH_SPEC mirror: `docs/TECH_SPEC-linear-217c6584-bb47-486a-9765-f24e8ed84fc0.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-217c6584-bb47-486a-9765-f24e8ed84fc0.md`
- Task checklist: `tasks/tasks-linear-217c6584-bb47-486a-9765-f24e8ed84fc0.md`
- Canonical owner key: `codex-cli-0128:removed-feature-defaults`
- Child lane manifest: `.runs/linear-217c6584-bb47-486a-9765-f24e8ed84fc0-docs-packet/cli/2026-05-05T01-54-52-566Z-51511a3f/manifest.json`
- Source anchor: `ctx:sha256:65228e314b6bfafd429ea381e50f53c312b8a534372bf4d2d9d5567cafca1da8#chunk:c000001`
- Parent source payload: `.runs/linear-217c6584-bb47-486a-9765-f24e8ed84fc0-docs-packet/cli/2026-05-05T01-54-52-566Z-51511a3f/memory/source-0/source.txt`
- Source payload note: the referenced `.runs` payload path is not present in this child checkout, so this packet uses the parent-provided issue contract, protected terms, non-goals, source anchor, and manifest pointer as the authoritative docs-first contract.

## Summary
- Problem Statement: CO defaults and advisory setup surfaces can still treat removed Codex feature keys as normal configurable defaults even after Codex CLI 0.128 exposes `js_repl` and `js_repl_tools_only` as removed through `codex features list`.
- Desired Outcome: parent implementation has a bounded CO-481 packet for making `codex-orchestrator doctor` and `codex-orchestrator codex defaults` respect removed feature keys without reopening CO-452 or broadening into runtime, provider-worker, model, release, or cloud posture changes.

## User Request Translation
- User intent / needs: create the CO-481 docs-first packet and registry mirrors only. The packet must preserve exact removed-feature terminology, keep `codex features list` as the current authority, and define parent-owned implementation expectations for doctor/default setup behavior.
- Success criteria / acceptance:
  - `codex features list` is treated as the authoritative current feature-state surface for removed feature keys.
  - `codex-orchestrator doctor` does not recommend enabling, disabling, or otherwise configuring removed feature keys.
  - `codex-orchestrator codex defaults` does not write or preserve `js_repl` or `js_repl_tools_only` as current defaults when the active Codex CLI reports them as removed.
  - CO-452 remains the reference lane for retiring the active `js_repl` posture; CO-481 only guards removed feature defaults in doctor/default setup.
  - parent validation includes focused tests or live command evidence for removed-feature handling in doctor/default setup.

## Intent Checksum
- Protected terms:
  - Codex CLI 0.128
  - codex features list
  - js_repl
  - js_repl_tools_only
  - removed feature keys
  - codex-orchestrator doctor
  - codex-orchestrator codex defaults
  - CO-452
  - canonical owner key `codex-cli-0128:removed-feature-defaults`
- Nearby wrong interpretations to reject:
  - treating removed feature keys as merely disabled, experimental, or toggleable
  - reviving `js_repl` active-posture work that CO-452 already retired
  - adding `js_repl` canaries or feature usage matrices
  - rewriting broad Codex config generation unrelated to removed defaults
  - using this lane to change runtime/provider-worker behavior
  - using this lane to change model posture, release posture, or cloud posture

## Parity / Alignment Matrix

| Surface | Current truth | Reference truth | Target truth | Explicitly out-of-scope differences |
| --- | --- | --- | --- | --- |
| Feature-state authority | Current Codex CLI feature state is available from `codex features list`. | Codex CLI 0.128 reports removed feature keys such as `js_repl` and `js_repl_tools_only` through the feature list. | Doctor/default setup classifies removed keys as unavailable for current defaults. | No broad feature inventory cleanup and no unrelated feature flag policy changes. |
| CO-452 relationship | CO-452 retired active `js_repl` posture and canary guidance. | CO-452 remains the historical/reference owner for active `js_repl` retirement. | CO-481 builds on that reference only for removed default generation and advisory setup. | No reopening CO-452 implementation or js_repl canaries. |
| `codex-orchestrator doctor` | Doctor can advise on Codex posture drift. | Advisories must not ask operators to configure keys removed by the active CLI. | Doctor either suppresses removed-key recommendations or reports them as removed/unavailable. | No runtime/provider-worker behavior change. |
| `codex-orchestrator codex defaults` | Defaults setup can write feature/default posture into local config. | Removed feature keys are not valid current defaults. | Defaults generation avoids writing `js_repl` and `js_repl_tools_only` when `codex features list` marks them removed. | No broad config rewrite and no model/release/cloud posture change. |

## Acceptance Criteria
- Removed feature detection is grounded in `codex features list` or a documented equivalent current feature-state parser.
- `codex-orchestrator doctor` does not recommend removed feature keys as normal enable/disable/default actions.
- `codex-orchestrator codex defaults` does not seed `js_repl` or `js_repl_tools_only` as current defaults under Codex CLI 0.128 removed-feature truth.
- CO-452 remains referenced as the prior active `js_repl` retirement lane, not duplicated.
- Focused parent validation covers removed-feature defaults and advisory behavior.

## Not Done If
- `codex-orchestrator doctor` still recommends configuring `js_repl`, `js_repl_tools_only`, or any known removed feature keys as current posture.
- `codex-orchestrator codex defaults` writes `js_repl` or `js_repl_tools_only` into current generated/default config under Codex CLI 0.128 removed-feature truth.
- `codex features list` is ignored or replaced with stale hard-coded assumptions for removed keys.
- The implementation treats removed feature keys as merely disabled, experimental, or break-glass toggleable.
- The lane adds runtime/provider-worker behavior, js_repl canaries, broad config rewrite, unrelated feature flag cleanup, or model/release/cloud posture changes.
- Validation lacks focused tests or live command evidence for doctor/default setup behavior.

## Goals
- Create the CO-481 docs-first packet and registry mirrors inside the declared docs scope.
- Preserve the exact removed-feature terms required by the parent issue.
- Define a narrow parent-owned implementation target for doctor/default setup.
- Keep CO-452 as the reference owner for active `js_repl` posture retirement.

## Non-Goals
- No runtime/provider-worker behavior.
- No js_repl canaries.
- No broad config rewrite.
- No unrelated feature flag cleanup.
- No model/release/cloud posture change.
- No implementation, test, package, workflow, Linear, GitHub, workpad, PR, or lifecycle edits from this child lane.

## Metrics & Guardrails
- Primary Success Metrics:
  - all six packet and mirror files exist
  - `tasks/index.json` uses task id `20260505-linear-217c6584-bb47-486a-9765-f24e8ed84fc0`
  - registry mirrors include current `last_review` date `2026-05-05`
  - protected-term scan finds every requested term
  - changed-file review stays inside declared docs/task scope
- Guardrails / Error Budgets:
  - zero source/test/package/workflow edits from this child lane
  - zero active `js_repl` posture revival
  - zero broad posture changes outside removed feature defaults

## Technical Considerations
- Architectural Notes:
  - `codex features list` should be the evidence source for current removed-feature classification, or the parent implementation should document any equivalent parser/probe it uses.
  - Removed feature keys should be fail-closed for generated defaults and advisory remediation: they may be reported as removed/unavailable but should not be recommended as actions.
  - CO-452 context should remain reference-only so this lane does not reintroduce `js_repl` canary or cloud feature-toggle work.
- Dependencies / Integrations:
  - parent-owned `codex-orchestrator doctor`
  - parent-owned `codex-orchestrator codex defaults`
  - active Codex CLI 0.128 `codex features list` output or equivalent feature-state parser

## Fallback / Refactor Decision
- Applies to fallback, compatibility, legacy, stale, cached, break-glass, or minor-seam behavior? `Yes`.
- Large-refactor decision: not required for this bounded removed-defaults guardrail; the lane removes stale default/advisory behavior instead of adding another compatibility layer.
- Minor-seam decision: acceptable because the implementation target is limited to existing doctor/default setup surfaces.

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Removed feature defaults | `js_repl`, `js_repl_tools_only`, or other removed feature keys are written as current defaults. | remove fallback | CO-481 | Codex CLI 0.128 `codex features list` reports a key as removed. | pre-0.128 guidance | 2026-05-05 | immediate removal | defaults setup no longer writes removed feature keys as current config. | Focused defaults test or live `codex-orchestrator codex defaults` evidence. |
| Removed feature advisories | Doctor recommends configuring removed feature keys. | remove fallback | CO-481 | Doctor/default setup sees removed feature-state evidence. | pre-0.128 guidance | 2026-05-05 | immediate removal | doctor reports removed keys as unavailable or suppresses remediation. | Focused doctor test or live `codex-orchestrator doctor` evidence. |

## Open Questions
- Parent should select the exact current-feature parser or live command probe shape used by doctor/default setup.
- Parent should decide whether removed feature keys are surfaced as explicit advisory notes or silently omitted from generated defaults.

## Approvals
- Product: Linear CO-481, pending parent review
- Engineering: parent docs-review / implementation review, pending
- Design: N/A
