# ACTION_PLAN - CO validation: classify current-main docs:freshness stale-doc baseline after CO-165 rebase

## Added by Bootstrap 2026-04-13

## Summary
- Goal: establish the live current-main truth for the post-`CO-165` `docs:freshness` failure and either repair it minimally or document the exact blocker path.
- Scope:
  - register the `CO-166` docs-first packet, registry mirrors, and single workpad source
  - run an audited `docs-review` child stream before implementation
  - reproduce `docs:freshness` with a saved JSON report and classify the stale set
  - implement only the smallest bounded repair required by the repro, or record the exact blocker/follow-up path
- Assumptions:
  - the issue snapshot is directionally correct but still requires fresh confirmation on today’s `origin/main`
  - `scripts/docs-freshness.mjs` already exposes the right classification data for this lane

## Issue Readiness Gate
- Intent checksum / protected terms carried forward:
  - `docs:freshness`
  - `current-main stale-doc baseline`
  - `Task Packet`
  - `Task Mirror`
  - `Report Only`
  - `truthful validation story`
- Not done if:
  - the lane cannot tell baseline drift from local drift
  - the exact stale sources remain unidentified
  - the closeout is only a vague waiver
- Pre-implementation issue-quality review:
  - the issue snapshot already bounds the lane well enough to start docs-first scaffolding and a fresh mainline repro without reopening `CO-165`

## Milestones & Sequencing
1. Register the `CO-166` docs-first packet, update `tasks/index.json`, `docs/TASKS.md`, `docs/docs-freshness-registry.json`, mirror the checklist, and stage the single workpad source.
2. Run `linear child-stream --pipeline docs-review` for the registered packet and record the manifest or truthful fallback if it only stops on the standing baseline.
3. Reproduce `npm run docs:freshness` with saved log/report artifacts, then classify the stale set by exact path and class.
4. Decide between a bounded metadata repair and a blocker/follow-up path based on the repro.
5. Rerun the relevant validation, refresh the workpad/checklist/spec packet, and prepare review handoff only if a non-trivial diff remains.

## Dependencies
- `scripts/docs-freshness.mjs`
- `docs/docs-freshness-registry.json`
- `tasks/index.json`
- `docs/TASKS.md`
- current `origin/main` state after the `CO-165` rebase

## Validation
- Checks / tests:
  - `linear child-stream --pipeline docs-review`
  - `npm run docs:freshness`
  - targeted reruns after any bounded metadata repair
- Rollback plan:
  - if a proposed fix broadens beyond the proven stale cohort, stop, document the blocker, and create a follow-up instead of widening scope here

## Risks & Mitigations
- Risk: the live repro differs from the issue snapshot.
  - Mitigation: treat the new report as source of truth and update the docs packet/workpad immediately.
- Risk: the stale set spans multiple independent cohorts.
  - Mitigation: classify by path/class first and split with a follow-up if the work is no longer one bounded lane.
- Risk: the lane drifts into `CO-165` runtime/test scope.
  - Mitigation: keep runtime/test surfaces and validation fixes explicitly out of scope in the docs packet and workpad.

## Status Update 2026-04-13
- Completed milestones 1 through 3:
  - docs-first packet registered and initial workpad published
  - truthful docs-review fallback recorded after the child-stream provenance failure and the packet-local `docs/TASKS.md` line-budget fix
  - fresh `docs:freshness` repro captured and classified into the March 13 (`1138` through `1163`) and January release-doc cohorts
- Milestone 4 resolved as blocker routing rather than local repair:
  - follow-up `CO-167` now owns the actual stale-cohort refresh because the repair spans `198` docs across two cohorts
- Remaining work in this lane:
  - refresh the packet/workpad/checklist to final closeout state
  - decide whether the docs-only classification diff should be handed off through the standard PR/review path or closed without further repo mutation beyond this packet

## Approvals
- Reviewer: pending docs-review
- Date: 2026-04-13
