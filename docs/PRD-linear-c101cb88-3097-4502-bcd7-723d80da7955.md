# PRD - CO workflow: refresh Apr 16 docs:freshness stale cohort blocking handoffs

## Traceability
- Linear issue: `CO-201` / `c101cb88-3097-4502-bcd7-723d80da7955`
- Linear URL: https://linear.app/asabeko/issue/CO-201/co-workflow-refresh-apr-16-docsfreshness-stale-cohort-blocking
- Source issue: `CO-195` / `4122489e-1a3b-43cf-a181-e98ada0a55e1`
- Current baseline: `origin/main` at `79cb4a193d99b08f989f5e9b51ebe79edaca8b0a`

## Problem
On 2026-04-16 UTC, unrelated review handoffs are blocked by repo-wide docs freshness rollover debt. `spec-guard` fails on an Apr 16 stale spec (`last_review=2026-03-16`), `docs:freshness` reports blocking historical packet rows, and `docs:freshness:maintain` cannot produce a passable unrelated-feature decision while the current repo-wide baseline is over rolling policy capacity.

This is not a Codex CLI adoption decision and not a feature-lane packet issue. It is a docs-freshness baseline owner lane for the historical Task Packet, Task Mirror, and Report Only cohort that rolled stale on Apr 16.

## Intent Checksum
Protected terms and surfaces: `docs:freshness`, `spec-guard`, `docs:freshness:maintain`, `block_diff_local`, `block_policy_over_budget`, `CO-175`, `rolling freshness cohorts`, `Task Packet`, `Task Mirror`, `Report Only`, `last_review=2026-03-16`, `current diff health`, and `repo-wide docs baseline`.

Wrong interpretations to reject:
- Do not make CO-195 refresh unrelated historical task/spec/report cohorts as part of the Codex CLI 0.121.0 posture decision.
- Do not weaken `docs:freshness`, `spec-guard`, or provider-worker handoff policy.
- Do not silently mass-bump `last_review` without classification and review rationale.
- Do not hide CO-175 rolling debt or expand the rolling caps to pass an unrelated lane.
- Do not treat the 0.121.0 cloud-canary blocker as resolved by this docs freshness work.

## Goals
- Reproduce the Apr 16 baseline failure on current `origin/main`.
- Classify all blocking stale entries by `last_review`, doc class, path family, task/spec lineage, and CO-175 relationship.
- Apply the smallest truthful action that restores handoff validation without weakening freshness gates.
- Preserve CO-175 owner, rolling cohort provenance, and policy-cap truth.
- Record before/after machine-checkable artifacts under the CO-201 `out/` directory.
- Confirm CO-195 can rerun its Core Lane without the Apr 16 docs freshness blocker.

## Non-Goals
- No Codex CLI 0.121.0 adoption decision changes.
- No cloud canary, auth-profile, provider runtime, app-server, or review-wrapper behavior changes.
- No global disabling or warning-only conversion of docs freshness gates.
- No rolling freshness cohort cap increase unless a separate policy lane explicitly approves it.
- No plugin marketplace, MCP Apps, or spark-policy work.

## Parity Matrix
| Surface | Current | Reference | Target |
| --- | --- | --- | --- |
| `spec-guard` | Fails on `tasks/specs/1220...` with `last_review=2026-03-16` now 31 days old. | Active specs fail when stale unless a declared eligible rolling policy applies. | Green after reviewed refresh/archive/reclassify action. |
| `docs:freshness` | Fails with 192 blocking stale docs plus 221 CO-175 rolling rows on current `origin/main`. | CO-175 rolling debt remains visible and owned within policy. | No blocking stale docs, with CO-175 debt still visible. |
| `docs:freshness:maintain` | Reports `block_diff_local` and over-budget policy capacity for the baseline. | Provider-worker gates distinguish current diff health from repo-wide historical debt. | Unrelated clean feature diffs can pass without refreshing unrelated historical docs. |
| CO-195 handoff | Posture decision work is blocked by repo-wide freshness debt. | Version adoption lanes should not absorb baseline maintenance. | CO-195 can rerun Core Lane after baseline owner repair. |

## Acceptance Criteria
1. Reproduce `spec-guard`, `docs:freshness`, and `docs:freshness:maintain` failures on the selected baseline and save artifacts under `out/linear-c101cb88-3097-4502-bcd7-723d80da7955/`.
2. Classify all blocking stale entries by review date, class, path family, task/spec lineage, and relationship to CO-175.
3. Document and implement the smallest truthful action without weakening `docs:freshness`.
4. Restore `node scripts/spec-guard.mjs`, `npm run docs:freshness`, and `BASE_SHA=origin/main npm run docs:freshness:maintain` to green or an explicitly passable owned-debt decision for unrelated clean diffs.
5. Preserve CO-175 provenance in `docs/guides/docs-freshness-cohorts.md` or the relevant policy surface.
6. Update docs/task mirrors and machine-readable reports consistently.
7. Confirm CO-195 PR #490 can rerun Core Lane without the Apr 16 blocker.

## Stakeholders
- Provider-worker lanes blocked by repo-wide docs freshness debt.
- Maintainers of docs freshness, spec guard, and current-diff maintenance policy.
- Reviewers validating CO-195 and other version/feature handoffs.

## Open Questions
- After the reviewed Apr 16 refresh, does any residual stale cohort remain outside CO-175 that should become a separate owner issue rather than a date refresh?
