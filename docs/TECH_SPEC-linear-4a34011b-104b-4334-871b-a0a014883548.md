---
id: 20260418-linear-4a34011b-104b-4334-871b-a0a014883548
title: CO STATUS observability stop direct co-status --format json fetch refusals on healthy control-hosts
relates_to: docs/PRD-linear-4a34011b-104b-4334-871b-a0a014883548.md
risk: high
owners:
  - Codex
last_review: 2026-04-18
---

## Canonical Reference
- Canonical TECH_SPEC: `tasks/specs/linear-4a34011b-104b-4334-871b-a0a014883548.md`
- PRD: `docs/PRD-linear-4a34011b-104b-4334-871b-a0a014883548.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-4a34011b-104b-4334-871b-a0a014883548.md`
- Task checklist: `tasks/tasks-linear-4a34011b-104b-4334-871b-a0a014883548.md`

## Traceability
- Linear issue: `CO-246` / `4a34011b-104b-4334-871b-a0a014883548`
- Source anchor: `ctx:sha256:ed8d7cee28dd1ae41d954a39ca3c8ec761012ade0011d04e9e673f3abb5fa86a#chunk:c000001`
- Parent issue-context evidence: `.runs/linear-4a34011b-104b-4334-871b-a0a014883548/cli/2026-04-18T08-35-23-027Z-c520a178/provider-linear-issue-context-cache.json`
- Docs packet child lane manifest: `.runs/linear-4a34011b-104b-4334-871b-a0a014883548-co-246-docs-packet/cli/2026-04-18T08-43-23-951Z-861dc439/manifest.json`
- Shared source-0 note: the child-lane `source-0` payload is metadata-only; protected wording is anchored on the live `CO-246` issue body preserved in the parent issue-context cache.

## Summary
- Objective: stop direct `co-status --format json` from failing on intermittent stale/dead endpoint reads when the control-host remains healthy.
- Scope:
  - bounded endpoint re-resolution or equivalent truthful recovery for the direct JSON path
  - explicit alignment with attach-style recovery
  - explicit boundaries against `CO-243`, `CO-223`, `CO-41`, `CO-194`, and `CO-211`
- Constraints:
  - keep the patch local to the CLI shell / attach recovery seam
  - preserve current JSON snapshot schema
  - preserve truthful failure messages and genuine host-health alarms

## Issue-Shaping Contract
- User-request translation carried forward: this lane is about the direct JSON snapshot path failing on stale/dead control endpoints while the control-host remains healthy enough to keep serving after endpoint re-resolution. It is not a `provider_intake` truth issue, not a `tracked.linear` truth issue, and not a generic refresh-stuck / restart-required host-failure issue.
- Protected terms / exact artifact and surface names:
  - `co-status --format json`
  - `ECONNREFUSED`
  - healthy control-host
  - direct JSON snapshot path
  - endpoint re-resolution
  - attach-style recovery
  - intermittent dashboard-read refusal
  - `.runs/local-mcp/cli/control-host/provider-intake-state.json`
  - `polling.stuck=false`
  - `polling.restart_required=false`
  - `coStatusCliShell.ts`
  - `coStatusAttachCliShell.ts`
  - `readUiDatasetWithEndpointRecovery`
  - `resolveAttachTarget`
  - `fetchUiDataset`
- Nearby wrong interpretations to reject:
  - `CO-243` provider-intake truth under concurrent running claims
  - `CO-223` stale top-level `tracked.linear` fallback truth
  - `CO-41`, `CO-194`, or `CO-211` genuine stuck/restart-required/probe-timeout host failures
  - suppressing fetch errors without retry/re-resolution behavior
- Explicit non-goals carried forward:
  - no payload-semantic redesign for `provider_intake` or `tracked.linear`
  - no weakening of genuine host-health alarms
  - no attach-only workaround as the intended steady state
  - no source or test edits in this child lane

## Parity / Alignment Matrix
- Current truth:
  - `coStatusCliShell.ts` resolves the attach target once and fetches `/ui/data.json` once for `--format json`
  - `coStatusAttachCliShell.ts` already recovers interactive reads through `readUiDatasetWithEndpointRecovery`
  - live `CO-246` evidence shows intermittent `ECONNREFUSED` while healthy-host evidence still exists
- Reference truth:
  - direct JSON path should be able to recover from stale/dead endpoints the same way attach mode already does, while staying truthful if the refreshed endpoint is still unreadable
  - adjacent issue ownership should stay explicit
- Target truth / intended delta:
  - direct JSON path re-resolves or otherwise truthfully retries after dead-port snapshot reads
  - attach-style recovery remains the behavior reference
  - healthy-host intermittent refusal stays distinct from adjacent issues

## Readiness Gate
- Not done if:
  - direct `co-status --format json` still dies on the first stale/dead endpoint read with no bounded recovery attempt
  - the repair hides real auth/network/timeout failures
  - the implementation broadens into `CO-243`, `CO-223`, `CO-41`, `CO-194`, or `CO-211`
- Pre-implementation issue-quality review evidence:
  - 2026-04-18: child-lane review confirms this is a direct-JSON recovery seam whose correctness depends on preserving exact wording for intermittent refusal, healthy-host evidence, and adjacent-issue boundaries. The packet therefore rejects broader host-health or truth-semantic reinterpretation.
- Safeguard ownership split:
  - child lane owns only the packet files and `tasks/index.json`
  - parent lane owns implementation, focused tests, docs-review, validation, Linear/workpad reconciliation, PR lifecycle, and merge

## Technical Requirements
1. Preserve the exact `CO-246` checksum around `co-status --format json`, `ECONNREFUSED`, healthy control-host, direct JSON snapshot path, endpoint re-resolution, attach-style recovery, and intermittent dashboard-read refusal.
2. Reproduce the dead-port direct JSON seam while healthy-host evidence remains available.
3. Add direct JSON recovery that re-resolves the control endpoint or otherwise handles stale/dead-port snapshot reads truthfully.
4. Preserve attach-mode recovery behavior and keep the direct JSON path aligned with it where appropriate.
5. Add focused regression coverage for stale-endpoint / dead-port direct JSON reads.
6. Keep closeout evidence explicit that the fix is not `CO-243`, `CO-223`, `CO-41`, `CO-194`, or `CO-211`.

## Architecture & Data
- Architecture / design adjustments:
  - keep ownership in `coStatusCliShell.ts` and the existing recovery helpers in `coStatusAttachCliShell.ts`
  - prefer one bounded recovery contract shared or mirrored across direct JSON and attach reads
  - preserve the existing direct JSON payload contract
- Expected implementation surfaces:
  - `orchestrator/src/cli/coStatusCliShell.ts`
  - `orchestrator/src/cli/coStatusAttachCliShell.ts`
- Data model / artifact constraints:
  - no schema drift for the JSON snapshot payload
  - preserve explicit network/auth/timeout guidance when recovery cannot succeed
  - preserve auditability of healthy-host evidence in closeout artifacts

## Validation Plan
- Child-lane checks:
  - `python3 - <<'PY'\nimport json, pathlib\njson.loads(pathlib.Path('tasks/index.json').read_text())\nPY`
  - protected-term grep over the six packet/mirror files plus `tasks/index.json`
  - `git diff --check` over the declared docs scope
- Parent-lane checks:
  - focused `orchestrator/tests/CoStatusCliShell.test.ts`
  - focused `orchestrator/tests/CoStatusAttachCliShell.test.ts`
  - parent docs-review before implementation
  - parent `node scripts/spec-guard.mjs --dry-run` after packet acceptance
- Rollback plan:
  - revert the bounded direct-JSON recovery if it hides real failures or breaks adjacent issue boundaries

## Approvals
- Reviewer: docs child lane self-review for packet shape and issue boundaries.
- Date: 2026-04-18
