# ACTION_PLAN - CO STATUS / intake: prefer Rework reset truth over stale rehydrated Merging state and closed PR attachment

## Traceability
- Linear issue: `CO-220` / `27a61a75-236f-40c6-8cb0-8e4921708d91`
- Task id: `linear-27a61a75-236f-40c6-8cb0-8e4921708d91`

## Plan
1. Register the docs-first packet, the task mirrors, and the single active Linear workpad before source edits.
2. Keep source ownership in the parent lane while the `regression-tests` child lane owns the focused test files.
3. Narrow the current-truth seam for stale rehydrated `Merging` / attachment projection so reset `Rework` truth wins without weakening real live `Merging`.
4. Accept or reject the child-lane test patch based on file-scope cleanliness, then run focused regressions and the required repo validation floor.
5. Run manifest-backed standalone review plus an explicit elegance pass before any review handoff.

## Final Validation
- Docs packet created, mirrored, and approved by docs-review.
- Focused regressions prove reset `Rework` current-truth selection and stale-attachment historical demotion.
- Required provider-worker validation floor passes.
- Standalone review and elegance review are recorded before handoff.

## Rollback
- Revert the current-truth selection seam if it suppresses real live `Merging` or real attached PR truth.
- Prefer fail-closed historical/current separation over broad attachment or projection deletion.
