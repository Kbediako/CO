# ACTION_PLAN - CO STATUS / intake: prefer Rework reset truth over stale rehydrated Merging state and closed PR attachment

## User-Request Translation
- Complete CO-220 by making live reset truth authoritative: once the issue is back in `Rework` with no new PR, retired attempt `running` / `Merging` state and closed PR evidence must not project as current.
- Preserve audit/debug history for retired attempts while keeping real live `Merging` issues and real current PR attachments visible.

## Protected Terms / Exact Surface Names
- Surfaces: `co-status`, intake-derived projection, `linear issue-context`.
- Workflow truth: `Rework`, `Merging`, `running`, `no attached PR yet`.
- Artifact truth: original stale attachment example `#509`, current worker reset retired PR `#516`, GitHub PR attachments, historical PR evidence.

## Nearby Wrong Interpretations To Reject
- Do not solve this by deleting retired attempt artifacts or hiding all PR attachments.
- Do not treat every closed PR as irrelevant; closed retired PRs remain historical evidence.
- Do not suppress real live `Merging` state or a real current attached PR.
- Do not expand CO-210 beyond its child-lane hydration/progress scope.

## Explicit Non-Goals
- Do not regress CO-214 merge-closeout cleanup or CO-219 dead-proof running-count fixes.
- Do not broaden the status surfaces beyond the reset truth split described in CO-220.
- Do not remove historical artifacts from previous attempts.

## Not Done If
- A reset issue can still show stale rehydrated `Merging` / attached-PR truth from a retired attempt after live truth is `Rework` with no new PR.
- `issue-context` still presents a closed retired PR as current attached PR truth.
- Only one status/projection surface is fixed while another keeps the stale truth split.

## Current / Reference / Target Parity Matrix
| Surface | Current broken shape | Reference truth | Target behavior |
| --- | --- | --- | --- |
| `co-status` | Stale retired-attempt `running` / `Merging` can outlive a reset. | Live Linear issue state is `Rework`. | Display reset `Rework` truth and keep retired progress historical-only. |
| Intake-derived projection | Rehydrated claim/proof state can project a retired attempt as current. | Current reset attempt has no new attached PR yet. | Do not select retired lifecycle state as current when reset truth is fresher. |
| `issue-context` | Closed stale attachments can appear as current attached PR truth. | Closed retired PRs are audit/debug history. | Classify closed retired PRs as historical, unknown, or conflicting as appropriate, not current. |
| Live `Merging` issues | Risk of over-suppressing valid active merge work. | Real live `Merging` and real current PR truth remain authoritative. | Preserve current PR projection for real active merge/review states. |

## Plan
1. Keep the docs packet, task mirrors, and single Linear workpad current.
2. Land the narrow current-truth selection change in observability plus `issue-context` attachment classification.
3. Preserve real live `Merging` and real current PR behavior with focused regressions.
4. Run the required validation floor, standalone review fallback if needed, and explicit elegance pass before handoff.

## Rollback
- Revert the current-truth predicate if it suppresses real live merge or current-PR truth.
