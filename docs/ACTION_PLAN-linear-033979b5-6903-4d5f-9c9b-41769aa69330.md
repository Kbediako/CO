# ACTION_PLAN - CO-447 ready-review CodeRabbit acknowledgement-only mention classification

## Summary
- Goal: keep `ready-review` from treating acknowledgement-only `@coderabbitai` comments as fresh CodeRabbit rereview/resolve requests.
- Scope: docs-first packet in this child lane; parent-owned implementation in `scripts/lib/pr-watch-merge.js`, focused `tests/pr-watch-merge.spec.ts` coverage, validation, Linear state, workpad, PR lifecycle, and review handoff.
- Assumptions: the parent lane owns all source/test changes and can widen implementation scope only if source inspection proves the classifier cannot stay isolated.

## Issue Readiness Gate
- Intent checksum / protected terms carried forward:
  - `ready-review classification`
  - `CodeRabbit acknowledgement-only mentions`
  - `explicit rereview/resolve requests`
  - `unacknowledged_bot_feedback`
  - `unresolved review-thread handling`
  - `CodeRabbit checks`
  - `explicit rereview requests`
  - `resolveLatestBotRereviewRequests`
  - `bot_rereview_pending=coderabbitai`
- Not done if:
  - acknowledgement-only `@coderabbitai` comments still create a CodeRabbit rereview wait
  - explicit CodeRabbit rereview or resolve requests stop blocking
  - `unacknowledged_bot_feedback` stops blocking current-head actionable bot comments
  - unresolved review threads stop blocking
  - stale or failed CodeRabbit check evidence clears an explicit request
  - source/test/Linear/GitHub lifecycle edits happen in this docs-only child lane
- Pre-implementation issue-quality review:
  - 2026-04-30: packet preserves both sides of the classifier contract: acknowledgement-only mentions are ignored, explicit rereview/resolve requests remain blocking.
  - 2026-04-30: packet preserves all issue constraints named by the parent prompt: `unacknowledged_bot_feedback`, unresolved review-thread handling, CodeRabbit checks, and explicit rereview requests.
- Fallback / refactor decision:
  - Applies: `Yes`, because this removes an over-broad mention-as-request seam in review handoff.
  - Decision: remove mention-only CodeRabbit request classification; retain existing review-readiness gates as non-expiring correctness contracts.
- Durable retention evidence:
  - `unacknowledged_bot_feedback`, unresolved review threads, required checks, merge state, and CodeRabbit current-head proof remain supported readiness contracts.
- Large-refactor check:
  - A large refactor is not expected. Parent should keep the implementation to request-intent classification unless source inspection proves the helper cannot be separated from feedback, thread, or check gates.

## Milestones & Sequencing
1. Register docs-first packet and task index entry.
2. Parent inspects `scripts/lib/pr-watch-merge.js` request extraction around `extractMentionedBotKinds` and `resolveLatestBotRereviewRequests`.
3. Parent adds or refines request-intent detection so acknowledgement-only CodeRabbit mentions are ignored.
4. Parent preserves explicit rereview and resolve request handling.
5. Parent adds focused `tests/pr-watch-merge.spec.ts` cases for acknowledgement-only, explicit rereview, explicit resolve, and existing gate preservation.
6. Parent runs focused tests, then selected broader validation/review gates.
7. Parent updates workpad, PR, and `ready-review` handoff evidence only after blockers are truly clear.

## Dependencies
- `scripts/lib/pr-watch-merge.js`
- `tests/pr-watch-merge.spec.ts`
- GitHub issue comments, pull comments, reviews, review threads, and current-head status/check rollup.
- Parent-owned Linear workpad and PR lifecycle.

## Validation
- Checks / tests:
  - child lane scoped JSON parse for `tasks/index.json`
  - child lane scoped protected-term scan across packet files
  - child lane `git diff --check` across owned files
  - parent-owned focused `tests/pr-watch-merge.spec.ts` run after implementation
  - parent-owned broader build/lint/test/docs/review gates selected before handoff
- Rollback plan:
  - Revert only the classifier/test changes if explicit rereview/resolve requests stop blocking or if any existing readiness gate is weakened. Preserve this packet as issue traceability unless parent relaunches with widened scope.

## Risks & Mitigations
- Risk: classifier becomes too permissive and continues false-blocking acknowledgement-only mentions.
  - Mitigation: add a direct acknowledgement-only regression.
- Risk: classifier becomes too strict and ignores real CodeRabbit rereview or resolve requests.
  - Mitigation: add explicit rereview and explicit resolve regressions.
- Risk: parent accidentally weakens unacknowledged feedback or unresolved threads while fixing the latch.
  - Mitigation: keep existing gate tests green and add guard-preservation expectations if needed.
- Risk: status output becomes less diagnostic.
  - Mitigation: keep existing gate reason strings and ensure real blockers still name `bot_rereview_pending`, `unacknowledged_bot_feedback`, or `unresolved_threads`.

## Approvals
- Reviewer: parent lane / docs-review pending
- Date: 2026-04-30
