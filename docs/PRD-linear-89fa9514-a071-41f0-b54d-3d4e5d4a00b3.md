# PRD - CO: resolve weakly-owned historical reference and archive residue surfaced by repo stewardship audit

## Added by Bootstrap 2026-04-11

## Traceability
- Linear issue: `CO-126` / `89fa9514-a071-41f0-b54d-3d4e5d4a00b3`
- Linear URL: https://linear.app/asabeko/issue/CO-126/co-resolve-weakly-owned-historical-reference-and-archive-residue
- Source issue: `CO-124` / `d43b6785-88d6-442b-a34e-2ad19d4f723a`

## Summary
- Problem Statement: `CO-124` added the recurring `repo:stewardship` audit, and current main now uses that contract to flag a bounded historical-residue cluster. The live 2026-04-11 audit already differs slightly from the issue snapshot: `archives/REPORT.mcp_code_mode.md` is no longer tracked, but six historical surfaces still land as `update` because they have no qualifying local `README.md` / rationale anchor inside `reference/` or `archives/`.
- Desired Outcome: make a truthful keep/delete decision for each currently flagged historical surface, add the minimum local rationale anchors for any retained `reference/**` subtree, delete the orphaned archive report if it no longer has a truthful retained purpose, and rerun `repo:stewardship` so the targeted residue cluster no longer appears as unexplained action-required output.

## User Request Translation (Context Anchor)
- User intent / needs (in your own words): keep the follow-up narrowly focused on the historical residue surfaced by the new stewardship audit, not on changing the audit contract itself. The lane should explicitly decide which retained historical files still deserve to live in `reference/**` or `archives/**`, add the smallest rationale anchors that satisfy the stewardship contract, and remove anything that no longer has a truthful purpose.
- Success criteria / acceptance:
  - each live flagged `reference/**` or `archives/**` surface has an explicit keep/delete decision
  - retained historical surfaces gain a local `README.md` / rationale anchor in the correct subtree
  - `repo:stewardship` no longer reports the targeted residue as unexplained `update` output
  - the diff stays limited to the residue cluster plus minimum packet/rationale updates
- Constraints / non-goals:
  - do not reopen `CO-124` as an audit-contract redesign lane
  - do not broaden into unrelated README/runtime/provider-worker cleanup
  - do not keep historical files by default without explicit rationale

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `reference/**`
  - `archives/**`
  - `retain_with_rationale`
  - `local README/rationale anchor`
  - `repo:stewardship`
- Protected terms / exact artifact and surface names:
  - `archives/REPORT.mcp_code_mode.json`
  - `reference/0956-subagents-skill-codex-cli-refresh/*`
  - `reference/mirror.config.wp.example.json`
- Nearby wrong interpretations to reject:
  - reopen the `CO-124` audit implementation lane
  - perform unrelated docs/runtime cleanup while touching these files
  - keep every flagged historical file by default without explicit rationale

## Parity / Alignment Matrix
- Current truth:
  - `npm run repo:stewardship` on 2026-04-11 reports `4610` tracked files, `6` action-required historical surfaces, and `0` uncatalogued surfaces
  - the live flagged set is:
    - `archives/REPORT.mcp_code_mode.json`
    - `reference/0956-subagents-skill-codex-cli-refresh/README-snippet.txt`
    - `reference/0956-subagents-skill-codex-cli-refresh/codex-cli-refresh-dry-run.txt`
    - `reference/0956-subagents-skill-codex-cli-refresh/codex-cli-refresh-help.txt`
    - `reference/0956-subagents-skill-codex-cli-refresh/global-guidance-manifest.md`
    - `reference/mirror.config.wp.example.json`
  - `archives/REPORT.mcp_code_mode.md` from the issue text is already deleted and is not part of the current tracked residue set
- Reference truth:
  - historical files are only retained when they still have a truthful retained purpose and a local rationale anchor inside the applicable stewardship boundary
  - the 0956 evidence files and the WordPress mirror example should only remain if current repo docs/specs still rely on them
- Target truth / intended delta:
  - the 0956 evidence pack remains in `reference/**` with a subtree-local rationale anchor
  - `reference/mirror.config.wp.example.json` remains only if a root-level rationale anchor under `reference/` truthfully explains why it is kept
  - the orphaned archive report is deleted unless new live evidence justifies keeping it
- Explicitly out-of-scope differences:
  - repo-wide stewardship catalog or command changes
  - unrelated cleanup of other historical surfaces already classified by the audit
  - reopening `CO-75`, `CO-88`, or `CO-102`

## Not Done If
- Any currently flagged historical surface still lacks an explicit keep/delete decision.
- A retained historical subtree still has no local `README.md` / rationale anchor in its subtree.
- The lane closes without rerunning `repo:stewardship` or otherwise proving the targeted residue no longer appears as unexplained action-required output.

## Goals
- Preserve the current `repo:stewardship` contract and use it to resolve the bounded residue cluster.
- Keep still-referenced historical evidence under `reference/**` only when local rationale is explicit.
- Remove the orphaned archive payload if no current docs/tasks/commands still rely on it.
- Keep the packet and implementation truthful about the already-deleted `archives/REPORT.mcp_code_mode.md`.

## Non-Goals
- Changing `docs/repo-stewardship-catalog.json` semantics unless a real bug is found.
- Rewriting unrelated historical references or archive directories.
- Broadening into runtime, review-wrapper, or provider-worker behavior.

## Stakeholders
- Product: CO maintainers relying on truthful repo stewardship follow-ups.
- Engineering: maintainers of historical reference surfaces, audit outputs, and task archives.
- Design: not applicable.

## Metrics & Guardrails
- Primary Success Metrics:
  - the targeted six-path historical residue cluster no longer appears as unexplained `update`
  - retained reference surfaces have local rationale anchors that match their current use
  - no unrelated repo stewardship classes change
- Guardrails / Error Budgets:
  - preserve fail-closed stewardship behavior
  - keep the diff bounded to residue + rationale anchors + required docs/task mirrors
  - prefer deletion over weakly-owned retention stories

## User Experience
- Personas:
  - maintainers reading `repo:stewardship` output
  - future agents deciding whether a historical surface is intentionally retained
- User Journeys:
  - a maintainer runs `npm run repo:stewardship` and sees the residue cluster resolved rather than silently waived
  - a future agent opens the local rationale anchors under `reference/` and can tell why those files still live in the repo

## Technical Considerations
- Architectural Notes:
  - rely on the existing `reference/**` and `archives/**` `retain_with_rationale` rules in `docs/repo-stewardship-catalog.json`
  - prove current retained use before keeping historical reference payloads
  - delete orphaned historical residue instead of adding weak anchors
- Dependencies / Integrations:
  - `docs/repo-stewardship-catalog.json`
  - `scripts/repo-stewardship-audit.mjs`
  - `tasks/specs/0956-subagents-skill-codex-cli-refresh.md`
  - `docs/guides/pixel-perfect-local-clones.md`

## Open Questions
- None expected at bootstrap; delete-vs-keep decisions will follow the current live references and audit output.

## Approvals
- Product: pending
- Engineering: pending
- Design: not applicable
