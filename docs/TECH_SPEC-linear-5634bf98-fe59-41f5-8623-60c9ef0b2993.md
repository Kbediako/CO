---
id: 20260524-linear-5634bf98-fe59-41f5-8623-60c9ef0b2993
title: CO-579 replace terminal CO-575 docs freshness maintenance owner
relates_to: docs/PRD-linear-5634bf98-fe59-41f5-8623-60c9ef0b2993.md
risk: high
owners:
  - Codex
last_review: 2026-05-24
related_action_plan: docs/ACTION_PLAN-linear-5634bf98-fe59-41f5-8623-60c9ef0b2993.md
task_checklists:
  - tasks/tasks-linear-5634bf98-fe59-41f5-8623-60c9ef0b2993.md
---

# TECH_SPEC - CO-579 replace terminal CO-575 docs freshness maintenance owner

## Summary
- Objective: restore a non-terminal live global owner for `docs:freshness:maintain` and remove the root causes that keep task-packet freshness drift recurring.
- Scope: CO-579 docs packet and mirrors, `docs/docs-catalog.json` owner binding, `docs/guides/docs-freshness-cohorts.md` current-owner prose, task-index-backed freshness resolution for active indexed packet mirrors, guarded historical numeric task-key aliasing, one CO-300 finding registry reclassification, two CO-545 historical path prose repairs surfaced by `docs:check`, and linked backlog issue CO-580 for the broader lifecycle/finalizer refactor.
- Constraints: no freshness/spec gate weakening, no blind review-date changes, no deletion of historical packets, and no provider/runtime/review-wrapper behavior changes.

## Issue-Shaping Contract
- User-request translation carried forward: re-home global `docs:freshness:maintain` owner metadata from terminal `CO-575` to live `CO-579` while preserving `CO-575` lineage and exact-key `CO-568`/`CO-569` cohort owners.
- Protected terms / exact artifact and surface names: `docs:freshness`, `docs:freshness:maintain`, `canonical_owner_key=docs:freshness:maintain`, `owner_issue=CO-575`, `configured_owner_terminal`, `docs/docs-catalog.json`, `CO-568`, `CO-569`, feature-lane review handoff.
- Nearby wrong interpretations to reject: terminal CO-575 is usable, the fix can delete historical docs, blind bumps are acceptable, CO-568/CO-569 can be hidden, or this is provider-intake/create-follow-up work.
- Lifecycle interpretation guardrail: do not infer archive readiness or hard terminal lifecycle debt from task-spec frontmatter alone when registry/task-index evidence intentionally keeps a packet active-reviewed because linked local obligations remain.
- Explicit non-goals carried forward: no gate weakening, no broad docs archive cleanup, no provider-intake changes, no review-wrapper changes, no runtime freshness changes.

## Readiness Gate
- Not done if:
  - global owner still points at terminal CO-575
  - CO-575 lineage disappears
  - CO-568/CO-569 exact-key cohort owners are overwritten
  - docs gates are cleared by weakening policy
  - CO-545 evidence meaning changes while fixing path syntax
- Pre-implementation issue-quality review evidence:
  - 2026-05-24: CO-527 packet validation reproduced `docs:freshness:maintain` terminal-owner failure with `owner_issue=CO-575`, state `Done` / `completed`, while CO-568 and CO-569 verified live Backlog for their cohort keys. CO-579 was created directly in Linear to avoid using the known CO-527 `create-follow-up` partial-success path.
- Safeguard ownership split:
  - parent owns the small owner-binding change and packet
  - provider workers remain blocked until the global owner recurrence is routed or fixed

## Technical Requirements
- Functional requirements:
  1. Change only `policies.rolling_freshness_cohorts.owner_issue` from `CO-575` to `CO-579`.
  2. Leave `canonical_owner_key`, team id, project id, baseline cohorts, and exact-key cohort owner mappings unchanged.
  3. Preserve CO-575 terminal lineage in issue/task docs and docs freshness guide lineage.
  4. Update `docs/guides/docs-freshness-cohorts.md` current-policy prose to match the live owner.
  5. Convert two CO-545 historical local worktree references from backticked path syntax to plain prose so `docs:check` no longer treats them as current repo paths.
  6. Register CO-579 packet in task and freshness mirrors.
  7. Extend task-packet lifecycle indexing to retain non-terminal task-index rows by packet path.
  8. In `docs:freshness`, use the newer task-index `last_review` only for an indexed task-packet path and include report-level `task_index_review_overrides` metadata (`last_review_source`, registry date, task-index date).
  9. Resolve historical numeric task-key aliases so `1318-...` task rows cover PRD/ACTION mirrors named without the numeric prefix.
  10. Report rolling cohorts with their exact `canonical_owner_issues` owner when a baseline cohort has one in both `docs:freshness` and `spec-guard`.
  11. Reclassify the already-archived CO-300 classification finding row from active 30-day review to archived 365-day review.
  12. Reclassify the 21 pre-expiry task specs inactive `done` only after live Linear issue-context verifies the source issues are `Done/completed`; update matching task-index rows and mark full historical packet registry rows `retained_terminal_packet` without deleting packet history or claiming archive-stub safety.
  13. Make implementation-docs archive automation treat `retained_terminal_packet` as a retain-on-main lifecycle status only when task-index or registry task-status terminal evidence exists, so retention age and line thresholds cannot convert valid retained packets into archive stubs while invalid stray status-only rows still archive when otherwise eligible.
  14. File linked CO-580 for a shared docs freshness lifecycle resolver and owner-finalizer contract instead of landing an over-broad frontmatter-only terminal classifier in CO-579.
- Non-functional requirements:
  - keep the diff small and auditable
  - maintain strict fail-closed owner verification
  - preserve stale/rolling debt visibility
- Interfaces / contracts:
  - `docs/docs-catalog.json`
  - `docs/guides/docs-freshness-cohorts.md`
  - `scripts/docs-freshness.mjs`
  - `scripts/docs-freshness-maintain.mjs`
  - `scripts/implementation-docs-archive.mjs`
  - `scripts/docs-hygiene.ts`

## Fallback Expiry / Refactor Decision
- Applies to fallback, compatibility, legacy, stale, cached, break-glass, or minor-seam behavior? `Yes`.

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `docs:freshness:maintain` global owner binding | Terminal `CO-575` remains configured as live global owner. | remove fallback | CO-579 | `docs:freshness:maintain --check` reports `configured_owner_terminal`. | 2026-05-23 | 2026-05-24 | Not retained | `docs/docs-catalog.json` points to non-terminal CO-579 and maintain check verifies live owner. | docs freshness maintain, docs check, spec guard. |
| Task-index review-date override | Newer `tasks/index.json` `last_review` can override older registry review dates for indexed task-packet paths. | expire fallback | CO-579 / CO-580 | Task-index review date is newer than registry date for the same task-packet path. | 2026-05-24 | 2026-06-07 | 2026-06-23 | CO-580 lands a centralized lifecycle resolver/finalizer or task-packet mirrors are explicit/archive-clean so registry freshness no longer needs to be inferred from task-index authority. | P1 regressions for statusless entries and numeric alias collisions, task-index override audit output, docs freshness, docs freshness maintain. |
| Historical numeric task-key aliases | Older task packets can list task/spec/docs paths under `1318-...` while PRD/ACTION mirrors omit the numeric prefix. | expire fallback | CO-579 / CO-580 | Historical PRD/ACTION mirrors are not covered by task-index review authority without numeric aliasing. | 2026-05-24 | 2026-06-07 | 2026-06-23 | Historical mirrors are archive-safe, retained as explicit terminal packets, or task-index rows explicitly enumerate all mirror paths. | P1 numeric alias collision regression plus live docs freshness override audit output. |
| Lifecycle authority split | Terminal source state, spec frontmatter, registry status, task-index status, and linked checklist readiness can disagree. | expire fallback | CO-580 | CO-579 review found a frontmatter-only terminal classifier would block existing active-reviewed packets with unresolved local obligations. | 2026-05-24 | 2026-06-07 | 2026-06-23 | CO-580 lands a shared resolver/finalizer or an equivalent smaller proven contract. | CO-580 linked Backlog issue with labels and acceptance criteria. |

- Large-refactor check: a broad lifecycle/finalizer refactor is warranted and tracked by CO-580, but CO-579 stays bounded to the live owner re-home plus guarded task-index freshness authority, so the current gate can pass without adding a second lifecycle authority.
- Minor-seam decision: the remaining task-index freshness seam is accepted only as an expiring fallback through CO-580, with active-status gating, unique-alias gating, report-level override audit output, and focused regressions.

## Validation Plan
- Tests / checks:
  - JSON parse for `tasks/index.json`, `docs/docs-freshness-registry.json`, and `docs/docs-catalog.json`
  - `git diff --check`
  - `npx vitest run tests/docs-freshness.spec.ts`
  - `npx vitest run tests/implementation-docs-archive.spec.ts`
  - retained archive-consumer regression coverage must include a negative stray case where `retained_terminal_packet` plus nonterminal task status archives instead of skipping
  - protected-term scan
  - `node scripts/spec-guard.mjs --dry-run`
  - `npm run docs:check`
  - `npm run docs:freshness:maintain -- --check --format json`
  - `npm run docs:freshness`
  - `node scripts/implementation-docs-archive.mjs --dry-run`
- Rollout verification:
  - Linear CO-579 remains non-terminal while configured as owner, then stays Backlog rather than Done if owned rolling debt remains
  - CO-580 exists in Backlog as the follow-up for shared lifecycle/finalizer work
  - no WIP cap pressure introduced by this routing fix

## Approvals
- Reviewer: parent CO orchestrator issue-quality review.
- Date: 2026-05-24
