# ACTION_PLAN - CO STATUS: exclude accepted dead-proof rehydration rows from running/max-allowed projection

## Added by Bootstrap 2026-04-17

## Summary
- Goal: close `CO-219` by making `co-status` and `/ui/data.json` treat accepted pending-revalidation rows with dead local proof as non-running, without changing the configured cap or hiding real live over-cap incidents.
- Scope: docs-first packet, narrow projection fix in shared running-authority code, focused runtime regressions, required validation/review/elegance gates, and normal parent-owned PR/review handoff if needed.
- Assumptions:
  - the stale row is entering active output through the shared compatibility/runtime projection
  - the existing local PID liveness helper is the correct narrow truth source for the dead-proof distinction
  - released-row stale behavior from `CO-192` / `CO-194` already covers adjacent but different shapes

## Issue Readiness Gate
- Intent checksum / protected terms carried forward:
  - preserve `CO STATUS`, `Agents: 4/3 max allowed`, `max_concurrent_agents=3`, `accepted`, `provider_issue_rehydration_pending_revalidation`, dead local proof PID, manifest `in_progress`, `co-status --format json`, `/ui/data.json`, stale `CO-196` row, and live occupancy `3`
- Not done if:
  - the stale accepted dead-proof row still contributes to running occupancy
  - the stale PID/event still render on an active running surface
  - the fix changes the configured cap instead of projection truth
  - the fix hides a real live over-cap incident
  - accepted pending-revalidation rows with genuinely live proof stop showing as running
- Pre-implementation issue-quality review:
  - 2026-04-17: this lane is bounded to running/max-allowed projection truth. It explicitly rejects cap changes, admission-policy changes, and released-row cleanup generalization.

## Milestones & Sequencing
1. Parent-owned docs packet and registry mirrors land for `CO-219`, including this action plan and the task checklist.
2. Parent runs docs-review and refreshes the workpad with the docs-first outcome or bounded fallback.
3. Parent updates the running-authority helper in `orchestrator/src/cli/control/controlRuntime.ts` so accepted pending-revalidation claims with dead local proof do not authorize current running projection.
4. Parent adds focused regression coverage in `orchestrator/tests/ControlRuntime.test.ts`, and only adds route-level proof elsewhere if the shared dataset coverage is insufficient.
5. Parent validates with the issue command `co-status --format json` or equivalent focused runtime coverage, then completes the required guard/build/lint/test/docs/review/elegance gates.
6. Parent refreshes the workpad, handles any review findings, and proceeds through PR/review lifecycle if a PR is needed.

## Dependencies
- `co-status --format json`
- `/ui/data.json`
- `orchestrator/src/cli/control/controlRuntime.ts`
- `orchestrator/tests/ControlRuntime.test.ts`
- `provider_issue_rehydration_pending_revalidation`
- local provider proof PID liveness
- `CO-125`, `CO-162`, `CO-192`, `CO-194`

## Validation
- Checks / tests:
  - focused runtime projection regression for the accepted dead-proof shape
  - focused preserved-live regression for accepted pending-revalidation with live proof
  - existing neighboring stale-proof regressions remain green
  - `node scripts/spec-guard.mjs --dry-run`
  - full validation/review/elegance floor before handoff
- Validation command from the issue:
  - `co-status --format json`
- Rollback plan:
  - revert the narrow running-authority change if it suppresses genuinely live rows
  - keep the configured cap unchanged
  - preserve the retained audit artifacts and revert classification only

## Risks & Mitigations
- Risk: the fix is too broad and hides live accepted pending-revalidation rows.
  - Mitigation: gate the new suppression on the existing dead local proof classifier, not on accepted state alone.
- Risk: the stale row is suppressed in one surface but not another.
  - Mitigation: keep the change in the shared runtime/projection seam that feeds both `co-status` and `/ui/data.json`.
- Risk: adjacent released-row stale logic regresses.
  - Mitigation: keep released-row helpers untouched and rely on focused neighboring regression coverage.

## Approvals
- Reviewer: pending docs-review and implementation validation
- Date: 2026-04-17
