# ACTION_PLAN - Coordinator Symphony-Aligned Standalone Review Structured Scope-Path Rendering Boundary

## Phase 1 - Docs and registration

- Register `1099` across PRD / TECH_SPEC / ACTION_PLAN / task checklist / `.agent` mirror / `tasks/index.json` / `docs/TASKS.md` / docs freshness registry.
- Capture the post-`1098` rationale that the next bounded review-reliability seam is structured prompt-side scope-path rendering, not broader wrapper churn.

## Phase 2 - Bounded implementation

- Refine prompt-side scope rendering in `scripts/run-review.ts`.
- Extend nearby path parsing/rendering only as needed for paired/unusual path surfaces.
- Keep `review-execution-state.ts` unchanged.

## Phase 3 - Validation and closeout

- Update focused parser/prompt tests.
- Run the standard validation lane plus pack-smoke.
- Run a live bounded review pass to verify whether structured path rendering reduces remaining speculative helper inspection.
