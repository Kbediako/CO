# ACTION_PLAN - Coordinator Symphony-Aligned Control Bootstrap Assembly Extraction

## Steps

1. Register `1082` docs-first from the `1081` next-slice note and the bounded bootstrap-assembly scout.
2. Extract the remaining bootstrap collaborator assembly from `ControlServer.start()` into one control-local helper.
3. Keep HTTP bind/listen, base URL derivation, and final bootstrap start handling in `ControlServer.start()`.
4. Add focused regressions for collaborator wiring and bootstrap sequencing.
5. Run the validation bundle, sync mirrors, and queue the next bounded Symphony-aligned seam.
