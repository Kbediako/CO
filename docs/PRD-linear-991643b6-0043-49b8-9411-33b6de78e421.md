# PRD - CO-512 governed review contract no-fallback packet

## Traceability
- Linear issue: `CO-512` / `991643b6-0043-49b8-9411-33b6de78e421`
- Task id: `linear-991643b6-0043-49b8-9411-33b6de78e421`
- Canonical spec: `tasks/specs/linear-991643b6-0043-49b8-9411-33b6de78e421.md`
- Canonical registry id: `20260514-linear-991643b6-0043-49b8-9411-33b6de78e421`
- Source anchor: `ctx:sha256:141ca5b9d78167ddcb18b23d5b89c747b145a3a2bee95da7f903b322ab037291#chunk:c000001`
- Source manifest: `.runs/linear-991643b6-0043-49b8-9411-33b6de78e421-docs-packet/cli/2026-05-14T14-45-59-322Z-a6960ee0/manifest.json`
- Source payload note: parent reconciliation found the source payload at `.runs/linear-991643b6-0043-49b8-9411-33b6de78e421/cli/2026-05-14T14-43-04-812Z-ce117406/memory/source-0/source.txt`; this packet now carries the parent implementation contract as well as the original bounded child-lane packet.

## Summary
- Problem Statement: CO-512 needs a docs-first packet before parent implementation of the governed review contract. The packet must preserve the issue's protected no-fallback wording so review handoff cannot be satisfied by stale, missing, fallback, or semantically dirty review evidence.
- Desired Outcome: PRD, TECH_SPEC mirror, ACTION_PLAN, canonical task spec, task checklist, and `tasks/index.json` registration define the no-fallback governed review contract implemented by the parent lane across schema validation, structured review inputs, telemetry, provider-worker handoff gating, proposal routing, validation, workpad, Linear state, and PR lifecycle.

## User Request Translation
- User intent / needs:
  - create the CO-512 docs-first packet inside the bounded child-lane file scope
  - register the packet in `tasks/index.json`
  - preserve protected wording around `governed review contract`, `no fallback`, and `no-fallback`
  - preserve acceptance criteria that clean governed review evidence is required before parent review handoff
  - implement the parent-owned governed review contract while preserving Linear/workpad/PR lifecycle gates
- Success criteria / acceptance:
  - all five declared packet files exist and cross-reference the same task id
  - `tasks/index.json` contains one CO-512 registration whose `paths.docs` field value is the TECH_SPEC pointer string
  - PRD, TECH_SPEC mirror, ACTION_PLAN, canonical task spec, and task checklist include acceptance criteria, non-goals, Not Done If, validation requirements, and a parseable CO-382 fallback decision table
  - protected-term scan finds `governed review contract`, `no fallback`, `no-fallback`, `clean governed review evidence`, and `review handoff`
  - the packet rejects any path where fallback review evidence, missing telemetry, stale review output, or semantically dirty verdicts satisfy handoff
- Constraints / non-goals:
  - no weakening of spec guard, docs freshness, diff budget, standalone review, CI, or `ready-review`
  - no clean handoff through prose-only output, empty findings, wrapper success, scoped title, or artifact-only prompt transport
  - no direct application of reviewer proposals; code-change proposals remain proposals
  - no raw review suggestions in learning/prompt memory

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `governed review contract`
  - `no fallback`
  - `no-fallback`
  - `acceptance criteria`
  - `review handoff`
  - `clean governed review evidence`
- Protected terms / exact artifact and surface names:
  - `docs/PRD-linear-991643b6-0043-49b8-9411-33b6de78e421.md`
  - `docs/TECH_SPEC-linear-991643b6-0043-49b8-9411-33b6de78e421.md`
  - `docs/ACTION_PLAN-linear-991643b6-0043-49b8-9411-33b6de78e421.md`
  - `tasks/specs/linear-991643b6-0043-49b8-9411-33b6de78e421.md`
  - `tasks/tasks-linear-991643b6-0043-49b8-9411-33b6de78e421.md`
  - `tasks/index.json`
  - source anchor `ctx:sha256:141ca5b9d78167ddcb18b23d5b89c747b145a3a2bee95da7f903b322ab037291#chunk:c000001`
- Nearby wrong interpretations to reject:
  - treating bounded-success, missing review telemetry, stale review output, quota fallback, or timeout fallback as satisfying the governed review contract
  - weakening the no-fallback wording into a best-effort recommendation
  - moving Linear/workpad/PR lifecycle ownership into this child lane
  - broadening the packet into unrelated docs freshness, provider admission, queue capacity, or control-host cleanup work

## Parity / Alignment Matrix

| Surface | Current Truth | Reference Truth | Target Truth | Explicitly Out Of Scope |
| --- | --- | --- | --- | --- |
| CO-512 packet | The parent launched a bounded docs child lane with five packet files plus `tasks/index.json` in scope. | Docs-first lanes make intent, issue-shaping, Not Done If, validation, and fallback decisions visible before implementation. | The CO-512 packet preserves the governed review contract and no-fallback wording before parent implementation starts. | `.agent/task` mirror, docs freshness registry, docs catalog, `docs/TASKS.md`, source implementation, PR, and Linear mutations. |
| Governed review handoff | Review handoff must not proceed on ambiguous or fallback review evidence. | Review handoff needs current, clean, governed review evidence. | Parent implementation requires clean governed review evidence with no fallback before Human Review / In Review handoff. | Changing unrelated review semantics outside the CO-512 issue contract. |
| No-fallback wording | The child prompt marks no-fallback wording as protected. | Protected wording must remain exact across packet surfaces. | Packet repeats `no fallback` and `no-fallback`, and rejects fallback review acceptance explicitly. | Replacing no-fallback with optional, advisory, or best-effort language. |
| Acceptance criteria | Parent reconciled the source payload and live issue prompt. | Source payload and Linear issue text remain authoritative for acceptance. | Packet carries the full parent contract: schema, bundles, contract telemetry, fail-closed handoff, proposal routing, accepted-only memory ingestion, and validation evidence. | Claiming raw review suggestions can bypass acceptance or memory validation. |

## Not Done If
- Any scoped packet file omits `governed review contract`, `no fallback`, `no-fallback`, `clean governed review evidence`, or `review handoff`.
- The packet allows fallback review evidence, missing telemetry, stale output, or semantically dirty verdicts to satisfy handoff.
- The packet weakens no-fallback wording into optional, advisory, best-effort, or "acceptable with waiver" language.
- Review output depends on prose-only clean, empty findings, wrapper success, scoped-title retry, or artifact-only prompt transport.
- The `paths.docs` field in `tasks/index.json` is anything other than the CO-512 TECH_SPEC pointer string.
- Agent-loop feedback is mixed into code findings or ingested into memory before explicit validation and acceptance.

## Goals
- Create the scoped CO-512 docs-first packet.
- Register the packet in `tasks/index.json`.
- Preserve no-fallback governed review handoff wording across packet surfaces.
- Make acceptance criteria, non-goals, Not Done If, and validation expectations visible.
- Keep parent-owned implementation and lifecycle responsibilities explicit.
- Provide parseable CO-382 fallback/seam decision evidence for the no-fallback review contract.
- Require `co.review.contract.v1` JSON output with four axes: `spec_conformance`, `coding_standards`, `code_changes`, and `agent_loop`.
- Preserve separate code-change proposals and agent-loop proposal routing, with accepted validated agent-loop proposals as the only memory-ingestion source.

## Non-Goals
- No unrelated review-wrapper, provider admission, control-host, queue, docs freshness, or runtime routing changes.
- No `.agent/task` mirror in this lane.
- No `docs/TASKS.md`, docs catalog, Linear, GitHub, or PR lifecycle mutation from the docs child lane.
- No waiver path that treats fallback review evidence as sufficient.

## Stakeholders
- Product: CO operators relying on governed review handoff state.
- Engineering: CO-512 parent lane and provider-worker/review-wrapper maintainers.
- Review: parent lane reviewer importing this child patch artifact.

## Metrics & Guardrails
- Primary Success Metrics:
  - five scoped packet files are created
  - `tasks/index.json` has one CO-512 registration
  - protected no-fallback terms scan cleanly across the scoped files
  - parent implementation emits contract telemetry from the authoritative enforce-mode `review/contract.json` file and validates fail-closed handoff behavior
- Guardrails / Error Budgets:
  - zero prose-only clean verdicts accepted in enforce mode
  - zero raw review suggestions accepted into memory
  - zero fallback review acceptance paths in the packet contract

## Technical Considerations
- Architectural Notes:
  - The parent implementation adds `schemas/review-contract.v1.schema.json`, `schemas/review-contract.v1.output.schema.json`, and `scripts/lib/review-contract.ts`.
  - Enforce mode uses `codex exec --output-schema schemas/review-contract.v1.output.schema.json --output-last-message review/contract.json -` as the primary no-fallback transport, with the full prompt delivered on stdin; `codex review` remains for non-enforce and shadow review surfaces.
  - `scripts/run-review.ts` writes `review/inputs/{spec,standards,change,agent-loop}-bundle.json` and validates the authoritative `review/contract.json` last-message artifact instead of scraping noisy stdout.
  - `review/telemetry.json` records contract mode, transport, validation source, axis verdicts, finding counts, proposal counts, and keeps wrapper execution outcome separate from semantic review verdict.
  - `orchestrator/src/cli/services/commandRunner.ts` and provider-worker prompt guidance fail closed on missing, invalid, blocked, findings, or unknown enforce-mode contracts.
- Dependencies / Integrations:
  - parent-owned source payload reconciliation
  - parent-owned review-wrapper/provider-worker implementation
  - parent-owned validation, workpad, Linear state, PR lifecycle, and review handoff

## Fallback / Refactor Decision
- Applies to fallback, compatibility, legacy, stale, cached, break-glass, or minor-seam behavior? Yes.
- Decision: `remove fallback` for review handoff paths that treat fallback, missing, stale, or semantically dirty review evidence as sufficient for the governed review contract.

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Governed review handoff | Fallback review evidence, missing telemetry, stale output, or semantically dirty verdict accepted as handoff proof | `remove fallback` | CO-512 parent lane | Parent attempts Human Review / In Review handoff without current clean governed review evidence | Unknown from child source; parent must confirm | 2026-05-14 | Not retained; remove before handoff | Handoff requires clean governed review evidence with no fallback, or the lane remains blocked/rework instead of advancing | Parent-owned focused tests plus manifest/review telemetry proof showing no fallback and clean verdict before handoff |

- Large-refactor decision: no extra broad refactor is required; the smallest governed path is one schema/module plus wrapper telemetry and provider-worker gate integration.
- Minor-seam decision: no retained fallback is allowed for governed review handoff; enforce mode fails closed instead.

## Open Questions
- Final review handoff still requires current clean standalone review telemetry, full validation, PR checks, and `ready-review` drain.

## Approvals
- Product: CO-512 bounded child-lane prompt
- Engineering: packet-only child lane, 2026-05-14
- Design: N/A
