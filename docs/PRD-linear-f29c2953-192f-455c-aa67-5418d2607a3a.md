# PRD - Control host: stabilize Doctor.test.ts full-suite timeout cluster under suite load

## Added by Docs Child Lane 2026-04-18

## Traceability
- Linear issue: `CO-229` / `f29c2953-192f-455c-aa67-5418d2607a3a`
- Linear URL: https://linear.app/asabeko/issue/CO-229/control-host-stabilize-doctortestts-full-suite-timeout-cluster-under
- Task id: `linear-f29c2953-192f-455c-aa67-5418d2607a3a`
- Canonical spec: `tasks/specs/linear-f29c2953-192f-455c-aa67-5418d2607a3a.md`
- Docs packet child lane manifest: `.runs/linear-f29c2953-192f-455c-aa67-5418d2607a3a-docs-packet/cli/2026-04-18T00-31-50-715Z-81077b6b/manifest.json`
- Source anchor: `ctx:sha256:ce8d79c8861c039782f62f47ed8c1e50aaa5e0c89390d66072caab9ae13d8df4#chunk:c000001`
- Source payload note: the parent-provided `source-0` object preserves the run envelope and issue identifier only, so this packet carries the exact `CO-229` issue description and intent checksum from the live read-only Linear issue body.

## Summary
- Problem Statement: Full-suite validation for `CO-216` still fails in `orchestrator/tests/Doctor.test.ts` under suite load even after the operator-autopilot fix is stable.
- Desired Outcome: isolate and fix the suite-load timeout behavior in the doctor coverage so provider-worker validation stops failing for unrelated reasons, `npm run test` no longer times out in the affected doctor cases, and isolated `Doctor.test.ts` coverage stays green.

## User Request Translation (Context Anchor)
- User intent / needs (in your own words): create the docs-first packet for `CO-229` only, preserving the exact issue wording around the `Doctor.test.ts` full-suite timeout cluster, the five named readiness cases, and the contrast between full-suite failure and isolated-file success before the parent lane edits source or tests.
- Success criteria / acceptance:
  - Reproduce the `Doctor.test.ts` timeout cluster under `npm run test` with concrete evidence from the current suite.
  - Prove whether the root cause is load-sensitive doctor work, environment-sensitive probing, or another suite interaction.
  - Land a bounded fix so `npm run test` no longer times out in the affected doctor cases.
  - Preserve `npx vitest run --config vitest.config.core.ts orchestrator/tests/Doctor.test.ts` passing in isolation after the fix.
  - Keep the follow-up scoped to doctor-suite validation behavior rather than `CO-216` autopilot logic.
- Constraints / non-goals:
  - Do not reopen `CO-216` backlog-promotion or manual-demotion policy logic.
  - Do not broaden into generic doctor feature work unrelated to the timeout cluster.
  - Do not weaken validation by deleting the failing doctor coverage without replacing its signal.
  - Do not treat isolated doctor-file success as proof that the full-suite timeout cluster is acceptable.
  - This child lane remains docs-only; parent owns implementation, validation, workpad, PR, and merge.

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `Doctor.test.ts`
  - `full-suite`
  - `suite load`
  - `passes in isolation`
  - `timeouts`
- Protected terms / exact artifact and surface names:
  - `orchestrator/tests/Doctor.test.ts`
  - `npm run test`
  - `npx vitest run --config vitest.config.core.ts orchestrator/tests/Doctor.test.ts`
  - `reports direct-dist delegation readiness and initialize latency`
  - `keeps top-level doctor status ok when delegation is advisory but the rest of the install is healthy`
  - `keeps overall doctor status at warning when providers are incomplete`
  - `reports provider readiness when the repo is seeded and env is configured`
  - `resolves provider readiness from the repo root when doctor runs in a nested directory`
- Nearby wrong interpretations to reject:
  - this is another `CO-216` operator-autopilot bug
  - isolated `Doctor.test.ts` is failing on the same diff
  - this follow-up should reopen backlog-promotion or manual-demotion logic
  - this is a generic docs-packet bug rather than a doctor suite-load timeout issue

## Parity / Alignment Matrix

| Surface | Current Truth | Reference Truth | Target Truth |
| --- | --- | --- | --- |
| `npm run test` full suite | On the settled `CO-216` diff, `npm run test` failed with 5 `Doctor.test.ts` timeouts under suite load. | Full-suite validation should not fail for unrelated provider-worker lanes because of load-sensitive doctor readiness timeouts. | `npm run test` completes without timing out in the affected doctor cases. |
| Isolated doctor file | `npx vitest run --config vitest.config.core.ts orchestrator/tests/Doctor.test.ts` passed in isolation with `47 passed`, duration about `39s`. | Isolated doctor coverage should remain green. | Isolated doctor coverage still passes after the bounded fix. |
| Scope boundary | The failure can be misread as another `CO-216` autopilot regression or as generic doctor/docs churn. | The issue is a bounded follow-up about suite-load timeout behavior in doctor coverage. | The parent fix stays scoped to doctor suite-load timeout behavior and leaves `CO-216` logic untouched. |

## Acceptance Criteria
- Reproduce the `Doctor.test.ts` timeout cluster under `npm run test` with concrete evidence from the current suite.
- Prove whether the root cause is load-sensitive doctor work, environment-sensitive probing, or another suite interaction.
- Land a bounded fix so `npm run test` no longer times out in the affected doctor cases.
- Preserve `npx vitest run --config vitest.config.core.ts orchestrator/tests/Doctor.test.ts` passing in isolation after the fix.
- Keep the follow-up scoped to doctor-suite validation behavior rather than `CO-216` autopilot logic.

## Non-Goals
- Reopening `CO-216` backlog-promotion or manual-demotion policy logic.
- Broadening into generic doctor feature work unrelated to the timeout cluster.
- Weakening validation by deleting the failing doctor coverage without replacing its signal.
- Treating isolated doctor-file success as proof that the full-suite timeout cluster is acceptable.
- Implementation or test edits from this docs-only child lane.

## Not Done If
- `npm run test` still times out in the same `Doctor.test.ts` readiness cases under full-suite load.
- The investigation cannot explain why isolated doctor coverage passes while the full suite times out.
- The fix relies only on raising global timeouts without understanding or containing the suite-load behavior.
- The follow-up leaves provider-worker validation blocked in the same way for unrelated issue lanes.

## Validation Contract
- Parent reproduces the `Doctor.test.ts` timeout cluster under `npm run test` and captures concrete evidence from the current suite.
- Parent preserves `npx vitest run --config vitest.config.core.ts orchestrator/tests/Doctor.test.ts` passing in isolation after the fix.
- Parent keeps the repair scoped to doctor-suite validation behavior rather than `CO-216` autopilot logic.
- Child lane runs only scoped docs checks: JSON parse, protected-term grep, `git diff --check`, and `git status --short` over the declared docs/task files.
