# ACTION_PLAN - CO-367 current-host-unhealthy matcher wording drift

## Summary
- Goal: keep current and legacy current-host-unhealthy error wording classified correctly in `co-status --format json`.
- Scope: docs/task mirrors, source matcher hardening, focused regression, validation, review, PR handoff.
- Assumptions: current `origin/main` is the source of truth for CO-363 matcher and producer wording; this lane must not change CO-363 projection semantics.

## Issue Readiness Gate
- Intent checksum / protected terms carried forward: `isCurrentHostUnhealthyErrorMessage`, `current-host-unhealthy`, `control_endpoint.json`, `co-status --format json`, `CO-363`, `PR #644`, `CodeRabbit`.
- Not done if: current fallback wording is not matched, legacy wording loses coverage, focused regression is missing, or active issue projection semantics change.
- Pre-implementation issue-quality review: parent self-review completed after fetching `origin/main`; stale detached workspace base was corrected before source work.

## Milestones & Sequencing
1. Read Linear issue context, create/update the single workpad, move `Ready` to `In Progress`, and record the required parallelization decision.
2. Refresh the workspace to current `origin/main` and branch `linear/co-367-current-host-unhealthy-matcher`.
3. Add docs-first packet and registry/checklist mirrors for CO-367.
4. Harden matcher fragments in `coStatusCliShell.ts`.
5. Add focused current/legacy unhealthy-host regressions.
6. Run focused validation and repo guardrails.
7. Run standalone review, elegance pass, PR handoff, ready-review drain, and Linear review-state transition.

## Dependencies
- Current producer wording in `orchestrator/src/cli/coStatusAttachCliShell.ts`.
- Current matcher in `orchestrator/src/cli/coStatusCliShell.ts`.
- Focused tests in `orchestrator/tests/CoStatusCliShell.test.ts`.
- Linear issue `CO-367`.

## Validation
- Checks / tests:
  - `npx vitest run orchestrator/tests/CoStatusCliShell.test.ts`
  - `node scripts/spec-guard.mjs --dry-run`
  - `npm run build`
  - `npm run lint`
  - `npm run test`
  - `npm run docs:check`
  - `npm run docs:freshness`
  - `npm run repo:stewardship`
  - `node scripts/diff-budget.mjs`
  - manifest-backed standalone review
- Rollback plan: revert the matcher/test/docs commit; no data migration or persistent runtime state changes are introduced.

## Risks & Mitigations
- Risk: fallback matching becomes too broad.
  - Mitigation: match only explicit marker/current/legacy/rotated unhealthy-host fragments.
- Risk: legacy coverage is lost.
  - Mitigation: retain a named legacy fragment and focused regression.
- Risk: CO-363 behavior changes accidentally.
  - Mitigation: do not touch projection code and run focused `CoStatusCliShell` coverage.

## Approvals
- Reviewer: parent provider worker.
- Date: 2026-04-25
