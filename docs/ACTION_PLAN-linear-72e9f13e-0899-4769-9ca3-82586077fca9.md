# ACTION_PLAN - CO workflow: refresh current-main docs:freshness stale cohorts 1138-1163 plus release docs

## Added by Bootstrap 2026-04-13

## Summary
- Goal: restore the current-main `docs:freshness` baseline by refreshing the two stale cohorts already proven by `CO-166`.
- Scope:
  - register the `CO-167` docs-first packet, workpad source, and mirror docs
  - run one audited `docs-review` child stream and record the result or truthful fallback
  - refresh only the March 13 packet cohort (`1138` through `1163`) and the January release-doc cohort (`release.md`, `0942`, `0943`, `0944`)
  - rerun the required validation floor and complete standalone/elegance review before any handoff
- Assumptions:
  - the saved `CO-166` evidence still matches the live stale scope until a fresh rerun proves otherwise
  - a bounded metadata refresh is the smallest truthful fix for the current tree

## Issue Readiness Gate
- Intent checksum / protected terms carried forward:
  - `docs:freshness`
  - `current-main stale-doc baseline`
  - `1138` through `1163`
  - `release.md`
  - `0942`
  - `0943`
  - `0944`
  - `truthful validation story`
- Not done if:
  - `npm run docs:freshness` still fails on the repaired tree
  - either stale cohort is only partially refreshed
  - the closeout cannot point back to exact `CO-166` before-state artifacts and new `CO-167` rerun artifacts
- Pre-implementation issue-quality review:
  - live saved evidence shows one March 13 packet cohort and one January release-doc cohort with no missing/invalid drift, so the lane stays bounded to targeted metadata refresh rather than broader docs cleanup or policy work

## Milestones & Sequencing
1. Register the `CO-167` packet, update `tasks/index.json`, `docs/TASKS.md`, and `docs/docs-freshness-registry.json`, and publish the initial workpad.
2. Run `linear child-stream --pipeline docs-review` for the registered packet and record the manifest or truthful baseline-only fallback.
3. Refresh the exact stale registry rows plus mirrored `last_review` metadata for the March 13 cohort and January release-doc cohort.
4. Rerun the required validation suite, save post-fix artifacts, and update the workpad/checklist.
5. Run standalone review, address any findings, then run an explicit elegance pass before PR/review handoff.

## Dependencies
- `scripts/docs-freshness.mjs`
- `docs/docs-freshness-registry.json`
- `tasks/index.json`
- `docs/TASKS.md`
- the stale cohort files listed in `out/linear-6af47129-33cc-45fa-9a55-e49423536752/manual/baseline-docs-freshness-report.json`

## Validation
- Checks / tests:
  - `linear child-stream --pipeline docs-review`
  - `node scripts/spec-guard.mjs --dry-run`
  - `npm run build`
  - `npm run lint`
  - `npm run test`
  - `npm run docs:check`
  - `npm run docs:freshness`
  - `npm run repo:stewardship`
  - `node scripts/diff-budget.mjs`
  - `FORCE_CODEX_REVIEW=1 npm run review -- --uncommitted`
- Rollback plan:
  - revert the metadata refresh if a fresh rerun proves a different live stale cohort or if the bounded repair introduces a new guardrail regression

## Risks & Mitigations
- Risk: packet registration pushes `docs/TASKS.md` over the line budget.
  - Mitigation: use the supported `npm run docs:archive-tasks` helper if `docs:check` or docs-review surfaces the threshold.
- Risk: updating only registry rows leaves mirrored packet metadata stale.
  - Mitigation: refresh `tasks/specs/*`, `docs/TECH_SPEC-*`, and `tasks/index.json` dates where those mirrors already exist.
- Risk: the lane drifts back into `CO-165` implementation or unrelated docs cleanup.
  - Mitigation: keep the workpad and packet explicit about the two proven stale cohorts and create a follow-up instead of expanding scope.

## Approvals
- Reviewer: pending audited docs-review
- Date: 2026-04-13
