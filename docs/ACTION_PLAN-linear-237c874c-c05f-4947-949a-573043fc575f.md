# ACTION_PLAN - CO: restore Apr 15 docs:freshness historical packet baseline

## Added by Bootstrap 2026-04-15

## Summary
- Goal: restore the April 15 `docs:freshness` baseline without weakening the rolling freshness policy or hiding CO-175 debt.
- Scope: docs-first packet, stale-entry classification, registry refresh, cohort guide note, required validation, review handoff.
- Assumptions:
  - the stale set is historical task/report packet debt, not public or active guidance drift
  - `max_entries=300` must remain unchanged
  - CO-175 continues to own its existing rolling cohort

## Issue Readiness Gate
- Intent checksum / protected terms carried forward:
  - `npm run docs:freshness`, `docs/docs-freshness-registry.json`, `docs/docs-catalog.json`, `docs/guides/docs-freshness-cohorts.md`, CO-175, rolling freshness cohort, `max_entries=300`, `.agent/task/0951-delegation-rlm-quick-wins.md`, task cohort `1196+`, Task Packet, Task Mirror, Report Only
- Not done if:
  - `npm run docs:freshness` still fails
  - the new stale historical packet set is hidden by policy expansion
  - CO-175 rolling debt is hidden or reassigned
  - final evidence lacks stale-count, rolling-count, and sample-path data
- Pre-implementation issue-quality review:
  - The issue is not a narrow CO-183 validation rerun. The reproduced report proves a repo-wide April 15 baseline failure outside the CO-175 rolling cohort. The smallest safe outcome is reviewed registry refresh for the new historical rows, with an explicit classification note and no rolling policy broadening.

## Milestones & Sequencing
1. Inspect live Linear state, move `Ready` to `In Progress`, create the single required workpad, and record the same-turn parallelization decision.
2. Reproduce `npm run docs:freshness` and preserve `out/linear-237c874c-c05f-4947-949a-573043fc575f/pre-fix-docs-freshness.json`.
3. Launch a same-issue child lane for bounded classification evidence and decide whether to accept or reject its artifact.
4. Create docs-first packet, classification note, task mirrors, and registry/task index entries.
5. Refresh only the classified April 15 stale historical packet rows in `docs/docs-freshness-registry.json`.
6. Run docs-review, validation gates, standalone review, and elegance pass.
7. Open or update PR, attach it to Linear, drain automated review feedback, and move to `In Review` only after coding stops.

## Dependencies
- `docs/docs-freshness-registry.json`
- `docs/docs-catalog.json`
- `docs/guides/docs-freshness-cohorts.md`
- `scripts/docs-freshness.mjs`
- CO-175 rolling freshness cohort evidence

## Validation
- Checks / tests:
  - `npm run docs:check`
  - `npm run docs:freshness`
  - `node scripts/diff-budget.mjs`
  - manifest-backed standalone review when review tooling is available
- Rollback plan:
  - revert registry refresh and packet docs together; the previous failing `docs:freshness` report remains as the baseline artifact.

## Risks & Mitigations
- Risk: registry refresh could look like a silent date bump.
  - Mitigation: add this docs-first packet plus the classification note and cohort-guide rationale.
- Risk: CO-175 rolling debt could become invisible.
  - Mitigation: do not edit the CO-175 baseline cohort, owner, window, or cap.
- Risk: public or active docs could accidentally be swept into the refresh.
  - Mitigation: update only paths listed in `stale_entries` from the reproduced report, all of which are Task Packet, Task Mirror, or Report Only.

## Approvals
- Reviewer: docs-review child stream attempted and failed before review on generated task-id/provenance bootstrap; fallback review completed with bounded-success and no actionable findings after the evidence-path repair.
- Date: 2026-04-15
