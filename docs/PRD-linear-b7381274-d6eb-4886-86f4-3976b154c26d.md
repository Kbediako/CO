# PRD - CO-465 Doctor gpt-5.5 defaults full-suite validation gate

## Traceability
- Linear issue: `CO-465` / `b7381274-d6eb-4886-86f4-3976b154c26d`
- Linear URL: https://linear.app/asabeko/issue/CO-465
- Task id: `linear-b7381274-d6eb-4886-86f4-3976b154c26d`
- Canonical spec: `tasks/specs/linear-b7381274-d6eb-4886-86f4-3976b154c26d.md`
- TECH_SPEC mirror: `docs/TECH_SPEC-linear-b7381274-d6eb-4886-86f4-3976b154c26d.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-b7381274-d6eb-4886-86f4-3976b154c26d.md`
- Task checklist: `tasks/tasks-linear-b7381274-d6eb-4886-86f4-3976b154c26d.md`
- Child lane manifest: `.runs/linear-b7381274-d6eb-4886-86f4-3976b154c26d-co465-docs-packet/cli/2026-05-01T19-14-31-418Z-9a2afec6/manifest.json`
- Expected child patch: `.runs/linear-b7381274-d6eb-4886-86f4-3976b154c26d-co465-docs-packet/cli/2026-05-01T19-14-31-418Z-9a2afec6/provider-linear-child-lane.patch`
- Source anchor: `ctx:sha256:16b4be52eda49eabac589fc41aca4b843e4a04597237ee4143aa87a4ae0c4603#chunk:c000001`
- Source payload note: the referenced `.runs/.../memory/source-0/source.txt` path is absent in this child checkout, so this packet uses the parent-provided issue contract and protected terms as the authoritative source.

## Summary
- Problem Statement: the provider-worker validation gate for `CO-465` is blocked by a `full-suite timeout` in `orchestrator/tests/Doctor.test.ts` around the Doctor model-default policy case `accepts unmarked gpt-5.5 defaults when access is verified`, while the isolated rerun passes.
- Desired Outcome: preserve the Doctor policy that `gpt-5.5 defaults` are accepted when access is verified, explain and fix the full-suite-only slow path, and restore a truthful `npm run test` provider-worker validation gate without changing `CO-456` spec-guard behavior or weakening Doctor model-default policy assertions.

## User Request Translation
- User intent / needs: create the docs-first packet and registry/task mirrors for `CO-465` only. Parent implementation should investigate why `npm run test` times out in `orchestrator/tests/Doctor.test.ts` while the focused rerun passes, then make the smallest validation-stabilizing change that keeps the Doctor `gpt-5.5` default assertion meaningful.
- Success criteria / acceptance:
  - `npm run test` no longer blocks the provider-worker validation gate on the same `orchestrator/tests/Doctor.test.ts` full-suite timeout.
  - The focused Doctor rerun remains green, including `accepts unmarked gpt-5.5 defaults when access is verified`.
  - The parent explains the full-suite-only slow path instead of relying on a blanket global timeout increase.
  - The fix does not change `CO-456` spec-guard behavior.
  - The fix does not weaken Doctor model-default policy assertions or skip the Doctor test.
- Constraints / non-goals:
  - this child lane edits only the declared docs packet, task mirror, task registry, task snapshot, and docs freshness registry files
  - no source, test, config, Linear, GitHub, workpad, PR lifecycle, or full repo validation changes from this child lane
  - no recommendation to treat isolated success as sufficient when full-suite validation still blocks provider-worker handoff

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `npm run test`
  - `orchestrator/tests/Doctor.test.ts`
  - `accepts unmarked gpt-5.5 defaults when access is verified`
  - `gpt-5.5 defaults`
  - `full-suite timeout`
  - `isolated rerun passes`
  - `provider-worker validation gate`
- Nearby wrong interpretations to reject:
  - this is a `CO-456` spec-guard behavior change
  - the right fix is weakening Doctor model-default policy assertions
  - the right fix is skipping `orchestrator/tests/Doctor.test.ts`
  - isolated rerun success alone satisfies the provider-worker validation gate
  - a blanket global timeout increase is acceptable without explaining the full-suite-only slow path
  - this docs child lane should edit tests, source code, or lifecycle state

## Parity / Alignment Matrix

| Surface | Current truth | Reference truth | Target truth | Explicitly out-of-scope differences |
| --- | --- | --- | --- | --- |
| Provider-worker validation gate | `npm run test` is blocked by a full-suite timeout in `orchestrator/tests/Doctor.test.ts`. | Provider-worker handoff requires the full validation gate to be truthful, not bypassed by isolated-only evidence. | Parent removes or contains the full-suite-only timeout so `npm run test` can complete without losing Doctor coverage. | Full repo validation from this child lane, CI/PR lifecycle mutation, or treating the timeout as unrelated without proof. |
| Doctor model-default policy | The policy case `accepts unmarked gpt-5.5 defaults when access is verified` must remain meaningful. | Doctor should accept `gpt-5.5 defaults` only when access verification proves they are usable. | Parent preserves the assertion and any needed access-verification semantics while stabilizing the full-suite path. | Weakening assertions, marking the case skipped, or replacing it with a looser model-name check. |
| Isolated rerun | The isolated rerun passes. | Isolated success is necessary evidence but not enough for provider-worker handoff when `npm run test` times out. | Parent keeps the isolated Doctor rerun green and explains why the full suite previously timed out. | Declaring the issue done from isolated rerun evidence alone. |
| CO-456 boundary | Prompt explicitly says not to change `CO-456` spec-guard behavior. | Spec-guard behavior must stay owned by CO-456 or successor lanes. | Parent keeps CO-465 focused on Doctor validation stability and model-default policy. | Any spec-guard policy, guard threshold, or stale-spec behavior change. |

## Not Done If
- `npm run test` still hits the same `orchestrator/tests/Doctor.test.ts` full-suite timeout.
- `accepts unmarked gpt-5.5 defaults when access is verified` is skipped, deleted, or weakened.
- The parent cannot explain why the isolated rerun passes while the full suite times out.
- The fix changes `CO-456` spec-guard behavior.
- The solution is only a blanket global timeout increase without evidence for the full-suite-only slow path.
- Provider-worker validation remains blocked on the same Doctor surface.

## Goals
- Create the CO-465 docs-first packet and mirrors before implementation.
- Preserve the exact protected Doctor, model-default, and provider-worker validation terms.
- Give the parent a narrow validation contract for the full-suite timeout and isolated rerun split.
- Keep source/test/lifecycle ownership with the parent lane.

## Non-Goals
- No source, test, config, package, workflow, Linear, GitHub, workpad, or PR edits from this child lane.
- No `CO-456` spec-guard behavior change.
- No Doctor policy weakening, test skipping, or blanket timeout-only prescription.
- No broad cleanup of unrelated stale docs or validation debt.

## Metrics & Guardrails
- Primary Success Metrics:
  - all six packet and mirror files exist
  - `tasks/index.json` and `docs/docs-freshness-registry.json` parse as JSON
  - protected-term scan finds all required issue terms across the packet
  - changed-file review stays inside the declared file scope
- Guardrails / Error Budgets:
  - zero implementation or test edits in this child lane
  - zero Linear/GitHub lifecycle mutations
  - zero recommendations that treat isolated rerun success as sufficient while full-suite validation fails

## Technical Considerations
- Architectural Notes:
  - parent should first reproduce the full-suite timeout and compare it to the passing isolated Doctor rerun
  - prefer a targeted Doctor harness or access-verification stabilization over global timeout changes
  - preserve the intended `gpt-5.5 defaults` acceptance policy and only adjust the slow path once proven
- Dependencies / Integrations:
  - `orchestrator/tests/Doctor.test.ts`
  - Doctor model-default access verification
  - provider-worker validation gate and `npm run test`
  - CO-456 spec-guard behavior, as an explicit non-owned boundary

## Validation Contract
- Child lane:
  - JSON parse for `tasks/index.json` and `docs/docs-freshness-registry.json`
  - protected-term scan over packet and mirrors
  - scoped `git diff --check --` over declared files
  - scoped changed-file review
- Parent lane:
  - reproduce the full-suite timeout under `npm run test`
  - preserve passing focused Doctor rerun for `orchestrator/tests/Doctor.test.ts`
  - prove the full-suite-only slow path and avoid blanket timeout-only handling
  - run normal provider-worker validation and review gates after implementation

## Open Questions
- Which Doctor setup, access-verification, or model-default path is slow only under full-suite load?
- Is the timeout caused by shared process state, real CLI/model probing, temp-home reuse, or cumulative suite pressure?
- What is the smallest change that preserves the `gpt-5.5 defaults` assertion while removing the provider-worker validation blocker?

## Approvals
- Docs packet child lane: produced in this workspace for parent patch export.
- Parent docs-review, implementation, validation, and lifecycle handoff: pending parent ownership.
- Date: 2026-05-02
