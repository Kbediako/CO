# ACTION_PLAN - CO-533 docs/TASKS archive reserve restoration

## Summary
- Goal: restore durable `docs/TASKS.md` reserve headroom by fixing archive command root causes.
- Scope: parser behavior, diagnostics, verified metadata, archive output, and CO-533 packet mirrors.
- Assumptions: current main is the source of truth; CO-489 remains worker-owned and should not be patched by this lane.

## Issue Readiness Gate
- Intent checksum / protected terms carried forward: `docs/TASKS.md`, `docs/TASKS-archive-2026.md`, `tasks/index.json`, `reserve_lines=10`, `target_lines=440`, `Unable to restore reserve target 440`.
- Not done if: the reserve command still fails or only works by deleting the newest packet snapshot.
- Pre-implementation issue-quality review: read-only audit reproduced the failing dry-run and isolated archive-index adjacency plus stale canonical metadata.
- Fallback / refactor decision: touches legacy/stale metadata and a parser seam. Remove the parser seam; retain fail-closed stale metadata diagnostics.
- Durable retention evidence: diagnostic contract is covered by focused tests and does not change canonical status authority.
- Large-refactor check: defer broad lifecycle reconciliation to existing docs freshness prevention lanes; this lane restores the archive command contract directly.

## Milestones & Sequencing
1. Reproduce current main line count and archive failure.
2. Add parser and diagnostic regressions for archive-index adjacency and stale nonterminal status.
3. Implement parser/diagnostic fix.
4. Correct verified completed metadata for 0933 and 0934 only.
5. Run archive dry-run, then tracked archive execution.
6. Register CO-533 docs packet and mirrors.
7. Run validation and review, then open/monitor PR. Local build/lint/test/docs/stewardship/diff-budget gates, same-issue child lane, standalone review, and elegance pass are complete; PR drain remains pending.

## Dependencies
- Current clean `origin/main` shared root.
- CO-489 remains independently worker-owned.

## Validation
- Checks / tests: `npx vitest run tests/tasks-archive.spec.ts`, `npm run docs:archive-tasks -- --dry-run`, `npm run docs:archive-tasks -- --out docs/TASKS-archive-YYYY.md`, `npm run docs:check`, `node scripts/spec-guard.mjs --dry-run`, `npm run build`, `npm run lint`, `npm run test`, `npm run repo:stewardship`, `node scripts/diff-budget.mjs`, and review/elegance gates.
- Rollback plan: revert parser/test/metadata changes and restore `docs/TASKS.md` / archive diffs from git if validation shows archive evidence loss.

## Risks & Mitigations
- Risk: marking historical rows completed incorrectly. Mitigation: only 0933/0934 are promoted because their mirrors show implementation and validation complete.
- Risk: archive-index lost from active file. Mitigation: parser regression asserts index stays in `docs/TASKS.md` and not in archive payload.

## Approvals
- Reviewer: manifest-backed standalone review clean; parent elegance pass complete.
- Date: 2026-05-13.
