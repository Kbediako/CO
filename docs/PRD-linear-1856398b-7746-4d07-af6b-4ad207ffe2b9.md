# PRD - CO-387 multi-surface Codex posture matrix

## Traceability
- Linear issue: `CO-387` / `1856398b-7746-4d07-af6b-4ad207ffe2b9`
- Linear title: `CO: make docs hygiene enforce a multi-surface Codex posture matrix`
- Task id: `linear-1856398b-7746-4d07-af6b-4ad207ffe2b9`
- Canonical spec: `tasks/specs/linear-1856398b-7746-4d07-af6b-4ad207ffe2b9.md`
- TECH_SPEC mirror: `docs/TECH_SPEC-linear-1856398b-7746-4d07-af6b-4ad207ffe2b9.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-1856398b-7746-4d07-af6b-4ad207ffe2b9.md`
- Parent manifest: `.runs/linear-1856398b-7746-4d07-af6b-4ad207ffe2b9-docs-packet-v2/cli/2026-04-26T02-11-12-522Z-470a967d/manifest.json`
- Source anchor: `ctx:sha256:363de60f154c2d3fe3216bec435c5fdadf6c34c16a7095552bda42dd1832bf8a#chunk:c000001`
- Source payload note: this child checkout has no `.runs/` tree, so the packet is anchored to the parent-provided issue text in the prompt.

## Summary
- Problem Statement: current Codex posture truth is distributed across public docs, internal docs, docs-hygiene regex parsing, workflow pins, and `pack:smoke` marketplace assumptions. That lets current-facing surfaces drift or keep superseded release evidence active, including the stale `0.124` book case described by the parent.
- Desired Outcome: introduce a machine-readable Codex posture matrix or equivalent canonical data source and make docs hygiene fail when README, docs/book navigation, public posture, downstream setup docs, version policy, workflow pins, or pack-smoke expectations disagree with it.

## User Request Translation
- User intent / needs: make Codex CLI/model/runtime/workflow posture a multi-surface contract, not a set of separately maintained strings. Active current-facing docs should agree with the canonical posture, while historical release evidence stays preserved but explicitly demoted or archived.
- Success criteria / acceptance:
  - a machine-readable Codex posture matrix or equivalent canonical data source exists
  - README, docs/book index or current docs navigation, public posture, downstream setup docs, version policy, workflow pins, and pack-smoke expectations validate against that source
  - active current-facing public guides cannot carry superseded release evidence without explicit historical/archive status
  - docs-hygiene tests fail on stale active release evidence residue, including a `0.124` book-style fixture
  - historical evidence is preserved through archive, demotion, or explicit historical status rather than deletion
  - this child lane creates only the local docs-first packet and `tasks/index.json` registration
- Constraints / non-goals:
  - do not move runtime targets or workflow pins by itself
  - do not remove useful release evidence history
  - do not duplicate CO-379 immediate `0.124` cleanup
  - do not call Linear, GitHub, web, PR, or issue-context tools from this child lane
  - do not run full repo validation suites from this child lane

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `CO-387`
  - `CO: make docs hygiene enforce a multi-surface Codex posture matrix`
  - `machine-readable Codex posture matrix`
  - `README`
  - docs/book index
  - `public posture`
  - `downstream setup docs`
  - `version policy`
  - `workflow pins`
  - `pack-smoke expectations`
  - `0.124 book case`
  - `preserve historical evidence through archive/demotion instead of deletion`
  - `do not duplicate CO-379 immediate 0.124 cleanup`
- Protected surfaces:
  - `README.md`
  - `docs/README.md`
  - any docs/book or generated book/navigation index if present
  - `docs/public/downstream-setup.md`
  - `docs/guides/codex-version-policy.md`
  - `docs/docs-catalog.json`
  - `scripts/docs-hygiene.ts`
  - `scripts/lib/docs-catalog.js`
  - `.github/workflows/core-lane.yml`
  - `.github/workflows/release.yml`
  - `.github/workflows/pack-smoke-backstop.yml`
  - `.github/workflows/cloud-canary.yml`
  - `scripts/pack-smoke.mjs`
  - `tests/docs-hygiene.spec.ts`
  - `tests/pack-smoke.spec.ts`
- Nearby wrong interpretations to reject:
  - treating this as a version bump or workflow-pin move
  - deleting historical release evidence to make checks pass
  - only checking README while docs/book or public downstream guides drift
  - leaving a superseded evidence page in current navigation without historical/archive status
  - using this lane to perform CO-379's immediate cleanup instead of adding durable enforcement

## Parity / Alignment Matrix

| Surface | Current Truth | Reference Truth | Target Truth | Explicitly Out Of Scope |
| --- | --- | --- | --- | --- |
| Canonical posture source | `docs/guides/codex-version-policy.md` is parsed as a prose policy source by `scripts/lib/docs-catalog.js`; workflow pins and pack-smoke assumptions are validated separately. | One machine-readable posture matrix or equivalent canonical source names active targets, candidate pins, workflow pins, model posture, runtime posture, historical evidence status, and allowed exceptions. | Docs-hygiene consumes the canonical source and reports drift per surface. | Promoting a newer Codex CLI or model posture. |
| README and public posture | Current main carries CO-local Codex CLI `0.125.0`, model `gpt-5.5` / `xhigh`, runtime `appserver`, portable `gpt-5.4` fallback, and spark exception posture. | Current-facing public docs should agree with the canonical matrix. | README and public posture fail docs:check when active values or exceptions drift. | Rewriting README beyond posture enforcement needs. |
| Docs/book index and current navigation | `docs/book/README.md` links the current public posture page and a `0.124` adoption evidence page that must stay explicitly historical. | Book/navigation indexes must not present superseded release evidence as current guidance. | A docs-hygiene rule or catalog rule fails when book/current navigation links a superseded evidence page without historical/archive status. | CO-379's immediate cleanup of the concrete `0.124` page. |
| Downstream setup docs | `docs/public/downstream-setup.md` carries install/setup posture and marketplace notes. | Shipped setup docs should match the canonical active target and compatibility-pin explanation. | Downstream setup docs are validated against the matrix, including marketplace/pack-smoke compatibility wording. | Changing the supported npm baseline or marketplace behavior without evidence. |
| Version policy | `docs/guides/codex-version-policy.md` explains CO-local/appserver `0.125.0` + `gpt-5.5` posture, release-facing package/downstream-smoke `0.125.0`, cloud-only `0.124.0`, and portable `gpt-5.4` fallback boundaries. | Version policy should be a human-readable rendering of the matrix and its evidence gates. | Policy prose fails if it contradicts active targets, candidates, workflow pins, pack-smoke expectations, or historical status in the matrix. | Re-auditing external Codex releases in this child lane. |
| Workflow pins and pack-smoke | Release-facing workflows pin `@openai/codex@0.125.0`; cloud-canary pins `@openai/codex@0.124.0`; `pack:smoke` requires supported marketplace plugin add behavior with legacy fallback. | Pin choices should be explicit matrix rows with reasons and expected capability support. | Workflow and pack-smoke tests fail when pins or prerequisite expectations drift from matrix rows. | Blindly moving pins to latest or suppressing pack-smoke marketplace coverage. |

## Not Done If
- `docs:check` or `docs:freshness` can pass while active current-facing docs disagree about current Codex CLI/model/runtime/workflow posture.
- Book navigation can present a superseded release evidence page as current guidance.
- A stale active `0.124` evidence fixture passes docs-hygiene.
- Historical release evidence is deleted instead of demoted, archived, or explicitly marked historical.
- Runtime targets or workflow pins move without a separate evidence-backed posture decision.
- The implementation duplicates CO-379's immediate cleanup instead of adding durable enforcement.

## Goals
- Define the matrix-backed posture contract for parent implementation.
- Preserve exact protected surfaces and non-goals for review.
- Require focused docs-hygiene and pack-smoke tests that prove stale active release evidence fails.
- Keep this child lane limited to the local docs-first packet and task index registration.

## Non-Goals
- No direct Codex CLI target promotion.
- No workflow pin changes from this child lane.
- No deletion of useful historical release evidence.
- No CO-379 immediate cleanup.
- No Linear/GitHub/PR lifecycle work from this child lane.

## Stakeholders
- Product: CO maintainers and downstream operators reading public/current posture.
- Engineering: docs-hygiene, docs-catalog, workflow, pack-smoke, and release-policy owners.
- Design: N/A.

## Metrics & Guardrails
- Primary Success Metrics:
  - focused tests fail on stale active posture residue and pass on explicit historical/archive demotion
  - README, docs/book/current nav, public setup docs, version policy, workflows, and pack-smoke expectations are all matrix-validated
  - `tasks/index.json` links this packet
- Guardrails / Error Budgets:
  - zero target/pin movement without evidence
  - zero deletion-only cleanup of historical evidence
  - zero edits outside this child lane's declared file scope

## Technical Considerations
- Architectural Notes:
  - Prefer a single structured data source such as `docs/codex-posture-matrix.json`, or an equivalent `docs/docs-catalog.json` policy extension if parent implementation chooses that smaller seam.
  - The current `readCurrentCodexPosture(...)` prose parser can be retained as compatibility only after the structured matrix becomes authoritative.
  - Historical evidence pages should carry machine-readable status through docs catalog metadata, the matrix, or both.
- Dependencies / Integrations:
  - `scripts/docs-hygiene.ts`
  - `scripts/lib/docs-catalog.js`
  - `docs/docs-catalog.json`
  - `tests/docs-hygiene.spec.ts`
  - `tests/pack-smoke.spec.ts`
  - `.github/workflows/*.yml`
  - `scripts/pack-smoke.mjs`

## Open Questions
- None blocking for the docs packet. Parent implementation should confirm whether a generated docs/book index exists outside this child checkout and bind the validator to that concrete surface when present.

## Approvals
- Product: parent CO-387 lane, pending
- Engineering: parent docs-review / implementation review, pending
- Design: N/A
