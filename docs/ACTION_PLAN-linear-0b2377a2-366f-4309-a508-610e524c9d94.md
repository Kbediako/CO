# ACTION_PLAN - CO-393 control-host provider-worker recovery

## Readiness Gate
- Intent checksum preserved from `CO-393`: root control-host relaunch/recover/nudge for a specific Linear issue, direct `codex-orchestrator start provider-linear-worker` guard, matching control-host provenance, deterministic issue worktree cwd, root `.runs` / `out`, manifest and provider-intake provenance, launch-token/intake reservation truth, and adjacent `CO-392` / `CO-244` / `CO-189` boundaries.
- Not done if direct starts can create wrong-root/null-provenance truth, supported recovery misses cwd/artifact/provenance/token truth, child lanes fail after recovery, or the fix broadens stale pending-reopen/provenance-schema/duplicate-worker scope.
- Fallback decision: retain root control-host authority as supported non-expiring contract; remove unsafe direct-start fallback by failing closed without provenance.

## Implementation Steps
1. Create docs packet and register task mirrors.
2. Inspect control-host launch, provider handoff, direct start, provider-intake, and child-lane provenance seams.
3. Add `control-host recover|relaunch|nudge --issue-id <id>` CLI client for the running root control host.
4. Add authenticated control-host API route for issue-specific provider-worker recovery.
5. Add provider handoff recovery service that reuses existing admission, duplicate-worker protection, launch-token generation, and launch/resume logic.
6. Release stale existing claims when direct issue lookup resolves the issue as gone or out of scope.
7. Add direct-start guard for `provider-linear-worker` when control-host provenance or launch token is absent.
8. Add focused regressions and run full validation.
9. Run standalone review, address findings, perform elegance pass, open PR, drain `pr ready-review`, and hand off to `In Review` only when clean.

## Validation
- Focused tests: direct-start guard, API route, provider handoff recovery, stale-claim release, and child-lane compatibility.
- Required gates: `node scripts/delegation-guard.mjs`; `node scripts/spec-guard.mjs --dry-run`; `npm run build`; `npm run lint`; `npm run test`; `npm run docs:check`; `npm run docs:freshness`; `npm run repo:stewardship`; `node scripts/diff-budget.mjs`; `npm run pack:smoke`.
- Review gates: manifest-backed standalone review under `FORCE_CODEX_REVIEW=1`, then explicit elegance/minimality pass.
- PR gate: attach PR to Linear, wait for required checks and bot feedback with `codex-orchestrator pr ready-review`.

## Rollback
If recovery cannot preserve root control-host provenance and artifact routing, revert the recovery command/API and keep direct starts fail-closed until a wider launch-authority refactor is scoped. Do not restore unguarded direct starts as a recovery path.
