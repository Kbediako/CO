# ACTION_PLAN - CO: add supported provider-worker relaunch/recover command and guard direct starts

## Added by Docs Child Lane 2026-04-26

## Summary

- Goal: give the parent lane a bounded implementation plan for adding a supported root control-host relaunch/recover/nudge path for a specific Linear issue and guarding unsafe direct `provider-linear-worker` starts.
- Scope: this child lane creates the CO-393 docs-first packet, task mirrors, and registry mirrors only; parent owns implementation, tests, docs-review, validation, Linear state, workpad evidence, PR lifecycle, and any state reconciliation.
- Assumptions:
  - the worker prompt's shared source anchor and `source-0` payload are the authoritative issue-shaping source
  - root control-host remains the supported launch authority
  - direct `codex-orchestrator start provider-linear-worker` must fail closed when matching control-host provenance is absent
  - `CO-392`, `CO-244`, and `CO-189` are adjacent invariants to preserve, not scope to absorb

## Issue Readiness Gate

- Intent checksum / protected terms carried forward:
  - `root control-host relaunch/recover/nudge`
  - `specific Linear issue`
  - `codex-orchestrator start provider-linear-worker`
  - `matching control-host provenance`
  - `actionable command hint`
  - `deterministic issue worktree cwd`
  - root `.runs` / `out` routing
  - `provider_launch_source=control-host`
  - `provider_control_host_task_id`
  - `provider_control_host_run_id`
  - provider-intake `launch_source=control-host`
  - non-null launch-token/intake reservation truth
  - duplicate-worker protection/provider admission caps
  - `CO-392` stale pending-reopen refresh logic remains out of scope
  - existing root control-host launcher remains in place
- Not done if:
  - direct `codex-orchestrator start provider-linear-worker` can still create wrong-root artifacts or null provenance without an actionable failure
  - supported relaunch/recover does not preserve deterministic issue worktree cwd and root `.runs` / `out`
  - manifest or provider-intake control-host provenance is missing after supported relaunch/recover
  - launch-token/intake reservation truth is null after supported relaunch/recover
  - child-lane helpers still fail after supported relaunch because control-host provenance is incomplete
  - implementation weakens duplicate-worker protection/provider admission caps
  - implementation broadens `CO-392`, redoes `CO-244`, or uses hand-edited `provider-intake-state.json` as the solution
- Pre-implementation issue-quality review:
  - 2026-04-26: child-lane review confirms CO-393 is the operator relaunch/recover/nudge and direct-start guard lane. It is not `CO-392` stale pending-reopen refresh, not `CO-244` manifest-field introduction, and not permission to weaken `CO-189` duplicate-worker protection. The micro-task path is ineligible because correctness depends on exact protected surfaces, parity/alignment, launch provenance, and fallback/seam behavior.
- Fallback / refactor decision:
  - This task touches fallback/seam behavior. Retain root control-host launch authority as the supported non-expiring contract. Remove direct provider-worker starts without matching control-host provenance as an unsafe fallback by making them fail closed.
- Durable retention evidence:
  - Root control-host relaunch/recover/nudge is retained because it owns provider admission, launch tokens, provenance, and run truth. Parent must prove this with supported relaunch provenance and child-lane compatibility tests.
- Large-refactor check:
  - A bounded fix is acceptable if parent can implement the supported command and direct-start guard inside existing launch/admission/provenance seams. If authority is split across lifecycle phases such that the direct-start guard cannot be made reliable, parent should stop and widen ownership rather than adding another minor seam.

## Milestones & Sequencing

1. Child lane drafts the PRD, TECH_SPEC mirror pair, ACTION_PLAN, task spec, task checklist, agent-task mirror, `tasks/index.json` entry, `docs/TASKS.md` snapshot, and docs-freshness registry entries inside the declared file scope.
2. Parent accepts or adjusts the packet in the authoritative issue workspace and reconciles any parent-owned Linear/workpad state.
3. Parent inspects current root control-host launch/recovery surfaces, direct `provider-linear-worker` startup, provider admission, provider-intake reservation, manifest provenance, and child-lane provenance checks.
4. Parent defines the supported issue-specific root control-host relaunch/recover/nudge command spelling and command hint.
5. Parent implements supported relaunch/recover so it uses deterministic issue worktree cwd, root `.runs` / `out`, existing admission caps, provider-intake reservation, and manifest control-host provenance.
6. Parent implements direct-start fail-closed behavior outside matching control-host provenance, with an actionable command hint.
7. Parent adds focused regressions for direct-start failure, supported relaunch provenance, provider-intake launch-token/reservation truth, and child-lane compatibility after relaunch.
8. Parent reruns focused preservation tests for `CO-244`, `CO-392`, and `CO-189` adjacent invariants before review handoff.

## Dependencies

- Shared source anchor: `ctx:sha256:69a80e9d4a8a919410857a6ff81972d38b23e6ec4a7988ddfa79bb23dd6505e1#chunk:c000001`
- Origin manifest: `../../.runs/linear-0b2377a2-366f-4309-a508-610e524c9d94/cli/2026-04-26T17-26-50-546Z-360e85a4/manifest.json`
- Source payload: `../../.runs/linear-0b2377a2-366f-4309-a508-610e524c9d94/cli/2026-04-26T17-26-50-546Z-360e85a4/memory/source-0/source.txt`
- Source availability note: the authoritative shared `source-0` payload is present under the parent artifact root; this packet preserves the source issue text and protected terms.
- Candidate parent-owned source seams:
  - `orchestrator/src/cli/controlHostCliShell.ts`
  - `orchestrator/src/cli/control/providerIssueHandoff.ts`
  - `orchestrator/src/cli/control/providerIntakeState.ts`
  - `orchestrator/src/cli/providerLinearWorkerRunner.ts`
  - `orchestrator/src/cli/providerLinearChildLaneShell.ts`
  - `orchestrator/src/cli/run/manifest.ts`
- Candidate parent-owned focused tests:
  - `orchestrator/tests/ControlHostCliShell.test.ts`
  - `orchestrator/tests/ProviderIssueHandoff.test.ts`
  - `orchestrator/tests/ProviderLinearWorkerRunner.test.ts`

## Validation

- Child-lane checks:
  - `jq empty tasks/index.json docs/docs-freshness-registry.json`
  - protected-term scan across the six packet/checklist/mirror files plus registry mirrors
  - `git diff --check -- docs/PRD-linear-0b2377a2-366f-4309-a508-610e524c9d94.md docs/TECH_SPEC-linear-0b2377a2-366f-4309-a508-610e524c9d94.md docs/ACTION_PLAN-linear-0b2377a2-366f-4309-a508-610e524c9d94.md tasks/specs/linear-0b2377a2-366f-4309-a508-610e524c9d94.md tasks/tasks-linear-0b2377a2-366f-4309-a508-610e524c9d94.md .agent/task/linear-0b2377a2-366f-4309-a508-610e524c9d94.md tasks/index.json docs/TASKS.md docs/docs-freshness-registry.json`
- Parent implementation checks:
  - focused direct-start failure regression
  - focused supported relaunch/recover provenance regression
  - focused provider-intake launch-token/intake reservation truth regression
  - focused child-lane compatibility after relaunch regression
  - preservation coverage for `CO-244` provenance behavior
  - preservation coverage for `CO-392` stale pending-reopen refresh/reclaim scope
  - preservation coverage for `CO-189` duplicate-worker protection/provider admission caps
  - `node scripts/spec-guard.mjs --dry-run`
  - parent-owned docs-review and implementation-gate manifests
- Rollback plan:
  - keep existing root control-host launcher intact
  - if supported relaunch/recover cannot preserve provenance and artifact routing, parent should revert the new command/guard changes and leave direct starts fail-closed until a wider authority refactor is launched
  - do not restore unguarded direct provider-worker starts as a rollback path

## Risks & Mitigations

- Risk: implementation adds a new command but still direct-starts the worker without root control-host provenance.
  - Mitigation: require manifest/provider-intake provenance assertions and child-lane compatibility proof.
- Risk: direct-start guard blocks a valid control-host launch because provenance matching is too strict or reads the wrong root.
  - Mitigation: parent tests matching and mismatched control-host task/run ids plus deterministic issue worktree cwd.
- Risk: relaunch/recover bypasses duplicate-worker protection or provider admission caps.
  - Mitigation: parent uses existing admission/reservation paths and reruns preservation tests.
- Risk: the lane drifts into `CO-392` stale pending-reopen refresh behavior.
  - Mitigation: keep CO-392 as a named non-goal and preserve focused refresh/reclaim tests without expanding them.
- Risk: operators receive a fail-closed message without enough recovery guidance.
  - Mitigation: direct-start failure must include an actionable root control-host command hint with the issue id.

## Approvals

- Reviewer: pending parent docs-review and implementation.
- Date: 2026-04-26.
