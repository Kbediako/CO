# ACTION_PLAN - CO-393 control-host provider-worker recovery

## Readiness Gate
Intent preserved from `CO-393`: root control-host relaunch/recover/nudge for one Linear issue, direct provider-worker start fail-closed guard, matching provenance, deterministic issue worktree cwd, root `.runs` / `out`, manifest/provider-intake provenance, launch-token truth, and `CO-392` / `CO-244` / `CO-189` boundaries. Fallback decision: retain root control-host authority; remove unsafe direct-start fallback without matching provenance.

## Steps
1. Register docs packet and mirrors.
2. Add root `control-host recover|relaunch|nudge --issue-id <id>` CLI and authenticated control-host API route.
3. Add provider handoff recovery using existing admission, duplicate-worker protection, launch-token generation, and launch/resume logic.
4. Release stale existing claims when direct issue lookup resolves gone/out-of-scope.
5. Guard direct `provider-linear-worker` starts by validating env provenance, requested task id, issue/identifier, token, and unconsumed `state=starting` reservation against root provider-intake state.
6. Add focused regressions, run full validation, standalone review, elegance pass, PR update, and ready-review drain.

## Validation / Rollback
Focused: direct-start guard, API route, handoff recovery, stale-claim release, and child-lane compatibility. Full gates: delegation guard, spec guard, build, lint, test, docs check/freshness, repo stewardship, diff budget, pack smoke, standalone review, elegance pass, PR ready-review. If recovery cannot preserve root control-host provenance and artifact routing, revert the recovery command/API and keep direct starts fail-closed; do not restore unguarded starts.
