# ACTION_PLAN - Coordinator Symphony-Aligned Compatibility Route Contract + Dispatch Extension Boundary

1. Register `1030` docs-first artifacts, checklist mirrors, task index entries, and freshness registry coverage.
2. Capture read-only review/deliberation notes against the real Symphony router/controller/presenter reference.
3. Refine the remaining compatibility route contract so state/refresh/issue follow a clearer Symphony-aligned controller/presenter shape.
4. Isolate `/api/v1/dispatch` as an explicit CO extension boundary without changing its current advisory-only behavior.
5. Update route and regression coverage to distinguish Symphony-aligned compatibility routes from the CO-specific dispatch extension.
6. Run the ordered validation lane, manual mock route checks, elegance review, and closeout sync.
