# ACTION_PLAN - CO-171 Provider Artifact Root Parity
Scope/steps/validation: document and align provider-worker artifact-root resolution for `.runs`, `runs`, configured absolute roots, and repo-relative inherited shared-root values; preserve CO-169 default worktree-root behavior and workspace-scoped child-stream/review evidence; run docs-review before code; audit provider context loading, environment path resolution, and review-wrapper root selection; implement the smallest shared decision contract; add regressions for default `.runs`, `runs`, custom absolute roots, and relative inherited roots; then run delegation guard, spec guard, build, lint, focused/full tests, docs checks, stewardship, diff budget, standalone review, elegance, and pack smoke before PR handoff.
