# PRD - CO child-lane lineage: preserve parent provenance across manual provider-worker launch and issue workspace rebinding

## Added by Docs Child Lane 2026-04-18

## Traceability
- Linear issue: `CO-245` / `0e426da7-6455-4292-8110-f44ee07b827a`
- Task id: `linear-0e426da7-6455-4292-8110-f44ee07b827a`
- Canonical spec: `tasks/specs/linear-0e426da7-6455-4292-8110-f44ee07b827a.md`
- Docs packet child lane manifest: `.runs/linear-0e426da7-6455-4292-8110-f44ee07b827a-co245-docs-packet/cli/2026-04-18T08-07-11-555Z-d63daa80/manifest.json`
- Source anchor: `ctx:sha256:a705905f842ad361de1c5f68a088b5f4210340f7b8dd1105544184664a80283e#chunk:c000001`
- Source payload note: the expected shared `source-0` payload is not present in this child checkout; this packet is therefore anchored on the protected handoff wording plus current repo seams in `tests/delegation-guard.spec.ts`, `tests/run-review.spec.ts`, `orchestrator/src/cli/providerLinearChildLaneShell.ts`, `orchestrator/src/cli/providerLinearChildLaneRunner.ts`, `orchestrator/src/cli/run/source0.ts`, `orchestrator/src/cli/run/manifest.ts`, `orchestrator/src/cli/controlHostCliShell.ts`, and `orchestrator/src/cli/control/selectedRunProjection.ts`.

## Summary
- Problem Statement: `manual provider-worker launch` and `same-issue child lane` flows already carry real lineage fields, but that lineage can drift across a `parent provenance gate`, stale claim paths, an `older canonical parent manifest`, `parent_run_id`, `issue workspace rebinding`, and downstream read/projection seams. Without one explicit contract, real current runs can fail closed for the wrong reason or stale path/layout residue can outrank `current-run lineage attribution`.
- Desired Outcome: preserve a narrow provenance and lineage contract where `manual provider-worker launch` remains fail-closed unless launch provenance exists, `same-issue child lane` lineage survives `issue workspace rebinding`, `older canonical parent manifest` only acts as a bounded backstop when `parent_run_id` continuity proves the same parent, and `current-run lineage attribution` stays authoritative. This lane must remain explicitly separate from `CO-241` and `CO-244`.

## User Request Translation (Context Anchor)
- User intent / needs (in your own words): create the docs-first packet for `CO-245` only, preserving the exact issue checksum around `manual provider-worker launch`, `same-issue child lane`, `parent provenance gate`, `older canonical parent manifest`, `parent_run_id`, `issue workspace rebinding`, and `current-run lineage attribution` before the parent lane changes code or tests.
- Success criteria / acceptance:
  - `manual provider-worker launch` remains accepted only when the `parent provenance gate` can prove real launch provenance
  - `same-issue child lane` lineage remains valid after `issue workspace rebinding`
  - an `older canonical parent manifest` can backstop stale path/layout drift only when `parent_run_id` continuity proves the same current parent
  - `current-run lineage attribution` prefers the real current parent/child lineage over stale claim paths or stale layout roots
  - the packet keeps this lane explicitly distinct from `CO-241` and `CO-244`
- Constraints / non-goals:
  - child lane owns only the declared docs packet, checklist mirrors, `tasks/index.json`, `docs/TASKS.md`, and `docs/docs-freshness-registry.json`
  - parent lane owns the authoritative issue workspace, implementation, validation commands, Linear state, workpad refreshes, PR lifecycle, and merge
  - do not widen this child lane into implementation edits, test edits, or generic workspace-root / artifact-root parity redesign

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `manual provider-worker launch`
  - `same-issue child lane`
  - `parent provenance gate`
  - `older canonical parent manifest`
  - `parent_run_id`
  - `issue workspace rebinding`
  - `current-run lineage attribution`
- Protected terms / exact artifact and surface names:
  - `manual provider-worker launch`
  - `same-issue child lane`
  - `parent provenance gate`
  - `older canonical parent manifest`
  - `parent_run_id`
  - `issue workspace rebinding`
  - `current-run lineage attribution`
  - `tests/delegation-guard.spec.ts`
  - `tests/run-review.spec.ts`
  - `providerLinearChildLaneShell.ts`
  - `providerLinearChildLaneRunner.ts`
  - `source0.ts`
  - `manifest.ts`
- Nearby wrong interpretations to reject:
  - this is only a generic issue-worktree authority or review-wrapper rebinding lane
  - this is only a generic artifact-root parity lane
  - this can be fixed by weakening the `parent provenance gate` to accept path-only or stream-only matches
  - an `older canonical parent manifest` can replace `parent_run_id` continuity or missing launch provenance
  - `current-run lineage attribution` can keep diverging from rebinding/projection seams
  - this is just `CO-241` or `CO-244` under a new name

## Parity / Alignment Matrix

| Surface | Current Truth | Reference Truth | Target Truth |
| --- | --- | --- | --- |
| `manual provider-worker launch` | Real provider-started fallback runs can depend on stale claim paths or older manifest shapes. | Launch acceptance must stay fail-closed unless real provenance exists. | `manual provider-worker launch` remains accepted only when real launch provenance is present, with bounded fallback for stale path/layout drift. |
| `parent provenance gate` | Path drift or missing locator fields can make a real current run look ambiguous. | The gate should protect against false positives without discarding real current lineage. | The `parent provenance gate` reuses one explicit rule for current lineage, stale claim-path fallback, and fail-closed rejection when provenance is missing. |
| `older canonical parent manifest` | Older canonical manifests can be the only stable anchor when paths drift after launch. | Older canonical state is a bounded backstop, not a replacement for current lineage. | An `older canonical parent manifest` is consulted only when it agrees with current identity and `parent_run_id`; it never overrides missing provenance. |
| `parent_run_id` | `parent_run_id` already exists in manifests and child-lane surfaces, but downstream seams can still de-emphasize it when paths change. | `parent_run_id` should remain the primary parent-lineage link across launch, child lanes, source-0, and projection. | `parent_run_id` stays authoritative across launch, child-lane attribution, rebinding, and projection surfaces. |
| `issue workspace rebinding` | Rebinding can rewrite artifact paths to the issue workspace or keep the real shared manifest when no workspace counterpart exists. | Rebinding should correct paths without changing logical lineage. | `issue workspace rebinding` updates artifact paths without breaking the authoritative current parent/child lineage. |
| `same-issue child lane` | Same-issue child lanes can be real even when artifact paths or claim paths no longer match the original launch-time path shape. | Same-issue child lanes should remain attributable through metadata-safe lineage rather than path-only matching. | `same-issue child lane` attribution follows parent/issue lineage fields first and survives bounded rebinding/path drift. |
| `current-run lineage attribution` | Current lineage can drift between launch provenance, manifest/source-0 lineage, and projection/reporting surfaces. | One current authoritative lineage should outrank stale path/layout residue. | `current-run lineage attribution` is shared across provenance, rebinding, and projection seams so the same current parent/child truth wins everywhere. |

## Acceptance Criteria
- Docs packet and checklist mirrors exist for the declared `CO-245` files only.
- The packet preserves the exact issue wording around `manual provider-worker launch`, `same-issue child lane`, `parent provenance gate`, `older canonical parent manifest`, `parent_run_id`, `issue workspace rebinding`, and `current-run lineage attribution`.
- Parent implementation can keep launch provenance fail-closed while still preserving real current lineage across stale claim paths and rebinding.
- Parent implementation scope is clearly bounded to lineage/provenance correctness, not broad workspace-root or artifact-root redesign.
- The packet explicitly rejects drift into `CO-241` and `CO-244`.
- The parity matrix current/reference/target contract is explicit before implementation begins.

## Non-Goals
- No generic issue-worktree authority rewrite.
- No generic artifact-root parity redesign.
- No weakening of the `parent provenance gate`.
- No replacement of `parent_run_id` with path-only or stream-only matching.
- No implementation or test edits in this docs child lane.
- No Linear or PR mutations from this child lane.

## Not Done If
- `manual provider-worker launch` can still pass without real provenance.
- `same-issue child lane` lineage still breaks after `issue workspace rebinding`.
- `older canonical parent manifest` can outrank missing `parent_run_id` continuity or missing launch provenance.
- `current-run lineage attribution` still diverges between provenance, rebinding, and projection surfaces.
- The packet drifts into generic workspace-root or artifact-root parity work or stops being clearly distinct from `CO-241` and `CO-244`.
- The packet omits the explicit parity matrix or protected wording.

## Stakeholders
- Product: CO operators and reviewers who need current parent/child lineage to stay truthful in provider-worker and child-lane flows.
- Engineering: provenance, child-lane, source-0, run-manifest, run-review, and read/projection maintainers.
- Design: N/A.

## Metrics & Guardrails
- Primary Success Metrics:
  - one current-lineage contract exists across launch provenance, child-lane manifests, source-0, rebinding, and projection surfaces
  - `manual provider-worker launch` remains fail-closed when provenance is missing
  - real same-parent, same-issue lineage survives stale path/layout drift and bounded rebinding
  - `CO-245` remains clearly distinct from `CO-241` and `CO-244`
- Guardrails / Error Budgets:
  - preserve fail-closed launch-provenance behavior
  - preserve `parent_run_id` as the primary lineage key
  - keep rebinding path correction separate from broader artifact-root policy redesign

## User Experience
- Personas: CO operator or reviewer auditing a provider-started lane, a same-issue child lane, or a review/projection surface after issue workspace rebinding.
- User Journeys:
  - operator sees a real provider-started or same-issue child lane and can still trace it back to the correct parent after paths drift
  - reviewer can inspect `parent_run_id`, launch provenance, and canonical parent manifest evidence without inferring lineage from raw paths alone
  - operator sees one current parent/child truth across provenance, source-0, and projection surfaces

## Technical Considerations
- Architectural Notes:
  - `tests/delegation-guard.spec.ts` already covers the provenance side: stale locator fields, stale claim paths, canonical parent manifest fallback, `parent_run_id`, and fail-closed missing launch provenance
  - `tests/run-review.spec.ts` already covers the rebinding side: issue-workspace artifact rebasing versus staying on the real shared manifest when no workspace counterpart exists
  - `providerLinearChildLaneRunner.ts`, `run/manifest.ts`, and `run/source0.ts` already carry `parent_run_id` and source lineage into child-lane artifacts
  - `providerLinearChildLaneShell.ts`, `controlHostCliShell.ts`, and `selectedRunProjection.ts` are the likely parent-owned authority seams for preserving lineage across provenance and projection
  - the smallest correct fix is one shared lineage/provenance contract reused across provenance and projection seams instead of separate path-based exceptions
- Dependencies / Integrations:
  - `tests/delegation-guard.spec.ts`
  - `tests/run-review.spec.ts`
  - `orchestrator/src/cli/providerLinearChildLaneShell.ts`
  - `orchestrator/src/cli/providerLinearChildLaneRunner.ts`
  - `orchestrator/src/cli/run/source0.ts`
  - `orchestrator/src/cli/run/manifest.ts`
  - `orchestrator/src/cli/controlHostCliShell.ts`
  - `orchestrator/src/cli/control/selectedRunProjection.ts`

## Open Questions
- Should `older canonical parent manifest` matching live only inside the `parent provenance gate`, or in a shared helper reused by source-0 and projection surfaces?
- Which seam should own the final `current-run lineage attribution` decision once both rebinding and stale claim-path fallback exist?
- How much lineage metadata must remain explicit in source-0 artifacts so same-issue child-lane and review surfaces remain auditable after rebinding?

## Validation Contract
- Parent lane runs focused regressions in `tests/delegation-guard.spec.ts`, `tests/run-review.spec.ts`, and any additional narrow lineage/projection tests selected by the final seam choice.
- Child lane runs only scoped docs checks: JSON parse for `tasks/index.json` and `docs/docs-freshness-registry.json`, protected-term grep over the touched packet files, and `git diff --check` over the declared docs scope.
