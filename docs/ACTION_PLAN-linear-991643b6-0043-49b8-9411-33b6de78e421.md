# ACTION_PLAN - CO-512 governed review contract no-fallback packet

## Summary
- Goal: produce the CO-512 docs-first packet, implement the governed review contract, and validate the no-fallback review handoff path while preserving protected no-fallback wording and acceptance criteria.
- Scope: PRD, TECH_SPEC mirror, ACTION_PLAN, canonical task spec, task checklist, `tasks/index.json`, canonical contract schema, structured-output transport schema, contract module, review wrapper telemetry, provider-worker handoff gating, proposal routing, and focused regression tests.
- Assumptions:
  - the parent reconciled the source payload at `.runs/linear-991643b6-0043-49b8-9411-33b6de78e421/cli/2026-05-14T14-43-04-812Z-ce117406/memory/source-0/source.txt`
  - the parent owns validation, workpad, Linear state, PR lifecycle, and review handoff
  - `no fallback` and `no-fallback` are protected wording, not optional phrasing
  - clean governed review evidence is required before review handoff

## Issue Readiness Gate
- Intent checksum / protected terms carried forward:
  - `governed review contract`
  - `no fallback`
  - `no-fallback`
  - `acceptance criteria`
  - `clean governed review evidence`
  - `review handoff`
- Not done if:
  - any scoped packet file omits the protected no-fallback wording
  - fallback review evidence, missing telemetry, stale output, or semantically dirty verdicts are allowed to satisfy handoff
  - clean handoff depends on prose-only output, empty findings, wrapper success, scoped-title retry, or artifact-only prompt transport
  - reviewer changes are applied directly instead of proposed
  - agent-loop feedback is mixed into code findings or ingested into memory before explicit validation and acceptance
- Pre-implementation issue-quality review:
  - 2026-05-14: child scope is packet-only and can complete without widening file ownership.
  - 2026-05-14: the issue is not merely docs wording because the parent must enforce the governed review contract with no fallback before review handoff.
  - 2026-05-14: micro-task path is unavailable because correctness depends on exact protected wording and fallback/seam behavior.
- Fallback / refactor decision: `remove fallback` for any handoff path that accepts fallback, missing, stale, or semantically dirty review evidence as satisfying the governed review contract.
- Durable retention evidence: none. This packet does not justify retaining fallback review acceptance.
- Large-refactor decision: no extra broad refactor is required; the smallest governed path is one schema/module plus wrapper telemetry and provider-worker gate integration.

## CO-382 Fallback Decision Table

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Governed review handoff | Fallback review evidence, missing telemetry, stale output, or semantically dirty verdict accepted as handoff proof | `remove fallback` | CO-512 parent lane | Parent attempts Human Review / In Review handoff without current clean governed review evidence | Unknown from child source; parent must confirm | 2026-05-14 | Not retained; remove before handoff | Handoff requires clean governed review evidence with no fallback, or the lane remains blocked/rework instead of advancing | Parent-owned focused tests plus manifest/review telemetry proof showing no fallback and clean verdict before handoff |

## Milestones & Sequencing
1. Draft PRD, TECH_SPEC mirror, ACTION_PLAN, canonical task spec, and task checklist.
2. Register the packet in `tasks/index.json` with the TECH_SPEC pointer string.
3. Run scoped child-lane sanity checks only.
4. Leave changes in place for parent patch export.
5. Parent reconciles against source payload, implements the governed review contract with `codex exec --output-schema` / `--output-last-message` / stdin prompt delivery as the enforce-mode no-fallback transport, validates focused behavior, and owns PR/Linear lifecycle.
6. Run standalone review and elegance review before review handoff.

## Dependencies
- CO-512 parent lane.
- Source anchor `ctx:sha256:141ca5b9d78167ddcb18b23d5b89c747b145a3a2bee95da7f903b322ab037291#chunk:c000001`.
- Parent-owned source payload and live Linear state.

## Validation
- Child checks:
  - scoped file existence
  - `tasks/index.json` JSON parse
  - protected-term scan across scoped files
  - `git diff --check`
- Parent checks:
  - source payload reconciliation
  - focused implementation tests for governed review handoff behavior, proposal routing, and accepted-only memory entries
  - governed review telemetry proof showing no fallback, the structured-output enforce transport, and a clean verdict before handoff
  - parent-required validation and review gates
- Rollback plan:
  - parent can reject or omit this packet patch if source payload reconciliation shows the child prompt was insufficient.

## Risks & Mitigations
- Risk: the source payload or live issue contains additional acceptance criteria beyond the child packet.
  - Mitigation: parent reconciled the source payload and mirrored the full issue acceptance criteria into implementation tests and workpad gates.
- Risk: no-fallback wording is softened during implementation.
  - Mitigation: Not Done If and fallback decision table reject fallback, missing, stale, or semantically dirty review evidence as handoff proof.
- Risk: agent-loop feedback becomes an ungoverned memory input.
  - Mitigation: proposal routing exports memory entries only for accepted validated agent-loop proposals.

## Approvals
- Reviewer: CO-512 parent lane
- Date: 2026-05-14
