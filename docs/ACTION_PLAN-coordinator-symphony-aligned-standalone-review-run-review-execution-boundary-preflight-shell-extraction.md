# ACTION_PLAN: Coordinator Symphony-Aligned Standalone Review Run-Review Execution-Boundary Preflight Shell Extraction

1. Move the cohesive execution-boundary preflight cluster behind a dedicated helper/module without widening scope beyond the remaining pre-launch boundary setup.
2. Rewire `scripts/run-review.ts` so `main()` delegates bounded-mode/env/timeout/startup-loop normalization while keeping broader wrapper orchestration local.
3. Add focused regressions, run the deterministic validation lane, and close with an explicit elegance pass plus truthful override notes if docs-review or bounded review drifts.
