# PRD - CO: restore same-issue re-admission for reopened terminal released/not_active claims without stale merge_closeout or replay-block suppression

## Added by Docs Child Lane 2026-04-18

## Traceability
- Linear issue: `CO-238` / `5570250a-1361-4af0-857c-119649b902ab`
- Task id: `linear-5570250a-1361-4af0-857c-119649b902ab`
- Canonical spec: `tasks/specs/linear-5570250a-1361-4af0-857c-119649b902ab.md`
- Source anchor: `ctx:sha256:1d858a457b5eb91c9daeee7c4422ca5926d9bde1dda0409a099abb0b9acfa930#chunk:c000001`
- Source object id: `sha256:1d858a457b5eb91c9daeee7c4422ca5926d9bde1dda0409a099abb0b9acfa930`
- Expected source payload: `.runs/linear-5570250a-1361-4af0-857c-119649b902ab-co-238-docs/cli/2026-04-18T02-50-07-931Z-b4fa7aed/memory/source-0/source.txt`
- Origin manifest: `.runs/linear-5570250a-1361-4af0-857c-119649b902ab-co-238-docs/cli/2026-04-18T02-50-07-931Z-b4fa7aed/manifest.json`
- Source payload note: the expected shared `source-0` payload is not present in this child checkout; this packet is anchored on the protected issue wording from the bounded-lane handoff plus adjacent issue-family boundaries already recorded for `CO-138`, `CO-160` / `CO-161`, `CO-189`, `CO-212`, `CO-214`, and `CO-175`.

## Summary
- Problem Statement: a previously terminal same issue can remain locally `released` with reason `provider_issue_released:not_active` after the live issue later reopens to runnable truth. If stale `merge_closeout residue` or prior `replay-block` state stays authoritative, `fresh discovery` can continue suppressing that same issue even though the claim is now a `reopened terminal claim`, no live same-issue worker remains, and the issue should be eligible for bounded same-issue re-admission.
- Desired Outcome: reopened terminal claims re-enter the bounded same-issue admission path when live truth is newer and runnable, while preserving zero-burn released-claim safeguards, `replay-block` for unrelated released claims, duplicate-worker protection, and explicit separation from adjacent `CO-138`, `CO-212`, and `CO-175` boundaries.

## User Request Translation (Context Anchor)
- User intent / needs (in your own words): create the missing docs-first packet and registry mirrors for `CO-238` using the issue wording as the source of truth, without touching implementation files.
- Success criteria / acceptance:
  - preserve the exact protected terms `provider_issue_released:not_active`, `reopened terminal claim`, `merge_closeout residue`, `Ready reclaim`, `replay-block`, `fresh discovery`, `same-issue re-admission`, and `CO-175`
  - keep this lane explicitly separate from `CO-138` and `CO-212` while still referencing those boundaries
  - define the parent implementation as a bounded same-issue re-admission seam, not a broad merge-closeout redesign, Ready reclaim rewrite, or docs-freshness policy change
  - keep parent-owned responsibilities explicit: implementation, validation, Linear state, workpad refresh, PR, and merge
- Constraints / non-goals:
  - child lane owns only the declared docs/task packet files plus `tasks/index.json`
  - the child lane must not edit implementation or test files
  - the child lane must not mutate Linear state or workpad state
  - registry work in this child lane stays limited to the declared mirrors; no broader `docs/TASKS.md` or docs-freshness ownership changes are in scope

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `provider_issue_released:not_active`
  - `reopened terminal claim`
  - `merge_closeout residue`
  - `Ready reclaim`
  - `replay-block`
  - `fresh discovery`
  - `same-issue re-admission`
  - `CO-175`
- Protected terms / exact artifact and surface names:
  - `provider-intake-state.json`
  - `provider_issue_released:not_active`
  - `merge_closeout`
  - `replay-block`
  - `fresh discovery`
  - `same-issue re-admission`
  - `providerIssueHandoff.ts`
  - `providerMergeCloseout.ts`
  - `providerLinearWorkflowStates.ts`
  - `controlRuntime.ts`
  - `CO-138`
  - `CO-212`
  - `CO-175`
- Nearby wrong interpretations to reject:
  - this is only `CO-138` stale `merge_closeout residue` cleanup after leaving `Merging`
  - this is only `CO-212` `Ready reclaim`
  - this is a broad `CO-160` / `CO-161` replay-block or deferred-poll redesign
  - this is only `CO-189` live same-issue rehydrate with no reopened-terminal boundary
  - the fix is deleting retained provider-intake history or weakening duplicate-worker protection
  - touching registry mirrors here implicitly reopens `CO-175` docs-freshness ownership or policy

## Parity / Alignment Matrix

| Surface | Current Truth | Reference Truth | Target Truth |
| --- | --- | --- | --- |
| Reopened terminal claim | A previously terminal same issue can keep a retained local `provider_issue_released:not_active` claim after live truth reopens. | Retained terminal history can stay auditable without suppressing a later reopened issue forever. | Once live truth reopens the same issue to runnable state, the retained terminal claim stops blocking bounded same-issue re-admission. |
| `merge_closeout residue` | Stale `merge_closeout residue` can still make old closeout state look authoritative after reopen. | Only current closeout state should control whether the reopened issue is still blocked. | Stale `merge_closeout residue` is superseded when fresher reopened truth is explicit. |
| `replay-block` and `fresh discovery` | Released-claim `replay-block` can keep `fresh discovery` from re-admitting the same reopened issue. | Replay-block should preserve zero-burn behavior for unrelated released claims, not strand a proven same-issue reopen. | `fresh discovery` or equivalent same-issue re-admission can proceed for the reopened issue without removing replay-block from unrelated claim families. |
| Same-issue safety | Reopening a terminal claim risks duplicate launch if live-worker proof is stale or missing. | No live same-issue worker remains a hard admission prerequisite. | Same-issue re-admission stays gated on no-live-worker proof and truthful retained history. |
| Scope | The issue can be misread as `Ready reclaim`, stale merge-closeout cleanup, or docs-freshness maintenance. | Adjacent issues already own those seams separately. | `CO-238` stays bounded to reopened terminal claim same-issue re-admission with explicit boundaries against `CO-138`, `CO-212`, and `CO-175`. |

## Goals
- Restore bounded same-issue re-admission for reopened terminal claims that still retain `provider_issue_released:not_active`.
- Make stale `merge_closeout residue` and `replay-block` stop suppressing the same issue once fresher reopened truth is explicit.
- Preserve zero-burn released-claim behavior for unrelated deferred-poll claims.
- Preserve same-issue duplicate-worker protection and retained audit history.

## Non-Goals
- No implementation or test edits in this child lane.
- No broad replay-block or deferred-poll redesign from `CO-160` / `CO-161`.
- No rewrite of `CO-138` stale `merge_closeout residue` cleanup semantics.
- No rewrite of `CO-212` completed-blocker `Ready reclaim` semantics.
- No `CO-175` docs-freshness policy, cap, or ownership changes.
- No Linear state, workpad, PR, or merge mutation from this child lane.

## Stakeholders
- Product: CO operators and control-host owners who need reopened issues to re-enter admission truthfully.
- Engineering: maintainers of provider handoff, merge-closeout truth, runtime projection, and released-claim recovery seams.
- Design: N/A.

## Metrics & Guardrails
- Primary Success Metrics:
  - reopened terminal same issues are no longer stranded behind stale retained-release truth
  - `replay-block` remains bounded to unrelated released-claim no-burn behavior
  - no duplicate same-issue worker is admitted
  - registry/task mirrors stay truthful without reopening `CO-175`
- Guardrails / Error Budgets:
  - do not broaden into `CO-138` or `CO-212`
  - do not delete retained local audit evidence as the product fix
  - do not weaken same-issue live-worker protection
  - do not widen docs-freshness policy ownership

## User Experience
- Personas:
  - operator waiting for a legitimately reopened issue to re-enter provider admission
  - maintainer debugging why stale retained-release truth still suppresses a reopened issue
  - reviewer checking that replay-block remains zero-burn without blocking proven same-issue re-admission
- User Journeys:
  - a previously terminal issue reopens to runnable truth; stale retained-release history remains visible for audit, but the same issue can be re-admitted
  - stale `merge_closeout residue` no longer keeps the reopened issue excluded once fresher runnable truth exists
  - `replay-block` still protects unrelated all-released deferred-poll paths without stranding the reopened same issue

## Technical Considerations
- Architectural Notes:
  - the likely implementation seam is in provider handoff and released-claim admission logic, with merge-closeout and runtime surfaces kept aligned only where necessary
  - the bounded fix should compare retained release/closeout truth against fresher reopened live truth instead of rewriting broad poll or workflow policy
  - same-issue re-admission should stay subordinate to no-live-worker proof and existing duplicate-worker safeguards
- Dependencies / Integrations:
  - `orchestrator/src/cli/control/providerIssueHandoff.ts`
  - `orchestrator/src/cli/control/providerMergeCloseout.ts`
  - `orchestrator/src/cli/control/providerLinearWorkflowStates.ts`
  - `orchestrator/src/cli/control/controlRuntime.ts`
  - `orchestrator/tests/ProviderIssueHandoff.test.ts`
  - `orchestrator/tests/ControlRuntime.test.ts`

## Open Questions
- Should the reopened-terminal allowance be expressed as a dedicated same-issue re-admission predicate, or as a narrower exception inside existing `fresh discovery` replay-block logic?
- Does stale `merge_closeout residue` need an explicit superseded marker, or is fresher reopened truth sufficient when the same issue becomes runnable again?
- Which runtime/status surface, if any, needs explicit parity checks so reopened same-issue truth does not drift from admission truth?

## Approvals
- Reviewer: docs child lane self-review for packet shape and adjacent-lane boundaries.
- Date: 2026-04-18
