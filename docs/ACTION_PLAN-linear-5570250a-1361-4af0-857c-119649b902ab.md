# ACTION_PLAN - CO: restore same-issue re-admission for reopened terminal released/not_active claims without stale merge_closeout or replay-block suppression

## Added by Docs Child Lane 2026-04-18

## Traceability
- Linear issue: `CO-238` / `5570250a-1361-4af0-857c-119649b902ab`
- Task id: `linear-5570250a-1361-4af0-857c-119649b902ab`
- Source anchor: `ctx:sha256:1d858a457b5eb91c9daeee7c4422ca5926d9bde1dda0409a099abb0b9acfa930#chunk:c000001`
- Origin manifest: `.runs/linear-5570250a-1361-4af0-857c-119649b902ab-co-238-docs/cli/2026-04-18T02-50-07-931Z-b4fa7aed/manifest.json`

## Summary
- Goal: finish `CO-238` by restoring bounded same-issue re-admission when a previously terminal released/not_active claim later reopens to runnable truth.
- Scope: docs-first packet, parent-owned provider handoff and re-admission repair, focused regressions for replay-block preservation and reopened-terminal eligibility, scoped validation, and review handoff.
- Assumptions:
  - the retained-release and stale closeout shapes already exist in current provider handoff history or issue-family fixtures
  - the smallest safe fix lives in same-issue admission truth rather than broad merge-closeout or deferred-poll redesign
  - `CO-175` docs-freshness ownership remains unchanged; this child packet touches only the declared mirrors

## Issue Readiness Gate
- Intent checksum / protected terms carried forward:
  - preserve `provider_issue_released:not_active`, `reopened terminal claim`, `merge_closeout residue`, `Ready reclaim`, `replay-block`, `fresh discovery`, `same-issue re-admission`, and `CO-175`
  - reject "this is just `CO-138`", "this is just `CO-212`", "redesign replay-block globally", and "reopen `CO-175` ownership because registry files moved"
- Not done if:
  - reopened terminal same issues remain stranded by retained release or closeout truth after fresher runnable truth returns
  - replay-block loses zero-burn protection for unrelated released claims
  - same-issue duplicate-worker protection weakens
  - the lane widens into `CO-138`, `CO-212`, or `CO-175`
- Pre-implementation issue-quality review:
  - the live issue wording and adjacent packet audit narrow the seam to reopened-terminal same-issue re-admission. Accepted scope is fresher runnable truth outranking stale retained terminal truth for the same issue, not a general poll, merge-closeout, or docs-freshness redesign.

## Milestones & Sequencing
1. Register the `CO-238` docs-first packet, task checklist, `.agent` mirror, and `tasks/index.json` entry within the declared docs-only scope.
2. Parent reproduces the reopened terminal same-issue shape and records which retained signals remain authoritative today: `provider_issue_released:not_active`, stale `merge_closeout residue`, release-family `replay-block`, and same-issue liveness proof.
3. Parent implements the smallest reopened-terminal re-admission seam so fresher runnable truth can re-admit the same issue without broadening unrelated replay-block behavior.
4. Parent adds or updates focused regressions for:
   - reopened terminal same-issue re-admission
   - stale `merge_closeout residue` no longer blocking the same issue after reopen
   - unrelated released-claim replay-block preservation
   - no duplicate same-issue worker admission
5. Parent runs docs-review, focused validation, standalone review, and elegance review before PR handoff.

## Dependencies
- `orchestrator/src/cli/control/providerIssueHandoff.ts`
- `orchestrator/src/cli/control/providerMergeCloseout.ts`
- `orchestrator/src/cli/control/providerLinearWorkflowStates.ts`
- `orchestrator/src/cli/control/controlRuntime.ts`
- `orchestrator/tests/ProviderIssueHandoff.test.ts`
- `orchestrator/tests/ControlRuntime.test.ts`

## Validation
- Child-lane checks:
  - `node -e "JSON.parse(require('fs').readFileSync('tasks/index.json','utf8'))"`
  - protected-term grep across the touched packet files
  - `git diff --check` over the touched files only
- Parent-owned checks:
  - focused provider handoff regression for reopened terminal same-issue re-admission
  - focused replay-block preservation regression for unrelated released claims
  - focused stale `merge_closeout residue` supersession regression only where reopened same-issue truth requires it
  - `node scripts/spec-guard.mjs --dry-run` after patch import
  - parent-owned docs-review before implementation
- Rollback plan:
  - revert the reopened-terminal allowance if it weakens duplicate-worker protection or broadens replay-block bypass beyond the same issue
  - keep the issue open until reopened same-issue re-admission and unrelated replay-block preservation are proven together

## Risks & Mitigations
- Risk: the repair broadens into `CO-138` stale merge-closeout cleanup.
  - Mitigation: key the fix to reopened same-issue runnable truth rather than generic stale closeout invalidation.
- Risk: the repair broadens into `CO-212` completed-blocker `Ready reclaim`.
  - Mitigation: keep blocker-state semantics untouched; the acceptance seam is reopened terminal truth, not completed-blocker policy.
- Risk: bypassing replay-block for the reopened issue also weakens unrelated all-released no-burn behavior.
  - Mitigation: require same-issue identity plus fresher runnable truth before any bypass is allowed.
- Risk: registry edits are misread as `CO-175` freshness-policy work.
  - Mitigation: record explicitly that this child lane updates only the declared packet mirrors and `tasks/index.json`.

## Approvals
- Reviewer: docs child lane self-review for packet shape and boundaries.
- Date: 2026-04-18
