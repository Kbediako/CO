# PRD - CO workflow restore docs/TASKS headroom and make task archiving proactive

## Added by Parent Lane 2026-04-18

## Traceability
- Linear issue: `CO-221` / `5f3bf177-0fa7-4378-9438-5dcaf7919394`
- Linear URL: https://linear.app/asabeko/issue/CO-221/co-workflow-restore-non-zero-docstasks-headroom-and-make-task
- Task id: `linear-5f3bf177-0fa7-4378-9438-5dcaf7919394`
- Source anchor: `ctx:sha256:e3025df78e9a2e0a019d7086dd31f4aa02bc7e6a3e107fccbdcbd8d86d18b012#chunk:c000001`
- Origin manifest: `../../.runs/linear-5f3bf177-0fa7-4378-9438-5dcaf7919394/cli/2026-04-17T14-06-45-422Z-9f681458/manifest.json`

## Summary
- Problem Statement: `docs/TASKS.md` on `main` is sitting at `450/450`, so routine docs-first lanes can fail once they add one more snapshot line. The current guardrail and automation only react after the file is already full or overfull, which keeps unrelated work stuck doing packet-local archive cleanup.
- Desired Outcome: preserve a documented non-zero reserve in `docs/TASKS.md`, make the existing archive path restore that reserve proactively, and add focused coverage so the repo cannot silently drift back to the `450/450` steady state.

## User Request Translation (Context Anchor)
- User intent / needs (in your own words): fix the standing workflow gap, not the specific triggering lane, so ordinary docs-first work stops inheriting a full `docs/TASKS.md` baseline.
- Success criteria / acceptance:
  - `docs/TASKS.md` no longer lives at `450/450`
  - a concrete reserve headroom target is documented and enforced
  - the existing archive path remains the supported path and still preserves displaced snapshot truth, including `linear-*` task-key snapshots
  - focused regression coverage proves both the proactive near-full behavior and the zero-headroom guard
- Constraints / non-goals:
  - keep the current archive system; do not replace it
  - do not broaden into generic docs freshness or implementation-doc archive work
  - do not treat `CO-213` as the root cause; it is only the trigger

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `restore non-zero docs/TASKS headroom`
  - `make task archiving proactive`
  - `docs/TASKS.md`
  - `450/450`
  - `docs:check`
  - `docs/TASKS-archive-2026.md`
  - `linear-*`
- Protected terms / exact artifact and surface names:
  - `scripts/tasks-archive.mjs`
  - `scripts/docs-hygiene.ts`
  - `docs/tasks-archive-policy.json`
  - `tests/tasks-archive.spec.ts`
  - `tests/docs-hygiene.spec.ts`
  - `docs/TASKS.md`
  - `docs/TASKS-archive-2026.md`
- Nearby wrong interpretations to reject:
  - replacing the current archive system with a new one
  - broad docs freshness cleanup
  - solving only the packet-local symptom from `CO-213`
  - dropping `linear-*` archive truth to gain headroom

## Parity / Alignment Matrix

| Surface | Current Truth | Target Truth |
| --- | --- | --- |
| `docs/TASKS.md` reserve | `main` can sit at `450/450` with zero reserve. | `main` keeps a documented non-zero reserve below the hard ceiling. |
| Archive trigger | `scripts/tasks-archive.mjs` archives only after the file is already over the hard threshold. | the same archive script trims proactively once reserve is exhausted. |
| Zero-headroom guard | `docs:check` treats `450/450` as healthy because it fails only on overflow. | `docs:check` fails closed before the repo can treat `450/450` as acceptable steady state. |
| Archive truth | current archive handling already supports inline snapshots and `linear-*` task keys. | proactive trimming preserves the same truth and format. |

## Goals
- Document an explicit reserve headroom target in the existing tasks archive policy.
- Make `scripts/tasks-archive.mjs` preserve or restore that reserve proactively.
- Keep `docs:check` truthful around zero-headroom `docs/TASKS.md` states.
- Add focused regression coverage for the near-full and zero-headroom cases.

## Non-Goals
- Replacing the archive branch or archive payload format.
- General docs freshness remediation.
- Implementation-doc archive changes.
- Any broader redesign of task mirror content.

## Not Done If
- `main` can return to `450/450` without any reserve target.
- ordinary docs-first work still fails only because base `main` was already full and the archive path stayed reactive.
- displaced snapshot truth is lost or `linear-*` handling breaks.

## Stakeholders
- Product: operators and contributors using the docs-first workflow.
- Engineering: maintainers of docs hygiene and archive automation.
- Design: N/A.

## Metrics & Guardrails
- Primary Success Metrics:
  - proactive archive logic restores reserve without changing the archive system
  - zero-headroom `docs/TASKS.md` states are rejected or repaired, not treated as healthy
  - focused tests cover the intended near-full and `linear-*` archive paths
- Guardrails / Error Budgets:
  - preserve the existing `docs/TASKS-archive-2026.md` path and task-archives branch
  - preserve current inline snapshot truth and `linear-*` key handling
  - keep the fix narrow to tasks archive/headroom behavior

## User Experience
- Personas:
  - contributor opening a routine docs-first lane
  - maintainer watching `main` for recurring docs/TASKS pressure
- User Journeys:
  - a normal packet can add its mirror without inheriting a permanently full base file
  - once reserve is exhausted, the repo-supported archive path trims back to the documented target
  - zero-headroom states are surfaced immediately instead of lingering silently

## Technical Considerations
- Architectural Notes:
  - `scripts/tasks-archive.mjs` is already the supported archive seam and should remain so
  - `scripts/docs-hygiene.ts` owns the repo-facing docs check signal and should become truthful about zero headroom
  - `docs/tasks-archive-policy.json` should become the single place that documents the reserve target
- Dependencies / Integrations:
  - `scripts/tasks-archive.mjs`
  - `scripts/docs-hygiene.ts`
  - `.github/workflows/tasks-archive-automation.yml`
  - `docs/tasks-archive-policy.json`
  - `tests/tasks-archive.spec.ts`
  - `tests/docs-hygiene.spec.ts`

## Open Questions
- What reserve size is the smallest useful default that restores non-zero headroom without over-archiving?
- Should `docs:check` fail exactly at zero headroom, or at the reserve boundary itself?

## Approvals
- Reviewer: pending parent docs-review and implementation validation
- Date: 2026-04-18
