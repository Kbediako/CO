# PRD - CO: Stabilize timeout-sensitive repo validation suites blocking handoff

## Added by Bootstrap 2026-04-13

## Traceability
- Linear issue: `CO-131` / `6ed6ef11-538e-48f0-936c-8547632bf92e`
- Linear URL: https://linear.app/asabeko/issue/CO-131/co-stabilize-timeout-sensitive-repo-validation-suites-blocking-handoff
- Source issue: `CO-89` / `70e83230-4f52-4850-b494-41fd0ec32f13`
- Related historical packets reviewed for context only:
  - `CO-46` / `linear-56067b9f-2aef-4033-8e12-08ac89bd9834`
  - `CO-84` / `linear-74d6e549-46cc-46be-9a61-76ae61b014f4`
  - `CO-130` / `linear-b792b517-98e4-4518-9fb2-860996dd62c8`

## Summary
- Problem Statement: the guarded resident-session seam from `CO-89` is not the current blocker, but the repo validation floor used for worker handoff is still unreliable because `npm run test` reportedly times out in unrelated suites. The issue specifically names `tests/diff-budget.spec.ts`, `tests/subagent-edit-guard.spec.ts`, `tests/cli-frontend-test.spec.ts`, and `orchestrator/tests/CodexOrchestratorCli.test.ts`, with isolated evidence already showing a split between one clean repro (`diff-budget`) and one still-live isolated timeout (`subagent-edit-guard`).
- Desired Outcome: make the repo validation floor trustworthy again by reproducing the named timeout-sensitive suites on the current head, fixing any real harness or timing defect with the smallest truthful patch, and clearly separating unrelated validation-floor work from the `CO-89` resident-session seam.

## Execution Update 2026-04-13
- Fresh current-head evidence on branch `linear/co-131-stabilize-validation-timeouts` at `b66ca7778` shows `CO-131` is a non-repro for the named timeout cluster: all four isolated suites passed, and final `npm run test` finished `335/335` files with `3703/3703` tests passing in `133.61s`.
- Because no live timeout blocker remained, the only shipped code change stayed packet-local and truthfulness-oriented: `scripts/tasks-archive.mjs` now recognizes `linear-...` snapshot keys, `tests/tasks-archive.spec.ts` covers that archive path, and `docs/TASKS-archive-2026.md` restores the dropped `1015` snapshot.
- This closeout remains explicitly separate from `CO-89`; no resident-session runtime seam was reopened.

## User Request Translation (Context Anchor)
- User intent / needs (in your own words): complete `CO-131` in the current provider-worker workspace by restoring deterministic full-suite validation for handoff, using the exact Linear worker workflow, keeping a single workpad current, and refusing shortcuts such as broad timeout inflation or reopening `CO-89`.
- Success criteria / acceptance:
  - the exact named timeout-sensitive suites are reproduced and classified with machine-checkable evidence
  - `npm run test` reaches a trustworthy terminal result again on the standard repo lane, or any remaining failure is split into an explicit separately tracked blocker with bounded evidence
  - the final closeout explains what was unrelated to `CO-89` and what changed to restore validation trust
- Constraints / non-goals:
  - do not reopen or weaken the guarded resident-session seam from `CO-89`
  - do not bypass the repo validation floor by deleting or skipping tests
  - do not treat a higher global timeout or partial run as an acceptable steady-state fix

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `stabilize timeout-sensitive repo validation suites blocking handoff`
  - `keep validation behavior truthful and deterministic rather than papering over timeouts`
  - `not changing the CO-89 resident-session seam`
- Protected terms / exact artifact and surface names:
  - `npm run test`
  - `tests/diff-budget.spec.ts`
  - `tests/subagent-edit-guard.spec.ts`
  - `tests/cli-frontend-test.spec.ts`
  - `orchestrator/tests/CodexOrchestratorCli.test.ts`
  - `flags overlapping scope edits as ownership collisions`
- Nearby wrong interpretations to reject:
  - `fix CO-89 by weakening or skipping npm run test`
  - `treat all timeout failures as harmless host noise without investigation`
  - `use this as a pretext for a broad Vitest migration or runtime redesign`

## Parity / Alignment Matrix
- Not applicable beyond validation-floor truthfulness.
- Current truth:
  - issue evidence from the `CO-89` validation lane reports repo-floor failures in `tests/diff-budget.spec.ts`, `tests/subagent-edit-guard.spec.ts`, `tests/cli-frontend-test.spec.ts`, and `orchestrator/tests/CodexOrchestratorCli.test.ts`
  - issue evidence already says `tests/diff-budget.spec.ts` passed in isolation while `tests/subagent-edit-guard.spec.ts` still timed out in isolation on `flags overlapping scope edits as ownership collisions`
  - a different historical timeout packet (`CO-130`) closed as a no-code non-repro on a previous head, so it is context only and not proof that `CO-131` is already resolved on current `b66ca7778`
- Reference truth:
  - provider-worker review handoff requires a truthful repo-wide validation story
  - `CO-89`'s resident-session implementation remains separately owned and already has focused green evidence
- Target truth / intended delta:
  - the named timeout-sensitive suites have exact reproduction and classification evidence on the current head
  - the current repo head regains a trustworthy `npm run test` closeout path, or any residual blocker is explicitly split and tracked without weakening the validation contract
- Explicitly out-of-scope differences:
  - changing the `CO-89` resident-session seam
  - adding broad timeout inflation without root-cause evidence
  - general-purpose test-framework rearchitecture

## Not Done If
- `npm run test` still fails on the same timeout-sensitive suites without a bounded root-cause explanation.
- The only mitigation is to raise timeouts globally or otherwise weaken the validation contract.
- The closeout still leaves reviewers unable to distinguish unrelated repo validation blockers from the `CO-89` seam.

## Goals
- Reproduce the named timeout-sensitive suites and the current full-suite lane with exact commands.
- Identify whether the live blocker is a shared harness/process leak, a suite-local timing assumption, or historical non-repro evidence.
- Land the smallest truthful repair or blocker split needed to restore validation trust.

## Non-Goals
- Reopening `CO-89` resident-session behavior.
- Broad refactors across unrelated CLI/runtime surfaces unless a named failing suite proves they are required.
- Review/merge workflow changes that do not materially affect the validation-floor blocker.

## Stakeholders
- Product: CO operators and reviewers who rely on deterministic handoff gates.
- Engineering: CLI/runtime and test-harness maintainers responsible for repo-floor truthfulness.
- Design: Not applicable.

## Metrics & Guardrails
- Primary Success Metrics:
  - focused reproductions for the named suites are captured with terminal outcomes
  - `npm run test` regains a truthful terminal result, or remaining blockers are split explicitly with evidence
  - any code diff stays bounded to the actual validation seam rather than `CO-89`
- Guardrails / Error Budgets:
  - no false-green full-suite claims
  - no blanket timeout inflation without root-cause proof
  - no scope drift into unrelated runtime or docs work except explicit follow-ups

## User Experience
- Personas:
  - provider-worker operators closing out validation-heavy issues
  - maintainers diagnosing whether current failures are suite-local, harness-wide, or historical noise
- User Journeys:
  - a worker reruns the named suites and `npm run test`, sees a terminal result, and can hand off with confidence
  - if a blocker remains, the worker can point to an exact new follow-up rather than implying `CO-89` is still broken

## Technical Considerations
- Architectural Notes:
  - start from reproduction-first narrowing because prior packets already show similar timeout classes can flip between live defect and non-repro on adjacent heads
  - prefer the smallest shared harness fix if multiple named suites prove the same root cause
  - prefer a scoped blocker split over speculative repo-wide changes when the remaining issue is genuinely independent
- Dependencies / Integrations:
  - `package.json`
  - `vitest.config.core.ts`
  - `tests/diff-budget.spec.ts`
  - `tests/subagent-edit-guard.spec.ts`
  - `tests/cli-frontend-test.spec.ts`
  - `orchestrator/tests/CodexOrchestratorCli.test.ts`
  - Linear workpad and provider-worker handoff surfaces

## Open Questions
- Does the current head still reproduce the exact isolated `subagent-edit-guard` timeout reported in the issue, or has the blocker shifted again since the issue was filed?
  - Resolved: no live isolated timeout remained on the updated tree; `npx vitest run --config vitest.config.core.ts tests/subagent-edit-guard.spec.ts` passed `12/12` tests in `6.42s`.
- If the full suite still times out, is the owner a shared process-leak/worker-pool seam or simply accumulated long-tail runtime from a small set of heavy suites?
  - Resolved: no current-head full-suite timeout blocker remained; final `npm run test` finished cleanly in `133.61s`.

## Approvals
- Product: Self-approved for docs-first investigation start against the Linear issue contract.
- Engineering: Audited docs-review completed with `review_outcome=clean-success`, final validation is green, standalone review completed with `review_outcome=clean-success`, and the explicit elegance pass is recorded. PR create/attach and review handoff remain pending.
- Design: Not applicable.
