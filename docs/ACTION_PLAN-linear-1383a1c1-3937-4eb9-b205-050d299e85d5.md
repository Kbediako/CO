# ACTION_PLAN - CO-391 installed control-host timeout and stale terminal suppression

## Readiness Gate
Preserve `5cc7be319`, `co-status --format json`, `ui_request_timeout`, `active_worker_probe_timeout_quarantine`, `CO-381`, and `provider_issue_merge_closeout_action_required`; not done if terminal rows remain active/running, status exceeds attach timeout, or supervisor remains quarantined. Baseline confirms stale installed runtime plus a source active-classification gap, not a CO-376 source regression.

## Milestones
1. Capture baseline installed version, LaunchAgent path, supervisor quarantine, direct status timing, and active IDs.
2. Add docs packet and focused provider intake/status/high-volume discovery tests.
3. Implement terminal handoff suppression and bounded high-volume discovery while preserving queued retries and active non-terminal handoff failures.
4. Run gates, install current source globally, restart LaunchAgent, and capture after proof.
5. Refresh workpad, open PR, drain automated review/checks, attach PR, and hand off to review.

## Validation
Focused Vitest plus full `build`, `lint`, `test`, docs, stewardship, pack smoke, standalone review, elegance pass, and runtime proof with no `degraded_read`, no CO-381 active/running/debug projection, and stable supervisor restart count.
