# PRD - CO-445 provider closeout live-owner guard

Traceability: Linear `CO-445`; task `linear-345cd50c-4962-4376-a7ca-1982ba311699`; spec `tasks/specs/linear-345cd50c-4962-4376-a7ca-1982ba311699.md`; child lane `.runs/linear-345cd50c-4962-4376-a7ca-1982ba311699-co445-docs-packet-r2/cli/2026-04-30T09-00-50-146Z-4d8a155f/manifest.json`.

## Contract
`provider merge closeout` must not finish the `provider closeout lifecycle` by moving an issue to `Done` after a `metadata PR merge` while `origin/main` still identifies it as a `docs:freshness:maintain` `owner_issue` or cohort canonical owner. Preserve protected evidence terms: `CO-444`, `configured_owner_terminal`, `block_unowned_repo_debt`, `blocking_changed_paths=[]`, `pass_with_owned_rolling_debt`, `live owner`, `canonical-owner automation`, and `providerMergeCloseout.ts`.

Reject owner re-home as the answer, docs freshness weakening, hidden terminal-owner evidence, treating `blocking_changed_paths=[]` as clean when the owner is terminal, or silently folding this provider lifecycle fix into CO-431.

## Matrix
| Surface | Current | Reference | Target |
| --- | --- | --- | --- |
| CO-444 closeout | Merged PR then `Done` while still owner. | Owned rolling debt needs live same-project owner. | Hold/move non-terminal while owner-bearing. |
| Provider closeout | Terminal transition follows merge/shared-root closeout. | Non-owner issues still reach `Done`. | Branch on live owner evidence before `Done`. |
| CO-431 | Adjacent canonical-owner automation. | May later re-home/retire metadata. | Related only; CO-445 owns this guard. |

## Acceptance
Live-owner closeout returns action-required/non-terminal evidence instead of `Done`; re-homed or retired ownership keeps normal `Done`; evidence preserves `policy_owner_issue`, `owner_issue`, `owner_issue_action`, `owner_issue_verification`, `candidate_cohorts`, `freshness_decision`, and `blocking_changed_paths`; regressions cover live-owner, no-longer-owner, canonical cohort owner, and observability paths.
