# ACTION_PLAN - CO: Restore baseline doctor and stage-set tests blocking review handoff

## Added by Bootstrap (refresh as needed)

## Traceability
- Linear issue: `CO-47` / `9553982c-6360-4c9c-83a4-674702701158`
- Linear URL: https://linear.app/asabeko/issue/CO-47/co-restore-baseline-doctor-and-stage-set-tests-blocking-review-handoff

## Summary
- Goal: finish Linear issue `CO-47` by restoring truthful clean-main passing coverage for the three named baseline orchestrator suites that are currently blocking provider-worker review handoff.
- Scope: docs-first packet, docs-review child stream, targeted reproduction, minimal doctor/pipeline-resolver/user-config fix, focused validation, and the required review/elegance gate before any handoff.
- Assumptions:
  - the current workspace commit matches the issue’s clean-main reproduction baseline
  - the blocker is confined to the three named suites and their direct implementation seams
  - a minimal fix should be sufficient to unblock `CO-44` review handoff without widening into unrelated orchestrator cleanup

## Milestones & Sequencing
1) Register the CO-47 docs-first packet, mirror the checklist, update `tasks/index.json`, update `docs/TASKS.md`, and keep the Linear workpad aligned.
2) Run an audited `linear child-stream --pipeline docs-review` lane and record its manifest before implementation.
3) Reproduce the three targeted failures with the exact issue command and inspect the failing doctor/pipeline-resolver/user-config seams.
4) Patch the smallest truthful fix, rerun focused suite coverage, and refresh the workpad with the updated status.
5) If the diff is non-trivial, run standalone review and an explicit elegance pass before PR/review handoff.

## Dependencies
- `orchestrator/tests/Doctor.test.ts`
- `orchestrator/tests/PipelineResolverEnvOverrides.test.ts`
- `orchestrator/tests/UserConfigStageSets.test.ts`
- the corresponding doctor, pipeline resolver, and user config implementation files those suites cover

## Validation
- Checks / tests:
  - `node "/Users/kbediako/Code/CO/dist/bin/codex-orchestrator.js" linear child-stream --pipeline docs-review --issue-id 9553982c-6360-4c9c-83a4-674702701158 --format json`
  - `npm run test:orchestrator -- Doctor.test.ts PipelineResolverEnvOverrides.test.ts UserConfigStageSets.test.ts`
  - focused reruns of each touched suite after the fix
  - `npm run review` plus an explicit elegance/minimality pass before review handoff if the diff is non-trivial
- Rollback plan:
  - revert the minimal doctor/pipeline-resolver/user-config patch if it changes current runtime truth rather than restoring it
  - keep the issue in an active state until the blocker is fixed or a concrete residual blocker is documented

## Risks & Mitigations
- Risk: the failures may reflect intentional runtime changes and not a single code bug.
  - Mitigation: reproduce first, then update expectations only when code inspection confirms the runtime truth.
- Risk: the three suites may share a hidden ambient-config dependency that makes the failures look larger than they are.
  - Mitigation: keep reproduction on the clean-main-based branch and rerun suites both together and individually after the fix.
- Risk: review-handoff expectations could drift if the lane reports only focused validation.
  - Mitigation: keep the workpad explicit about what was rerun and run review/elegance gates before handoff.

## Approvals
- Reviewer: codex-orchestrator docs-review with manual fallback after stalled forced review
- Date: 2026-03-30
