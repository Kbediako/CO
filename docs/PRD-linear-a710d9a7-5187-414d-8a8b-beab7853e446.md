# PRD - CO workflow: restore Apr 18 docs:freshness baseline for Mar 18 stale cohort

## Added by Bounded Docs Child Lane 2026-04-18

## Traceability
- Linear issue: `CO-239` / `a710d9a7-5187-414d-8a8b-beab7853e446`
- Linear URL: https://linear.app/asabeko/issue/CO-239/co-workflow-restore-apr-18-docsfreshness-baseline-for-mar-18-stale
- Task id: `linear-a710d9a7-5187-414d-8a8b-beab7853e446`
- Canonical spec: `tasks/specs/linear-a710d9a7-5187-414d-8a8b-beab7853e446.md`
- Docs packet child lane manifest: `.runs/linear-a710d9a7-5187-414d-8a8b-beab7853e446-docs-packet/cli/2026-04-18T04-39-54-760Z-8c9db8b8/manifest.json`
- Source anchor: `ctx:sha256:0d82ad4fdf8297fae69d0937a0588fb2976cb0c927eb468ec132a5ff9c789318#chunk:c000001`
- Source payload path: `.runs/linear-a710d9a7-5187-414d-8a8b-beab7853e446-docs-packet/cli/2026-04-18T04-39-54-760Z-8c9db8b8/memory/source-0/source.txt`
- Source payload note: the shared `source-0` payload for this child lane is run metadata only. The exact CO-239 wording and live Apr 18 counts below are preserved from the parent-owned read-only issue cache at `/Users/kbediako/Code/CO/.runs/linear-a710d9a7-5187-414d-8a8b-beab7853e446/cli/2026-04-18T04-35-39-294Z-290af894/provider-linear-issue-context-cache.json`.

## Summary
- Problem Statement: clean current `main` is red on `npm run docs:freshness` again as of 2026-04-18, but the blocker is not the existing CO-175-owned Mar 14 rolling freshness cohort. Live verification reports `70` blocking stale rows, `221` still-visible rolling cohort entries owned by `CO-175`, and `0` missing or invalid registry drift. The new blocking rows are all `last_review=2026-03-18`, belong to one coherent historical packet family (`1289-1298`), and break down as `Task Packet=50`, `Task Mirror=10`, and `Report Only=10`.
- Desired Outcome: restore a truthful green Apr 18 docs:freshness baseline for unrelated clean diffs by giving the Mar 18 `1289-1298` cohort an explicit reviewed disposition through review/refresh, archive, reclassification, or another explicit owner action, without weakening `docs:freshness`, without hiding the `221` CO-175 rolling rows, and without reopening `CO-175`.

## User Request Translation (Context Anchor)
- User intent / needs (in your own words): create the CO-239 docs-first packet only. The packet must preserve the Apr 18 / Mar 18 baseline-repair scope and protected terms so the parent lane can reproduce, classify, and repair or explicitly re-own the Mar 18 cohort without touching unrelated product work or weakening the freshness policy.
- Success criteria / acceptance:
  - the six packet files exist for `linear-a710d9a7-5187-414d-8a8b-beab7853e446` and preserve the Apr 18 / Mar 18 issue checksum
  - the packet records the live blocker shape: `70` blocking stale rows, `221` CO-175 rolling rows, classes `Task Packet`, `Task Mirror`, and `Report Only`, and lineage `1289-1298`
  - the packet makes the parent-owned implementation path explicit: reproduce, classify, choose a disposition, restore a truthful green `docs:freshness` path, and update the owner evidence
  - the packet rejects blind `last_review` bumps, policy-cap changes, or reopening `CO-175`
- Constraints / non-goals:
  - child lane owns only these six files: `docs/PRD-*`, `docs/TECH_SPEC-*`, `docs/ACTION_PLAN-*`, `tasks/specs/*`, `tasks/tasks-*`, and `.agent/task/*`
  - parent lane owns `tasks/index.json`, `docs/TASKS.md`, `docs/docs-freshness-registry.json`, `docs/guides/docs-freshness-cohorts.md`, `out/` artifacts, validation, workpad state, Linear state, PR lifecycle, and merge
  - this child lane must not broaden into implementation, registry edits, cohort-guide edits, or full validation

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `docs:freshness`
  - `CO-175`
  - `rolling freshness cohort`
  - `repo-wide freshness debt`
  - `per-PR diff health`
  - `Mar 18 cohort`
  - `Task Packet`
  - `Task Mirror`
  - `Report Only`
  - `machine-checkable evidence`
- Protected terms / exact artifact and surface names:
  - `docs:freshness`
  - `docs:freshness:maintain`
  - `spec-guard`
  - `last_review=2026-03-18`
  - `1289-1298`
  - `70` blocking stale rows
  - `221` rolling cohort entries
  - `docs/docs-freshness-registry.json`
  - `docs/guides/docs-freshness-cohorts.md`
  - `tasks/index.json`
  - `docs/TASKS.md`
  - `MCP_RUNNER_TASK_ID=verify-co-175 npm run docs:freshness`
- Nearby wrong interpretations to reject:
  - reopen or rewrite `CO-175`
  - hide the `221` CO-175 rolling rows
  - expand the rolling window, row cap, or cohort cap to make Apr 18 pass
  - solve the blocker with a blind `last_review` bump across the Mar 18 cohort
  - treat this as active feature-lane drift in unrelated issues
  - weaken `docs:freshness`, `docs:freshness:maintain`, `spec-guard`, or provider-worker handoff policy
  - broaden this lane into unrelated historical cleanup beyond the blocking Mar 18 cohort

## Parity / Alignment Matrix

| Surface | Current Truth | Reference Truth | Target Truth | Explicitly Out Of Scope |
| --- | --- | --- | --- | --- |
| `docs:freshness` baseline | Clean current `main` fails with `70` blocking stale rows plus `221` visible CO-175 rolling rows. | After the Apr 17 repair (`CO-209`), the repo returned to `0` blocking stale rows while CO-175 rolling debt stayed visible. | Unrelated clean diffs return to `0` blocking stale rows while CO-175 rolling debt remains visible and explicit. | Weakening the gate or hiding repo-wide freshness debt. |
| Cohort ownership | The new blocking rows are all `last_review=2026-03-18` and have no Apr 18 owner packet yet. | `CO-175`, `CO-184`, `CO-201`, and `CO-209` each carried explicit ownership and evidence for their respective rollover cohorts. | `CO-239` becomes the explicit Apr 18 owner for the Mar 18 `1289-1298` packet family. | Reopening already-closed CO-175 implementation or silently folding this cohort back into it. |
| Cohort shape | Historical packet family only: `Task Packet=50`, `Task Mirror=10`, `Report Only=10`, path families `10` each, lineage `1289-1298`. | Historical packet debt should be reviewed and classified by class, path family, lineage, and disposition. | Parent produces a classification artifact and picks the smallest truthful disposition for the Mar 18 cohort. | Blind review-date bump without class/path/lineage review. |
| Policy posture | Repo-wide freshness debt is blocking current validation while per-PR diff health is otherwise clean. | CO docs freshness policy separates `repo-wide freshness debt` from `per-PR diff health` and leaves rolling debt visible. | Apr 18 closeout preserves that distinction and leaves any owned residual debt machine-checkable. | Cap or window changes that dilute policy truth. |

## Not Done If
- `npm run docs:freshness` still fails on current `main` because of the Mar 18 `1289-1298` cohort with no explicit reviewed owner action.
- The fix depends on broadening CO-175 rolling eligibility or changing caps or windows instead of reviewing the blocking cohort.
- The resulting closeout hides CO-175 rolling debt or collapses repo-wide maintenance into unrelated feature-lane validation.
- Before and after artifacts do not capture exact stale counts, path-family breakdowns, and task-lineage ownership for the Apr 18 blocker.

## Goals
- Register the CO-239 docs-first packet and preserve the issue-shaping contract before implementation.
- Keep the live Apr 18 blocker shape explicit and machine-checkable.
- Give the parent lane a bounded path to classify and resolve or explicitly re-own the Mar 18 cohort.
- Preserve the distinction between `repo-wide freshness debt` and `per-PR diff health`.

## Non-Goals
- No reopening or redesign of `CO-175`.
- No cap or window changes for rolling freshness cohorts.
- No warning-only downgrade or other weakening of `docs:freshness`.
- No unrelated historical cleanup beyond the Mar 18 blocking cohort unless the parent files a separate follow-up.
- No implementation, registry, or validation work in this child lane.

## Stakeholders
- Product: CO maintainers who need unrelated feature lanes to stop inheriting repo-wide freshness debt.
- Engineering: docs freshness, docs maintenance policy, registry, and review-handoff maintainers.
- Design: N/A.

## Metrics & Guardrails
- Primary Success Metrics:
  - before and after artifacts record the Apr 18 blocker shape and final disposition
  - `docs:freshness` returns to `0` blocking stale rows for unrelated clean diffs
  - the `221` CO-175 rolling rows remain visible while still in window
  - the Mar 18 cohort owner and disposition stay machine-checkable
- Guardrails / Error Budgets:
  - no blind `last_review` bump
  - no policy-cap or window changes
  - no hidden collapse of `repo-wide freshness debt` into unrelated feature validation
  - no reopening CO-175 without fresh explicit evidence

## User Experience
- Personas:
  - maintainer triaging a date-boundary docs:freshness regression
  - feature-lane owner blocked by repo-wide historical packet debt
  - reviewer checking whether residual debt is still explicit and correctly owned
- User Journeys:
  - maintainer opens CO-239 and immediately sees the exact Apr 18 blocker shape
  - parent lane reproduces the issue, classifies the Mar 18 cohort, and picks the smallest truthful action
  - future clean diffs can cite the correct Apr 18 baseline owner instead of widening unrelated scopes

## Technical Considerations
- Architectural Notes:
  - the current issue body already confirms the blocker is historical packet debt, not missing registry rows, missing-on-disk docs, invalid entries, or uncatalogued docs
  - the parent should prefer the existing docs freshness machinery and owner-evidence surfaces over new policy or workflow abstractions
  - the parent should keep the CO-175 Mar 14 rolling cohort visible and unchanged while handling the Mar 18 cohort separately
- Dependencies / Integrations:
  - `docs/docs-freshness-registry.json`
  - `docs/guides/docs-freshness-cohorts.md`
  - `tasks/index.json`
  - `docs/TASKS.md`
  - `scripts/docs-freshness.mjs`
  - `scripts/docs-freshness-maintain.mjs`
  - `scripts/spec-guard.mjs`
  - prior owner lanes `CO-175`, `CO-184`, `CO-201`, and `CO-209`

## Open Questions
- Is the smallest truthful action for the Mar 18 cohort a reviewed refresh of all `70` rows, or is there a smaller archive or reclassification split for some path families?
- After the Mar 18 cohort is handled, do any additional stale historical packets remain outside the declared CO-175 rolling cohort that should become a separate owner issue?

## Approvals
- Product: pending parent acceptance of the docs-first packet.
- Engineering: pending parent docs-review, implementation, and validation.
- Design: N/A.
