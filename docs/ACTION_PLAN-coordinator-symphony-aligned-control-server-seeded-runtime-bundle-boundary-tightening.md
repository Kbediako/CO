# ACTION_PLAN - Coordinator Symphony-Aligned Control Server Seeded Runtime Bundle Boundary Tightening

1. Register `1087` docs-first from the `1086` next-slice note plus bounded runtime-surface inspection.
2. Add one neutral owner for the linear advisory state filename constant and repoint the seed loader plus seeded-runtime assembly to it.
3. Tighten the seeded-runtime assembly return contract around one shared runtime bundle.
4. Update `ControlServer` to consume the tighter runtime bundle without changing startup behavior.
5. Refresh focused seeded-runtime/startup regressions.
6. Run the full validation + closeout bundle, capture the elegance review, and sync the task mirrors.
