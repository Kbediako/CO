# ACTION PLAN - CO-465 Doctor gpt-5.5 defaults full-suite validation gate

## Summary
- Goal: give the parent lane a docs-first packet for the `orchestrator/tests/Doctor.test.ts` full-suite timeout that blocks the provider-worker validation gate while preserving `gpt-5.5 defaults` policy coverage.
- Scope: docs packet, task mirrors, `tasks/index.json`, `docs/TASKS.md`, and `docs/docs-freshness-registry.json` only.
- Assumptions:
  - the parent-provided source anchor and protected terms are authoritative
  - the source payload path is unavailable in this child checkout
  - parent owns source inspection, tests, docs-review, validation, Linear/GitHub lifecycle, workpad, PR, and patch integration

## Issue Readiness Gate
- Intent checksum / protected terms carried forward:
  - `npm run test`
  - `orchestrator/tests/Doctor.test.ts`
  - `accepts unmarked gpt-5.5 defaults when access is verified`
  - `gpt-5.5 defaults`
  - `full-suite timeout`
  - `isolated rerun passes`
  - `provider-worker validation gate`
  - `CO-456`
- Not done if:
  - `npm run test` still times out in the same Doctor full-suite path
  - the isolated rerun is treated as sufficient while full-suite validation fails
  - the Doctor case `accepts unmarked gpt-5.5 defaults when access is verified` is skipped, deleted, or weakened
  - parent changes `CO-456` spec-guard behavior
  - parent recommends or lands only a blanket global timeout increase without explaining the full-suite-only slow path
- Pre-implementation issue-quality review:
  - 2026-05-02: approved for docs packet bootstrap. The lane is not suitable for the micro-task path because correctness depends on exact protected terms, validation-gate traceability, and rejected interpretations.

## Milestones & Sequencing
1. Create the CO-465 PRD, TECH_SPEC mirror, canonical task spec, ACTION_PLAN, task checklist, and `.agent` mirror.
2. Register task id `20260502-linear-b7381274-d6eb-4886-86f4-3976b154c26d` in `tasks/index.json`.
3. Add docs freshness rows for all six packet/mirror files.
4. Add a current CO-465 snapshot to `docs/TASKS.md`.
5. Validate protected terms, JSON parsing, markdown diff hygiene, and scoped changed files.
6. Leave child-lane changes uncommitted for parent patch export.

## Parent-Owned Follow-On Plan
1. Reproduce the `npm run test` full-suite timeout in `orchestrator/tests/Doctor.test.ts` or classify why current head differs. Status: complete via `/tmp/co456-npm-test.log` plus current focused pre-fix pass.
2. Rerun the focused Doctor surface and preserve the isolated pass for `accepts unmarked gpt-5.5 defaults when access is verified`. Status: complete before and after patch.
3. Compare full-suite and isolated behavior to identify the full-suite-only slow path. Status: complete; ambient real Codex CLI feature/cloud probes were left unisolated in model-default tests that otherwise mocked debug-model access.
4. Apply the smallest bounded fix in Doctor test setup, access-verification, or proven adjacent helper code. Status: complete in `orchestrator/tests/Doctor.test.ts` by scoping the existing fake Codex binary to the model-default tests.
5. Avoid changing `CO-456` spec-guard behavior. Status: complete; no spec-guard files changed.
6. Avoid weakening the Doctor model-default policy assertion or skipping `orchestrator/tests/Doctor.test.ts`. Status: complete; assertions and test remain active.
7. Run final provider-worker validation and review gates. Status: `npm run test` complete; review gates pending parent handoff.
8. Record the full-suite and isolated rerun evidence in task mirrors, workpad, PR, and review handoff. Status: task mirrors updated; workpad/PR/review handoff pending parent lifecycle.

## Dependencies
- Source anchor `ctx:sha256:16b4be52eda49eabac589fc41aca4b843e4a04597237ee4143aa87a4ae0c4603#chunk:c000001`.
- Parent manifest `.runs/linear-b7381274-d6eb-4886-86f4-3976b154c26d-co465-docs-packet/cli/2026-05-01T19-14-31-418Z-9a2afec6/manifest.json`.
- `orchestrator/tests/Doctor.test.ts`.
- Provider-worker validation gate via `npm run test`.
- Doctor model-default access verification for `gpt-5.5 defaults`.
- `CO-456` spec-guard behavior as an explicit non-owned boundary.

## Validation
- Checks / tests:
  - protected-term scan across packet files
  - JSON parse for `tasks/index.json` and `docs/docs-freshness-registry.json`
  - scoped `git diff --check --` over declared files
  - scoped changed-file review to confirm no out-of-scope edits
- Parent-owned validation:
  - `npm run test`
  - focused Doctor rerun for `orchestrator/tests/Doctor.test.ts`
  - focused preservation of `accepts unmarked gpt-5.5 defaults when access is verified`
  - docs-review and normal provider-worker handoff gates after implementation
- Rollback plan: because this lane is docs-only, parent can reject or revert only this packet plus registry rows before integration.

## Risks & Mitigations
- Risk: isolated rerun success is over-promoted.
  - Mitigation: action plan requires full-suite validation or explicit parent-owned blocker classification.
- Risk: policy coverage is weakened to clear timeout pressure.
  - Mitigation: protected terms and Not Done If keep the `gpt-5.5 defaults` assertion non-negotiable.
- Risk: scope drifts into CO-456.
  - Mitigation: spec-guard behavior remains explicitly out of scope.
- Risk: a timeout-only fix hides the root cause.
  - Mitigation: parent must explain the full-suite-only slow path before relying on timeout adjustments.

## Approvals
- Docs packet child lane: produced in this workspace for parent patch export.
- Parent docs-review, implementation, and lifecycle handoff: pending parent ownership.
- Date: 2026-05-02
