# ACTION_PLAN: Coordinator Symphony-Aligned Standalone Review Shell Command Parser Primitives Extraction

## Objective

Open the next truthful standalone-review follow-on after `1210` by extracting the shared shell-command parser primitives now reused by multiple `review-execution-state` analyzers.

## Steps

1. Register `1211` docs-first with explicit evidence that the shared shell-command parser cluster remains inline inside `scripts/lib/review-execution-state.ts`.
2. Extract the bounded parser helper while preserving current policy ownership in `scripts/lib/review-execution-state.ts`.
3. Add only the focused regressions needed to pin shell-control segmentation, launcher unwrap, and payload parsing behavior.
4. Run the lane validations and record any truthful review overrides.
5. Sync closeout mirrors and reassess whether the next truthful follow-on stays inside `review-execution-state` or becomes a separate policy-family lane.
