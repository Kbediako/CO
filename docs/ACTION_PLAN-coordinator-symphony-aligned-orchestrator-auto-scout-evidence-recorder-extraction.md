# ACTION_PLAN: Coordinator Symphony-Aligned Orchestrator Auto-Scout Evidence Recorder Extraction

- Date: 2026-03-14
- Owner: Codex (top-level agent)
- Task: `1167-coordinator-symphony-aligned-orchestrator-auto-scout-evidence-recorder-extraction`

## Plan

1. Register the docs-first packet for the auto-scout evidence recorder seam.
2. Extract the class-local `runAutoScout(...)` cluster into one adjacent helper/service without changing router ownership.
3. Add focused helper coverage for `recorded`, `timeout`, and `error`.
4. Retain orchestration integration coverage for the cloud/non-trivial auto-scout path.
5. Run the deterministic gate bundle, bounded review, and elegance pass on the final tree.
