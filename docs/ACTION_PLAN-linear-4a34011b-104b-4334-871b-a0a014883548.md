# ACTION_PLAN - CO STATUS / observability: stop direct `co-status --format json` fetch refusals on healthy control-hosts

## Added by Docs Child Lane 2026-04-18

## Summary
- Goal: give the parent lane a bounded implementation plan for intermittent direct `co-status --format json` `ECONNREFUSED` failures on healthy control-hosts.
- Scope: docs-first packet, `tasks/index.json` registration, parent-owned direct JSON endpoint-recovery implementation, and parent-owned focused validation.
- Assumptions:
  - the exact issue wording is preserved from the parent worker's cached `CO-246` issue body
  - the smallest correct fix stays at the CLI shell / attach recovery seam
  - attach-style recovery is the intended alignment reference for the direct JSON path

## Issue Readiness Gate
- Intent checksum / protected terms carried forward:
  - `co-status --format json`
  - `ECONNREFUSED`
  - healthy control-host
  - direct JSON snapshot path
  - endpoint re-resolution
  - attach-style recovery
  - intermittent dashboard-read refusal
- Not done if:
  - direct JSON still fails on the first stale/dead endpoint read while healthy-host evidence remains available
  - the repair hides genuine auth/network/timeout failures
  - the lane drifts into `CO-243`, `CO-223`, `CO-41`, `CO-194`, or `CO-211`
- Pre-implementation issue-quality review:
  - 2026-04-18: child-lane review confirms the issue is a bounded direct-JSON recovery seam, not a truth-semantic rewrite and not a generic host-failure lane.

## Milestones & Sequencing
1. Create the docs-first packet and `tasks/index.json` registration for `CO-246`.
2. Parent reproduces the stale-endpoint / dead-port direct JSON seam while healthy control-host evidence remains available.
3. Parent inspects `orchestrator/src/cli/coStatusCliShell.ts` and `orchestrator/src/cli/coStatusAttachCliShell.ts` to confirm the narrowest truthful reuse or mirroring of attach-style recovery.
4. Parent implements bounded direct JSON endpoint re-resolution or equivalent truthful recovery.
5. Parent adds focused regression coverage in `orchestrator/tests/CoStatusCliShell.test.ts` and updates `orchestrator/tests/CoStatusAttachCliShell.test.ts` only if shared recovery behavior changes.
6. Parent runs docs-review before implementation, then completes focused validation, standalone review, and elegance review as required for the non-trivial implementation diff.
7. Parent refreshes the workpad and continues normal PR handoff once implementation and validation are clean.

## Dependencies
- Shared source anchor: `ctx:sha256:ed8d7cee28dd1ae41d954a39ca3c8ec761012ade0011d04e9e673f3abb5fa86a#chunk:c000001`
- Parent issue-context evidence: `.runs/linear-4a34011b-104b-4334-871b-a0a014883548/cli/2026-04-18T08-35-23-027Z-c520a178/provider-linear-issue-context-cache.json`
- Parent workpad plan already names the intended implementation seam:
  - `orchestrator/src/cli/coStatusCliShell.ts`
  - `orchestrator/src/cli/coStatusAttachCliShell.ts`
- Parent focused tests already named in the workpad plan:
  - `orchestrator/tests/CoStatusCliShell.test.ts`
  - `orchestrator/tests/CoStatusAttachCliShell.test.ts`

## Validation
- Child lane only:
  - `python3 - <<'PY'\nimport json, pathlib\njson.loads(pathlib.Path('tasks/index.json').read_text())\nPY`
  - `rg -n "co-status --format json|ECONNREFUSED|healthy control-host|direct JSON snapshot path|endpoint re-resolution|attach-style recovery|intermittent dashboard-read refusal|CO-243|CO-223|CO-41|CO-194|CO-211|coStatusCliShell.ts|coStatusAttachCliShell.ts|readUiDatasetWithEndpointRecovery|resolveAttachTarget|fetchUiDataset" docs/PRD-linear-4a34011b-104b-4334-871b-a0a014883548.md docs/TECH_SPEC-linear-4a34011b-104b-4334-871b-a0a014883548.md docs/ACTION_PLAN-linear-4a34011b-104b-4334-871b-a0a014883548.md tasks/specs/linear-4a34011b-104b-4334-871b-a0a014883548.md tasks/tasks-linear-4a34011b-104b-4334-871b-a0a014883548.md .agent/task/linear-4a34011b-104b-4334-871b-a0a014883548.md`
  - `git diff --check -- docs/PRD-linear-4a34011b-104b-4334-871b-a0a014883548.md docs/TECH_SPEC-linear-4a34011b-104b-4334-871b-a0a014883548.md docs/ACTION_PLAN-linear-4a34011b-104b-4334-871b-a0a014883548.md tasks/specs/linear-4a34011b-104b-4334-871b-a0a014883548.md tasks/tasks-linear-4a34011b-104b-4334-871b-a0a014883548.md .agent/task/linear-4a34011b-104b-4334-871b-a0a014883548.md tasks/index.json`
- Parent implementation lane:
  - focused `orchestrator/tests/CoStatusCliShell.test.ts`
  - focused `orchestrator/tests/CoStatusAttachCliShell.test.ts`
  - parent docs-review before implementation
  - parent `node scripts/spec-guard.mjs --dry-run`
  - parent standalone review and elegance review as required by the implementation diff
- Rollback plan:
  - revert the bounded direct-JSON recovery if it obscures genuine failures or broadens into adjacent issue scope

## Risks & Mitigations
- Risk: the fix gets reframed as a generic `provider_intake` or `tracked.linear` truth repair.
  - Mitigation: keep `CO-243` and `CO-223` explicit in non-goals and protected-term grep checks.
- Risk: the fix gets reframed as generic refresh-stuck / restart-required host-failure handling.
  - Mitigation: keep `CO-41`, `CO-194`, and `CO-211` explicit in non-goals and parity notes.
- Risk: the direct JSON path masks genuine auth/network/timeout failures while trying to recover.
  - Mitigation: preserve attach-style fail-closed behavior and existing error guidance when the refreshed endpoint is still unreadable.

## Approvals
- Docs packet child lane: `.runs/linear-4a34011b-104b-4334-871b-a0a014883548-co-246-docs-packet/cli/2026-04-18T08-43-23-951Z-861dc439/manifest.json`
- Parent docs-review: pending parent acceptance
- Parent implementation/review/PR lifecycle: pending parent lane
