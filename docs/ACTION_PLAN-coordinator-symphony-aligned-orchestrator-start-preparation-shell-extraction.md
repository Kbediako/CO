# ACTION_PLAN: Coordinator Symphony-Aligned Orchestrator Start Preparation Shell Extraction

## Docs-first

- [x] Create PRD / TECH_SPEC / ACTION_PLAN / findings / spec / checklist / .agent mirror for `1194`.
- [x] Update `tasks/index.json`, `docs/TASKS.md`, and `docs/docs-freshness-registry.json`.
- [x] Capture local deliberation plus docs-review approval or explicit override for the start-preparation shell seam.

## Implementation

- [ ] Extract the `start()` preparation cluster from `orchestrator.ts`.
- [ ] Keep manifest/bootstrap, runtime-mode application, and persister wiring exact.
- [ ] Preserve focused regressions around `start()` bootstrap and control-plane lifecycle handoff.

## Closeout

- [ ] Run the standard gate bundle and capture closeout artifacts under `out/1194-coordinator-symphony-aligned-orchestrator-start-preparation-shell-extraction/manual/<timestamp>-closeout/`.
- [ ] Run an explicit elegance review.
- [ ] Record the next truthful seam after `1194`.
