# PRD - CO: restore unrelated full-suite SelectedRunProjection reservation-ledger placeholder timeout blocking CO-226 / CO-219

## Added by Bootstrap 2026-04-18

## Traceability
- Linear issue: `CO-233` / `2594bf7f-12f3-4e59-8b9f-62c551fe58a0`
- Related issue linkage to preserve: `CO-226`, `CO-219`
- Shared source anchor: `ctx:sha256:3f6604004897f02bc794db8336db014b25eac836f27afb671f1ef946761d47ee#chunk:c000001`
- Shared source object id: `sha256:3f6604004897f02bc794db8336db014b25eac836f27afb671f1ef946761d47ee`
- Parent-provided source payload reference: `.runs/linear-2594bf7f-12f3-4e59-8b9f-62c551fe58a0-docs-packet-current-main-audit/cli/2026-04-18T13-48-58-782Z-e41ffc06/memory/source-0/source.txt`
- Source payload note: the current-main audit prompt carries this source anchor; the referenced payload is not present in this child checkout, so the audit relies on the prompt-carried issue checksum plus current repo seams in `selectedRunProjection.ts`, `providerLinearWorkerRunner.ts`, and `providerLinearChildLaneShell.ts`.
- Parent-provided manifest reference: `.runs/linear-2594bf7f-12f3-4e59-8b9f-62c551fe58a0-docs-packet-current-main-audit/cli/2026-04-18T13-48-58-782Z-e41ffc06/manifest.json`

## Summary
- Problem Statement: `CO-226` and `CO-219` should not absorb an unrelated branch-baseline blocker. On the current branch, the live mismatch is no longer `orchestrator/tests/Doctor.test.ts`; in the same rerun the blocker is the full-suite-only timeout around `orchestrator/tests/SelectedRunProjection.test.ts`, specifically the case `refreshes projection proofs when child-lane reservation ledger placeholders exist`. The isolated repro `npx vitest run orchestrator/tests/SelectedRunProjection.test.ts -t "refreshes projection proofs when child-lane reservation ledger placeholders exist"` passes, so the issue is the divergence between full-suite `npm run test` truth and isolated repro truth.
- Desired Outcome: the branch baseline becomes truthful again for the parent lanes. That means either the full-suite-only timeout is fixed in a bounded way, or any remaining blocker is explicitly owned so `CO-226` and `CO-219` can point at `CO-233` instead of inheriting unrelated suite instability.

## User Request Translation (Context Anchor)
- User intent / needs (in your own words): audit only the existing `CO-233` issue packet across the six owned issue-doc files, refreshing drift where needed while preserving the exact full-suite-vs-isolated-repro contrast, the fact that `Doctor.test.ts` is no longer the live blocker in the same rerun, and the explicit linkage to `CO-226` / `CO-219`.
- Success criteria / acceptance:
  - the packet preserves `npm run test` as the authoritative full-suite surface for the blocker
  - the packet preserves `orchestrator/tests/SelectedRunProjection.test.ts` and the exact case `refreshes projection proofs when child-lane reservation ledger placeholders exist`
  - the packet preserves the isolated passing repro command `npx vitest run orchestrator/tests/SelectedRunProjection.test.ts -t "refreshes projection proofs when child-lane reservation ledger placeholders exist"`
  - the packet states that `orchestrator/tests/Doctor.test.ts` is no longer the live blocker in the same rerun
  - `CO-226` and `CO-219` remain explicit adjacent issues rather than implied background context
- Constraints / non-goals:
  - child lane stays docs-only
  - no implementation, test, build-script, Linear, workpad, PR, or full-validation mutations in this patch
  - do not treat the isolated repro pass as sufficient proof that the full-suite blocker is gone

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `npm run test`
  - `orchestrator/tests/SelectedRunProjection.test.ts`
  - `refreshes projection proofs when child-lane reservation ledger placeholders exist`
  - `npx vitest run orchestrator/tests/SelectedRunProjection.test.ts -t "refreshes projection proofs when child-lane reservation ledger placeholders exist"`
  - `orchestrator/tests/Doctor.test.ts`
  - `full-suite-only timeout`
  - `passing isolated repro`
  - `CO-226`
  - `CO-219`
- Protected terms / exact artifact and surface names:
  - `provider-linear-worker-proof.json`
  - `provider-linear-worker-child-lanes.json`
  - `selectedRunProjection.ts`
  - `providerLinearWorkerRunner.ts`
  - `providerLinearChildLaneShell.ts`
  - `Child lane reserved before child run startup.`
  - `launching-docs-packet`
- Nearby wrong interpretations to reject:
  - `Doctor.test.ts` is still the active blocker, so reopen doctor-only triage
  - the isolated repro passes, therefore the issue is already fixed
  - widen `CO-233` into `CO-226` or `CO-219` implementation work
  - solve this only by raising generic suite timeouts without understanding the proof-refresh / placeholder seam
  - refactor broad control-runtime or observability flows before proving the narrow SelectedRunProjection mismatch

## Parity / Alignment Matrix
- Current truth:
  - full `npm run test` still times out or stalls only in suite context around `orchestrator/tests/SelectedRunProjection.test.ts`
  - the named case is `refreshes projection proofs when child-lane reservation ledger placeholders exist`
  - the isolated repro command for that exact case passes
  - `orchestrator/tests/Doctor.test.ts` is no longer the live blocker in the same rerun
- Reference truth:
  - unrelated baseline blockers should live in their own issue instead of being absorbed by adjacent feature or closeout lanes
  - full-suite truth and isolated-repro truth should either agree or be explicitly classified as a suite-context defect
  - the smallest correct fix should live at the proof-refresh / placeholder-resolution seam rather than in unrelated runtime areas
- Target truth / intended delta:
  - the full suite and the isolated repro agree for the SelectedRunProjection placeholder case
  - `CO-226` and `CO-219` can cite `CO-233` as the unrelated blocker or former blocker instead of re-triaging it
  - the repair remains narrowly about proof refresh when child-lane reservation placeholders exist
- Explicitly out-of-scope differences:
  - `CO-226` and `CO-219` implementation details
  - reopening doctor-only baseline repair absent fresh proof
  - unrelated full-suite flakes beyond the named SelectedRunProjection case unless they become the authoritative blocker after this one is resolved

## Why Now
- `CO-233` exists to keep the current unrelated full-suite blocker separate from the parent lanes that are trying to move `CO-226` and `CO-219` forward.
- The branch truth is narrower than a generic baseline-red story: the isolated repro is already green, so the docs need to preserve the suite-context mismatch exactly instead of collapsing it into a general failing-test narrative.

## Non-Goals
- Implementing the actual fix in this child lane.
- Reopening `orchestrator/tests/Doctor.test.ts` as the active blocker without fresh evidence.
- Folding the SelectedRunProjection timeout into `CO-226` / `CO-219` feature scope.
- Reworking broad orchestration, observability, or child-lane lifecycle design unless the parent later proves the narrow seam is insufficient.

## Stakeholders
- Product: parent owners trying to close out `CO-226` and `CO-219` against truthful branch baseline status
- Engineering: control-host / SelectedRunProjection / provider-linear-worker maintainers
- Design: N/A

## Metrics & Guardrails
- Primary Success Metrics:
  - the packet preserves the exact blocker contrast between full-suite timeout and isolated passing repro
  - the packet preserves that `Doctor.test.ts` is no longer the blocker in the same rerun
  - the packet leaves parent lanes with a bounded implementation/validation seam instead of broad redesign pressure
- Guardrails / Error Budgets:
  - do not claim the issue is fixed because the isolated repro passes
  - do not let `CO-226` or `CO-219` silently absorb this unrelated full-suite mismatch
  - preserve the reservation-ledger placeholder wording and the likely proof-refresh seam exactly

## User Experience
- Personas: parent lane owner, reviewer checking branch-baseline truth, maintainer resuming the SelectedRunProjection seam later
- User Journeys:
  - a parent lane reruns `npm run test`, sees the suite-context timeout, and can immediately distinguish it from the already-passing isolated repro
  - a reviewer can tell `Doctor.test.ts` is no longer the blocker and does not need to reopen that older hypothesis
  - a maintainer can find the likely proof-refresh and child-lane placeholder seams without confusing them with adjacent issue work

## Technical Considerations
- Architectural Notes:
  - `orchestrator/tests/SelectedRunProjection.test.ts` exercises proof refresh when a reserved child-lane ledger placeholder (`run_id='launching-docs-packet'`, summary `Child lane reserved before child run startup.`) coexists with a matching real child manifest
  - the test expects SelectedRunProjection to refresh the projection proof from the real child manifest while leaving the ledger placeholder file itself untouched
  - the isolated repro already passes, so the live defect is likely suite-context timing or shared-state interaction rather than the narrow assertion set by itself
  - `orchestrator/tests/Doctor.test.ts` is no longer the live blocker in the same rerun and should remain explicit historical contrast, not the active problem statement
- Dependencies / Integrations:
  - `orchestrator/src/cli/control/selectedRunProjection.ts`
  - `orchestrator/src/cli/providerLinearWorkerRunner.ts`
  - `orchestrator/src/cli/providerLinearChildLaneShell.ts`
  - `orchestrator/src/cli/control/providerIssueObservability.ts`
  - `orchestrator/tests/SelectedRunProjection.test.ts`

## Open Questions
- Is the suite-only timeout driven by proof refresh itself, by shared placeholder-ledger state, or by interaction with other full-suite fixtures?
- Does the parent need only a narrow SelectedRunProjection fix, or a matching child-lane proof-refresh hardening path in `providerLinearChildLaneShell.ts` / `providerLinearWorkerRunner.ts`?
- After the SelectedRunProjection mismatch is fixed, does any other unrelated full-suite blocker become authoritative for `CO-226` / `CO-219`?

## Approvals
- Product: Self-approved from the Linear issue scope as carried through the parent prompt
- Engineering: Pending parent docs acceptance, implementation, and validation
- Design: N/A
