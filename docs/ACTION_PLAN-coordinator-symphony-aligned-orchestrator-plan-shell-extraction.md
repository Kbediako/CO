# ACTION_PLAN: Coordinator Symphony-Aligned Orchestrator Plan Shell Extraction

## Docs-first

- [x] Create PRD / TECH_SPEC / ACTION_PLAN / findings / spec / checklist / .agent mirror for `1197`.
- [x] Update `tasks/index.json`, `docs/TASKS.md`, and `docs/docs-freshness-registry.json`.
- [x] Capture local deliberation plus docs-review approval or explicit override for the plan-shell seam.

## Implementation

- [ ] Extract the `plan()` preview cluster from `orchestrator.ts`.
- [ ] Keep stage mapping, pipeline source normalization, and plan-preview fallback behavior exact.
- [ ] Preserve focused regressions around the plan shell without widening into lifecycle or routing flows.

## Closeout

- [ ] Run the standard gate bundle and capture closeout artifacts under `out/1197-coordinator-symphony-aligned-orchestrator-plan-shell-extraction/manual/<timestamp>-closeout/`.
- [ ] Run an explicit elegance review.
- [ ] Record the next truthful seam after `1197`.
