# ACTION_PLAN - Coordinator Symphony-Aligned Standalone Review Active Closeout-Root Provenance Hint

1. Register `1112` across PRD / TECH_SPEC / ACTION_PLAN / findings / task checklist / `.agent` mirror / `tasks/index.json` / `docs/TASKS.md` / docs freshness registry.
2. Extend `scripts/run-review.ts` so diff-mode handoff text surfaces the already-resolved active closeout root set.
3. Add focused `tests/run-review.spec.ts` coverage for direct task, delegated parent-task inheritance, and `TODO-closeout` plus latest completed closeout handling.
4. Update `docs/standalone-review-guide.md` with the new provenance hint behavior.
5. Run the docs-first guard bundle and sync the registration evidence.
