# Deliberation - 1295 RLM CLI Request Shell Extraction

## Decision

Open `1295` as a real extraction lane.

## Why

`1294` confirmed that `handleRlm(...)` still owns a meaningful binary-facing request-shaping seam above the extracted RLM launch and completion shells. That is a real nearby extraction, not a forced symmetry slice.
