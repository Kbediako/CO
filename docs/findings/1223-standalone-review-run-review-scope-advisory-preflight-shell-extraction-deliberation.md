# 1223 Deliberation: Standalone Review Run-Review Scope Advisory Preflight Shell Extraction

- `1220` already established that the next truthful standalone-review move had to come from the remaining `run-review.ts` orchestration shell rather than reopening the exhausted helper-family cluster.
- `1222` then removed the launch-attempt shell, leaving the scope assessment and scope-advisory shaping block as the first still-cohesive pre-launch contract inside `run-review.ts`.
- This is a stronger next seam than another broad orchestration reassessment because the remaining scope/advisory work already forms one bounded contract: scope-mode resolution, scope-path collection, large-scope assessment, console advisories, and prompt guidance.
- The main risk is widening into adjacent runtime/handoff/monitor/reporting surfaces that are separate follow-on blocks and should remain out of scope for this lane.
- The lane should stay behavior-preserving and bounded to the scope/advisory preflight shell only.
