# ACTION_PLAN: Coordinator Symphony-Aligned RLM Runner Codex Runtime and Collab Lifecycle Shell Extraction

## Steps

1. Register `1237` docs-first artifacts, task mirrors, and registry and index entries.
2. Reinspect `rlmRunner.ts` against the iterative and symbolic cores plus focused RLM tests.
3. Extract the remaining Codex runtime and collab lifecycle shell into a bounded helper.
4. Add or update focused RLM regressions for runtime command resolution, JSONL completion handling, and collab lifecycle validation.
5. Run the full validation lane and close the task with summary, elegance review, and override notes.

## Guardrails

- Do not widen into the iterative or symbolic loop cores except for consumer wiring needed by the extracted shell.
- Preserve collab lifecycle validation semantics exactly; avoid false passes or false failures.
- Preserve runtime command selection and non-interactive env shaping.
