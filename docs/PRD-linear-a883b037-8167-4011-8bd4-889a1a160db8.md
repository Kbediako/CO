# PRD - CO: clear stale merge-closeout transition_failed residue once CO-210 / PR #512 are merged and live Linear truth is Done

## Added by Docs Child Lane 2026-04-18

## Traceability
- Linear issue: `CO-228` / `a883b037-8167-4011-8bd4-889a1a160db8`
- Task id: `linear-a883b037-8167-4011-8bd4-889a1a160db8`
- Canonical spec: `tasks/specs/linear-a883b037-8167-4011-8bd4-889a1a160db8.md`
- Docs packet child lane manifest: `.runs/linear-a883b037-8167-4011-8bd4-889a1a160db8-docs-packet/cli/2026-04-17T22-41-33-103Z-08808e6e/manifest.json`
- Source anchor: `ctx:sha256:9aebacf8637c2460a3ea3590e492f1426b765f792d70d3b700563c1591d7ef39#chunk:c000001`
- Source payload note: the expected shared `source-0` payload is not present in this child checkout; this packet is anchored on the protected issue wording from the bounded lane handoff plus current repo seams in `providerMergeCloseout.ts`, `providerIssueHandoff.ts`, `providerLinearWorkflowFacade.ts`, `providerIssueObservability.ts`, `controlRuntime.ts`, and `compatibilityIssuePresenter.ts`.

## Summary
- Problem Statement: after `CO-210` / `PR #512` are already merged and live Linear truth has advanced to `Done`, local `provider-intake-state.json` can still retain older merge-closeout `transition_failed` residue. The retained shape includes top-level claim reason `provider_issue_merge_closeout_transition_failed`, `merge_closeout.status=transition_failed`, `reason=linear_done_transition_failed`, `linear_transition.status=failed`, and `error=linear_transition_conflict` even though merged PR truth plus live Linear Done truth are already authoritative.
- Desired Outcome: merged PR truth plus live Linear `Done` truth dominate stale local `transition_failed` residue once that residue is no longer current, without weakening real current transition-failure handling, historical auditability, or adjacent issue boundaries.

## User Request Translation (Context Anchor)
- Create the docs-first packet and registry mirrors for `CO-228`.
- Preserve the exact stale-residue issue checksum anchored on `CO-210`, `PR #512`, live Linear Done truth, `provider_issue_merge_closeout_transition_failed`, `merge_closeout.status=transition_failed`, `reason=linear_done_transition_failed`, `linear_transition.status=failed`, and `error=linear_transition_conflict`.
- Keep this lane explicitly separate from shared-root reconciliation redesign, new attachment-selector regressions, `CO-104`, `CO-138`, `CO-214`, and broader `CO-210` current-progress hydration scope.
- Define the parent implementation as bounded stale-residue invalidation or supersession in provider handoff and read-model projection seams, not manual intake cleanup and not workflow-state redesign.

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `CO-210`
  - `PR #512`
  - `live Linear Done truth`
  - `provider_issue_merge_closeout_transition_failed`
  - `merge_closeout.status=transition_failed`
  - `reason=linear_done_transition_failed`
  - `linear_transition.status=failed`
  - `error=linear_transition_conflict`
- Protected terms / exact artifact and surface names:
  - `provider-intake-state.json`
  - `provider_issue_merge_closeout_transition_failed`
  - `merge_closeout.status=transition_failed`
  - `reason=linear_done_transition_failed`
  - `linear_transition.status=failed`
  - `error=linear_transition_conflict`
  - `providerMergeCloseout.ts`
  - `providerIssueHandoff.ts`
  - `providerLinearWorkflowFacade.ts`
  - `providerIssueObservability.ts`
  - `controlRuntime.ts`
  - `compatibilityIssuePresenter.ts`
- Nearby wrong interpretations to reject:
  - this is shared-root reconciliation redesign rather than stale terminal residue cleanup
  - this is a new attachment-selector regression or a reopening of `CO-104`
  - this is just `CO-138` or `CO-214` with renamed status text
  - this is a broader `CO-210` current-progress hydration change
  - every `linear_transition_conflict` should be auto-cleared without proving newer merged/Done truth

## Parity / Alignment Matrix

| Surface | Current Truth | Reference Truth | Target Truth |
| --- | --- | --- | --- |
| Local intake closeout state | Merged PR truth plus live Linear `Done` truth can coexist with stale local `provider_issue_merge_closeout_transition_failed` residue and nested `linear_transition.status=failed` / `error=linear_transition_conflict` detail. | Stale local merge-closeout residue must not outrank newer merged PR truth and live Linear `Done` truth. | Merged PR truth plus newer live Linear `Done` truth dominate stale local `transition_failed` residue once it is no longer current. |
| Fresh discovery / handoff suppression | Stale terminal residue can keep the issue behaving like live blocked work even after the merge and live `Done` advance are already real. | Stale terminal residue should not continue suppressing later pickup or recovery decisions. | Provider handoff treats the stale merged-and-Done `transition_failed` shape as non-current while preserving audit history. |
| `CO STATUS` active/backoff projection | Read-side projection can still inherit stale `transition_failed` closeout truth as if the issue were current work. | Projection should respect newer merged PR truth plus live Linear `Done` truth when the failure residue is stale. | `CO STATUS active/backoff projection` no longer reports the stale merged-and-Done `transition_failed` shape as current work. |
| Terminal transition failure truth | Real current transition failures and stale post-merge residue are both represented as `transition_failed` today. | True current failures should remain explicit, but stale merged-and-Done residue should not remain authoritative forever. | Real current failures remain visible when live truth has not advanced; only stale merged-and-Done `transition_failed` residue is invalidated or superseded. |
| Scope | The lane can be misread as shared-root redesign, attachment-selector fallout, or an adjacent stale-residue lane. | Each adjacent lane keeps its own seam. | `CO-228` stays bounded to stale merged-terminal `transition_failed` residue after `CO-210` / `PR #512` and live Linear `Done` truth. |

## Acceptance Criteria
- The docs packet and registry mirrors exist for the declared `CO-228` files only.
- Parent implementation can reproduce the `CO-210` / `PR #512` shape where merged PR truth plus live Linear `Done` truth coexist with stale local `provider_issue_merge_closeout_transition_failed` residue and nested `linear_transition.status=failed` / `error=linear_transition_conflict` detail.
- That stale residue no longer suppresses fresh discovery or later handoff decisions once merged PR truth plus live Linear `Done` truth are authoritative.
- `CO STATUS active/backoff projection` no longer reports the stale merged-and-Done `transition_failed` shape as current work.
- Real current transition-failure truth remains visible for issues whose live Linear truth has not advanced to terminal `Done`.
- The repair does not drift into shared-root reconciliation redesign, new attachment-selector regressions, `CO-104`, `CO-138`, `CO-214`, or broader `CO-210` current-progress hydration semantics.

## Non-Goals
- No live Linear mutation or workpad mutation from this child lane.
- No shared-root reconciliation redesign.
- No new attachment-selector regression work and no reopening of `CO-104` historical attached-PR selection.
- No rewrite of `CO-138` stale active-work residue or `CO-214` stale action-required merged-terminal residue.
- No broader `CO-210` current-progress hydration change.
- No implementation or test edits in this child lane.

## Not Done If
- Merged PR truth plus live Linear `Done` truth can still leave the issue governed by stale local `provider_issue_merge_closeout_transition_failed` residue.
- `CO STATUS active/backoff projection` still reports the stale merged-and-Done `transition_failed` shape as current work.
- The repair hides real current transition failures instead of separating stale residue from live failure truth.
- The lane drifts into shared-root reconciliation redesign, attachment-selector regressions, `CO-104`, `CO-138`, `CO-214`, or broader `CO-210` scope.

## Validation Contract
- Parent runs focused provider handoff / stale-residue regressions for the merged-and-Done `transition_failed` shape.
- Parent runs focused read-model / `CO STATUS` projection regressions proving stale merged-and-Done residue is no longer treated as current work.
- Parent proves real current `transition_failed` truth remains intact when live Linear truth has not advanced.
- Child lane runs only scoped docs checks: JSON parse, protected-term grep, and `git diff --check` over the touched docs files.
