# PRD - CO-452 retire js_repl posture after Codex CLI 0.128.0 removal

## Traceability
- Linear issue: `CO-452` / `d412792b-9a2a-43d9-96dc-ca021e728d09`
- Linear URL: https://linear.app/asabeko/issue/CO-452/co-retire-js-repl-posture-and-feature-toggles-after-codex-cli-01280
- Task id: `linear-d412792b-9a2a-43d9-96dc-ca021e728d09`
- Canonical spec: `tasks/specs/linear-d412792b-9a2a-43d9-96dc-ca021e728d09.md`
- Source anchor: `ctx:sha256:16dbde8f2aa6d7d4bb0341d4618425add924fcec775778f2e35ba95b7c181b26#chunk:c000001`
- Live local evidence: `/opt/homebrew/bin/codex` reports `codex-cli 0.128.0`; `codex features list` reports `js_repl` and `js_repl_tools_only` as `removed false`.

## Summary
- Problem Statement: CO still describes `js_repl` as default-on and still carries active feature-toggle examples even though Codex CLI `0.128.0` removed the feature surface.
- Desired Outcome: current-facing docs, config guidance, and cloud feature-toggle tests stop recommending or expecting `js_repl`; historical evidence remains clearly historical.

## User Request Translation (Context Anchor)
- User intent / needs: retire active `js_repl` posture and feature-toggle guidance discovered during the `0.128.0` release intake without absorbing adjacent release-intake follow-ups.
- Success criteria / acceptance:
  - current-facing docs no longer say `js_repl` is default-on or should be enabled/disabled
  - cloud feature toggle helpers/tests no longer use `js_repl` as an active example
  - historical evidence docs are preserved only as archive/history
  - CO-449 remains the canonical broader `0.128.0` release-intake issue
- Constraints / non-goals:
  - do not reopen CO-450 binary provenance or CO-451 Agent Identity auth provenance
  - do not remove generic `CODEX_CLOUD_ENABLE_FEATURES` / `CODEX_CLOUD_DISABLE_FEATURES` support for still-active Codex features
  - do not delete historical evidence that is still useful as past decision context

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `Codex CLI 0.128.0`
  - `js_repl`
  - `js_repl_tools_only`
  - `removed false`
  - `#19410 Remove js_repl feature`
  - `CODEX_CLOUD_ENABLE_FEATURES=js_repl`
  - `CODEX_CLOUD_DISABLE_FEATURES=js_repl`
  - `CO-449`
  - `CO-450`
  - `CO-451`
- Protected terms / exact artifact and surface names:
  - `AGENTS.md`
  - `docs/README.md`
  - `docs/AGENTS.md`
  - `.agent/AGENTS.md`
  - `docs/guides/cloud-mode-preflight.md`
  - `docs/guides/rlm-recursion-v2.md`
  - `docs/guides/upstream-codex-cli-sync.md`
  - `orchestrator/tests/CodexCloudTaskExecutor.test.ts`
  - `orchestrator/tests/OrchestratorCloudTargetExecutor.test.ts`
  - scripts/js-repl-usage-matrix.mjs
- Nearby wrong interpretations to reject:
  - treating `js_repl` as merely disabled or experimental instead of removed in `0.128.0`
  - deleting generic cloud feature flag support for still-active features
  - broadening into all `0.128.0` release-intake cleanup
  - treating the old evidence-gate packet as current guidance

## Parity / Alignment Matrix
- Current truth: active CLI evidence and upstream release intake say `js_repl` is removed; current CO docs/tests still treat it as default-on and toggleable.
- Reference truth: Codex CLI `0.128.0` feature list has `js_repl` and `js_repl_tools_only` in removed state with `false` enablement.
- Target truth / intended delta: CO says `js_repl` is removed after `0.128.0`, not default-on, not a break-glass toggle, and not a cloud feature-contract lane.
- Explicitly out-of-scope differences: broader `0.128.0` release posture stays in CO-449; binary provenance stays in CO-450; Agent Identity auth provenance stays in CO-451.

## Not Done If
- current-facing docs still tell users to run `codex features enable js_repl` or `codex features disable js_repl`
- cloud guidance still recommends `CODEX_CLOUD_ENABLE_FEATURES=js_repl` or `CODEX_CLOUD_DISABLE_FEATURES=js_repl`
- tests still use `js_repl` as an active cloud feature toggle example
- the historical evidence-gate packet can be mistaken for current policy
- generic cloud feature flag support is removed instead of narrowed away from the removed feature

## Goals
- Retire active `js_repl` posture from current docs and policy guidance.
- Keep feature-toggle examples on features that are not removed by the active CLI.
- Remove or retire source checkout canary surfaces whose only purpose was `js_repl` usage gating.
- Preserve old evidence docs as historical context with clear archive wording.

## Non-Goals
- No broad release-intake rewrite beyond the `js_repl` removal posture.
- No changes to cloud execution semantics other than removing `js_repl` from active examples and expectations.
- No CO-450 or CO-451 implementation.
- No deletion of the old evidence docs solely to clear references.

## Stakeholders
- Product: CO operator workflow users.
- Engineering: CO maintainers and provider workers.
- Design: Not applicable.

## Metrics & Guardrails
- Primary Success Metrics: no current-facing `js_repl` default-on or enable/disable guidance remains; focused cloud feature tests pass; docs-first and validation gates capture the posture change.
- Guardrails / Error Budgets: keep generic feature flag plumbing intact; keep historical packet wording explicitly historical; avoid unrelated release-intake scope.

## User Experience
- Personas: CO operators who follow AGENTS and docs guidance for local/cloud execution.
- User Journeys: an operator reading current guidance after `0.128.0` should see that `js_repl` is removed and should not set removed feature toggles.

## Technical Considerations
- Architectural Notes: `CODEX_CLOUD_ENABLE_FEATURES` / `CODEX_CLOUD_DISABLE_FEATURES` remain generic pass-through controls for current Codex feature names; tests should use non-removed examples.
- Dependencies / Integrations: active local Codex CLI feature list, CO-449 release intake context, docs freshness/spec guards.

## Fallback / Refactor Decision
- Applies to fallback, compatibility, legacy, stale, cached, break-glass, or minor-seam behavior? `Yes`.
- Decision: remove the `js_repl` break-glass/default-on guidance because the upstream feature is removed in Codex CLI `0.128.0`.
- Large-refactor check: this is a narrow posture cleanup, not another compatibility seam; no larger refactor is required.

## Open Questions
- None blocking implementation. Adjacent removed or changed feature posture discovered outside `js_repl` should be routed through CO-449 or a separate follow-up.

## Approvals
- Product: Linear CO-452 issue body.
- Engineering: 2026-05-01 pre-implementation issue-quality self-review confirms the lane is not narrower than the user request and should proceed with docs, tests, and narrow source cleanup.
- Design: Not applicable.
