# ACTION PLAN - CO-530 Apr 9-12 Legacy Docs Freshness Cohort Reclassification

## Summary
- Goal: Remove the Apr 9-12 / 1076-1137 historical task packet family from active rolling docs freshness debt without weakening validators or losing historical evidence.
- Scope: Docs freshness registry/task metadata, cohort guide notes, machine evidence, focused recurrence checks for the bounded legacy cohort, and the May 14 terminal task-packet recurrence that validation proved shares the same root cause.
- Assumptions: CO-522 remains the live same-project owner while this repair lands; unrelated public/current hard-stale docs must not be classified as historical or refreshed in CO-530 and remain with existing owner CO-532 unless formally transferred.

## Issue Readiness Gate
- Intent checksum / protected terms carried forward: `docs:freshness:maintain`, `block_policy_over_budget`, `current_entries=440`, `current_cohorts=28`, `max_entries=300`, `max_cohorts=2`, `co-429-completed-lane-registry-residue`, `last_review=2026-04-09..2026-04-12`, Apr 13 `1138-1163`, release packets `0942-0944`, terminal Linear task specs, `Task Packet`, `Task Mirror`, `Report Only`, `.agent/task`, `docs/PRD-*`, `docs/TECH_SPEC-*`, `docs/ACTION_PLAN-*`, `tasks/specs`, `tasks/tasks-*`.
- Not done if: The Apr 9-12 rows remain active candidate cohorts, current-head terminal recurrence rows are archived without explicit scope/evidence, the fix hides debt by widening caps or deleting rows, or CO-522 owner truth is lost.
- Pre-implementation issue-quality review: Baseline evidence reproduced the exact owner-routed over-budget family and separated it from 5 unrelated hard-stale current docs that stay out of CO-530 scope.
- Fallback / refactor decision: This touches legacy/stale docs freshness behavior; decision is remove the active-debt fallback by archiving/reclassifying verified historical rows. No retained fallback.
- Durable retention evidence: Historical packet files stay on disk and registry/task metadata records the classification.
- Large-refactor check: A broad freshness refactor would add unnecessary classifier complexity because the existing status model already supports archived historical rows.
- Minor-seam decision: A bounded metadata repair is acceptable because it removes one legacy active-debt seam without adding a new runtime or validation branch.

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| docs freshness | Apr 9-12 completed-lane rows treated as active rolling debt | remove fallback | CO-530 | `block_policy_over_budget` for legacy completed-lane rows | 2026-05-09 | N/A after removal | N/A after removal | Rows are archived/reclassified and no longer appear as active rolling debt | Before/after `docs:freshness:maintain` JSON plus focused regression assertion |

## Milestones & Sequencing
1) Reproduce baseline and save JSON evidence for the Apr 9-12 cohort shape.
2) Create packet/mirror docs and record the issue-quality scope.
3) Update the exact affected registry/task/guide rows to archived or equivalent strict historical classification, including current-head recurrence rows only when terminal task evidence is present.
4) Add focused regression or machine assertion coverage for the cohort.
5) Run validation, standalone review, elegance pass, PR handoff, and ready-review drain.

## Dependencies
- CO-522 live owner verification through `docs:freshness:maintain`.
- CO-529 archive-stub validation remains adjacent but out of scope.
- Existing `docs:freshness` status semantics for `archived`, `terminal_pending_archive`, and `preserved_historical_stub`.

## Validation
- Checks / tests: `node scripts/delegation-guard.mjs`, `node scripts/spec-guard.mjs --dry-run`, `npm run build`, `npm run lint`, `npm run test`, `npm run docs:check`, `npm run docs:freshness`, `npm run docs:freshness:maintain -- --format json`, `npm run repo:stewardship`, `node scripts/diff-budget.mjs`, manifest-backed review, elegance pass.
- Rollback plan: Revert registry/task metadata edits and the CO-530 packet if validation shows active current docs were incorrectly reclassified.

## Risks & Mitigations
- Risk: Over-classifying current docs as historical. Mitigation: Match only completed task packet/mirror/report families with terminal evidence; leave hard-stale current docs untouched.
- Risk: `terminal_pending_archive` creates a different freshness blocker. Mitigation: Use `archived` for full historical packets unless a valid preserved-historical stub exists.
- Risk: Large metadata diff. Mitigation: Generate structured summaries and focused assertions to make the diff auditable.

## Approvals
- Reviewer: CO provider worker.
- Date: 2026-05-13.
