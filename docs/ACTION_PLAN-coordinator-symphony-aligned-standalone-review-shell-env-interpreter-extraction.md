# ACTION_PLAN: Coordinator Symphony-Aligned Standalone Review Shell Env Interpreter Extraction

## Objective

Open the next truthful standalone-review follow-on after `1208` by extracting the shared shell-env interpreter seam now reused by multiple `review-execution-state` analyses.

## Steps

1. Register `1209` docs-first with explicit evidence that the shared shell-env interpreter remains inline inside `scripts/lib/review-execution-state.ts`.
2. Extract the bounded shell-env interpreter helper while preserving the current meta-surface and startup-anchor behavior.
3. Add only the focused `review-execution-state` regressions needed to pin the extracted interpreter contract.
4. Run the lane validations and record any truthful review overrides.
5. Sync closeout mirrors and reassess whether the next truthful follow-on stays inside standalone-review or becomes a review-behavior fix instead of another extraction.
