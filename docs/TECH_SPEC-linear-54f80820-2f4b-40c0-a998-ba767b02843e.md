---
id: linear-54f80820-2f4b-40c0-a998-ba767b02843e
title: CO-513 suppress same-attempt retries for create-follow-up label-resolution failures
relates_to: docs/PRD-linear-54f80820-2f4b-40c0-a998-ba767b02843e.md
risk: medium
owners:
  - Codex
last_review: 2026-05-14
---

## Summary
- Objective: classify `linear_follow_up_label_resolution_failed` as deterministic and suppress same-attempt `create-follow-up` retries with source-label repair guidance.
- Scope: provider Linear worker retry classification and CLI retry-suppression guidance only.
- Constraints: preserve CO-482 fail-closed source-label requirements and avoid CO-400 label projection.

## Issue-Shaping Contract
- User-request translation carried forward: close the sidecar-discovered gap where an under-labeled source issue can cause repeated same-attempt `create-follow-up` attempts.
- Protected terms / exact artifact and surface names: `linear_follow_up_label_resolution_failed`, `linear_follow_up_label_resolution_retry_suppressed`, `providerLinearWorkerTruth.ts`, `linearCliShell.ts`, `resolveFollowUpLabelsFromSourceIssue`.
- Nearby wrong interpretations to reject: do not make label assignment less strict; do not synthesize missing labels; do not broaden into generic label projection.
- Explicit non-goals carried forward: CO-400 projection, cached label authority, label taxonomy changes, and CO-482 fail-closed weakening.

## Parity / Alignment Matrix
- Not applicable.

## Readiness Gate
- Not done if: same-attempt retry remains possible after source-label resolution failure, or the guidance does not tell the worker to source-label the issue first.
- Pre-implementation issue-quality review evidence: live Linear issue-context confirmed CO-513 is narrow and unblocked, with source evidence naming `providerLinearWorkerTruth.ts` and `linearCliShell.ts`.
- Safeguard ownership split: parent owns source classifier/CLI changes; child lane owns the focused regression file.

## Technical Requirements
- Functional requirements:
  - Add `linear_follow_up_label_resolution_failed` to deterministic provider mutation classification.
  - Emit `linear_follow_up_label_resolution_retry_suppressed` for same-attempt retry suppression.
  - Use clear no-retry guidance that requires source-label repair and issue-context reread before retry.
- Non-functional requirements (performance, reliability, security): no additional live Linear call on suppressed retry; no change to label creation or assignment semantics.
- Interfaces / contracts: provider Linear audit summary, `findDeterministicProviderMutationSuppression`, and `resolveCreateFollowUpRetrySuppression`.

## Fallback Expiry / Refactor Decision
- Applies to fallback, compatibility, legacy, stale, cached, break-glass, or minor-seam behavior? Yes.

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Provider Linear retry suppression | Deterministic mutation failures suppress repeated same-attempt helper retries. | justify retaining fallback | CO-513 | Prior `create-follow-up` failure is deterministic and recorded in the same attempt audit. | 2026-05-14 | 2026-05-14 | Non-expiring supported contract | Replaced by a consolidated deterministic mutation policy with equivalent source-label no-retry behavior. | Focused regression and provider-worker validation gates. |

- Large-refactor check: no large refactor is warranted because the existing deterministic suppression abstraction is the correct owner and only one failure code is missing.

## Architecture & Data
- Architecture / design adjustments: extend the existing suppression code-family helper and CLI suppression branch.
- Data model changes / migrations: none.
- External dependencies / integrations: none beyond existing Linear audit evidence.

## Validation Plan
- Tests / checks: focused regression for source-label resolution no-retry instruction, targeted test command, build/lint/test/docs gates, standalone review, elegance pass.
- Rollout verification: provider-worker handoff includes validation evidence and unchanged CO-482 behavior.
- Monitoring / alerts: existing provider audit summary surfaces the deterministic suppression summary.

## Open Questions
- None.

## Approvals
- Reviewer: standalone review executed under `FORCE_CODEX_REVIEW=1`; post-fix output reported no actionable issues, but telemetry classified the clean prose as `review_verdict=unknown`, so the handoff uses a bounded review-verdict parser waiver recorded in the workpad.
- Date: 2026-05-14.
