# TECH SPEC - CO-329 provider stdin bootstrap recurrence

## Added by Bootstrap 2026-04-23

## Summary
- Objective: restore truthful `provider-linear-worker` classification and retry evidence for `stderr | Reading additional input from stdin...` control-host exits.
- Scope: classification helpers, proof guidance, retry/handoff evidence, and focused regression coverage.
- Constraints: preserve fail-closed behavior and stronger diagnostic precedence; avoid issue-content or generic control-host expansion.

## Issue-Shaping Contract
- User-request translation carried forward: `CO-274` is terminal, but the exact stdin bootstrap stderr signature has recurred in live control-host provider-worker runs and needs a non-terminal owner with deterministic coverage.
- Protected terms / exact artifact and surface names: `provider-linear-worker`, `control-host`, `stderr | Reading additional input from stdin...`, `provider_runtime`, `provider-linear-worker-proof.json`, `manifest.json`, `linear_audit.attempted_count`, `retry/resumable queue behavior`, `codex_exit_1`.
- Nearby wrong interpretations to reject: already solved because `CO-274` closed; issue-specific `CO-314` or `CO-328` content work; stale dashboard projection only; retry muting without classification restoration.
- Explicit non-goals carried forward: no fail-open behavior, no generic control-host redesign, no issue-content remediation.

## Parity / Alignment Matrix
- Current truth:
  - live `CO-328` proof has the exact stdin signal, generic `provider_runtime`, `codex_exit_1`, and `linear_audit.attempted_count: 0`
  - live `CO-314` proof has since moved into active/review work, while earlier failed-run evidence still shows retry churn
  - current source lacks a dedicated stdin bootstrap diagnostic category
- Reference truth:
  - the CO-274 seam should classify stdin bootstrap separately while retaining stronger root-cause categories
  - retry/resumable evidence should identify pre-work bootstrap failure rather than ordinary progress
- Target truth / intended delta:
  - `failure_diagnosis.diagnostic_category` can be `provider_stdin_bootstrap`
  - guidance names stdin bootstrap and the control-host/provider worker handoff
  - retry error evidence includes the specific diagnostic category and signal
- Explicitly out-of-scope differences:
  - retry policy changes, issue-specific execution work, and unrelated dashboard projection logic

## Readiness Gate
- Not done if: future proofs classify `Reading additional input from stdin...` only as generic `provider_runtime`, or retry evidence hides the meaningful-work boundary.
- Pre-implementation issue-quality review evidence: parent inspected live `CO-328` and `CO-314` proof artifacts plus current source; the issue is scoped to classification and retry evidence, not a broad control-host rewrite.
- Safeguard ownership split: parent owns implementation because source mapping established a narrow two-file code seam plus tests; no safe independent child lane remains after mapping.

## Technical Requirements
- Functional requirements:
  - add a dedicated `provider_stdin_bootstrap` diagnostic category
  - recognize structured diagnostic values and free-text variants of `Reading additional input from stdin...`
  - preserve stronger root-cause categories over stdin bootstrap preamble
  - prefer proof `failure_diagnosis` when deriving failed-run retry errors
- Non-functional requirements (performance, reliability, security):
  - no network or secret handling changes
  - no retry suppression or fail-open behavior
  - deterministic tests should cover current recurrence shape
- Interfaces / contracts:
  - `ProviderLinearWorkerFailureDiagnosis.diagnostic_category`
  - `provider-linear-worker-proof.json.failure_diagnosis`
  - provider handoff claim `retry_error`

## Architecture & Data
- Architecture / design adjustments:
  - classification remains local to `providerLinearWorkerRunner.ts`
  - handoff retry text remains local to `providerIssueHandoff.ts`
- Data model changes / migrations:
  - no persistent schema migration; JSON proof/category enum expands additively
- External dependencies / integrations:
  - none

## Validation Plan
- Tests / checks:
  - focused `ProviderLinearWorkerRunner` regressions for stdout/stderr stdin bootstrap classification and stronger-category precedence
  - focused `ProviderIssueHandoff` regression for failed proof diagnosis flowing into retry evidence
  - build/typecheck and required repo gates before review handoff
- Rollout verification:
  - workpad records current live `CO-328` pre-work boundary and `CO-314` recovered/current-run distinction
- Monitoring / alerts:
  - future live proofs should show `provider_stdin_bootstrap` for the exact stdin bootstrap signature

## Open Questions
- None.

## Approvals
- Reviewer: Pending
- Date: 2026-04-23
