# PRD - CO-171 Provider Artifact Root Parity
Problem/goal/acceptance: CO-169 made the default provider issue worktree authoritative for `.runs`, but configured non-default artifact roots still need parity so `CODEX_ORCHESTRATOR_RUNS_DIR`, `CODEX_ORCHESTRATOR_OUT_DIR`, `.runs`, `runs`, provider issue worktree, shared root, and review wrapper behavior do not diverge. Preserve CO-169 default shared-root behavior, keep issue-worktree truth authoritative only when workspace identity is proven, and finish only when provider context loading, environment path resolution, and `scripts/run-review.ts` share the same documented contract for `.runs`, `runs`, custom absolute roots, and inherited repo-relative shared-root values.
