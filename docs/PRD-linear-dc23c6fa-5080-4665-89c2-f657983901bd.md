# PRD - CO-359 appserver child-lane runtime classification after pack smoke validation stall

## Traceability
- Linear issue: `CO-359` / `dc23c6fa-5080-4665-89c2-f657983901bd`
- Task id: `20260425-linear-dc23c6fa-5080-4665-89c2-f657983901bd`
- Packet prefix: `linear-dc23c6fa-5080-4665-89c2-f657983901bd`
- Canonical spec: `tasks/specs/linear-dc23c6fa-5080-4665-89c2-f657983901bd.md`
- TECH_SPEC mirror: `docs/TECH_SPEC-linear-dc23c6fa-5080-4665-89c2-f657983901bd.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-dc23c6fa-5080-4665-89c2-f657983901bd.md`
- Task checklist: `tasks/tasks-linear-dc23c6fa-5080-4665-89c2-f657983901bd.md`
- Source anchor: `ctx:sha256:ec738e2d6838b7a07389e50b49cb93adb3d410ead5a7d9e087dd8760c790b635#chunk:c000001`
- Parent-provided source payload reference: `../../.runs/linear-dc23c6fa-5080-4665-89c2-f657983901bd/cli/2026-04-25T03-39-57-861Z-9a9e914c/memory/source-0/source.txt`
- Source payload note: the parent source payload is outside the worker checkout-local artifact root; this packet is anchored on that parent-provided lane prompt and protected issue wording.
- Origin manifest: `.runs/linear-dc23c6fa-5080-4665-89c2-f657983901bd-docs-packet/cli/2026-04-25T03-44-14-263Z-48fc482a/manifest.json`

## Summary
- Problem Statement: `pack-smoke-current-validation-r2` exposed a focused child-lane runtime classification gap: `providerLinearChildLaneRunner` selected `runtime_mode=appserver`, reached `appserver startup`, and left a `child manifest in_progress`, but there was `no assistant follow-up`, `no scoped Vitest`, and the lane later reached `child-lane invalidation` with `codex_exit_1`. That evidence points at an appserver child-lane runtime regression/classification issue, not a marketplace command or pack-smoke logic issue.
- Desired Outcome: parent-owned implementation should classify this appserver child-lane failure shape truthfully, preserve same-issue child-lane proof/patch validation, and give operators actionable runtime truth without weakening parent-dirty fail-closed behavior.

## User Request Translation
- User intent / needs: create the CO-359 docs-first packet only, preserving exact protected terms and issue boundaries while parent owns runtime classification, implementation, tests, Linear state, workpad, PR, and merge.
- Success criteria / acceptance:
  1. The packet classifies CO-359 as a focused runtime regression/classification lane for `providerLinearChildLaneRunner`, not as a marketplace command change and not as a `pack-smoke-current-validation-r2` logic issue.
  2. The packet preserves the observed evidence chain: `runtime_mode=appserver`, `appserver startup`, `child manifest in_progress`, `no assistant follow-up`, `no scoped Vitest`, `child-lane invalidation`, and `codex_exit_1`.
  3. Parent implementation remains responsible for deciding how to classify or recover the appserver child-lane runtime failure.
  4. Parent tests remain responsible for focused runtime coverage; this child lane does not run or add scoped Vitest.
  5. The packet preserves non-goals: no marketplace command changes, no model posture changes, no broad control-host rewrite, no bypass of same-issue child-lane proof/patch validation, and no weakening of parent-dirty fail-closed behavior.
  6. The packet keeps CO-359 distinct from adjacent historical issues `CO-224` and `CO-303`.
- Constraints / non-goals:
  - docs-only child lane
  - no implementation or test file edits
  - no Linear issue/workpad mutations
  - no broad validation suites from this child lane

## Intent Checksum
- Exact user wording / phrases to preserve:
  - `providerLinearChildLaneRunner`
  - `runtime_mode=appserver`
  - `pack-smoke-current-validation-r2`
  - `child manifest in_progress`
  - `appserver startup`
  - `no assistant follow-up`
  - `no scoped Vitest`
  - `child-lane invalidation`
  - `codex_exit_1`
  - `CO-224`
  - `CO-303`
- Protected non-goals:
  - no marketplace command changes
  - no model posture changes
  - no broad control-host rewrite
  - no bypass of same-issue child-lane proof/patch validation
  - no weakening of parent-dirty fail-closed behavior
- Nearby wrong interpretations to reject:
  - CO-359 is a marketplace or pack-smoke command regression because the evidence came from `pack-smoke-current-validation-r2`
  - CO-359 should change model defaults or Codex model posture
  - CO-359 should broadly rewrite control-host state projection
  - CO-359 can weaken child-lane proof, patch validation, or parent-dirty fail-closed behavior to make acceptance easier
  - CO-359 is simply `CO-224` again, despite the different evidence that `appserver startup` and `child manifest in_progress` were reached

## Parity / Alignment Matrix

| Surface | Current Truth | Reference Truth | Target Truth | Explicitly Out Of Scope |
| --- | --- | --- | --- | --- |
| Runtime selection | The evidence chain includes `runtime_mode=appserver`. | Runtime selection alone is not enough to prove useful child-lane execution. | Parent classification distinguishes selected appserver runtime from completed assistant/test activity. | Model posture changes or runtime-mode defaults. |
| Startup evidence | The evidence chain includes `appserver startup` and `child manifest in_progress`. | `CO-224` covered a pre-startup stall boundary; this lane starts from a later appserver-startup shape. | Parent runtime classification captures the post-startup/no-follow-up shape without conflating it with `CO-224`. | Reopening `CO-224` as the primary issue. |
| Assistant/test activity | The evidence chain includes `no assistant follow-up` and `no scoped Vitest`. | A useful child lane must produce assistant progress and appropriate scoped verification before patch acceptance. | Parent implementation makes the missing follow-up/test signal explicit and actionable. | Adding tests in this docs child lane. |
| Failure/invalidation | The evidence chain includes `child-lane invalidation` and `codex_exit_1`. | Terminal failure truth should be visible without converting failed runtime evidence into a valid child patch. | Parent classification records the failure as runtime classification evidence while preserving same-issue proof/patch validation. | Bypassing proof, patch, or parent-dirty gates. |
| Pack smoke context | `pack-smoke-current-validation-r2` surfaced the evidence. | Pack smoke can expose runtime regressions without owning their fix. | CO-359 remains a runtime regression/classification lane. | Marketplace command changes or pack-smoke command logic changes. |
| Adjacent issue boundary | `CO-303` and `CO-224` remain nearby context. | Adjacent issues should prevent scope drift, not absorb this later failure shape. | CO-359 records why this is not a marketplace lane and not only the `CO-224` pre-startup stall. | Broad control-host rewrite or unrelated queue/policy changes. |

## Not Done If
- The packet lets CO-359 be treated as a marketplace command change or pack-smoke logic issue.
- The packet omits any protected evidence term from `pack-smoke-current-validation-r2`.
- Parent implementation can still treat `child manifest in_progress` plus `no assistant follow-up` and `no scoped Vitest` as an ambiguous active lane without a truthful runtime classification.
- The plan weakens same-issue child-lane proof/patch validation or parent-dirty fail-closed behavior.
- The plan broadens into model posture, marketplace commands, or a broad control-host rewrite.
- The packet erases the distinction between this post-`appserver startup` shape, `CO-224`, and `CO-303`.

## Goals
- Create the CO-359 docs-first packet and task registry entry.
- Preserve the exact protected terms and non-goals from the parent issue contract.
- Define CO-359 as a focused appserver child-lane runtime regression/classification lane.
- Leave implementation, tests, validation, Linear state, workpad updates, PR, and merge to the parent lane.

## Non-Goals
- No marketplace command changes.
- No model posture changes.
- No broad control-host rewrite.
- No bypass of same-issue child-lane proof/patch validation.
- No weakening of parent-dirty fail-closed behavior.
- No implementation or test file changes from this child lane.
- No Linear mutation helpers.
- No full repo validation suites.

## Stakeholders
- Product: CO operators relying on pack-smoke and child-lane evidence to classify runtime regressions correctly.
- Engineering: provider child-lane runtime, appserver startup supervision, proof/patch validation, and parent fail-closed behavior owners.
- Design: N/A.

## Metrics & Guardrails
- Primary Success Metrics:
  - CO-359 PRD, TECH_SPEC mirror, canonical spec, ACTION_PLAN, task checklist, and `tasks/index.json` entry exist.
  - All protected terms appear in the packet.
  - `tasks/index.json` remains valid JSON.
  - Child-lane diff touches only declared docs-phase files.
- Guardrails / Error Budgets:
  - zero implementation/test changes
  - zero Linear/workpad mutations
  - zero weakening language for child-lane proof/patch validation or parent-dirty fail-closed behavior

## Technical Considerations
- Architectural Notes:
  - Parent implementation should likely inspect `providerLinearChildLaneRunner` and adjacent child-lane runtime/appserver startup supervision, but this child lane does not edit those surfaces.
  - The issue should preserve `pack-smoke-current-validation-r2` as evidence, not as the owner of the fix.
  - Classification should make the absence of assistant follow-up and scoped Vitest visible without treating the lane as a successful child patch.
- Dependencies / Integrations:
  - parent-owned child-lane runtime investigation
  - parent-owned focused tests
  - parent-owned docs-review / implementation-gate

## Open Questions
- What is the smallest parent-owned runtime classification that distinguishes `child manifest in_progress` from useful assistant/test progress?
- Should the parent surface `codex_exit_1` as a distinct appserver child-lane runtime failure reason, or map it into an existing terminal proof category with more detail?
- Which artifact should carry the final parent-visible reason for `no assistant follow-up` and `no scoped Vitest` after `appserver startup`?

## Approvals
- Product: parent CO-359 lane, pending
- Engineering: parent docs-review / implementation review, pending
- Design: N/A
