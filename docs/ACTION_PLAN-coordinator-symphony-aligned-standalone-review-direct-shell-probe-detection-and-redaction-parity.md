# ACTION_PLAN - Coordinator Symphony-Aligned Standalone Review Direct Shell-Probe Detection and Redaction Parity

1. Register `1111` across PRD / TECH_SPEC / ACTION_PLAN / findings / task checklist / `.agent` mirror / `tasks/index.json` / `docs/TASKS.md` / docs freshness registry.
2. Extend shell-probe classification so direct probe commands can reuse the existing bounded probe heuristics when no shell payload is present.
3. Introduce a smaller shell-probe sample formatter for boundary reasons and telemetry.
4. Add focused state and wrapper regressions for direct-probe detection and redacted failure output.
5. Update `docs/standalone-review-guide.md`, run the bounded validation lane, and sync closeout evidence/mirrors.
