# ACTION_PLAN - CO-329 provider stdin bootstrap recurrence

## Added by Bootstrap 2026-04-23

## Summary
- Goal: restore exact stdin bootstrap classification and retry evidence for recurring control-host `provider-linear-worker` exits after `CO-274`.
- Scope: docs packet, classifier/proof guidance, retry evidence propagation, focused tests, review/validation.
- Assumptions: live `CO-328` is the canonical pre-work recurrence shape; live `CO-314` demonstrates retry/recovery churn and should not be conflated with issue-content work.

## Issue Readiness Gate
- Intent checksum / protected terms carried forward: `provider-linear-worker`, `control-host`, `stderr | Reading additional input from stdin...`, `provider_runtime`, `provider-linear-worker-proof.json`, `manifest.json`, `linear_audit.attempted_count`, `retry/resumable queue behavior`, `codex_exit_1`.
- Not done if: the exact stdin bootstrap signature remains generic `provider_runtime` or retry evidence makes pre-work bootstrap failures look like normal issue progress.
- Pre-implementation issue-quality review: parent source/proof mapping found a narrow implementation seam in runner classification and handoff retry evidence.

## Milestones & Sequencing
1. Register docs-first packet and mirror the Linear acceptance criteria.
2. Restore dedicated stdin bootstrap diagnosis and operator guidance in the provider worker proof path.
3. Propagate proof diagnosis into provider retry/resumable evidence.
4. Add focused regressions for classification, stronger-category precedence, stderr/runtime proof, and handoff retry evidence.
5. Run validation, standalone review, elegance review, and PR review-drain before handoff.

## Dependencies
- Current issue branch in this workspace.
- Existing provider worker tests and handoff tests.
- Linear workpad updates for milestones and review handoff.

## Validation
- Checks / tests:
  - focused Vitest tests for changed surfaces
  - `node scripts/delegation-guard.mjs`
  - `node scripts/spec-guard.mjs --dry-run`
  - build/lint/test/docs gates as required before review
- Rollback plan:
  - revert additive category and retry-evidence changes; existing generic `provider_runtime` behavior remains the fallback.

## Risks & Mitigations
- Risk: stdin bootstrap text can co-occur with stronger root-cause diagnostics.
  - Mitigation: test precedence so auth/quota/cloud/env/guardian categories continue to win.
- Risk: historical proofs remain generic after code changes.
  - Mitigation: workpad distinguishes historical current evidence from future deterministic classification, and tests lock future behavior.

## Approvals
- Reviewer: Pending
- Date: 2026-04-23
