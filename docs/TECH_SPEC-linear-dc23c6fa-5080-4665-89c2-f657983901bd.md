---
id: 20260425-linear-dc23c6fa-5080-4665-89c2-f657983901bd
title: CO-359 appserver child-lane runtime classification after pack smoke validation stall
relates_to: docs/PRD-linear-dc23c6fa-5080-4665-89c2-f657983901bd.md
risk: high
owners:
  - Codex
last_review: 2026-04-25
canonical_spec: tasks/specs/linear-dc23c6fa-5080-4665-89c2-f657983901bd.md
---

# TECH_SPEC Mirror - CO-359 Appserver Child-Lane Runtime Classification

## Canonical Reference
- Canonical TECH_SPEC: `tasks/specs/linear-dc23c6fa-5080-4665-89c2-f657983901bd.md`
- PRD: `docs/PRD-linear-dc23c6fa-5080-4665-89c2-f657983901bd.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-dc23c6fa-5080-4665-89c2-f657983901bd.md`
- Task checklist: `tasks/tasks-linear-dc23c6fa-5080-4665-89c2-f657983901bd.md`

## Summary
- Objective: preserve CO-359 as a focused runtime regression/classification lane for `providerLinearChildLaneRunner` under `runtime_mode=appserver`.
- Evidence carried forward: `pack-smoke-current-validation-r2`, `appserver startup`, `child manifest in_progress`, `no assistant follow-up`, `no scoped Vitest`, `child-lane invalidation`, and `codex_exit_1`.
- Boundary: this is not a marketplace command issue, not a model posture issue, not a broad control-host rewrite, not a bypass of same-issue child-lane proof/patch validation, and not a weakening of parent-dirty fail-closed behavior.

## Issue-Shaping Contract
- User-request translation carried forward: CO-359 asks the parent to classify a post-startup `providerLinearChildLaneRunner` appserver wedge, where `runtime_mode=appserver` and `appserver startup` happened before the child stayed as `child manifest in_progress` with `no assistant follow-up`, `no scoped Vitest`, stale heartbeat/no live runner evidence, `child-lane invalidation`, and `codex_exit_1`.
- Protected terms / exact artifact and surface names:
  - `providerLinearChildLaneRunner`
  - `runtime_mode=appserver`
  - `pack-smoke-current-validation-r2`
  - `child manifest in_progress`
  - `appserver startup`
  - `no assistant follow-up`
  - `no scoped Vitest`
  - `child-lane invalidation`
  - `codex_exit_1`
  - `CO-224`
  - `CO-303`
- Nearby wrong interpretations to reject:
  - Do not treat parent-owned classification as marketplace command logic just because `pack-smoke-current-validation-r2` exposed the wedge.
  - Do not treat `child-lane invalidation` as accepted proof or treat `codex_exit_1` as a successful scoped-test outcome.
  - Do not reopen model posture or broad control-host behavior in this lane.
  - Do not bypass same-issue child-lane proof/patch validation or weaken parent-dirty fail-closed behavior.
- Explicit non-goals:
  - No marketplace command changes.
  - No model posture changes.
  - No broad control-host rewrite.
  - No bypass of same-issue child-lane proof/patch validation.
  - No weakening of parent-dirty fail-closed behavior.
- Current/reference/target split:
  - Current: pack smoke surfaced an appserver child lane that reached startup/in-progress manifest state but produced no assistant/test follow-through and ended invalidated.
  - Reference: `CO-224` and `CO-303` are adjacent shipped behavior; child-lane proof/patch validation and parent-dirty fail-closed behavior are safety boundaries.
  - Target: parent-owned classification records runtime failure truth for `providerLinearChildLaneRunner` under `runtime_mode=appserver` without moving the fix into marketplace logic.

## Parity / Alignment Matrix
| Surface | Current | Reference | Target |
| --- | --- | --- | --- |
| `providerLinearChildLaneRunner` / `runtime_mode=appserver` | Child reached `appserver startup`, then produced `no assistant follow-up`. | `CO-224` and `CO-303` established adjacent appserver child-lane fail-closed behavior. | Parent can distinguish live appserver work from stale post-startup no-output candidates. |
| `pack-smoke-current-validation-r2` | Evidence source for the wedge, not the implementation owner. | Pack smoke remains validation context only. | Runtime diagnostics classify the child-lane state without marketplace command changes. |
| `child manifest in_progress` / no scoped output | Manifest stayed active-looking while there was `no scoped Vitest` and no proof/patch output. | Existing proof/patch validation rejects missing output. | Parent proof surfaces stale heartbeat/no live runner as an invalidation candidate. |
| `child-lane invalidation` / `codex_exit_1` | Manual invalidation let CO-355 continue after parent `codex_exit_1`. | Invalidation remains a valid workaround. | Parent records actionable diagnostics without treating the stale lane as healthy. |

## Not Done If
- Parent still accepts `codex_exit_1` as proof of useful child-lane progress.
- Child-lane output expands beyond docs and `tasks/index.json` registration in the docs packet.
- Any marketplace logic, model posture, or broad control-host behavior changes are included in this lane.
- A post-startup appserver child lane can remain active-looking with stale heartbeat/no live runner and no clear invalidation-candidate diagnostics.

## Technical Requirements
1. Parent must classify the observed state as runtime regression/classification evidence, not marketplace command logic.
2. Parent must preserve child-lane invalidation and `codex_exit_1` as failure truth, not accepted proof.
3. Parent must keep `CO-224` and `CO-303` boundaries explicit.
4. Parent must leave model posture and broad control-host behavior unchanged unless a separate issue opens that scope.
5. Child lane must only produce this docs packet and `tasks/index.json` registration.

## Validation
- Child lane may run only scoped docs sanity checks.
- Parent owns implementation tests and any scoped Vitest decisions after this packet lands.
