# ACTION_PLAN - CO Add Bounded Pre-Review Automated-Feedback Drain Before In Review

## Added by Bootstrap (refresh as needed)

## Traceability
- Linear issue: `CO-10` / `9d962236-4c38-4b28-b144-007c6f3a1395`
- Linear URL: https://linear.app/asabeko/issue/CO-10/co-add-bounded-pre-review-automated-feedback-drain-before-in-review

## Summary
- Goal: finish `CO-10` by codifying a bounded automated-feedback drain before the provider worker transitions an issue to `In Review`.
- Scope: docs-first packet, one persistent Linear workpad, Symphony baseline audit, pre-implementation docs-review, mode-aware PR monitor changes, provider-worker prompt/skill updates, focused tests, full validation, and review handoff preparation.
- Assumptions:
  - the live team review handoff state is `In Review`
  - the narrowest reusable implementation seam is the shipped PR quiet-window monitor, not a new review poller
  - current Symphony Elixir workflow documents PR feedback sweeps before `Human Review`, but not a separate bounded quiet-window drain, so CO can add this as a truthful repo-local refinement
  - subagent spawning remains unavailable in-session, so delegation must be explicitly overridden

## Milestones & Sequencing
1) Register the docs-first packet for `linear-9d962236-4c38-4b28-b144-007c6f3a1395`, update `tasks/index.json`, update `docs/TASKS.md`, mirror the checklist into `.agent/task/`, and capture the baseline audit.
2) Run docs-review with an explicit delegation override before touching implementation code.
3) Extend the shipped PR quiet-window monitor with a review-handoff mode that:
   - keeps required checks and automated feedback gating
   - ignores `REVIEW_REQUIRED`
   - preserves bounded timeout and action-required exit behavior
4) Add the new `pr` CLI entry point and help/test coverage for the pre-review drain mode.
5) Update the provider-worker prompt and `skills/linear/SKILL.md` so the worker runs the bounded drain after PR open/update and before `In Review`.
6) Run focused regressions, then the required repo validation floor, refresh the workpad, and prepare the PR/review handoff state.

## Dependencies
- `scripts/lib/pr-watch-merge.js`
- `orchestrator/src/cli/prCliShell.ts`
- `orchestrator/src/cli/providerLinearWorkerRunner.ts`
- `skills/linear/SKILL.md`
- `tests/pr-watch-merge.spec.ts`
- `tests/cli-command-surface.spec.ts`
- `orchestrator/tests/ProviderLinearWorkerRunner.test.ts`

## Validation
- Checks / tests:
  - `DELEGATION_GUARD_OVERRIDE_REASON="Provider worker run could not delegate because spawn_agent is unavailable without explicit user authorization in this session." node dist/bin/codex-orchestrator.js start docs-review --format json --no-interactive --task linear-9d962236-4c38-4b28-b144-007c6f3a1395`
  - targeted Vitest for `tests/pr-watch-merge.spec.ts`, `tests/cli-command-surface.spec.ts`, and `orchestrator/tests/ProviderLinearWorkerRunner.test.ts`
  - `DELEGATION_GUARD_OVERRIDE_REASON="Provider worker run could not delegate because spawn_agent is unavailable without explicit user authorization in this session." node scripts/delegation-guard.mjs`
  - `node scripts/spec-guard.mjs --dry-run`
  - `npm run build`
  - `npm run lint`
  - `npm run test`
  - `npm run docs:check`
  - `npm run docs:freshness`
  - `node scripts/diff-budget.mjs`
  - `npm run review`
  - `npm run pack:smoke`
- Rollback plan:
  - if the new pre-review mode regresses merge monitoring, revert the mode-aware monitor change and keep the issue active until a narrower approach is landed
  - if prompt/skill changes prove too broad, fall back to the smallest worker instruction change that still requires the shipped pre-review command

## Risks & Mitigations
- Risk: the new mode accidentally weakens the current merge-monitor contract.
  - Mitigation: keep mode-specific policy isolated and preserve current merge tests/behavior.
- Risk: provider workers still hand off without the bounded drain because the prompt/skill contract stays vague.
  - Mitigation: add an explicit named command and assert for it in the provider-worker prompt tests.
- Risk: the wait becomes too long or indefinite in practice.
  - Mitigation: reuse the existing bounded timeout and quiet-window flags/env behavior.

## Approvals
- Reviewer: pending docs-review
- Date: 2026-03-25

## Manifest Evidence
- Baseline audit: `out/linear-9d962236-4c38-4b28-b144-007c6f3a1395/manual/20260325T070120Z-baseline-audit.md`
- Docs-review manifest: pending
