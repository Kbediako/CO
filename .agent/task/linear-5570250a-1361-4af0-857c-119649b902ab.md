---
id: 20260418-linear-5570250a-1361-4af0-857c-119649b902ab
title: CO: restore same-issue re-admission for reopened terminal released/not_active claims without stale merge_closeout or replay-block suppression
relates_to: docs/PRD-linear-5570250a-1361-4af0-857c-119649b902ab.md
risk: high
owners:
  - Codex
last_review: 2026-04-18
---

## Canonical Reference
- Canonical TECH_SPEC: `tasks/specs/linear-5570250a-1361-4af0-857c-119649b902ab.md`
- TECH_SPEC mirror: `docs/TECH_SPEC-linear-5570250a-1361-4af0-857c-119649b902ab.md`
- PRD: `docs/PRD-linear-5570250a-1361-4af0-857c-119649b902ab.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-5570250a-1361-4af0-857c-119649b902ab.md`
- Task checklist: `tasks/tasks-linear-5570250a-1361-4af0-857c-119649b902ab.md`

## Traceability
- Linear issue: `CO-238` / `5570250a-1361-4af0-857c-119649b902ab`
- Source anchor: `ctx:sha256:1d858a457b5eb91c9daeee7c4422ca5926d9bde1dda0409a099abb0b9acfa930#chunk:c000001`
- Parent-provided source payload reference: `.runs/linear-5570250a-1361-4af0-857c-119649b902ab-co-238-docs/cli/2026-04-18T02-50-07-931Z-b4fa7aed/memory/source-0/source.txt`
- Shared source-0 note: the payload path is referenced by the parent prompt but is not present in this child checkout; the issue statement is therefore anchored on the protected prompt-carried wording plus adjacent repo seams.
- Docs packet child lane manifest: `.runs/linear-5570250a-1361-4af0-857c-119649b902ab-co-238-docs/cli/2026-04-18T02-50-07-931Z-b4fa7aed/manifest.json`

## Summary
- Objective: treat reopened terminal same-issue re-admission as its own bounded seam when retained `provider_issue_released:not_active`, stale `merge_closeout residue`, or release-family `replay-block` would otherwise keep the same issue out of `fresh discovery`.
- Scope:
  - docs-first packet and declared registry mirrors for `CO-238`
  - explicit reopened-terminal admission contract
  - explicit boundaries against `CO-138`, `CO-212`, and `CO-175`
- Constraints:
  - child lane remains docs-only
  - parent owns implementation, validation, docs-review, Linear/workpad reconciliation, PR lifecycle, and merge

## Issue-Shaping Contract
- User-request translation carried forward: this is a reopened-terminal same-issue re-admission lane. When a previously terminal same issue later reopens to runnable truth, stale `provider_issue_released:not_active`, `merge_closeout residue`, or release-family `replay-block` must not keep that same issue permanently excluded.
- Protected terms / exact artifact and surface names:
  - `provider_issue_released:not_active`
  - `reopened terminal claim`
  - `merge_closeout residue`
  - `Ready reclaim`
  - `replay-block`
  - `fresh discovery`
  - `same-issue re-admission`
  - `provider-intake-state.json`
  - `providerIssueHandoff.ts`
  - `providerMergeCloseout.ts`
  - `providerLinearWorkflowStates.ts`
  - `controlRuntime.ts`
  - `CO-138`
  - `CO-212`
  - `CO-175`
- Nearby wrong interpretations to reject:
  - `CO-138` stale merge-closeout cleanup
  - `CO-212` `Ready reclaim`
  - broad `CO-160` / `CO-161` replay-block redesign
  - implicit `CO-175` docs-freshness ownership change

## Parity / Alignment Matrix
- Current truth:
  - retained terminal release or closeout truth can still outrank a later reopened runnable state for the same issue
  - replay-block can keep `fresh discovery` from re-admitting that same issue
- Reference truth:
  - fresher reopened runnable truth should outrank stale retained terminal signals for the same issue
  - replay-block should remain zero-burn for unrelated released claims
- Target truth / intended delta:
  - reopened same issues can re-enter bounded admission when live truth is fresher and no live same-issue worker remains
  - stale retained-release and closeout residue no longer strand that same issue
- Explicitly out-of-scope differences:
  - merge-closeout redesign
  - completed-blocker reclaim redesign
  - docs-freshness owner or policy changes

## Readiness Gate
- Not done if:
  - reopened-terminal suppression is still plausibly just `CO-138` or `CO-212`
  - replay-block loses zero-burn behavior for unrelated claims
  - the packet implies `CO-175` ownership or policy change
- Pre-implementation issue-quality review evidence:
  - 2026-04-18: child-lane review confirmed the issue is broader than any one adjacent lane and narrower than a broad replay-block, merge-closeout, or docs-freshness redesign. The packet must keep exact protected terms and adjacent boundaries intact.

## Technical Requirements
1. Create the `CO-238` docs-first packet and declared registry mirrors.
2. Define the reopened-terminal same-issue re-admission contract.
3. Preserve replay-block for unrelated all-released claims.
4. Preserve explicit separation from `CO-138`, `CO-212`, and `CO-175`.

## Architecture & Data
- Expected implementation surfaces:
  - `orchestrator/src/cli/control/providerIssueHandoff.ts`
  - `orchestrator/src/cli/control/providerMergeCloseout.ts`
  - `orchestrator/src/cli/control/providerLinearWorkflowStates.ts`
  - `orchestrator/src/cli/control/controlRuntime.ts`
- Data constraints:
  - retained terminal history stays auditable
  - no destructive cleanup of provider-intake evidence
  - no docs-freshness ownership changes beyond the declared packet mirrors

## Validation Plan
- Child-lane checks:
  - `node -e "JSON.parse(require('fs').readFileSync('tasks/index.json','utf8'))"`
  - protected-term grep across the packet files
  - `git diff --check` over the touched files only
- Parent-lane checks:
  - focused provider handoff coverage for reopened-terminal re-admission
  - focused replay-block preservation coverage
  - focused runtime/status parity only if reopened admission truth is also projected there
  - parent-owned docs-review and spec-guard after patch import

## Open Questions
- Should the reopened-terminal allowance live in a dedicated same-issue re-admission helper or inside existing replay-block evaluation?
- Which stale closeout signals need direct comparison with live reopened truth?

## Approvals
- Docs-first packet: same-issue docs child lane `.runs/linear-5570250a-1361-4af0-857c-119649b902ab-co-238-docs/cli/2026-04-18T02-50-07-931Z-b4fa7aed/manifest.json`
- Parent integration note: child acceptance failed closed on `provider_worker_child_lane_stale` after the issue `updated_at` advanced; parent manually imported the docs-only patch after confirming the scope stayed within the declared docs files.
- Parent validation status: `npm run build`, `npm run test`, `npm run docs:check`, `npm run repo:stewardship`, targeted reclaim/runtime regressions, `node scripts/spec-guard.mjs --dry-run`, and `node scripts/delegation-guard.mjs` are green; `npm run docs:freshness` remains red on pre-existing repo backlog (`rolling cohort CO-175`, `stale docs: 70`).
- Parent review status: one wrapper run completed with `status=succeeded` / `review_outcome=bounded-success`; the follow-up wrapper rerun failed at the startup-anchor boundary, so parent completed the required manual correctness review plus explicit elegance pass and found no remaining actionable issues after closing the direct refresh-path guard gap.
- Date: 2026-04-18
