# Task Checklist Mirror - linear-6ef38dcf-eee6-4e2a-a261-179a040b52f3

Canonical checklist: `tasks/tasks-linear-6ef38dcf-eee6-4e2a-a261-179a040b52f3.md`.

## Current Status
- [x] Docs-only child lane created the six-file docs-first packet for `CO-305`.
- [x] Packet scope stays on the singleton overwrite seam around `provider-linear-issue-context-cache.json`, `issue-context`, run-scoped artifact truth, cross-issue reads, and parent issue evidence.
- [x] Packet records that the provided source pointers are provenance-only in this checkout and that the substantive issue contract comes from the parent-owned live `linear issue-context` read and workpad.
- [x] Scoped child-lane checks are complete for protected-term coverage and `git diff --check`.
- [ ] Parent implementation and focused regression coverage are pending.

## Guardrails
- Preserve exact terms: `provider-linear-issue-context-cache.json`, `issue-context`, `run-scoped artifact`, `cross-issue reads`, `CO-301`, `CO-295`, `CO-299`, `CO-302`, `parent issue evidence`, `docs-first packet`.
- Reject widenings into generic Linear truth, PR attachment ownership, stale-blocker reconcile redesign, or broad provider-worker issue-context rewrites.
- Not done if a later cross-issue read can still overwrite the same apparent parent-authoritative path, or if downstream docs/task packets can still cite a parent cache path that now contains a different issue body.
