# Agent Task Mirror - linear-7b8e48c8-3971-42ab-b779-949d810d4e6c

- Linear Issue: `CO-457` / `7b8e48c8-3971-42ab-b779-949d810d4e6c`
- Source Issue: `CO-352` / `f4469614-cfdf-49a6-a7ff-366f58229816`
- Title: Provider-worker resolved model provenance
- PRD: `docs/PRD-linear-7b8e48c8-3971-42ab-b779-949d810d4e6c.md`
- TECH_SPEC: `docs/TECH_SPEC-linear-7b8e48c8-3971-42ab-b779-949d810d4e6c.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-7b8e48c8-3971-42ab-b779-949d810d4e6c.md`
- Checklist: `tasks/tasks-linear-7b8e48c8-3971-42ab-b779-949d810d4e6c.md`

## Current Truth
- Provider-worker launches normally omit `--model` and inherit host config.
- Active Codex profile config is part of inherited config truth when `--profile`, `CODEX_PROFILE`, root `profile = "..."`, or injected `-c profile=...` selects a profile.
- Current CO-local ChatGPT-auth/appserver posture is `gpt-5.5` / `xhigh` when available.
- Portable packaged/generated fallback posture remains `gpt-5.4` / `xhigh`.
- Implementation adds structured resolved model provenance to `provider-linear-worker-proof.json` and preserves or backfills non-null provenance through matching `provider-linear-worker-session-log-hydration.json` state without re-signing stale hydration provenance.
- `CO STATUS` terminal frames plus read-model payloads expose model provenance or degraded/unknown evidence explicitly from provider-worker proof, including selected-run refresh backfill for older proofs.
- Packet setup clears `backlog_head_follow_up_traceability_pending`.

## Guardrails
- Do not change current model defaults.
- Do not make portable templates default to `gpt-5.5`.
- Do not report `gpt-5.5` as runtime-proven when evidence is config-derived.
- Do not claim root config defaults when an active profile is selected but no matching profile table exists.
- Do not treat binary provenance (`CO-450`) or auth provenance (`CO-451`) as equivalent to resolved model provenance.
- Do not block provider-worker launch solely because Codex did not emit a model slug when truthful degraded proof can be written.

## CO-382 Fallback Decision Table

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Provider-worker resolved model provenance | Degraded proof path for missing runtime model or effort metadata | expire fallback | CO-457 | Codex JSONL omits runtime model metadata from provider-worker `codex exec --json` or resume output | 2026-05-05 | 2026-05-19 | 2026-06-04 | Codex runtime always emits authoritative model/reasoning metadata for provider-worker runs, or CO adopts a stronger proof source | Focused runtime-reported, inherited-config, command override, unknown/degraded, hydration, and read-model projection tests |

- Large refactor decision: no large refactor is justified; this remains a narrow proof/hydration/status projection extension under existing provider-worker authority.
- Minor seam decision: the temporary degraded-provenance seam is acceptable only with source, confidence, degraded reason, expiry metadata, and focused validation.

## Not Done If
- Operators still infer model truth from `~/.codex/config.toml`.
- Proof reports a model without source/confidence.
- CO STATUS or read-model hides missing provenance as authoritative.
- Current model defaults change.
- `CO-450` or `CO-451` scope is absorbed.

## Validation Handoff
- Packet setup:
  - PRD, TECH_SPEC mirror, ACTION_PLAN, canonical task spec, task checklist, and agent mirror
  - `tasks/index.json` registration
  - `docs/TASKS.md` snapshot
  - `docs/docs-freshness-registry.json` rows
- Parent-owned implementation:
  - proof/hydration provenance object: implemented
  - CO STATUS/read-model projection and terminal rendering: implemented
  - inherited `gpt-5.5`, active profile config from env/default profile/config override, signed hydration backfill, stale hydration rejection, skip-hydration re-sign rejection, projection refresh, explicit override, terminal CO STATUS rendering, and missing-runtime-model degraded tests: implemented

## Evidence
- Protected model terms: `gpt-5.5`, `gpt-5.4`, `model_reasoning_effort`.
- Protected artifact terms: `provider-linear-worker-proof.json`, `provider-linear-worker-session-log-hydration.json`.
- Protected command terms: `codex exec --json`, `codex exec resume --json`, `--model`, `--profile`, `CODEX_PROFILE`, root `profile = "..."`, injected `-c profile=...`.
- Protected projection terms: resolved model provenance, `CO STATUS`, read-model.
