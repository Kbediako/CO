# Agent Task Mirror: CO-403 provider-worker retry recovery for completed child lanes and stale proof locks

## Context
- MCP Task ID: linear-c8e3a464-ec50-4fa4-aff5-d8a780626600
- Primary PRD: docs/PRD-linear-c8e3a464-ec50-4fa4-aff5-d8a780626600.md
- TECH_SPEC: tasks/specs/linear-c8e3a464-ec50-4fa4-aff5-d8a780626600.md
- ACTION_PLAN: docs/ACTION_PLAN-linear-c8e3a464-ec50-4fa4-aff5-d8a780626600.md
- Canonical task checklist: tasks/tasks-linear-c8e3a464-ec50-4fa4-aff5-d8a780626600.md

## Intent Checksum
- Preserve: `parallelize_now`, `parallelization_launch_missing`, `provider-linear-worker-proof.json.lock`, `provider-linear-worker-proof.json`, `co-status`, `providerLinearWorkerRunner.ts`, `withProviderLinearWorkerProofLock`, `PROVIDER_LINEAR_WORKER_PROOF_LOCK_RETRY`, `orchestrator/src/persistence/lockFile.ts`, same-issue child lane, pending parent acceptance, stale orphaned proof lock.
- Reject: disabling enforcement, duplicate no-op child lanes, unrelated/older child-lane attribution, manual lock deletion, manual proof JSON edits, stale snapshot masking, and CO-400 current-state authority scope creep.

## Evidence Gates
- [x] Issue-quality review captured - Evidence: scoped TECH_SPEC carries user-request translation, protected terms, wrong interpretations, non-goals, parity matrix, and `Not Done If` clauses.
- [x] Fallback / refactor decision captured - Evidence: scoped TECH_SPEC decision table removes stale attempt-local proof and stale projection behavior, and justifies stale-owner proof-lock recovery as durable lock correctness.
- [x] Durable retention evidence captured - Evidence: scoped TECH_SPEC defines `provider proof lock stale recovery` with explicit stale timeout, live-lock keepalive, and concurrent writer serialization tests.
- [ ] Standalone review approval captured - Parent lane owned.
- [ ] Docs-review manifest captured - Parent lane owned.
- [ ] Implementation review manifest captured - Parent lane owned.

## Parent Implementation Checklist
1. [ ] Recover completed same-issue child-lane proof for the active `parallelize_now` decision when retrying before parent acceptance.
2. [ ] Keep `parallelization_launch_missing` fail-closed for no launch, failed child, unrelated child, older child, and lineage mismatch.
3. [ ] Recover stale orphaned `provider-linear-worker-proof.json.lock` files only after the configured stale timeout while preserving live-lock keepalive.
4. [ ] Preserve exclusive proof writes so concurrent writers cannot corrupt `provider-linear-worker-proof.json`.
5. [ ] Show recovered child-lane and stale-lock state truthfully in proof and `co-status` surfaces.
6. [ ] Add focused CO-400-shaped retry, negative lineage, stale-lock, concurrent writer, and proof/status tests.

## Child Lane Notes
- This mirror was created by a docs-only same-issue child lane.
- No Linear/GitHub/PR lifecycle commands were run or inspected.
- No source code or tests were changed.
