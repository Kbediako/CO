---
id: 20260328-linear-9b2a195b-4603-4ed4-98c6-20eff87049e4
title: Fix Active Provider-Worker Linear Write-Back When Reads Succeed
status: in_progress
owner: Codex
created: 2026-03-28
last_review: 2026-04-28
review_cadence_days: 30
risk_level: high
related_prd: docs/PRD-linear-9b2a195b-4603-4ed4-98c6-20eff87049e4.md
related_action_plan: docs/ACTION_PLAN-linear-9b2a195b-4603-4ed4-98c6-20eff87049e4.md
related_tasks:
  - tasks/tasks-linear-9b2a195b-4603-4ed4-98c6-20eff87049e4.md
review_notes:
  - 2026-03-28: Opened from Linear issue `CO-33` after rechecking the live workflow states with `linear issue-context`; the live CO team exposes `In Progress`, `In Review`, `Merging`, and `Rework`, and the issue started this lane in `Ready` with no attached PR or workpad comment.
  - 2026-03-28: The workspace started detached at `42a2f295af6844b1dd195093ae959e42b45c6ce3` (`origin/main`) and moved onto branch `co-33-fix-provider-linear-write-back` before repo edits.
  - 2026-03-28: Live provider-worker evidence from `/Users/kbediako/Code/CO/.runs/linear-4646e08c-5c53-4072-b09e-ec1aeff6fb21/cli/2026-03-28T09-08-12-918Z-060f9d7e/provider-linear-worker-linear-audit.jsonl` shows the exact seam: `issue-context` eventually succeeds, then `transition` and `upsert-workpad` fail repeatedly with `linear_request_failed`, and later succeed after the hourly reset window.
  - 2026-03-28: Raw upstream reproduction at `2026-03-28T11:36:21Z` shows the underlying failure is not transport or auth drift. Linear returns GraphQL `RATELIMITED` on HTTP `400` with `retry-after: 3600`, `x-ratelimit-requests-remaining: 0`, and `x-ratelimit-requests-reset: 1774701380970`.
  - 2026-03-28: Pre-implementation review approves the narrow path: expose rate-limit failures truthfully, reuse successful run-scoped `issue-context` data for immediate `transition` / `upsert-workpad` calls, avoid global caching, and add focused regressions for the read-succeeds / write-fails seam.
  - 2026-03-28: Docs-review succeeded via `/Users/kbediako/Code/CO/.runs/linear-9b2a195b-4603-4ed4-98c6-20eff87049e4/cli/2026-03-28T11-44-12-656Z-a99f3693/manifest.json`.
  - 2026-03-28: Standalone review against the intermediate diff found a real cache regression on `delete-workpad`: the new run-scoped cache left deleted workpad comments selected for later upserts. The lane now patches cache invalidation after delete and adds a focused recreate-after-delete regression before the final review rerun.
  - 2026-03-28: The final wrapper-led review rerun stalled in speculative cache analysis without a bounded verdict, so the lane used the documented manual correctness + elegance fallback and recorded the outcome in `out/linear-9b2a195b-4603-4ed4-98c6-20eff87049e4/manual/review-fallback.md`.
---

# Technical Specification

## Context

`CO-33` is a bounded provider-worker workflow repair lane. The live `CO-31` provider-worker run already proved the operator/control-host surfaces can show an issue as active while Linear still shows `Ready` because the active-lane write-back path burns its request budget on redundant preflight reads and then collapses the real rate-limit failure into an opaque `linear_request_failed` error. The repair lane must keep scope limited to active provider-worker state transitions and workpad updates.

## Requirements

1. Classify Linear rate-limit responses explicitly instead of collapsing them into generic request failures.
2. Reuse a successful run-scoped `issue-context` result for immediately-following `transition` and `upsert-workpad` calls in the same provider-worker attempt.
3. Update the cached state/workpad comment on successful mutations so the same attempt can continue without another live preflight read.
4. Preserve live-read fallback behavior when no trusted run-scoped cache exists.
5. Add focused regressions covering the read-succeeds / write-fails seam for both `transition` and `upsert-workpad`.
6. Close the lane with an explicit root-cause classification: Linear hourly rate-limit exhaustion plus redundant mutation preflights, not auth or malformed mutation contracts.

## Current Truth

- `origin/main` at `42a2f295af6844b1dd195093ae959e42b45c6ce3` already contains the upstream `CO-31` patch set, so this lane focuses strictly on the provider-worker Linear write-back contract.
- The current issue moved from `Ready` to `In Progress` successfully with the packaged helper, proving the mutation path is not permanently broken.
- A same-run concurrent `issue-context` + `upsert-workpad` attempt reproduced the opaque failure locally, and a direct raw fetch confirmed Linear request-budget exhaustion as the real cause.
- The current reset window from the raw Linear headers lands at `2026-03-28T12:36:20.970Z`, so live mutation verification may remain blocked until that hour rolls over.

## Validation Plan

- Run docs-review for `linear-9b2a195b-4603-4ed4-98c6-20eff87049e4` before implementation handoff.
- Capture raw rate-limit evidence and closeout notes under `out/linear-9b2a195b-4603-4ed4-98c6-20eff87049e4/manual/`.
- Add focused tests in `orchestrator/tests/LinearGraphqlClient.test.ts` and `orchestrator/tests/ProviderLinearWorkflowFacade.test.ts`.
- Run the required validation floor after implementation.
- Capture standalone review and explicit elegance review before review handoff.
