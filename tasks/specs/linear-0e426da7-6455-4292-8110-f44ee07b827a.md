---
id: 20260418-linear-0e426da7-6455-4292-8110-f44ee07b827a
title: CO child-lane lineage preserve parent provenance across manual provider-worker launch and issue workspace rebinding
status: in_progress
relates_to: docs/PRD-linear-0e426da7-6455-4292-8110-f44ee07b827a.md
risk: high
owners:
  - Codex
last_review: 2026-06-17
review_notes:
  - 2026-06-17: CO-579 pre-expiry review kept this spec active-current; no verified terminal/archive evidence was established in this stream, CO-579 is the live non-terminal docs-freshness owner, and docs/spec gates remain unchanged.
  - 2026-05-18: CO-522 active-spec audit found 11 unchecked task checklist items, so this spec remains active and was reviewed for current lifecycle ownership rather than archived. Evidence: `out/linear-b642e879-ba50-45ef-b0d9-b059afa9e932-recovery/spec-preexpiry-local-classification.json`.
---

## Canonical Reference
- Canonical TECH_SPEC: `tasks/specs/linear-0e426da7-6455-4292-8110-f44ee07b827a.md`
- TECH_SPEC mirror: `docs/TECH_SPEC-linear-0e426da7-6455-4292-8110-f44ee07b827a.md`
- PRD: `docs/PRD-linear-0e426da7-6455-4292-8110-f44ee07b827a.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-0e426da7-6455-4292-8110-f44ee07b827a.md`
- Task checklist: `tasks/tasks-linear-0e426da7-6455-4292-8110-f44ee07b827a.md`

## Traceability
- Linear issue: `CO-245` / `0e426da7-6455-4292-8110-f44ee07b827a`
- Source anchor: `ctx:sha256:a705905f842ad361de1c5f68a088b5f4210340f7b8dd1105544184664a80283e#chunk:c000001`
- Docs packet child lane: `.runs/linear-0e426da7-6455-4292-8110-f44ee07b827a-co245-docs-packet/cli/2026-04-18T08-07-11-555Z-d63daa80/manifest.json`
- Shared source note: the expected shared source payload is absent in this child checkout, so the packet is anchored on the bounded lane prompt and current repo seam names only.

## Summary
- Objective: make `manual provider-worker launch`, `same-issue child lane`, `parent provenance gate`, `older canonical parent manifest`, `parent_run_id`, `issue workspace rebinding`, and `current-run lineage attribution` resolve to one current authoritative lineage contract.
- Scope:
  - docs-first packet and registry/checklist mirrors for `CO-245`
  - one parent-owned lineage/provenance contract
  - focused parent validation only
- Constraints:
  - preserve exact issue wording
  - preserve fail-closed provenance behavior
  - preserve distinction from `CO-241` and `CO-244`
  - do not widen into generic workspace-root or artifact-root parity redesign

## Issue-Shaping Contract
- User-request translation carried forward: this lane is about lineage/provenance correctness where manual provider-worker launch and same-issue child lanes must remain attributable after issue-workspace rebinding. It is not a generic issue-worktree authority lane and not a generic artifact-root parity lane.
- Protected terms / exact artifact and surface names:
  - `manual provider-worker launch`
  - `same-issue child lane`
  - `parent provenance gate`
  - `older canonical parent manifest`
  - `parent_run_id`
  - `issue workspace rebinding`
  - `current-run lineage attribution`
  - `tests/delegation-guard.spec.ts`
  - `tests/run-review.spec.ts`
  - `providerLinearChildLaneShell.ts`
  - `providerLinearChildLaneRunner.ts`
  - `source0.ts`
  - `manifest.ts`
- Nearby wrong interpretations to reject:
  - generic issue-worktree authority work
  - generic artifact-root parity work
  - weakening the `parent provenance gate`
  - letting `older canonical parent manifest` replace missing current identity
  - treating rebinding as the whole issue without lineage consequences
  - treating this as duplicate `CO-241` or `CO-244`
- Explicit non-goals carried forward:
  - no implementation or test edits in this child lane
  - no generic root/layout redesign
  - no provenance-gate weakening
  - no Linear mutation or PR handling from this child lane

## Parity / Alignment Matrix
- Current truth:
  - provenance tests already allow bounded stale-path and stale-locator fallback
  - rebinding tests already allow artifact-path correction into the issue workspace
  - `parent_run_id` already exists in lineage-carrying artifacts
  - current lineage can still drift when provenance, rebinding, and projection use different fallback rules
- Reference truth:
  - launch provenance remains the first authority gate
  - `parent_run_id` remains the primary lineage key
  - rebinding corrects paths without changing logical lineage
  - older canonical parent state is a bounded backstop only
- Target truth / intended delta:
  - one shared lineage contract decides current parent/child truth across provenance, rebinding, and projection surfaces
  - real same-issue child lineage survives rebinding/path drift when current lineage still matches
  - stale path/layout residue does not outrank real current lineage
- Explicitly out-of-scope differences:
  - generic issue-worktree authority
  - generic artifact-root parity
  - broader review-wrapper redesign
  - broader delegation-gate weakening

## Readiness Gate
- Not done if:
  - `manual provider-worker launch` still passes without real provenance
  - `same-issue child lane` lineage still breaks after rebinding
  - `older canonical parent manifest` still overrides missing `parent_run_id` or missing provenance
  - `current-run lineage attribution` still diverges across provenance, rebinding, and projection surfaces
  - the lane is no longer clearly distinct from `CO-241` and `CO-244`
- Pre-implementation issue-quality review evidence:
  - 2026-04-18: this child lane accepted the wider lineage/provenance framing from the bounded handoff because correctness depends on exact wording across provenance, rebinding, canonical parent fallback, and projection. `tests/delegation-guard.spec.ts` and `tests/run-review.spec.ts` prove the seam split already exists in the repo, so the issue is not plausibly narrower than a shared lineage contract and is not a duplicate of generic workspace-root or artifact-root parity work.
  - 2026-04-18: the micro-task path is ineligible because correctness depends on exact wording, explicit non-goals, explicit not-done-if conditions, and the parity matrix before implementation starts.
- Safeguard ownership split:
  - child lane owns only the declared docs/checklist/registry files
  - parent lane owns source/test inspection, implementation, docs-review, validation, Linear/workpad reconciliation, PR lifecycle, and merge

## Technical Requirements
- Functional requirements:
  1. Create the docs-first packet and registry/checklist mirrors for `CO-245`.
  2. Preserve fail-closed `manual provider-worker launch` behavior when launch provenance is absent.
  3. Require `parent_run_id` and issue lineage as the primary parent/child identity across provenance and child-lane surfaces.
  4. Allow `older canonical parent manifest` only as a bounded backstop for stale path/layout drift after current identity is established.
  5. Keep `issue workspace rebinding` limited to artifact-path correction, not logical lineage mutation.
  6. Apply the same current-lineage rule to source-0 and projection/read surfaces.
  7. Keep the lane explicitly separate from `CO-241` and `CO-244`.
- Non-functional requirements:
  - deterministic metadata-safe lineage matching
  - fail-closed on missing provenance or ambiguous lineage
  - additive only; no broad artifact-root rewrite
  - auditable lineage in manifest and source-0 artifacts
- Interfaces / contracts:
  - `tests/delegation-guard.spec.ts`
  - `tests/run-review.spec.ts`
  - `orchestrator/src/cli/providerLinearChildLaneShell.ts`
  - `orchestrator/src/cli/providerLinearChildLaneRunner.ts`
  - `orchestrator/src/cli/run/source0.ts`
  - `orchestrator/src/cli/run/manifest.ts`
  - `orchestrator/src/cli/controlHostCliShell.ts`
  - `orchestrator/src/cli/control/selectedRunProjection.ts`

## Architecture & Data
- Architecture / design adjustments:
  - prefer one shared provenance/lineage helper or rule ordering reused across provenance and projection seams
  - require launch provenance first, then current identity via `parent_run_id` and issue lineage, then consult `older canonical parent manifest` only as a bounded backstop
  - keep rebinding path correction separate from logical lineage decisions
  - make source-0 and projection surfaces consumers of the same authoritative lineage rule
- Data model changes / migrations:
  - additive metadata or helper reuse is acceptable
  - no schema migration or broad root-layout rewrite is required
  - no deletion of historical manifest or source-0 artifacts is required
- External dependencies / integrations:
  - no Linear mutation from this child lane
  - no parent implementation details are changed here

## Current Truth
- Provenance coverage already lives in `tests/delegation-guard.spec.ts`.
- Rebinding coverage already lives in `tests/run-review.spec.ts`.
- `providerLinearChildLaneRunner.ts`, `run/manifest.ts`, and `run/source0.ts` already carry `parent_run_id`.
- Current lineage can still drift when one surface trusts stale paths more than current metadata.

## Proposed Design
- Introduce or reuse one shared lineage contract that:
  - preserves fail-closed launch provenance
  - treats `parent_run_id` and issue lineage as the primary identity
  - uses `older canonical parent manifest` only as a bounded backstop
  - lets rebinding rewrite paths without changing lineage
  - lets projection/read surfaces reuse the same current-lineage decision

## Protected Expectations
- Preserve exact issue wording around `manual provider-worker launch`, `same-issue child lane`, `parent provenance gate`, `older canonical parent manifest`, `parent_run_id`, `issue workspace rebinding`, and `current-run lineage attribution`.
- Preserve fail-closed launch-provenance behavior.
- Preserve `parent_run_id` as the primary lineage link.
- Preserve explicit distinction from `CO-241` and `CO-244`.

## Reject These Wrong Interpretations
- `This is only issue-worktree authority.`
- `This is only artifact-root parity.`
- `Older canonical parent manifest can replace missing provenance.`
- `Current-run lineage attribution can stay projection-only.`
- `Path-only matching is good enough for same-issue child lanes.`

## Validation Plan
- Child-lane checks:
  - JSON parse of `tasks/index.json` and `docs/docs-freshness-registry.json`
  - protected-term grep across the six packet/mirror files
  - `git diff --check` over the declared docs scope
- Parent-lane checks:
  - focused `tests/delegation-guard.spec.ts` regressions for provenance, stale-path fallback, canonical parent backstop, and fail-closed missing-provenance behavior
  - focused `tests/run-review.spec.ts` regressions for issue-workspace rebinding with and without workspace counterparts
  - focused lineage/projection regressions if the final seam touches `providerLinearChildLaneShell.ts`, `source0.ts`, or `selectedRunProjection.ts`
  - parent docs-review before implementation
  - parent-selected scoped validation after source edits

## Approvals
- Reviewer: pending parent docs-review / implementation
- Date: 2026-04-18
