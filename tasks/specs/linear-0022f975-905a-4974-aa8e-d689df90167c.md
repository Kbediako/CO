---
id: 20260414-linear-0022f975-905a-4974-aa8e-d689df90167c
title: CO: reconcile Done issue mirrors and validation-only closeout provenance
status: done
owner: Codex
created: 2026-04-14
last_review: 2026-05-16
review_cadence_days: 30
risk_level: high
related_prd: docs/PRD-linear-0022f975-905a-4974-aa8e-d689df90167c.md
related_action_plan: docs/ACTION_PLAN-linear-0022f975-905a-4974-aa8e-d689df90167c.md
related_tasks:
  - tasks/tasks-linear-0022f975-905a-4974-aa8e-d689df90167c.md
review_notes:
  - 2026-04-14: Provider-worker parent created the docs-first packet after live issue-context showed CO-178 in Ready and the issue was transitioned to In Progress.
  - 2026-04-14: Issue-quality review approved the boundary as terminal Done closeout provenance and prevention, not functional reopening or CO-172 manifest-summary duplication.
  - 2026-04-14: Implementation classified six sampled implementation issues as stale mirror only, backfilled a local CO-170 validation-only closeout pointer, and added a read-only checker with hash-bound stale-mirror waivers.
  - 2026-04-14: Manifest-backed review findings were resolved for Windows drive-qualified path rejection, report status truthfulness, completed PR-attachment prose, and whitespace-normalized validation-only classification; final review telemetry ended clean-success and the elegance pass retained the minimal checker/manifest/docs/test shape.
  - 2026-05-16: CO-545 strict spec-guard audit reclassified this stale Apr 14/15 row as inactive done; live `node bin/codex-orchestrator.js linear issue-context --issue-id 0022f975-905a-4974-aa8e-d689df90167c --format json` verified CO-178 is Linear Done/completed and attached/related PR evidence https://github.com/Kbediako/CO/pull/476. No completed_at was inferred or fabricated.
---

# Technical Specification

## Context

CO-178 targets stale terminal provenance for completed CO issues. The issue text identifies Done issues with merged green PRs whose local task/docs mirrors still imply pending implementation, review, PR attachment, or validation work. It also identifies no-PR validation-only Done issues where the closeout may be legitimate but local provenance is weak.

The implementation must distinguish stale mirror debt from true false-Done gaps. A stale local checklist row is not itself proof that implementation failed; the checker and reconciliation must cite Linear, GitHub, and local artifact evidence before changing mirror truth or recording a waiver.

## Requirements

1. Enumerate sampled Done issue evidence:
   - include `CO-92`, `CO-94`, `CO-78`, `CO-89`, `CO-43`, and `CO-62`;
   - include no-PR validation-only samples such as `CO-170` when evidence exists;
   - record local mirror paths, terminal pending rows, Linear state evidence, PR/closeout evidence, and classification.
2. Classify each sampled issue:
   - `stale_mirror_only`: Done and terminal evidence exists, but local mirror rows are stale or misleading;
   - `validation_only_provenance_gap`: no PR expected, but a local closeout pointer must be recorded;
   - `true_follow_up_needed`: fresh evidence shows functional or provenance work remains outside this lane.
3. Reconcile sampled stale mirrors without deleting audit history:
   - mark terminal rows complete when current evidence supports completion;
   - or add an explicit stale-mirror waiver with issue id, file, line/context, reason, evidence pointer, and review date;
   - preserve older validation and closeout notes.
4. Add a read-only checker:
   - consumes a checked-in provenance manifest and local markdown mirrors;
   - fails or reports unwaived terminal pending rows in Done issue mirrors;
   - requires local closeout pointer evidence for no-PR validation-only Done entries;
   - emits machine-readable JSON for CI/agent use;
   - makes no live Linear/GitHub mutations or API calls.
5. Document future closeout behavior:
   - after provider-worker merge completion, update terminal mirrors and provenance manifest before Done;
   - keep `CO-172` manifest-summary truthfulness out of this checker.

## Issue-Shaping Contract
- User-request translation carried forward: reconcile terminal Done closeout provenance and local mirror truth for completed CO issues, then add a read-only prevention checker.
- Protected terms / exact artifact and surface names: `Linear Done`, `docs/TASKS.md`, `task mirrors`, `.agent/task mirrors`, `tasks/index.json`, `Linear workpad`, `PR attachment`, `merged PR evidence`, `no-PR validation-only closeout`, `out/<linear-id>/manual/workpad.md`, `stale mirror waiver`.
- Nearby wrong interpretations to reject: only cleaning `CO-92` and `CO-94`; using stale local rows as proof of failed implementation; reopening completed implementation scope without fresh repro evidence; weakening docs freshness or archive policy.
- Explicit non-goals carried forward: no functional reopen without repro; no duplicate `CO-172` manifest-summary truthfulness work; no deletion of useful historical evidence; no requirement that every validation-only Done issue have a PR.

## Parity / Alignment Matrix

| Surface | Current truth | Reference truth | Target truth |
| --- | --- | --- | --- |
| Sampled Done implementation issues | Local mirrors can carry stale pending rows after Linear Done and merged PR closeout. | Terminal issue mirrors should match Done/PR evidence or explain why they intentionally do not. | Sampled mirrors are reconciled or carry explicit stale-mirror waivers. |
| Validation-only Done issues | Some Done issues legitimately have no PR but weak local evidence. | No-PR Done closeouts need local provenance pointers. | Manifest entries require `out/<linear-id>/manual/workpad.md` or equivalent pointer evidence. |
| Prevention | No read-only gate catches pending terminal rows across Done issue mirrors. | Agents/CI need a local checker that cannot silently mutate state. | Checker reports/fails unwaived pending terminal rows and missing no-PR closeout pointers. |
| CO-172 boundary | Manifest summary truthfulness is handled by CO-172. | This lane should not duplicate that work. | Checker focuses on mirror/provenance rows and references CO-172 as out of scope. |

## Readiness Gate
- Not done if: any sampled Done issue has unwaived pending terminal rows; no-PR validation-only samples depend only on Linear comments; the checker needs live APIs; or classification treats stale mirror rows as functional failure without corroborating evidence.
- Pre-implementation issue-quality review evidence: provider-worker parent reviewed the issue text on 2026-04-14 and preserved the full scope, protected terms, wrong interpretations, non-goals, and acceptance criteria in the PRD/spec/action plan.
- Safeguard ownership split: parent owns docs/checker implementation, mirror reconciliation, Linear workpad, PR lifecycle, validation, and final handoff. The `sample-audit` child lane was attempted for bounded local audit evidence, completed successfully, and was rejected because it produced an empty patch/no report; the parent owns the remaining evidence inventory locally.

## Technical Requirements
- Functional requirements: provenance manifest, read-only scanner, terminal pending-row detection, stale waiver handling, no-PR closeout pointer validation, JSON output, focused tests, sampled reconciliation.
- Non-functional requirements: deterministic local/CI behavior, no network calls in the checker, no destructive edits by the checker, stable output schema, and high-signal error messages with file/line evidence.
- Interfaces / contracts: a CLI or npm script that accepts a manifest path, optional output path, and report/check mode; a manifest schema with issue id, identifier, classification, state, mirror paths, evidence pointers, and waivers.

## Architecture & Data
- Architecture / design adjustments: add a small read-only script under `scripts/`, a sampled provenance manifest under `docs/` or `tasks/`, and focused tests under the existing test harness.
- Data model changes / migrations: no runtime data migration. The provenance manifest is a documentation/checker input, not application state.
- External dependencies / integrations: local filesystem only for checker execution. Live Linear/GitHub evidence can inform the manifest during implementation but is not required at checker runtime.

## Validation Plan
- Tests / checks: focused tests for clean sampled manifests, unwaived pending terminal rows, waived stale rows, missing no-PR local pointer, and JSON report shape; then required repo validation gates.
- Rollout verification: run the checker against the CO-178 sampled manifest and record the output path under `out/linear-0022f975-905a-4974-aa8e-d689df90167c/`.
- Monitoring / alerts: future agents can run the checker before Done/review handoff and cite the JSON output in workpads.

## Open Questions
- Resolved 2026-04-14: sampled stale implementation mirrors are reconciled through explicit hash-bound stale-mirror waivers in `docs/done-closeout-provenance.json` rather than editing historical rows in place.
- Resolved 2026-04-14: `CO-170` is retained as a validation-only no-PR closeout and backfilled with `docs/closeout-provenance/linear-ae5b2b98-f03e-44fd-9c13-a4d58457d8cb.md`; no functional reopen is warranted by the available evidence.

## Approvals
- Reviewer: docs-review r4 clean before implementation; manifest-backed standalone implementation review clean after P2 fixes; explicit elegance/minimality pass complete.
- Date: 2026-04-14
