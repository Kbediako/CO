---
id: 20260416-linear-2886b57d-b844-4803-9e55-6fe3657d516d
title: CO: Add Codex 0.121 MCP Apps metadata to delegation tools without authority expansion
status: done
relates_to: docs/PRD-linear-2886b57d-b844-4803-9e55-6fe3657d516d.md
risk: high
owners:
  - Codex
last_review: 2026-05-17
review_notes:
  - 2026-05-17: CO-543 strict spec-guard audit reclassified this stale Apr 16 row inactive done; live Linear evidence verified CO-197 is Done/completed. No completed_at was inferred or fabricated.
---

## CO-382 Fallback Decision Table
- Large-refactor check: no new fallback mechanism or guard split is warranted; CO-543 removes lifecycle metadata drift while keeping the existing strict spec-guard and docs-freshness ownership surfaces authoritative.
- Minor-seam decision: the bounded seam is acceptable because completed packet rows are reclassified inactive or archived with evidence, and future active specs surface through the existing docs-freshness-maintain owner-action route before hard expiry.

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Active spec lifecycle freshness | Completed task/spec packet rows remained active and recurred as stale spec-guard debt | remove fallback | CO-543 | Apr 16 terminal packet rows reached the 30-day strict spec-guard boundary | 2026-04-16 | 2026-05-17 | N/A after removal | Completed rows are inactive done; related terminal packet registry rows are archived; future active specs surface pre-expiry owner action | node scripts/spec-guard.mjs; npm run docs:check; focused docs-freshness/spec-guard tests; diff-budget override |

## Canonical Reference
- Canonical TECH_SPEC: `tasks/specs/linear-2886b57d-b844-4803-9e55-6fe3657d516d.md`
- PRD: `docs/PRD-linear-2886b57d-b844-4803-9e55-6fe3657d516d.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-2886b57d-b844-4803-9e55-6fe3657d516d.md`
- Task checklist: `tasks/tasks-linear-2886b57d-b844-4803-9e55-6fe3657d516d.md`

## Traceability
- Linear issue: `CO-197` / `2886b57d-b844-4803-9e55-6fe3657d516d`
- Linear URL: https://linear.app/asabeko/issue/CO-197
- Source anchor: `ctx:sha256:2530a62a2d3bc7e9c618145eacee0f6812e93c669766132aa4f049961e939d0c#chunk:c000001`
- Parent run context: `.runs/linear-2886b57d-b844-4803-9e55-6fe3657d516d/cli/2026-04-16T02-11-44-691Z-26f7f8c0/manifest.json`
- Rejected docs child-lane evidence: `.runs/linear-2886b57d-b844-4803-9e55-6fe3657d516d-co197-docs-metadata/cli/2026-04-16T02-15-09-423Z-e92d3ec3/manifest.json`

## Summary
- Objective: add supported MCP Apps metadata to CO delegation MCP tools so Codex 0.121 app-server/operator surfaces can classify tool risk without changing authority.
- Scope:
  - `orchestrator/src/cli/delegationServerToolDispatchShell.ts`
  - delegation MCP `initialize`, `tools/list`, and `tools/call` tests in `orchestrator/tests/DelegationServer.test.ts`
  - docs-first packet and registry files for this issue
- Constraints:
  - no schema broadening
  - no mode visibility changes
  - no runtime migration
  - no server-level `supports_parallel_tool_calls` enablement
  - no bridge attestation, nonce, or secret-boundary relaxation

## Issue-Shaping Contract
- User-request translation carried forward: adopt Codex 0.121 MCP Apps/tool metadata where it improves app-server/operator UX, while preserving existing mode, attestation, nonce, and secret-boundary protections.
- Protected terms / exact artifact and surface names: `Codex 0.121`, `MCP Apps`, `tools/list`, `initialize`, `annotations`, `title`, `readOnlyHint`, `destructiveHint`, `idempotentHint`, `openWorldHint`, `supports_parallel_tool_calls`, `delegate.status`, `delegate.question.poll`, `dynamic_tool_bridge_attestation`, `confirm_nonce`, `delegation_token`.
- Nearby wrong interpretations to reject: app widget metadata, marketplace distribution, provider-worker runtime migration, schema loosening, per-tool parallel metadata, and treating annotations as enforcement.
- Explicit non-goals carried forward: no new delegation capabilities, no dynamic bridge policy changes, no provider-worker runtime migration, no marketplace distribution.

## Parity / Alignment Matrix
- Current truth:
  - delegation tools are defined as `name`, `description`, and `inputSchema`
  - `initialize` advertises only tools capability
  - mode visibility is enforced by `buildToolList` plus `getDelegateModeViolationMessage`
  - bridge attestation, `confirm_nonce`, and `delegation_token` protections are enforced at call time
- Reference truth:
  - MCP tool descriptors support `title` and `annotations`
  - Codex app configuration uses destructive/open-world hints for app approval posture
  - Codex 0.121 parallel MCP calls are server-level config via `supports_parallel_tool_calls`, serial by default
  - MCP annotations are hints and clients must not treat untrusted hints as authorization
- Target truth / intended delta:
  - add `title` and `annotations` to existing delegation tool descriptors
  - mark true read-only tools read-only
  - mark write tools non-read-only, destructive false for additive writes, and destructive true for cancel/merge
  - keep `delegate.question.poll` non-read-only due existing expiry fallback side effect
  - add tests that compare pre/post names and stripped schemas for visibility/stability
- Explicitly out-of-scope differences:
  - no `execution`/task augmentation metadata
  - no `_meta` widget/output template fields
  - no `supports_parallel_tool_calls` config edit
  - no protocol-version bump unless required by existing code

## Readiness Gate
- Not done if:
  - any existing input schema changes outside metadata additions
  - `status_only` exposes anything except `delegate.status`
  - `question_only` suppresses or exposes different tools than before
  - cancel/merge lose confirmation behavior
  - dynamic coordinator tools can bypass bridge attestation
  - model-supplied `confirm_nonce` or `delegation_token` is accepted
- Pre-implementation issue-quality review evidence:
  - 2026-04-16: parent rejected the first docs child-lane patch because it reframed the issue as docs-only, used the wrong source anchor, and included Apps widget fields outside this delegation-tool metadata scope.
- Safeguard ownership split:
  - parent owns docs corrections, implementation, validation, workpad, PR, and Linear state
  - rejected child lane supplies completed delegation evidence only

## Technical Requirements
- Functional requirements:
  - extend `DelegationServerToolDefinition` with supported MCP descriptor metadata
  - keep `createDelegationServerRpcHandler` `initialize` response unchanged
  - keep `tools/list` returning the same tool set by mode and GitHub operation policy
  - add a small typed annotation helper so each tool's behavior classification is explicit at definition time
  - classify destructive/confirmation-required tools as destructive: `delegate.cancel`, `coordinator.cancel`, and `github.merge`
  - classify read-only tools as read-only: `delegate.status`, `coordinator.status`, and `github.get_checks`
  - classify `delegate.question.poll` as non-read-only because current expiry fallback can issue child control actions
- Non-functional requirements:
  - minimize code changes to descriptor construction and tests
  - avoid broad refactors
  - preserve JSON-RPC response compatibility for clients that ignore unknown descriptor fields
  - keep app-server metadata as UX hints only
- Interfaces / contracts:
  - `buildToolList({ mode, githubEnabled, allowedGithubOps })`
  - `createDelegationServerRpcHandler(...)`
  - `tools/list` response objects
  - existing `tools/call` handler guards

## Architecture & Data
- Architecture / design adjustments:
  - add `DelegationServerToolAnnotations` and optional `title`/`annotations` fields to the local tool definition type
  - update `toolDefinition` to accept title/annotation metadata without changing schemas
  - define local constants for read-only, additive write, destructive write, and open-world GitHub classifications
  - do not wire any metadata into authorization decisions
- Data model changes / migrations:
  - none
- External dependencies / integrations:
  - official Codex 0.121 release notes
  - official Codex config docs for Apps and MCP parallel server config
  - official MCP schema for tool annotations

## Validation Plan
- Tests / checks:
  - add focused tests in `orchestrator/tests/DelegationServer.test.ts` for tool metadata, tool visibility, schema stability, and app-server-compatible descriptor shape
  - rerun focused `npx vitest run orchestrator/tests/DelegationServer.test.ts`
  - run required validation floor before review handoff
- Rollout verification:
  - inspect `tools/list` output for metadata-bearing descriptors
  - confirm existing guard tests still pass for mode restrictions, bridge attestation, secrets, and confirmation behavior
- Monitoring / alerts:
  - no new monitoring; operator benefit appears through app-server tool metadata and approval surfaces

## Open Questions
- None blocking. A future issue can evaluate whether the delegation MCP server should ever opt into `supports_parallel_tool_calls`; this issue leaves it serial.

## Approvals
- Reviewer: pending docs-review and implementation validation
- Date: 2026-04-16
