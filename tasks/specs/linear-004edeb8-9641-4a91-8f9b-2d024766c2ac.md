---
id: 20260416-linear-004edeb8-9641-4a91-8f9b-2d024766c2ac
title: CO-208 cloud connector/auth drift classification
status: done
relates_to: docs/PRD-linear-004edeb8-9641-4a91-8f9b-2d024766c2ac.md
risk: high
owners:
  - Codex
last_review: 2026-05-18
review_notes:
  - 2026-05-18: CO-554 live issue-context verified CO-208 is Done/completed with PR #503 attached; reclassified inactive done so strict spec-guard no longer treats the completed packet as active stale work.
---

## Canonical Reference
- Canonical TECH_SPEC: `tasks/specs/linear-004edeb8-9641-4a91-8f9b-2d024766c2ac.md`
- TECH_SPEC mirror: `docs/TECH_SPEC-linear-004edeb8-9641-4a91-8f9b-2d024766c2ac.md`
- PRD: `docs/PRD-linear-004edeb8-9641-4a91-8f9b-2d024766c2ac.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-004edeb8-9641-4a91-8f9b-2d024766c2ac.md`
- Task checklist: `tasks/tasks-linear-004edeb8-9641-4a91-8f9b-2d024766c2ac.md`

## Traceability
- Linear issue: `CO-208` / `004edeb8-9641-4a91-8f9b-2d024766c2ac`
- Linear URL: https://linear.app/asabeko/issue/CO-208
- Source anchor: `ctx:sha256:7f405408c0778e5b1c89f4493b0bf5fd26674e9b2ceb9a6afaa5c552630b085a#chunk:c000001`
- Docs packet child lane: `.runs/linear-004edeb8-9641-4a91-8f9b-2d024766c2ac-docs-packet/cli/2026-04-16T22-50-39-659Z-cf579188/manifest.json`

## Summary
- Objective: classify cloud connector/auth drift as a distinct, machine-readable diagnostic instead of generic cloud setup, denial, or provider runtime failure.
- Scope:
  - docs-first packet and registry mirrors for `CO-208`
  - diagnostic contract for `missing_github_connector_link`
  - safe signal preservation for `GitHub connection not found for user`
  - readiness caveat for `CODEX_CLOUD_ENV_ID`
  - parent-owned implementation requirements across `scripts/cloud-canary-ci.mjs`, cloud failure diagnostics, and provider-worker proof
- Constraints:
  - child lane owns docs packet only
  - parent owns implementation, tests, docs-review, Linear state, workpad, PR lifecycle, and full validation
  - no live connector repair, OAuth relink, token refresh, or cloud account provisioning in this lane
  - no tokens, cookies, emails, org ids, connector secrets, or raw account identifiers in artifacts

## Issue-Shaping Contract
- User-request translation carried forward: create the docs-first packet and registry mirrors for classifying cloud connector/auth drift exposed by `CO-207`, preserving exact protected terms and making the diagnostic useful across canary, cloud-failure, and provider-proof surfaces.
- Protected terms / exact artifact and surface names: `missing_github_connector_link`, `GitHub connection not found for user`, `CODEX_CLOUD_ENV_ID`, cloud-canary gates, provider-worker proof, `scripts/cloud-canary-ci.mjs`, cloud failure diagnostics, required cloud canary, fallback cloud canary, manifest, run summary.
- Nearby wrong interpretations to reject: live GitHub connector repair, connector provisioning, OAuth re-link automation, token/cookie/key inspection, broad cloud runtime redesign, Codex version promotion, generic `CODEX_CLOUD_ENV_ID` readiness, and cloud-canary gate weakening.
- Explicit non-goals carried forward: no implementation/test edits in this child lane, no Linear mutation, no secret collection, no live account probing, no provider admission/scheduler redesign, and no connector repair.

## Parity / Alignment Matrix
- Current truth:
  - existing cloud canary/preflight behavior can identify missing `CODEX_CLOUD_ENV_ID`
  - connector/auth drift exposed by `CO-207` can still collapse into generic cloud setup, cloud denial, or provider runtime failure
  - provider-worker proof can record terminal failure without enough structured cause to support automated retry/repair decisions
- Reference truth:
  - a missing cloud environment id and a missing GitHub connector link require different operator actions
  - `GitHub connection not found for user` is safe diagnostic text only if it is not paired with raw identifiers or secrets
  - cloud-canary gates must fail closed and preserve why they failed
- Target truth / intended delta:
  - `cloud_connector_auth_drift` is the stable diagnostic category for the upstream `missing_github_connector_link` condition
  - `GitHub connection not found for user` is preserved as safe diagnostic signal
  - `CODEX_CLOUD_ENV_ID` indicates only target configuration and never readiness by itself
  - cloud failure diagnostics and provider-worker proof carry the same class/signal/guidance contract
  - required/fallback cloud canary outputs distinguish missing env, missing connector link, cloud denial, quota/auth/rate-limit, and provider/runtime failure
- Explicitly out-of-scope differences:
  - repairing the GitHub connector
  - changing cloud identity or account ownership
  - promoting a Codex version
  - broadening provider-worker authority
  - collecting secrets or account identifiers

## Readiness Gate
- Not done if:
  - `missing_github_connector_link` is collapsed into generic environment setup, cloud denial, quota, or provider runtime categories
  - `GitHub connection not found for user` is lost or rewritten into an unsearchable generic message
  - `CODEX_CLOUD_ENV_ID` alone reports cloud readiness
  - cloud-canary gates pass or silently fallback when connector/auth drift blocks the required cloud path
  - provider-worker proof does not expose the diagnostic class or safe signal
  - proof/log/fixture artifacts contain tokens, cookies, emails, org ids, connector secrets, raw account identifiers, or raw upstream auth payloads
- Pre-implementation issue-quality review evidence:
  - 2026-04-17: docs child lane reviewed the launch contract and preserved `CO-208` as a diagnostics/reporting issue exposed by `CO-207`, not a live connector repair. The micro-task path is ineligible because correctness depends on exact protected terms, exact surfaces, and auth-boundary redaction rules.
- Safeguard ownership split:
  - child lane owns only docs/task packet and registry mirror files
  - parent lane owns authoritative issue workspace, implementation, source/test inspection, Linear state, workpad, validation, review, PR lifecycle, and patch integration

## Technical Requirements
- Functional requirements:
  - introduce or update a diagnostic taxonomy with `cloud_connector_auth_drift` as the stable machine-readable diagnostic category for `missing_github_connector_link`
  - preserve `GitHub connection not found for user` as safe redacted signal when emitted by cloud connector/auth failure paths
  - distinguish at least these classes:
    - missing `CODEX_CLOUD_ENV_ID` / missing cloud environment configuration
    - missing GitHub connector link: `cloud_connector_auth_drift` matching `missing_github_connector_link`
    - cloud denial or remote policy failure
    - quota/auth/rate-limit failure
    - provider/runtime failure
  - ensure `scripts/cloud-canary-ci.mjs` outputs the class in required and fallback cloud canary summaries when that condition is observed
  - ensure cloud failure diagnostics normalize the same class/signal/guidance
  - ensure provider-worker proof records the same diagnostic class, safe signal, source/gate provenance, and guidance
  - prevent readiness wording from treating `CODEX_CLOUD_ENV_ID` as sufficient readiness proof
  - keep cloud-canary gates fail-closed when connector/auth drift blocks required cloud execution
- Non-functional requirements:
  - diagnostics are additive and backward compatible for consumers that ignore unknown fields
  - output is deterministic and fixture-testable without live GitHub connector credentials
  - redaction happens before writing to proof, manifests, run summaries, task docs, logs, or snapshots
  - wording stays operator-actionable without exposing account identity
- Interfaces / contracts:
  - `scripts/cloud-canary-ci.mjs`
  - cloud failure diagnostics
  - provider-worker proof
  - run manifest cloud diagnostics
  - run-summary cloud diagnostics
  - required/fallback cloud canary gate output
  - parent-selected status/closeout surfaces that consume provider-worker proof

## Architecture & Data
- Architecture / design adjustments:
  - normalize cloud connector/auth failure text into a small internal diagnostic shape before rendering
  - keep parser/classifier logic separate from operator-facing rendering so fixtures can assert machine-readable output
  - feed the same normalized result to canary summary, cloud failure diagnostics, and provider-worker proof where applicable
  - keep live connector repair out of scope; the diagnostic only points to the repair action
- Required diagnostic data:
  - `category`: `credentials`
  - `diagnostic_category`: `cloud_connector_auth_drift`
  - upstream detail or equivalent matchable signal: `missing_github_connector_link`
  - `signal`: `GitHub connection not found for user`
  - `surface` or equivalent source field
  - `gate`: cloud-canary gate when applicable
  - `cloud_env_id_configured`: boolean only, never the raw value
  - `connector`: `github`
  - `guidance`: reconnect or authorize the GitHub connector for the active cloud identity, then rerun required cloud canary
- Forbidden diagnostic data:
  - tokens
  - cookies
  - API keys
  - OAuth refresh/access tokens
  - connector secrets
  - email addresses
  - raw org ids
  - raw account/user ids
  - auth headers
  - full upstream auth/account payloads
- Data model changes / migrations:
  - no persistent migration expected
  - optional diagnostic fields in existing JSON artifacts should be backward compatible
- External dependencies / integrations:
  - no live connector call required for deterministic tests
  - parent may use sanitized fixtures copied from the CO-207 failure shape if available

## Acceptance Criteria
1. `missing_github_connector_link` maps to distinct `cloud_connector_auth_drift` diagnostic classification when the GitHub connector link is missing.
2. `GitHub connection not found for user` is preserved as safe diagnostic signal without unsafe identifiers.
3. Missing `CODEX_CLOUD_ENV_ID` remains a separate environment configuration class.
4. `CODEX_CLOUD_ENV_ID` being configured does not mark cloud readiness without connector/auth proof from cloud-canary gates.
5. Cloud denial, quota/auth/rate-limit, and provider/runtime failure remain distinguishable from missing connector link drift.
6. `scripts/cloud-canary-ci.mjs` writes machine-readable diagnostic output for the class in the relevant canary summary path.
7. Cloud failure diagnostics normalize the same class/signal/guidance.
8. Provider-worker proof records the class/signal/source/guidance in redacted form.
9. Required/fallback cloud canary behavior remains fail-closed when connector/auth drift blocks required cloud execution.
10. Focused fixtures/tests reject tokens, cookies, emails, org ids, connector secrets, raw account ids, and raw upstream auth payloads.

## Validation Plan
- Tests / checks:
  - parent-owned fixture for `missing_github_connector_link` and `GitHub connection not found for user`, expecting `cloud_connector_auth_drift`
  - parent-owned contrast fixture for missing `CODEX_CLOUD_ENV_ID`
  - parent-owned contrast fixture for cloud denial
  - parent-owned contrast fixture for quota/auth/rate-limit or provider/runtime failure
  - parent-owned provider-worker proof fixture/assertion for class propagation and redaction
  - parent-owned cloud-canary gate assertion that readiness is not claimed from `CODEX_CLOUD_ENV_ID` alone
  - docs child lane runs `jq empty tasks/index.json docs/docs-freshness-registry.json`
- Focused validation commands:
  - parent-selected focused unit test command for the chosen diagnostic/classifier modules
  - `node scripts/spec-guard.mjs --dry-run`
  - manifest-backed docs-review or implementation gate selected by parent
- Rollout verification:
  - parent records sanitized sample diagnostic artifacts for missing env, missing connector link, and one non-connector cloud failure class
- Monitoring / alerts:
  - no new alerting required in this slice
  - existing proof/manifest/status surfaces should expose the class for operator triage

## Open Questions
- Resolved: cloud failure diagnostics own the shared connector/auth classifier, `scripts/cloud-canary-ci.mjs` exposes the same `diagnostic_category`, and provider-worker proof normalizes the same value.
- Resolved: parent uses `category: "credentials"` plus `diagnostic_category: "cloud_connector_auth_drift"` for compatibility, while preserving `missing_github_connector_link` as a protected upstream detail/signal.
- Resolved: parent synthesized deterministic CO-207-shaped fixtures containing only safe connector drift text, with no live connector probing.

## Approvals
- Docs-first packet: same-issue docs child lane `.runs/linear-004edeb8-9641-4a91-8f9b-2d024766c2ac-docs-packet/cli/2026-04-16T22-50-39-659Z-cf579188/manifest.json`
- Parent docs-review: pending parent lane acceptance
- Parent implementation/review/PR lifecycle: pending parent lane
- Date: 2026-04-17

## CO-382 Fallback Decision Table
- Large-refactor check: no new fallback mechanism; CO-554 removes stale active-spec lifecycle drift while keeping strict spec-guard authoritative.
- Minor-seam decision: bounded seam accepted because active pre-expiry specs now block through docs:freshness:maintain before hard expiry.

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Active spec lifecycle freshness | Completed or rolling active spec metadata can remain active until unrelated strict spec-guard jobs hit expiry | remove fallback | CO-554 | Apr 17 cohort reached strict spec-guard boundary after CO-543 | 2026-04-17 | 2026-05-18 | N/A after removal | Terminal specs are inactive done and active pre-expiry cohorts block through docs:freshness:maintain owner action | node scripts/spec-guard.mjs; docs-freshness-maintain regression |
