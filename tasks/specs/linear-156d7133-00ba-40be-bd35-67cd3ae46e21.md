---
id: 20260328-linear-156d7133-00ba-40be-bd35-67cd3ae46e21
title: Reconcile Shared Local Checkout to origin/main After Autonomous Merge Closeout
status: in_progress
owner: Codex
created: 2026-03-28
last_review: 2026-03-28
review_cadence_days: 30
risk_level: high
related_prd: docs/PRD-linear-156d7133-00ba-40be-bd35-67cd3ae46e21.md
related_action_plan: docs/ACTION_PLAN-linear-156d7133-00ba-40be-bd35-67cd3ae46e21.md
related_tasks:
  - tasks/tasks-linear-156d7133-00ba-40be-bd35-67cd3ae46e21.md
review_notes:
  - 2026-03-28: Opened from Linear issue `CO-25` after rechecking the live team workflow states, moving the issue from `Ready` to `In Progress`, and resyncing the provider workspace onto branch `linear-156d7133-shared-root-closeout` at `origin/main` `52224a30b`.
  - 2026-03-28: Current live root state at `/Users/kbediako/Code/CO` is narrower than the original stale example: `git status --short --branch` now reports clean `main...origin/main` and `HEAD` equals `origin/main` at `52224a30b`, so the remaining issue is the missing merge-closeout contract rather than a still-stale root checkout.
  - 2026-03-28: Pre-implementation review approved the bounded path: update the provider-worker prompt and repo-local `linear` / `land` skills so merged closeout must inspect the shared root checkout, capture before/after `git status --short --branch`, fast-forward only when the root is on `main` and clean, and otherwise record an explicit skip reason in the same workpad closeout.
  - 2026-03-28: Delegation is explicitly overridden for this worker run because subagent spawning is unavailable in-session; the implementation must stay narrow, auditable, and test-backed.
  - 2026-03-28: Docs-review approved via `.runs/linear-156d7133-00ba-40be-bd35-67cd3ae46e21/cli/2026-03-27T22-20-59-833Z-8c11f043/manifest.json`; the packet stayed on the bounded prompt/skill/test seam.
  - 2026-03-28: Final validation passed (`build`, `lint`, full `test`, `docs:check`, `docs:freshness`, `diff-budget`, `review`, `pack:smoke`). The standalone review reported no findings, and the explicit elegance pass kept the solution at the smallest truthful surface by reusing the existing control-host repo-root resolver and only extending the worker/skill contract plus focused tests.
---

# Technical Specification

## Context

CO’s autonomous provider-worker contract already requires branch-local merge hygiene before review handoff, but the merged closeout path still ends with “move the issue to `Done`.” That misses the operator’s shared root checkout at `/Users/kbediako/Code/CO`, which is where post-merge drift becomes visible. `CO-25` closes that gap by making merge-closeout explicitly inspect and reconcile the shared root only when it is safe, while recording a machine-checkable closeout summary in the same Linear workpad.

## Requirements

1. Keep the implementation bounded to the provider-worker merge-closeout contract in `providerLinearWorkerRunner.ts` and the repo-local `linear` / `land` skills.
2. Inspect the shared root checkout at `/Users/kbediako/Code/CO` after PR merge completion and before moving the issue to `Done`.
3. Record machine-checkable before/after shared-root state in the final workpad closeout, using `git status --short --branch` or an equivalent summary.
4. Attempt a fast-forward-only shared-root sync when the root checkout is on `main` and clean.
5. Skip mutation with an explicit reason when the shared root is dirty, detached, on another branch, or otherwise unsafe to mutate.
6. Keep the per-issue workspace execution model unchanged and avoid any instruction that would reset or discard local root-checkout changes.
7. Cover the new merge-closeout contract with focused prompt/skill regression expectations.

## Current Truth

- `orchestrator/src/cli/providerLinearWorkerRunner.ts` builds the first-turn and continuation prompts that define provider-worker behavior in this lane.
- `skills/linear/SKILL.md` defines workpad discipline, pre-review gating, and active-state workflow notes.
- `skills/land/SKILL.md` defines the `Merging` loop and currently ends with “Once the PR merges, update the issue to `Done`.”
- The shared root checkout is `/Users/kbediako/Code/CO`, outside the per-issue worktree.
- After the current fetch, `/Users/kbediako/Code/CO` is clean on `main` and aligned with `origin/main`; that live state proves the safe target surface exists but does not change the missing contract for future merged closeouts.

## Validation Plan

- Run docs-review for `linear-156d7133-00ba-40be-bd35-67cd3ae46e21` before implementation.
- Add focused prompt/contract regressions for the first-turn and continuation provider-worker prompts.
- Run the required repo validation floor after implementation.
- Refresh the single active workpad with the docs-first packet, branch stamp, and final closeout contract/evidence expectations.
