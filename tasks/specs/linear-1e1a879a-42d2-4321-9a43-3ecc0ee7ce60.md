---
id: 20260330-linear-1e1a879a-42d2-4321-9a43-3ecc0ee7ce60
title: CO: Investigate Core Lane vitest teardown hang after visible full-suite pass
status: done
owner: Codex
created: 2026-03-30
last_review: 2026-04-30
review_cadence_days: 30
risk_level: high
related_prd: docs/PRD-linear-1e1a879a-42d2-4321-9a43-3ecc0ee7ce60.md
related_action_plan: docs/ACTION_PLAN-linear-1e1a879a-42d2-4321-9a43-3ecc0ee7ce60.md
related_tasks:
  - tasks/tasks-linear-1e1a879a-42d2-4321-9a43-3ecc0ee7ce60.md
review_notes:
  - 2026-04-30: CO-428 live Linear audit confirmed CO-38 is `Done`; this completed-lane spec is reclassified to inactive `done` under canonical owner key `spec-guard:active-specs:last_review=2026-03-30` so historical implementation evidence remains preserved without staying in active-spec freshness.
  - 2026-03-30: Opened after rechecking the live Linear workflow with `linear issue-context`, moving `CO-38` from `Ready` to `In Progress`, creating the single active workpad comment, and switching the detached workspace onto branch `co-38-vitest-teardown-hang`.
  - 2026-03-30: Current tree already contains the earlier CO-24 mitigation that disables Vite's default middleware websocket listener (`server.ws = false` in `vitest.config.core.ts` and `vitest.config.ts`), so CO-38 must follow fresh current-tree evidence rather than assuming port `24678` still owns the hang.
  - 2026-03-30: Pre-implementation review approves the bounded path: reproduce the scrubbed full-suite hang on the live tree, capture the current lingering owner with durable evidence, patch the smallest responsible seam without reducing coverage, and prove terminal local plus CI behavior before review handoff.
  - 2026-03-30: Fresh scrubbed local runs on the current workspace, detached `HEAD` baseline, and PR `#320` head all exited cleanly with terminal summaries; the original local teardown-hang hypothesis is not currently reproducible on those trees.
  - 2026-03-30: GitHub run `23712103211` on head `8f0bbb63d` failed a specific command-surface test instead: `tests/cli-command-surface.spec.ts > prints pr ready-review help`, where `node --loader ts-node/esm bin/codex-orchestrator.ts pr ready-review --help` was killed by the test timeout with `signal: SIGTERM`.
---

# Technical Specification

## Context

CO-38 is a fresh validation-blocker investigation on top of a tree that already contains the older CO-24 websocket-listener suppression. The current issue description and live provider-worker evidence show the late-suite quiet-tail symptom still exists: the suite appears to finish, `vitest` stays idle in the libuv loop, and watcher or `esbuild` activity remains alive, which stalls `Core Lane` in `Test`. This lane must therefore identify the live owner on the current tree instead of reasserting the old diagnosis.

## Evidence Update - 2026-03-30

- Fresh current-tree repros no longer confirm the local teardown-hang story on the relevant trees for this run.
- The cited live blocker is now the CI timeout in `tests/cli-command-surface.spec.ts > prints pr ready-review help`.
- This implementation therefore pivots to the evidence-backed PR help path instead of continuing to optimize around a stale hang hypothesis.

## Requirements

1. Keep `npm run test` as the authoritative local equivalent of `Core Lane`.
2. Reproduce the current hang locally using the scrubbed worker environment from the issue description.
3. Capture concrete lingering-owner evidence using process, handle, or code-path inspection.
4. Determine whether the live owner is a regression of the older Vite websocket path or a different lingering seam.
5. Land the smallest responsible fix that restores a terminal full-suite exit without reducing intended coverage.
6. Verify the fix with focused seam validation, full `npm run test`, and a fresh PR head `Core Lane` result.
7. Keep review handoff blocked until validation is terminally truthful.

## Current Truth

- `npm run test` still maps to `vitest run --config vitest.config.core.ts`.
- `vitest.config.core.ts` and `vitest.config.ts` already disable Vite's default middleware websocket listener via `server.ws = false`.
- Issue evidence for CO-38 still reports a post-suite hang with `vitest` idle in `uv__io_poll` / `kevent` and `fsevents.node` alive after the visible suite pass.
- The lane therefore starts from a stronger baseline than CO-24: the old listener fix is present, but the current teardown blocker remains unresolved.

## Validation Plan

- Run an audited `linear child-stream --pipeline docs-review` before implementation.
- Record scrubbed local repro notes and owner evidence under `out/linear-1e1a879a-42d2-4321-9a43-3ecc0ee7ce60/manual/`.
- Add or update focused regressions for the actual offending seam.
- Re-run the full validation floor, including `npm run test`.
- Run manifest-backed standalone review first, then an explicit elegance/minimality pass before review handoff.
