---
id: 20260413-linear-72e9f13e-0899-4769-9ca3-82586077fca9
title: CO workflow: refresh current-main docs:freshness stale cohorts 1138-1163 plus release docs
status: in_progress
owner: Codex
created: 2026-04-13
last_review: 2026-04-13
review_cadence_days: 30
risk_level: medium
related_prd: docs/PRD-linear-72e9f13e-0899-4769-9ca3-82586077fca9.md
related_action_plan: docs/ACTION_PLAN-linear-72e9f13e-0899-4769-9ca3-82586077fca9.md
related_tasks:
  - tasks/tasks-linear-72e9f13e-0899-4769-9ca3-82586077fca9.md
review_notes:
  - 2026-04-13: Opened from Linear issue `CO-167` in the provider-worker workspace after rechecking live CO workflow states with the packaged `linear issue-context` helper, moving the issue from `Ready` to `In Progress`, recording the required same-turn `stay_serial` / `overlapping_scope` parallelization decision, confirming there is no attached PR, and starting a new branch `linear/co-167-refresh-docs-freshness-cohorts`.
  - 2026-04-13: The single reusable workpad source for this attempt is staged at `out/linear-72e9f13e-0899-4769-9ca3-82586077fca9/manual/workpad.md`, and the persistent Linear workpad comment is `ea11e8f7-d41a-49df-a3c2-8e0902189f24`.
  - 2026-04-13: `CO-166` already proved the current-main baseline truth. The saved baseline report shows `198` stale docs with `0` missing registry entries, `0` missing files, `0` invalid entries, and `0` uncatalogued docs.
  - 2026-04-13: The live stale set stays bounded to two coherent cohorts: March 13 packet docs spanning task ids `1138` through `1163` (`182` docs total) and January release docs spanning `.agent/SOPs/release.md` plus release packets `0942`, `0943`, and `0944` (`16` docs total).
  - 2026-04-13: `scripts/docs-freshness.mjs` reads freshness dates from `docs/docs-freshness-registry.json`, so the repair must refresh exact registry rows and keep mirrored metadata (`tasks/specs/*`, `docs/TECH_SPEC-*`, `tasks/index.json`) aligned where those mirrors already carry review dates.
  - 2026-04-13: Pre-implementation issue-quality review approves the bounded scope: register the `CO-167` packet, run an audited docs-review child stream, refresh only the proven stale cohorts, rerun validation, and keep `CO-165` implementation/test work plus freshness-policy changes explicitly out of scope.
  - 2026-04-13: `linear child-stream --pipeline docs-review --stream co-167-docs-review --format json` failed closed with `provider_worker_child_stream_provenance_invalid`, so the lane used the direct fallback docs-review run recorded at `.runs/linear-72e9f13e-0899-4769-9ca3-82586077fca9-co-167-docs-review/cli/2026-04-13T01-21-57-946Z-0b933108/manifest.json`.
  - 2026-04-13: The direct fallback docs-review run passed `delegation-guard` and `spec-guard` and failed only at `docs/TASKS.md: tasks-file-too-large: lines=451 max=450`; `npm run docs:archive-tasks` then trimmed the file back to `450` lines and saved `out/linear-72e9f13e-0899-4769-9ca3-82586077fca9/TASKS-archive-2026.md`.
  - 2026-04-13: The stale-cohort repair refreshed exact registry rows and matching mirror metadata only; `npm run docs:freshness` now reports `docs:freshness OK - 3725 docs, 3728 registry entries` with zero stale, missing, invalid, or uncatalogued drift.
  - 2026-04-13: Validation is green through `delegation-guard`, `spec-guard`, `build`, `lint`, `test` (`334` files / `3696` tests passed), `docs:check`, `docs:freshness`, `repo:stewardship`, and `diff-budget` with an explicit large-scope override.
  - 2026-04-13: Forced standalone review reached the reviewer but `.runs/linear-72e9f13e-0899-4769-9ca3-82586077fca9/cli/2026-04-13T01-11-49-327Z-dd662068/review/telemetry.json` reports `status=failed`, `review_outcome=failed-boundary`, and `termination_boundary.kind=startup-anchor`; manual review fallback and explicit elegance notes are recorded at `out/linear-72e9f13e-0899-4769-9ca3-82586077fca9/manual/20260413T012358Z-validation/12-manual-review-fallback.md` and `.../13-elegance-review.md`.
---

# Technical Specification

## Context
`CO-167` exists to repair the stale-doc baseline that `CO-166` classified on current `main`. The issue is not a request to revisit `CO-165` implementation work or to weaken freshness policy. The task is to refresh the exact stale cohorts already proven by saved baseline evidence and restore a truthful green `docs:freshness` baseline for unrelated lanes.

## Requirements
1. Preserve the issue wording and traceability while recording the live workspace truth: `198` stale docs across the March 13 `1138` through `1163` cohort plus the January release-doc cohort.
2. Keep the repair bounded to the exact stale docs listed in `out/linear-6af47129-33cc-45fa-9a55-e49423536752/manual/baseline-docs-freshness-report.json`.
3. Refresh registry dates and mirrored review metadata consistently instead of applying waivers.
4. Record audited docs-review evidence before implementation, or a truthful fallback if the child stops only on the known repo baseline.
5. Leave closeout evidence that other workers can cite directly for the before/after docs-baseline story.

## Current Truth
- `CO-166` already proved the failure is current-main baseline drift, not branch-local fallout.
- `npm run docs:freshness` last failed with:
  - `198` stale docs
  - `Task Packet stale=142`
  - `Task Mirror stale=29`
  - `Report Only stale=26`
  - `Active Guide stale=1`
  - `missing_registry=0`
  - `missing_on_disk=0`
  - `invalid_entries=0`
  - `uncatalogued_docs=0`
- Every stale entry belongs to one of two cohorts:
  - March 13 packet docs: `1138` through `1163`
  - January release docs: `.agent/SOPs/release.md`, `0942`, `0943`, `0944`

## Issue-Shaping Contract
- User-request translation carried forward:
  - refresh the current-main stale-doc baseline
  - preserve the truthful validation story from `CO-166`
  - keep the work bounded to the two proven cohorts
- Protected terms / exact artifact and surface names:
  - `docs:freshness`
  - `current-main stale-doc baseline`
  - `1138` through `1163`
  - `release.md`
  - `0942`
  - `0943`
  - `0944`
  - `out/linear-6af47129-33cc-45fa-9a55-e49423536752/manual/baseline-docs-freshness-report.json`
- Nearby wrong interpretations to reject:
  - reopen `CO-165` runtime/test work
  - waive or weaken freshness policy
  - bulk-refresh unrelated docs while the lane is active
- Explicit non-goals carried forward:
  - `CO-165` code or test changes
  - `docs:freshness` policy changes
  - unrelated docs cleanup outside the proven cohorts

## Parity / Alignment Matrix
- Not a formal parity lane, but baseline alignment still matters.
- Current truth: repo-baseline `docs:freshness` is red because one March 13 packet cohort and one January release-doc cohort aged past their cadence.
- Reference truth: unrelated implementation lanes should be able to rely on repo-level freshness checks being green when their issue-local work is healthy.
- Target truth / intended delta: refresh only those cohorts so `docs:freshness` is green again, while keeping the truthful claim that the failure was current-main baseline drift.
- Explicitly out-of-scope differences: `CO-165` implementation, freshness-policy changes, and unrelated stale docs.

## Readiness Gate
- Not done if:
  - `docs:freshness` still fails on the repaired tree
  - either proven stale cohort is only partially refreshed
  - the lane cannot point to the exact stale rows and saved before/after artifacts
- Pre-implementation issue-quality review evidence:
  - the saved baseline report shows a coherent, fully enumerated stale set with no missing/invalid drift, so the lane is correctly scoped to targeted metadata refresh rather than broader docs-system work
- Safeguard ownership split:
  - packet/workpad continuity: `CO-167` docs, mirrors, and Linear workpad
  - stale-cohort repair: exact registry rows and mirrored review metadata for the proven stale docs
  - broader docs/process changes: separate follow-up issue only if new evidence appears

## Technical Requirements
- Functional requirements:
  - register the new `CO-167` packet and workpad source
  - run audited `docs-review` before implementation
  - refresh the March 13 and January stale-cohort review metadata consistently
  - rerun `docs:freshness` and save post-fix artifacts
- Non-functional requirements:
  - keep the diff reviewable and metadata-only
  - prefer mechanical targeted edits over narrative churn
  - preserve truthful issue-local versus repo-baseline attribution
- Interfaces / contracts:
  - `scripts/docs-freshness.mjs`
  - `docs/docs-freshness-registry.json`
  - `tasks/index.json`
  - `docs/TASKS.md`

## Architecture & Data
- Architecture / design adjustments:
  - no freshness-system behavior changes are introduced
  - the repair touches only stale-doc metadata plus the new issue packet
- Data model changes / migrations:
  - add the `linear-72e9f13e-0899-4769-9ca3-82586077fca9` packet to task registries
  - refresh `last_review` metadata for the proven stale docs
- External dependencies / integrations:
  - audited `linear child-stream --pipeline docs-review`
  - saved report artifacts under `out/linear-72e9f13e-0899-4769-9ca3-82586077fca9/manual/`

## Validation Plan
- Tests / checks:
  - `MCP_RUNNER_TASK_ID=linear-72e9f13e-0899-4769-9ca3-82586077fca9 "/opt/homebrew/Cellar/node/25.2.1/bin/node" "/Users/kbediako/Code/CO/bin/codex-orchestrator.js" linear child-stream --pipeline docs-review --stream co-167-docs-review --format json`
  - `node scripts/spec-guard.mjs --dry-run`
  - `npm run build`
  - `npm run lint`
  - `npm run test`
  - `npm run docs:check`
  - `npm run docs:freshness`
  - `npm run repo:stewardship`
  - `node scripts/diff-budget.mjs`
  - `FORCE_CODEX_REVIEW=1 npm run review -- --uncommitted`
- Rollout verification:
  - preserve the `CO-166` baseline failure artifacts as the before state
  - confirm stale entries reduce from `198` to `0`
  - refresh the workpad after docs-first, after implementation, and before any stop point
- Monitoring / alerts:
  - ongoing signal remains the repo freshness gates and saved reports; no new automation is added here

## Approvals
- Reviewer: docs-review fallback, validation floor, and standalone/elegance closeout recorded; PR handoff pending
- Date: 2026-04-13
