---
id: 20260425-linear-6b3b8bf4-63fa-44e8-a182-e2e20209aca7
title: CO-360 task registry docs pointer TECH_SPEC mirror alignment
status: in_progress
relates_to: docs/PRD-linear-6b3b8bf4-63fa-44e8-a182-e2e20209aca7.md
risk: medium
owners:
  - Codex
last_review: 2026-06-17
review_notes:
  - 2026-06-17: CO-579 pre-expiry review kept this spec active-current; no verified terminal/archive evidence was established in this stream, CO-579 is the live non-terminal docs-freshness owner, and docs/spec gates remain unchanged.
  - 2026-05-18: CO-522 active-spec audit found 1 unchecked task checklist item, so this spec remains active and was reviewed for current lifecycle ownership rather than archived. Evidence: `out/linear-b642e879-ba50-45ef-b0d9-b059afa9e932-recovery/spec-preexpiry-local-classification.json`.
---

## Summary
- Objective: define the `CO-369` docs-first contract for verifying and resolving the `CO-360` `tasks/index.json` `paths.docs` relationship to its docs-side `TECH_SPEC` mirror.
- Scope: packet creation, issue-shaping contract, protected terms, alignment matrix, implementation decision fork, child/parent ownership split, and focused validation guidance.
- Constraints: this child lane cannot edit registry files, docs freshness files, the `CO-360` mirror, provider runtime files, Linear state, or PR lifecycle artifacts.

## Issue-Shaping Contract
- User-request translation carried forward: `CO-369` is a bounded docs-packet lane for `CO-360: align task registry docs pointer with TECH_SPEC mirror`. The implementation decision belongs to the parent lane after current-state verification: add the missing `CO-360` docs `TECH_SPEC` mirror and repoint `paths.docs`, or record a specific legacy fallback rationale.
- Protected terms / exact artifact and surface names:
  - `CO-360`
  - `CO-369`
  - `tasks/index.json`
  - `items[]`
  - `paths.docs`
  - `TECH_SPEC`
  - `TECH_SPEC mirror`
  - `CO-360 docs TECH_SPEC mirror`
  - `tasks/specs`
  - `docs/TECH_SPEC-*`
  - `docs/TASKS.md`
  - `docs/docs-freshness-registry.json`
  - provider runtime files
  - `source-0`
- Nearby wrong interpretations to reject:
  - the child lane should edit `tasks/index.json`
  - the parent should repoint `paths.docs` before verifying the `CO-360` mirror and current registry row
  - all legacy `paths.docs` values are acceptable without a recorded rationale
  - the issue is broad registry normalization
  - the issue is docs freshness policy work
  - the issue is provider runtime work
  - the child lane should mutate Linear
- Explicit non-goals carried forward:
  - no child edits to `tasks/index.json`, `docs/TASKS.md`, `docs/docs-freshness-registry.json`, the `CO-360` mirror, or provider runtime files
  - no broad `tasks/index.json` migration
  - no blind docs pointer update
  - no full repo validation in this child lane

## Parity / Alignment Matrix

| Dimension | Current Truth | Reference Truth | Target Truth / Intended Delta | Explicitly Out Of Scope |
| --- | --- | --- | --- | --- |
| Registry docs pointer | Parent is verifying the current `CO-360` `tasks/index.json` row and `paths.docs` value. | For docs-first issues with docs mirrors, `paths.docs` should take readers to the docs-side `TECH_SPEC` surface rather than an ambiguous or missing path. | Parent makes `paths.docs` explicitly align with the `CO-360` docs `TECH_SPEC` mirror, or records a concrete legacy fallback reason. | Normalizing unrelated registry rows. |
| Docs `TECH_SPEC` mirror | Issue text says the `CO-360` mirror may be missing or misaligned. | A docs mirror should exist when the registry docs pointer is expected to land in `docs/TECH_SPEC-*`. | Parent adds the missing mirror from canonical task-spec truth if needed. | Child editing the `CO-360` mirror. |
| Canonical task spec | Parent owns inspection of the `CO-360` canonical task spec under `tasks/specs`. | Canonical task spec remains the implementation source of truth. | Docs mirror and registry pointer should not contradict the canonical spec. | Rewriting canonical spec content beyond the verified alignment need. |
| Fallback handling | Legacy behavior may exist but is not accepted by default. | Exceptions to docs mirror linkage need a specific, evidence-backed rationale. | If no mirror/repoint is appropriate, parent records why this `CO-360` row must remain a legacy fallback. | Silent or generic fallback claims. |

## Readiness Gate
- Not done if:
  - `paths.docs` remains missing, stale, or ambiguous with no specific rationale
  - the parent chooses mirror creation or fallback before verifying current `CO-360` state
  - the packet omits protected terms, nearby wrong interpretations, non-goals, or the alignment matrix
  - child edits leave the declared six-file scope
  - implementation broadens into docs freshness, provider runtime, or unrelated registry cleanup
- Pre-implementation issue-quality review evidence:
  - 2026-04-25 child lane translated the parent prompt into a bounded issue contract, preserved protected terms and non-goals, and kept the implementation decision fork open pending parent verification.
- Safeguard ownership split:
  - child owns only the six `CO-369` packet files
  - parent owns `CO-360` source inspection, registry/doc mirror implementation, Linear state, workpad, validation, PR lifecycle, and final issue classification

## Technical Requirements
- Functional requirements:
  1. Child creates the `CO-369` PRD, docs `TECH_SPEC` mirror, canonical task spec, ACTION_PLAN, task checklist, and `.agent` mirror.
  2. Parent identifies the exact `CO-360` task UUID, canonical task spec path, docs `TECH_SPEC` mirror path, and `tasks/index.json` `paths.docs` value.
  3. Parent verifies whether the docs-side `CO-360` `TECH_SPEC` mirror exists and whether it mirrors the canonical task spec enough for registry readers.
  4. Parent chooses exactly one implementation outcome:
     - add the missing `CO-360` docs `TECH_SPEC` mirror and repoint `paths.docs`; or
     - record a specific legacy fallback rationale with evidence.
  5. Parent avoids unrelated registry/doc cleanup and keeps all edits tied to the verified `CO-360` alignment issue.
  6. Parent records validation evidence in the task checklist and workpad after implementation.
- Non-functional requirements (performance, reliability, security):
  - no network or Linear mutation is required from this child lane
  - registry pointer behavior should be deterministic and reviewer-readable
  - any fallback rationale must be concrete enough for future docs stewards to audit
- Interfaces / contracts:
  - `tasks/index.json`
  - `items[]`
  - `paths.docs`
  - `tasks/specs`
  - `docs/TECH_SPEC-*`
  - `node scripts/spec-guard.mjs --dry-run`
  - focused docs/registry checks selected by the parent after implementation

## Architecture & Data
- Architecture / design adjustments:
  - Prefer the existing docs-first dual-surface pattern: canonical task spec under `tasks/specs/` and docs-facing mirror under `docs/TECH_SPEC-*`.
  - Do not add new registry schema or pointer semantics unless parent proves existing `paths.docs` cannot express the desired state.
- Data model changes / migrations:
  - Expected parent change is a narrow existing-field update to the `CO-360` `paths.docs` value, plus a docs mirror file if missing.
  - No child-lane data model changes.
- External dependencies / integrations:
  - Parent-owned Linear issue/workpad, parent-owned PR lifecycle, and existing docs-first validation commands.

## Validation Plan
- Child-lane checks:
  - exact six target files exist
  - `git diff --check` over the six target files
  - protected terms present in the packet
  - no full repo validation suites
- Parent-lane checks:
  - inspect the exact `CO-360` `tasks/index.json` entry and docs mirror state
  - run focused docs/registry validation for the touched surfaces
  - run `node scripts/spec-guard.mjs --dry-run`
  - run additional docs gates only if parent implementation touches surfaces that require them
  - standalone review/elegance pass per parent workflow if non-trivial implementation occurs
- Rollout verification:
  - reviewer follows the `CO-360` `paths.docs` value and lands on the intended docs-facing `TECH_SPEC` mirror or an explicit fallback rationale.
- Monitoring / alerts:
  - no new monitoring; future drift should surface through existing docs/registry checks.

## Open Questions
- What exact `CO-360` UUID and current paths does parent verify?
- Is the missing surface the docs mirror itself, the registry pointer, or both?
- If a legacy fallback remains, what exact command, historical entry shape, or tooling constraint requires it?

## Approvals
- Reviewer: pending parent docs-review or equivalent issue-quality review after patch acceptance
- Date: 2026-04-25
