---
id: linear-5d1836b2-98e7-452a-93be-a17c64b3b5fe
title: Control host supervision stale co-status endpoint and probe-timeout recurrence
status: in_progress
relates_to: CO-336
risk: medium
owners:
  - Codex
last_review: 2026-05-18
review_notes:
  - 2026-05-18: CO-522 active-spec audit found 1 unchecked task checklist item, so this spec remains active and was reviewed for current lifecycle ownership rather than archived. Evidence: `out/linear-b642e879-ba50-45ef-b0d9-b059afa9e932-recovery/spec-preexpiry-local-classification.json`.
---

## Summary
- Objective: fix the recurrence where stale endpoint recovery and supervision `probe_timeout` flapping combine after endpoint rotation.
- Scope: direct `co-status --format json`, `/ui/data.json` endpoint recovery, supervision probe timeout budget, and restart/probe diagnostics.
- Constraints: no manual host reset, no active provider-worker kill/restart, no queue/admission policy changes.

## Issue-Shaping Contract
- User-request translation carried forward: independently validate whether the recurrence is stale endpoint recovery, same-endpoint timeout/degraded read, supervision probe coupling, stale-owner routing, or queue/admission churn.
- Protected terms / exact artifact and surface names: `control_endpoint.json`, `co-status --format json`, `ECONNREFUSED`, `probe_timeout`, `control-host supervise status --format json`, `restart_count`, `/ui/data.json`, endpoint rotation, healthy active provider workers, `provider-intake-state.json`.
- Nearby wrong interpretations to reject: `CO-329`, `CO-330`, or `CO-331` do not automatically own this recurrence; a later successful status read does not close it.
- Explicit non-goals carried forward: no whole UI redesign, no weakening real stuck alarms, no provider-worker restarts, no manual restart fix.

## Parity / Alignment Matrix
- Current truth: the status read can wait 15s while supervision kills the `co-status` subprocess after a 10s probe cap.
- Reference truth: supervision should let the status read classify its own outcome before declaring an unhealthy sample.
- Target truth / intended delta: probe budget and diagnostics align with the read path while preserving fail-closed repeated-failure behavior.
- Explicitly out-of-scope differences: provider-worker stdin bootstrap, stale-owner reclaim unless proven causal, and queue/admission churn.

## Readiness Gate
- Not done if: slow healthy status reads are still counted as `probe_timeout`, or real timeouts no longer fail closed.
- Pre-implementation issue-quality review evidence: initial source inspection found `CONTROL_HOST_SUPERVISION_PROBE_TIMEOUT_CAP_MS = 10_000` and `DEFAULT_ATTACH_REQUEST_TIMEOUT_MS = 15_000`.
- Safeguard ownership split: parent-owned bounded source/test changes.

## Technical Requirements
- Functional requirements: align probe timeout with stale-endpoint recovery's two status-read attempts plus bounded headroom; record probe duration in restart/status artifacts; keep endpoint recovery behavior.
- Non-functional requirements: no unbounded waits, no new dependencies, no live mutation during diagnosis.
- Interfaces / contracts: additive optional JSON fields are allowed in supervision state/restart records.

## Architecture & Data
- Architecture / design adjustments: reuse existing attach/status endpoint recovery; adjust supervision probe contract only.
- Data model changes / migrations: additive optional fields only.
- External dependencies / integrations: none.

## Validation Plan
- Tests / checks: focused Vitest tests and scoped build/test commands.
- Rollout verification: workpad closeout and standalone/elegance review before review handoff.
- Monitoring / alerts: `control-host supervise status --format json` restart history exposes probe duration/reason context.

## Open Questions
- None.

## Approvals
- Reviewer: pending.
- Date: 2026-04-23
