---
id: 20260411-linear-89fa9514-a071-41f0-b54d-3d4e5d4a00b3
title: CO: resolve weakly-owned historical reference and archive residue surfaced by repo stewardship audit
status: done
owner: Codex
created: 2026-04-11
last_review: 2026-05-12
review_cadence_days: 30
risk_level: medium
related_prd: docs/PRD-linear-89fa9514-a071-41f0-b54d-3d4e5d4a00b3.md
related_action_plan: docs/ACTION_PLAN-linear-89fa9514-a071-41f0-b54d-3d4e5d4a00b3.md
related_tasks:
  - tasks/tasks-linear-89fa9514-a071-41f0-b54d-3d4e5d4a00b3.md
review_notes:
  - 2026-04-11: Opened from Linear issue `CO-126` in the provider-worker workspace after rechecking live CO workflow states with the packaged `linear issue-context` helper, recording the required same-turn `stay_serial` / `single_bounded_change` parallelization decision, moving the issue from `Ready` to `In Progress`, creating the single persistent `## Codex Workpad`, and switching the detached workspace onto branch `linear/co-126-historical-reference-archive-residue`.
  - 2026-04-11: Live repo truth is narrower than the issue snapshot. `archives/REPORT.mcp_code_mode.md` is already deleted and untracked, so the current residue set is the six-path cluster still reported as `update` by `npm run repo:stewardship`.
  - 2026-04-11: Current retained use was verified before implementation. `tasks/specs/0956-subagents-skill-codex-cli-refresh.md` still cites the four `reference/0956-subagents-skill-codex-cli-refresh/*` evidence files, and `docs/guides/pixel-perfect-local-clones.md` still cites `reference/mirror.config.wp.example.json`.
  - 2026-04-11: No live repo references were found for `archives/REPORT.mcp_code_mode.json`, and its markdown sibling was already deleted in `CO-88`, so delete-first is the truthful default unless new evidence appears.
  - 2026-04-11: Pre-implementation issue-quality review approves the bounded scope: add explicit rationale anchors only where current repo references still justify retention, delete the orphan archive residue, rerun `repo:stewardship`, and do not widen into audit-contract or unrelated historical cleanup work.
  - 2026-05-12: CO-523 live Linear audit verified CO-126 is Done/completed; reclassified this task spec as inactive done metadata for strict spec-guard evidence. Evidence: .runs/linear-8573da42-d9f9-44ce-a24e-224984539044/cli/2026-05-12T18-47-35-293Z-376d8842/provider-linear-issue-context-cache-89fa9514-a071-41f0-b54d-3d4e5d4a00b3.json.
---

# Technical Specification

## Context
`CO-124` intentionally split broad historical residue out of the stewardship-audit implementation diff. On current main, that split now resolves to a bounded, mechanically-verifiable cluster: the audit itself is healthy, but six historical surfaces remain classified as `update` because they do not satisfy the local-README retention contract for `reference/**` and `archives/**`. The task is not to redesign the catalog. The task is to make those six paths truthful.

## Requirements
1. Preserve the issue wording and traceability while recording the live six-path residue set and the already-deleted `archives/REPORT.mcp_code_mode.md`.
2. Give every currently flagged path an explicit disposition: keep with rationale or delete.
3. Any retained historical subtree must gain a local `README.md` / rationale anchor within the applicable boundary.
4. `repo:stewardship` must be rerun to prove the targeted residue cluster no longer appears as unexplained action-required output.
5. The diff must stay bounded to the residue cluster and the minimum supporting docs/task/workpad updates.

## Current Truth
- `docs/repo-stewardship-catalog.json` already classifies `reference/**` and `archives/**` as `retain_with_rationale` surfaces requiring a local `README.md` anchor within `reference/` or `archives/`.
- `npm run repo:stewardship` on 2026-04-11 reports:
  - `4610` tracked files
  - `6` action-required surfaces
  - `0` uncatalogued surfaces
  - `Reference History: files=7 validate=0 update=5 delete=0 retain=2`
  - `Archive History: files=2 validate=0 update=1 delete=0 retain=1`
- The live action-required residue set is:
  - `archives/REPORT.mcp_code_mode.json`
  - `reference/0956-subagents-skill-codex-cli-refresh/README-snippet.txt`
  - `reference/0956-subagents-skill-codex-cli-refresh/codex-cli-refresh-dry-run.txt`
  - `reference/0956-subagents-skill-codex-cli-refresh/codex-cli-refresh-help.txt`
  - `reference/0956-subagents-skill-codex-cli-refresh/global-guidance-manifest.md`
  - `reference/mirror.config.wp.example.json`

## Issue-Shaping Contract
- User-request translation carried forward:
  - resolve the historical residue surfaced by `repo:stewardship`
  - keep the lane bounded to `reference/**` / `archives/**`
  - add local rationale only where retention is still truthful
- Protected terms / exact artifact and surface names:
  - `reference/**`
  - `archives/**`
  - `retain_with_rationale`
  - `local README/rationale anchor`
  - `repo:stewardship`
- Nearby wrong interpretations to reject:
  - reopen the `CO-124` audit implementation
  - broad unrelated docs/runtime cleanup
  - default keep without explicit rationale
- Explicit non-goals carried forward:
  - changing stewardship catalog semantics
  - reopening `CO-75`, `CO-88`, or `CO-102`
  - broad cleanup beyond the current six-path residue set

## Parity / Alignment Matrix
- Not applicable as a formal parity lane.
- Current truth: the audit contract is correct and fail-closed; the remaining problem is historical surfaces lacking truthful local rationale.
- Reference truth: current repo references should be able to justify every kept historical file locally, and orphaned archive residue should be deleted.
- Target truth / intended delta: a root-level rationale anchor under `reference/` explains the mirror config example, a subtree-local rationale anchor explains the 0956 evidence pack, and the orphaned archive report JSON is removed.
- Explicitly out-of-scope differences: audit command/catalog redesign, unrelated reference/archive cleanup, review-wrapper/runtime/provider-worker changes.

## Readiness Gate
- Not done if:
  - any live flagged residue surface still lacks a keep/delete decision
  - a retained surface lacks a local anchor in its subtree
  - the rerun audit still reports the targeted cluster as unexplained `update`
- Pre-implementation issue-quality review evidence:
  - live audit output, live tracked-file inventory, and current internal references all confirm the lane is correctly scoped to the six-path residue cluster rather than to a broader cleanup or audit redesign
- Safeguard ownership split:
  - packet/workpad continuity: current issue docs, registry mirrors, and Linear workpad
  - residue cleanup: `reference/**` local rationale anchors and archive deletion only
  - broader residue or audit issues: separate follow-up only if new out-of-scope truth appears

## Technical Requirements
- Functional requirements:
  - add a root-level truthful boundary-local rationale anchor for retained root-level reference history
  - add a subtree-local rationale anchor explaining why the 0956 evidence pack is retained
  - delete `archives/REPORT.mcp_code_mode.json` if it remains unreferenced
  - rerun the stewardship audit and record explicit output artifacts
- Non-functional requirements:
  - keep rationale concise and concrete
  - prefer deletion over speculative retention
  - preserve current retained `reference/plus-ex-15th/**` and `archives/hi-fi-tests/README.md` behavior
- Interfaces / contracts:
  - `docs/repo-stewardship-catalog.json`
  - `scripts/repo-stewardship-audit.mjs`
  - `tasks/specs/0956-subagents-skill-codex-cli-refresh.md`
  - `docs/guides/pixel-perfect-local-clones.md`

## Architecture & Data
- Architecture / design adjustments:
  - no stewardship catalog changes are expected
  - retention rationale will be carried by boundary-local `README.md` files under `reference/`
  - archive residue resolves by deletion rather than by creating a weak new archive README
- Data model changes / migrations:
  - add two rationale anchors under `reference/`
  - remove `archives/REPORT.mcp_code_mode.json`
- External dependencies / integrations:
  - repo task/spec references for 0956 evidence
  - pixel-perfect mirror guide reference to the WordPress mirror example

## Validation Plan
- Tests / checks:
  - audited `linear child-stream --pipeline docs-review`
  - `npm run repo:stewardship`
  - explicit audit artifact capture via `node scripts/repo-stewardship-audit.mjs --report ... --summary-markdown ...`
  - standard repo validation/review floor before handoff if the diff remains non-trivial
- Rollout verification:
  - prove the current six-path residue cluster exists before cleanup
  - rerun the audit after cleanup and confirm the targeted cluster no longer appears as unexplained `update`
  - refresh the Linear workpad after docs-first, after implementation, and before the next stop point
- Monitoring / alerts:
  - the recurring stewardship audit remains the continuing signal; no new automation is added here

## Approvals
- Reviewer: pending audited docs-review
- Date: 2026-04-11
