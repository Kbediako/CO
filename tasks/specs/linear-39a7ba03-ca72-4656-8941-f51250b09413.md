---
id: 20260412-linear-39a7ba03-ca72-4656-8941-f51250b09413
title: CO workflow: restore repo-wide spec/docs freshness baselines after 30-day rollover regression
status: in_progress
owner: Codex
created: 2026-04-12
last_review: 2026-04-12
review_cadence_days: 30
risk_level: medium
related_prd: docs/PRD-linear-39a7ba03-ca72-4656-8941-f51250b09413.md
related_action_plan: docs/ACTION_PLAN-linear-39a7ba03-ca72-4656-8941-f51250b09413.md
related_tasks:
  - tasks/tasks-linear-39a7ba03-ca72-4656-8941-f51250b09413.md
review_notes:
  - 2026-04-12: Opened from Linear issue `CO-155` in the provider-worker workspace after rechecking live CO workflow states with the packaged `linear issue-context` helper, confirming the issue is already in active `In Progress`, recording the required same-turn `stay_serial` / `overlapping_scope` parallelization decision, and confirming there is no attached PR and no existing `## Codex Workpad` comment yet.
  - 2026-04-12: The single reusable workpad source for this attempt is staged at `out/linear-39a7ba03-ca72-4656-8941-f51250b09413/manual/workpad.md` before the first Linear upsert.
  - 2026-04-12: Live repo truth differs from the older issue snapshot in one material way: `node scripts/spec-guard.mjs --dry-run` is already green on this workspace. The current blocker is `npm run docs:freshness`, which fails with `119` stale docs and no missing or invalid entries.
  - 2026-04-12: The live stale set is one coherent March 12 cohort spanning packet ids `1121` through `1137` across task packet, task mirror, and report-only surfaces. Every stale row carries `last_review: 2026-03-12`, `review_cadence_days: 30`, and `age_days: 31`.
  - 2026-04-12: Pre-implementation issue-quality review approves the bounded scope: register the `CO-155` packet, run audited `docs-review`, refresh the March 12 cohort consistently across task metadata and freshness registry rows, rerun `docs:freshness`, and keep `CO-94` implementation/test work explicitly out of scope.
  - 2026-04-12: The initial audited docs-review stream showed the `CO-155` packet was correct but `docs/TASKS.md` had rolled past the `450` line budget (`451/450`). Running the supported `npm run docs:archive-tasks` helper displaced snapshot `1007` into the shared archive payload, and this lane restored `1007` into tracked `docs/TASKS-archive-2026.md` so the archive surface stayed truthful before the post-fix docs-review rerun.
  - 2026-04-12: After refreshing the March 12 cohort metadata, the post-fix docs-review child stream succeeded at `.runs/linear-39a7ba03-ca72-4656-8941-f51250b09413-co-155-docs-review-post-fix/cli/2026-04-12T00-50-17-603Z-4e66f408/manifest.json`, and `.runs/linear-39a7ba03-ca72-4656-8941-f51250b09413-co-155-docs-review-post-fix/cli/2026-04-12T00-50-17-603Z-4e66f408/review/telemetry.json` reports `review_outcome=clean-success`.
  - 2026-04-12: The non-trivial validation floor is green through `npm run repo:stewardship`; `node scripts/diff-budget.mjs` required a recorded scope override because the truthful baseline repair spans `42` files; the parent standalone review telemetry at `/Users/kbediako/Code/CO/.runs/linear-39a7ba03-ca72-4656-8941-f51250b09413/cli/2026-04-11T23-33-42-773Z-fe533160/review/telemetry.json` reports `status=succeeded` with `review_outcome=bounded-success`; and the explicit minimality pass is recorded in `out/linear-39a7ba03-ca72-4656-8941-f51250b09413/manual/20260412T0059Z-handoff-validation/11-elegance-review.md`.
---

# Technical Specification

## Context
`CO-155` exists to restore repo-wide freshness guardrails after `CO-94` resumed validation. On the current workspace, the issue snapshot is slightly stale: the spec-freshness portion is already resolved, but the docs-freshness baseline has rolled over again. The task is not to reopen `CO-94`. The task is to clear the live repo-baseline blocker that unrelated lanes inherit.

## Requirements
1. Preserve the issue wording and traceability while recording the live workspace truth: `spec-guard` green, `docs:freshness` red on `119` stale docs.
2. Keep the repair bounded to the March 12 stale cohort spanning packet ids `1121` through `1137`.
3. Refresh the stale cohort consistently across task metadata and registry surfaces instead of applying waivers.
4. Record audited docs-review evidence before implementation and save pre/post validation artifacts under the current issue task id.
5. Leave closeout evidence other workers can cite directly for repo-baseline freshness, separate from `CO-94` implementation health.

## Current Truth
- `node scripts/spec-guard.mjs --dry-run` is green on this workspace as of 2026-04-12.
- `npm run docs:freshness` fails with:
  - `119` stale docs
  - `Task Packet stale=85`
  - `Task Mirror stale=17`
  - `Report Only stale=17`
  - `missing_registry=0`
  - `missing_on_disk=0`
  - `invalid_entries=0`
- Every stale entry belongs to packet ids `1121` through `1137` and carries `last_review=2026-03-12`, `cadence_days=30`, `age_days=31`.

## Issue-Shaping Contract
- User-request translation carried forward:
  - restore repo-wide freshness baselines without weakening policy
  - keep `CO-94` implementation truth intact
  - leave machine-checkable evidence for later review/handoff lanes
- Protected terms / exact artifact and surface names:
  - `node scripts/spec-guard.mjs --dry-run`
  - `npm run docs:freshness`
  - `docs/docs-freshness-registry.json`
  - `tasks/index.json`
  - `docs/TASKS.md`
  - `CO-94`
- Nearby wrong interpretations to reject:
  - this lane should change `CO-94` code
  - the old `1093` through `1119` cohort is still the live blocker here
  - freshness policy should be weakened instead of repaired
- Explicit non-goals carried forward:
  - controller/consumer implementation work
  - unrelated repo-test stabilization
  - broad docs cleanup outside the reproduced stale cohort

## Parity / Alignment Matrix
- Not a formal parity lane, but freshness-baseline parity still matters.
- Current truth: repo-baseline freshness is red only because the March 12 cohort aged past the 30-day cadence.
- Reference truth: unrelated implementation lanes should be able to rely on repo-level freshness gates being green when their issue-local work is healthy.
- Target truth / intended delta: refresh the March 12 cohort so `docs:freshness` is green again while `spec-guard` stays green and no one needs repo-baseline fallback notes.
- Explicitly out-of-scope differences: `CO-94` code, legacy stale cohorts that are not part of the live report, and policy weakening.

## Readiness Gate
- Not done if:
  - `docs:freshness` still fails on the repaired tree
  - `spec-guard` regresses from green
  - the lane cannot point to the exact stale cohort and saved repro artifacts
- Pre-implementation issue-quality review evidence:
  - the saved April 12 freshness report shows one coherent stale cohort with no missing/invalid registry rows, so the lane is correctly scoped to metadata refresh rather than broader docs-system redesign
- Safeguard ownership split:
  - packet/workpad continuity: `CO-155` docs, registry mirrors, and Linear workpad
  - stale-cohort repair: March 12 packet metadata only
  - broader baseline or workflow changes: separate follow-up issue only if new evidence appears

## Technical Requirements
- Functional requirements:
  - register the new `CO-155` packet and workpad source
  - run audited `docs-review` before implementation
  - refresh March 12 review metadata consistently across the active stale cohort
  - rerun `docs:freshness` and save post-fix artifacts
- Non-functional requirements:
  - keep the diff reviewable
  - prefer mechanical metadata updates over narrative churn
  - preserve truthful issue-local versus repo-baseline attribution
- Interfaces / contracts:
  - `scripts/docs-freshness.mjs`
  - `docs/docs-freshness-registry.json`
  - `tasks/index.json`
  - task spec and tech-spec frontmatter `last_review` fields

## Architecture & Data
- Architecture / design adjustments:
  - no new freshness-system behavior should be introduced
  - the repair should primarily touch packet metadata and review-tracking docs
- Data model changes / migrations:
  - add the `linear-39a7ba03-ca72-4656-8941-f51250b09413` packet to task registries
  - refresh `last_review` metadata for the active stale cohort
- External dependencies / integrations:
  - audited `linear child-stream --pipeline docs-review`
  - saved report artifacts in `out/linear-39a7ba03-ca72-4656-8941-f51250b09413/manual/`

## Validation Plan
- Tests / checks:
  - `MCP_RUNNER_TASK_ID=linear-39a7ba03-ca72-4656-8941-f51250b09413 "/opt/homebrew/Cellar/node/25.2.1/bin/node" "/Users/kbediako/Code/CO/bin/codex-orchestrator.js" linear child-stream --pipeline docs-review --stream co-155-docs-review --format json`
  - `node scripts/spec-guard.mjs --dry-run`
  - `npm run docs:freshness`
  - repo validation floor if the diff remains non-trivial
- Rollout verification:
  - preserve the baseline failure artifacts
  - confirm stale entries reduce from `119` to `0`
  - refresh the workpad after docs-first, after implementation, and before any stop point
- Monitoring / alerts:
  - ongoing signal remains the repo freshness gates and saved reports; no new automation is added here

## Approvals
- Reviewer: audited docs-review clean-success and standalone review bounded-success recorded; human review pending PR handoff
- Date: 2026-04-12
