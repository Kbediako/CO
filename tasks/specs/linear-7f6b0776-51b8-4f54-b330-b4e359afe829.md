---
id: 20260425-linear-7f6b0776-51b8-4f54-b330-b4e359afe829
title: CO-364 RLM GPT-5.5 Route Posture
status: done
relates_to: docs/PRD-linear-7f6b0776-51b8-4f54-b330-b4e359afe829.md
risk: medium
owners:
  - Codex
last_review: 2026-05-18
review_notes:
  - 2026-05-18: CO-522 spec lifecycle audit found the linked task checklist has zero unchecked items (17 checked), so this spec is terminal and eligible for implementation-docs archive. Evidence: `out/linear-b642e879-ba50-45ef-b0d9-b059afa9e932-recovery/spec-preexpiry-local-classification.json`.
---

## Summary
- Objective: align RLM route default model posture in `orchestrator/src/cli/rlm/alignment.ts` with the CO-352 `gpt-5.5` / `xhigh` CO-local posture.
- Scope: default route model constants, focused route policy tests, and CO-364 docs/checklist registry mirrors.
- Constraints: preserve route override merging, leave `explorer_fast` and spark/file-search exceptions untouched, and avoid CO-356/CO-360 owned surfaces.

## Issue-Shaping Contract
- User-request translation carried forward: CO-364 specifically asks that `sentinel_model`, `high_reasoning_model`, and `arbitration_model` no longer default to `gpt-5.4` now that CO-352 has merged.
- Protected terms / exact artifact and surface names: `DEFAULT_ALIGNMENT_POLICY.route`, `sentinel_model`, `high_reasoning_model`, `arbitration_model`, `gpt-5.5`, `xhigh`, `explorer_fast`, `spark exceptions`.
- Nearby wrong interpretations to reject: generic packaged default promotion, provider/appserver runtime changes, CO-356 workflow edits, CO-360 provider-worker/appserver edits, or removing explicit override support.
- Explicit non-goals carried forward: cloud/release posture, downstream template defaults, spark role posture, and model-catalog discovery behavior.

## Parity / Alignment Matrix
- Current truth: `DEFAULT_ALIGNMENT_POLICY.route` still uses `gpt-5.4` for all three route model defaults.
- Reference truth: CO-352 merged with current CO-local validated `gpt-5.5` / `xhigh` posture for eligible local surfaces.
- Target truth / intended delta: RLM route defaults use `gpt-5.5`; route override merging still allows callers/config to select another model.
- Explicitly out-of-scope differences: `explorer_fast` remains a file/codebase-search-only spark exception and generic packaged defaults remain outside this route-local change.

## Readiness Gate
- Not done if: defaults remain stale, override behavior regresses, or unrelated active-lane surfaces are touched.
- Pre-implementation issue-quality review evidence: parent worker reviewed the user request, existing CO-352 posture packet, `alignment.ts`, and `RlmAlignment.test.ts`; the issue is narrow enough for a direct constant/test update.
- Safeguard ownership split: this CO-364 worker owns RLM route defaults and nearby tests only; CO-356 and CO-360 workers own their separate worktrees/surfaces.

## Technical Requirements
- Functional requirements:
  - Default `sentinel_model` to `gpt-5.5`.
  - Default `high_reasoning_model` to `gpt-5.5`.
  - Default `arbitration_model` to `gpt-5.5`.
  - Preserve `high_reasoning_available: true`.
  - Preserve explicit `policy.route` override behavior.
- Non-functional requirements: no new dependencies, no broad refactor, no unrelated model posture churn.
- Interfaces / contracts: `AlignmentPolicy.route` and `resolvePolicy(policy?: Partial<AlignmentPolicy>)`.

## Architecture & Data
- Architecture / design adjustments: update the default route constants and focused assertions.
- Data model changes / migrations: none.
- External dependencies / integrations: none.

## Validation Plan
- Tests / checks:
  - Focused `orchestrator/tests/RlmAlignment.test.ts`.
  - `node scripts/spec-guard.mjs --dry-run`.
  - `npm run build`.
  - `npm run test -- --runInBand` when supported, otherwise closest repo test command.
  - `npm run docs:check`.
  - `npm run docs:freshness`.
- Rollout verification: draft PR tied to CO-364 after local gates pass.
- Monitoring / alerts: not applicable.

## Open Questions
- None.

## Approvals
- Reviewer: parent worker self-review.
- Date: 2026-04-25.
