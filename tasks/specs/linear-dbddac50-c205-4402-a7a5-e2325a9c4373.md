---
id: 20260402-linear-dbddac50-c205-4402-a7a5-e2325a9c4373
title: CO: Restore clean full-suite Vitest exit for provider-worker validation
status: done
owner: Codex
created: 2026-04-02
last_review: 2026-05-03
review_cadence_days: 30
risk_level: high
related_prd: docs/PRD-linear-dbddac50-c205-4402-a7a5-e2325a9c4373.md
related_action_plan: docs/ACTION_PLAN-linear-dbddac50-c205-4402-a7a5-e2325a9c4373.md
related_tasks:
  - tasks/tasks-linear-dbddac50-c205-4402-a7a5-e2325a9c4373.md
review_notes:
  - 2026-05-03: CO-498 live Linear baseline review verified CO-69 is Done/completed with merged PR #347; archived/reclassified this historical packet metadata without deleting packet evidence or weakening docs:freshness/spec-guard.
  - 2026-04-02: Opened from Linear issue `CO-69` in the provider-worker workspace using issue id `dbddac50-c205-4402-a7a5-e2325a9c4373`.
  - 2026-04-02: Packaged `linear issue-context` confirmed the CO workflow states (`Ready`, `In Progress`, `In Review`, `Merging`, `Rework`, `Done`), showed no attached PR, and showed no existing workpad comment.
  - 2026-04-02: The issue was moved from `Ready` to `In Progress` before active coding via packaged `linear transition`.
  - 2026-04-02: The workspace started detached and was switched onto branch `linear/co-69-clean-full-suite-vitest-exit`.
  - 2026-04-02: Prior repo context was reviewed before implementation: CO-57 (`linear-fdefaeca-8c14-4dc3-980e-cdc6cfa6d955`) fixed stale manifest heartbeat truth for `implementation-gate`, but it explicitly did not prove that the actual full-suite Vitest process exits cleanly in provider-worker workspaces.
  - 2026-04-02: Pre-implementation approval: proceed with a narrow full-suite validation lane that starts from reproduction and only widens beyond the smallest owner if evidence proves a bounded fix or fallback is impossible.
  - 2026-04-02: Audited child docs-review rerun `.runs/linear-dbddac50-c205-4402-a7a5-e2325a9c4373-co-69-docs-review-rerun/cli/2026-04-02T10-02-03-042Z-c947ed42/manifest.json` succeeded with review telemetry `status: succeeded`, `review_outcome: clean-success`.
  - 2026-04-02: A bounded `240000ms` harness reproduced the apparent late-tail hang but timed out before terminal output. The captured descendants were still inside `tests/run-review.spec.ts` / `tests/cli-command-surface.spec.ts` work rather than a proven post-suite orphan tree. Evidence: `out/linear-dbddac50-c205-4402-a7a5-e2325a9c4373/manual/20260402T095856Z-repro-npm-run-test/`.
  - 2026-04-02: Patience-first rerun `npm run test` reached the terminal Vitest footer after `272.79s`, with `tests/run-review.spec.ts` (`257745ms`) and `tests/cli-command-surface.spec.ts` (`271478ms`) owning the late tail. Post-exit process sweeps found no lingering `npm run test:orchestrator`, `node (vitest)`, or `esbuild --service`. Evidence: `out/linear-dbddac50-c205-4402-a7a5-e2325a9c4373/manual/20260402T100743Z-full-suite-patience/01-npm-run-test.log`, `out/linear-dbddac50-c205-4402-a7a5-e2325a9c4373/manual/20260402T101507Z-repro-classification.md`.
  - 2026-04-02: Issue-scoped `implementation-gate` child stream `.runs/linear-dbddac50-c205-4402-a7a5-e2325a9c4373-co-69-implementation-gate/cli/2026-04-02T10-19-16-455Z-c47a274a/manifest.json` succeeded. The `test` stage exited `0` after `270.55s`, and review telemetry reported `status: succeeded`, `review_outcome: clean-success` with no findings. Elegance review confirmed the docs-only/evidence-only resolution remained the smallest truthful change. Evidence: `.runs/linear-dbddac50-c205-4402-a7a5-e2325a9c4373-co-69-implementation-gate/cli/2026-04-02T10-19-16-455Z-c47a274a/review/telemetry.json`, `out/linear-dbddac50-c205-4402-a7a5-e2325a9c4373/manual/20260402T102805Z-elegance-review.md`.
---

# Technical Specification

## Context

`CO-69` is the follow-up after `CO-57`. The earlier lane corrected stale `manifest.json` heartbeats during long-running `implementation-gate` stages so raw-manifest readers stopped misclassifying active runs as stalled. Fresh current-workspace evidence narrowed this issue further: the apparent late-green hang was a false timeout during a long quiet tail, not a reproduced post-suite linger on the default `npm run test` path. The lane therefore resolves operator truth for provider-worker validation rather than reopening a broader code-path rewrite.

## Requirements

1. Reproduce the apparent full-suite non-exit behavior with a deterministic command or an explicit classification of which cited command shapes still reproduce it.
2. Identify the smallest truthful owner, whether that is a real lingering process or a false timeout during the current late-tail profile.
3. Restore or preserve a clean terminal success exit for the approved full-suite validation path in CO issue workspaces.
4. Preserve truthful failure semantics and full-suite coverage; no success reclassification or cleanup that can hide failures.
5. Document the root cause or approved fallback for future provider-worker lanes in the task packet and Linear workpad.

## Current Truth

- The issue body cites multiple full-suite command shapes that can appear to hang after late green suites:
  - `npm run test`
  - `npx vitest run --config vitest.config.core.ts`
  - `npx vitest run --config vitest.config.core.ts --pool forks --maxWorkers 1 --minWorkers 1 --no-file-parallelism`
  - `npx vitest run --config vitest.config.core.ts --shard 1/2`
- `CO-57` already fixed one related false-stall owner in `implementation-gate`: stale `manifest.json` heartbeat persistence while the run was still active.
- That prior fix does not prove the actual full Vitest process exits cleanly, so this lane must start from current-workspace reproduction instead of assuming the owner is already known.
- The initial `240000ms` harness timed out after `quiet_for_ms: 206217` while `node (vitest)` still had active descendants in `tests/run-review.spec.ts` / `tests/cli-command-surface.spec.ts`; that harness did not reach the terminal footer.
- The patience-first rerun of the default `npm run test` path reached a clean terminal footer in `272.79s`, proving the current default path is terminal on this workspace tree.
- The issue body names `esbuild --service` as a concrete hypothesis; current post-exit sweeps found no lingering service after the terminal footer.

## Issue-Shaping Contract

- User-request translation carried forward:
  - restore a truthful full-suite validation path with a clean terminal exit
  - preserve full-suite coverage and failure truth
  - document either the root cause or the approved fallback
- Protected terms / exact artifact and surface names:
  - `full Vitest suite`
  - `clean terminal success exit`
  - `node (vitest)`
  - `esbuild --service`
  - `validation fallback`
- Nearby wrong interpretations to reject:
  - close the issue by skipping or narrowing `npm run test`
  - add timeout-based cleanup that could hide a red suite
  - treat the prior CO-57 heartbeat fix as sufficient proof that this issue is already solved
- Explicit non-goals carried forward:
  - unrelated CO-50 parser work
  - broad Vitest refactors without fresh evidence
  - any reduction in test coverage purely to force green exit behavior

## Parity / Alignment Matrix

- Not applicable.
- Current truth:
  - provider-worker lanes can still misclassify the late tail as a hang if they stop around `240s`
  - the prior heartbeat fix is related context but not the answer to this issue
  - the current default `npm run test` path is clean-success terminal when allowed to run through `tests/run-review.spec.ts` and `tests/cli-command-surface.spec.ts`
- Reference truth:
  - provider-worker validation should have a full-suite command path that returns a truthful terminal result
- Target truth / intended delta:
  - default `npm run test` remains the approved full-suite path
  - future lanes have an evidence-backed late-tail duration profile and do not classify the current tree as hung without stronger evidence
  - failures stay truthful
- Explicitly out-of-scope differences:
  - unrelated CLI or orchestration behavior changes

## Readiness Gate

- Not done if:
  - the default full-suite validation path no longer reaches the clean terminal footer observed in this workspace
  - the chosen fallback is not full-suite or not machine-checkable
  - the fix can hide genuine failures
  - the issue packet or workpad does not document the final owner or fallback
- Pre-implementation issue-quality review evidence:
  - the issue wording is broad enough to mention several possible owners, but the user request remains narrower than a repo-wide test cleanup. The correct next step is current-workspace reproduction of the cited full-suite shapes, then the smallest truthful owner only.
- Post-reproduction issue-quality review evidence:
  - the current tree does not need a new code-path fix for the default `npm run test` path. The truthful owner is the long quiet tail from `tests/run-review.spec.ts` and `tests/cli-command-surface.spec.ts`, so the minimal resolution is documentation plus approved validation guidance.
- Safeguard ownership split:
  - docs/workflow packet and Linear workpad: current issue packet files plus packaged `linear` helper
  - reproduction and root-cause isolation: relevant test scripts, Vitest config, and targeted helpers
  - implementation: only the narrow owner files proven by reproduction

## Technical Requirements

- Functional requirements:
  - preserve a full-suite validation path for provider-worker use
  - eliminate false classification of the reproduced quiet tail as a terminal hang on green runs
  - preserve non-zero exits for red runs
  - capture evidence for the reproduced command shape and the final validation path
- Non-functional requirements (performance, reliability, security):
  - bounded implementation scope
  - no destructive cleanup beyond current-run owned resources
  - auditable evidence under the issue task id
- Interfaces / contracts:
  - `package.json` test scripts
  - `vitest.config.core.ts`
  - any test helpers or spawned-process lifetimes implicated by the reproduction

## Architecture & Data

- Architecture / design adjustments:
  - begin at the user-cited command surface, then inspect active handles and owners only after reproduction
  - if the owner is a false timeout during a long active tail, prefer issue-local documentation and validation guidance over a speculative code change
  - fix shutdown at the narrow owner only if a code change is required
- Data model changes / migrations:
  - none expected
- External dependencies / integrations:
  - Vitest
  - esbuild service lifetime
  - provider-worker validation handoff workflow

## Validation Plan

- Tests / checks:
  - audited child `docs-review`
  - deterministic reproduction and classification of the cited apparent full-suite hang
  - explicit runbook validation for the chosen owner when no code change is required
  - required repo validation floor after implementation
- Rollout verification:
  - prove the selected full-suite command path exits cleanly
  - prove failure semantics remain truthful
  - keep the Linear workpad aligned with exact commands and outcomes
- Monitoring / alerts:
  - rely on command logs, issue-scoped artifacts, manifests, and review telemetry

## Open Questions

- The default `npm run test` path is terminal on this tree; should future operator guidance classify the direct `vitest run` shapes as well, or is the approved default path sufficient for provider-worker handoff?

## Approvals

- Reviewer: docs-review child rerun approved cleanly via `.runs/linear-dbddac50-c205-4402-a7a5-e2325a9c4373-co-69-docs-review-rerun/cli/2026-04-02T10-02-03-042Z-c947ed42/manifest.json`
- Date: 2026-04-02
