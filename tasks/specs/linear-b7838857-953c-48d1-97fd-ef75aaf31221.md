---
id: 20260422-linear-b7838857-953c-48d1-97fd-ef75aaf31221
title: "CO-307 child-lane trusted-project pollution and global config growth"
relates_to: docs/PRD-linear-b7838857-953c-48d1-97fd-ef75aaf31221.md
risk: high
owners:
  - Codex
last_review: 2026-04-22
related_action_plan: docs/ACTION_PLAN-linear-b7838857-953c-48d1-97fd-ef75aaf31221.md
task_checklists:
  - tasks/tasks-linear-b7838857-953c-48d1-97fd-ef75aaf31221.md
---

## Canonical Reference
- PRD: `docs/PRD-linear-b7838857-953c-48d1-97fd-ef75aaf31221.md`
- TECH_SPEC mirror: `docs/TECH_SPEC-linear-b7838857-953c-48d1-97fd-ef75aaf31221.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-b7838857-953c-48d1-97fd-ef75aaf31221.md`
- Task checklist: `tasks/tasks-linear-b7838857-953c-48d1-97fd-ef75aaf31221.md`
- Source anchor: `ctx:sha256:5592e921272085385fb1db3d450715f2978bddf72fb4d9e61f25ea945e79bafd#chunk:c000001`

## Summary
- Objective: create the docs-first packet and registry/checklist updates for CO-307, the child-lane trust/config isolation lane.
- Current evidence:
  - `prepareLaneWorkspace()` clones child workspaces into `.child-lanes/<stream>-<run>`
  - `runProviderLinearChildLane()` builds `childEnv` from inherited env plus runtime env
  - `resolveCodexHome()` falls back to the global home when `CODEX_HOME` is not isolated
  - the issue description defines the failure mode as `trusted-project pollution`, `child-lane paths`, and `global ~/.codex/config.toml growth`
- Scope:
  - CO-307 PRD, TECH_SPEC, ACTION_PLAN, task checklist, and `.agent` mirror
  - `tasks/index.json` registration
  - `docs/TASKS.md` snapshot
- Constraints:
  - this child lane does not implement the launch/home/config fix
  - parent owns reproduction, implementation, validation, Linear state, workpad, PR lifecycle, and merge
  - do not weaken trust boundaries
  - do not edit files outside the declared CO-307 docs scope

## Issue-Shaping Contract
- User-request translation carried forward: keep same-issue child lanes and parent-owned acceptance, but stop ephemeral child-lane workspaces from polluting global trusted-project/config state. The parent fix must stay bounded to the launch/home/config seam and must not weaken trust boundaries.
- Protected terms / exact artifact and surface names:
  - `trusted-project pollution`
  - `child-lane paths`
  - `global ~/.codex/config.toml growth`
  - `do not weaken trust boundaries`
  - `.child-lanes/<stream>-<run>`
  - `lane_workspace_path`
  - `CODEX_HOME`
  - `~/.codex/config.toml`
  - `resolveCodexHome`
  - `orchestrator/src/cli/providerLinearChildLaneRunner.ts`
  - `orchestrator/src/cli/providerLinearChildLaneShell.ts`
  - `orchestrator/src/cli/providerLinearWorkerRunner.ts`
  - `orchestrator/src/cli/controlHostCliShell.ts`
  - `provider-linear-child-lane-proof.json`
- Nearby wrong interpretations to reject:
  - disable or bypass trust checks
  - globally trust all `.child-lanes/*` paths
  - collapse child lanes back into the parent workspace
  - broaden into generic global config cleanup or unrelated auth/profile work
- Explicit non-goals carried forward:
  - no generic global Codex config redesign
  - no parent-owned lifecycle or PR flow changes
  - no weakening of trust or authority boundaries
  - no code edits in this child lane

## Parity / Alignment Matrix
- Current truth:
  - child-lane workspaces are ephemeral clones under `.child-lanes/*`
  - child-lane runtime inherits `CODEX_HOME` unless the parent isolates it
  - session discovery and related Codex state lookups use `resolveCodexHome(env)`
  - the issue identifies the resulting host-level symptom as trusted-project/config residue in global `~/.codex/config.toml`
- Reference truth:
  - child lanes stay bounded under parent ownership
  - ephemeral lane paths do not become durable global trust/config state
  - trust boundaries remain strict and auditable
- Target truth:
  - child-lane launches stop causing durable trusted-project pollution or global config growth
  - the parent-owned acceptance and authority model stays unchanged
  - the final fix is explainable through a bounded launch/home/config contract
- Explicitly out-of-scope differences:
  - auth/profile redesign
  - broad host-global config management changes
  - unrelated provider runtime or review-policy work

## Readiness Gate
- Not done if:
  - parent cannot show why ephemeral child-lane paths no longer create durable global trust/config residue
  - the fix depends on weakened or bypassed trust
  - the child-lane acceptance boundary or parent authority model changes
- Pre-implementation issue-quality review evidence:
  - 2026-04-22: the local child checkout lacks the `.runs/.../memory/source-0/source.txt` payload, so the packet uses the protected issue wording from the parent brief plus in-repo launch evidence from `providerLinearChildLaneRunner.ts`. Correctness depends on those exact protected terms and the explicit rejection of trust weakening, so the lane is ineligible for a looser micro-task treatment.
- Safeguard ownership split:
  - child lane writes only the declared docs/checklist/registry files
  - parent lane owns reproduction, implementation, focused tests, validation, workpad, Linear state, and PR lifecycle

## Technical Requirements
- Functional requirements:
  1. Register the CO-307 docs-first packet before implementation.
  2. Preserve the exact protected wording around trust/config pollution and no trust weakening.
  3. Parent must reproduce the current residue seam with bounded evidence before selecting a fix.
  4. Parent implementation must stop ephemeral child-lane paths from becoming durable trusted-project/config state.
  5. Parent implementation must preserve the current parent-owned same-issue child-lane and patch-acceptance model.
  6. Parent must add focused launch/home/config regressions on the touched seams.
  7. Parent must close with machine-checkable evidence that global config growth is no longer driven by child-lane path churn.
- Non-functional requirements:
  - fail-closed trust posture
  - narrow launch/home/config scope
  - no reliance on permanent global host-state growth
  - concise docs packet suitable for parent patch export
- Interfaces / contracts:
  - `orchestrator/src/cli/providerLinearChildLaneRunner.ts`
  - `orchestrator/src/cli/providerLinearChildLaneShell.ts`
  - `orchestrator/src/cli/providerLinearWorkerRunner.ts`
  - `orchestrator/src/cli/controlHostCliShell.ts`
  - `orchestrator/src/cli/utils/codexPaths.ts`
  - `provider-linear-child-lane-proof.json`
  - `tasks/index.json`
  - `docs/TASKS.md`

## Validation Plan
- Child-lane scoped checks:
  - JSON parse for `tasks/index.json`
  - `git diff --check` on the declared CO-307 paths
  - scoped status review
- Parent-owned checks:
  - focused reproduction of trusted-project/config residue
  - focused tests on child-lane runner/shell and parent launch seams
  - docs-review / implementation-gate as required
  - normal parent validation floor and review handoff

## Risks
- Source payload unavailable in this child checkout.
  - Mitigation: packet records the missing local payload explicitly and anchors scope on the parent-provided issue wording plus current in-repo launch evidence.
- Fix drifts into trust weakening.
  - Mitigation: make "do not weaken trust boundaries" a hard packet gate.
- Scope creep into generic global config work.
  - Mitigation: keep the implementation seam limited to child-lane workspace, env, and Codex-home behavior.

## Completion Criteria
- CO-307 packet and mirrors exist.
- `tasks/index.json` remains valid JSON.
- The protected trust/config wording is preserved verbatim across the packet.
- Parent can proceed with reproduction, implementation, and focused validation from the packet without widening scope.
