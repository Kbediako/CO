---
id: 20260422-linear-b7838857-953c-48d1-97fd-ef75aaf31221
title: "CO-307 child-lane trusted-project pollution and global config growth"
relates_to: docs/PRD-linear-b7838857-953c-48d1-97fd-ef75aaf31221.md
risk: high
owners:
  - Codex
last_review: 2026-04-22
related_action_plan: docs/ACTION_PLAN-linear-b7838857-953c-48d1-97fd-ef75aaf31221.md
task_checklists:
  - tasks/tasks-linear-b7838857-953c-48d1-97fd-ef75aaf31221.md
---

## Problem
- Ephemeral child-lane workspaces under `.child-lanes/<stream>-<run>` can leave durable entries in `~/.codex/config.toml`.
- Stable ancestors such as `/Users/kbediako/Code/CO` are already trusted, so per-lane growth is residue, not required security state.

## Issue-Shaping Contract
- Translation: keep same-issue child lanes and parent-owned acceptance, but stop ephemeral lane paths from polluting global trust/config state.
- Protected terms: `trusted-project pollution`, `child-lane paths`, `global ~/.codex/config.toml growth`, `do not weaken trust boundaries`
- Protected surfaces: `.child-lanes/<stream>-<run>`, `lane_workspace_path`, `CODEX_HOME`, `~/.codex/config.toml`, `resolveCodexHome`, `provider-linear-child-lane-proof.json`
- Reject: disabling trust checks, trusting all `.child-lanes/*`, collapsing lanes into the parent workspace, or broadening into generic global config redesign.
- Non-goals: auth/profile work, unrelated provider workflow changes, generic Codex defaults cleanup.

## Parity Matrix
- Current: child lanes clone into `.child-lanes/<stream>-<run>`, child env inherits `CODEX_HOME`, Codex resolves config/session state from that home, and repeated runs can append unique trusted project entries.
- Reference: a trusted stable ancestor should be sufficient and unrelated roots must remain untrusted.
- Target: cleanup or inheritance removes only the current redundant child-lane trust residue while preserving fail-closed trust boundaries.
- Not done if: each new lane can still add durable trust residue, only manual cleanup exists, or the fix widens trust.

## Requirements
- Reproduce and explain the global trusted-project growth cause.
- Remove only the current redundant child-lane trust entry when a separate trusted ancestor exists.
- Preserve unrelated project entries and unrelated TOML sections.
- Persist a failed `provider-linear-child-lane-proof.json` when post-exec artifact export fails.
- Cover exact-lane cleanup, array-table boundaries, quoted `]` headers, nested project subtables, and multiline header-like content in regression tests.

## Validation
- Focused runner validation during implementation: build, targeted lint, targeted vitest, diff budget.
- Full repo validation, manifest-backed review, elegance pass, and clean `pr ready-review` drain before review handoff.

## Evidence Note
- The docs child-lane checkout lacked the local `source-0/source.txt` payload, so the packet anchors on the issue text and current in-repo runner behavior.
