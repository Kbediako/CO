---
id: 20260418-linear-0ae7288b-cc5f-493d-896f-713990e51808
title: CO stabilize run-review fake Codex subprocess timeout flake
status: done
relates_to: docs/PRD-linear-0ae7288b-cc5f-493d-896f-713990e51808.md
risk: high
owners:
  - Codex
last_review: 2026-05-18
review_notes:
  - 2026-05-18: CO-522 spec lifecycle audit found the linked task checklist has zero unchecked items (24 checked), so this spec is terminal and eligible for implementation-docs archive. Evidence: `out/linear-b642e879-ba50-45ef-b0d9-b059afa9e932-recovery/spec-preexpiry-local-classification.json`.
---

# TECH_SPEC - CO: stabilize run-review fake Codex subprocess timeout flake

## Summary
- Objective: create the docs-first contract for `CO-251` so the parent can stabilize the `tests/run-review.spec.ts` fake Codex subprocess timeout flake under full `npm run test` without weakening cleanup coverage.
- Scope: docs packet and registry mirrors in this child lane; parent-owned source/test inspection, root cause, implementation, validation, workpad, PR, and merge.
- Constraints: preserve exact protected surfaces, keep `CO-234` `LockFile` work out of scope, and do not edit implementation or test files from this lane.

## Issue-Shaping Contract
- User-request translation carried forward: `CO-251` is a full-suite flake stabilization task for the fake Codex subprocess timeout cleanup path in `tests/run-review.spec.ts`. The parent must keep the subprocess cleanup assertion intact while identifying why it flakes under `npm run test`.
- Protected terms / exact artifact and surface names:
  - `tests/run-review.spec.ts`
  - `scripts/run-review.ts`
  - `runReviewCommandSubprocess`
  - `RUN_REVIEW_MODE=hang`
  - `RUN_REVIEW_HANG_MARKER`
  - `CODEX_REVIEW_TIMEOUT_SECONDS=0`
  - `CODEX_REVIEW_STALL_TIMEOUT_SECONDS=0`
  - `hang-started.txt`
  - `findRunReviewMockPids`
  - `fake Codex subprocess`
  - `full npm run test`
  - `CO-234`
  - `LockFile`
- Nearby wrong interpretations to reject:
  - this is not `CO-234` `LockFile` keepalive timing work
  - this is not permission to delete, skip, or relax the subprocess cleanup regression
  - this is not generic review-wrapper redesign unless parent evidence proves redesign is required
  - this is not solved by hiding leaked fake subprocesses from `findRunReviewMockPids(...)`
  - focused pass/fail status alone is not enough because the issue is specifically full-suite flake behavior
- Explicit non-goals carried forward:
  - no implementation edits in this child lane
  - no test edits in this child lane
  - no Linear mutation from this child lane
  - no `CO-234` `LockFile` changes
  - no coverage weakening

## Parity / Alignment Matrix
- Required because this lane aligns focused fake subprocess cleanup truth with full-suite `npm run test` behavior.
- Current truth:
  - `tests/run-review.spec.ts` includes a regression that creates a hanging fake Codex process, writes `hang-started.txt`, kills the wrapper subprocess through a harness timeout, and checks `findRunReviewMockPids(...)` returns to baseline
  - the issue reports that this surface flakes under full `npm run test`
  - the source payload path provided to this child lane is not present in the child checkout, so the packet is anchored on the provided source pointer and protected issue wording
- Reference truth:
  - cleanup coverage must prove newly-created fake Codex subprocesses are not leaked
  - process cleanup must not target real user review processes
  - adjacent `CO-234` `LockFile` behavior is separate unless parent evidence proves otherwise
- Target truth / intended delta:
  - parent classifies isolated and full-suite behavior independently
  - parent fixes the smallest proven timing, ownership, cleanup, or harness gap
  - the fake subprocess cleanup assertion stays strong and full `npm run test` becomes stable
- Explicitly out-of-scope differences:
  - unrelated suite flakes
  - blanket timeout inflation without root-cause classification and full-suite validation
  - generic wrapper redesign
  - manual host process cleanup as the implementation

## Readiness Gate
- Not done if:
  - the packet drops any protected term or the `CO-234` non-goal
  - parent validation does not include full `npm run test`
  - fake Codex subprocess cleanup coverage is skipped or weakened
  - real review processes can be killed by the cleanup path
- Pre-implementation issue-quality review evidence:
  - the issue is narrower than "fix all full-suite flakes" and narrower than "redesign run-review". The exact protected surfaces identify a specific fake Codex subprocess timeout contract. Parent may widen only after evidence shows the narrow contract cannot explain the flake.
- Safeguard ownership split:
  - child lane owns only docs packet and registry mirror files declared in the handoff
  - parent lane owns source/test changes, root cause, validation, Linear/workpad state, PR, and merge

## Technical Requirements
- Functional requirements:
  - preserve the existing fake Codex subprocess timeout cleanup assertion
  - classify focused `tests/run-review.spec.ts` behavior separately from full `npm run test`
  - ensure any cleanup remains bounded to test/mock children owned by the fake Codex harness
  - ensure `findRunReviewMockPids(...)` still detects leaks rather than masking them
  - keep `CO-234` `LockFile` work out of this issue
  - document any required scope widening before implementing outside the protected surfaces
- Non-functional requirements (performance, reliability, security):
  - restore reliable full-suite test completion
  - avoid real-process termination risk
  - keep validation evidence machine-checkable
- Interfaces / contracts:
  - `tests/run-review.spec.ts`
  - `scripts/run-review.ts`
  - `runReviewCommandSubprocess`
  - `findRunReviewMockPids`
  - fake Codex subprocess harness environment variables
  - `npm run test`

## Architecture & Data
- Architecture / design adjustments:
  - none in this child lane
  - parent should prefer test-harness-local or narrow runtime cleanup changes if evidence supports them
  - parent should not introduce a broader process supervisor abstraction unless the narrow cleanup path cannot be made correct
- Data model changes / migrations:
  - none
- External dependencies / integrations:
  - local process table observation may be used as validation evidence
  - no Linear mutation from this child lane

## Validation Plan
- Tests / checks:
  - child lane: JSON validity for `tasks/index.json` and `docs/docs-freshness-registry.json`
  - parent: focused fake Codex subprocess timeout rerun in `tests/run-review.spec.ts`
  - parent: relevant `run-review` timeout/stall focused lane when needed
  - parent: full `npm run test`
  - parent: process-health scan when needed to prove no new fake Codex subprocess leak remains
  - parent: `node scripts/spec-guard.mjs --dry-run`
  - parent: standalone review and elegance review before PR handoff if implementation lands
- Rollout verification:
  - full `npm run test` completes without the reported flake
  - focused cleanup coverage remains present and green
  - any unrelated flake is explicitly split out rather than absorbed into `CO-251`
- Monitoring / alerts:
  - no new monitor surface; process-health evidence and test artifacts are the audit path

## Open Questions
- Does the fake Codex subprocess timeout case fail in isolation on this head?
- Is the full-suite flake caused by cleanup timing, process-group handling, ambient fake processes, sandbox matching, or another test-order interaction?
- Can the parent keep the fix inside `tests/run-review.spec.ts`, or does evidence require a narrow `scripts/run-review.ts` cleanup adjustment?

## Approvals
- Reviewer: pending parent root cause and validation
- Date: 2026-04-18
