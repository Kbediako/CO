---
id: 20260425-linear-5d015f15-6a72-4096-bf65-162b85a83080
title: CO-367 current-host-unhealthy matcher wording drift
status: done
relates_to: docs/PRD-linear-5d015f15-6a72-4096-bf65-162b85a83080.md
risk: low
owners:
  - Codex
last_review: 2026-05-18
review_notes:
  - 2026-05-18: CO-522 spec lifecycle audit found the linked task checklist has zero unchecked items (29 checked), so this spec is terminal and eligible for implementation-docs archive. Evidence: `out/linear-b642e879-ba50-45ef-b0d9-b059afa9e932-recovery/spec-preexpiry-local-classification.json`.
---

# TECH_SPEC - CO-367 current-host-unhealthy matcher wording drift

## Summary
- Objective: harden `isCurrentHostUnhealthyErrorMessage` against current attach-shell wording drift while preserving intentional legacy coverage.
- Scope: `orchestrator/src/cli/coStatusCliShell.ts` matcher fragments and focused `orchestrator/tests/CoStatusCliShell.test.ts` coverage.
- Constraints: no change to CO-363 active issue projection semantics, attach-shell producer wording, provider-intake overlays, or dashboard rendering.

## Issue-Shaping Contract
- User-request translation carried forward: finish the PR #644 CodeRabbit follow-up for fallback matcher wording drift.
- Protected terms / exact artifact and surface names: `isCurrentHostUnhealthyErrorMessage`, `current-host-unhealthy`, `control_endpoint.json`, `co-status --format json`, `CO-363`, `orchestrator/src/cli/coStatusCliShell.ts`, `orchestrator/src/cli/coStatusAttachCliShell.ts`, `orchestrator/tests/CoStatusCliShell.test.ts`.
- Nearby wrong interpretations to reject: broad CO STATUS projection work, dropping legacy coverage silently, producer wording churn, endpoint retry redesign, and provider-intake changes.
- Explicit non-goals carried forward: no active projection, provider-intake, Linear workflow, dashboard, or endpoint-rotation redesign.

## Parity / Alignment Matrix
- Current truth: the canonical marker is present, but the stale fallback substring drifted from current producer text.
- Reference truth: current invariant attach-shell wording and intentionally supported legacy wording should both classify the host as unhealthy.
- Target truth / intended delta: matcher fragments cover canonical, current, legacy, and rotated-endpoint unhealthy-host messages.
- Explicitly out-of-scope differences: CO-363 projection behavior and attach-shell producer wording.

## Readiness Gate
- Not done if current fallback wording is not matched, legacy wording is not tested, focused regression is missing, or CO-363 active projection semantics change.
- Pre-implementation issue-quality review evidence: parent provider worker reviewed the issue and current `origin/main` source before implementation.
- Safeguard ownership split: parent source/docs; child-lane tests where child-lane runtime completes.

## Technical Requirements
- Match the canonical `current-host-unhealthy` token.
- Match current stale endpoint wording around `control-host unavailable; stale endpoint after control-host restart`.
- Preserve legacy stale endpoint wording around `control_endpoint.json has not rotated to a reachable host`.
- Keep rotated-endpoint unhealthy wording covered.
- Validate through focused `CoStatusCliShell` tests.

## Architecture & Data
- Architecture / design adjustments: local matcher constant extraction only.
- Data model changes / migrations: none.
- External dependencies / integrations: none.

## Validation Plan
- `npx vitest run orchestrator/tests/CoStatusCliShell.test.ts`
- repo guardrails listed in `AGENTS.md` as applicable before review handoff
- manifest-backed standalone review and elegance pass before handoff

## Open Questions
- None known.

## Approvals
- Reviewer: parent provider worker.
- Date: 2026-04-25
