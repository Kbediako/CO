---
id: 20260418-linear-9ce7811e-45ca-4d04-b759-bc5e7da77511
title: CO workflow reclassify blocked CO-231 validation floor on current head
status: done
relates_to: docs/PRD-linear-9ce7811e-45ca-4d04-b759-bc5e7da77511.md
risk: high
owners:
  - Codex
last_review: 2026-05-18
review_notes:
  - 2026-05-18: CO-522 spec lifecycle audit found the linked task checklist has zero unchecked items (20 checked), so this spec is terminal and eligible for implementation-docs archive. Evidence: `out/linear-b642e879-ba50-45ef-b0d9-b059afa9e932-recovery/spec-preexpiry-local-classification.json`.
---

# TECH_SPEC - CO workflow: reclassify blocked CO-231 validation floor on current head

## Summary
- Objective: produce a truthful current-head docs-first packet for `CO-247` that refreshes the blocked `CO-231` validation-floor story before the parent decides whether either inherited blocker still needs a fix.
- Scope: packet-only work in this child lane plus a parent-owned validation plan for the two inherited blocker surfaces.
- Constraints: preserve the `CO-231` source anchors, keep exact protected terms, do not assume either blocker outcome, and do not touch registry mirrors, `docs/TASKS.md`, code, tests, workpad, or PR lifecycle in this child lane.

## Issue-Shaping Contract
- User-request translation carried forward: blocked `CO-231` named two blocker surfaces, but current-head truth may now be narrower. This packet must preserve the issue language while making the parent re-run both surfaces before treating either one as still live.
- Protected terms / exact artifact and surface names:
  - `docs/TASKS.md`
  - `450/450 zero_headroom`
  - `tasks-file-too-large`
  - `npm run docs:check`
  - `orchestrator/tests/SelectedRunProjection.test.ts`
  - `refreshes projection proofs when child-lane reservation ledger placeholders exist`
  - `npm run test`
  - `CO-231`
  - `91749283-6dc8-4df8-aee3-5c9127c1200c`
  - `linear/co-231-doctor-readiness-stability-r2`
  - `out/linear-9ce7811e-45ca-4d04-b759-bc5e7da77511/workpad.md`
- Nearby wrong interpretations to reject:
  - treating the old `450/450` state as proof that the docs surface is still red
  - treating the current `446` line count as proof that the docs surface is green
  - assuming the `SelectedRunProjection` timeout is still live without current-head reruns
  - broadening into generic docs cleanup, archive redesign, or broad projection refactors before evidence
  - dropping the `CO-231` source anchors just because `CO-247` is a follow-up
- Explicit non-goals carried forward:
  - no registry changes in this child lane
  - no `docs/TASKS.md` or archive edits in this child lane
  - no test or production code changes in this child lane
  - no Linear, workpad, review, or PR mutation in this child lane

## Parity / Alignment Matrix
- Required because the lane is about truth-alignment between inherited blocker wording and current-head reality.
- Current truth:
  - the blocked source packet says the docs floor failed at `450/450` with `tasks-file-too-large` / `zero_headroom`
  - the live workpad on `3d3d56959` now records `docs/TASKS.md` at `446` lines, so stale source wording is plausible
  - the exact `SelectedRunProjection` case still exists on this head, but its current isolated and full-suite status has not yet been refreshed
  - `CO-231` still lacks a truthful review-handoff validation floor until those surfaces are reclassified
- Reference truth:
  - source packet anchors `CO-231` / `91749283-6dc8-4df8-aee3-5c9127c1200c` and `linear/co-231-doctor-readiness-stability-r2` remain the authoritative historical context
  - current-head evidence can narrow or clear historical blockers, but it must do so explicitly
- Target truth / intended delta:
  - parent reruns the docs gate and exact `SelectedRunProjection` case on current head
  - each inherited surface is classified independently as live blocker, broader validation-path interaction, or non-repro
  - follow-up implementation, if any, is bounded only to surfaces that remain red
  - `CO-231` handoff truth names the real remaining blocker, if any
- Explicitly out-of-scope differences:
  - generic docs cleanup not required by the current docs gate
  - broad `SelectedRunProjection` refactors without a fresh reproduction
  - replacing or deleting the `CO-231` source anchors

## Readiness Gate
- Not done if:
  - the packet implies the docs surface or the `SelectedRunProjection` surface is already decided without fresh evidence
  - the parent cannot tell which exact surface to rerun next
  - the packet fails to preserve the `CO-231` source anchors and blocked branch
- Pre-implementation issue-quality review evidence:
  - the live workpad already shows one inherited blocker may be stale (`446` lines instead of `450/450`), so the truthful issue is narrower than a blind carry-forward fix. The task is still too exact for the micro-task path because correctness depends on preserved source anchors, exact protected terms, and explicit blocker-vs-non-repro branching.
- Safeguard ownership split:
  - child lane owns only the six docs packet files listed in the handoff
  - parent lane owns current-head reproduction, any registry or docs-headroom changes, any test or production changes, workpad updates, validation, and handoff

## Technical Requirements
- Functional requirements:
  - preserve the `CO-231` source anchors and blocked branch in the packet
  - state explicitly that `docs/TASKS.md` zero-headroom and the `SelectedRunProjection` timeout may each be live blockers or non-repros on the current head
  - require parent-owned reruns for `npm run docs:check` and the exact `SelectedRunProjection` test case before any fix
  - require the parent to record independent verdicts for both inherited surfaces
  - allow implementation only for still-live surfaces and only inside their narrow owning seams
  - require the final `CO-231` handoff to name the real remaining blocker, if any, or record a non-repro closeout truthfully
- Non-functional requirements (performance, reliability, security):
  - keep this child-lane diff docs-only
  - keep the parent follow-up evidence machine-checkable and artifact-backed
  - fail closed on stale blocker assumptions
- Interfaces / contracts:
  - `docs/TASKS.md`
  - `npm run docs:check`
  - `orchestrator/tests/SelectedRunProjection.test.ts`
  - `refreshes projection proofs when child-lane reservation ledger placeholders exist`
  - `npm run test`
  - `out/linear-9ce7811e-45ca-4d04-b759-bc5e7da77511/workpad.md`

## Architecture & Data
- Architecture / design adjustments:
  - no production architecture changes in this child lane
  - parent follow-up should separate docs classification from test classification so the final blocker story stays auditable
  - if the docs surface is red, the parent should use the supported docs headroom/archive path rather than inventing a new mechanism
  - if the test surface is red, the parent should stay inside the narrow projection/test seam needed to explain current-head behavior
- Data model changes / migrations:
  - none in this child lane
  - parent may add artifact paths under `out/linear-9ce7811e-45ca-4d04-b759-bc5e7da77511/**` when reproduction and validation run
- External dependencies / integrations:
  - no external mutation from this child lane
  - parent depends on the saved workpad source and the existing repo commands named above

## Validation Plan
- Tests / checks:
  - child lane: packet-only consistency pass across the six owned files
  - parent: `wc -l docs/TASKS.md`
  - parent: `npm run docs:check`
  - parent: `npx vitest run --config vitest.config.core.ts orchestrator/tests/SelectedRunProjection.test.ts -t "refreshes projection proofs when child-lane reservation ledger placeholders exist"`
  - parent: broader validation only if needed to classify a still-live surface
  - parent: `node scripts/spec-guard.mjs --dry-run`
  - parent: review and elegance pass before any review handoff if a non-trivial diff lands
- Rollout verification:
  - confirm whether the docs surface is current-head red or stale source wording
  - confirm whether the exact `SelectedRunProjection` surface is current-head red or stale source wording
  - update blocked `CO-231` handoff language only after those verdicts are captured
- Monitoring / alerts:
  - no new monitor surface; the key signals are the current-head repro artifacts and handoff truth

## Open Questions
- Is the `docs/TASKS.md` source blocker now a non-repro because the current head sits below the recorded `450/450` threshold, or does the gate still fail due to reserve semantics?
- Does the exact `SelectedRunProjection` case fail in isolation, only under a broader validation path, or not at all on current head `3d3d56959`?
- After both reruns, which remaining surface, if any, still blocks a truthful review handoff for `CO-231`?

## Approvals
- Reviewer: pending parent current-head reproduction and classification
- Date: 2026-04-18
