---
id: 20260501-linear-f57f28e8-e876-4bff-9217-fc5e17ee030f
title: "CO-458 source-root freshness drift"
status: in_progress
owner: Codex
created: 2026-05-01
last_review: 2026-05-01
review_cadence_days: 30
risk_level: high
related_prd: docs/PRD-linear-f57f28e8-e876-4bff-9217-fc5e17ee030f.md
related_action_plan: docs/ACTION_PLAN-linear-f57f28e8-e876-4bff-9217-fc5e17ee030f.md
related_tasks:
  - tasks/tasks-linear-f57f28e8-e876-4bff-9217-fc5e17ee030f.md
review_notes:
  - 2026-05-01: Bounded child lane created the docs-first packet and declared-scope registry mirrors.
---

# TECH_SPEC - CO-458 source-root freshness drift

## Canonical Reference
- PRD: `docs/PRD-linear-f57f28e8-e876-4bff-9217-fc5e17ee030f.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-f57f28e8-e876-4bff-9217-fc5e17ee030f.md`
- Task checklist: `tasks/tasks-linear-f57f28e8-e876-4bff-9217-fc5e17ee030f.md`
- Agent mirror: `.agent/task/linear-f57f28e8-e876-4bff-9217-fc5e17ee030f.md`
- Registry: `tasks/index.json`
- Task snapshot: `docs/TASKS.md`
- Source anchor: `ctx:sha256:574bd8d4264b6ff98102b84cdb1968660a10226102286ef392f76c7647b0ac89#chunk:c000001`

## Summary
- Objective: make control-host status/proof surfaces expose actual source/root provenance and freshness so operators can distinguish stale supervised control-host source roots from unrelated checkout, source-vs-dist, or package-provenance drift.
- Scope:
  - source/root provenance contract for control-host status/proof surfaces
  - actual control-host source root, command path, package root, git ref, and freshness relative to `origin/main`
  - distinct drift classes: shared checkout drift, supervised source-root drift, source-vs-dist drift, and global binary/package provenance drift
  - task packet and declared registry/checklist mirrors
- Constraints:
  - preserve `CO-113`, `CO-25`, `CO-388`, and `CO-450` as historical related lanes, not reopened implementation scope
  - keep status/proof inspection read-only unless another lifecycle path explicitly owns mutation
  - avoid Linear mutation helpers, lifecycle edits, and implementation/test edits in this child lane

## Issue-Shaping Contract
- User-request translation:
  - operators need proof of which source root and package root the control host actually uses
  - freshness must be relative to local `origin/main`, not implied from a clean working tree, a global binary, or fresh `dist/`
  - command path, package root, and git ref are first-class proof fields
  - the implementation must separate shared checkout drift, supervised source-root drift, source-vs-dist drift, and global binary/package provenance drift
- Protected terms / exact artifact and surface names:
  - `actual control-host source root`
  - `command path`
  - `package root`
  - `git ref`
  - `origin/main`
  - `freshness relative to origin/main`
  - `shared checkout drift`
  - `supervised source-root drift`
  - `source-vs-dist drift`
  - `global binary/package provenance drift`
  - `provider-linear-worker-proof.json`
  - `provider-intake-state.json`
  - `co-status`
  - `control-host`
  - `CODEX_ORCHESTRATOR_PACKAGE_ROOT`
  - `dist/`
- Nearby wrong interpretations to reject:
  - stale checkout posture alone satisfies the issue
  - source-vs-dist launch mode alone satisfies the issue
  - shared-root merge closeout sync alone satisfies the issue
  - global binary path or `CODEX_ORCHESTRATOR_PACKAGE_ROOT` alone proves control-host provenance
  - status/proof reads should fetch, checkout, rebuild, restart, or mutate Linear
- Explicit non-goals:
  - no source execution policy redesign
  - no merge-closeout sync redesign
  - no stale checkout advisory redesign
  - no global install cleanup
  - no source/test edits in this child docs lane

## Parity / Alignment Matrix

| Surface | Current Truth | Reference Truth | Target Truth | Explicitly Out Of Scope |
| --- | --- | --- | --- | --- |
| `co-status` and control-host proof | Can present current operational state without complete launch/source/root provenance. | Operator-facing status should say what source root produced the running/proved state. | Exposes actual control-host source root, command path, package root, git ref, and freshness relative to `origin/main`. | Broad status UI redesign. |
| Shared checkout drift | CO-25 and CO-388 already cover separate shared-root/current-checkout concerns. | Shared checkout state is useful context but not proof of the supervised process source root. | Shared checkout drift appears only as its own classification. | Reopening CO-25 or CO-388. |
| Supervised source-root drift | A supervised control-host can remain on a source root/ref different from the checkout an operator inspects later. | The supervised process should supply its own source-root evidence. | Actual control-host source root and git ref are compared against local `origin/main`. | Automatic relaunch or restart. |
| Source-vs-dist drift | CO-113 covers source checkouts preferring source and packaged installs staying dist-only. | Launch mode should be visible when interpreting source/root freshness. | Status/proof records source-backed versus `dist/`-backed command path and whether it matches the package root. | Reworking CO-113. |
| Global binary/package provenance drift | A global binary can launch from a package root unrelated to the visible checkout. | Launch command and package root must be explicit. | Global binary/package mismatch is reported separately from checkout/source freshness. | Npm release or install management. |

## Readiness Gate
- Not done if:
  - control-host status/proof surfaces omit actual control-host source root
  - command path, package root, git ref, or `origin/main` freshness is inferred instead of recorded/projected
  - drift classes are collapsed into one generic stale warning
  - parent implementation mutates Git, Linear, workpad, or PR state from the status/proof read path
  - related historical lanes are reopened or absorbed
  - task registry or checklist mirrors omit the CO-458 packet
- Pre-implementation issue-quality review evidence:
  - 2026-05-01: child lane reviewed nearby packet style, CO-113 source-checkout semantics, CO-25 shared-root closeout semantics, CO-388 stale checkout posture semantics, and current control-host freshness/proof source names. CO-458 remains a distinct source/root provenance issue.
- Safeguard ownership split:
  - child owns docs packet and declared mirrors only
  - parent owns source inspection, implementation, validation, Linear state, workpad, PR, and review lifecycle

## Technical Requirements
1. Define a structured source/root provenance bundle for control-host status/proof surfaces.
2. Capture or project actual control-host source root from the supervised process or its launch/proof context.
3. Capture command path and package root from launch/proof context, including `CODEX_ORCHESTRATOR_PACKAGE_ROOT` when present.
4. Capture git ref for the source root and compare it to local `origin/main` without fetching or mutating repository state by default.
5. Represent source-vs-dist mode explicitly, including `dist/`-backed launch when that is expected for packaged installs.
6. Report global binary/package provenance drift when the command path/package root differs from the visible checkout or expected source root.
7. Keep shared checkout drift separate from supervised source-root drift.
8. Add focused tests for fresh source root, stale source root, missing `origin/main`, command path/package root mismatch, source-backed versus `dist/`-backed launch, and global binary/package mismatch.

## Fallback Expiry / Refactor Decision
- Applies to fallback, compatibility, legacy, stale, cached, break-glass, or minor-seam behavior? Yes.

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| control-host status/proof provenance | Inferred source root / package root from current shell or global binary assumptions | remove fallback | CO-458 | status/proof output lacks actual launch/source/root provenance | 2026-05-01 | 2026-05-01 | 0 days | Explicit provenance bundle implemented | Focused status/proof regressions |

- Large-refactor check: keep this scoped to one governed status/proof provenance surface and one lifecycle phase; a shared helper is acceptable only when it removes duplication across proof, intake, and status projection.
- Minor-seam behavior is acceptable only because CO-458 removes inferred source-root provenance and records one bounded fallback decision.

## Acceptance Criteria
- CO-458 packet docs are created in the declared file scope.
- `tasks/index.json` registers the canonical task id and TECH_SPEC.
- `docs/TASKS.md` includes a CO-458 snapshot.
- Parent implementation exposes actual control-host source root, command path, package root, git ref, and freshness relative to `origin/main`.
- Parent implementation distinguishes shared checkout drift, supervised source-root drift, source-vs-dist drift, and global binary/package provenance drift.
- Parent implementation preserves historical boundaries for `CO-113`, `CO-25`, `CO-388`, and `CO-450`.
- Parent implementation does not mutate Git/Linear/PR/workpad state from status/proof inspection.

## Validation Plan
- Child lane:
  - JSON parse for `tasks/index.json`
  - protected-term scan over declared CO-458 packet/mirror files
  - `git diff --check` over declared touched paths
- Parent lane:
  - focused provenance/drift classification tests
  - implementation gate
  - standalone review
  - elegance/minimality review
  - PR checks and ready-review drain

## Open Questions
- Which parent-owned artifact should carry the canonical provenance bundle first?
- Should missing `origin/main` be `unknown` or `degraded` in human status output while remaining structured in JSON?

## Approvals
- Reviewer: CO-458 provider worker
- Date: 2026-05-01
