---
id: 20260423-linear-8d010b73-9338-4f35-9494-a9a3d773d284
title: CO-329 provider stdin bootstrap recurrence
status: in_progress
relates_to: docs/PRD-linear-8d010b73-9338-4f35-9494-a9a3d773d284.md
risk: high
owners:
  - Codex
last_review: 2026-06-17
review_notes:
  - 2026-06-17: CO-579 pre-expiry review kept this spec active-current; no verified terminal/archive evidence was established in this stream, CO-579 is the live non-terminal docs-freshness owner, and docs/spec gates remain unchanged.
  - 2026-05-18: CO-522 active-spec audit found 1 unchecked task checklist item, so this spec remains active and was reviewed for current lifecycle ownership rather than archived. Evidence: `out/linear-b642e879-ba50-45ef-b0d9-b059afa9e932-recovery/spec-preexpiry-local-classification.json`.
---

## Added by Bootstrap 2026-04-23

## Summary
- Objective: restore truthful provider stdin bootstrap classification and retry evidence after the post-`CO-274` recurrence.
- Scope: `provider-linear-worker` proof classification, guidance, handoff retry evidence, and focused tests.
- Constraints: no fail-open provider behavior, no retry muting, and no issue-specific work for `CO-314` or `CO-328`.

## Issue-Shaping Contract
- User-request translation carried forward: current control-host runs can still show `stderr | Reading additional input from stdin...` with generic `provider_runtime` and retry churn; this lane owns the exact recurrence.
- Protected terms / exact artifact and surface names: `provider-linear-worker`, `control-host`, `stderr | Reading additional input from stdin...`, `provider_runtime`, `provider-linear-worker-proof.json`, `manifest.json`, `linear_audit.attempted_count`, `retry/resumable queue behavior`, `codex_exit_1`.
- Nearby wrong interpretations to reject: `CO-274` closure means no work; dashboard-only stale projection; issue-content work; muting retries.
- Explicit non-goals carried forward: no broader control-host redesign or unrelated Linear state cleanup.

## Parity / Alignment Matrix
- Current truth: current code lacks stdin-specific diagnosis; live `CO-328` is pre-work with `linear_audit.attempted_count: 0`; live `CO-314` has recovered into active work after prior failure churn.
- Reference truth: stdin bootstrap failure is a provider handoff/runtime seam with its own operator guidance and stronger-category precedence.
- Target truth / intended delta: future proofs and retry evidence identify `provider_stdin_bootstrap` and carry the exact signal.
- Explicitly out-of-scope differences: queue policy, issue content, unrelated control-host lifecycle.

## Readiness Gate
- Not done if: future exact stdin bootstrap failures remain only generic `provider_runtime`, or retry evidence hides pre-work failure.
- Pre-implementation issue-quality review evidence: parent inspected live proof artifacts and source; the scoped fix has two implementation files plus tests.
- Safeguard ownership split: parent-owned implementation; no independent child lane remains after source mapping.

## Technical Requirements
- Functional requirements:
  - add and emit dedicated stdin bootstrap diagnostic category
  - preserve stronger diagnostic categories
  - include diagnosis in retry error derivation
- Non-functional requirements (performance, reliability, security):
  - no external dependency or secret behavior changes
  - deterministic test fixtures over live mutation
- Interfaces / contracts:
  - proof JSON `failure_diagnosis`
  - handoff claim `retry_error`

## Architecture & Data
- Architecture / design adjustments: local classifier and handoff helper updates only.
- Data model changes / migrations: additive diagnostic value only.
- External dependencies / integrations: none.

## Validation Plan
- Tests / checks:
  - focused runner and handoff regressions
  - guard/build/lint/test/docs gates before review
- Rollout verification: workpad evidence distinguishes `CO-328` pre-work failure from `CO-314` recovered current work.
- Monitoring / alerts: future provider proofs should report `provider_stdin_bootstrap` for the exact signal.

## Open Questions
- None.

## Approvals
- Reviewer: Pending
- Date: 2026-04-23
