---
id: 20260328-linear-4646e08c-5c53-4072-b09e-ec1aeff6fb21
title: Fix Repeated Post-Merge Core Lane Failures in ProviderIssueHandoff and CLI Command Surface Suites
status: done
owner: Codex
created: 2026-03-28
last_review: 2026-05-03
review_cadence_days: 30
risk_level: high
related_prd: docs/PRD-linear-4646e08c-5c53-4072-b09e-ec1aeff6fb21.md
related_action_plan: docs/ACTION_PLAN-linear-4646e08c-5c53-4072-b09e-ec1aeff6fb21.md
related_tasks:
  - tasks/tasks-linear-4646e08c-5c53-4072-b09e-ec1aeff6fb21.md
review_notes:
  - 2026-05-03: CO-498 live Linear baseline review verified CO-31 is Done/completed with merged PR #315; archived/reclassified this historical packet metadata without deleting packet evidence or weakening docs:freshness/spec-guard.
  - 2026-03-28: Opened from Linear issue `CO-31` after rechecking the live workflow states with `linear issue-context`; the live team exposes `In Progress`, `In Review`, `Merging`, and `Rework`, and the issue remains in `Ready` with no attached PR or workpad comment.
  - 2026-03-28: The workspace started detached at failing merge head `b81084ed121a59ca98f2b522ca1b5b602ceb54e8`; implementation moved onto branch `co-31-fix-post-merge-core-lane` before repo edits.
  - 2026-03-28: Authoritative CI evidence from GitHub Actions run `23680994977` / job `68994221283` shows four repeated failures on the same merge head: `ProviderIssueHandoff.test.ts` stalls in `preserves the current retry attempt when queued retry refetch is skipped`, then `tests/cli-command-surface.spec.ts` times out in `rejects resume without a run id through the binary shell`, `prints delegate-server help`, and `rejects unknown pr subcommands through the binary shell`.
  - 2026-03-28: The packaged Linear helper can read issue context successfully. Mutation calls that initially returned HTTP 400 were recovered by passing the explicit workspace/team/project scope IDs from `issue-context`; with those IDs, the issue now sits in `In Progress`, the single workpad comment is current, and PR `#315` is attached.
  - 2026-03-28: Pre-implementation review approves the narrow path: root-cause the repeated four-test pattern on current evidence, patch the smallest shared seam, avoid broad harness rewrites, and classify the outcome truthfully at closeout.
  - 2026-03-28: `docs-review` succeeded via manifest `/Users/kbediako/Code/CO/.runs/linear-4646e08c-5c53-4072-b09e-ec1aeff6fb21/cli/2026-03-28T09-20-51-476Z-b169d281/manifest.json`.
  - 2026-03-28: Focused local reruns did not reproduce a deterministic runtime bug on merge head `b81084ed121a59ca98f2b522ca1b5b602ceb54e8`; the strongest evidence points to a mixed shared-lane timing problem where the provider queued-retry tests used too-small fake-timer settle budgets and the three binary-shell CLI cases used the short `15000ms` subprocess budget. Evidence: `out/linear-4646e08c-5c53-4072-b09e-ec1aeff6fb21/manual/root-cause-evidence.md`.
  - 2026-03-28: Final standalone review reran clean after the evidence refresh, with the latest artifact at `/Users/kbediako/Code/CO/.runs/linear-4646e08c-5c53-4072-b09e-ec1aeff6fb21/cli/2026-03-28T09-08-12-918Z-060f9d7e/review/output.log`.
  - 2026-03-28: Final-diff validation reran `npm run test` cleanly on branch `co-31-fix-post-merge-core-lane`, passing `299` files / `2568` tests in `248.76s`.
  - 2026-03-28: Live `issue-context` recheck with explicit scope IDs now reports state `In Progress`, active workpad comment `71d7292d-9968-4d0e-a2fd-f5a8377df342`, and attached PR `#315`; only PR-check completion still blocks the `In Review` handoff.
---

# Technical Specification

## Context

`CO-31` is a bounded post-merge stability lane. PR `#314` passed its own `Core Lane`, but the merge head `b81084ed121a59ca98f2b522ca1b5b602ceb54e8` failed twice on `main` in the same `Core Lane` job. The CI log shows one early `ProviderIssueHandoff` retry-timing failure and three late binary-shell CLI timeouts in the same run, which points to either a shared suite interaction or two seams exposed only in full-lane order. The task must follow current evidence rather than assuming the provider seam alone owns the whole failure.

## Requirements

1. Reproduce the repeated `main`-branch failure on merge head `b81084ed121a59ca98f2b522ca1b5b602ceb54e8` or current `main`, or otherwise establish a high-signal root cause with durable evidence.
2. Determine whether the `ProviderIssueHandoff` failure is caused by retry timer sequencing, cleanup ordering, or cross-suite state leakage.
3. Determine whether the CLI command-surface timeouts are caused by subprocess lifecycle regressions, leaked ambient state, or interaction with earlier suites.
4. Prefer the smallest fix at the proven seam over broad harness or runtime rewrites.
5. Add focused regressions that guard the exact failing seams and any required isolation/ordering contract.
6. Run the required validation floor and keep review handoff blocked until the full-lane equivalent is clean.
7. Close the lane with an explicit root-cause classification: CO-24 regression, preexisting flaky interaction, or mixed shared-lane timing problem.

## Current Truth

- The authoritative failing lane is GitHub Actions run `23680994977`, job `68994221283`, started on `2026-03-28T08:44:28Z` and failed after `1063.97s`.
- `orchestrator/tests/ProviderIssueHandoff.test.ts` failed first at `08:44:32Z` with `Condition not met after 256 timer turns.` in `waitForCondition(...)`.
- `tests/cli-command-surface.spec.ts` failed much later at `09:02:13Z` after the rest of the suite finished, and the three failing cases each timed out at `15000ms`.
- This workspace still starts from the same failing merge head, so local reproduction can stay anchored to the shipped tree before any fix.
- Post-fix local validation passed both affected files (`242` tests) and the full core suite (`299` files / `2568` tests) on the same merge-head-derived branch without reproducing a production/runtime failure.
- Nearby historical docs already narrowed adjacent seams:
  - `tasks/specs/1308-coordinator-live-provider-child-run-cli-command-surface-subprocess-completion-follow-up.md`
  - `tasks/specs/1320-coordinator-symphony-post-merge-retry-timer-follow-up.md`
  - `tasks/specs/linear-723139d4-2d01-4022-aa09-e88bda7dfd89.md`

## Validation Plan

- Run docs-review for `linear-4646e08c-5c53-4072-b09e-ec1aeff6fb21` before implementation.
- Record durable reproduction or root-cause notes under `out/linear-4646e08c-5c53-4072-b09e-ec1aeff6fb21/manual/`.
- Use focused Vitest runs for the exact failing files and ordered combinations when needed to surface shared leakage.
- Re-run the full required validation floor after implementation.
- Capture standalone review and explicit elegance review before review handoff.
