---
id: 20260418-linear-4a34011b-104b-4334-871b-a0a014883548
title: CO STATUS observability stop direct co-status --format json fetch refusals on healthy control-hosts
status: in_progress
relates_to: docs/PRD-linear-4a34011b-104b-4334-871b-a0a014883548.md
risk: high
owners:
  - Codex
last_review: 2026-06-17
review_notes:
  - 2026-06-17: CO-579 pre-expiry review kept this spec active-current; no verified terminal/archive evidence was established in this stream, CO-579 is the live non-terminal docs-freshness owner, and docs/spec gates remain unchanged.
  - 2026-05-18: CO-522 active-spec audit found 9 unchecked task checklist items, so this spec remains active and was reviewed for current lifecycle ownership rather than archived. Evidence: `out/linear-b642e879-ba50-45ef-b0d9-b059afa9e932-recovery/spec-preexpiry-local-classification.json`.
---

## Canonical Reference
- Canonical TECH_SPEC: `tasks/specs/linear-4a34011b-104b-4334-871b-a0a014883548.md`
- TECH_SPEC mirror: `docs/TECH_SPEC-linear-4a34011b-104b-4334-871b-a0a014883548.md`
- PRD: `docs/PRD-linear-4a34011b-104b-4334-871b-a0a014883548.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-4a34011b-104b-4334-871b-a0a014883548.md`
- Task checklist: `tasks/tasks-linear-4a34011b-104b-4334-871b-a0a014883548.md`

## Traceability
- Linear issue: `CO-246` / `4a34011b-104b-4334-871b-a0a014883548`
- Source anchor: `ctx:sha256:ed8d7cee28dd1ae41d954a39ca3c8ec761012ade0011d04e9e673f3abb5fa86a#chunk:c000001`
- Parent issue-context evidence: `.runs/linear-4a34011b-104b-4334-871b-a0a014883548/cli/2026-04-18T08-35-23-027Z-c520a178/provider-linear-issue-context-cache.json`
- Docs packet child lane manifest: `.runs/linear-4a34011b-104b-4334-871b-a0a014883548-co-246-docs-packet/cli/2026-04-18T08-43-23-951Z-861dc439/manifest.json`
- Shared source note: the child lane `source-0` payload is metadata-only, so the exact issue wording is anchored on the parent worker's cached live issue body plus the repo seams named in the parent workpad plan.

## Summary
- Objective: give the parent lane a bounded contract for the intermittent direct `co-status --format json` dead-port fetch seam on an otherwise healthy control-host.
- Scope:
  - docs-first packet and registry/checklist mirrors for `CO-246`
  - direct JSON snapshot recovery only
  - explicit boundaries against `CO-243`, `CO-223`, `CO-41`, `CO-194`, and `CO-211`
- Constraints:
  - child lane remains docs-only
  - parent owns implementation, focused tests, docs-review, validation, Linear/workpad reconciliation, PR lifecycle, and merge
  - no broad control-host health redesign

## Issue-Shaping Contract
- User-request translation carried forward: this is the bounded direct dashboard-read fetch-refusal seam. Direct `co-status --format json` can fail with `ECONNREFUSED` even while the control-host read model and provider-intake ledger remain healthy. The parent lane needs the narrowest truthful endpoint re-resolution recovery for the direct JSON path, not a rewrite of `provider_intake`, `tracked.linear`, or generic host-health semantics.
- Protected terms / exact artifact and surface names:
  - `co-status --format json`
  - `ECONNREFUSED`
  - healthy control-host
  - direct JSON snapshot path
  - endpoint re-resolution
  - attach-style recovery
  - intermittent dashboard-read refusal
  - `.runs/local-mcp/cli/control-host/provider-intake-state.json`
  - `polling.stuck=false`
  - `polling.restart_required=false`
  - `coStatusCliShell.ts`
  - `coStatusAttachCliShell.ts`
  - `readUiDatasetWithEndpointRecovery`
  - `resolveAttachTarget`
  - `fetchUiDataset`
- Nearby wrong interpretations to reject:
  - do not fold this into `provider_intake` truth semantics
  - do not fold this into `tracked.linear` truth semantics
  - do not treat this as proof that the control-host itself is unhealthy every time
  - do not solve it by suppressing fetch errors without retry/re-resolution behavior
  - do not widen this into the generic refresh-stuck / restart-loop failure class
- Explicit non-goals carried forward:
  - no top-level `provider_intake` redesign
  - no top-level `tracked.linear` redesign
  - no weakening of genuine host-health alarms
  - no attach-only workaround presented as the intended product path
  - no code or test edits in this child lane

## Parity / Alignment Matrix
- Current truth:
  - direct `co-status --format json` currently resolves the attach target once and calls the UI dataset fetch once
  - attach mode already has endpoint re-resolution/retry behavior after network failure via `readUiDatasetWithEndpointRecovery`
  - live `CO-246` evidence shows intermittent `ECONNREFUSED` while the same window still had a healthy advancing intake ledger and `polling.stuck=false` / `polling.restart_required=false`
- Reference truth:
  - direct JSON consumers should not fail immediately on a stale/dead endpoint when the host remains healthy enough to continue serving after endpoint re-resolution
  - stale-endpoint recovery should remain truthful and fail-closed if the refreshed endpoint is still unreadable
  - adjacent issue boundaries should stay explicit
- Target truth / intended delta:
  - direct `co-status --format json` gains bounded endpoint re-resolution or equivalent truthful recovery
  - attach-mode recovery behavior remains intact and is used as the alignment reference
  - the lane remains distinct from `CO-243`, `CO-223`, `CO-41`, `CO-194`, and `CO-211`
- Explicitly out-of-scope differences:
  - `provider_intake` truth semantics
  - `tracked.linear` truth semantics
  - genuine stuck/restart-required/probe-timeout host failures

## Readiness Gate
- Not done if:
  - direct `co-status --format json` still fails on the first dead-port snapshot read while healthy control-host evidence remains available
  - the direct JSON path hides genuine network/auth/timeout failures instead of staying truthful
  - the implementation broadens into `CO-243`, `CO-223`, `CO-41`, `CO-194`, or `CO-211`
- Pre-implementation issue-quality review evidence:
  - 2026-04-18: child-lane review confirms the issue is narrower than generic control-host failure handling and broader than a superficial string tweak. The packet therefore preserves the exact direct-JSON failure checksum plus the explicit adjacent-issue boundaries.
  - 2026-04-18: the micro-task path is ineligible because correctness depends on exact protected wording, explicit boundary preservation, and a parity contract between direct JSON and attach recovery behavior.
- Safeguard ownership split:
  - child lane owns only the packet files and `tasks/index.json`
  - parent lane owns source/test inspection, implementation, docs-review, focused validation, Linear/workpad reconciliation, PR lifecycle, and merge

## Technical Requirements
1. Create the docs-first packet and registry/checklist mirrors for `CO-246`.
2. Reproduce a direct `co-status --format json` `ECONNREFUSED` while the control-host ledger/read model remains healthy.
3. Add direct JSON recovery that re-resolves the control endpoint or otherwise handles stale/dead-port snapshot reads truthfully.
4. Preserve attach-mode recovery behavior and keep the direct JSON path aligned with it where appropriate.
5. Add regression coverage for stale-endpoint / dead-port direct JSON reads in addition to the existing happy-path tests.
6. Keep closeout evidence explicit that this failure class is distinct from `CO-243`, `CO-223`, `CO-41`, `CO-194`, and `CO-211`.

## Architecture & Data
- Architecture / design adjustments:
  - keep the repair at the CLI shell / attach recovery seam rather than introducing a new control-host health model
  - prefer reusing or closely mirroring `readUiDatasetWithEndpointRecovery` for the direct JSON path
  - preserve current direct JSON output shape while adding bounded endpoint recovery
- Expected implementation surfaces:
  - `orchestrator/src/cli/coStatusCliShell.ts`
  - `orchestrator/src/cli/coStatusAttachCliShell.ts`
- Data model / artifact constraints:
  - preserve existing JSON snapshot schema
  - preserve truthful network/auth/timeout messages when recovery cannot succeed
  - preserve operator evidence that the host remained healthy when the dead-port read occurred

## Validation Plan
- Child-lane checks:
  - `python3 - <<'PY'\nimport json, pathlib\njson.loads(pathlib.Path('tasks/index.json').read_text())\nPY`
  - `rg -n "co-status --format json|ECONNREFUSED|healthy control-host|direct JSON snapshot path|endpoint re-resolution|attach-style recovery|intermittent dashboard-read refusal|CO-243|CO-223|CO-41|CO-194|CO-211|coStatusCliShell.ts|coStatusAttachCliShell.ts|readUiDatasetWithEndpointRecovery|resolveAttachTarget|fetchUiDataset" docs/PRD-linear-4a34011b-104b-4334-871b-a0a014883548.md docs/TECH_SPEC-linear-4a34011b-104b-4334-871b-a0a014883548.md docs/ACTION_PLAN-linear-4a34011b-104b-4334-871b-a0a014883548.md tasks/specs/linear-4a34011b-104b-4334-871b-a0a014883548.md tasks/tasks-linear-4a34011b-104b-4334-871b-a0a014883548.md .agent/task/linear-4a34011b-104b-4334-871b-a0a014883548.md`
  - `git diff --check -- docs/PRD-linear-4a34011b-104b-4334-871b-a0a014883548.md docs/TECH_SPEC-linear-4a34011b-104b-4334-871b-a0a014883548.md docs/ACTION_PLAN-linear-4a34011b-104b-4334-871b-a0a014883548.md tasks/specs/linear-4a34011b-104b-4334-871b-a0a014883548.md tasks/tasks-linear-4a34011b-104b-4334-871b-a0a014883548.md .agent/task/linear-4a34011b-104b-4334-871b-a0a014883548.md tasks/index.json`
- Parent-lane checks:
  - focused `orchestrator/tests/CoStatusCliShell.test.ts`
  - focused `orchestrator/tests/CoStatusAttachCliShell.test.ts`
  - parent docs-review before implementation
  - parent-owned `node scripts/spec-guard.mjs --dry-run` after packet acceptance
- Rollback plan:
  - revert the bounded direct-JSON recovery if it hides genuine failure, broadens into adjacent issue scope, or diverges from truthful attach recovery behavior

## Open Questions
- Should the parent call `readUiDatasetWithEndpointRecovery` directly from `coStatusCliShell.ts`, or introduce a JSON-path wrapper that reuses the same recovery contract?
- Is one endpoint re-resolution attempt sufficient for the direct JSON path, matching attach behavior closely enough for this bounded seam?
- Does the parent want additive closeout evidence for stale-endpoint versus still-unreadable refreshed-endpoint cases, or is the existing error messaging already sufficient once the retry contract exists?

## Approvals
- Reviewer: docs child lane self-review for packet shape and issue boundaries.
- Date: 2026-04-18
