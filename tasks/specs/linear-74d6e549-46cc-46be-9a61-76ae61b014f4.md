---
id: 20260405-linear-74d6e549-46cc-46be-9a61-76ae61b014f4
title: CO: Fix non-interactive npm run test quiet-tail / MessagePort hang
status: done
owner: Codex
created: 2026-04-05
last_review: 2026-05-06
review_cadence_days: 30
risk_level: high
related_prd: docs/PRD-linear-74d6e549-46cc-46be-9a61-76ae61b014f4.md
related_action_plan: docs/ACTION_PLAN-linear-74d6e549-46cc-46be-9a61-76ae61b014f4.md
related_tasks:
  - tasks/tasks-linear-74d6e549-46cc-46be-9a61-76ae61b014f4.md
review_notes:
  - 2026-05-06: CO-503 live Linear audit confirmed CO-84 is `Done` (state_type=completed, updated_at=2026-04-05T01:03:06.230Z) with merged PR #362 attached; this completed-lane spec is reclassified to inactive `done` under canonical owner key `spec-guard:active-specs:last_review=2026-04-05` so historical implementation evidence remains preserved without staying in active-spec freshness.
  - 2026-04-05: Opened from Linear issue `CO-84` in the provider-worker workspace using issue id `74d6e549-46cc-46be-9a61-76ae61b014f4`.
  - 2026-04-05: Packaged `linear issue-context` confirmed the CO workflow states (`Ready`, `In Progress`, `In Review`, `Merging`, `Rework`, `Done`), showed no attached PR, no comments, and no existing workpad comment.
  - 2026-04-05: The issue was moved from `Ready` to `In Progress` before active coding via packaged `linear transition`.
  - 2026-04-05: The workspace started detached and was switched onto branch `linear/co-84-non-interactive-test-quiet-tail`.
  - 2026-04-05: Prior related packet reviewed before implementation: `CO-69` (`linear-dbddac50-c205-4402-a7a5-e2325a9c4373`) documented an earlier patience-only late-tail classification, but the fresh CO-84 issue body adds new non-interactive provider-worker evidence with four ref'd `MessagePort`s after visible late-suite passes, so this lane must rerun reproduction rather than reuse the older no-code conclusion.
  - 2026-04-05: Pre-implementation approval: proceed with a reproduction-first narrow owner search and do not introduce timeout-only or force-exit behavior unless artifact-backed evidence proves no smaller truthful fix exists.
  - 2026-04-05: Fresh current-head control runs showed `npm run test` exits `0` in the provider-worker env after `tests/run-review.spec.ts` and `tests/cli-command-surface.spec.ts` complete. The sampled `MessagePort`s were in-flight Vitest worker-pool handles during the quiet tail, not a reproduced post-suite leak on this head.
  - 2026-04-05: Implementation narrowed to the existing Vitest progress reporter gate so worker/non-interactive lanes emit bounded progress during the long final specs instead of looking hung.
---

# Technical Specification

## Context

`CO-84` is a follow-up after `CO-80` and reopens the non-interactive test-exit question on fresher evidence than the older `CO-69` packet. Fresh current-head reproduction now shows the suite does reach a terminal `0` exit in the provider-worker env, but it spends several minutes quiet inside `tests/run-review.spec.ts` and `tests/cli-command-surface.spec.ts`. The sampled ref'd `MessagePort`s belong to the active Vitest worker pool during that quiet tail. This lane therefore shifts from "find a post-suite leak" to "make the long quiet tail observable so worker validation stays truthful."

## Requirements

1. Reproduce the current non-interactive `npm run test` quiet tail with deterministic evidence in this workspace.
2. Identify whether the sampled ref'd `MessagePort`s are in-flight or leaked on the current head.
3. Preserve the truthful terminal exit for the affected non-interactive worker lane without reducing suite coverage.
4. Preserve truthful non-zero exits for real failures.
5. Emit bounded progress during the long final quiet-tail specs in worker/non-interactive runs.
6. Capture artifact-backed reproduction, fix, and validation evidence for future provider-worker handoffs.

## Current Truth

- Fresh current-head reproduction in the provider-worker env shows `npm run test` exits `0` after `tests/run-review.spec.ts` and `tests/cli-command-surface.spec.ts` complete.
- Active-handle sampling during the quiet tail showed ref'd `MessagePort`s while Vitest was still running those long final specs; later control runs reached `beforeExit`/terminal success rather than a reproduced linger.
- The earlier `CO-69` packet classified a different current-tree run as a long quiet tail; the current lane confirms that classification still applies on this head, but the worker lane required updated machine-checkable proof plus a quieter-tail fix.
- No attached PR, prior workpad, or current review feedback exists yet for this issue.

## Issue-Shaping Contract

- User-request translation carried forward:
  - restore truthful terminal behavior for the current non-interactive worker validation lane
  - classify the actual `MessagePort` samples rather than lowering the validation bar
  - preserve full-suite coverage and truthful failure semantics
- Protected terms / exact artifact and surface names:
  - `npm run test`
  - `quiet-tail`
  - `MessagePort`
  - `tests/cli-frontend-test.spec.ts`
  - `orchestrator/tests/CodexOrchestratorCli.test.ts`
- Nearby wrong interpretations to reject:
  - focused or earlier validation means the current full-suite hang can be ignored
  - visible late-suite success output is equivalent to a terminal green validation
  - force-exit or manual interruption is an acceptable steady-state worker workflow
- Explicit non-goals carried forward:
  - broad test-suite refactors unrelated to the leak owner
  - reducing validation coverage to force a green terminal exit
  - unrelated CO control-host or merge-closeout work

## Parity / Alignment Matrix

- Not applicable.
- Current truth:
  - the current issue claims fresh `MessagePort` leakage in a worker/non-interactive run
  - the exact responsible seam is not yet isolated
  - earlier late-tail classification exists as context only
- Reference truth:
  - provider-worker lanes need a machine-checkable terminal result from the full suite
- Target truth / intended delta:
  - the affected worker lane exits truthfully and terminally after the fix
  - the leaking owner is documented with focused proof
  - future handoffs can cite exact reproduction and validation artifacts
- Explicitly out-of-scope differences:
  - unrelated provider-worker workflow changes
  - general UI/control-host behavior

## Readiness Gate

- Not done if:
  - `npm run test` still appears to finish and then remains non-terminal in the non-interactive worker lane
  - the worker lane still goes silent through the long final specs without bounded progress
  - the fix depends on manual interruption or a blanket timeout
  - failure truth or suite coverage is weakened
  - the final owner and validation path are not machine-checkable
- Pre-implementation issue-quality review evidence:
  - the issue is specific about the failure mode (`MessagePort` leak after visible passes) and should stay bounded to current-tree reproduction, owner isolation, and the smallest truthful fix.
- Safeguard ownership split:
  - docs/workpad and task mirrors: current issue packet files plus packaged `linear` helper
  - reproduction and owner isolation: implicated tests, helpers, Vitest config, and targeted diagnostics
  - implementation: only the files proven to own the lingering handle lifecycle

## Technical Requirements

- Functional requirements:
  - deterministic reproduction path for the non-interactive quiet tail
  - explicit classification of the sampled handle owner
  - terminal `npm run test` exit after the fix
  - bounded progress output during the long final specs in worker/non-interactive runs
  - focused regression validation for the repaired seam
- Non-functional requirements (performance, reliability, security):
  - bounded fix scope
  - no blanket cleanup that can hide failures
  - auditable evidence under the issue task id
- Interfaces / contracts:
  - `package.json` test scripts
  - `vitest.config.core.ts`
  - the implicated test/helper lifecycle seams
  - provider-worker validation and review wrappers

## Architecture & Data

- Architecture / design adjustments:
  - begin with current worker-lane reproduction and process/handle capture
  - classify the quiet tail against the full suite's real completion path before changing exit behavior
  - use the existing Vitest progress reporter and widen its gate for worker/non-interactive envs
  - add regression coverage at the config seam before rerunning the full suite
- Data model changes / migrations:
  - none expected
- External dependencies / integrations:
  - Vitest
  - Node `MessagePort` / worker-thread lifecycle
  - provider-worker validation surfaces

## Validation Plan

- Tests / checks:
  - audited child `docs-review`
  - non-interactive reproduction artifact with handle/owner evidence
  - focused regression coverage for the repaired seam
  - required repo validation floor after implementation
- Rollout verification:
  - prove the worker-lane command exits terminally on green
  - prove red runs still fail truthfully
  - keep the Linear workpad aligned after docs, reproduction, fix, and handoff prep
- Monitoring / alerts:
  - rely on manifests, targeted command logs, and review telemetry

## Open Questions

- Should broader non-TTY lanes reuse the same reporter gating, or should that stay scoped to worker-style env flags in follow-up work?

## Approvals

- Reviewer: pending
- Date: 2026-04-05
