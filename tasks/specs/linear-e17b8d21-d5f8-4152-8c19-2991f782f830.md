---
id: 20260423-linear-e17b8d21-d5f8-4152-8c19-2991f782f830
title: CO-317 fresh-discovery admission backfill after duplicate or terminal cleanup frees capacity
status: in_progress
relates_to: docs/PRD-linear-e17b8d21-d5f8-4152-8c19-2991f782f830.md
risk: high
owners:
  - Codex
last_review: 2026-06-17
review_notes:
  - 2026-06-17: CO-579 pre-expiry review kept this spec active-current; no verified terminal/archive evidence was established in this stream, CO-579 is the live non-terminal docs-freshness owner, and docs/spec gates remain unchanged.
  - 2026-05-18: CO-522 active-spec audit found 5 unchecked task checklist items, so this spec remains active and was reviewed for current lifecycle ownership rather than archived. Evidence: `out/linear-b642e879-ba50-45ef-b0d9-b059afa9e932-recovery/spec-preexpiry-local-classification.json`.
---

## Canonical Reference
- Canonical TECH_SPEC: `tasks/specs/linear-e17b8d21-d5f8-4152-8c19-2991f782f830.md`
- PRD: `docs/PRD-linear-e17b8d21-d5f8-4152-8c19-2991f782f830.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-e17b8d21-d5f8-4152-8c19-2991f782f830.md`
- Task checklist: `tasks/tasks-linear-e17b8d21-d5f8-4152-8c19-2991f782f830.md`

## Traceability
- Linear issue: `CO-317` / `e17b8d21-d5f8-4152-8c19-2991f782f830`
- Shared source 0 anchor: `ctx:sha256:31636a7360560a3f876e35ad4984470e39e99576c5039c3b2f52b24fa1634eda#chunk:c000001`
- Source object id: `sha256:31636a7360560a3f876e35ad4984470e39e99576c5039c3b2f52b24fa1634eda`
- Parent-provided source payload: `.runs/linear-e17b8d21-d5f8-4152-8c19-2991f782f830-docs-regression/cli/2026-04-23T15-44-53-118Z-47a35258/memory/source-0/source.txt`
- Parent-provided origin manifest: `.runs/linear-e17b8d21-d5f8-4152-8c19-2991f782f830-docs-regression/cli/2026-04-23T15-44-53-118Z-47a35258/manifest.json`
- Source note: this child checkout still has no `.runs` tree, so the packet preserves the relaunch checksum and adjacent repo seams instead of quoting a local source payload.

## Summary
- Objective: repair the narrow cleanup-to-admission seam where duplicate cleanup or terminal cleanup frees provider capacity but `fresh_discovery` still does not backfill the newly free slot because admission-budget or `selected_claim` refresh remains stale.
- Scope:
  - provider intake state and selected-claim refresh truth
  - bounded `fresh_discovery` admission after cleanup-freed capacity
  - parent-owned diagnosis of whether stale admission-budget accounting, stale `selected_claim`, or both block the backfill
  - focused parent-owned regression coverage for cleanup-freed capacity
- Constraints:
  - reject generic Linear queue redesign
  - reject duplicate-cleanup advisory generation or terminal-cleanup hook redesign
  - preserve truthful active/queued occupancy and no over-admission
  - child lane remains docs-only and touches only the declared packet files plus `tasks/index.json`

## Issue-Shaping Contract
- User-request translation carried forward: keep the issue narrowly on provider intake, fresh discovery, and admission-budget or `selected_claim` refresh behavior after duplicate cleanup or terminal cleanup frees capacity. The parent needs to fix or diagnose why the newly free slot is not backfilled immediately.
- Protected terms / exact artifact and surface names:
  - `provider-intake-state.json`
  - `provider_intake`
  - `selected_claim`
  - `fresh_discovery`
  - `providerIssueHandoff.ts`
  - `providerIntakeState.ts`
  - `observabilityReadModel.ts`
  - `co-status --format json`
  - `duplicate_cleanup`
  - `terminal_cleanup`
- Nearby wrong interpretations to reject:
  - broad queue redesign
  - unconditional discovery
  - cleanup advisory generation
  - terminal cleanup execution/hook work
  - observability-only `selected_claim` cleanup with no admission fix
  - manual state pruning or host relaunch as steady state
- Explicit non-goals carried forward:
  - no Linear mutation from this child lane
  - no implementation or test edits from this child lane
  - no cleanup-feature redesign
  - no broad scheduler, capacity, or priority rewrite

## Parity / Alignment Matrix
- Current truth:
  - cleanup can make a retained issue non-capacity-consuming while `provider_intake` or `selected_claim` still points at pre-cleanup truth
  - a free slot can exist without immediate `fresh_discovery` backfill
  - operators may need unrelated later refresh churn to see admission recover
- Reference truth:
  - `CO-266` owns duplicate-cleanup advisory generation
  - `CO-5` owns terminal-cleanup execution before workspace deletion
  - `CO-181` and `CO-255` establish that truthful free capacity should admit current `Ready` work
  - `CO-243` keeps `selected_claim` bounded as a summary rather than authoritative queue truth
- Target truth / intended delta:
  - once cleanup frees capacity, admission budgeting and intake summary are refreshed in time for bounded `fresh_discovery`
  - cleaned issues stop consuming the newly free slot unless they still have real live occupancy
  - `fresh_discovery` launches or resumes eligible `Ready` work, or records an explicit no-admission reason
- Explicitly out-of-scope differences:
  - queue redesign
  - cleanup generation or cleanup hook ownership changes
  - observability-only wording updates with no admission behavior correction
  - capacity-limit changes

## Readiness Gate
- Not done if:
  - cleanup frees real capacity but `fresh_discovery` still does not backfill current `Ready` work
  - `selected_claim` or related intake summary remains stale enough to suppress the freed slot
  - the only recovery path is host relaunch, unrelated later refresh, or manual state cleanup
  - the parent fix broadens into generic queue or cleanup redesign
- Pre-implementation issue-quality review evidence:
  - 2026-04-24: current-main re-check confirms the task is narrower than broad fair-admission or cleanup-feature work. Correctness depends on exact provider-intake, `fresh_discovery`, admission-budget, and `selected_claim` boundaries, so the micro-task path is ineligible.
- Safeguard ownership split:
  - child lane owns only the declared docs packet files and `tasks/index.json`
  - parent lane owns authoritative issue context, implementation, tests, validation, workpad, Linear state, and PR lifecycle

## Technical Requirements
- Functional requirements:
  1. Reproduce or model a refresh/poll where duplicate cleanup or terminal cleanup changes a claim from capacity-consuming to non-capacity-consuming truth while at least one `Ready` issue remains eligible.
  2. Determine whether the missed admission is caused by stale admission-budget accounting, stale `selected_claim` / provider-intake refresh, or both.
  3. Recompute, invalidate, or otherwise refresh free-slot truth in time for bounded `fresh_discovery` to use the newly freed slot.
  4. Keep cleaned issues excluded from renewed admission unless live proof shows they remain active/queued.
  5. Preserve real active workers, real queued workers, and still-current action-required cleanup states as capacity-consuming.
  6. Keep cleanup generation/execution semantics unchanged aside from the admission backfill that follows truthful cleanup.
  7. Add focused coverage that proves cleanup-freed capacity backfills `fresh_discovery` without over-admission.
- Non-functional requirements:
  - minimal localized change in existing provider-intake/admission seams
  - no unbounded discovery or queue rewrites
  - no token or private payload leakage in tests or diagnostics
  - preserve audit history in `provider-intake-state.json`
  - fail closed when live occupancy still exists or cleanup truth is ambiguous
- Interfaces / contracts:
  - `orchestrator/src/cli/control/providerIssueHandoff.ts`
  - `orchestrator/src/cli/control/providerIntakeState.ts`
  - `orchestrator/src/cli/control/observabilityReadModel.ts`
  - `provider-intake-state.json`
  - `co-status --format json`

## Architecture & Data
- Architecture / design adjustments:
  - inspect whether cleanup-freed capacity is computed only once before cleanup mutates occupancy
  - inspect whether `selected_claim` or summary refresh lags behind occupancy truth and accidentally blocks fresh discovery
  - prefer one shared predicate or invalidation seam if the same cleaned-row classification affects both admission and summary surfaces
  - keep the change inside existing provider intake and fresh-discovery code paths rather than adding new scheduler layers
- Data model changes / migrations:
  - no migration expected
  - existing claim fields should remain sufficient
  - an additive diagnostic reason may be acceptable if needed to distinguish "no slot" from "slot freed but not backfilled"
- External dependencies / integrations:
  - local provider-intake artifacts
  - existing cleanup/advisory outputs
  - focused provider handoff and read-model tests

## Validation Plan
- Child-lane checks:
  - `node -e "JSON.parse(require('fs').readFileSync('tasks/index.json','utf8'))"`
  - protected-term grep across the touched packet files
  - `git diff --check --` on the touched files only
- Parent-lane checks:
  - focused `ProviderIssueHandoff` regression for cleanup-freed capacity triggering `fresh_discovery`
  - focused `ProviderIntakeState`, `ControlRuntime`, or read-model coverage if `selected_claim` / intake summary behavior changes
  - `node scripts/spec-guard.mjs --dry-run`
  - parent-owned docs-review and normal validation after patch import
- Required focused coverage:
  - one variant where duplicate cleanup frees capacity
  - one variant where terminal cleanup frees capacity, or one shared predicate proving both paths
  - proof that real active or queued live occupancy still blocks admission
  - proof that cleaned issues do not immediately reoccupy the freed slot without live proof
- Rollout verification:
  - parent records evidence that a newly free slot is consumed by bounded fresh discovery or an explicit no-eligible-work reason
  - parent records whether stale `selected_claim` participated in the bug or was only a symptom

## Open Questions
- Should the fix refresh free-slot budgeting immediately after cleanup, or invalidate and rebuild intake summary before discovery executes?
- Is `selected_claim` part of the admission decision path here, or only an adjacent stale-summary symptom that must stay aligned?
- What is the smallest shared predicate that covers duplicate-cleanup and terminal-cleanup capacity release without broadening the queue model?

## Approvals
- Reviewer: pending parent docs-review and implementation validation
- Date: 2026-04-24
