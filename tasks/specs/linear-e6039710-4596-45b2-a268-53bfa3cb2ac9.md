---
id: 20260427-linear-e6039710-4596-45b2-a268-53bfa3cb2ac9
title: "CO-401 docs freshness maintenance owner verification"
relates_to: docs/PRD-linear-e6039710-4596-45b2-a268-53bfa3cb2ac9.md
risk: high
owners:
  - Codex
last_review: 2026-05-20
---

# TECH_SPEC - CO-401 docs freshness maintenance owner verification

## Summary
- Objective: register the CO-401 docs-first lane and repair the exact `docs:freshness` / `docs:freshness:maintain` stale Mar 27 owner-verification failure without broadening into CO-390 release-detector work.
- Scope: docs-first packet, task checklist mirrors, `tasks/index.json`, `docs/TASKS.md`, reviewed stale-cohort freshness metadata, and validation evidence.
- Constraints:
  - no stale metadata changes without recorded review evidence
  - preserve `docs:freshness:maintain` as the canonical owner key
  - parent owns Linear state, validation, review, workpad, and PR lifecycle

## Issue-Shaping Contract
- User-request translation carried forward: CO-401 needs a packet that keeps both freshness surfaces in scope: `30 stale docs` at `last_review=2026-03-27` with `blocking_changed_paths=[]`, plus `CO-343 owner verification failed` for the `docs:freshness:maintain canonical owner key`.
- Protected terms / exact artifact and surface names:
  - `docs:freshness`
  - `docs:freshness:maintain`
  - `last_review=2026-03-27`
  - `30 stale docs`
  - `blocking_changed_paths=[]`
  - `CO-343 owner verification failed`
  - `docs:freshness:maintain canonical owner key`
  - `npm run docs:freshness`
  - `npm run docs:freshness:maintain`
- Nearby wrong interpretations to reject:
  - the stale cohort can be refreshed without reviewing the affected packets
  - `blocking_changed_paths=[]` makes CO-401 a no-op
  - the owner issue should be fixed by renaming the canonical owner key
  - CO-401 should broaden into CO-390 release-detector surfaces
- Explicit non-goals carried forward:
  - no CO-390 release-detector behavior, scheduling, or canonical release-intake edits
  - no scripts/tests/package/release-detector edits
  - no Linear mutations beyond required provider-worker state, workpad, PR attachment, and review handoff

## Parity / Alignment Matrix

| Surface | Current Truth | Reference Truth | Target Truth | Explicitly Out Of Scope |
| --- | --- | --- | --- | --- |
| Freshness stale cohort | `docs:freshness` reports `30 stale docs` at `last_review=2026-03-27` and `blocking_changed_paths=[]`. | Freshness debt is reviewed, classified, and refreshed by owner evidence. | Review the exact stale Mar 27 cohort and restore truthful `npm run docs:freshness` output. | Broad sweeps, blind date bumps, or CO-390 release-detector edits. |
| Maintenance owner | `docs:freshness:maintain` reports `CO-343 owner verification failed`; direct helper verification also fails project scope. | `docs:freshness:maintain` remains the canonical owner key for freshness maintenance, and `docs/docs-catalog.json` is the live owner source. | CO-401 records the owner-verification evidence, re-homes `rolling_freshness_cohorts.owner_issue` to live same-project `CO-401`, and refreshes the reviewed cohort metadata so unrelated clean diffs are no longer blocked by the invalid CO-343 owner. | Owner-key replacement or unrelated owner policy changes. |
| Task registration | CO-401 has no packet or task registration mirrors at lane start. | `tasks/index.json` uses `YYYYMMDD-linear-<uuid>` ids and links canonical spec/checklist paths. | Register CO-401 as `20260427-linear-e6039710-4596-45b2-a268-53bfa3cb2ac9`. | Linear state and PR lifecycle. |

## Readiness Gate
- Not done if:
  - any protected term is missing or renamed
  - stale metadata changes lack recorded review evidence
  - the owner key is renamed away from `docs:freshness:maintain`
  - validation commands omit `npm run docs:freshness` or `npm run docs:freshness:maintain`
  - CO-401 is narrowed away from either stale docs or owner verification
- Pre-implementation issue-quality review evidence:
  - 2026-04-27: child lane preserved the protected terms and rejected adjacent wrong interpretations from the handoff.
  - 2026-04-27: micro-task path is unavailable because correctness depends on exact protected wording, owner-key preservation, and task-registration parity.
- Safeguard ownership split:
  - child lane produced the initial docs packet but its stale-base patch was rejected
  - parent adapted the packet on current `origin/main` and owns freshness metadata, validation, Linear integration, and PR lifecycle

## Technical Requirements
- Functional requirements:
  - Create the PRD, canonical spec, docs spec mirror, action plan, task checklist, and `.agent` mirror under the declared CO-401 paths.
  - Add the canonical `tasks/index.json` item for `20260427-linear-e6039710-4596-45b2-a268-53bfa3cb2ac9`.
  - Add one `docs/TASKS.md` snapshot for CO-401.
  - Preserve `blocking_changed_paths=[]` as issue evidence, not as a no-op signal.
  - Preserve `docs:freshness:maintain` as the canonical owner key.
  - Re-home `docs/docs-catalog.json` `rolling_freshness_cohorts.owner_issue` to live same-project `CO-401` after verifying `CO-343` is invalid.
  - Refresh `docs/docs-freshness-registry.json` only after reviewing the five affected Mar 27 packets.
  - Update stale TECH_SPEC/task-spec frontmatter dates where they still carry `last_review: 2026-03-27`.
- Non-functional requirements:
  - Keep the patch docs/freshness-metadata-only.
  - Avoid CO-390 release-detector scope.
  - Keep validation evidence in the task checklist and Linear workpad.
- Interfaces / contracts:
  - `tasks/index.json` remains valid JSON.
  - Task mirrors point to the same PRD/spec/action/checklist surfaces.

## Architecture & Data
- Architecture / design adjustments: none in this child lane.
- Data model changes / migrations: none.
- External dependencies / integrations: parent-owned `docs:freshness`, `docs:freshness:maintain`, Linear issue/workpad, and PR lifecycle.

## Validation Plan
- Child-lane / packet checks:
  - parse `tasks/index.json`
  - verify changed paths are restricted to declared file scope
  - verify protected terms appear in the CO-401 packet
- Parent-owned implementation checks:
  - `node scripts/spec-guard.mjs`
  - `node scripts/spec-guard.mjs --dry-run`
  - `npm run docs:freshness`
  - `npm run docs:freshness:maintain -- --format json`
  - any normal parent validation floor needed after stale cohort or registry changes
- Rollout verification:
  - parent confirms `30 stale docs`, `last_review=2026-03-27`, `blocking_changed_paths=[]`, and `CO-343 owner verification failed` are resolved or truthfully reclassified
  - parent records the canonical owner key as `docs:freshness:maintain`

## Open Questions
- None. Parent reconciled live Linear issue context and source-0 metadata before implementation.

## Approvals
- Reviewer: parent CO-401 lane issue-quality review
- Date: 2026-04-27
