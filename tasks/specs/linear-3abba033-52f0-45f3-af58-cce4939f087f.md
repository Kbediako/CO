---
id: 20260513-linear-3abba033-52f0-45f3-af58-cce4939f087f
title: "CO-480 MultiAgentV2 0.128 thread-cap audit"
relates_to: docs/PRD-linear-3abba033-52f0-45f3-af58-cce4939f087f.md
risk: medium
owners:
  - Codex
last_review: 2026-06-17
related_prd: docs/PRD-linear-3abba033-52f0-45f3-af58-cce4939f087f.md
related_action_plan: docs/ACTION_PLAN-linear-3abba033-52f0-45f3-af58-cce4939f087f.md
task_checklists:
  - tasks/tasks-linear-3abba033-52f0-45f3-af58-cce4939f087f.md
review_notes:
  - 2026-06-17: Reviewed during the hard spec-guard freshness tranche; kept active because this file did not contain terminal evidence sufficient for archival or inactive reclassification.
---

## Summary
- Objective: add current Codex CLI 0.128 MultiAgentV2 thread-cap truth to CO docs/doctor/default/init posture without weakening CO-354 old-path rejection.
- Scope:
  - `orchestrator/src/cli/doctor.ts`
  - `orchestrator/src/cli/codexDefaultsSetup.ts`
  - `orchestrator/src/cli/init.ts`
  - `orchestrator/src/cli/utils/codexFeatures.ts`
  - targeted doctor/default/init tests and command probes
  - docs/skills that describe `agents.max_threads` under multi-agent modes
- Constraints:
  - do not write `agents.max_threads` under `features.multi_agent_v2=true`
  - do not promote `multi_agent_v2` as default
  - do not remove stable `features.multi_agent=true` `[agents] max_threads = 12`
  - do not expand into model posture, cloud, release, or broad delegation redesign

## Issue-Shaping Contract
- User-request translation carried forward: CO-480 is the narrow follow-up from CO-466/CO-354 to document, probe, and classify the Codex CLI 0.128 v2-specific cap path `features.multi_agent_v2.max_concurrent_threads_per_session` while preserving old-path rejection and stable multi-agent defaults.
- Protected terms / exact artifact and surface names:
  - `Codex CLI 0.128`
  - `MultiAgentV2`
  - `features.multi_agent_v2=true`
  - `agents.max_threads`
  - `features.multi_agent_v2.max_concurrent_threads_per_session`
  - `CO-354`
  - `CO-466`
  - `doctor/default/init behavior`
- Nearby wrong interpretations to reject:
  - re-enable `agents.max_threads` under MultiAgentV2
  - make MultiAgentV2 the default
  - delete stable max_threads guidance
  - treat a docs-only mention as enough if doctor/default/init stay silently stale
- Explicit non-goals carried forward:
  - no model posture change
  - no workflow pin promotion
  - no cloud-canary rebaseline
  - no broad delegation redesign

## Parity / Alignment Matrix

| Surface | Current truth | Reference truth | Target truth / intended delta | Explicitly out-of-scope differences |
| --- | --- | --- | --- | --- |
| Old cap path | CO omits/rejects `agents.max_threads` under MultiAgentV2. | Codex CLI 0.128 still rejects `agents.max_threads` when v2 is enabled. | Preserve CO-354 behavior and tests. | Re-enabling `agents.max_threads` under v2. |
| New v2 cap path | CO lacks guidance/classification. | Codex CLI 0.128 accepts `features.multi_agent_v2.max_concurrent_threads_per_session`. | Support or explicitly classify as user-owned with actionable doctor/docs guidance. | Seeding v2 cap by default. |
| Stable multi-agent | CO seeds `[agents] max_threads = 12`. | Stable `features.multi_agent=true` remains current normal guidance. | Preserve unchanged. | Removing or weakening stable guidance. |

## Readiness Gate
- Not done if:
  - docs still imply no current MultiAgentV2 thread-cap path exists
  - defaults/init write `agents.max_threads` under v2
  - doctor gives no actionable v2 cap guidance or user-owned classification
  - old rejection and new acceptance are not covered by tests/probes
  - the patch changes model/cloud/release/delegation scope
- Pre-implementation issue-quality review evidence:
  - Live issue-context found CO-480 in `Rework` with attached stale PR #798 and one stale workpad.
  - Full PR feedback sweep found #798 open, merge-dirty, Core Lane red, and review threads resolved before reset.
  - Rework reset closed #798, deleted the old workpad, and rebuilt `linear/co-480-rework` from `origin/main` at `e7ed674534`.
  - Fresh source scan found CO-354 old-path guard code and no CO-480 packet files on current main.
- Safeguard ownership split:
  - Parent owns lifecycle reset, docs packet, source inspection, implementation, tests/probes, PR, and Linear handoff.
  - Child lanes are deferred until a safe file/phase split exists after source inspection; the first-turn reset recorded `forbid_parallel` / `parent_only_mutation`.

## Technical Requirements
- Functional requirements:
  1. Preserve `codexDefaultsSetup` behavior that deletes/omits `agents.max_threads` when MultiAgentV2 is enabled.
  2. Preserve `init` behavior that avoids copying invalid `agents.max_threads` under enabled v2.
  3. Add operator-facing guidance or explicit user-owned classification for `features.multi_agent_v2.max_concurrent_threads_per_session`.
  4. Keep stable `[agents] max_threads = 12` writes/checks for `features.multi_agent=true` and older/non-v2 configurations.
  5. Add focused regression tests or command probes for rejected old path and accepted new path.
  6. Update docs/skills that currently stop at the old "omit `agents.max_threads`" guidance.
- Non-functional requirements:
  - minimal diff
  - deterministic tests/probes where possible
  - no hidden fallback or silent stale guidance
  - no unrelated lifecycle or model changes
- Interfaces / contracts:
  - doctor summary remains readable in text and JSON result surfaces
  - defaults/init remain conservative and do not seed experimental v2 unless explicitly owned
  - docs-first registry uses `tasks/index.json` `items[]`

## Fallback Expiry / Refactor Decision
- Applies to fallback, compatibility, legacy, stale, cached, break-glass, or minor-seam behavior? `Yes`.
- Required decision table:

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| MultiAgentV2 cap guidance | Stale "omit only" guidance after 0.128 adds a replacement cap surface | remove fallback | CO-480 | v2 thread-cap tuning after Codex CLI 0.128 | 2026-05-13 | 2026-05-13 | N/A | docs/doctor classify the v2 cap surface | focused tests/probes plus docs/review gates |

- Large-refactor check: a large refactor is not preferred because the missing truth is one additional config surface in existing doctor/default/init guidance, not a new authority split or cross-phase lifecycle redesign.

## Architecture & Data
- Architecture / design adjustments:
  - Reuse existing MultiAgentV2 detection helpers where possible.
  - Extend doctor guidance/classification narrowly instead of adding another defaults authority.
  - Keep default config mutation conservative: omit invalid old key under v2 and do not seed a v2 cap unless the issue evidence proves CO owns that write.
- Data model changes / migrations:
  - Task registry and docs-freshness rows for the new packet only.
- External dependencies / integrations:
  - Local Codex CLI command probe for old/new cap behavior.

## Validation Plan
- Tests / checks:
  - focused `Doctor` tests for v2 cap guidance/classification
  - focused `CodexDefaultsSetup` tests for old-path omission and stable baseline preservation
  - focused `InitTemplates` tests for old-path omission and stable baseline preservation
  - local command probe for `agents.max_threads` rejection under `features.multi_agent_v2=true`
  - local command probe for `features.multi_agent_v2.max_concurrent_threads_per_session` acceptance or persisted-path classification
  - `node scripts/delegation-guard.mjs`
  - `node scripts/spec-guard.mjs --dry-run`
  - `npm run build`
  - `npm run lint`
  - `npm run test`
  - `npm run docs:check`
  - `npm run docs:freshness`
  - `npm run repo:stewardship`
  - `node scripts/diff-budget.mjs`
  - manifest-backed `codex-orchestrator review`
  - explicit elegance/minimality pass
- Rollout verification:
  - PR attached to CO-480 before review handoff.
  - `pr ready-review` exits cleanly before `In Review`.
- Monitoring / alerts:
  - N/A for local CLI/docs behavior.

## Open Questions
- Resolved 2026-05-13: active local `codex-cli 0.130.0` rejects `agents.max_threads` with `features.multi_agent_v2=true`, accepts `features.multi_agent_v2.max_concurrent_threads_per_session`, and also accepts `multi_agent_v2.max_concurrent_threads_per_session`. CO uses the protected feature path as primary guidance and treats either cap path as user-owned when already configured.

## Approvals
- Reviewer: docs-review child stream captured inherited `docs:freshness:maintain` blocker routed to CO-522; local implementation validation is current through pack smoke except the known repo-wide `docs:freshness` baseline failure. Standalone review completed on 2026-05-13 with `review_verdict=clean`.
- Date: 2026-05-13.
