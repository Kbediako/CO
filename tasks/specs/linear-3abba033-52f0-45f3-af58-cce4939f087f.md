---
id: 20260513-linear-3abba033-52f0-45f3-af58-cce4939f087f
title: CO-480 MultiAgentV2 0.128 feature-specific thread cap support
relates_to: docs/PRD-linear-3abba033-52f0-45f3-af58-cce4939f087f.md
risk: medium
owners:
  - Codex
last_review: 2026-05-13
related_action_plan: docs/ACTION_PLAN-linear-3abba033-52f0-45f3-af58-cce4939f087f.md
task_checklists:
  - tasks/tasks-linear-3abba033-52f0-45f3-af58-cce4939f087f.md
---

## Summary
- Objective: classify or support the Codex CLI 0.128 MultiAgentV2-specific thread cap path.
- Scope: `features.multi_agent_v2.max_concurrent_threads_per_session`, old-path rejection for `agents.max_threads`, stable `features.multi_agent=true` guidance, doctor/default/init behavior, docs wording, and focused tests/probes.
- Constraints: no model posture, cloud, release, workflow pin, or broad delegation changes.

## Issue-Shaping Contract
- User-request translation carried forward: keep CO aligned with latest Codex CLI 0.128 MultiAgentV2 behavior while preserving stable multi-agent defaults.
- Protected terms / exact artifact and surface names: Codex CLI 0.128, MultiAgentV2, `features.multi_agent_v2=true`, `agents.max_threads`, `features.multi_agent_v2.max_concurrent_threads_per_session`, CO-354, CO-466, doctor/default/init behavior.
- Nearby wrong interpretations to reject: do not re-enable invalid `agents.max_threads` under v2, promote v2 as default, remove stable max_threads guidance, or widen into model/cloud/release/delegation work.
- Explicit non-goals carried forward: no `gpt-5.5` posture change, no cloud-canary rebaseline, no workflow pin promotion, no broad delegation redesign.

## Parity / Alignment Matrix

| Surface | Current | Reference | Target |
| -- | -- | -- | -- |
| Old cap path | CO omits/rejects `agents.max_threads` under v2. | 0.128 still rejects old path. | Preserve CO-354 guard. |
| New cap path | CO does not classify `features.multi_agent_v2.max_concurrent_threads_per_session`. | 0.128 accepts the feature-specific cap. | Support it or classify it as user-owned with actionable guidance. |
| Stable multi-agent | CO seeds `[agents] max_threads = 12`. | Stable guidance remains valid. | Preserve unchanged. |

## Readiness Gate
- Not done if: old invalid path can be written, new accepted path stays hidden, stable guidance regresses, or validation lacks both old-path and new-path evidence.
- Pre-implementation issue-quality review evidence: 2026-05-13 packet repair confirmed CO-480 has valid scope and no tracked packet files yet, which was blocking backlog promotion.
- Safeguard ownership split: this packet branch only repairs traceability. Provider-worker implementation owns code/tests/docs behavior after admission.

## Technical Requirements
- Functional requirements:
  - preserve old-path rejection
  - classify or support new v2-specific cap
  - preserve stable multi-agent default
  - update docs and doctor/default/init output as needed
  - add focused tests or probes
- Non-functional requirements:
  - fail closed on ambiguous feature-list output
  - avoid defaulting users into experimental v2
  - keep messages actionable
- Interfaces / contracts:
  - `codex features list`
  - `codex-orchestrator doctor`
  - `codex-orchestrator codex defaults`
  - `codex-orchestrator init codex`
  - docs guidance

## Fallback Expiry / Refactor Decision

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| MultiAgentV2 thread config | Treating v2 as only "omit max_threads" after 0.128 added a feature-specific cap | remove fallback | CO-480 | `features.multi_agent_v2=true` with Codex CLI 0.128+ | 2026-05-03 | 2026-05-13 | No retained fallback | v2 cap is supported or explicitly user-owned | focused doctor/default/init tests or command probes |

- Large-refactor check: bounded update is preferred; escalate only if duplicated config authorities appear.

## Architecture & Data
- Architecture / design adjustments: extend existing CO-354 feature/config checks rather than creating a separate config model.
- Data model changes / migrations: none expected.
- External dependencies / integrations: local Codex CLI feature and config behavior.

## Validation Plan
- Tests / checks: focused feature parser and doctor/default/init tests, command probe or fixture, normal validation floor, standalone review.
- Rollout verification: stable users still see stable guidance; v2 users get no invalid `agents.max_threads` and clear v2-cap guidance.
- Monitoring / alerts: normal docs/check and provider-worker review handoff.

## Open Questions
- Should generated config ever write the v2-specific cap, or should CO classify it as user-owned only?

## Approvals
- Reviewer: Pending.
- Date: 2026-05-13
