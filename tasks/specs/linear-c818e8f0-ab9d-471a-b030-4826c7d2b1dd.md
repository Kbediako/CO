---
id: 20260519-linear-c818e8f0-ab9d-471a-b030-4826c7d2b1dd
title: CO-560 prevent provider-worker review-handoff false retry after clean nested review evidence
relates_to: docs/PRD-linear-c818e8f0-ab9d-471a-b030-4826c7d2b1dd.md
risk: high
owners:
  - Codex
last_review: 2026-06-17
review_notes:
  - 2026-06-17: CO-579 pre-expiry review kept this spec active-current; no verified terminal/archive evidence was established in this stream, CO-579 is the live non-terminal docs-freshness owner, and docs/spec gates remain unchanged.
related_action_plan: docs/ACTION_PLAN-linear-c818e8f0-ab9d-471a-b030-4826c7d2b1dd.md
task_checklists:
  - tasks/tasks-linear-c818e8f0-ab9d-471a-b030-4826c7d2b1dd.md
---

# TECH_SPEC - CO-560 prevent provider-worker review-handoff false retry after clean nested review evidence

## Canonical Reference
- PRD: `docs/PRD-linear-c818e8f0-ab9d-471a-b030-4826c7d2b1dd.md`
- TECH_SPEC mirror: `docs/TECH_SPEC-linear-c818e8f0-ab9d-471a-b030-4826c7d2b1dd.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-c818e8f0-ab9d-471a-b030-4826c7d2b1dd.md`
- Task checklist: `tasks/tasks-linear-c818e8f0-ab9d-471a-b030-4826c7d2b1dd.md`
- Linear issue: `CO-560` / `c818e8f0-ab9d-471a-b030-4826c7d2b1dd`
- Source anchor: `ctx:sha256:590ab51fb1f4bf827f3704ee1c7dfe50e3ddc370664e87c4fd92856e52a35118#chunk:c000001`

## Summary
- Objective: make provider-worker review handoff converge on explicit governed review evidence used by the worker, including nested implementation-gate evidence, without weakening fail-closed review semantics.
- Scope:
  - parent provider-linear-worker review evidence resolution.
  - focused CO-515-shaped provider-worker proof/review fixture.
  - control-host/co-status rehydration behavior for already-handed-off `In Review` issues.
- Constraints:
  - no missing-telemetry-as-clean shortcut.
  - no weakening of `review_verdict`, `contract_validation`, or `contract_overall_verdict`.
  - no manual provider-intake edits, run deletion, or manifest quarantine as the accepted fix.

## Issue-Shaping Contract
- User-request translation carried forward: successful `issue_review_handoff` with explicit clean nested governed review evidence must not become retryable failed WIP only because parent `review/telemetry.json` is missing.
- Protected terms / exact artifact and surface names: `provider-linear-worker-proof.json`, `owner_status=succeeded`, `end_reason=issue_review_handoff`, `review/telemetry.json`, `implementation-gate`, `review_verdict`, `contract_validation`, `contract_overall_verdict`, `provider_linear_worker_review_unknown`, `provider-intake-state.json`, `co-status --format json`, `In Review`, `PR #836`, `CO-515`.
- Nearby wrong interpretations to reject: missing telemetry is clean; empty findings are clean; Linear `In Review` alone proves review; PR green alone proves review; manual artifact quarantine is product behavior; parser wording issues are in scope by default.
- Explicit non-goals carried forward: no review parser relaxation, no contract weakening, no provider-intake manual cleanup, no broad control-host retry redesign.

## Parity / Alignment Matrix

| Surface | Current truth | Reference truth | Target truth / intended delta | Out of scope |
| --- | --- | --- | --- | --- |
| Parent review contract | Parent requires top-level `review/telemetry.json` and fails unknown when it is absent. | Explicit clean governed review evidence can be nested/child-owned if worker proof/audit records it. | Parent consumes only explicit, valid, run/task-bound clean evidence or reports the exact expected missing path. | Accepting implicit clean from PR/Linear state. |
| Nested implementation-gate evidence | Clean evidence exists but is invisible to the parent stage. | Child manifests are governed review artifacts when their telemetry/contract validates. | Nested evidence can satisfy handoff if it is explicitly referenced and clean. | Arbitrary manifest scanning without binding. |
| Control-host rehydration | Historical completed provider runs can be counted as retryable after handoff. | Successful handoff plus live `In Review` is not active implementation work. | Rehydration/status exclude the contained handoff shape from active/retrying WIP. | Hiding real failed active runs. |
| Fail-closed review semantics | Unknown review remains a blocker. | Unknown review must remain a blocker. | Negative fixtures prove no explicit clean evidence still fails closed. | Review-verdict parser rewrites. |

## Readiness Gate
- Not done if clean nested evidence still emits `provider_linear_worker_review_unknown`, if missing evidence is accepted as clean, if status still queues the contained handoff shape as active/retrying, or if the fix depends on artifact deletion/quarantine.
- Pre-implementation issue-quality review evidence: parent provider worker read live issue-context, GPT Pro advisory comment, repo workflow skills, and prior review-verdict memory on 2026-05-19.
- Safeguard ownership split: parent owns production code, tests, docs packet, Linear workpad, PR lifecycle, and review handoff.

## Technical Requirements
- Review evidence resolution must require an explicit governed signal:
  - `review_verdict` is `clean`.
  - `contract_mode: enforce` evidence, when present, has valid `contract_validation.status`, non-empty `contract_overall_verdict`, and does not report `findings`, `blocked`, or `unknown`.
  - evidence is tied to the current provider run, worker proof, audit entry, or child/implementation-gate manifest rather than arbitrary historical files.
  - when top-level telemetry is required and absent, the failure records the exact expected telemetry path.
- Provider-worker stage must not emit `provider_linear_worker_review_unknown` for the CO-515 contained shape when explicit clean governed evidence exists.
- Control-host rehydration/status must not count a completed successful `issue_review_handoff` for an issue already in review as active, retrying, or failed current WIP solely because the historical run remains discoverable.
- Negative coverage must preserve fail-closed behavior for missing evidence, unknown verdict, findings, invalid contract validation, and unbound historical evidence.

## Fallback Expiry / Refactor Decision

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Provider-worker review handoff | Parent stage treats missing top-level telemetry as the only review authority even when the worker recorded explicit clean nested governed evidence. | `remove fallback` | CO-560 | Successful `issue_review_handoff` with clean nested implementation-gate evidence and absent parent telemetry. | Existing pre-CO-560 behavior | 2026-05-19 | This issue | Parent resolves review verdict from explicit governed evidence or records the expected missing path. | Focused CO-515-shaped regression plus negative no-clean-evidence regression. |
| Review fail-closed contract | Missing or unknown review evidence blocks handoff. | `justify retaining fallback` | Owning surface: provider-worker review contract. | No explicit clean governed review evidence exists. | Existing review-verdict contract | 2026-05-19 | Non-expiring supported safety contract. | Replace only with an equivalent stricter governed review contract. | Negative regressions for absent evidence and unknown verdict. |

Durable retention evidence:
- Contract name: provider-worker fail-closed governed review evidence.
- Owning surface: `commandRunner.ts` review contract handling and provider-worker proof/status projection.
- Steady-state proof: missing or unknown review evidence remains a blocker; clean handoff needs explicit clean governed evidence.
- Tests/docs: CO-560 focused positive and negative regressions plus this packet.
- Non-expiring rationale: this is the safety contract that prevents unreviewed handoffs, not temporary compatibility debt.

Large-refactor check: a broad review-wrapper rewrite is not required. CO-560 needs the parent stage to consume explicit governed evidence already produced by child/implementation-gate review, while CO-478 and CO-506 remain the parser/legacy-bounded-success owners.

## Architecture & Data
- Expected source seams:
  - `orchestrator/src/cli/services/commandRunner.ts` for provider-worker review evidence resolution and exact missing path diagnostics.
  - `orchestrator/src/cli/control/providerLinearWorkerTruth.ts` and control-host status/projection helpers if historical handoff runs are rehydrated as active WIP.
  - `orchestrator/tests/ProviderLinearWorkerRunner.test.ts` and provider control-host freshness/status fixtures for focused regressions.
- No schema migration should be required. Any new evidence field or reason should be additive and machine-readable.

## Validation Plan
- Focused tests:
  - CO-515 positive: successful proof, `issue_review_handoff`, live `In Review`, clean nested implementation-gate evidence, absent parent `review/telemetry.json`.
  - negative: missing all explicit clean evidence remains unknown/fail-closed.
  - negative: empty findings without explicit clean verdict remains unknown.
  - negative: invalid contract validation or `contract_overall_verdict=findings|blocked|unknown` blocks handoff.
  - status: contained handoff fixture reports zero active/retrying issues.
- Required provider-worker gates: delegation guard, spec guard dry-run, build, lint, test, docs:check, docs:freshness, repo:stewardship, diff-budget, standalone review, elegance pass, PR ready-review before handoff.

## Open Questions
- None blocking.

## Approvals
- Reviewer: parent provider worker.
- Date: 2026-05-19.
