---
id: 20260422-linear-b2763227-e57b-497e-aae9-f40766a8a70c
title: CO stabilize cli-source-bootstrap signal-forwarding CI flake
status: in_progress
relates_to: docs/PRD-linear-b2763227-e57b-497e-aae9-f40766a8a70c.md
risk: medium
owners:
  - Codex
last_review: 2026-06-17
review_notes:
  - 2026-06-17: CO-579 pre-expiry review kept this spec active-current; no verified terminal/archive evidence was established in this stream, CO-579 is the live non-terminal docs-freshness owner, and docs/spec gates remain unchanged.
  - 2026-05-18: CO-522 active-spec audit found 2 unchecked task checklist items, so this spec remains active and was reviewed for current lifecycle ownership rather than archived. Evidence: `out/linear-b642e879-ba50-45ef-b0d9-b059afa9e932-recovery/spec-preexpiry-local-classification.json`.
---

## Canonical Reference
- Canonical TECH_SPEC: `tasks/specs/linear-b2763227-e57b-497e-aae9-f40766a8a70c.md`
- PRD: `docs/PRD-linear-b2763227-e57b-497e-aae9-f40766a8a70c.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-b2763227-e57b-497e-aae9-f40766a8a70c.md`
- Task checklist: `tasks/tasks-linear-b2763227-e57b-497e-aae9-f40766a8a70c.md`

## Traceability
- Linear issue: `CO-301` / `b2763227-e57b-497e-aae9-f40766a8a70c`
- Linear URL: https://linear.app/asabeko/issue/CO-301/co-stabilize-testscli-source-bootstrapspects-signal-forwarding-ci

## Summary
- Objective: stabilize the bootstrap signal-forwarding regression so it remains high-signal under CI timing.
- Scope:
  - characterize the current `child-signal.txt` `ENOENT` failure mode
  - harden the readiness/signal-handling contract in the focused regression surface
  - update docs/workpad/validation evidence for the narrowed cause and fix
- Constraints:
  - preserve `tests/cli-source-bootstrap.spec.ts` signal-forwarding coverage
  - keep unrelated lane, rerun-policy, and provider-runtime work out of scope
  - touch `bin/codex-orchestrator.js` only if the evidence shows a real implementation gap after the fixture contract is fixed

## Issue-Shaping Contract
- User-request translation carried forward: unrelated lanes must stop failing on a CI-only `child-signal.txt` race while keeping a truthful regression for bootstrap signal forwarding.
- Protected terms / exact artifact and surface names:
  - `tests/cli-source-bootstrap.spec.ts`
  - `child-signal.txt`
  - `bin/codex-orchestrator.js`
  - `forwards termination signals to the re-execed source entrypoint`
  - `Core Lane`
- Nearby wrong interpretations to reject:
  - widen the polling window again and call it done
  - remove the test or convert it into generic smoke coverage
  - broaden into provider-worker validation or control-host cleanup work
- Explicit non-goals carried forward:
  - rerun-policy changes
  - unrelated bootstrap or provider orchestration refactors
  - test-suite-wide flake infrastructure

## Parity / Alignment Matrix
- Required for parity/alignment lanes; otherwise state `Not applicable`.
- Current truth:
  - the fixture source body in `tests/cli-source-bootstrap.spec.ts` prints `ready\n` before calling `process.once('SIGTERM', ...)`
  - the test then waits for `ready\n` and immediately sends `SIGTERM`
  - `waitForFileContents(...)` can only mask filesystem timing; it cannot recover when the handler never runs
  - `bin/codex-orchestrator.js` currently forwards signals and re-emits only after child close
- Reference truth:
  - readiness should imply the source entrypoint can already observe and handle the forwarded signal
  - the regression should distinguish fixture timing bugs from real bootstrap regressions
- Target truth / intended delta:
  - the fixture arms `SIGTERM` handling before it reports readiness
  - focused validation proves the stabilized contract
  - bootstrap implementation remains unchanged unless targeted evidence disproves the fixture-race hypothesis
- Explicitly out-of-scope differences:
  - any change that only suppresses CI noise without narrowing the real race
  - broad cross-suite retry infrastructure

## Readiness Gate
- Not done if:
  - `child-signal.txt` can still be absent because the test signals the fixture before the handler exists
  - the final regression can no longer catch a real signal-forwarding break
  - docs/workpad evidence does not explain why the prior CO-134 polling hardening was insufficient
- Pre-implementation issue-quality review evidence:
  - local source inspection shows the current fixture advertises readiness before installing the `SIGTERM` handler
  - `git show 7644a02d4 -- tests/cli-source-bootstrap.spec.ts` shows the original coverage shipped with that ordering
  - `git show b93aca299 -- tests/cli-source-bootstrap.spec.ts` shows `CO-134` replaced one-shot `readFile(...)` with `waitForFileContents(...)`, but left the readiness ordering unchanged
- Safeguard ownership split:
  - the active same-issue child lane owns `tests/cli-source-bootstrap.spec.ts` while it is in flight
  - parent ownership stays on docs and bootstrap-side analysis unless child findings require parent-side implementation follow-up

## Technical Requirements
- Functional requirements:
  - capture deterministic enough evidence for why `child-signal.txt` can be missing today
  - harden the readiness contract so the fixture cannot report `ready` before it can handle `SIGTERM`
  - keep the assertion that proves the re-execed source entrypoint received and handled the forwarded signal
  - record any remaining platform-specific timing assumptions if they remain after the fix
- Non-functional requirements (performance, reliability, security):
  - keep test runtime bounded and CI-friendly
  - avoid introducing brittle sleeps or hidden retries that only mask the root cause
  - preserve current bootstrap signal semantics unless evidence requires code changes
- Interfaces / contracts:
  - `runBootstrapInteractive(...)` remains the harness surface for interactive signal tests
  - `ready\n` becomes a truthful contract that the child signal handler is armed
  - `child-signal.txt` remains the explicit artifact proving the handler ran

## Architecture & Data
- Architecture / design adjustments:
  - default path: fix the test fixture ordering in `tests/cli-source-bootstrap.spec.ts` so `process.once('SIGTERM', ...)` is registered before `ready\n` is emitted
  - if targeted evidence still shows a real bootstrap race after that change, inspect and patch `bin/codex-orchestrator.js` separately with the same bounded contract
  - keep the existing `waitForFileContents(...)` helper only as a filesystem-settlement aid, not as the primary race fix
- Data model changes / migrations:
  - none
- External dependencies / integrations:
  - Vitest
  - Node child-process signal behavior
  - temp-file writes under the fake package root

## Validation Plan
- Tests / checks:
  - same-issue child lane characterization and, if possible, a test-only patch with targeted validation
  - parent rerun of focused `tests/cli-source-bootstrap.spec.ts` validation after integrating the chosen fix
  - repo validation required by the final touched-file set
- Rollout verification:
  - repeated focused runs stop producing `child-signal.txt` `ENOENT`
  - the regression still fails if signal forwarding is broken intentionally in a local experiment or code inspection remains sufficient to show that it would
- Monitoring / alerts:
  - preserve workpad and task evidence that the root cause was readiness-ordering, not only slow filesystem visibility

## Open Questions
- Resolved 2026-04-22: no fresh evidence requires `bin/codex-orchestrator.js` changes after the readiness-order fix. Twenty focused reruns of the signal-forwarding regression plus the full `tests/cli-source-bootstrap.spec.ts` file passed with the fix isolated to the test fixture.

## Approvals
- Reviewer: Pre-implementation review captured in this packet. The earlier docs-review child stream remains valid historical evidence, and the rebased current-main branch now passes `docs:check`, `docs:freshness`, full repo validation, and manifest-backed standalone review without any remaining external baseline blocker for this packet.
- Date: 2026-04-22
