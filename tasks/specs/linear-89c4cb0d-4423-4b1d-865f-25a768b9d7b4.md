---
id: 20260422-linear-89c4cb0d-4423-4b1d-865f-25a768b9d7b4
title: CO-274 provider-worker stdin bootstrap failure classification
status: in_progress
relates_to: docs/PRD-linear-89c4cb0d-4423-4b1d-865f-25a768b9d7b4.md
risk: high
owners:
  - Codex
last_review: 2026-05-18
review_notes:
  - 2026-05-18: CO-522 active-spec audit found 1 unchecked task checklist item, so this spec remains active and was reviewed for current lifecycle ownership rather than archived. Evidence: `out/linear-b642e879-ba50-45ef-b0d9-b059afa9e932-recovery/spec-preexpiry-local-classification.json`.
---

## Added by Bootstrap (refresh as needed)
- TECH_SPEC mirror: `docs/TECH_SPEC-linear-89c4cb0d-4423-4b1d-865f-25a768b9d7b4.md`
- PRD: `docs/PRD-linear-89c4cb0d-4423-4b1d-865f-25a768b9d7b4.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-89c4cb0d-4423-4b1d-865f-25a768b9d7b4.md`
- Task checklist: `tasks/tasks-linear-89c4cb0d-4423-4b1d-865f-25a768b9d7b4.md`
- `.agent` mirror: `.agent/task/linear-89c4cb0d-4423-4b1d-865f-25a768b9d7b4.md`

## Summary
- Objective: investigate and classify the bootstrap failure where `control-host` launched `provider-linear-worker` runs exit after `stderr | Reading additional input from stdin...` before meaningful issue execution.
- Scope:
  - docs-first packet and registry/checklist mirrors for CO-274
  - parent-owned classification, proof, manifest/read-model, and retry/resumable queue behavior work
- Constraints:
  - preserve source anchor `ctx:sha256:395893ee7f0985529df981453f3c5cc80e32afb59fe1ed6d49d83252a23013ac#chunk:c000001`
  - keep `CO-224` and `CO-225` boundaries explicit
  - avoid broad provider admission, runtime-mode, or retry-queue redesign

## Issue-Shaping Contract
- User-request translation carried forward: multiple provider-worker runs launched by `control-host` fail before actual issue work with `stderr | Reading additional input from stdin...`; CO needs bootstrap-specific diagnosis and deterministic queue behavior, not only a generic `provider_runtime` bucket.
- Protected terms / exact artifact and surface names:
  - `provider-linear-worker`
  - `control-host`
  - `stderr | Reading additional input from stdin...`
  - `provider_runtime`
  - `provider-linear-worker-proof.json`
  - `manifest.json`
  - `linear_audit.attempted_count`
  - retry/resumable queue behavior
  - `CO-224`
  - `CO-225`
- Nearby wrong interpretations to reject:
  - this is the same seam as `CO-224` child-lane appserver stalling
  - this is the same seam as `CO-225` false guardrail-summary truth
  - this is an issue-specific content fix for CO-271 or CO-272
  - this can be fixed by hiding stderr, dropping audit counts, or disabling retries broadly
- Explicit non-goals carried forward:
  - no CO-224 child-lane artifact stall work
  - no CO-225 guardrail-summary work
  - no CO-271 or CO-272 issue-specific content work
  - no broad provider admission, runtime-mode, or retry-queue redesign

## Parity / Alignment Matrix

| Current truth | Reference truth | Target truth / intended delta | Explicitly out-of-scope differences |
| --- | --- | --- | --- |
| Parent issue state identifies a provider-worker bootstrap/stdin failure but prior surfaced classification is generic `provider_runtime`. | Provider-worker proof and manifests are authoritative failure evidence. | Classify the stdin-read exit separately while preserving existing proof/manifest evidence. | Do not rewrite old artifacts or collapse adjacent issues. |
| `linear_audit.attempted_count` can remain zero. | A zero attempt count means no meaningful Linear issue mutation began. | Preserve zero-attempt truth in summaries and validation. | Do not infer issue progress from pre-work exits. |
| retry/resumable queue behavior must remain deterministic. | Queue state should reflect ownership and progress evidence. | Keep resumability truthful for bootstrap failures and avoid false issue-progress signals. | Do not redesign queue ownership. |
| `CO-224` and `CO-225` are adjacent but separate. | Each issue owns a different failure boundary. | CO-274 coverage must distinguish both issue boundaries. | Do not fold CO-274 into either adjacent lane. |

## Readiness Gate
- Not done if:
  - `control-host` launched `provider-linear-worker` runs can still exit before meaningful work with only generic `provider_runtime` classification when the signal is `Reading additional input from stdin...`
  - `provider-linear-worker-proof.json` or `manifest.json` lose exact stderr or `linear_audit.attempted_count`
  - retry/resumable queue behavior implies issue-specific progress when work never began
  - the issue is merged into `CO-224`, `CO-225`, `CO-271`, or `CO-272` instead of preserving its own bootstrap boundary
- Pre-implementation issue-quality review evidence:
  - 2026-04-22: the issue body, protected terms, non-goals, Not Done If, and acceptance criteria make CO-274 a provider-worker bootstrap classification and retry/resumable truth lane, not a child-lane appserver stall, guardrail-summary cleanup, or issue-specific content fix.
  - Source payload was visible in this Rework workspace at `../../.runs/linear-89c4cb0d-4423-4b1d-865f-25a768b9d7b4/cli/2026-04-21T14-09-01-063Z-719778ff/memory/source-0/source.txt`; the exact source anchor is recorded.
- Safeguard ownership split:
  - parent lane owns implementation, tests, validation, Linear/workpad reconciliation, PR, and merge
  - no child lane is active in this reset turn because scoped ownership is not safely separable before source discovery

## Technical Requirements
- Functional requirements:
  1. Classify the seam separately from generic `provider_runtime`, with operator guidance for provider-worker bootstrap/stdin handoff.
  2. Preserve `provider-linear-worker-proof.json` evidence, including exact signal, owner status, and `linear_audit.attempted_count`.
  3. Preserve `manifest.json` terminal failure evidence.
  4. Preserve retry/resumable queue behavior while avoiding false progress signals for zero-attempt failures.
  5. Keep CO-274 distinct from `CO-224` and `CO-225`.
  6. Add focused regressions or deterministic fixtures distinguishing this seam from adjacent issues.
- Non-functional requirements:
  - minimal provider-worker/control-host surface change
  - no broad runtime-mode redesign
  - no diagnostic muting
  - machine-checkable proof and manifest evidence
- Interfaces / contracts:
  - `provider-linear-worker-proof.json`
  - `manifest.json`
  - provider diagnostic category/subtype fields
  - control-host read models
  - retry/resumable queue behavior

## Architecture & Data
- Architecture / design adjustments:
  - add a narrow diagnostic subtype or category for the stdin/bootstrap failure
  - route that classification through proof, manifest/read-model, and operator summary surfaces
  - preserve zero-attempt audit truth in queue/resume handling
- Data model changes / migrations:
  - no migrations required
  - any new diagnostic value must be backwards-compatible for proof readers
- External dependencies / integrations:
  - Linear state mutation remains parent-owned through the existing helper

## Validation Plan
- Tests / checks:
  - protected-term grep over packet and mirrors
  - focused provider-worker diagnostic regression for `stderr | Reading additional input from stdin...`
  - proof/manifest assertion preserving `linear_audit.attempted_count: 0`
  - retry/resumable queue behavior regression
  - regression/fixture distinguishing this seam from `CO-224` and `CO-225`
- Rollout verification:
  - record manifest-backed or fixture-backed proof that bootstrap/stdin failures surface a specific classification and keep exact stderr/audit evidence
- Monitoring / alerts:
  - operator surfaces should show bootstrap-specific guidance instead of only generic `provider_runtime`

## Open Questions
- Resolved: use a new `provider_stdin_bootstrap` diagnostic category instead of a `provider_runtime` subtype so operators can separate stdin/bootstrap handoff failures from generic runtime failures.
- Resolved remediation text: "Codex exited during stdin bootstrap before issue execution; inspect the control-host to provider-linear-worker Codex exec stdin/prompt handoff, not issue-specific content."

## Approvals
- Reviewer: pending standalone review
- Date: 2026-04-22
