---
id: 20260411-linear-62898ffa-fbb2-4713-8113-6e05dbabb777
title: CO: Reinvestigate current-main npm run test failure in ProviderIssueHandoff snapshot-only Todo retry path
status: done
relates_to: docs/PRD-linear-62898ffa-fbb2-4713-8113-6e05dbabb777.md
related_prd: docs/PRD-linear-62898ffa-fbb2-4713-8113-6e05dbabb777.md
related_action_plan: docs/ACTION_PLAN-linear-62898ffa-fbb2-4713-8113-6e05dbabb777.md
risk: medium
owners:
  - Codex
last_review: 2026-05-13
review_notes:
  - 2026-05-13: CO-523 live Linear audit verified CO-150 is Done/completed; reclassified this task spec as inactive done metadata for strict spec-guard evidence. Evidence: out/linear-8573da42-d9f9-44ce-a24e-224984539044/manual/20260512T1850Z-baseline/live-linear-states.json.
---

## Added by Bootstrap (refresh as needed)

## Summary
- Objective: reproduce and fix or truthfully re-contract the current-main `ProviderIssueHandoff` snapshot-only Todo retry failure that blocks `npm run test`.
- Scope:
  - docs-first packet and one persistent Linear workpad for `CO-150`
  - fresh focused and full-suite reproduction evidence
  - bounded inspection of snapshot-only Todo retry scheduling and persisted non-terminal blocker metadata
  - smallest code or test-contract repair needed for truthful validation
- Constraints:
  - keep `CO-128` scoped to `frontend-test` non-repro evidence
  - do not waive or hide repo-wide `npm run test`
  - do not widen into unrelated provider cleanup
- Current outcome:
  - the reported snapshot-only Todo failure is not reproducible after rebasing onto current `origin/main` `6d7ab74f8`
  - focused snapshot-only Todo tests, full `ProviderIssueHandoff.test.ts`, and full `npm run test` all pass for that surface
  - this lane records no production retry implementation change; after later PR Core Lane failures in nearby queued-retry overlap tests, the bounded code delta stabilizes the test harness to advance Vitest's fake timer clock synchronously instead of manually invoking captured timeout callbacks

## Issue-Shaping Contract
- User-request translation carried forward:
  - this lane owns the current-main `ProviderIssueHandoff` timeout-count failure, not the earlier `CO-128` `frontend-test` hang
- Protected terms / exact artifact and surface names:
  - `npm run test`
  - `orchestrator/tests/ProviderIssueHandoff.test.ts`
  - issue-reported `continues snapshot-only Todo retries when persisted blocker metadata is still non-terminal`
  - current source anchor `releases snapshot-only Todo retries when persisted blocker metadata is still non-terminal`
  - issue-reported `line-5975 timeout-count assertion mismatch`
  - `scheduled timeout count`
- Nearby wrong interpretations to reject:
  - the blocker belongs to `CO-128`
  - a narrow targeted pass is enough while repo-wide `npm run test` still fails
  - timeout inflation is a fix
  - generic provider cleanup can replace exact reproduction
- Explicit non-goals carried forward:
  - frontend-test bootstrap recovery
  - broad timeout-suite tuning
  - unrelated provider status/dashboard work

## Parity / Alignment Matrix
- Current truth:
  - current workspace branch is rebased onto `origin/main` `6d7ab74f8`
  - issue evidence reports `ProviderIssueHandoff.test.ts` scheduling two timeouts after refresh where the test expected one
  - fresh source inspection shows the issue line/test anchor has drifted; the current non-terminal snapshot-only Todo case is around `ProviderIssueHandoff.test.ts:6686`
  - fresh validation did not reproduce the timeout-count failure in focused, file-level, or repo-wide test paths
  - later PR Core Lane runs failed in `waits for the active refresh lifecycle lock before dispatching queued retry timers` and `rebuilds queued retry ownership on rehydrate and dispatches exactly once while a refresh overlaps the due-boundary launch`, which are nearby fake-timer test-harness seams rather than the issue-reported snapshot-only Todo release contract
  - PR #437 is attached to `CO-150`, and the single persistent workpad is maintained at comment `cdc4108c-e046-413c-815d-782c6afc52c2`
- Reference truth:
  - snapshot-only Todo retry behavior should release issues with non-terminal blocker metadata, continue retry progression only when blocker metadata is terminal, and avoid duplicating active timers across refresh
  - the test assertion should encode the real retry contract and remain deterministic under Vitest fake timers
  - `npm run test` should be a truthful gate for current main
- Target truth / intended delta:
  - reproduction and source inspection explain whether the extra timeout is current behavior
  - production retry implementation is updated only when a live owning retry seam is implicated; test-harness dispatch is stabilized when fake-timer scheduling itself is the source of CI nondeterminism
  - focused and full validation pass or a dedicated repo-owned contract records any remaining blocker
- Explicitly out-of-scope differences:
  - `CO-128` frontend-test behavior
  - old broad timeout or teardown incidents without fresh matching evidence
  - non-provider issue handoff refactors

## Readiness Gate
- Not done if:
  - the exact case still fails with timeout-count mismatch
  - the lane lacks fresh reproduction logs
  - the only outcome is a note in another issue
  - `CO-128` absorbs the blocker
- Pre-implementation issue-quality review evidence:
  - reviewed `CO-150` against the current workspace and Linear context before implementation. The issue carries exact failing surfaces, a clear non-goal boundary against `CO-128`, and sufficient acceptance criteria to proceed after docs-review.
- Safeguard ownership split:
  - parent lane owns docs packet, reproduction, implementation, validation, workpad, and handoff
  - docs-review child stream provides audited pre-implementation packet review

## Technical Requirements
- Functional requirements:
  - capture the current failing assertion context before code edits
  - release snapshot-only Todo retries when blocker metadata remains non-terminal, and continue retry progression only when blocker metadata is terminal
  - prevent duplicate active retry timers if the implementation is wrong
  - update the test contract only if the second timeout is proven to be intended behavior; for queued-retry overlap tests, drive and await the queued retry through the fake timer clock to match Vitest scheduling semantics
- Non-functional requirements (performance, reliability, security):
  - no network dependency in tests
  - deterministic fake-timer assertions
  - no hidden validation waiver
- Interfaces / contracts:
  - `orchestrator/tests/ProviderIssueHandoff.test.ts`
  - `orchestrator/src/cli/control/providerIssueHandoff.ts`
  - provider retry timer scheduling and persisted blocker metadata

## Architecture & Data
- Architecture / design adjustments:
  - inspect the existing timeout scheduling and refresh/retry release path before adding any state
  - prefer a targeted guard or cleanup for stale scheduled retry handles if duplicate scheduling is confirmed
  - prefer a narrowly documented assertion update if the product contract is already correct
- Data model changes / migrations:
  - none expected
- External dependencies / integrations:
  - Vitest fake timers
  - repo validation commands

## Validation Plan
- Tests / checks:
  - `npx vitest run --config vitest.config.core.ts orchestrator/tests/ProviderIssueHandoff.test.ts`
  - `npm run test`
  - standard validation floor before review handoff for non-trivial diffs
- Rollout verification:
  - exact failing case no longer fails without explanation
  - full suite is green or a specific repo-owned remaining blocker is documented
- Monitoring / alerts:
  - Linear workpad and local artifacts under `out/linear-62898ffa-fbb2-4713-8113-6e05dbabb777/manual/`

## Review Notes
- Docs-review first pass found P2 packet fixes; clean rerun completed at `.runs/linear-62898ffa-fbb2-4713-8113-6e05dbabb777-co-150-docs-review-r2/cli/2026-04-11T03-47-09-381Z-eee21cae/manifest.json` with `review_outcome: clean-success`.

## Implementation Notes
- Current source inspection: the issue-reported non-terminal `continues...` anchor is stale; current source uses `releases snapshot-only Todo retries when persisted blocker metadata is still non-terminal` around line 6686 and `continues...` is the terminal blocker case around line 6800.
- Focused and full reproduction evidence is green, so no implementation patch was made.

## Open Questions
- Resolved for current `origin/main`: no extra scheduled-timeout failure reproduced.

## Approvals
- Reviewer: docs-review clean after packet fixes
- Date: 2026-04-11
