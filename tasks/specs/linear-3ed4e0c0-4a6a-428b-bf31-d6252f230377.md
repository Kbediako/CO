---
id: linear-3ed4e0c0-4a6a-428b-bf31-d6252f230377
title: CO-443 completed intake claim live-truth recovery
status: in_progress
owner: Codex
created: 2026-04-30
last_review: 2026-05-24
review_cadence_days: 30
risk_level: high
related_prd: docs/PRD-linear-3ed4e0c0-4a6a-428b-bf31-d6252f230377.md
related_action_plan: docs/ACTION_PLAN-linear-3ed4e0c0-4a6a-428b-bf31-d6252f230377.md
related_tasks:
  - tasks/tasks-linear-3ed4e0c0-4a6a-428b-bf31-d6252f230377.md
review_notes:
  - "2026-05-24: CO-579 pre-expiry review: retained active or blocked scope; no spec contract changes required."
  - "2026-04-30: bounded same-issue child lane created a docs-only packet from parent-provided issue context; the referenced source payload path was absent in the child checkout."
---

# Technical Specification - CO-443 completed intake claim live-truth recovery

## Canonical Reference
- PRD: `docs/PRD-linear-3ed4e0c0-4a6a-428b-bf31-d6252f230377.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-3ed4e0c0-4a6a-428b-bf31-d6252f230377.md`
- Task checklist: `tasks/tasks-linear-3ed4e0c0-4a6a-428b-bf31-d6252f230377.md`
- Linear issue: `CO-443` / https://linear.app/asabeko/issue/CO-443
- Child lane manifest: `.runs/linear-3ed4e0c0-4a6a-428b-bf31-d6252f230377-docs-packet/cli/2026-04-30T06-34-58-998Z-34bebedf/manifest.json`
- Source anchor: `ctx:sha256:cf913ea2dcd62e7ac2df8f89fd2725b8eb43ade503dd23e7c55ae65d81ad7cc7#chunk:c000001`

## Summary
- Objective: ensure completed provider-intake claims with `provider_issue_run_already_completed` do not suppress supported `control-host recover|relaunch|nudge` when live Linear truth is active/fresher again, including after polling has already copied the latest active timestamp into the completed claim.
- Scope:
  - stale completed-claim revalidation for explicit control-host recovery actions
  - duplicate-run preservation for unchanged or older completed truth
  - focused parent-owned regressions around recover/relaunch/nudge behavior
- Constraints:
  - keep CO-393 command plumbing and direct-start guard intact
  - do not change CO-330 stale-owner recovery, CO-404 timeout acknowledgement, CO-406 no-run capacity, or CO-392 released-pending-reopen semantics
  - do not rely on manual provider-intake-state edits

## Issue-Shaping Contract
- User-request translation carried forward: completed provider-intake claims with `provider_issue_run_already_completed` must not suppress supported `control-host recover|relaunch|nudge` once live Linear truth is active/fresher again.
- Protected terms / exact artifact and surface names:
  - `completed provider-intake claims`
  - `provider_issue_run_already_completed`
  - `control-host recover|relaunch|nudge`
  - `live Linear truth`
  - `active/fresher again`
  - `provider-intake-state.json`
  - `provider-linear-worker`
  - `CO-330 stale-owner recovery`
  - `CO-393 command plumbing`
  - `CO-404 timeout acknowledgement`
  - `CO-406 no-run capacity`
  - `CO-392 released-pending-reopen`
- Nearby wrong interpretations to reject:
  - stale completed intake state can permanently override newer active Linear truth
  - fixing this by deleting or manually editing provider-intake rows
  - adding direct `provider-linear-worker` starts
  - changing control-host command plumbing from CO-393
  - absorbing adjacent CO-330, CO-404, CO-406, or CO-392 behaviors
- Explicit non-goals carried forward:
  - no lifecycle, workpad, Linear, GitHub, PR, or source edits from this child lane
  - no provider workflow redesign or global reconcile loop
  - no docs-freshness, review-wrapper, timeout, capacity, or released-state work

## Parity / Alignment Matrix

| Surface | Current truth | Reference truth | Target truth | Explicitly out-of-scope differences |
| --- | --- | --- | --- | --- |
| Completed intake cache | Completed rows can carry `provider_issue_run_already_completed`. | Completed rows prevent duplicate work for unchanged/equal non-recovery truth. | Completed rows are revalidated before suppressing recover/relaunch/nudge when live truth is active/fresher again. | Removing completed-row auditability. |
| Live Linear authority | Live issue state can become active/fresher after a completed provider run, and polling can copy that timestamp into the completed claim before the operator retries. | Active/fresher tracker truth must be able to restart supported provider work through explicit recovery. | Active/fresher Linear truth supersedes stale completed suppression for supported control-host actions. | CO-330 stale owner recovery or a broad polling loop. |
| Control-host action authority | CO-393 established supported root control-host recover/relaunch/nudge. | Recovery starts must flow through root control-host provenance and launch-token guardrails. | CO-443 uses those supported actions without changing command plumbing or allowing direct starts. | Direct `provider-linear-worker` starts or new command surfaces. |
| Adjacent issue boundaries | CO-404, CO-406, and CO-392 own other provider-intake residue families. | Each issue lane owns its exact failure class. | CO-443 only handles completed-run suppression with fresher active truth. | Timeout acknowledgement, no-run capacity, released-pending-reopen, or stale-owner recovery. |

## Readiness Gate
- Not done if:
  - `control-host recover|relaunch|nudge` remains suppressed solely by `provider_issue_run_already_completed` after live Linear truth is active/fresher
  - unchanged/equal live truth causes duplicate reruns instead of a completed no-op
  - the fix requires manual `provider-intake-state.json` edits or direct provider starts
  - source changes widen into CO-330, CO-393, CO-404, CO-406, or CO-392
  - focused tests fail to cover both fresher-active recovery and unchanged completed suppression
- Pre-implementation issue-quality review evidence:
  - 2026-04-30: the issue is narrower than CO-393 command plumbing and broader than a display-only status fix; it specifically targets stale completed-claim authority when current Linear truth is fresher and active.
  - 2026-04-30: the micro-task path is not appropriate because correctness depends on protected terms, exact adjacent non-goals, stale/cached provider-intake behavior, and duplicate-launch safety.
- Safeguard ownership split:
  - child lane owns only PRD, canonical TECH_SPEC, ACTION_PLAN, task checklist, and `tasks/index.json`
  - parent lane owns live Linear reconciliation, source inspection, implementation, focused tests, full validation, workpad, PR lifecycle, review, and merge

## Technical Requirements
- Functional requirements:
  1. Revalidate `provider_issue_run_already_completed` claims against live Linear issue state before suppressing explicit `control-host recover|relaunch|nudge`.
  2. Treat live Linear truth as recovery-eligible when it is active and fresher than the latest completed provider-worker run truth, even when healthy poll reconciliation has already copied that fresh timestamp into the completed duplicate claim.
  3. Preserve duplicate-run prevention for unchanged, equal, older, unknown, or otherwise non-active live truth.
  4. Preserve completed provider-intake rows as audit evidence unless the parent implementation deliberately supersedes them through normal provider-intake persistence.
  5. Reuse supported root control-host recovery actions and their provenance instead of adding a direct worker start path.
  6. Keep adjacent issue behavior unchanged for CO-330, CO-393, CO-404, CO-406, and CO-392.
- Non-functional requirements:
  - fail closed for ambiguous freshness comparisons
  - keep the implementation narrowly scoped and reviewable
  - preserve provider-worker duplicate launch safety
  - maintain truthful operator-facing reasons for no-op versus supported action
- Interfaces / contracts:
  - `provider-intake-state.json` remains backward compatible
  - `provider_issue_run_already_completed` remains an audit and duplicate-safety reason for unchanged truth
  - `control-host recover|relaunch|nudge` remains the supported operator action path
  - live Linear state and update time remain the authority for active/fresher truth

## Fallback Expiry / Refactor Decision
- Applies to fallback, compatibility, legacy, stale, cached, break-glass, or minor-seam behavior? `Yes`.

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Completed-claim suppression | Stale `provider_issue_run_already_completed` blocks supported control-host actions for active/fresher truth | `remove fallback` | CO-443 parent implementation lane | Recover/relaunch/nudge sees a completed intake claim while live Linear truth is active/fresher again. | observed 2026-04-30 | N/A after removal | N/A after removal | Suppression is gated by live truth activity/freshness and explicit recovery intent. | Focused regression proves supported action is not suppressed for active/fresher truth. |
| Completed-run audit and duplicate safety | Completed provider-intake row remains available for unchanged/equal truth | `justify retaining fallback` | CO-443 parent implementation lane | Live Linear truth is unchanged, equal, older, inactive, or otherwise not recovery-eligible. | existing provider-intake duplicate-safety contract | 2026-04-30 | Non-expiring audit and duplicate-safety contract | Replace only with a stronger completed-run freshness model. | Regression proves unchanged/equal truth still no-ops and does not duplicate runs. |
| Root control-host launch authority | Direct provider-worker starts stay blocked unless CO-393 provenance allows them | `justify retaining fallback` | CO-393 / CO-443 parent implementation lane | Operator recovery action targets a specific Linear issue. | CO-393 | 2026-04-30 | Non-expiring launch authority contract | Replace only with equivalent or stronger launch authority. | Existing/focused guard coverage keeps direct-start protection intact. |

- For `justify retaining fallback`, contract names:
  - completed provider-intake audit and duplicate-safety contract
  - root control-host launch authority contract
- Large-refactor check: another minor seam is acceptable because CO-443 is an exact freshness revalidation boundary. Escalate only if parent source inspection shows completed-claim suppression, live issue revalidation, and control-host action dispatch are split across incompatible authority paths that cannot be fixed with a small shared predicate or helper.

## Architecture & Data
- Architecture / design adjustments:
  - Parent should locate the completed-claim no-op decision and insert live-truth freshness revalidation before suppressing supported recovery actions.
  - Prefer a single helper or predicate for "completed claim still authoritative" rather than repeated timestamp checks.
  - Preserve current control-host action plumbing and launch provenance.
- Data model changes / migrations:
  - No migration expected.
  - Additive reason metadata is allowed if needed to explain stale completed claim supersession, but the issue does not require a schema rewrite.
- External dependencies / integrations:
  - Linear issue state and update timestamp
  - root control-host provider-worker recovery path
  - retained provider-intake state

## Validation Plan
- Child-lane checks:
  - JSON parse for `tasks/index.json`
  - scoped diff whitespace check over declared packet files and `tasks/index.json`
  - scoped `git status --short` review to confirm no edits outside declared file scope
- Parent-owned checks:
  - docs-review or equivalent packet review before implementation
  - focused regression for completed claim plus active/fresher Linear truth under `control-host recover`
  - focused regression for completed claim plus active/fresher Linear truth under `control-host relaunch`
  - focused regression for completed claim plus active/fresher Linear truth under `control-host nudge`
  - focused regression proving unchanged/equal completed truth still no-ops and does not duplicate runs
  - focused guard coverage proving direct `provider-linear-worker` starts remain blocked
  - normal provider-worker validation floor before review handoff
- Rollout verification:
  - parent workpad records live issue-context reconciliation, focused tests, validation, review telemetry, PR attachment, ready-review drain, and final Linear state
- Monitoring / alerts:
  - no new monitor required; existing control-host and provider-intake diagnostics should expose the resulting no-op or supported-action decision.

## Open Questions
- Parent source inspection should confirm which timestamp field on completed intake claims is authoritative for comparison with live Linear `updated_at`.
- Parent source inspection should confirm whether recover, relaunch, and nudge share the same completed-claim suppression path or need one shared helper to avoid drift.

## Approvals
- Reviewer: bounded same-issue child lane.
- Date: 2026-04-30.
