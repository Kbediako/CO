---
id: 20260417-linear-5b31889f-233c-4f23-9750-5ff21d737661
title: CO child-lane decisions repair stale pending ledger after post-startup launch-shell miss
status: done
relates_to: docs/PRD-linear-5b31889f-233c-4f23-9750-5ff21d737661.md
risk: high
owners:
  - Codex
last_review: 2026-05-18
review_notes:
  - 2026-05-18: CO-554 live issue-context verified CO-218 is Done/completed with PR #514 attached; reclassified inactive done so strict spec-guard no longer treats the completed packet as active stale work.
---

## Canonical Reference
- Canonical TECH_SPEC: `tasks/specs/linear-5b31889f-233c-4f23-9750-5ff21d737661.md`
- TECH_SPEC mirror: `docs/TECH_SPEC-linear-5b31889f-233c-4f23-9750-5ff21d737661.md`
- PRD: `docs/PRD-linear-5b31889f-233c-4f23-9750-5ff21d737661.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-5b31889f-233c-4f23-9750-5ff21d737661.md`
- Task checklist: `tasks/tasks-linear-5b31889f-233c-4f23-9750-5ff21d737661.md`

## Traceability
- Linear issue: `CO-218` / `5b31889f-233c-4f23-9750-5ff21d737661`
- Linear URL: https://linear.app/asabeko/issue/CO-218/co-child-lane-decisions-repair-stale-pending-ledger-after-post-startup
- Source anchor: `ctx:sha256:641135f0be6a5fc40e6c401ecc1a006e14d73a1768b2d6d602c3b75807302d30#chunk:c000001`
- Shared source-0 metadata anchor: `ctx:sha256:641135f0be6a5fc40e6c401ecc1a006e14d73a1768b2d6d602c3b75807302d30#chunk:c000001`
- Shared source-0 note: `../../.runs/linear-5b31889f-233c-4f23-9750-5ff21d737661/cli/2026-04-17T09-34-12-702Z-29d8fef8/memory/source-0/source.txt` carries run metadata and prompt-pack provenance only; the current issue body came from the user prompt and the packaged `linear issue-context` helper.
- Provider-worker origin manifest: `../../.runs/linear-5b31889f-233c-4f23-9750-5ff21d737661/cli/2026-04-17T09-34-12-702Z-29d8fef8/manifest.json`
- Live issue-body source: `"/opt/homebrew/Cellar/node/25.2.1/bin/node" "/Users/kbediako/Code/CO/bin/codex-orchestrator.js" linear issue-context --issue-id 5b31889f-233c-4f23-9750-5ff21d737661 --format json`
- Failed docs child-lane attempts:
  - `.runs/linear-5b31889f-233c-4f23-9750-5ff21d737661-docs-packet/cli/2026-04-17T09-40-30-654Z-0dfcba23/manifest.json`
  - `.runs/linear-5b31889f-233c-4f23-9750-5ff21d737661-docs-packet/cli/2026-04-17T09-42-56-585Z-65ed2abd/manifest.json`

## Summary
- Objective: repair stale `launching-*` pending ledger rows in parent-owned child-lane decision flows so `accept` / `reject` / `invalidate` can act on a repaired real child-lane record when stronger same-parent, same-issue proof/manifest evidence already exists.
- Scope:
  - docs-first packet and registry/checklist mirrors
  - narrow reconciliation in `providerLinearChildLaneShell`
  - preservation of proof lineage, artifact-root anchoring, and fail-closed behavior
  - truthful non-acceptance for zero-byte or missing-patch child lanes
  - focused regression coverage in `ProviderLinearChildLaneShell.test.ts`
- Constraints:
  - do not change `CO-210` STATUS/progress hydration semantics
  - do not weaken stale Linear `updated_at` invalidation
  - do not auto-accept child lanes or redesign launch admission/scheduler policy
  - do not delete or compact historical child-lane ledger or proof records

## Issue-Shaping Contract
- User-request translation carried forward: the current problem is not STATUS truth. STATUS/proof projection can already show the real child lane, but the parent decision ledger can still be stuck on a stale `launching-*` reservation. This lane must make the parent decision path repairable without weakening lineage or artifact safety.
- Protected terms / exact artifact and surface names:
  - `provider-linear-worker-child-lanes.json`
  - `provider-linear-child-lane`
  - `providerLinearChildLaneShell`
  - `launching-*` reservation row
  - `accept`
  - `reject`
  - `invalidate`
  - child-lane manifest
  - child-lane proof
  - `parent_run_id`
  - `issue_id`
  - `issue_identifier`
  - `pipeline_id=provider-linear-child-lane`
- Nearby wrong interpretations to reject:
  - changing `CO-210` STATUS/progress hydration semantics
  - weakening stale Linear `updated_at` invalidation
  - auto-accepting child lanes without parent-owned decision flow
  - broad child-lane scheduler redesign
  - deleting historical child-lane ledger or proof records
  - masking placeholder rows in STATUS without repairing the decision ledger
- Explicit non-goals carried forward:
  - no `CO-210` current-progress hydration change
  - no stale `updated_at` invalidation change
  - no scheduler/admission redesign
  - no authority change for child-lane decisions
  - no historical record deletion

## Parity / Alignment Matrix
- Current truth:
  - STATUS/proof hydration can surface a real child-lane manifest and proof even when the parent ledger still contains a stale `launching-*` reservation row
  - `findLatestPendingChildLane(...)` selects the latest pending ledger row for the stream
  - `resolveChildLaneDecisionBlockedOutcome(...)` blocks `accept` while the targeted row is still `status === "launching"`
  - `resolveAcceptedChildLaneProofViolation(...)` already requires matching `task_id`, `run_id`, `parent_run_id`, `stream`, `issue_id`, `issue_identifier`, and parent-anchored patch artifact path once a real row is targeted
  - zero-byte child lanes are already forced non-accepted when the completed record is written correctly
- Reference truth:
  - `CO-210` current-progress hydration remains authoritative for STATUS/proof projection
  - child-lane decisions remain parent-owned and fail closed on stale snapshots or invalid proof
  - patch acceptance remains anchored to the expected workspace-local child run directory and patch artifact path
- Target truth / intended delta:
  - parent decision flows can reconcile or upgrade a stale `launching-*` reservation row using stronger same-parent, same-issue child proof/manifest evidence
  - repaired `accept` / `reject` / `invalidate` flows operate on the real child-lane record instead of failing against the placeholder
  - lineage or artifact mismatches still fail closed before ledger mutation or patch apply
  - zero-byte or missing-patch child lanes remain non-accepted and truthful after repair
  - historical ledger/proof evidence stays inspectable
- Explicitly out-of-scope differences:
  - `CO-210` STATUS/progress hydration
  - stale `updated_at` invalidation
  - child-lane admission cap policy
  - auto-accept or auto-merge behavior

## Readiness Gate
- Not done if:
  - parent decisions still fail on a stale `launching-*` placeholder even when matching same-parent, same-issue child proof/manifest evidence exists
  - the repair path can claim unrelated child runs by stream name, mtime, or artifact recency
  - the fix changes stale Linear `updated_at` invalidation, parent authority, or `CO-210` hydration semantics
  - the fix only changes STATUS display while leaving the pending ledger unusable
- Pre-implementation issue-quality review evidence:
  - 2026-04-17: parent source inspection confirmed this is a decision-ledger repair problem centered on `findLatestPendingChildLane(...)`, `resolveChildLaneDecisionBlockedOutcome(...)`, and proof/patch safety checks. The issue is not plausibly narrower than the user request because the protected terms require exact child-lane lineage and artifact surfaces while explicitly rejecting `CO-210` scope expansion. The micro-task path is ineligible.
- Safeguard ownership split:
  - parent lane owns docs packet, implementation, focused tests, review, validation, workpad, PR, and merge
  - bounded same-issue child-lane attempts are still required by the active-turn parallelization contract when credits permit
  - failed docs child-lane attempts are recorded as delegation evidence; no blanket delegation override text is used

## Technical Requirements
- Functional requirements:
  1. Keep the decision repair in `providerLinearChildLaneShell`, centered on pending-lane targeting for `accept` / `reject` / `invalidate`.
  2. Allow repair when the current pending target is a stale `launching-*` reservation and stronger same-lineage child proof/manifest evidence exists.
  3. Require same `parent_run_id`, `issue_id`, `issue_identifier`, `pipeline_id=provider-linear-child-lane`, `stream`, expected child task id, expected child run id, and expected artifact root before any repair mutates the pending ledger.
  4. Require the child manifest and patch artifact path to stay anchored under the expected workspace-local child run directory.
  5. Fail closed when proof, manifest, task id, run id, issue metadata, pipeline id, or artifact-root checks do not match.
  6. After repair, let `accept` / `reject` / `invalidate` operate on the repaired real child-lane record instead of the stale placeholder.
  7. Preserve existing acceptance gates for readable proof, patch artifact path, patch changed paths, dirty-parent conflicts, and patch apply failures.
  8. Preserve truthful non-acceptance for zero-byte or missing-patch child lanes after repair.
  9. Preserve historical ledger/proof inspectability; do not delete prior records as part of repair.
  10. Keep `CO-210` current-progress hydration unchanged.
- Non-functional requirements:
  - keep the fix narrow to the decision seam
  - keep reconciliation deterministic and machine-checkable
  - preserve fail-closed semantics on malformed inputs
  - do not add broad file-system scans or scheduler churn
- Interfaces / contracts:
  - `findLatestPendingChildLane(...)`
  - `resolveChildLaneDecisionBlockedOutcome(...)`
  - `resolveAcceptedChildLaneArtifactRoot(...)`
  - `resolveAcceptedPatchArtifactPath(...)`
  - `resolveAcceptedChildLaneProofViolation(...)`
  - `ProviderLinearChildLaneProof`
  - `ProviderLinearWorkerChildLaneRecord`

## Architecture & Data
- Architecture / design adjustments:
  - add a narrow repair helper or upgraded target-selection path before parent decision mutation, rather than modifying STATUS/progress hydration
  - permit the repair path to upgrade the stale placeholder in place when no separate real ledger row exists, as long as stronger child proof/manifest evidence proves the real lineage
  - preserve the existing proof and artifact-root validation chain after repair so acceptance safety remains unchanged
  - keep reject/invalidate on the same repaired target-selection path so they no longer fail against a stale placeholder
- Evidence sources:
  - `provider-linear-worker-child-lanes.json` pending records
  - child manifest under the expected workspace-local child run directory
  - `provider-linear-child-lane-proof.json`
  - existing proof lineage and patch-path helpers
- Data model changes / migrations:
  - no schema migration
  - no child-lane scheduler or admission metadata redesign
  - no deletion or compaction of historical records
- External dependencies / integrations:
  - same parent `provider-linear-worker` run context
  - child-lane proof emitted by `providerLinearChildLaneRunner`
  - current-progress hydration remaining untouched under `CO-210`

## Validation Plan
- Focused tests / checks:
  - add a regression where a stale `launching-*` pending row blocks decisions today but a metadata-matching child proof/manifest already exists for the same parent run and issue
  - add coverage showing repaired `accept` succeeds only when lineage and artifact-root checks pass
  - add coverage showing repaired `reject` / `invalidate` can target the repaired real record
  - add fail-closed coverage for mismatched `parent_run_id`, `issue_id`, `issue_identifier`, `pipeline_id`, task id, run id, or artifact root
  - preserve coverage showing zero-byte or missing-patch child lanes remain non-accepted
  - `linear child-stream --pipeline docs-review`
  - `node scripts/delegation-guard.mjs`
  - `node scripts/spec-guard.mjs --dry-run`
  - `npm run build`
  - `npm run lint`
  - `npm run test`
  - `npm run docs:check`
  - `npm run docs:freshness`
  - `npm run repo:stewardship`
  - `node scripts/diff-budget.mjs`
  - manifest-backed standalone review and explicit elegance review
- Rollout verification:
  - workpad records the successful same-turn child-lane retry or the blocker evidence if another retry is still needed
  - workpad and checklist record the exact repair behavior, focused test commands, review telemetry, and final PR handoff state
- Monitoring / alerts:
  - STATUS/proof truth and parent decision truth should converge without hiding mismatches
  - malformed or unrelated child-run artifacts should continue to fail closed

## Open Questions
- What is the smallest repair helper that can reuse existing proof/patch validation without duplicating acceptance logic?
- Should the repair helper synthesize a completed record entirely from proof plus manifest, or should it first search for an already-recorded real child-lane row before upgrading the placeholder?
- Does any existing STATUS hydration helper already expose the strongest safe same-lineage selection logic, or is a dedicated decision-only reconciliation helper cleaner?

## Approvals
- Reviewer: pending parent docs-review and implementation review.
- Date: 2026-04-17
- Delegation note: bounded same-issue docs child-lane attempts failed because nested Codex execution hit a usage-limit blocker before producing a patch artifact; parent authored the docs packet locally while preserving the recorded child-lane evidence.

## CO-382 Fallback Decision Table
- Large-refactor check: no new fallback mechanism; CO-554 removes stale active-spec lifecycle drift while keeping strict spec-guard authoritative.
- Minor-seam decision: bounded seam accepted because active pre-expiry specs now block through docs:freshness:maintain before hard expiry.

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Active spec lifecycle freshness | Completed or rolling active spec metadata can remain active until unrelated strict spec-guard jobs hit expiry | remove fallback | CO-554 | Apr 17 cohort reached strict spec-guard boundary after CO-543 | 2026-04-17 | 2026-05-18 | N/A after removal | Terminal specs are inactive done and active pre-expiry cohorts block through docs:freshness:maintain owner action | node scripts/spec-guard.mjs; docs-freshness-maintain regression |
