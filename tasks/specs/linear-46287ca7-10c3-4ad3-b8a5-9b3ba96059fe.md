---
id: 20260503-linear-46287ca7-10c3-4ad3-b8a5-9b3ba96059fe
title: "CO-498 repo-wide spec-guard and docs freshness baseline debt"
status: in_progress
owner: Codex
created: 2026-05-03
last_review: 2026-05-03
review_cadence_days: 30
risk_level: high
related_prd: docs/PRD-linear-46287ca7-10c3-4ad3-b8a5-9b3ba96059fe.md
related_action_plan: docs/ACTION_PLAN-linear-46287ca7-10c3-4ad3-b8a5-9b3ba96059fe.md
related_tasks:
  - tasks/tasks-linear-46287ca7-10c3-4ad3-b8a5-9b3ba96059fe.md
review_notes:
  - 2026-05-03: Bounded same-issue docs child lane created the CO-498 packet and mirrors only. Parent owns Linear, workpad, PR, registry/catalog, task-index, docs snapshot, clean-baseline repro, and final validation.
---

# TECH_SPEC - CO-498 repo-wide spec-guard and docs freshness baseline debt

## Summary
- Objective: document the CO-498 issue shape so repo-wide `spec-guard` and `docs:freshness` baseline debt can be classified without weakening validators or widening unrelated provider child-lane behavior.
- Scope: docs-first packet and task mirrors in the six declared files.
- Constraints: no Linear/GitHub/workpad/PR lifecycle calls, no source/test/script/package edits, no full repo validation suites, and no parent-owned registry/catalog/task-index updates from this child lane.

## Issue-Shaping Contract
- User-request translation carried forward: CO-498 is about preserving and classifying repo-wide `spec-guard` / `docs:freshness` baseline debt, not about changing provider child-lane behavior or relaxing gates.
- Protected terms / exact artifact and surface names:
  - `docs:freshness`
  - `spec-guard`
  - `last_review`
  - `rolling freshness cohort`
  - `CO-444`
  - `task specs`
  - clean origin/main baseline
  - `node scripts/spec-guard.mjs --dry-run`
  - `npm run docs:freshness`
  - `npm run docs:freshness:maintain -- --format json`
- Nearby wrong interpretations to reject:
  - provider child-lane behavior changes are not an acceptable repair for repo-wide baseline debt
  - validator weakening is not an acceptable repair
  - historical packet deletion is not an acceptable repair
  - blind last_review bumps are not an acceptable repair
  - active-branch failures are not local regressions until clean `origin/main` evidence says so
- Explicit non-goals carried forward:
  - no provider child-lane behavior changes
  - no weakening `spec-guard`, `docs:freshness`, or `docs:freshness:maintain`
  - no historical packet deletion
  - no blind last_review bumps
  - no parent lifecycle or registry/catalog edits from this child lane

## Parity / Alignment Matrix

| Surface | Current truth | Reference truth | Target truth | Explicitly out-of-scope differences |
| --- | --- | --- | --- | --- |
| `spec-guard` / `task specs` | Stale active specs or old `last_review` dates may exist as repo-wide baseline debt. | `spec-guard` fails closed until task specs are truly reviewed or reclassified. | Parent proves whether the same failure appears on clean `origin/main` and routes ownership accordingly. | Guard weakening, skipped checks, or blind last_review bumps. |
| `docs:freshness` | Docs freshness may fail on registry/catalog debt outside the active diff. | Freshness metadata stays current through reviewed updates, not bypasses. | Parent separates lane-owned changed paths from repo-wide docs-freshness debt. | Historical packet deletion or registry/catalog edits from this child lane. |
| `docs:freshness:maintain` / `rolling freshness cohort` | CO-444 may remain the owner or reference issue for a rolling freshness cohort. | Owner truth is machine-readable and preserved with exact fields. | Parent captures CO-444 owner evidence, including `blocking_changed_paths=[]` when present, before routing. | Re-homing ownership or flattening evidence in this child lane. |
| Clean origin/main baseline | Current checkout failures can be inherited. | Clean-baseline repro is required before blaming an active diff. | Parent runs the reduced failure cluster on clean `origin/main` when needed. | Mutating shared root or broadening implementation before proof. |

## Readiness Gate
- Not done if:
  - protected terms are missing
  - the packet suggests provider child-lane behavior changes
  - validator weakening, historical packet deletion, or blind last_review bumps are allowed
  - clean origin/main baseline proof is missing from the parent workflow
  - CO-444 `rolling freshness cohort` ownership is not preserved
- Pre-implementation issue-quality review evidence:
  - The child-lane kickoff explicitly constrained this work to docs packet creation and named the protected surfaces and non-goals.
  - The source payload is a run-source metadata object; the issue contract comes from the child-lane kickoff and is carried into this packet.
- Safeguard ownership split:
  - Child owns the six packet/mirror files only.
  - Parent owns issue state, workpad, registry/catalog/task-index registration, baseline repro, validation, PR, and handoff.

## Technical Requirements
- Functional requirements:
  1. Create the PRD, canonical spec, TECH_SPEC mirror, ACTION_PLAN, task checklist, and `.agent` mirror for CO-498.
  2. Preserve protected terms and non-goals across the packet.
  3. Define the parent-owned clean origin/main baseline proof step.
  4. Define parent-owned owner-truth capture for CO-444 `rolling freshness cohort` debt.
  5. Keep registry/catalog/task-index/docs snapshot edits outside child scope.
- Non-functional requirements:
  - keep the packet concise enough for parent import and review
  - keep all statements auditable and tied to exact command/surface names
  - keep validators strict and fail-closed
- Interfaces / contracts:
  - `node scripts/spec-guard.mjs --dry-run`
  - `npm run docs:freshness`
  - `npm run docs:freshness:maintain -- --format json`
  - clean `origin/main` reduced repro
  - parent-owned patch import and registry/catalog updates

## Fallback Expiry / Refactor Decision
- Applies to fallback, compatibility, legacy, stale, cached, break-glass, or minor-seam behavior? `Yes`, because this issue concerns stale/baseline validator debt and owner routing. The child packet does not introduce code fallback behavior.
- Required decision table:

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `spec-guard` / `docs:freshness` baseline classification | strict validators fail on repo-wide stale or owner debt that may predate the active diff | justify retaining fallback | CO-498 parent lane | validation failure appears in active lane and may reproduce on clean `origin/main` | 2026-05-03 | 2026-05-03 | Non-expiring strict validation contract | debt is reviewed, reclassified, re-homed, routed, or the active lane is blocked with evidence | parent reduced repro, clean `origin/main` proof, `docs:freshness:maintain -- --format json`, and scoped repo gates |

- Durable retention evidence: the retained behavior is strict validator fail-closed behavior, not a temporary compatibility branch. It is documented here so the parent classifies ownership instead of weakening checks.
- Large-refactor check: not applicable to the child docs packet. If the parent finds repeated owner-routing drift across validators, it should file or use a separate owner issue rather than widening this packet lane.

## Architecture & Data
- Architecture / design adjustments: none from this child lane.
- Data model changes / migrations: none from this child lane.
- External dependencies / integrations: parent-owned Linear/GitHub lifecycle only.

## Validation Plan
- Child checks:
  - trailing-whitespace scan on the six scoped files
  - protected-term scan on the six scoped files
  - changed-path scope check limited to the six declared files
- Parent checks:
  - `node scripts/spec-guard.mjs --dry-run`
  - `npm run docs:freshness`
  - `npm run docs:freshness:maintain -- --format json`
  - reduced clean `origin/main` repro for any blocker cluster
  - required docs/task registry checks after parent imports and registers the packet

## Acceptance Criteria
- [x] Six docs packet and mirror files exist for `linear-46287ca7-10c3-4ad3-b8a5-9b3ba96059fe`.
- [x] Protected terms and non-goals are visible.
- [x] Parent-owned baseline proof and owner-truth classification are specified.
- [x] Parent imported packet and completed registry/catalog/task-index/docs snapshot updates.
- [x] Parent validated and routed repo-wide baseline debt without changing provider child-lane behavior, weakening validators, deleting historical packets, or blind last_review bumps.

## Open Questions
- Parent-owned validation will identify the exact failing `task specs`, `last_review` rows, docs freshness entries, and CO-444 cohort owner fields.

## Approvals
- Reviewer: parent CO-498 lane, pending
- Date: 2026-05-03

- 2026-05-03: Parent accepted child lane `packet-docs`, registered the packet, verified all affected stale-source Linear issues are Done/completed, archived/reclassified historical metadata, and reran focused guard/freshness validation cleanly.
