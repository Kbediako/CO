---
id: 20260425-linear-8a991d5a-8f02-4e50-9216-a5718569bf4a
title: CO appserver child-lane stale invalidation must prove runner identity before candidate emission
status: in_progress
relates_to: docs/PRD-linear-8a991d5a-8f02-4e50-9216-a5718569bf4a.md
risk: high
owners:
  - Codex
last_review: 2026-05-18
review_notes:
  - 2026-05-18: CO-522 active-spec audit found 14 unchecked task checklist items, so this spec remains active and was reviewed for current lifecycle ownership rather than archived. Evidence: `out/linear-b642e879-ba50-45ef-b0d9-b059afa9e932-recovery/spec-preexpiry-local-classification.json`.
---

## Canonical Reference
- Canonical TECH_SPEC: `tasks/specs/linear-8a991d5a-8f02-4e50-9216-a5718569bf4a.md`
- TECH_SPEC mirror: `docs/TECH_SPEC-linear-8a991d5a-8f02-4e50-9216-a5718569bf4a.md`
- PRD: `docs/PRD-linear-8a991d5a-8f02-4e50-9216-a5718569bf4a.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-8a991d5a-8f02-4e50-9216-a5718569bf4a.md`
- Task checklist: `tasks/tasks-linear-8a991d5a-8f02-4e50-9216-a5718569bf4a.md`

## Traceability
- Linear issue: `CO-371` / `8a991d5a-8f02-4e50-9216-a5718569bf4a`
- Source anchor: `ctx:sha256:66a6cc3b9ceee1998def43ba374bd15a5cb4586ea54de754de30f8c48dbfe7ba#chunk:c000001`
- Docs packet child lane: `.runs/linear-8a991d5a-8f02-4e50-9216-a5718569bf4a-docs-packet/cli/2026-04-25T13-29-35-913Z-88cc9756/manifest.json`
- Shared source note: the expected shared source payload is absent in this child checkout, so the packet is anchored on the bounded lane prompt and current repo seam names only.

## Summary
- Objective: make appserver child-lane stale invalidation safe by requiring proven `providerLinearChildLaneRunner` process identity before emitting `stale_invalidation_candidate`.
- Scope:
  - docs-first packet and registry/checklist mirrors for `CO-371`
  - parent-owned implementation around appserver child-lane runner identity and stale candidate emission
  - focused parent validation only
- Constraints:
  - preserve exact protected terms
  - ambiguous or unknown process identity remains fail-closed
  - no implementation, test, Linear, workpad, PR, or merge changes from this child lane

## Issue-Shaping Contract
- User-request translation carried forward: `CO-371` is a process-identity safety lane for appserver child-lane stale invalidation. `heartbeat stale` and `no proof output` can support diagnosis, but they must not produce `stale_invalidation_candidate` unless the original runner process identity is known and the same runner is proven not alive.
- Protected terms / exact artifact and surface names:
  - `providerLinearChildLaneRunner`
  - `runner_alive`
  - `provider_linear_child_lane_runner_pid`
  - `provider_linear_child_lane_runner_started_at`
  - `runtime_mode=appserver`
  - `stale_invalidation_candidate`
  - PID reuse
  - `heartbeat stale`
  - `no proof output`
  - `orchestrator/src/cli/providerLinearChildLaneRunner.ts`
  - `orchestrator/src/cli/providerLinearChildLaneShell.ts`
  - `orchestrator/tests/ProviderLinearChildLaneRunner.test.ts`
  - `orchestrator/tests/ProviderLinearChildLaneShell.test.ts`
- Nearby wrong interpretations to reject:
  - treating `heartbeat stale` as enough evidence on its own
  - treating `no proof output` as enough evidence on its own
  - treating PID number equality as identity proof when PID reuse is possible
  - treating missing start-time evidence as dead-runner proof
  - coercing unknown `runner_alive` to `false`
  - emitting `stale_invalidation_candidate` for ambiguous or unknown process identity
- Explicit non-goals carried forward:
  - no implementation or test edits in this child lane
  - no Linear mutation or PR handling from this child lane
  - no generic process supervisor or scheduler redesign
  - no broad appserver runtime-mode migration
  - no proof fabrication, auto-acceptance, or child-lane authority weakening

## Parity / Alignment Matrix
- Current truth:
  - `runtime_mode=appserver` is already represented in manifest/runtime surfaces
  - appserver child-lane stale behavior depends on runner, heartbeat, and proof-output truth
  - PID-only process checks are unsafe under PID reuse
  - missing or unreadable process identity is ambiguous, not equivalent to dead
- Reference truth:
  - stale invalidation candidates require evidence-backed runner identity
  - `runner_alive=false` only applies to the same original process identity
  - `heartbeat stale` and `no proof output` are necessary but insufficient symptoms
  - unknown or ambiguous process identity fails closed
- Target truth / intended delta:
  - `providerLinearChildLaneRunner` records parent-visible `provider_linear_child_lane_runner_pid` and `provider_linear_child_lane_runner_started_at`
  - stale evaluation compares current process identity against both fields
  - `stale_invalidation_candidate` is emitted only when identity matches and the same runner is dead
  - PID reuse, missing start identity, unreadable process state, or `runner_alive` unknown suppresses the candidate
- Explicitly out-of-scope differences:
  - CLI child-lane invalidation behavior
  - generic process cleanup
  - scheduler/admission policy
  - proof acceptance or patch acceptance loosening
  - Linear/workpad/PR lifecycle

## Readiness Gate
- Not done if:
  - `stale_invalidation_candidate` can still be produced without proven runner identity
  - PID reuse can be treated as dead original-runner evidence
  - missing `provider_linear_child_lane_runner_started_at` can still produce a candidate
  - unknown `runner_alive` can be treated as `runner_alive=false`
  - `heartbeat stale` plus `no proof output` is sufficient without process identity
  - the packet omits issue non-goals or `Not Done If`
- Pre-implementation issue-quality review evidence:
  - 2026-04-25: this child lane accepts the wider process-identity framing from the bounded handoff because correctness depends on exact protected terms and fail-closed semantics. The issue is not plausibly narrower than `heartbeat stale` handling or no-proof handling alone because the explicit safety boundary is ambiguous or unknown process identity.
  - 2026-04-25: the micro-task path is ineligible because correctness depends on protected wording, non-goals, `Not Done If`, and a parity matrix before implementation starts.
- Safeguard ownership split:
  - child lane owns only the declared docs/checklist/registry files
  - parent lane owns source/test inspection, implementation, docs-review, validation, Linear/workpad reconciliation, PR lifecycle, and merge

## Technical Requirements
- Functional requirements:
  1. Create the docs-first packet and registry/checklist mirrors for `CO-371`.
  2. In parent implementation, make `providerLinearChildLaneRunner` expose parent-visible `provider_linear_child_lane_runner_pid` and `provider_linear_child_lane_runner_started_at` for `runtime_mode=appserver` child-lane stale evaluation.
  3. Derive `runner_alive` from the same recorded runner identity, using both PID and start time.
  4. Treat missing PID, missing start time, unreadable start time, process lookup failure without identity proof, or PID reuse as unknown or ambiguous identity.
  5. Emit `stale_invalidation_candidate` only when `runtime_mode=appserver`, identity is proven, `runner_alive=false`, `heartbeat stale` is true, and `no proof output` is true.
  6. Suppress `stale_invalidation_candidate` when `runner_alive=true`, `runner_alive` is unknown, identity mismatches, PID reuse is possible, or proof output exists.
  7. Preserve fail-closed child-lane proof and patch-acceptance behavior.
- Non-functional requirements:
  - deterministic stale-candidate output
  - auditable process identity evidence
  - fail-closed behavior on ambiguity
  - platform-conscious PID reuse defense
  - minimal change local to provider child-lane stale evaluation
- Interfaces / contracts:
  - `provider_linear_child_lane_runner_pid`: recorded original runner PID for appserver child-lane stale evaluation
  - `provider_linear_child_lane_runner_started_at`: recorded original runner process start identity
  - `runner_alive`: same-runner liveness result, not PID-only liveness
  - `stale_invalidation_candidate`: emitted only after all safety preconditions pass
  - `heartbeat stale`: stale heartbeat symptom
  - `no proof output`: absence of real child-lane proof/output

## Architecture & Data
- Architecture / design adjustments:
  - prefer one narrow predicate for stale candidate eligibility so identity, heartbeat, and proof-output checks stay ordered and auditable
  - record runner identity at appserver child-lane runner startup before stale evaluation needs it
  - compare liveness using PID plus start identity to prevent PID reuse false positives
  - represent unknown identity separately from `runner_alive=false`
  - keep candidate emission separate from proof acceptance and patch application
  - 2026-04-25 final review fix: a dead or missing PID is not enough to prove the original runner is dead when `provider_linear_child_lane_runner_started_at` is missing or corrupt; that shape remains `runner_alive=null` with ambiguous identity and no `stale_invalidation_candidate`
- Data model changes / migrations:
  - additive fields for `provider_linear_child_lane_runner_pid` and `provider_linear_child_lane_runner_started_at` may be required in parent-visible lane evidence
  - no destructive migration or historical artifact rewrite is required
  - parent may need to tolerate absent fields on older artifacts by failing closed and not emitting a candidate
- External dependencies / integrations:
  - no Linear mutation from this child lane
  - no parent implementation details are changed here

## Current Truth
- `runtime_mode` and `runtime_mode_requested` already exist in manifest/runtime surfaces.
- `providerLinearChildLaneRunner` and `providerLinearChildLaneShell` are the likely implementation seams for runner identity and parent-visible stale decisions.
- The new issue contract is stricter than heartbeat or proof-output presence alone: ambiguous identity is a hard fail-closed condition.

## Proposed Design
- Parent implementation introduces or reuses a small stale-candidate eligibility predicate:
  - require `runtime_mode=appserver`
  - require `provider_linear_child_lane_runner_pid`
  - require `provider_linear_child_lane_runner_started_at`
  - compute same-runner `runner_alive`
  - require `runner_alive=false`
  - require `heartbeat stale`
  - require `no proof output`
  - otherwise return a no-candidate fail-closed reason

## Protected Expectations
- Preserve exact issue wording around `providerLinearChildLaneRunner`, `runner_alive`, `provider_linear_child_lane_runner_pid`, `provider_linear_child_lane_runner_started_at`, `runtime_mode=appserver`, `stale_invalidation_candidate`, PID reuse, `heartbeat stale`, and `no proof output`.
- Preserve fail-closed behavior for ambiguous or unknown process identity.
- Preserve parent-only ownership of implementation, validation, Linear, workpad, PR, and merge work.

## Reject These Wrong Interpretations
- `Heartbeat stale is enough.`
- `No proof output is enough.`
- `PID number equality proves identity.`
- `Unknown runner liveness means dead.`
- `A stale invalidation candidate can stand in for real proof output.`
- `This child lane should modify runtime code or tests.`

## Validation Plan
- Child-lane checks:
  - JSON parse of `tasks/index.json` and `docs/docs-freshness-registry.json`
  - protected-term grep across the six packet/mirror files
  - `git diff --check` over the declared docs scope
- Parent-lane focused checks:
  - focused `orchestrator/tests/ProviderLinearChildLaneRunner.test.ts` coverage for same-runner dead, live runner, PID reuse, missing start time, unknown liveness, heartbeat stale, and no proof output
  - focused `orchestrator/tests/ProviderLinearChildLaneShell.test.ts` coverage for parent-visible candidate/no-candidate behavior if the shell owns decision output
  - default repo validation gates before closeout:
    1. `node scripts/delegation-guard.mjs`
    2. `node scripts/spec-guard.mjs --dry-run`
    3. `npm run build`
    4. `npm run lint`
    5. `npm run test`
    6. `npm run docs:check`
    7. `npm run docs:freshness`
    8. `npm run repo:stewardship`
    9. `node scripts/diff-budget.mjs`
    10. `codex-orchestrator review` or `npm run review`
    11. `npm run pack:smoke` when parent source changes touch downstream CLI/package behavior

## Approvals
- Reviewer: pending parent docs-review / implementation
- Date: 2026-04-25
