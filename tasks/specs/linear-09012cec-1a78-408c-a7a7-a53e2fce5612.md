---
id: 20260413-linear-09012cec-1a78-408c-a7a7-a53e2fce5612
title: CO validation: stabilize unrelated provider-worker full-suite blockers surfaced during CO-164
status: in_progress
owner: Codex
created: 2026-04-13
last_review: 2026-04-13
review_cadence_days: 30
risk_level: high
related_prd: docs/PRD-linear-09012cec-1a78-408c-a7a7-a53e2fce5612.md
related_action_plan: docs/ACTION_PLAN-linear-09012cec-1a78-408c-a7a7-a53e2fce5612.md
related_tasks:
  - tasks/tasks-linear-09012cec-1a78-408c-a7a7-a53e2fce5612.md
review_notes:
  - 2026-04-13: Initial live issue-context read confirmed the CO team workflow states include `Ready`, `In Progress`, `In Review`, `Merging`, `Rework`, `Blocked`, and `Done`; the issue started in `Ready`, with no attached PR and no existing `## Codex Workpad` comment.
  - 2026-04-13: Pre-implementation issue-quality review confirmed the issue is already well-shaped around the two named provider-worker suite failures, explicit protected terms, and the truthful validation baseline requirement. The missing input is current-tree reproduction evidence, not broader scope.
  - 2026-04-13: Current workspace bootstrap differs from the source issue evidence because a one-line missing brace in `orchestrator/src/cli/control/providerIssueHandoff.ts` breaks TypeScript parsing and prevents the source-loaded `bin/codex-orchestrator.js` helper wrapper from booting. Until that seam is repaired, Linear workflow mutations use the built `dist/bin/codex-orchestrator.js` fallback with the same helper subcommands and arguments.
---

# Technical Specification

## Context
CO-165 is the provider-worker validation follow-up to CO-164. The issue names two specific provider-worker suite failures discovered during the required repo-wide `npm run test` floor. The current branch also carries a local one-line syntax regression in `providerIssueHandoff.ts` that blocks TypeScript parsing and the normal source-loaded workflow helper path, so the first technical step is to distinguish current local breakage from the issue’s named blocker surfaces.

## Requirements
1. Reproduce or truthfully non-repro `ProviderIssueHandoffAdmissionCache.test.ts` with exact commands and saved output.
2. Reproduce or truthfully non-repro `ProviderLinearWorkerRunner.test.ts` with exact commands and saved output.
3. Repair the smallest current-tree provider-worker seam that explains the failures.
4. Restore a truthful repo-wide `npm run test` validation baseline for provider-worker closeout.
5. Keep local source-bootstrap failures labeled separately from the issue’s named provider-worker blockers.
6. Do not reopen CO-164 cleanup semantics or weaken validation gates.

## Issue-Shaping Contract
- User-request translation carried forward:
  - stabilize the named provider-worker blocker suites and recover truthful provider-worker validation evidence on the current tree
  - keep the final outcome evidence-backed whether the blocker is live, fixed, or no longer reproducible after repairing current local breakage
- Protected terms / exact artifact and surface names:
  - `ProviderIssueHandoffAdmissionCache`
  - `ProviderLinearWorkerRunner`
  - `npm run test`
  - `orchestrator/tests/ProviderIssueHandoffAdmissionCache.test.ts`
  - `orchestrator/tests/ProviderLinearWorkerRunner.test.ts`
  - `orchestrator/src/cli/control/providerIssueHandoff.ts`
  - `orchestrator/src/cli/providerLinearWorkerRunner.ts`
- Nearby wrong interpretations to reject:
  - reopen CO-164 generic forced cleanup
  - apply broad timeout tuning or skip-based validation
  - assume the local missing-brace syntax error alone fully answers the issue without rerunning the named suites
- Explicit non-goals carried forward:
  - CO-164 control-host cleanup semantic changes
  - unrelated provider-worker refactors
  - hiding failures behind weaker assertions or gates

## Parity / Alignment Matrix
- Current truth:
  - the source issue reports failures in `ProviderIssueHandoffAdmissionCache.test.ts` and `ProviderLinearWorkerRunner.test.ts`
  - the current branch `linear/co-165-provider-worker-full-suite-blockers` also has a one-line missing brace in `providerIssueHandoff.ts` that breaks TypeScript parsing and the source-loaded helper bootstrap
  - there is no live PR or workpad comment attached to the issue yet
- Reference truth:
  - provider-worker review handoff requires a truthful repo-wide validation story
  - CO-164 itself stayed bounded to control-host cleanup behavior and should not be reopened casually here
- Target truth / intended delta:
  - exact current-tree evidence for both named suites
  - a smallest-correct repair that restores the affected provider-worker surfaces
  - a truthful `npm run test` result or narrowly-audited residual blocker explanation
- Explicitly out-of-scope differences:
  - unrelated control-host or provider-worker redesign
  - generic timeout inflation
  - validation downgrade

## Readiness Gate
- Not done if:
  - either named suite still fails without explanation
  - the repo-wide validation story is still ambiguous
  - the lane broadens into CO-164 cleanup work or generic timeout tuning
- Pre-implementation issue-quality review evidence:
  - 2026-04-13: the issue already preserves the exact blocker surfaces, wrong interpretations to reject, and non-goals needed for a bounded lane. Current-tree reproduction is the right next step.
- Safeguard ownership split:
  - parent: docs, issue workflow, reproductions, implementation choice, validation, review/handoff
  - delegated stream: audited `linear child-stream --pipeline docs-review`

## Technical Requirements
- Functional requirements:
  - repair or account for the current source bootstrap failure
  - rerun the named provider-worker suites and capture exact outcomes
  - apply the smallest fix needed for current-tree provider-worker validation
  - keep the workpad and checklist aligned with exact results
- Non-functional requirements (performance, reliability, security):
  - no false-green claims
  - bounded diff surface
  - fail closed when full-suite truthfulness cannot be restored
- Interfaces / contracts:
  - Linear workflow helper surface
  - `npm run test`
  - focused `npx vitest run ...` reproductions

## Architecture & Data
- Architecture / design adjustments:
  - prefer a shared provider-worker timing/state fix only if the named failures converge there
  - treat the current missing-brace syntax regression as a prerequisite local defect, not a reason to broaden scope
- Data model changes / migrations:
  - none expected
- External dependencies / integrations:
  - `orchestrator/src/cli/control/providerIssueHandoff.ts`
  - `orchestrator/src/cli/providerLinearWorkerRunner.ts`
  - `orchestrator/tests/ProviderIssueHandoffAdmissionCache.test.ts`
  - `orchestrator/tests/ProviderLinearWorkerRunner.test.ts`
  - `orchestrator/tests/ProviderWorkflowConfigStore.test.ts`

## Validation Plan
- Tests / checks:
  - audited docs-review child stream
  - focused reproductions for the two named suites
  - repo-wide `npm run test`
  - standard validation floor for the final non-trivial diff
  - standalone review and explicit elegance review before handoff
- Rollout verification:
  - refresh the same workpad after docs, reproduction, implementation, and final validation
- Monitoring / alerts:
  - keep workflow-helper bootstrap failures distinct from suite failures in notes and evidence

## Open Questions
- Whether the current local syntax regression fully or only partially overlaps the original admission-cache failure.
- Whether the two named provider-worker failures share a common root cause after the branch is parseable again.

## Approvals
- Reviewer: Self-approved for implementation start after this docs packet and pre-implementation issue-quality review. Docs-review, exact reproductions, and final validation/review remain pending.
- Date: 2026-04-13
