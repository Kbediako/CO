---
id: 20260406-linear-f0d312eb-055f-4926-80df-8fcaaf56839c
title: CO workflow: unblock repo-wide spec/docs freshness review blockers
status: in_progress
owner: Codex
created: 2026-04-06
last_review: 2026-04-06
review_cadence_days: 30
risk_level: high
related_prd: docs/PRD-linear-f0d312eb-055f-4926-80df-8fcaaf56839c.md
related_action_plan: docs/ACTION_PLAN-linear-f0d312eb-055f-4926-80df-8fcaaf56839c.md
related_tasks:
  - tasks/tasks-linear-f0d312eb-055f-4926-80df-8fcaaf56839c.md
review_notes:
  - 2026-04-06: Opened from Linear issue `CO-102` in the provider-worker workspace using issue id `f0d312eb-055f-4926-80df-8fcaaf56839c`.
  - 2026-04-06: Packaged `linear issue-context` confirmed the CO workflow states (`Ready`, `In Progress`, `In Review`, `Merging`, `Rework`, `Done`), showed no attached PR, no issue comments, and no existing workpad comment on this issue.
  - 2026-04-06: The issue was moved from `Ready` to `In Progress` before active coding via packaged `linear transition`.
  - 2026-04-06: The workspace started detached at `origin/main` commit `9118bf3682ff1d10faf3ee24b9fcd19d4b626c4f` and was switched onto branch `linear/co-102-spec-docs-freshness-blockers`.
  - 2026-04-06: Issue-quality review: this lane is specifically about repo-wide `Spec guard` / `docs:freshness` review handoff blockers and should stay bounded to stale docs/spec surfaces plus required packet mirrors, without weakening policy or reopening CO-99 runtime telemetry scope.
  - 2026-04-06: Pre-implementation approval: proceed with an audited stale-surface inventory first, then refresh or explicitly reconcile the stale docs/spec cohort with machine-checkable validation evidence.
  - 2026-04-06: Baseline stale inventory was split: `node scripts/spec-guard.mjs --dry-run` surfaced only task specs `1001` and `1009`-`1031` with `last_review: 2026-03-06`, while `npm run docs:freshness` reported a separate `stale docs: 19` registry cohort rooted in `.agent/SOPs/instruction-stamps.md` and the `0932`-`0934` packet family.
  - 2026-04-06: The blocker cohort was refreshed by updating the stale task-spec `last_review` dates plus the exact stale registry entries; post-fix `spec-guard --dry-run`, `docs:freshness`, `docs:check`, `build`, `lint`, and `test` all passed on branch `linear/co-102-spec-docs-freshness-blockers`.
  - 2026-04-06: Audited `docs-review` rerun succeeded at `.runs/linear-f0d312eb-055f-4926-80df-8fcaaf56839c-co-102-docs-review/cli/2026-04-06T03-20-19-962Z-29dd089f/manifest.json`.
  - 2026-04-06: Manifest-backed standalone review succeeded cleanly at `/Users/kbediako/Code/CO/.runs/linear-f0d312eb-055f-4926-80df-8fcaaf56839c/cli/2026-04-06T03-10-12-967Z-a94af73c/review/telemetry.json` (`status: succeeded`, `review_outcome: clean-success`).
  - 2026-04-06: Manual elegance review confirmed the fix is already the smallest truthful change set and recorded the result at `out/linear-f0d312eb-055f-4926-80df-8fcaaf56839c/manual/elegance-review-2026-04-06.md`.
  - 2026-04-06: Post-review truth-only fixes were limited to the `docs/TASKS.md` remaining-work line and matching `tasks/specs/1001` plus `1009`-`1031` registry-row sync; focused reruns of `spec-guard --dry-run`, `docs:freshness`, and `docs:check` stayed green.
  - 2026-04-06: A later standalone-review rerun stayed in low-signal doc-metadata inspection without surfacing a new concrete issue in that incremental delta, so fallback acceptance was recorded at `out/linear-f0d312eb-055f-4926-80df-8fcaaf56839c/manual/review-rerun-fallback-2026-04-06.md`.
---

# Technical Specification

## Context

`CO-102` is a follow-up from `CO-99` because PR `#368` was blocked at review handoff by repo-wide docs/spec freshness drift rather than by the bounded implementation change in that lane. The issue description records the current hypothesis: `Spec guard` is failing on stale 2026-03-06 task specs in the `1001`, `1009`-`1031` cohort, and `docs:freshness` still reports `stale docs: 19`.

## Requirements

1. Capture the exact stale surfaces behind `Spec guard` and `docs:freshness` on the current branch.
2. Refresh or otherwise reconcile every stale surface necessary to make those commands clean.
3. Preserve the current guardrail semantics and keep the fix scoped to docs/spec truth.
4. Record which stale surfaces were refreshed, archived, or otherwise reconciled.
5. Preserve provider-worker docs/workpad continuity through review handoff.

## Current Truth

- The lane started from clean `origin/main` commit `9118bf3682ff1d10faf3ee24b9fcd19d4b626c4f`.
- The exact `Spec guard` stale cohort on this branch was `1001` and `1009`-`1031`, all stale only because `last_review` remained `2026-03-06`.
- The exact `docs:freshness` stale cohort on this branch was the separate `stale docs: 19` registry set rooted in `.agent/SOPs/instruction-stamps.md` plus the `0932`-`0934` PRD/TECH_SPEC/ACTION_PLAN/task/mirror family.
- Those stale surfaces were refreshed on 2026-04-06, and the branch is now green on `node scripts/spec-guard.mjs --dry-run`, `npm run docs:freshness`, and `npm run docs:check`.
- No PR or existing workpad was attached to this issue at bootstrap time.

## Issue-Shaping Contract

- User-request translation carried forward:
  - unblock repo-wide review handoff blockers at the source
  - keep `Spec guard` and `docs:freshness` strict
  - avoid reopening CO-99 runtime telemetry scope
- Protected terms / exact artifact and surface names:
  - `Spec guard`
  - `docs:freshness`
  - `last_review`
  - `stale docs: 19`
  - `Core Lane`
  - `review handoff blockers`
- Nearby wrong interpretations to reject:
  - disable or weaken the freshness checks
  - treat this as only a CO-99-local problem
  - expand into unrelated runtime telemetry or app changes
- Explicit non-goals carried forward:
  - guardrail weakening
  - unrelated repo cleanup
  - provider-proof runtime behavior changes

## Parity / Alignment Matrix

- Not applicable.
- Current truth:
  - clean-branch review handoff is blocked by stale docs/spec surfaces
  - the issue points to the 2026-03-06 task-spec cohort as the main stale set
- Reference truth:
  - active lanes should be able to hand off once their own implementation gates are green
- Target truth / intended delta:
  - the stale docs/spec blocker cohort is refreshed or reconciled and the shared freshness gates pass again
- Explicitly out-of-scope differences:
  - any change to guardrail thresholds or meaning

## Readiness Gate

- Not done if:
  - stale task specs continue to fail `spec-guard --dry-run`
  - stale docs continue to fail `docs:freshness`
  - the only path to green is a policy waiver or guard weakening
- Pre-implementation issue-quality review evidence:
  - the issue has explicit stale-surface and non-goal framing, so it is not plausibly narrower than repo-wide freshness reconciliation.
- Safeguard ownership split:
  - docs/workpad and registry mirrors: current issue packet files plus packaged `linear` helper
  - stale-surface reconciliation: stale task/spec docs and any directly-required mirrors surfaced by the commands

## Technical Requirements

- Functional requirements:
  - exact stale-surface inventory from the current branch
  - truthful refresh or reconciliation of the stale blocker cohort
  - clean `spec-guard --dry-run` and `docs:freshness` on the resulting branch
  - workpad and task packet reflect the final stale-set disposition
- Non-functional requirements (performance, reliability, security):
  - keep the diff bounded to stale blocker truth maintenance
  - no unsupported assumptions about stale surfaces
  - evidence remains machine-checkable
- Interfaces / contracts:
  - `scripts/spec-guard.mjs`
  - `scripts/docs-freshness.mjs`
  - `tasks/specs/**`
  - `docs/docs-freshness-registry.json`
  - `tasks/index.json`
  - `docs/TASKS.md`

## Architecture & Data

- Architecture / design adjustments:
  - this is a repo-health docs lane, not a runtime or product feature lane
  - refresh a stale surface only after confirming it still represents current truth
  - keep packet mirrors and registry state aligned so the repo does not reintroduce synthetic freshness drift
- Data model changes / migrations:
  - none expected
- External dependencies / integrations:
  - packaged `linear` helper surface
  - repo-local guard scripts

## Validation Plan

- Tests / checks:
  - audited child `docs-review`
  - `node scripts/delegation-guard.mjs`
  - `node scripts/spec-guard.mjs --dry-run`
  - `npm run docs:freshness`
  - `npm run docs:check`
  - `npm run build`
  - `npm run lint`
  - `npm run test`
  - standalone review and elegance review before handoff
- Rollout verification:
  - record stale baseline command output
  - record clean post-refresh command output
  - refresh the Linear workpad after bootstrap, stale reconciliation, and final validation
- Monitoring / alerts:
  - rely on guard output and packet evidence

## Open Questions

- None. `docs:freshness` required the separate 19-entry registry cohort rooted in `.agent/SOPs/instruction-stamps.md` and the `0932`-`0934` packet family in addition to the stale task-spec `last_review` cohort.

## Approvals

- Reviewer: pending
- Date: 2026-04-06
