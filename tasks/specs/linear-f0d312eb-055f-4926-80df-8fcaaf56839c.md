---
id: 20260406-linear-f0d312eb-055f-4926-80df-8fcaaf56839c
title: CO workflow: stop repo-wide docs baseline regressions from forcing manual fallback in unrelated lanes
status: done
owner: Codex
created: 2026-04-06
last_review: 2026-04-09
review_cadence_days: 30
risk_level: high
related_prd: docs/PRD-linear-f0d312eb-055f-4926-80df-8fcaaf56839c.md
related_action_plan: docs/ACTION_PLAN-linear-f0d312eb-055f-4926-80df-8fcaaf56839c.md
related_tasks:
  - tasks/tasks-linear-f0d312eb-055f-4926-80df-8fcaaf56839c.md
review_notes:
  - 2026-04-09: Reopened from the previously merged `CO-102` attempt after `linear issue-context` showed the issue back in `Ready` with the old merged PR `#370` still attached and the old workpad comment still documenting the April 6 closeout.
  - 2026-04-09: `linear issue-context` confirmed the live CO workflow states (`Ready`, `In Progress`, `In Review`, `Merging`, `Rework`, `Done`) before any transition work in this reopened attempt.
  - 2026-04-09: The issue was moved from `Ready` to `In Progress` before active coding, and the required same-turn parallelization decision was recorded as `stay_serial` / `single_bounded_change` while the lane reset onto current `main`.
  - 2026-04-09: The workspace started detached at current `main` commit `41704f3fc` and was switched onto branch `linear/co-102-docs-baseline-regression-prevention`.
  - 2026-04-09: Full PR feedback sweep on the already-attached merged PR `#370` found no unresolved actionable review threads; the old feedback had been resolved before the prior merge and is historical context only for this reopened attempt.
  - 2026-04-09: Issue-quality review: this reopened lane is specifically about preventing shared docs-baseline relapse in unrelated provider-worker lanes, so implementation must stay bounded to archive eligibility, truthful stale-doc reconciliation, and packet/workpad evidence without blaming or weakening review policy.
  - 2026-04-09: Current baseline inventory is materially different from the April 6 repair: `node scripts/spec-guard.mjs --dry-run` returned `OK`, `npm run docs:check` returned `OK`, and `npm run docs:freshness` failed with `282` stale docs (`Task Packet=205`, `Task Mirror=41`, `Report Only=36`).
  - 2026-04-09: The implementation-doc archive automation is healthy but ineffective for this relapse: GitHub Actions `Implementation Docs Archive Automation` succeeded on 2026-04-08, and local `node scripts/implementation-docs-archive.mjs --dry-run` also reported `Archived docs: 0`, `Skipped docs: 313`, `Stray candidates: 146`.
  - 2026-04-09: Root cause narrowed before code edits: `scripts/implementation-docs-archive.mjs` only creates task-linked archive candidates when `tasks/index.json` has `status === "succeeded"` plus `completed_at`, but current completed task items use `status: "completed"` with `completed_at` and `gate.status: "succeeded"`.
  - 2026-04-09: Reopened audited child docs-review stream ran at `.runs/linear-f0d312eb-055f-4926-80df-8fcaaf56839c-co-102-docs-review-r2/cli/2026-04-08T22-48-54-430Z-492ca414/manifest.json` and failed only on the known repo-baseline `npm run docs:freshness` stage, which is the current in-scope blocker rather than a packet-shape or review-wrapper failure.
  - 2026-04-09: Pre-implementation approval: refresh the packet, rerun an audited docs-review child stream, patch the archive-eligibility seam with focused regression coverage, then rerun the shared docs baseline and reconcile any residual stale active docs truthfully.
  - 2026-05-07: CO-511 live issue-context confirmed CO-102 is Done (state_type=completed, updated_at=2026-04-17T08:54:26.933Z); existing packet lineage records PR #387. Reclassified the April 6 CO-102 packet rows as completed-lane historical metadata under live CO-511 docs:freshness:maintain ownership without weakening docs:freshness or deleting historical evidence.
---

# Technical Specification

## Context

The first `CO-102` attempt truthfully fixed the April 6 stale-spec cohort and merged as PR `#370`, but the issue reopened because unrelated lanes are again recording accepted manual fallback from shared docs-baseline drift. The current failure is no longer the old `1001` / `1009`-`1031` stale-spec set. On 2026-04-09, `spec-guard --dry-run` and `docs:check` are green while `docs:freshness` fails on `282` stale docs. The daily implementation-docs archive automation is succeeding but archiving nothing, which points to a candidate-selection seam rather than a dead workflow.

## Requirements

1. Capture the current shared docs-baseline failure shape with machine-checkable evidence.
2. Fix the implementation-doc archive selection logic so it recognizes the current completed-task vocabulary in `tasks/index.json`.
3. Restore a truthful green `docs:freshness` baseline without weakening review or docs policy.
4. Keep `spec-guard --dry-run` and `docs:check` green while the fix lands.
5. Record which stale surfaces were archived, refreshed, or explicitly left active after the rerun.

## Current Truth

- The lane started from clean current `main` commit `41704f3fc` in a detached workspace and moved onto branch `linear/co-102-docs-baseline-regression-prevention`.
- The old attached PR `#370` is merged history, not the current defect surface.
- `node scripts/spec-guard.mjs --dry-run` is clean on the reopened branch.
- `npm run docs:check` is clean on the reopened branch.
- `npm run docs:freshness` currently fails with `282` stale docs:
  - `Task Packet`: `205`
  - `Task Mirror`: `41`
  - `Report Only`: `36`
- `node scripts/implementation-docs-archive.mjs --dry-run` currently archives `0` docs even though the stale set is red.
- `tasks/index.json` currently contains both legacy completed items with `status: "succeeded"` and newer completed items with `status: "completed"` plus `completed_at` and `gate.status: "succeeded"`.
- `scripts/implementation-docs-archive.mjs` currently only promotes task-linked docs into archive candidates when `status === "succeeded"` and `completed_at` are both present.

## Issue-Shaping Contract

- User-request translation carried forward:
  - stop repo-wide docs-baseline regressions from forcing unrelated lanes into manual fallback
  - keep review and docs guardrails strict
  - repair the shared baseline rather than blaming review policy
- Protected terms / exact artifact and surface names:
  - `docs:check`
  - `docs:freshness`
  - `docs/TASKS.md`
  - `manual fallback`
  - `scripts/implementation-docs-archive.mjs`
  - `tasks/index.json`
- Nearby wrong interpretations to reject:
  - disable or loosen review/docs policy
  - treat the archive scheduler itself as broken without checking the local dry-run
  - solve the recurrence with another policy-free `last_review` churn pass
- Explicit non-goals carried forward:
  - review-wrapper redesign
  - unrelated runtime changes
  - broad archive-policy redesign

## Parity / Alignment Matrix

- Current truth:
  - shared docs baseline is red because completed packets are still active and stale
  - archive automation is healthy but not selecting those packets
- Reference truth:
  - completed implementation packets should leave the active baseline under the existing archive policy once they age past retention
- Target truth / intended delta:
  - archive eligibility matches the current task-completion vocabulary
  - completed packets archive truthfully
  - only genuinely active docs remain active after the rerun
- Explicitly out-of-scope differences:
  - changes to docs-freshness semantics
  - review-policy changes

## Readiness Gate

- Not done if:
  - completed task packets still cannot archive because the script only matches the old status vocabulary
  - unrelated lanes still need accepted manual fallback due shared docs-baseline drift
  - the lane claims prevention without rerunning the baseline after the code fix
- Pre-implementation issue-quality review evidence:
  - the issue is now narrowly shaped around the archive-selection seam and the resulting shared baseline recurrence, so it is not plausibly narrower than the intended prevention fix
- Safeguard ownership split:
  - packet/workpad truth: current issue docs, checklist mirrors, and `linear upsert-workpad`
  - archive-selection fix: `scripts/implementation-docs-archive.mjs` plus focused regression tests
  - residual baseline cleanup: only the docs surfaces directly surfaced by the post-fix rerun

## Technical Requirements

- Functional requirements:
  - exact failing baseline inventory on the reopened branch
  - archive selection that honors the current completion vocabulary
  - focused regression coverage for the new eligibility behavior
  - truthful post-fix baseline reconciliation and evidence capture
- Non-functional requirements (performance, reliability, security):
  - keep the implementation patch narrow and auditable
  - fail closed on ambiguous archive candidates
  - preserve machine-checkable evidence throughout
- Interfaces / contracts:
  - `scripts/implementation-docs-archive.mjs`
  - `tests/implementation-docs-archive.spec.ts`
  - `scripts/docs-freshness.mjs`
  - `docs/docs-freshness-registry.json`
  - `tasks/index.json`

## Architecture & Data

- Architecture / design adjustments:
  - the primary prevention seam is archive candidate selection, not docs-freshness reporting
  - the script should treat current completed task items as eligible when `completed_at` is present and the task is terminal under the repo's current vocabulary
  - residual docs that remain stale after the fix must be reviewed or split explicitly rather than hidden
- Data model changes / migrations:
  - none expected
- External dependencies / integrations:
  - packaged `linear` helper surface
  - GitHub Actions implementation-docs archive automation

## Validation Plan

- Tests / checks:
  - audited child `docs-review`
  - `node scripts/delegation-guard.mjs`
  - `node scripts/spec-guard.mjs --dry-run`
  - `npm run docs:check`
  - `npm run docs:freshness`
  - `node scripts/implementation-docs-archive.mjs --dry-run`
  - `npm run build`
  - `npm run lint`
  - `npm run test`
  - standalone review and elegance review before handoff
- Rollout verification:
  - record the current failing baseline
  - record the post-fix archive dry-run delta
  - record the final green baseline or the residual explicit split
- Monitoring / alerts:
  - rely on docs-freshness output, archive dry-run output, and the archive automation workflow

## Open Questions

- After the archive-selection fix lands, what residual stale docs remain truly active and still need review or a same-project follow-up?

## CO-382 Fallback Decision Table
- Large-refactor decision: bounded metadata repair; no large refactor is warranted because CO-511 removes the stale active freshness seam for this completed-lane residue instead of adding another owner-routing branch.
- Minor-seam decision: remove the minor docs-freshness seam now by archiving or reclassifying the April 6 completed-lane rows under live CO-511 owner evidence.

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `docs:freshness:maintain` | Completed-lane stale packet/spec row from the April 6 cohort | `remove fallback` | CO-511 parent lane | Terminal owner replacement plus clean-main baseline validation gate | 2026-04-06 | 2026-05-07 | Removed on 2026-05-07 | Row is archived or reclassified under live CO-511 owner evidence, with no retained fallback before handoff | CO-511 `node scripts/spec-guard.mjs`, `npm run docs:freshness`, and `npm run docs:freshness:maintain -- --format json` |

## Approvals

- Reviewer: CO-511 provider worker completed-lane reclassification
- Date: 2026-05-07
- Manifest: .runs/linear-df57f3cf-2e2e-4d29-a484-a8f3cd92e7c5/cli/2026-05-07T03-31-08-667Z-8c860a58/manifest.json
