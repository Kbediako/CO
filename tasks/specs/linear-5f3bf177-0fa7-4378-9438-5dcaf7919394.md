---
id: 20260417-linear-5f3bf177-0fa7-4378-9438-5dcaf7919394
title: CO workflow restore docs/TASKS headroom and make task archiving proactive
status: done
relates_to: docs/PRD-linear-5f3bf177-0fa7-4378-9438-5dcaf7919394.md
risk: high
owners:
  - Codex
last_review: 2026-05-18
review_notes:
  - 2026-05-18: CO-554 live issue-context verified CO-221 is Done/completed with PR #517 attached; reclassified inactive done so strict spec-guard no longer treats the completed packet as active stale work.
---

## Summary
- Objective: restore documented non-zero reserve headroom for `docs/TASKS.md` without replacing the existing archive system.
- Scope: policy, docs hygiene, archive trigger behavior, focused tests, and the required docs-first mirrors for `CO-221`.
- Constraints: preserve `docs/TASKS-archive-2026.md`, preserve `linear-*` handling, and avoid broad freshness or archive redesign.

## Issue-Shaping Contract
- User-request translation carried forward: prevent `main` from living at `450/450`, make the current archive path proactive, and stop unrelated lanes inheriting a full `docs/TASKS.md` baseline.
- Protected terms / exact artifact and surface names:
  - `docs/TASKS.md`
  - `450/450`
  - `docs:check`
  - `docs/TASKS-archive-2026.md`
  - `linear-*`
  - `scripts/tasks-archive.mjs`
  - `scripts/docs-hygiene.ts`
  - `docs/tasks-archive-policy.json`
- Nearby wrong interpretations to reject:
  - new archive system
  - generic docs freshness cleanup
  - packet-local `CO-213` only fix
- Explicit non-goals carried forward:
  - no implementation-doc archive work
  - no broader docs freshness cleanup
  - no archive-branch redesign

## Parity / Alignment Matrix
- Required for parity/alignment lanes; otherwise state `Not applicable`.
- Current truth: `docs/TASKS.md` can remain at `450/450`; archive automation trims only after overflow; recent lanes had to do packet-local cleanup.
- Reference truth: the repo-supported `tasks-archive` path already preserves inline snapshot truth and `linear-*` task keys.
- Target truth / intended delta: proactive reserve restoration and truthful zero-headroom signaling using the same archive path.
- Explicitly out-of-scope differences: any new archive system or generic freshness redesign.

## Readiness Gate
- Not done if:
  - `docs/TASKS.md` can still settle at `450/450`
  - the proactive trim loses snapshot truth or breaks `linear-*`
  - ordinary docs-first work still inherits a full-base failure path
- Pre-implementation issue-quality review evidence:
  - 2026-04-17 parent review confirms this is a narrow workflow-seam fix, not a broad docs-program cleanup. The task is docs-first ineligible for the micro-task path because correctness depends on exact protected artifact names, exact archive behavior, and explicit reserve semantics.
- Safeguard ownership split:
  - parent owns packet creation after the stalled docs child lane was invalidated
  - a later bounded child lane will own a focused regression slice after implementation shape is stable

## Technical Requirements
- Functional requirements:
  - policy documents a non-zero reserve target
  - archive script trims once reserve is exhausted, not only after overflow
  - docs hygiene stops treating zero headroom as healthy
  - focused tests cover near-full proactive archiving and zero-headroom checks
- Non-functional requirements (performance, reliability, security):
  - keep runtime and archive surface area unchanged outside the targeted seam
  - preserve deterministic archive output and current task-key handling
- Interfaces / contracts:
  - `docs/tasks-archive-policy.json`
  - `scripts/tasks-archive.mjs`
  - `scripts/docs-hygiene.ts`
  - `tests/tasks-archive.spec.ts`
  - `tests/docs-hygiene.spec.ts`

## Architecture & Data
- Architecture / design adjustments:
  - extend the existing archive policy with explicit reserve headroom
  - reuse the existing task-archives workflow with the updated script semantics
- Data model changes / migrations:
  - policy JSON grows a reserve field
- External dependencies / integrations:
  - `.github/workflows/tasks-archive-automation.yml`
  - `docs/TASKS.md`
  - `docs/TASKS-archive-2026.md`

## Validation Plan
- Tests / checks:
  - `node scripts/delegation-guard.mjs`
  - focused `tests/tasks-archive.spec.ts`
  - focused `tests/docs-hygiene.spec.ts`
  - `node scripts/spec-guard.mjs --dry-run`
  - `npm run build`
  - `npm run lint`
  - `npm run test`
  - `npm run docs:check`
  - `npm run docs:freshness`
  - `npm run repo:stewardship`
  - `node scripts/diff-budget.mjs`
  - manifest-backed standalone review
  - `npm run pack:smoke`
- Rollout verification:
  - run the updated archive script against the repo state to restore reserve before handoff
  - confirm `docs/TASKS.md` ends below the new proactive threshold
- Monitoring / alerts:
  - keep the existing tasks archive automation workflow; no new monitor surface

## Open Questions
- Is reserve `10` lines the smallest practical baseline, or should the repo carry a smaller explicit buffer?
- Should the docs check message include both the hard maximum and the reserve threshold?

## Approvals
- Reviewer: pending parent docs-review and implementation validation
- Date: 2026-04-17

## CO-382 Fallback Decision Table
- Large-refactor check: no new fallback mechanism; CO-554 removes stale active-spec lifecycle drift while keeping strict spec-guard authoritative.
- Minor-seam decision: bounded seam accepted because active pre-expiry specs now block through docs:freshness:maintain before hard expiry.

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Active spec lifecycle freshness | Completed or rolling active spec metadata can remain active until unrelated strict spec-guard jobs hit expiry | remove fallback | CO-554 | Apr 17 cohort reached strict spec-guard boundary after CO-543 | 2026-04-17 | 2026-05-18 | N/A after removal | Terminal specs are inactive done and active pre-expiry cohorts block through docs:freshness:maintain owner action | node scripts/spec-guard.mjs; docs-freshness-maintain regression |
