---
id: 20260401-linear-56067b9f-2aef-4033-8e12-08ac89bd9834
title: 'CO: Restore unrelated full-suite baseline blocking CO-43 handoff'
status: done
owner: Codex
created: 2026-04-01
last_review: 2026-05-02
review_cadence_days: 30
risk_level: high
related_prd: docs/PRD-linear-56067b9f-2aef-4033-8e12-08ac89bd9834.md
related_action_plan: docs/ACTION_PLAN-linear-56067b9f-2aef-4033-8e12-08ac89bd9834.md
related_tasks:
  - tasks/tasks-linear-56067b9f-2aef-4033-8e12-08ac89bd9834.md
review_notes:
  - 2026-05-02: CO-479 live Linear audit confirmed CO-46 is Done (state_type=completed, updated_at=2026-04-01T00:19:42.932Z) with merged PR #339 attached; this completed-lane spec is reclassified to inactive `done` under canonical owner key `spec-guard:active-specs:last_review=2026-04-01` so historical implementation evidence remains preserved without staying in active-spec freshness.
  - 2026-04-01: Opened from Linear issue `CO-46` in the provider-worker workspace after confirming the live CO workflow states with `linear issue-context`; the team exposes `In Progress`, `In Review`, `Merging`, and `Rework`, and the issue was moved from `Ready` to `In Progress`.
  - 2026-04-01: The workspace started detached at `902b968b864d704c26c1032c544b8aca431cd500` and was switched to branch `linear/co-46-restore-full-suite-baseline` before repo edits.
  - 2026-04-01: Initial `linear upsert-workpad` is currently blocked by a real Linear API rate limit (`429`, `requests_remaining: 0`, reset at `2026-03-31T15:28:56.479Z`); the local source body is staged at `/tmp/co46-workpad.md` and will be retried after the reset window.
  - 2026-04-01: This lane stays bounded to the unrelated full-suite baseline blocker for `CO-43`: reproduce, repair, or explicitly own the named `Doctor`, `UserConfigStageSets`, and `cli-frontend-test` failures without widening into `CO-43` feature work.
  - 2026-04-01: Delegation will be satisfied with an audited `linear child-stream --pipeline docs-review` run before implementation rather than a blanket delegation override.
  - 2026-04-01: The `docs-review` child stream manifest succeeded through `delegation-guard`, `spec-guard`, `docs:check`, and `docs:freshness`, then the forced review stage failed with telemetry `status: failed`, `review_outcome: failed-other`, and `termination_boundary: null`; the output log ended with the selected review model at capacity, so docs approval is carried via the recorded fallback note rather than by claiming a wrapper-boundary failure.
  - 2026-04-01: Focused reproduction showed `orchestrator/tests/Doctor.test.ts` and `orchestrator/tests/UserConfigStageSets.test.ts` already green on this branch; only `tests/cli-frontend-test.spec.ts` still failed, because its temp fixture preserved the shipped `frontend-testing` stage command while source-only worktree runs may not have a built `dist/orchestrator/src/cli/frontendTestingRunner.js` under the current checkout root.
  - 2026-04-01: Standalone review on the iterating diff caught two real issues in the frontend fixture seam: temporarily dropping fake-`codex` invocation assertions weakened shipped `frontendTestingRunner.js` coverage, and using a symlinked temp package root under macOS `/var` let the runner self-entry check skip `main()` because Node resolved the loaded file under `/private/var`. The final repair restores the fake-`codex` assertions and canonicalizes the temp package root with `realpath(...)`.
  - 2026-04-01: The final repair in `tests/cli-frontend-test.spec.ts` preserves the shipped `frontendTestingRunner.js` command, but replaces the earlier temp full-build approach with a lighter canonicalized temp package-root shim plus `runner_entry=` fake-`codex` logging. That keeps the package-root proof local without turning the focused spec into a repo-wide `tsconfig.build.json` build gate.
  - 2026-04-01: After the targeted fix, focused reruns were green and the full `npm run test` rerun printed green through `tests/cli-frontend-test.spec.ts`, `tests/linear-cli-help.spec.ts`, and the later tail suite `orchestrator/tests/CodexOrchestratorCli.test.ts`, but the command still hung in the separate nested `npm run test:orchestrator` quiet-tail case now owned by `CO-57` (`fdefaeca-8c14-4dc3-980e-cdc6cfa6d955`).
  - 2026-04-01: The final wrapper-led `FORCE_CODEX_REVIEW=1 npm run review` pass completed with telemetry `status: succeeded`, `review_outcome: clean-success`, and `termination_boundary: null` at `/Users/kbediako/Code/CO/.runs/linear-56067b9f-2aef-4033-8e12-08ac89bd9834/cli/2026-03-31T14-25-55-354Z-29b3b496/review/telemetry.json`; the supporting review and elegance notes are recorded in `out/linear-56067b9f-2aef-4033-8e12-08ac89bd9834/manual/20260401T000000Z-baseline-repair/11-standalone-review.md` and `out/linear-56067b9f-2aef-4033-8e12-08ac89bd9834/manual/20260401T000000Z-baseline-repair/12-elegance-review.md`.
---

# Technical Specification

## Context

`CO-43` is ready for scoped-review handoff, but the branch cannot move cleanly to review because the broader validation floor is still red outside that scoped wrapper seam. `CO-46` exists specifically to keep that unrelated baseline repair separate. The issue names three suites observed failing on 2026-03-30: `orchestrator/tests/Doctor.test.ts`, `orchestrator/tests/UserConfigStageSets.test.ts`, and `tests/cli-frontend-test.spec.ts`.

## Requirements

1. Reproduce the current branch-baseline truth for `orchestrator/tests/Doctor.test.ts`, `orchestrator/tests/UserConfigStageSets.test.ts`, and `tests/cli-frontend-test.spec.ts` in this workspace.
2. Determine whether each failure is a real runtime defect, stale expectation, fixture drift, or already-repaired seam that no longer reproduces.
3. Land the smallest truthful repair for any remaining failing seam, or record explicit issue/waiver ownership if a remaining failure is proven out of scope for this lane.
4. Run `npm run test` on the final branch state unless a remaining owned blocker makes that impossible; in that case record the owner and exact failing seam.
5. Keep the lane separate from `CO-43` feature work so the review-handoff issue can cite `CO-46` as the unrelated blocker.
6. Preserve docs-first and provider-worker workflow evidence: task packet, child docs-review, workpad updates when Linear rate limiting clears, and pre-handoff review/elegance gates for non-trivial diffs.

## Current Truth

- The issue description identifies the three named suites as the unrelated branch-baseline blocker for `CO-43`.
- This workspace initially arrived detached and is now on `linear/co-46-restore-full-suite-baseline`.
- There is currently no attached PR on the issue.
- Linear state is now `In Progress`, and the workpad retry can proceed once the earlier rate-limit window has cleared.
- Focused reruns show `Doctor.test.ts` and `UserConfigStageSets.test.ts` already pass on this branch; they no longer need code changes in this lane.
- `tests/cli-frontend-test.spec.ts` was the only still-live named failure. Its temp fixture used the shipped `frontend-testing` stage command, but source-only worktree runs may not have the built runner under the current checkout root and macOS temp-path aliasing could bypass the runner self-entry check. The final fixture therefore proves the package-root override with a canonicalized temp runner shim and explicit runner-entry logging instead of a repo-wide temp build.
- After that repair, the named suites are green. The only surviving full-suite blocker observed in this workspace is the separate nested `npm run test` quiet-tail hang already being worked in `CO-57`.

## Validation Plan

- Run an audited child `docs-review` stream after the packet exists and record the manifest plus truthful fallback when the review command fails without a classified wrapper boundary.
- Reproduce the named failures with focused commands and a full-suite baseline run:
  - `npm run test:orchestrator -- Doctor.test.ts UserConfigStageSets.test.ts`
  - `npx vitest run --config vitest.config.core.ts tests/cli-frontend-test.spec.ts`
  - `npm run test` until the named suites have emitted green output and any surviving non-terminal blocker ownership is explicit
- Rerun focused commands for every repaired seam before the final full-suite pass.
- Run the required pre-handoff review gates for any non-trivial diff:
  - `FORCE_CODEX_REVIEW=1 npm run review`
  - explicit elegance/minimality pass

## Manifest Evidence

- Linear issue read: packaged `linear issue-context --issue-id 56067b9f-2aef-4033-8e12-08ac89bd9834`
- Linear transition: packaged `linear transition --issue-id 56067b9f-2aef-4033-8e12-08ac89bd9834 --state "In Progress"`
- Docs-review child manifest: `.runs/linear-56067b9f-2aef-4033-8e12-08ac89bd9834-co-46-docs-review/cli/2026-03-31T14-34-19-787Z-aa1a35a9/manifest.json`
- Docs-review fallback note: `out/linear-56067b9f-2aef-4033-8e12-08ac89bd9834/manual/20260401T000000Z-baseline-repair/05-docs-review-override.md`
- Baseline reproduction note: `out/linear-56067b9f-2aef-4033-8e12-08ac89bd9834/manual/20260401T000000Z-baseline-repair/10-baseline-status.md`
