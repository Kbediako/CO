---
id: 20260406-linear-179fd570-c493-49a7-9eaf-9222beca114a
title: CO: Make shared-root reconciliation deterministic after merged closeout, including resumed Merging active-run recovery
status: done
relates_to: docs/PRD-linear-179fd570-c493-49a7-9eaf-9222beca114a.md
related_prd: docs/PRD-linear-179fd570-c493-49a7-9eaf-9222beca114a.md
related_action_plan: docs/ACTION_PLAN-linear-179fd570-c493-49a7-9eaf-9222beca114a.md
risk: high
owners:
  - Codex
last_review: 2026-04-06
review_notes:
  - 2026-05-07: CO-511 live issue-context confirmed CO-100 is Done (state_type=completed, updated_at=2026-04-19T06:41:07.632Z); existing packet lineage records PR #369. Reclassified the April 6 task packet/spec mirrors as completed-lane historical metadata under live CO-511 docs:freshness:maintain ownership without weakening docs:freshness or deleting historical evidence.
---

## Added by Bootstrap (refresh as needed)

## Summary
- Objective: make shared-root reconciliation deterministic and operator-visible after merged closeout while ensuring resumed `Merging` active-run recovery does not bypass deterministic merge closeout.
- Scope:
  - docs-first packet for `CO-100`
  - one active Linear workpad and audited docs-review child stream
  - recovery-ordering changes in `providerIssueHandoff.ts`
  - shared-root truth and operator-facing observability changes in merge closeout and provider observability
  - focused regressions for `CO-98` bypass recovery and shared-root skip visibility
  - standard validation, standalone review, elegance review, and review handoff
- Constraints:
  - preserve the existing dirty/non-`main`/non-`ff-only` safety guard for `/Users/kbediako/Code/CO`
  - keep the fix bounded to the merged closeout and `Merging` recovery seam
  - avoid broad provider/workspace redesign

## Issue-Shaping Contract
- User-request translation carried forward:
  - merged issues must leave the shared root reconciled or explicitly pending reconciliation, and `Merging` recovery must not preserve a resumed `activeRun` in a way that bypasses deterministic merge closeout
- Protected terms / exact artifact and surface names:
  - `shared-root reconciliation`
  - `deterministic merge closeout`
  - `providerMergeCloseout.ts`
  - `providerIssueHandoff.ts`
  - `providerIssueObservability.ts`
  - `provider_issue_rehydrated_active_run`
  - `merge_closeout`
  - `Merging`
  - `origin/main`
  - `--ff-only`
  - `/Users/kbediako/Code/CO`
  - `CO-80`
  - `CO-98`
  - `CO-40`
- Nearby wrong interpretations to reject:
  - safe-skip dirty-root behavior alone means no follow-up is needed
  - a dirty shared root means operators must reconcile manually forever
  - a rehydrated active run on `Merging` is sufficient even when merge closeout never ran
  - docs/workpad wording alone can fix this without control-host or closeout changes
- Explicit non-goals carried forward:
  - force-mutating unsafe shared checkouts
  - broad workspace-management redesign
  - reopening the full `CO-40` stale-success lane
  - changing general PR merge policy

## Parity / Alignment Matrix
- Current truth:
  - `providerMergeCloseout.ts` already records `shared_root.status` and `reason`, but merged closeout still reads as generic completion unless consumers inspect the raw sub-record
  - `providerIssueHandoff.ts` can still preserve `provider_issue_rehydrated_active_run` before invoking deterministic merge closeout on some `Merging` recovery paths
  - `providerIssueObservability.ts` treats merged closeout as fully completed with `no_action` even when `shared_root.status === skipped` and hides the exact reason from the projected PR snapshot
- Reference truth:
  - merged issues should reconcile the shared root automatically when safe
  - merged-but-unsafe shared-root mutation should be operator-visible as explicit pending reconciliation with exact reason
  - `Merging` recovery must not bypass deterministic merge closeout
- Target truth / intended delta:
  - shared-root skips are durable pending outcomes with exact machine-readable reason
  - `Merging` recovery invokes deterministic merge closeout before preserving a resumed active run when closeout is authoritative
  - the operator-facing progress/debug surface exposes shared-root status and reason directly and does not mark merged+skipped reconciliation as simply complete
- Explicitly out-of-scope differences:
  - force-merging or force-mutating unsafe checkouts
  - broad control-host lifecycle redesign
  - unrelated PR or review policy changes

## Readiness Gate
- Not done if:
  - a merged lane can still leave `/Users/kbediako/Code/CO` behind `origin/main` without a durable machine-readable reason
  - a resumed or retried `activeRun` on a `Merging` issue can still prevent deterministic merge closeout from ever running
  - the operator still needs manual git inspection as the only authoritative truth path
- Pre-implementation issue-quality review evidence:
  - reviewed the current tree against the issue before implementation. `CO-80` already added deterministic merge closeout and shared-root reconciliation recording, but the current repo audit shows two remaining gaps that match `CO-100`: refresh/rehydrate still prioritizes `provider_issue_rehydrated_active_run` in some `Merging` paths before merge closeout, and `providerIssueObservability.ts` still collapses merged+skipped shared-root reconciliation into a fully completed/no-action operator state.
- Safeguard ownership split:
  - this parent lane owns the docs packet, workpad, implementation, validation, and review/handoff
  - child `docs-review` is bounded to audited pre-implementation approval evidence for this same issue workspace

## Technical Requirements
- Functional requirements:
  - record an explicit durable pending shared-root reconciliation outcome when post-merge shared-root sync is skipped for safety
  - ensure live `Merging` refresh/rehydrate paths run deterministic merge closeout before preserving a resumed active-run claim
  - expose shared-root reconciliation status and exact reason in one authoritative operator-facing surface
  - preserve automatic shared-root fast-forward to `origin/main` when the shared root is exact clean `main`
  - preserve fail-closed behavior and explicit failure when shared-root status/fetch/merge commands fail
- Non-functional requirements (performance, reliability, security):
  - keep artifact updates monotonic and machine-checkable
  - avoid weakening shared-root mutation safety rules
  - keep the fix additive and bounded
- Interfaces / contracts:
  - `orchestrator/src/cli/control/providerMergeCloseout.ts`
  - `orchestrator/src/cli/control/providerIssueHandoff.ts`
  - `orchestrator/src/cli/control/providerIssueObservability.ts`
  - `orchestrator/tests/ProviderMergeCloseout.test.ts`
  - `orchestrator/tests/ProviderIssueHandoff.test.ts`
  - `orchestrator/tests/ProviderIssueObservability.test.ts`

## Architecture & Data
- Architecture / design adjustments:
  - keep shared-root outcome truth in `providerMergeCloseout.ts`, but encode pending shared-root reconciliation explicitly enough that consumers do not need to reverse-engineer it from summary text
  - move deterministic merge closeout ahead of resumed-active-run preservation in `providerIssueHandoff.ts` for live `Merging` recovery where closeout should now own the truth
  - project shared-root status and reason into `providerIssueObservability.ts` and make merged+skipped reconciliation a pending/actionable operator state rather than silent completion
- Data model changes / migrations:
  - additive changes to merge-closeout reason/summary/debug projection only
  - no destructive claim migration
- External dependencies / integrations:
  - shared-root git commands against `/Users/kbediako/Code/CO`
  - existing Linear tracked-issue refresh and GitHub PR snapshot plumbing

## Validation Plan
- Keep the docs-first packet, task checklist mirrors, and the Linear workpad aligned before review handoff.
- Run audited docs-review plus implementation-gate child streams, and record either successful review telemetry or an explicit wrapper-fallback note when the forced review wrapper stalls without a verdict.
- Rerun focused provider/control regressions that cover dirty-root skip visibility, resumed-active-run `Merging` recovery, proof freshness, and raw-vs-display shared-root status projection.
- Rerun the repo guardrail floor (`delegation-guard`, `spec-guard --dry-run`, `build`, `lint`, `test`, `docs:check`, `docs:freshness`, `diff-budget`, and `pack:smoke`) on the final diff before review handoff.
- Preserve coverage that safe shared-root fast-forward still reaches `/Users/kbediako/Code/CO` when the shared root is exact clean `main`.
- Preserve coverage that operator-facing progress/debug surfaces expose pending, skipped, or failed shared-root reconciliation with the exact reason rather than silently reporting success.

## Open Questions
- Resolved during implementation review: skipped shared-root reconciliation now upgrades the merge-closeout record to top-level `action_required` with `pending_shared_root_reconciliation` and keeps the issue in `Merging` so deterministic closeout stays retryable until shared-root sync succeeds.

## CO-382 Fallback Decision Table
- Large-refactor decision: bounded metadata repair; no large refactor is warranted because CO-511 removes the stale active freshness seam for this completed-lane residue instead of adding another owner-routing branch.
- Minor-seam decision: remove the minor docs-freshness seam now by archiving or reclassifying the April 6 completed-lane rows under live CO-511 owner evidence.

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `docs:freshness:maintain` | Completed-lane stale packet/spec row from the April 6 cohort | `remove fallback` | CO-511 parent lane | Terminal owner replacement plus clean-main baseline validation gate | 2026-04-06 | 2026-05-07 | Removed on 2026-05-07 | Row is archived or reclassified under live CO-511 owner evidence, with no retained fallback before handoff | CO-511 `node scripts/spec-guard.mjs`, `npm run docs:freshness`, and `npm run docs:freshness:maintain -- --format json` |

## Approvals
- Reviewer: CO-511 provider worker completed-lane reclassification
- Date: 2026-05-07
- Manifest: .runs/linear-df57f3cf-2e2e-4d29-a484-a8f3cd92e7c5/cli/2026-05-07T03-31-08-667Z-8c860a58/manifest.json
