---
id: 20260418-linear-a710d9a7-5187-414d-8a8b-beab7853e446
title: "CO workflow: restore Apr 18 docs:freshness baseline for Mar 18 stale cohort"
status: done
owner: Codex
created: 2026-04-18
last_review: 2026-05-18
review_cadence_days: 30
risk_level: high
related_prd: docs/PRD-linear-a710d9a7-5187-414d-8a8b-beab7853e446.md
related_action_plan: docs/ACTION_PLAN-linear-a710d9a7-5187-414d-8a8b-beab7853e446.md
related_tasks:
  - tasks/tasks-linear-a710d9a7-5187-414d-8a8b-beab7853e446.md
review_notes:
  - 2026-04-18: Issue-quality review approves CO-239 as a bounded Apr 18 baseline-repair lane for the Mar 18 `1289-1298` historical packet family. Preserve `docs:freshness`, CO-175 rolling visibility, `repo-wide freshness debt` versus `per-PR diff health`, and explicit rejection of blind review-date bumps, policy-cap changes, or reopening CO-175.
  - 2026-04-18: The shared `source-0` payload for this child lane is metadata-only. The packet therefore preserves the exact issue wording and counts from the parent-owned read-only issue cache while keeping child ownership limited to the six packet files only.
  - 2026-05-18: CO-522 spec lifecycle audit found the linked task checklist has zero unchecked items (29 checked), so this spec is terminal and eligible for implementation-docs archive. Evidence: `out/linear-b642e879-ba50-45ef-b0d9-b059afa9e932-recovery/spec-preexpiry-local-classification.json`.
---

# Technical Specification

## Context
CO-239 owns the Apr 18 docs:freshness rollover blocker for the Mar 18 `1289-1298` historical packet family. The live issue evidence on clean current `main` says the repo is red on `docs:freshness` again, but the blocker is not the existing CO-175-owned Mar 14 rolling cohort. Instead, one new Mar 18 historical packet family now contributes `70` blocking stale rows while the `221` CO-175 rolling rows remain visible.

This is a repo-wide baseline-repair issue, not active feature drift. The lane must preserve the distinction between `repo-wide freshness debt` and `per-PR diff health`, keep the CO-175 rolling cohort visible and unchanged, and reject blind date bumps or policy dilution.

## Current Truth
- Clean current `main` fails `docs:freshness` with:
  - `4071` docs
  - `4074` registry entries
  - `70` blocking stale rows
  - `221` rolling cohort entries owned by `CO-175`
  - `0` missing in registry
  - `0` missing on disk
  - `0` invalid entries
  - `0` uncatalogued docs
- The new blocking rows are all `last_review=2026-03-18`.
- The class split is:
  - `Task Packet=50`
  - `Task Mirror=10`
  - `Report Only=10`
- The path-family split is:
  - `.agent/task` (`10`)
  - `docs/ACTION_PLAN-*=10`
  - `docs/findings` (`10`)
  - `docs/PRD-*=10`
  - `docs/TECH_SPEC-*=10`
  - `tasks/specs` (`10`)
  - `tasks/tasks-*=10`
- The lineage is the historical packet family `1289-1298`.
- `docs/guides/docs-freshness-cohorts.md` currently documents baseline-repair ownership only through Apr 17.

## Requirements
1. Register this docs-first packet for CO-239 before any parent implementation.
2. Preserve the exact Apr 18 / Mar 18 issue wording and counts in the packet.
3. Parent must reproduce and preserve before artifacts for the Apr 18 blocker.
4. Parent must classify the Mar 18 cohort by class, path family, lineage, and disposition.
5. Parent must keep the CO-175 rolling freshness cohort visible and unchanged.
6. Parent must apply only evidence-backed review, refresh, archive, reclassification, or explicit owner-path changes.
7. Parent must restore a truthful green `docs:freshness` path for unrelated clean diffs without weakening policy.
8. Parent must update owner evidence so future lanes can cite Apr 18 baseline ownership directly.

## Issue-Shaping Contract
- User-request translation: restore the Apr 18 docs:freshness baseline for the Mar 18 historical packet family so unrelated clean diffs stop inheriting repo-wide freshness debt that they do not own.
- Protected terms / exact artifact and surface names:
  - `docs:freshness`
  - `docs:freshness:maintain`
  - `spec-guard`
  - `CO-175`
  - `rolling freshness cohort`
  - `repo-wide freshness debt`
  - `per-PR diff health`
  - `Mar 18 cohort`
  - `Task Packet`
  - `Task Mirror`
  - `Report Only`
  - `machine-checkable evidence`
  - `last_review=2026-03-18`
  - `1289-1298`
  - `70` blocking stale rows
  - `221` rolling cohort entries
  - `docs/docs-freshness-registry.json`
  - `docs/guides/docs-freshness-cohorts.md`
  - `tasks/index.json`
  - `docs/TASKS.md`
- Nearby wrong interpretations to reject:
  - CO-175 should be reopened or rewritten
  - the `221` rolling rows should be hidden
  - rolling caps or windows should be widened for Apr 18
  - the blocker should be solved by a blind review-date bump
  - this is active feature-lane drift rather than repo-wide freshness debt
  - the policy should be weakened to let the date-boundary cohort through
- Explicit non-goals carried forward:
  - no CO-175 product-scope reopen
  - no policy-cap or window changes
  - no warning-only downgrade of freshness gates
  - no unrelated historical cleanup beyond the Mar 18 blocker
  - no implementation or validation work in this child lane

## Parity / Alignment Matrix
- Current truth:
  - clean current `main` fails `docs:freshness` because of the Mar 18 `1289-1298` cohort plus the still-visible CO-175 rolling rows
  - the blocker is historical packet debt only, not missing or invalid registry drift
  - no Apr 18 packet or classification artifact exists yet
- Reference truth:
  - after `CO-209`, the repo had `0` blocking stale rows and only the visible `221` CO-175 rolling advisory
  - prior date-boundary owner lanes handled new historical packet cohorts without weakening policy or hiding CO-175
  - `repo-wide freshness debt` should remain explicit and separate from `per-PR diff health`
- Target truth / intended delta:
  - the Mar 18 cohort gets an explicit owner action under CO-239
  - `docs:freshness` returns to `0` blocking stale rows for unrelated clean diffs
  - CO-175 rolling debt remains visible and unchanged
  - Apr 18 ownership and disposition become machine-checkable for future lanes
- Explicitly out-of-scope differences:
  - reopening CO-175
  - cap or window changes
  - broader historical cleanup outside the Mar 18 blocker
  - child-lane registry, workpad, or validation ownership

## Readiness Gate
- Not done if: the Mar 18 `1289-1298` cohort still blocks `docs:freshness`; the fix depends on policy dilution; CO-175 rolling debt becomes hidden; or the packet fails to preserve class, path-family, and lineage truth for the blocker.
- Pre-implementation issue-quality review evidence: this task spec, PRD, and action plan preserve the exact protected wording, explicit non-goals, parity contract, and child-versus-parent ownership split that the user asked for.
- Safeguard ownership split: child lane writes only the six packet files. Parent owns `tasks/index.json`, `docs/TASKS.md`, `docs/docs-freshness-registry.json`, `docs/guides/docs-freshness-cohorts.md`, `out/` artifacts, validation, Linear state, workpad state, and PR lifecycle.

## Technical Requirements
- Functional requirements:
  1. Capture before artifacts for Apr 18 under `out/linear-a710d9a7-5187-414d-8a8b-beab7853e446/`.
  2. Produce a classification artifact for the Mar 18 `1289-1298` cohort covering `Task Packet`, `Task Mirror`, and `Report Only`.
  3. Include path-family grouping for `.agent/task`, `docs/ACTION_PLAN-*`, `docs/findings`, `docs/PRD-*`, `docs/TECH_SPEC-*`, `tasks/specs`, and `tasks/tasks-*`.
  4. Record the chosen disposition and rationale for each affected group.
  5. Implement only the smallest truthful disposition without weakening `docs:freshness`.
  6. Record after counts and any remaining visible owned debt.
  7. Update owner evidence so future lanes can cite Apr 18 ownership directly.
- Non-functional requirements:
  - machine-checkable before and after evidence
  - no hidden policy dilution
  - explicit preservation of the CO-175 rolling cohort
  - minimal bounded parent implementation
- Interfaces / contracts:
  - `docs/docs-freshness-registry.json`
  - `docs/guides/docs-freshness-cohorts.md`
  - `tasks/index.json`
  - `docs/TASKS.md`
  - `scripts/docs-freshness.mjs`
  - `scripts/docs-freshness-maintain.mjs`
  - `scripts/spec-guard.mjs`

## Architecture & Data
- Architecture / design adjustments: none required from this child lane. Parent should use the existing registry, cohort-guide, and maintenance-report surfaces.
- Data model changes / migrations: avoid schema changes unless current owner-evidence surfaces cannot express the Mar 18 disposition truthfully.
- External dependencies / integrations: parent-only updates to registry, cohort-guide, and workpad surfaces; no Linear mutation in this child lane.

## Validation Plan
- Child:
  - protected-term grep over the six packet files
  - `git diff --check` on the six packet files
- Parent:
  - before `npm run docs:freshness`
  - before `npm run docs:freshness:maintain` when needed
  - classification artifact for the Mar 18 cohort
  - `node scripts/spec-guard.mjs --dry-run` when relevant
  - `npm run docs:check`
  - `npm run docs:freshness`
  - parent docs-review before implementation
  - parent standalone review or explicit fallback
  - parent elegance or minimality pass

## Open Questions
- Is the smallest truthful action for the Mar 18 cohort a reviewed refresh of all `70` rows, or is there a smaller archive or reclassification split by path family?
- After the Mar 18 cohort is handled, do any additional stale historical packets remain outside the declared CO-175 rolling cohort and require a new owner lane?

## Approvals
- Reviewer: pending parent docs-review and parent implementation.
- Date: 2026-04-18
