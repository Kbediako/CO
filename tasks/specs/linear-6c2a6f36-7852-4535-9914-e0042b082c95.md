---
id: 20260415-linear-6c2a6f36-7852-4535-9914-e0042b082c95
title: CO: rehydrate released claims when same-issue live workers still exist
status: done
relates_to: docs/PRD-linear-6c2a6f36-7852-4535-9914-e0042b082c95.md
risk: high
owners:
  - Codex
last_review: 2026-05-17
review_notes:
  - 2026-05-17: CO-543 strict spec-guard audit reclassified this stale Apr 16 row inactive done; live Linear evidence verified CO-189 is Done/completed. No completed_at was inferred or fabricated.
---

## Canonical Reference
- Canonical TECH_SPEC: `tasks/specs/linear-6c2a6f36-7852-4535-9914-e0042b082c95.md`
- PRD: `docs/PRD-linear-6c2a6f36-7852-4535-9914-e0042b082c95.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-6c2a6f36-7852-4535-9914-e0042b082c95.md`
- Task checklist: `tasks/tasks-linear-6c2a6f36-7852-4535-9914-e0042b082c95.md`

## Traceability
- Linear issue: `CO-189` / `6c2a6f36-7852-4535-9914-e0042b082c95`
- Linear URL: https://linear.app/asabeko/issue/CO-189
- Shared source 0 anchor: `ctx:sha256:d583cfca8795d1397a52c70dcd9df8245c891b12282ff0c2c730035efd203dde#chunk:c000001`
- Source object id: `sha256:d583cfca8795d1397a52c70dcd9df8245c891b12282ff0c2c730035efd203dde`
- Child-lane run context: `.runs/linear-6c2a6f36-7852-4535-9914-e0042b082c95-docs-packet-cli/cli/2026-04-15T03-00-40-330Z-2d6bac51/manifest.json`

## Summary
- Objective: ensure provider-intake reconciliation and CO STATUS treat same-issue live provider-worker evidence as authoritative over stale retained `released` claim state.
- Rework update 2026-04-16: extend the already-merged pending-reopen fix to ordinary `released` / `provider_issue_released:not_active` claims when fresh Linear truth says the same issue is started and local run evidence is live.
- Scope:
  - docs-first packet for `CO-189`
  - parent-owned implementation in `providerIssueHandoff.ts` and `controlRuntime.ts`
  - parent-owned focused regressions in `orchestrator/tests/ProviderIssueHandoffRefreshSerialization.test.ts` and `orchestrator/tests/ControlRuntime.test.ts`
  - preservation of released-claim no-burn behavior from `CO-160` and `CO-162`, terminal pruning from `CO-182`, and historical pruning from `CO-73`
- Constraints:
  - child lane edits only the declared PRD, ACTION_PLAN, TECH_SPEC, and task checklist files
  - child lane does not edit implementation or test files
  - child lane does not call Linear mutation helpers
  - child lane does not run full repo validation suites
  - parent owns runtime fix, validation, workpad, PR, and Linear state

## Issue-Shaping Contract
- User-request translation carried forward: repair released-claim live-worker rehydration so a same-issue live provider worker rebuilds a running claim and remains visible in CO STATUS instead of preserving stale released truth.
- Protected terms / exact artifact and surface names: `provider-intake-state.json`, `claims[].state=released`, `provider_issue_released_pending_reopen`, `provider_issue_released:not_active`, `provider_issue_rehydrated_active_run`, `provider-linear-worker-proof.json`, `co-status --format json`, `counts.running`, `provider_debug_snapshot.claim`, live provider worker process tree, child-lane process tree, `CO-185`.
- Nearby wrong interpretations to reject: always preserve released claims, duplicate-launch a new worker, cancel the live worker, hide the mismatch with display-only text while accounting remains wrong, reintroduce direct issue-by-id burn for released claims, or broaden into scheduler/fresh-discovery redesign.
- Explicit non-goals carried forward: no Linear mutation, no child-lane implementation/test edit, no active worker kill/cancel/cleanup workaround, no request-budget redesign, no broad CO STATUS redesign.

## Parity / Alignment Matrix
- Not applicable as a parity lane.
- Current truth:
  - released-claim no-burn paths are intentionally local-first for claims without live worker evidence
  - intake reconciliation can discover same-issue active runs and worker proof
  - released-claim preservation can run before active-run rehydrate for the same claim
  - CO STATUS can omit a started worker when the retained claim is released/pending-reopen or ordinary released/not_active despite live local evidence
- Reference truth:
  - same-issue live worker evidence should outrank stale retained released state
  - stale released state should not hide active work from the durable claim ledger
  - no-live-worker released claims should keep no-burn behavior
- Target truth / intended delta:
  - rehydrate/refresh transitions a released claim with same-issue active run to `state: "running"` / `reason: "provider_issue_rehydrated_active_run"`
  - CO STATUS includes started released-pending-reopen and ordinary released/not_active workers while intake catches up and keeps `counts.running` aligned with rows
  - intake persists active run identity, manifest path, worker-host proof, and fresh issue metadata when available
  - released preservation, cancel, and cleanup remain scoped to genuinely released claims
- Explicitly out-of-scope differences:
  - scheduler/cap redesign
  - Linear workflow or label changes
  - fresh-discovery fairness policy beyond preserving existing guarantees
  - implementation/test changes by this child lane

## Readiness Gate
- Not done if:
  - a same-issue `in_progress` run exists and intake leaves the claim `released` or CO STATUS omits it from active-worker accounting
  - a live worker is canceled or cleaned up to satisfy stale released state
  - duplicate `launcher.start` or `launcher.resume` is invoked for the same issue
  - retained released no-live-worker cases regain `dispatch_source_issue_by_id` burn
  - terminal/historical rows from `CO-182` or `CO-73` return to active counts
  - parent implementation broadens into scheduler/request-budget changes without a new lane
  - this child lane edits files outside the declared four-file scope
- Pre-implementation issue-quality review evidence:
  - 2026-04-15: child-lane self-review confirms the issue is not a generic released-claim no-burn lane and not a fair-discovery lane. Correctness depends on exact same-issue live-worker precedence over stale retained released state, so the micro-task path is ineligible.
- Safeguard ownership split:
  - child lane owns this docs packet only
  - parent lane owns registry/mirror updates, implementation, Linear state/workpad, validation, review, PR lifecycle, and final integration

## Technical Requirements
- Functional requirements:
  - discover same-issue active provider runs during rehydrate/refresh before preserving released state
  - if an attachable same-issue run has `status: "in_progress"`, upsert the claim as `running`
  - for ordinary `provider_issue_released:not_active`, require fresh started Linear truth before rehydrating; cached claim state alone is insufficient
  - set `reason: "provider_issue_rehydrated_active_run"` for the recovered active worker
  - persist `task_id`, `run_id`, `run_manifest_path`, and `worker_host` from live run/proof
  - keep started released-pending-reopen and ordinary released/not_active provider workers visible in CO STATUS compatibility projection while intake rehydrates
  - keep `counts.running` and active rows aligned
  - refresh tracked issue metadata when the existing rehydrate metadata-refresh path is enabled
  - avoid `launcher.start` and `launcher.resume` for already-live same-issue workers
  - keep release cancellation and workspace cleanup limited to genuinely released claims with no authoritative active same-issue run
  - preserve no-burn fail-closed behavior for retained released claims with no live worker
  - preserve bounded newer-evidence reopen paths for released claims that become runnable again
  - preserve terminal and historical pruning for `CO-182` / `CO-73`
- Non-functional requirements:
  - minimal implementation localized to existing rehydrate/refresh/release ordering in `providerIssueHandoff.ts` plus current-activity classification in `controlRuntime.ts`
  - no new persistent schema migration expected
  - no unbounded live Linear reads
  - no private Linear payloads or tokens in diagnostics/fixtures
  - active worker safety outranks stale claim cleanup
- Interfaces / contracts:
  - `orchestrator/src/cli/control/providerIssueHandoff.ts`
  - `orchestrator/src/cli/control/controlRuntime.ts`
  - `orchestrator/src/cli/control/providerIntakeState.ts`
  - `orchestrator/src/cli/providerLinearWorkerRunner.ts`
  - `orchestrator/tests/ProviderIssueHandoffRefreshSerialization.test.ts`
  - `orchestrator/tests/ControlRuntime.test.ts`
  - `provider-intake-state.json`
  - `provider-linear-worker-proof.json`

## Architecture & Data
- Architecture / design adjustments:
  - parent should evaluate active same-issue run evidence before the released-claim preservation branch in rehydrate and refresh
  - parent should reuse existing active-run rehydrate claim-building behavior where practical rather than creating a second claim shape
  - parent should keep release cleanup/cancel checks after the active-run exclusion so live workers are not treated as stale released cleanup targets
  - parent should share the same started-worker state classification in CO STATUS compatibility projection so admission/accounting surfaces do not silently omit live workers during the rehydrate window
- Data model changes / migrations:
  - no migration expected
  - existing claim fields are sufficient: `state`, `reason`, `task_id`, `run_id`, `run_manifest_path`, `worker_host`, issue metadata, and retry/review/merge cleanup fields
  - older snapshots without `worker_host` should still normalize safely
- External dependencies / integrations:
  - local run manifests under `.runs/<task>/cli/<run>/manifest.json`
  - provider proof file `provider-linear-worker-proof.json`
  - live Linear tracked-issue resolver only when the existing rehydrate metadata-refresh path already asks for it

## Validation Plan
- Tests / checks:
  - child lane performs scoped review for the declared four docs/task files
  - parent adds or reruns focused regression in `orchestrator/tests/ProviderIssueHandoffRefreshSerialization.test.ts`
  - parent focused test should seed retained released pending-reopen and ordinary released/not_active claims for a started issue, create same-issue `in_progress` manifests, call `service.rehydrate()` or `service.refresh()`, and assert the claims become running with `provider_issue_rehydrated_active_run`
  - parent adds or reruns focused CO STATUS projection coverage in `orchestrator/tests/ControlRuntime.test.ts`
  - parent also reruns adjacent retained released no-burn and terminal/historical pruning tests from `CO-160`, `CO-162`, `CO-182`, and `CO-73`
  - parent runs `node scripts/spec-guard.mjs --dry-run` after accepting the docs packet and registry mirrors
  - parent runs normal validation/review/elegance gates before PR handoff
- Rollout verification:
  - after parent implementation, inspect `provider-intake-state.json` in a simulated or live restart case and confirm same-issue live worker evidence surfaces as running
  - confirm retained released no-live-worker rows remain released without direct issue-by-id burn
- Monitoring / alerts:
  - rely on existing provider-intake state and provider polling health; no new external alerting required in this slice

## Open Questions
- Should parent share the active-run precedence helper between rehydrate and refresh-cycle claim reconciliation if both can encounter released/live contradictions?
- Should parent add an operator-facing note for recovered released-live contradictions, or is the claim transition reason enough?

## Approvals
- Reviewer: pending parent docs-review and implementation validation
- Date: 2026-04-15
