---
id: 20260412-linear-2136ea50-1fdf-44c2-ae3c-0e8d8a364b59
title: CO: audit remaining top-level non-doc and config stewardship surfaces and prune low-value residue
status: in_progress
owner: Codex
created: 2026-04-12
last_review: 2026-04-13
review_cadence_days: 30
risk_level: medium
related_prd: docs/PRD-linear-2136ea50-1fdf-44c2-ae3c-0e8d8a364b59.md
related_action_plan: docs/ACTION_PLAN-linear-2136ea50-1fdf-44c2-ae3c-0e8d8a364b59.md
related_tasks:
  - tasks/tasks-linear-2136ea50-1fdf-44c2-ae3c-0e8d8a364b59.md
review_notes:
  - 2026-04-12: Opened from Linear issue `CO-139` in the provider-worker workspace after rechecking live CO workflow states with the packaged `linear issue-context` helper, moving the issue from `Ready` to `In Progress`, recording the required same-turn `parallelize_now` / `independent_scope_available` decision, and creating the single persistent `## Codex Workpad`.
  - 2026-04-12: The detached workspace at commit `f3d45a494` was switched onto branch `linear/co-139-top-level-stewardship-surfaces` before repo edits.
  - 2026-04-12: Current repo truth confirms the listed surfaces are active, but at least `.ai-dev-tasks/create-prd.md`, `.ai-dev-tasks/generate-tasks.md`, and `.ai-dev-tasks/process-task-list.md` still point at deprecated template paths and older task-id conventions.
  - 2026-04-12: Direct consumer evidence is already present for the rest of the target set: `.codex/orchestrator.toml` via provider-worker tests/docs, `adapters/**/*` via CLI imports/tests, `eslint-plugin-patterns/**/*` via the file dependency in `package.json`, `patterns/**/*` via build/tests/docs, `prompt-snippets/frontend-aesthetics-v1.md` via the frontend design packet, `types/**/*` via the TypeScript configs/build output, and the root config files via repo CLI/runtime/docs/test contracts.
  - 2026-04-12: The same-issue child lane `prompt-snippets-audit` completed successfully and produced a one-file patch artifact that adds bounded consumer metadata to `prompt-snippets/frontend-aesthetics-v1.md`; parent acceptance is still pending.
  - 2026-04-12: Pre-implementation issue-quality review approves a bounded disposition lane: refresh stale retained guidance, sharpen catalog truth for the named surfaces, accept or reject the child patch explicitly, and do not reopen `CO-126`.
  - 2026-04-13: Parent accepted the bounded `prompt-snippets/frontend-aesthetics-v1.md` metadata patch, refreshed `.ai-dev-tasks/create-prd.md`, `.ai-dev-tasks/generate-tasks.md`, and `.ai-dev-tasks/process-task-list.md` to current docs-first/template truth, and added exact required-text stewardship entries for those refreshed files.
  - 2026-04-13: The docs-review child-stream history is now explicit evidence rather than a blocker guess: the first reruns exposed packet-local `docs:check` defects that were fixed (`docs/TASKS.md` line budget and one stale backticked path), while the latest rerun passed `docs:check` and stopped only on the broader repo `docs:freshness` baseline.
  - 2026-04-13: The branch was fast-forwarded onto current `origin/main`; `git rev-list --left-right --count origin/main...HEAD` now reports `0 0`, and repo-local validation reruns with provider-only `CODEX_ORCHESTRATOR_*` overrides stripped passed `delegation-guard`, `spec-guard`, `build`, `lint`, `test`, `docs:check`, `docs:freshness`, `repo:stewardship`, the explicit stewardship audit report, and `diff-budget`.
  - 2026-04-13: The forced standalone review rerun under `FORCE_CODEX_REVIEW=1` reached explicit `review_outcome: failed-boundary` telemetry with `termination_boundary.kind=command-intent` / `provenance=validation-suite` after the reviewer launched a validation suite. Manual fallback then refreshed the stale blocked-state packet/workpad narrative and recorded no remaining correctness or regression findings before the explicit elegance pass.
---

# Technical Specification

## Context
`CO-124` made repo stewardship whole-repo and recurring, while `CO-126` removed the immediate archive/reference residue cluster. The remaining gap is different: these top-level surfaces are mostly still active, but their retention story is uneven. Some are only justified by broad globs, and at least `.ai-dev-tasks/*.md` is still tracked despite stale content. This lane exists to make that retained top-level surface set truthful.

## Requirements
1. Preserve the issue wording and keep the lane limited to the named top-level non-doc/config surfaces plus minimum supporting docs/task/workpad updates.
2. Record an explicit disposition for every targeted surface: `keep`, `update`, `move`, or `delete`.
3. Refresh any retained surface whose local guidance or stewardship rationale is stale.
4. Review the completed `prompt-snippets/**` child-lane patch and accept, reject, or invalidate it explicitly from the parent lane.
5. Rerun `repo:stewardship` and capture explicit post-change artifacts for this slice.

## Current Truth
- `.ai-dev-tasks/**/*`:
  - active docs/tasking surface via `scripts/lib/docs-helpers.js`, `docs/docs-catalog.json`, and `docs/docs-freshness-registry.json`
  - current content is stale and should land as `update`, not blind `keep`
- `.codex/**/*`:
  - active repo policy surface via `.codex/orchestrator.toml`
  - consumed by provider-worker/delegation docs and tests
  - disposition target: `keep`
- `adapters/**/*`:
  - active code surface imported by CLI services and covered by adapter tests
  - disposition target: `keep`
- `eslint-plugin-patterns/**/*`:
  - active code/package surface via `package.json` file dependency and plugin loader
  - disposition target: `keep`
- `patterns/**/*`:
  - active codemod/linter surface via `npm run build:patterns`, tests, and README
  - disposition target: `keep`
- `prompt-snippets/**/*`:
  - active prompt surface via the frontend design packet
  - current folder is minimal and likely `keep`, but parent must decide whether to accept the bounded metadata patch
- `types/**/*`:
  - active contract surface via `tsconfig.json`, `tsconfig.build.json`, and packaged `dist/types/**`
  - disposition target: `keep`
- Root config files:
  - `package.json`, `package-lock.json`, `codex.orchestrator.json`, `mcp-client.json`, `design.config.yaml`, `tsconfig.json`, `tsconfig.build.json`
  - all have live consumer or packaging evidence
  - disposition target: `keep`, with sharper exact-entry rationale/checks where useful

## Issue-Shaping Contract
- User-request translation carried forward:
  - audit the remaining top-level non-doc/config surfaces
  - classify each retained surface as `keep`, `update`, `move`, or `delete`
  - align kept surfaces with current behavior and documented ownership
  - avoid silently reopening `CO-126`
- Protected terms / exact artifact and surface names:
  - `.ai-dev-tasks`
  - `.codex`
  - `adapters`
  - `eslint-plugin-patterns`
  - `patterns`
  - `prompt-snippets`
  - `types`
  - `root JSON/YAML config files`
  - `repo:stewardship`
- Nearby wrong interpretations to reject:
  - broad archive/reference cleanup
  - generic docs freshness baseline repair
  - runtime/provider/control-host work
- Explicit non-goals carried forward:
  - `CO-126` archive/reference cleanup
  - unrelated `CO-88` compatibility cleanup
  - generic UI/status work

## Parity / Alignment Matrix
- Not applicable as a formal parity lane.
- Current truth: the target surfaces are active but unevenly justified, and `.ai-dev-tasks` is demonstrably stale.
- Reference truth: each retained top-level surface should have a current local story backed by nearby code, docs, tests, packaging, or policy config.
- Target truth / intended delta: the target set has explicit dispositions, refreshed retained guidance where needed, sharper machine-checkable catalog coverage, and a post-change stewardship rerun.
- Explicitly out-of-scope differences: archive/reference cleanup, runtime behavior changes, unrelated docs baseline repairs.

## Readiness Gate
- Not done if:
  - any targeted surface still lacks an explicit disposition
  - `.ai-dev-tasks` remains stale after the lane claims the surface is active
  - the child-lane patch decision is left pending
  - `repo:stewardship` is not rerun with explicit artifacts
- Pre-implementation issue-quality review evidence:
  - live file inventory, consumer search results, and the child-lane patch artifact all support a bounded update-focused lane rather than a broader cleanup or catalog redesign
- Safeguard ownership split:
  - parent owns docs packet, catalog changes, `.ai-dev-tasks`, and final disposition matrix
  - child lane owns only the bounded `prompt-snippets/**` patch proposal until parent accepts or rejects it
  - archive/reference or other residual cleanup stays out of scope unless a new separate follow-up is required

## Technical Requirements
- Functional requirements:
  - refresh `.ai-dev-tasks/create-prd.md`, `.ai-dev-tasks/generate-tasks.md`, and `.ai-dev-tasks/process-task-list.md` to current docs-first/template truth
  - sharpen `docs/repo-stewardship-catalog.json` for the targeted top-level surface set with explicit rationale and cheap checks where available
  - review and accept/reject the child-lane `prompt-snippets/frontend-aesthetics-v1.md` patch
  - record the explicit disposition matrix for the targeted set in the packet/checklist/workpad
  - capture post-change stewardship JSON and markdown artifacts
- Non-functional requirements:
  - keep the diff bounded and reviewable
  - avoid speculative ownership claims not backed by current repo evidence
  - preserve fail-closed stewardship behavior
- Interfaces / contracts:
  - `docs/repo-stewardship-catalog.json`
  - `scripts/repo-stewardship-audit.mjs`
  - `scripts/lib/docs-helpers.js`
  - `package.json`
  - `docs/design/PRD-frontend-design-pipeline-v2.md`
  - `docs/design/specs/FRONTEND_DESIGN_PIPELINE_V2.md`
  - `tasks/frontend-design-pipeline-v2.md`

## Architecture & Data
- Architecture / design adjustments:
  - prefer exact catalog entries for root config files and stale `.ai-dev-tasks` files where simple required-text checks can enforce truth
  - keep broad patterns as fallback for the rest of the repo
  - accept the prompt-surface patch only if it stays smaller than inventing a new folder-local rationale layer
- Data model changes / migrations:
  - refresh three tasking prompt markdown files
  - likely add several exact catalog entries
  - optionally accept one child-lane snippet metadata patch
- External dependencies / integrations:
  - none beyond existing internal docs, tests, and stewardship tooling

## Validation Plan
- Tests / checks:
  - audited `linear child-stream --pipeline docs-review`
  - `MCP_RUNNER_TASK_ID=linear-2136ea50-1fdf-44c2-ae3c-0e8d8a364b59 npm run repo:stewardship`
  - `MCP_RUNNER_TASK_ID=linear-2136ea50-1fdf-44c2-ae3c-0e8d8a364b59 node scripts/repo-stewardship-audit.mjs --report out/linear-2136ea50-1fdf-44c2-ae3c-0e8d8a364b59/manual/repo-stewardship.json --summary-markdown out/linear-2136ea50-1fdf-44c2-ae3c-0e8d8a364b59/manual/repo-stewardship.md`
  - `node scripts/delegation-guard.mjs`
  - `node scripts/spec-guard.mjs --dry-run`
  - `npm run build`
  - `npm run lint`
  - `npm run test`
  - `npm run docs:check`
  - `npm run docs:freshness`
  - `node scripts/diff-budget.mjs`
  - manifest-backed standalone review plus explicit elegance review
- Rollout verification:
  - docs-review is captured before implementation continues
  - workpad is refreshed after docs-first, after implementation, and before handoff
  - post-change stewardship report shows the target set clean or explicitly justified
- Monitoring / alerts:
  - recurring `repo:stewardship` remains the long-term detector

## Approvals
- Reviewer: docs-review child-stream recorded; standalone review boundary failure manually closed with explicit fallback and elegance notes
- Date: 2026-04-13
