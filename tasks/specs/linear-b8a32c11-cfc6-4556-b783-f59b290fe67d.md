---
id: 20260328-linear-b8a32c11-cfc6-4556-b783-f59b290fe67d
title: CO Run Codex 0.117.x-to-main Canary for Multi-Agent-v2 and App-Server Adoption
status: done
relates_to: tasks/tasks-linear-b8a32c11-cfc6-4556-b783-f59b290fe67d.md
risk: high
owners:
  - Codex
last_review: 2026-05-03
review_notes:
  - 2026-05-03: CO-498 live Linear baseline review verified CO-22 is Done/completed; archived/reclassified this historical packet metadata without deleting packet evidence or weakening docs:freshness/spec-guard.
---

## Added by Bootstrap (refresh as needed)

## Summary
- Objective: re-audit CO against the live `codex-cli 0.117.0` plus upstream-main multi-agent-v2/app-server baseline and land only the active guidance/runtime changes that are already evidence-backed.
- Scope: docs-first packet, active policy/default corrections, additive collab-lineage capture where current payloads expose richer metadata, and canary evidence plus recommendation artifacts.
- Constraints: do not weaken delegation guard, do not widen issue-level authority boundaries, and treat `thread/shellCommand` as a sensitive unsandboxed surface.

## Technical Requirements
- Functional requirements:
  - Verify current upstream multi-agent-v2 and app-server surfaces against CO wrappers/docs.
  - Remove stale active `max_spawn_depth` guidance/default assumptions.
  - Refresh the Codex version posture from the stale `0.115.0` baseline only if canary evidence is clean.
  - State whether provider workers should stay on exec/resume supervision or start planning an app-server-backed supervision seam.
- Non-functional requirements (performance, reliability, security):
  - Keep changes additive and reviewable.
  - Preserve stable manifest compatibility for existing collab lineage fields.
  - Document explicit deferrals when richer metadata is not currently observable under the exec/app-server history seam.
- Interfaces / contracts:
  - `packages/shared/manifest/types.ts`
  - `orchestrator/src/cli/services/commandRunner.ts`
  - `orchestrator/src/cli/doctor.ts`
  - operator-facing policy/docs surfaces under `AGENTS.md` and `docs/`.

## Architecture & Data
- Architecture / design adjustments:
  - Keep `provider-linear-worker` on the current exec/resume supervision seam in this issue unless the canary proves a narrow richer control seam that does not widen authority.
  - Treat app-server as an evidence-backed candidate control substrate, not an automatic supervision migration.
- Data model changes / migrations:
  - Additive-only manifest lineage metadata if current payloads expose it; keep `sender_thread_id` and `receiver_thread_ids` stable.
- External dependencies / integrations:
  - Local Codex fork under `/Users/kbediako/Code/codex`
  - Installed `codex-cli 0.117.0`
  - CO validation and review wrappers.

## Validation Plan
- Tests / checks:
  - `node scripts/delegation-guard.mjs`
  - `node scripts/spec-guard.mjs --dry-run`
  - `npm run build`
  - `npm run lint`
  - `npm run test`
  - `npm run docs:check`
  - `npm run docs:freshness`
  - `node scripts/diff-budget.mjs`
  - `npm run review`
  - `npm run pack:smoke` if downstream-facing CLI/package surfaces change
  - local canary artifacts under `out/linear-b8a32c11-cfc6-4556-b783-f59b290fe67d/manual/`
- Rollout verification:
  - Confirm the live version/fork baseline.
  - Confirm the policy/docs/default posture is internally consistent after the update.
  - Confirm canary recommendation for `CO-13` / `CO-14` is explicit and evidence-backed.
- Monitoring / alerts:
  - n/a for this bounded local canary lane.

## Open Questions
- Whether current exec/app-server history items expose enough metadata to justify additive path-aware lineage capture now, or only a truthful defer.

## Approvals
- Reviewer: Self-reviewed against the issue brief before implementation.
- Date: 2026-03-28
