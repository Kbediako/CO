---
id: 20260422-linear-20102af9-9eb4-44d1-8988-c5de7869d990
title: CO-298 stale control-host supervision status truth versus healthy live host truth
status: in_progress
relates_to: docs/PRD-linear-20102af9-9eb4-44d1-8988-c5de7869d990.md
risk: high
owners:
  - Codex
last_review: 2026-06-17
review_notes:
  - 2026-06-17: CO-579 pre-expiry review kept this spec active-current; no verified terminal/archive evidence was established in this stream, CO-579 is the live non-terminal docs-freshness owner, and docs/spec gates remain unchanged.
  - 2026-05-18: CO-522 active-spec audit found 8 unchecked task checklist items, so this spec remains active and was reviewed for current lifecycle ownership rather than archived. Evidence: `out/linear-b642e879-ba50-45ef-b0d9-b059afa9e932-recovery/spec-preexpiry-local-classification.json`.
---

## Canonical Reference
- Canonical TECH_SPEC: `tasks/specs/linear-20102af9-9eb4-44d1-8988-c5de7869d990.md`
- PRD: `docs/PRD-linear-20102af9-9eb4-44d1-8988-c5de7869d990.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-20102af9-9eb4-44d1-8988-c5de7869d990.md`
- Task checklist: `tasks/tasks-linear-20102af9-9eb4-44d1-8988-c5de7869d990.md`
- Agent-task mirror: `.agent/task/linear-20102af9-9eb4-44d1-8988-c5de7869d990.md`

## Traceability
- Linear issue: `CO-298` / `20102af9-9eb4-44d1-8988-c5de7869d990`
- Source anchor: `ctx:sha256:c525de17de4f7d9a3e2563fe0bc9102be9bfa86b386bf459bd96dae6990b5dc5#chunk:c000001`
- Docs packet child lane manifest: `.runs/linear-20102af9-9eb4-44d1-8988-c5de7869d990-docs-packet/cli/2026-04-21T21-55-47-184Z-46412546/manifest.json`
- Shared source note: the child checkout has no `.runs` tree, so this packet is anchored on the parent-provided live issue wording plus the current repo source audit only.

## Summary
- Objective: give the parent lane a bounded contract for stale `control-host supervise status --format json` launchd/state truth versus healthy live host truth on `co-status --format json` and `provider-intake-state.json`.
- Scope:
  - docs-first packet and registry/checklist mirrors for `CO-298`
  - stale supervision-state versus live-host truth only
  - current source audit of supervision status rendering and live-health probing
- Constraints:
  - child lane remains docs-only
  - parent owns implementation, focused tests, docs-review, validation, Linear/workpad reconciliation, PR lifecycle, and merge
  - no broad launchd or provider workflow redesign

## Issue-Shaping Contract
- User-request translation carried forward: preserve `control-host supervise status --format json`, `service.loaded`, `status=restart_required`, `last_health_status=probe_timeout`, `co-status --format json`, and `provider-intake-state.json`; keep the fix bounded to stale launchd/state truth versus healthy live host truth.
- Protected terms / exact artifact and surface names:
  - `control-host supervise status --format json`
  - `service.loaded`
  - `status=restart_required`
  - `last_health_status=probe_timeout`
  - `co-status --format json`
  - `provider-intake-state.json`
  - `controlHostSupervisionCliShell.ts`
  - `controlHostSupervision.ts`
  - `ControlHostSupervision.test.ts`
  - `providerControlHostFreshnessGauge.ts`
- Nearby wrong interpretations to reject:
  - state-file deletion or manual cleanup is the fix
  - `service.loaded` alone is the current live-health verdict
  - stale persisted `restart_required` or `probe_timeout` should always dominate newer live host truth
  - healthy live host truth means suppressing genuine current unhealthy states
  - this is a broad launchd or provider scheduling lane
- Explicit non-goals carried forward:
  - no launchd command-family redesign
  - no attach recovery or direct JSON dashboard-fetch redesign
  - no provider scheduling or intake policy redesign
  - no code or test edits in this child lane

## Parity / Alignment Matrix
- Current truth:
  - `printControlHostSupervisionStatus()` reads `launchctl print`, stored supervision `state.json`, and the LaunchAgent plist
  - `buildControlHostSupervisionStatusPayload()` sets `service.loaded` from the `launchctl` exit code
  - `formatControlHostSupervisionStatus()` prints persisted `state.status` and `state.last_health_status`
  - `runControlHostSupervision()` persists `status: 'restart_required'` and `last_health_status: probe.reason`
  - `probeControlHostHealth()` separately probes live health through `co-status --format json`
  - `providerControlHostFreshnessGauge.ts` separately discovers `provider-intake-state.json`
- Reference truth:
  - launchd truth, persisted supervision-state truth, and live host truth are related but not identical
  - persisted residue should stay auditable without silently becoming the current live-health verdict
  - genuine current `restart_required` / `probe_timeout` still must surface
- Target truth / intended delta:
  - parent implementation distinguishes stale persisted residue from current healthy live host truth
  - `service.loaded` remains explicit launchd evidence, not the whole health verdict
  - live `co-status --format json` / `provider-intake-state.json` truth stays visible when newer than stale supervision state
- Explicitly out-of-scope differences:
  - launchd install/uninstall/restart ergonomics
  - dashboard attach recovery
  - provider scheduling, intake-admission, or issue workflow policy

## Readiness Gate
- Not done if:
  - `control-host supervise status --format json` still cannot distinguish stale persisted residue from healthy current live host truth
  - `service.loaded` is still treated as the whole health decision
  - stale `status=restart_required` or `last_health_status=probe_timeout` still override newer healthy live truth with no explicit classification
  - the implementation hides genuine current `restart_required` or `probe_timeout`
- Pre-implementation issue-quality review evidence:
  - 2026-04-22: current source audit confirms the core seam is status-surface composition, not launchd lifecycle or provider scheduling policy.
  - 2026-04-22: the micro-task path is ineligible because correctness depends on exact protected wording and on maintaining a three-surface contract between launchd truth, persisted state truth, and live host truth.
- Safeguard ownership split:
  - child lane owns only the packet files and registry mirrors
  - parent lane owns source/test inspection, implementation, docs-review, focused validation, Linear/workpad reconciliation, PR lifecycle, and merge

## Technical Requirements
1. Create the docs-first packet and registry/checklist mirrors for `CO-298`.
2. Preserve the exact surfaces `control-host supervise status --format json`, `service.loaded`, `status=restart_required`, `last_health_status=probe_timeout`, `co-status --format json`, and `provider-intake-state.json`.
3. Record the current source audit showing `status` rendering is built from stored state plus `launchctl`, not a live `co-status --format json` probe.
4. Keep the repair bounded to stale supervision-state truth versus live host truth.
5. Preserve genuine current `restart_required` and `probe_timeout` outcomes as machine-checkable unhealthy truth.

## Architecture & Data
- Architecture / design adjustments:
  - the likely seam is inside `controlHostSupervisionCliShell.ts`, where status composition currently stops at persisted state and launchd truth
  - any repair should preserve historical supervision-state evidence while making current live truth explicit
  - the smallest correct fix is a bounded reconciliation or classification contract, not a launchd redesign
- Expected implementation surfaces:
  - `orchestrator/src/cli/controlHostSupervisionCliShell.ts`
  - `orchestrator/src/cli/control/controlHostSupervision.ts`
  - `orchestrator/src/cli/control/providerControlHostFreshnessGauge.ts`
- Data model / artifact constraints:
  - preserve persisted supervision state as audit evidence
  - preserve `provider-intake-state.json` as a separate live-host artifact surface
  - additive classification or current-live-truth fields are acceptable; destructive cleanup is out of scope

## Validation Plan
- Child-lane checks:
  - `python3 - <<'PY'\nimport json, pathlib\njson.loads(pathlib.Path('tasks/index.json').read_text())\nPY`
  - `rg -n "control-host supervise status --format json|service.loaded|status=restart_required|last_health_status=probe_timeout|co-status --format json|provider-intake-state.json|stale launchd/state truth|healthy live host truth|controlHostSupervisionCliShell.ts|controlHostSupervision.ts|ControlHostSupervision.test.ts|providerControlHostFreshnessGauge.ts" docs/PRD-linear-20102af9-9eb4-44d1-8988-c5de7869d990.md tasks/specs/linear-20102af9-9eb4-44d1-8988-c5de7869d990.md docs/ACTION_PLAN-linear-20102af9-9eb4-44d1-8988-c5de7869d990.md tasks/tasks-linear-20102af9-9eb4-44d1-8988-c5de7869d990.md .agent/task/linear-20102af9-9eb4-44d1-8988-c5de7869d990.md`
  - `git diff --check -- docs/PRD-linear-20102af9-9eb4-44d1-8988-c5de7869d990.md tasks/specs/linear-20102af9-9eb4-44d1-8988-c5de7869d990.md docs/ACTION_PLAN-linear-20102af9-9eb4-44d1-8988-c5de7869d990.md tasks/tasks-linear-20102af9-9eb4-44d1-8988-c5de7869d990.md .agent/task/linear-20102af9-9eb4-44d1-8988-c5de7869d990.md tasks/index.json docs/TASKS.md docs/docs-freshness-registry.json`
- Parent-lane checks:
  - focused `orchestrator/tests/ControlHostSupervision.test.ts` coverage for stale persisted state versus current live-health truth
  - parent-owned evidence comparing `control-host supervise status --format json`, `co-status --format json`, and `provider-intake-state.json`
  - parent docs-review before implementation
- Rollback plan:
  - revert any repair that suppresses genuine current unhealthy truth, broadens into launchd redesign, or discards persisted supervision-state evidence

## Open Questions
- Should the parent add a bounded live `co-status --format json` check inside `control-host supervise status --format json`, or keep the command offline and expose a stale-state classification instead?
- Should `provider-intake-state.json` participate directly in current-live-truth classification, or only remain corroborating evidence for the same incident?
- What is the smallest additive output shape that preserves historical `status=restart_required` / `last_health_status=probe_timeout` while making current healthy live host truth explicit?

## Approvals
- Reviewer: docs child lane self-review for packet shape and issue boundaries.
- Date: 2026-04-22
