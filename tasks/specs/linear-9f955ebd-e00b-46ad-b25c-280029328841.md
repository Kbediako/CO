---
id: 20260428-linear-9f955ebd-e00b-46ad-b25c-280029328841
title: CO-417 recover stale provider-worker proof locks during active appserver runs
relates_to: docs/PRD-linear-9f955ebd-e00b-46ad-b25c-280029328841.md
risk: high
owners:
  - Codex
last_review: 2026-04-28
---

## Added by Provider Worker 2026-04-28

## Summary
- Objective: Keep active appserver provider-worker proof/read-model updates from freezing behind stale `provider-linear-worker-proof.json.lock` files while preserving lock safety.
- Scope: Provider proof lock acquisition, lock metadata/diagnostics, stale recovery or fail-closed classification, live proof update persistence, focused tests, and CO-415/CO-400-style replay/fixture evidence.
- Constraints: Preserve visible proof persistence failures, avoid manual lock deletion as a fix, preserve concurrent-writer safety, and keep unrelated provider-intake/control-host UI timeout work out of scope.

## Issue-Shaping Contract
- User-request translation carried forward: A live appserver worker can continue heartbeating while proof writes freeze behind a stale proof lock. The fix must make that lock lifecycle recoverable or explicitly classified without hiding the error or weakening provider proof requirements.
- Protected terms / exact artifact and surface names: `provider-linear-worker-proof.json`, `provider-linear-worker-proof.json.lock`, `Failed to acquire provider-linear-worker proof lock after 50 attempts`, appserver provider worker, manifest heartbeat, CO-415, CO-403, CO-400, stale lock, live proof update.
- Nearby wrong interpretations to reject: Manual lock deletion; treating manifest heartbeat as sufficient read-model progress; reopening CO-403 without source/replay evidence; suppressing stderr warnings without a replacement diagnostic; broad control-host/provider-intake redesign.
- Explicit non-goals carried forward: No weakening proof/read-model requirements, no best-effort silent proof writes, no removal of lock protection, no unrelated UI timeout or provider-intake freshness work.

## Parity / Alignment Matrix
- Current truth: Active appserver workers can emit fresh manifest heartbeats while `provider-linear-worker-proof.json` and its `.lock` stop updating; the lock payload can be only an opaque token; live proof updates then fail after repeated acquisition attempts.
- Reference truth: CO-403 repaired a related stale lock/retry path with stale timeout and keepalive, but this active-run recurrence still needs current proof-update lifecycle coverage.
- Target truth / intended delta: Proof locks include owner/age diagnostics or equivalent metadata; stale locks are recovered only when safe or fail closed with an actionable classification; live proof updates advance again after recovery/classification; concurrent active writers remain protected.
- Explicitly out-of-scope differences: Child-lane retry lineage recovery from CO-403, CO-400 current-state authority consolidation, queue capacity accounting, control-host UI timeout behavior, and manual artifact cleanup.

## Readiness Gate
- Not done if: Active appserver workers can still freeze proof behind a stale lock while manifest heartbeats continue; lock diagnostics remain opaque; recurrence requires manual cleanup; repeated stderr failures lack an actionable owner.
- Pre-implementation issue-quality review evidence: The issue is not a generic lock cleanup and not a CO-403 reopen by default. It specifically targets the active appserver live-proof update path after CO-403 landed, with CO-415 fresh evidence and CO-400 recurrence evidence.
- Safeguard ownership split: Parent owns docs, implementation, Linear state, PR lifecycle, and validation. Same-issue child lane `proof-lock-regression-tests` owns only delegated test files until accepted, rejected, or invalidated.

## Technical Requirements
- Functional requirements:
  - Reproduce or fixture a stale proof-lock shape where manifest `heartbeat_at` remains fresh while `provider-linear-worker-proof.json` updates fail after repeated lock attempts.
  - Extend `provider-linear-worker-proof.json.lock` payloads or acquisition diagnostics with owner/age metadata sufficient for safe classification and operator diagnosis.
  - Configure the active provider proof write path to recover stale locks or fail closed with a precise classification when recovery is unsafe.
  - Preserve live-lock keepalive or equivalent freshness for active writers.
  - Keep proof write failures visible in stderr/logs unless recovery succeeds, and replace spam with an actionable diagnostic when recovery is refused.
  - Add regression coverage for proof updates after stale recovery/classification and for active writer preservation.
- Non-functional requirements:
  - Conservative recovery only after stale/owner evidence is sufficient.
  - Deterministic tests without requiring live Linear mutation.
  - No increased Linear/GitHub polling pressure.
  - No corruption of `provider-linear-worker-proof.json` under concurrent write attempts.
- Interfaces / contracts:
  - `orchestrator/src/persistence/lockFile.ts`: lock acquisition, lock payload, stale cleanup, keepalive, and diagnostic error construction.
  - `orchestrator/src/cli/providerLinearWorkerRunner.ts`: `withProviderLinearWorkerProofLock`, `PROVIDER_LINEAR_WORKER_PROOF_LOCK_RETRY`, live proof update warning path, and proof snapshot refresh.
  - `orchestrator/tests/LockFile.test.ts` and `orchestrator/tests/ProviderLinearWorkerRunner.test.ts`: focused lock lifecycle and proof update regressions.

## Fallback Expiry / Refactor Decision
- Applies to fallback, compatibility, legacy, stale, cached, break-glass, or minor-seam behavior? Yes.
- Required decision table:

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `withProviderLinearWorkerProofLock` / `PROVIDER_LINEAR_WORKER_PROOF_LOCK_RETRY` | Active live proof updates can use a retry policy without stale recovery/diagnostic owner evidence. | remove fallback | CO-417 | Live appserver proof write encounters an existing proof lock. | 2026-04-28 | 2026-04-28 | Immediate removal in CO-417 | Active proof write path uses stale-aware owner diagnostics or fails closed with classification. | Provider worker proof update regression for stale lock shape. |
| `orchestrator/src/persistence/lockFile.ts` | Opaque lock token makes stale owner diagnosis difficult. | justify retaining fallback | CO-417 | Any lock file is acquired or inspected. | 2026-04-28 | 2026-04-28 | Durable lock safety contract | Not removed; metadata-backed lock ownership remains part of the lock contract. | Lock file tests for legacy token compatibility, owner metadata, stale recovery, and live writer preservation. |
| Runner stderr warning path | Repeated acquisition failures can spam without actionable stale-lock owner classification. | remove fallback | CO-417 | Proof update cannot acquire lock after configured attempts. | 2026-04-28 | 2026-04-28 | Immediate removal in CO-417 | Warning includes actionable stale-lock classification or recovery succeeds. | Focused runner test asserting classified diagnostic. |

- For `justify retaining fallback`, also record contract name, owning surface, steady-state proof, tests/docs, and why it is not governed as an expiring fallback: Contract name `provider proof lock ownership diagnostics`; owning surface `lockFile.ts`; steady-state proof is metadata-backed exclusive lock acquisition, stale recovery only after owner/age evidence, live keepalive freshness, and concurrent writer tests. This is durable correctness behavior, not a temporary compatibility bridge.
- Large-refactor check: A large lock subsystem refactor is deferred. The existing `acquireLockWithRetry` seam is acceptable if it can support owner metadata, legacy token compatibility, stale cleanup, and actionable errors without broad caller rewrites.

## Architecture & Data
- Architecture / design adjustments: Keep lock acquisition centralized in `lockFile.ts`; add metadata parsing/writing and stale diagnostic helpers there, then consume the same retry contract from provider proof writes.
- Data model changes / migrations: New lock payloads may be JSON metadata, but legacy token-only lock files must remain parseable as opaque legacy owners for stale recovery and diagnostics.
- External dependencies / integrations: None beyond existing filesystem, manifest, and provider proof artifacts.

## Validation Plan
- Tests / checks:
  - Focused lock tests for metadata payload, legacy token compatibility, stale recovery, acquisition lock behavior, and active writer preservation.
  - Focused provider-worker regression for CO-415-style live proof update after stale lock recovery/classification.
  - Replay/fixture assertion that manifest heartbeat freshness does not mask stale proof updates.
  - Required gates: delegation guard, spec guard, build, lint, test, docs checks, docs freshness, repo stewardship, diff budget, standalone review, elegance pass, and pack smoke if downstream-facing CLI/package surfaces are touched.
- Rollout verification: Workpad records focused test output and any CO-415/CO-400 artifact replay proof before PR handoff.
- Monitoring / alerts: Runner warnings remain visible and classified; proof/read-model freshness becomes inspectable from sidecar timestamps and lock diagnostics.

## Open Questions
- Should lock acquisition errors expose structured stale-owner details directly in the thrown error, or should provider proof writes inspect a helper result after acquisition failure?
- Should stale-aware recovery be limited to provider proof locks, or can the shared lock helper safely support metadata for all current lock callers while preserving compatibility?

## Approvals
- Reviewer: Pending docs-review and implementation review.
- Date: 2026-04-28
