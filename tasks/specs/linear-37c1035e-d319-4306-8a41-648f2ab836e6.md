---
id: 20260423-linear-37c1035e-d319-4306-8a41-648f2ab836e6
title: CO-328 accepted child-lane workspace artifacts and diff-budget scope
status: done
owner: Codex
created: 2026-04-23
last_review: 2026-05-18
review_cadence_days: 30
risk_level: medium
related_prd: docs/PRD-linear-37c1035e-d319-4306-8a41-648f2ab836e6.md
related_action_plan: docs/ACTION_PLAN-linear-37c1035e-d319-4306-8a41-648f2ab836e6.md
review_notes:
  - 2026-04-23: Opened from Linear issue CO-328 with source anchor `ctx:sha256:a682cb36f3caccc0c778bb0b07906789fb8107c421f1e7f541ce1d6aef0ba9a4#chunk:c000001`.
  - 2026-04-23: Pre-implementation issue-quality review confirms the issue is not a request to disable diff-budget or ignore all untracked files. Accepted scope is accepted same-issue child-lane `.child-lanes/` workspace copies after durable patch/ledger evidence exists.
  - 2026-05-18: CO-522 spec lifecycle audit found the linked task checklist has zero unchecked items (23 checked), so this spec is terminal and eligible for implementation-docs archive. Evidence: `out/linear-b642e879-ba50-45ef-b0d9-b059afa9e932-recovery/spec-preexpiry-local-classification.json`.
---

# Technical Specification

## Canonical Reference
- Canonical TECH_SPEC: `tasks/specs/linear-37c1035e-d319-4306-8a41-648f2ab836e6.md`
- TECH_SPEC mirror: `docs/TECH_SPEC-linear-37c1035e-d319-4306-8a41-648f2ab836e6.md`
- PRD: `docs/PRD-linear-37c1035e-d319-4306-8a41-648f2ab836e6.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-37c1035e-d319-4306-8a41-648f2ab836e6.md`
- Task checklist: `tasks/tasks-linear-37c1035e-d319-4306-8a41-648f2ab836e6.md`

## Summary
- Objective: exclude accepted same-issue child-lane workspace artifacts from `diff-budget` untracked accounting only after durable provider evidence exists.
- Scope:
  - `scripts/diff-budget.mjs` accepted child-lane artifact predicate
  - focused regressions in `tests/diff-budget.spec.ts`
  - provider-worker lifecycle guidance in this packet
- Constraints:
  - keep normal untracked files counted
  - keep pending or unaccepted `.child-lanes/` output counted
  - preserve hard `diff-budget` behavior

## Issue-Shaping Contract
- User-request translation carried forward: accepted `.child-lanes/` workspace copies are ephemeral local provider artifacts after parent acceptance and durable run evidence exists, but they currently inflate `diff-budget`.
- Protected terms / exact artifact and surface names: `diff-budget`, `.child-lanes`, accepted same-issue child-lane, `provider-linear-child-lane.patch`, acceptance ledger, untracked budget surface.
- Nearby wrong interpretations to reject:
  - ignore all untracked files
  - ignore `.child-lanes/` without acceptance evidence
  - weaken `diff-budget` for source/doc/test changes
  - rely on manual workspace pruning as the permanent fix
- Explicit non-goals carried forward:
  - no diff-budget disablement
  - no broad child-lane lifecycle redesign
  - no unrelated provider-worker scheduling or admission change

## Parity / Alignment Matrix
- Current truth: `diff-budget` consumes `git ls-files --others --exclude-standard`, and the ignore list omits `.child-lanes/`.
- Reference truth: durable accepted child-lane evidence is under `.runs/.../provider-linear-child-lane.patch` plus proof/ledger metadata, while the `.child-lanes/` checkout is a local workspace copy.
- Target truth / intended delta: ignore `.child-lanes/<workspace>/...` only when durable evidence proves that workspace/stream was accepted; keep all other untracked files counted.
- Explicitly out-of-scope differences: pending child-lane workspaces, unrelated `.child-lanes/` scratch directories without evidence, and normal untracked files.

## Readiness Gate
- Not done if:
  - accepted child-lane workspace copies can still fail `diff-budget` after durable evidence exists
  - pending or unaccepted child-lane workspace copies are ignored
  - unrelated untracked files are ignored
- Pre-implementation issue-quality review evidence: parent provider-worker reviewed the issue body on 2026-04-23 and preserved the protected terms, CO-321 incident evidence, and non-goals in this spec.
- Safeguard ownership split: parent owns docs, script, Linear/workpad, PR lifecycle, and final validation; same-issue child lane owns only focused `tests/diff-budget.spec.ts` regression drafting.

## Technical Requirements
1. Add a narrow accepted-child-lane artifact predicate to `scripts/diff-budget.mjs`.
2. The predicate must recognize `.child-lanes/<workspace>/...` paths only when durable evidence under `.runs/**` proves the child lane was accepted.
3. Durable evidence must include `provider-linear-child-lane.patch` and an acceptance/proof/ledger signal; a patch artifact alone is insufficient for pending output.
4. The predicate must fail closed: malformed JSON, missing artifacts, missing workspace/stream linkage, or non-accepted dispositions keep the untracked files counted.
5. Add regression coverage showing accepted child-lane workspace files are excluded.
6. Add regression coverage showing unrelated untracked files remain counted.
7. Add regression coverage showing `.child-lanes/` without durable accepted evidence remains counted.
8. Record provider-worker lifecycle guidance: `.child-lanes/` is ephemeral local workspace state; `.runs/.../provider-linear-child-lane.patch` plus proof/ledger is durable review evidence.

## Architecture & Data
- `scripts/diff-budget.mjs` already funnels ignored-path decisions through `isIgnoredPath(filePath)`.
- The new predicate can remain script-local and synchronous if it scans a small evidence set under `.runs/`.
- Evidence matching should prefer explicit `workspace_path`, `workspace`, `stream`, `disposition`, `decision`, `status`, or equivalent fields when present, and keep the rule conservative.

## Validation Plan
- Focused:
  - `npx vitest run tests/diff-budget.spec.ts`
- Required floor before handoff:
  - `node scripts/delegation-guard.mjs`
  - `node scripts/spec-guard.mjs --dry-run`
  - `npm run build`
  - `npm run lint`
  - `npm run test`
  - `npm run docs:check`
  - `npm run docs:freshness`
  - `npm run repo:stewardship`
  - `node scripts/diff-budget.mjs`
  - manifest-backed standalone review
  - explicit elegance review

## Open Questions
- Exact live proof/ledger schema should be verified against current child-lane artifacts before finalizing the runtime predicate.

## Approvals
- Reviewer: parent provider-worker.
- Date: 2026-04-23.
