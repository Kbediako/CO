---
id: 20260422-linear-59a75110-93e5-42f4-a2d2-31dcde884c91
title: CO-299 local Node OOM backpressure and provenance-safe child-lane progress truth
status: in_progress
relates_to: docs/PRD-linear-59a75110-93e5-42f4-a2d2-31dcde884c91.md
risk: high
owners:
  - Codex
last_review: 2026-06-17
review_notes:
  - 2026-06-17: CO-579 pre-expiry review kept this spec active-current; no verified terminal/archive evidence was established in this stream, CO-579 is the live non-terminal docs-freshness owner, and docs/spec gates remain unchanged.
  - 2026-05-18: CO-522 active-spec audit found 11 unchecked task checklist items, so this spec remains active and was reviewed for current lifecycle ownership rather than archived. Evidence: `out/linear-b642e879-ba50-45ef-b0d9-b059afa9e932-recovery/spec-preexpiry-local-classification.json`.
---

## Added by Bootstrap (refresh as needed)
- TECH_SPEC mirror: `docs/TECH_SPEC-linear-59a75110-93e5-42f4-a2d2-31dcde884c91.md`
- PRD: `docs/PRD-linear-59a75110-93e5-42f4-a2d2-31dcde884c91.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-59a75110-93e5-42f4-a2d2-31dcde884c91.md`
- Task checklist: `tasks/tasks-linear-59a75110-93e5-42f4-a2d2-31dcde884c91.md`
- `.agent` mirror: `.agent/task/linear-59a75110-93e5-42f4-a2d2-31dcde884c91.md`

## Summary
- Objective: define the parent-owned fix for local Node OOM/resource pressure under concurrent provider-worker launches while preserving fail-closed child-lane provenance and truthful retry/progress accounting.
- Scope:
  - docs-first packet and registry/checklist mirrors for CO-299
  - parent-owned host-safety backpressure and observability work
  - parent-owned provenance-invalid retry/progress truth work
- Constraints:
  - preserve source anchor `ctx:sha256:0320d3a6b57e0f8847509632a1b1cf89ffd868f1412cbb569e67ac7eb380f394#chunk:c000001`
  - delegated child lanes own only their declared scopes
  - no provenance bypass, cap-only increase, hidden retry suppression, or source/test/package edits in docs lanes

## Issue-Shaping Contract
- User-request translation carried forward: CO-299 is a local-host safety and progress-truth issue under provider-worker concurrency pressure. It is not a request to weaken provenance validation; provenance-invalid child-lane and child-stream failures must remain fail-closed while retry/progress surfaces stop presenting failed launch attempts as work progress.
- Protected terms / exact artifact and surface names:
  - `local Node OOM`
  - `concurrent provider-worker pressure`
  - `provenance-invalid child-lane retry/progress churn`
  - `host-safety backpressure/observability`
  - `fail-closed provenance`
  - `provider-worker`
  - `provider-linear-worker`
  - `linear child-lane`
  - `linear child-stream`
  - `provider_worker_child_lane_provenance_invalid`
  - `provider_worker_child_stream_provenance_invalid`
  - `manifest.json`
  - `provider-linear-worker-proof.json`
  - `provider-intake-state.json`
  - `co-status --format json`
  - `CO STATUS`
  - `max_concurrent_agents`
- Nearby wrong interpretations to reject:
  - bypass child-lane provenance checks
  - treat local Node OOM as ordinary issue-specific failure
  - solve by increasing `max_concurrent_agents`
  - hide provenance-invalid output
  - count failed child-lane launch attempts as progress
  - fold this lane into unrelated provider-worker bootstrap, stale-host, or attach-before-capacity work
- Explicit non-goals carried forward:
  - no Linear/GitHub/workpad mutation in delegated child lanes
  - no source/test/package edits in docs child lanes
  - no broad scheduler redesign
  - no cloud-mode redesign

## Parity / Alignment Matrix

| Current truth | Reference truth | Target truth / intended delta | Explicitly out-of-scope differences |
| --- | --- | --- | --- |
| Local Node OOM/resource pressure can occur under concurrent provider-worker load. | Provider-worker admission should protect the local control host from known resource pressure. | Parent implementation classifies OOM/resource pressure and activates observable host-safety backpressure before adding more local launches. | No hardware capacity tuning, cloud-mode redesign, or cap-only increase. |
| Provenance-invalid child-lane/child-stream output is a fail-closed safety signal. | Missing or mismatched provider control-host provenance must remain terminal for the attempted child launch. | Preserve fail-closed behavior while ensuring progress/retry summaries identify the attempt as blocked/failed before work. | No env-only override or issue-id-only trust rule. |
| Retry/progress churn can make repeated failed child-lane attempts look like progress. | Operator progress should track real work separately from attempted launches and terminal guard failures. | Parent implementation separates attempted launch, provenance-invalid failure, host-pressure pause, and actual progress states. | Do not hide attempts or erase historical manifests/proofs. |
| Operator observability may not connect OOM pressure, backpressure, and provenance-invalid failures. | `manifest.json`, `provider-linear-worker-proof.json`, `provider-intake-state.json`, `co-status --format json`, and `CO STATUS` should be audit surfaces. | Surface host-safety and provenance decisions in machine-checkable output. | No child-lane Linear state mutation. |

## Readiness Gate
- Not done if:
  - local Node OOM under concurrent provider-worker pressure does not trigger observable host-safety backpressure
  - provenance-invalid child-lane or child-stream failures are retried or summarized as real issue progress
  - fail-closed provenance behavior is weakened
  - parent implementation fixes only display wording while launch/admission pressure is unchanged
  - operator evidence does not identify the host-safety or provenance reason
- Pre-implementation issue-quality review evidence:
  - 2026-04-22: this packet carries the source anchor, protected terms, Not Done If clauses, parity matrix, and explicit non-goals. The issue is broader than a log-message cleanup because it requires host-safety backpressure, and narrower than scheduler redesign because strict provenance and existing local provider-worker surfaces remain authoritative.
  - The packet is anchored on the parent-provided source anchor and prompt framing. The first docs-reset child lane was invalidated for host-safety recovery, and the parent imported only the docs patch shape before completing a successful zero-byte docs-shape advisory child lane.
- Safeguard ownership split:
  - docs child lanes: docs packet, checklist, registry mirrors only
  - parent lane: implementation, tests, validation, Linear/workpad reconciliation, PR lifecycle, and acceptance

## Technical Requirements
- Functional requirements:
  1. Classify local Node OOM/resource-exhaustion signals under provider-worker concurrency as host-safety pressure.
  2. Apply provider-worker launch/admission backpressure when host-safety pressure is active.
  3. Preserve fail-closed `linear child-lane` and `linear child-stream` provenance validation for missing or mismatched provider control-host provenance.
  4. Prevent provenance-invalid child-lane retry/progress churn from being reported as real issue progress.
  5. Surface host-safety pressure, backpressure state, provenance-invalid terminal failures, and actual progress as distinct machine-checkable states.
  6. Keep `max_concurrent_agents` authoritative and unchanged unless the parent explicitly records a separate cap-policy issue.
- Non-functional requirements:
  - preserve auditability of `manifest.json` and `provider-linear-worker-proof.json`
  - no diagnostic muting
  - no broad provider scheduler rewrite
  - clear operator-facing remediation for local host pressure versus provenance mismatch
- Interfaces / contracts:
  - provider-worker launch/admission path
  - `linear child-lane`
  - `linear child-stream`
  - `manifest.json`
  - `provider-linear-worker-proof.json`
  - `provider-intake-state.json`
  - `co-status --format json`
  - `CO STATUS`
  - `max_concurrent_agents`

## Architecture & Data
- Architecture / design adjustments:
  - add or reuse a narrow host-safety pressure classification for local Node OOM/resource exhaustion
  - gate additional local provider-worker launches when that classification is active
  - route provenance-invalid child-lane results into retry/progress accounting as terminal pre-work failures
  - project distinct pressure/provenance states into operator evidence surfaces
- Data model changes / migrations:
  - no migration required for this docs packet
  - parent should prefer additive diagnostic/backpressure fields if existing proof/status fields cannot express the distinction
- External dependencies / integrations:
  - Linear integration remains parent-owned
  - GitHub/PR lifecycle remains parent-owned

## Validation Plan
- Tests / checks:
  - protected-term grep over PRD, TECH_SPEC, ACTION_PLAN, checklist, and `.agent` mirror
  - JSON parse for `tasks/index.json` and `docs/docs-freshness-registry.json`
  - `git diff --check` over the declared docs scope
  - parent-owned focused regression for local Node OOM/resource-pressure classification
  - parent-owned focused regression for launch/admission backpressure under pressure
  - parent-owned focused regression proving provenance-invalid child-lane/child-stream attempts remain fail-closed and do not count as progress
  - parent-owned valid-provenance regression proving normal child-lane/child-stream behavior still works
- Rollout verification:
  - parent records manifest/proof/status evidence showing host-safety backpressure and provenance decisions
- Monitoring / alerts:
  - `co-status --format json` / `CO STATUS` should expose enough reason data for operators to distinguish host pressure from provenance mismatch

## Open Questions
- Which OOM signatures are canonical enough for parent classification: Node stderr, process signal, exit code, proof diagnosis, manifest boundary, or a combination?
- Should backpressure live in provider-intake state, launch budget calculation, control-host health, or all three?
- What attempt budget or quiet window prevents provenance-invalid retry/progress churn without hiding the first failure?

## Approvals
- Reviewer: pending parent docs-review / implementation review
- Date: 2026-04-22
