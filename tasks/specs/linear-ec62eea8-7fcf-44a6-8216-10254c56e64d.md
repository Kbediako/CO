---
id: 20260416-linear-ec62eea8-7fcf-44a6-8216-10254c56e64d
title: CO: terminate hanging run-review mock children after timeout
status: done
relates_to: docs/PRD-linear-ec62eea8-7fcf-44a6-8216-10254c56e64d.md
risk: high
owners:
  - Codex
last_review: 2026-05-17
review_notes:
  - 2026-05-17: CO-543 strict spec-guard audit reclassified this stale Apr 16 row inactive done; live Linear evidence verified CO-205 is Done/completed. No completed_at was inferred or fabricated.
---

## CO-382 Fallback Decision Table
- Large-refactor check: no new fallback mechanism or guard split is warranted; CO-543 removes lifecycle metadata drift while keeping the existing strict spec-guard and docs-freshness ownership surfaces authoritative.
- Minor-seam decision: the bounded seam is acceptable because completed packet rows are reclassified inactive or archived with evidence, and future active specs surface through the existing docs-freshness-maintain owner-action route before hard expiry.

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Active spec lifecycle freshness | Completed task/spec packet rows remained active and recurred as stale spec-guard debt | remove fallback | CO-543 | Apr 16 terminal packet rows reached the 30-day strict spec-guard boundary | 2026-04-16 | 2026-05-17 | N/A after removal | Completed rows are inactive done; related terminal packet registry rows are archived; future active specs surface pre-expiry owner action | node scripts/spec-guard.mjs; npm run docs:check; focused docs-freshness/spec-guard tests; diff-budget override |

## Canonical Reference
- Canonical TECH_SPEC: `tasks/specs/linear-ec62eea8-7fcf-44a6-8216-10254c56e64d.md`
- PRD: `docs/PRD-linear-ec62eea8-7fcf-44a6-8216-10254c56e64d.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-ec62eea8-7fcf-44a6-8216-10254c56e64d.md`
- Task checklist: `tasks/tasks-linear-ec62eea8-7fcf-44a6-8216-10254c56e64d.md`

## Traceability
- Linear issue: `CO-205` / `ec62eea8-7fcf-44a6-8216-10254c56e64d`
- Linear URL: https://linear.app/asabeko/issue/CO-205
- Source anchor: `ctx:sha256:3b4ea97b5bcc16b71fdd2942cb023f0d4031067c77a83fb0c0c36ff65ba5d8c4#chunk:c000001`
- Docs packet child lane: `.runs/linear-ec62eea8-7fcf-44a6-8216-10254c56e64d-docs-packet/cli/2026-04-16T10-20-19-149Z-7914bf20/manifest.json`

## Summary
- Objective: prevent hanging `run-review` mock processes from surviving timeout cleanup as orphaned PPID=1 descendants.
- Scope:
  - review-wrapper timeout cleanup/reaping for test/mock children
  - focused regression coverage for `RUN_REVIEW_MODE=hang`
  - passive review drain/process-health evidence
  - checklist evidence before parent implementation handoff
- Constraints:
  - do not manually kill observed orphans as the solution
  - do not hide leaked processes from status output
  - do not kill real user review processes
  - do not change provider admission, Linear polling, or `CO-189` / `CO-203` / `CO-204` behavior unless reproduction proves a shared cleanup path

## Issue-Shaping Contract
- User-request translation carried forward: fix the review-wrapper mock timeout cleanup path so `RUN_REVIEW_MODE=hang` cannot leave `codex-mock.sh` / `run-review-*` children as orphaned PPID=1 processes, and prove it with focused tests plus a passive process-health scan.
- Protected terms / exact artifact and surface names: `run-review`, `codex-mock.sh`, `RUN_REVIEW_MODE=hang`, orphaned PPID=1 process, review drain/process-health scan, `run-review-*`, `tests/run-review.spec.ts`, review wrapper temp harness.
- Nearby wrong interpretations to reject: live provider-admission bug, Linear polling bug, manual host process cleanup, status-output filtering, broad process supervisor rewrite, and unrelated `CO-189` / `CO-203` / `CO-204` remediation.
- Explicit non-goals carried forward: no manual orphan kill as completion, no provider/Linear scope changes without shared-path proof, no real review process cleanup, no masking of process-health evidence.

## Parity / Alignment Matrix
- Current truth:
  - the issue report names `RUN_REVIEW_MODE=hang` and `codex-mock.sh` as the leak reproduction surface
  - hanging review mocks can become orphaned PPID=1 if timeout cleanup misses descendants
  - process/status scans must remain truthful to leaked process state
- Reference truth:
  - review-wrapper tests are allowed to create temporary mock children
  - real user review processes are outside test/mock cleanup ownership
  - parent/provider issue handling is separate from review-wrapper process cleanup
- Target truth / intended delta:
  - timeout cleanup terminates and reaps only the temp-harness-owned mock child tree
  - focused regression proves the hanging mock path is cleaned up
  - passive process-health scan shows no new orphaned `run-review-*` / `codex-mock.sh` review mock processes
  - real review processes remain visible and untouched
- Explicitly out-of-scope differences:
  - provider admission and Linear polling behavior
  - `CO-189`, `CO-203`, or `CO-204` behavior unless shared cleanup proof exists
  - manual cleanup of current host process state

## Readiness Gate
- Not done if:
  - the leak is not freshly reproduced and no obsolete-path proof is recorded
  - no regression covers hanging review mock timeout cleanup
  - cleanup can target real review processes
  - the fix hides leaked processes from status output
  - the checklist lacks exact commands and evidence paths for focused tests and process-health scan
- Pre-implementation issue-quality review evidence:
  - 2026-04-16: same-issue docs child lane reviewed the launch contract and preserved the protected review-wrapper/mock cleanup scope. The packet explicitly rejects live provider-admission and unrelated CO behavior interpretations.
- Safeguard ownership split:
  - child lane owns only docs/task packet and registry mirror files
  - parent lane owns authoritative issue workspace, implementation, Linear state, workpad, validation, review, PR lifecycle, and patch integration

## Technical Requirements
- Functional requirements:
  - reproduce the `RUN_REVIEW_MODE=hang` cleanup leak or prove the path obsolete on current code
  - identify `run-review` temp harness ownership for mock children
  - terminate and reap harness-owned hanging mock descendants after timeout
  - avoid targeting real review processes outside the temp harness tree
  - keep status/process output truthful
  - record evidence in the task checklist before leaving analysis
- Non-functional requirements:
  - smallest viable change in the review-wrapper cleanup seam
  - no provider/Linear scope change unless shared cleanup evidence is explicit
  - deterministic focused regression without relying on manual host cleanup
  - no secrets or host-specific PIDs persisted as required fixtures
- Interfaces / contracts:
  - `scripts/run-review.ts`
  - existing review launch / process cleanup helpers, if applicable
  - `tests/run-review.spec.ts`
  - review mock fixture behavior for `RUN_REVIEW_MODE=hang` and `codex-mock.sh`

## Architecture & Data
- Architecture / design adjustments:
  - prefer reusing existing child-process cleanup and timeout paths
  - model the cleanup boundary around the temp harness root/process group or explicit child tree ownership
  - keep passive process-health scan outside the cleanup action so it remains an evidence check, not the implementation
- Data model changes / migrations:
  - no schema migration expected
  - no persistent state shape changes expected
  - test fixtures may create temporary directories/processes only
- External dependencies / integrations:
  - no Linear mutation or provider-control dependency
  - local process inspection via `ps`

## Validation Plan
- Tests / checks:
  - focused regression for `RUN_REVIEW_MODE=hang` under `tests/run-review.spec.ts`
  - focused safety check that real review processes outside the temp harness are not killed
  - relevant review-wrapper/run-review test lane
  - `jq empty tasks/index.json docs/docs-freshness-registry.json`
  - passive `ps` review drain/process-health scan for orphaned PPID=1 `run-review-*` / `codex-mock.sh` review processes
- Rollout verification:
  - checklist includes exact commands and evidence paths for reproduction or obsolete-path proof, regression, focused test lane, and passive scan
  - parent review confirms no provider/Linear surfaces were changed without shared-path proof
- Monitoring / alerts:
  - no new alerting required in this slice
  - existing status/process visibility must remain truthful

## Open Questions
- Resolved 2026-04-16: the leak path is still representable through a subprocess harness kill while `RUN_REVIEW_MODE=hang` disables wrapper timeout/stall termination; the regression verifies sandbox mock children are reaped after that path.
- Resolved 2026-04-16: cleanup ownership stays in the test harness, keyed to the exact temporary `codex-mock.sh review` path; production review process signaling already targets detached child groups and is not widened.

## Approvals
- Docs-first packet: same-issue docs child lane `.runs/linear-ec62eea8-7fcf-44a6-8216-10254c56e64d-docs-packet/cli/2026-04-16T10-20-19-149Z-7914bf20/manifest.json`
- Standalone review: forced non-interactive review telemetry `../../.runs/linear-ec62eea8-7fcf-44a6-8216-10254c56e64d/cli/2026-04-16T10-17-05-248Z-09e74e8e/review/telemetry.json` with `status=succeeded` and `review_outcome=bounded-success`
- Elegance pass: completed; no further simplification before PR handoff
- Date: 2026-04-16
