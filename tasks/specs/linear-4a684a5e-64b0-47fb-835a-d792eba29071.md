---
id: 20260424-linear-4a684a5e-64b0-47fb-835a-d792eba29071
title: CO Codex CLI 0.124.0 and GPT-5.5 Posture
status: in_progress
owner: Codex
created: 2026-04-24
last_review: 2026-04-24
review_cadence_days: 30
risk_level: high
related_prd: docs/PRD-linear-4a684a5e-64b0-47fb-835a-d792eba29071.md
related_action_plan: docs/ACTION_PLAN-linear-4a684a5e-64b0-47fb-835a-d792eba29071.md
related_tasks:
  - tasks/tasks-linear-4a684a5e-64b0-47fb-835a-d792eba29071.md
review_notes:
  - 2026-04-24: Issue-quality review approves CO-341 as a posture/alignment lane, not a blind version or model bump. Required evidence includes official sources, local auth, top-level exec, delegated subagent, review model, provider-worker/app-server, cloud blocker/pass, hooks/config hygiene, marketplace command shape, and full validation before handoff.
  - 2026-04-24: Same-issue child lane source-evidence completed successfully but produced a zero-byte patch and was rejected as advisory; parent owns evidence file and implementation.
---

# Technical Specification

## Context
CO policy still names Codex CLI `0.118.0` and `gpt-5.4` as active posture while the local CLI is `codex-cli 0.124.0`, npm latest is `0.124.0`, official `rust-v0.124.0` shipped on April 23, 2026, and local ChatGPT-auth `gpt-5.5` now works with `model_reasoning_effort = "xhigh"`.

## Requirements
1. Capture official `rust-v0.124.0` release and OpenAI docs source evidence with URLs and dates.
2. Capture local `codex --version`, `npm view @openai/codex version`, and `codex login status`.
3. Probe `gpt-5.5` / `xhigh` across top-level `codex exec`, delegated subagent, review model, provider-worker/app-server, provider `codex exec` / `codex exec resume` seam or blocker, and Cloud or blocker.
4. Audit `exec`, `exec resume`, `review`, `login --device-auth`, `app-server`, `mcp`, `plugin`, `features`, and marketplace/plugin command surfaces.
5. Decide hold/promote/no-op buckets for 0.124 deltas and align docs/tests/configuration accordingly.
6. Track or fix local stale config warning keys and `legacy_notify` hook path evidence.
7. Keep CO in-progress count at or below the active control-host cap.
8. Run full validation floor and `pack:smoke` if downstream package/smoke surfaces change.

## Issue-Shaping Contract
Protected terms: Codex CLI `0.124.0`, `rust-v0.124.0`, `gpt-5.5`, `model_reasoning_effort = "xhigh"`, `review_model`, `codex_hooks`, `hooks.json`, `legacy_notify`, `after_agent hook failed`, `remote_connections`, `skills`, `voice_transcription`, `Fast service tier`, `wait_agent`, `app-server multiple environments`, `codex exec`, `codex exec resume`, `codex review`, `provider-linear-worker`, `pack:smoke`, `cloud-canary`, `CODEX_CLOUD_ENV_ID`.

Wrong interpretations rejected: blind promotion, cloud/API proof from ChatGPT-auth local smoke, weakened runtime/cloud canaries, ignoring config/hook warnings, and marketplace changes without command proof.

Explicit non-goals carried forward: no shared-root mutation, no unrelated release recovery, no broad app-server control rewrite, and no user-level config mutation.

## Parity / Alignment Matrix
- Current truth: active docs and setup defaults are `0.118.0` / `gpt-5.4`, with cloud-canary at `0.122.0` and marketplace smoke at `0.121.0`.
- Reference truth: official docs recommend `gpt-5.5` for most Codex tasks when available and `rust-v0.124.0` records stable hook and plugin/runtime changes.
- Target truth: promote only verified local ChatGPT-auth/model and `0.124.0` command/workflow surfaces; hold unsupported Cloud/API model selection with explicit evidence.
- Out-of-scope differences: CO-282/CO-278 dirty root changes, CO-337/CO-340 stale issue cleanup except superseding notes, and CO-338 publishing recovery.

## Readiness Gate
- Not done if: any required surface lacks pass/blocker evidence, active docs disagree, or warning/hook drift is not tracked.
- Pre-implementation issue-quality review evidence: PRD, TECH_SPEC, ACTION_PLAN, task spec, task checklist, and `.agent` mirror preserve the full issue scope before implementation.
- Safeguard ownership split: parent owns all implementation after the no-output advisory child lane.

## Technical Requirements
- Functional requirements: update posture docs, defaults, tests, workflow pins, and pack-smoke command shape only when supported by evidence.
- Non-functional requirements: changes must remain narrow, dated, and source-backed.
- Interfaces / contracts: docs-hygiene posture checks, `codex-orchestrator codex defaults`, doctor baseline, pack-smoke marketplace install, cloud-canary workflow, runtime-mode canary, and review wrapper.

## Architecture & Data
- Architecture / design adjustments: no runtime architecture change unless validation exposes a blocker.
- Data model changes / migrations: none.
- External dependencies / integrations: Codex CLI, OpenAI Codex docs, GitHub release page, Linear, GitHub Actions, Codex Cloud.

## Validation Plan
- Tests / checks: required repo validation floor plus focused pack-smoke/docs/defaults tests.
- Rollout verification: PR checks, ready-review drain, and Linear workpad closeout.
- Monitoring / alerts: workflow pins keep cloud and pack-smoke candidates reproducible.

## Open Questions
- Cloud canary final status after `npm run build`.
- Full local `pack:smoke` status after `plugin marketplace add` alignment.

## Approvals
- Docs-review: pending.
- Standalone review: pending.
- Date: 2026-04-24
