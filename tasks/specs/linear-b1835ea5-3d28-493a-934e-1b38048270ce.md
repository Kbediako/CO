---
id: 20260420-linear-b1835ea5-3d28-493a-934e-1b38048270ce
title: Control host stale tracked and retained active claim admission repair
status: in_progress
relates_to: docs/PRD-linear-b1835ea5-3d28-493a-934e-1b38048270ce.md
risk: high
owners:
  - Codex
last_review: 2026-06-17
review_notes:
  - 2026-06-17: CO-579 pre-expiry review kept this spec active-current; no verified terminal/archive evidence was established in this stream, CO-579 is the live non-terminal docs-freshness owner, and docs/spec gates remain unchanged.
  - 2026-05-18: CO-522 active-spec audit found 2 unchecked task checklist items, so this spec remains active and was reviewed for current lifecycle ownership rather than archived. Evidence: `out/linear-b642e879-ba50-45ef-b0d9-b059afa9e932-recovery/spec-preexpiry-local-classification.json`.
---

## Canonical Reference
- Canonical TECH_SPEC: `tasks/specs/linear-b1835ea5-3d28-493a-934e-1b38048270ce.md`
- TECH_SPEC mirror: `docs/TECH_SPEC-linear-b1835ea5-3d28-493a-934e-1b38048270ce.md`
- PRD: `docs/PRD-linear-b1835ea5-3d28-493a-934e-1b38048270ce.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-b1835ea5-3d28-493a-934e-1b38048270ce.md`
- Task checklist: `tasks/tasks-linear-b1835ea5-3d28-493a-934e-1b38048270ce.md`
- `.agent` mirror: `.agent/task/linear-b1835ea5-3d28-493a-934e-1b38048270ce.md`

## Traceability
- Linear issue: `CO-255` / `b1835ea5-3d28-493a-934e-1b38048270ce`
- Linear URL: https://linear.app/asabeko/issue/CO-255/control-host-post-relaunch-stale-tracked-co-1-and-retained
- Shared source 0 anchor: `ctx:sha256:de1e154c0eeab0ff773e815641b72013b7de2f3a9f39ddc170e520646a5c300a#chunk:c000001`

## Summary
- Objective: repair the retained-state/projection/admission seam where stale top-level `tracked.linear=CO-1` plus retained active/handoff_failed provider claims keep `co-status --format json` flat and prevent current `Ready` work from being admitted after rebuild/relaunch.
- Scope:
  - docs-first packet and registry mirrors for CO-255
  - parent-owned diagnosis of `provider_intake`, selected claim, tracked projection, and admission-gate source paths
  - narrow implementation in the identified control-host/provider seams
  - focused regression coverage or durable diagnostics distinguishing CO-255 from CO-248, CO-41, CO-214, CO-219, and CO-223
- Constraints:
  - preserve real active worker safety and capacity enforcement
  - preserve real current `Merging` closeout action-required behavior
  - avoid manual state-file cleanup as the fix

## Issue-Shaping Contract
- User-request translation carried forward: the issue is not "CO-248 again"; the observed checksum combines stale top-level `tracked.linear=CO-1`, retained active claims `CO-212` / `CO-217`, retained `CO-219` `handoff_failed` merge-closeout residue, and no fresh `Ready` admission after the rebuilt/relaunched branch.
- Protected terms / exact artifact and surface names: `control-host`, `co-status --format json`, `provider_intake.active_issue_identifiers`, `provider_intake.running_issue_identifiers`, `selected_claim`, `tracked.linear`, `provider-intake-state.json`, `provider_issue_merge_closeout_action_required`, `providerIssueHandoff.ts`, `providerIntakeState.ts`, `controlRuntime.ts`, `observabilityReadModel.ts`.
- Nearby wrong interpretations to reject: CO-248 duplicate, CO-223-only tracked fallback, CO-214-only stale merged closeout residue, CO-219-only accepted dead-proof projection, CO-41-only lifecycle stall, status-only rendering, or manual JSON pruning.
- Explicit non-goals carried forward: no manual Linear state forcing, no manual intake JSON cleanup, no shared-root branch switch to resolve CO-219, no broad scheduler redesign.

## Parity / Alignment Matrix
- Current truth:
  - rebuilt/relaunched host still reports flat `running=2`, `issues=2`, `max_allowed=3`; `provider_intake.active_issue_identifiers` stays `CO-212`, `CO-217`, `CO-219`; `running_issue_identifiers` stays `CO-212`, `CO-217`; `selected_claim` stays pinned to `CO-212`; top-level `tracked.linear.identifier` stays stale `CO-1`; current `Ready` candidates are not admitted
- Reference truth:
  - CO-248 is a narrower plain `released/not_active` cached Blocked claim shape; CO-41 is a refresh lifecycle stall class; CO-214 is stale merged/Done closeout residue; CO-219 is accepted dead-proof running/max projection; CO-223 is stale top-level tracked fallback
- Target truth / intended delta:
  - retained stale active/handoff_failed rows stop flattening current queue projection when live issue truth is no longer active capacity; real active claims and current merge-closeout action-required states remain visible; fresh `Ready` issues are admitted when capacity is free; status/debug output distinguishes adjacent issue families
- Explicitly out-of-scope differences:
  - broad workflow-state policy changes, deleting historical claim evidence, bypassing provider-worker launch ownership, or changing capacity limits

## Readiness Gate
- Not done if:
  - `co-status --format json` can still stay pinned to stale `tracked.linear=CO-1`
  - retained `CO-212` / `CO-217` / `CO-219` claims can still flatten the projected queue indefinitely
  - available capacity still fails to admit newly truthful `Ready` work
  - diagnostics cannot distinguish CO-255 from CO-248, CO-41, CO-214, CO-219, and CO-223
- Pre-implementation issue-quality review evidence:
  - 2026-04-20: parent reviewed the issue description, existing adjacent docs packets, and source search output before implementation. The micro-task path is ineligible because correctness depends on exact issue-family boundaries, retained claim state, top-level tracked projection, and admission contracts.
- Safeguard ownership split:
  - parent owns diagnosis, implementation, tests, workpad, validation, PR, and review handoff
  - no child lane owns files yet because the first-turn decomposition found no safe non-overlapping file scope before seam classification

## Technical Requirements
- Functional requirements:
  - reproduce or model a retained `provider_intake` set with stale active/running identifiers and stale top-level tracked truth
  - identify whether the admission blocker is top-level tracked projection, retained claim selection, merge-closeout residue interaction, or another gate
  - preserve real active/handoff capacity semantics for live workers
  - preserve real current merge-closeout action-required behavior for live `Merging` lanes
  - demote, supersede, or diagnose stale retained rows only when live truth proves they should no longer block fresh admission
  - update status/debug projection, so operators can see why a row is no longer active or why admission remains blocked
  - add focused regression coverage for the CO-255 stale tracked plus retained active/handoff_failed shape
- Non-functional requirements:
  - keep the fix localized to existing provider/control-host seams
  - avoid extra Linear request burn where cached/local truth is sufficient
  - fail closed when freshness is unknown or live truth cannot safely supersede stale local claims
  - keep existing audit fields in `provider-intake-state.json`
- Interfaces / contracts:
  - `providerIssueHandoff.ts`
  - `providerIntakeState.ts`
  - `controlRuntime.ts`
  - `observabilityReadModel.ts`
  - `co-status --format json`
  - `provider-intake-state.json`

## Architecture & Data
- Architecture / design adjustments:
  - inspect provider intake claim ordering and selected-claim summary generation first
  - inspect runtime/status projection predicates for stale active claim pruning or demotion
  - inspect provider handoff fresh discovery and admission gates for retained rows that still consume capacity
  - prefer a shared predicate or diagnostic helper when the same stale-state classification affects both admission and status
- Data model changes / migrations:
  - no migration expected
  - existing claim fields should be preserved
  - a small diagnostic enum/reason may be added only if needed to make the retained-state seam explicit
- External dependencies / integrations:
  - Linear live issue state and workflow state types
  - local control-host artifacts under `.runs/local-mcp/cli/control-host`
  - provider-worker proof sidecars

## Validation Plan
- Tests / checks:
  - focused provider handoff or runtime regression for stale `tracked.linear=CO-1` plus retained `CO-212` / `CO-217` / `CO-219`-like claims
  - focused projection coverage for `co-status --format json` or equivalent read-model helpers
  - `node scripts/delegation-guard.mjs`
  - `node scripts/spec-guard.mjs --dry-run`
  - `npm run build`
  - `npm run lint`
  - `npm run test`
  - `npm run docs:check`
  - `npm run docs:freshness`
  - `npm run repo:stewardship`
  - `node scripts/diff-budget.mjs`
  - manifest-backed standalone review plus explicit elegance/minimality pass before review handoff
- Rollout verification:
  - rebuild/relaunch proof or focused model must show fresh `Ready` admission is no longer blocked by stale retained rows when capacity is free
  - if live relaunch is not safe in the issue workspace, record the focused regression as the durable proof and avoid mutating shared root
- Monitoring / alerts:
  - existing `co-status --format json` and provider intake debug snapshots remain the operator surfaces

## Open Questions
- Should stale retained claim classification live in `providerIntakeState.ts`, admission flow, runtime projection, or a shared helper?
- What exact freshness comparison safely separates current `provider_issue_merge_closeout_action_required` from stale residue without weakening real Merging ownership?

## Approvals
- Docs-first packet: parent provider worker
- Parent implementation/review/PR lifecycle: pending
- Date: 2026-04-20
