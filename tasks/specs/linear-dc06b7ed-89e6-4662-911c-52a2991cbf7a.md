---
id: 20260424-linear-dc06b7ed-89e6-4662-911c-52a2991cbf7a
title: CO Codex CLI 0.123.0 Posture And Marketplace Command Surface Correction
status: in_progress
relates_to: docs/PRD-linear-dc06b7ed-89e6-4662-911c-52a2991cbf7a.md
risk: high
owners:
  - Codex
last_review: 2026-05-18
related_action_plan: docs/ACTION_PLAN-linear-dc06b7ed-89e6-4662-911c-52a2991cbf7a.md
task_checklists:
  - tasks/tasks-linear-dc06b7ed-89e6-4662-911c-52a2991cbf7a.md
review_notes:
  - 2026-05-18: CO-522 active-spec audit found 13 unchecked task checklist items, so this spec remains active and was reviewed for current lifecycle ownership rather than archived. Evidence: `out/linear-b642e879-ba50-45ef-b0d9-b059afa9e932-recovery/spec-preexpiry-local-classification.json`.
---

## Canonical Reference
- PRD: `docs/PRD-linear-dc06b7ed-89e6-4662-911c-52a2991cbf7a.md`
- TECH_SPEC mirror: `docs/TECH_SPEC-linear-dc06b7ed-89e6-4662-911c-52a2991cbf7a.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-dc06b7ed-89e6-4662-911c-52a2991cbf7a.md`
- Task checklist: `tasks/tasks-linear-dc06b7ed-89e6-4662-911c-52a2991cbf7a.md`

## Summary
- Objective: register CO-337 as the bounded `0.123.0` posture-and-marketplace-surface correction lane.
- Scope:
  - official/local/versioned evidence for `0.121.0` / `0.122.0` / `0.123.0`
  - marketplace capability detection and invocation in `scripts/pack-smoke.mjs`
  - matching tests in `tests/pack-smoke.spec.ts`
  - docs/message refresh in the delegated docs slice
  - workflow pin rationale and `rust-v0.123.0` delta classification
- Constraints:
  - do not auto-promote `0.123.0`
  - do not weaken `pack:smoke` or cloud-canary requirements
  - keep parent/child ownership split intact until child patch accept/reject

## Issue-Shaping Contract
- User-request translation carried forward: CO-337 corrects a false command-surface assumption. The parent lane must verify the actual marketplace add surface, then update capability detection/docs/workflow rationale without widening scope into packaging redesign or runtime migration.
- Protected terms / exact artifact and surface names: Codex CLI `0.123.0`, Codex CLI `0.122.0`, Codex CLI `0.121.0`, Codex CLI `0.118.0`, `rust-v0.123.0`, `codex plugin marketplace add`, `codex marketplace add`, `pack:smoke`, `scripts/pack-smoke.mjs`, `tests/pack-smoke.spec.ts`, `docs/guides/codex-version-policy.md`, `README.md`, `docs/public/downstream-setup.md`, `plugins/codex-orchestrator/launcher.mjs`, `.github/workflows/core-lane.yml`, `.github/workflows/release.yml`, `.github/workflows/pack-smoke-backstop.yml`, `.github/workflows/cloud-canary.yml`, `node scripts/runtime-mode-canary.mjs`, `CODEX_CLOUD_ENV_ID`, `remote_sandbox_config`.
- Nearby wrong interpretations to reject: `0.122.0+` has no marketplace surface, the active target should jump automatically, `pack:smoke` should be softened, cloud canary can be weakened, or CO-196 packaging scope should reopen here.
- Explicit non-goals carried forward: no automatic target promotion, no cloud-gate weakening, no provider-worker runtime migration, and no broad Bedrock adoption unless evidence proves a CO-specific need.

## Parity / Alignment Matrix
- Current truth:
  - local `codex-cli 0.123.0` exposes `plugin` and rejects top-level `marketplace add`
  - CO code/tests/docs/workflow rationale still key off top-level `codex marketplace add`
  - cloud-canary is pinned to `0.122.0`, release-facing smoke remains pinned to `0.121.0`
- Reference truth:
  - official `rust-v0.123.0` release notes enumerate the new deltas
  - `0.121.0` supports both marketplace add paths
  - `0.122.0` and `0.123.0` support `codex plugin marketplace add`
- Target truth:
  - CO detects and uses the supported marketplace add path truthfully
  - docs/messages no longer instruct the stale command unconditionally
  - release-facing pin policy is explicitly justified after corrected-surface smoke
- Explicitly out-of-scope differences:
  - automatic target promotion
  - cloud gate redesign
  - provider-worker runtime redesign

## Technical Requirements
- Functional requirements:
  1. Capture official/local/versioned command evidence with timestamps.
  2. Correct `pack-smoke` capability detection and invocation.
  3. Update matching tests and workflow assertions.
  4. Refresh delegated docs/messages to the corrected command-surface truth.
  5. Classify `rust-v0.123.0` deltas into adopt / hold / no-op buckets for CO.
  6. Re-decide the release-facing pin policy only after corrected-surface smoke.
- Non-functional requirements:
  - fail closed when no supported marketplace add path exists
  - keep workflow proof explicit and deterministic
  - preserve current active-target gating and cloud evidence requirements
- Interfaces / contracts:
  - pack-smoke CLI contract
  - workflow install-pin assertions
  - current version-policy contract

## Validation Plan
- `linear child-stream --pipeline docs-review` after the packet lands
- focused `vitest` on `tests/pack-smoke.spec.ts`
- workflow-matched `npm run pack:smoke` against a post-`0.121.0` candidate
- required validation floor for final diff
- standalone review plus explicit elegance review before handoff

## Completion Criteria
- Corrected marketplace truth is implemented in code/tests/docs.
- `rust-v0.123.0` deltas are classified with explicit rationale.
- Release-facing pin policy is explicitly evidence-backed.
- Active-target posture language remains aligned with runtime/cloud evidence.
