---
id: 20260418-linear-f2c25700-a367-4e69-923f-cdf3e7f726be
title: CO CLI test surface remove clean-tree dependency on prebuilt dist entrypoint
status: in_progress
relates_to: docs/PRD-linear-f2c25700-a367-4e69-923f-cdf3e7f726be.md
risk: high
owners:
  - Codex
last_review: 2026-06-17
review_notes:
  - 2026-06-17: CO-579 pre-expiry review kept this spec active-current; no verified terminal/archive evidence was established in this stream, CO-579 is the live non-terminal docs-freshness owner, and docs/spec gates remain unchanged.
  - 2026-05-18: CO-522 active-spec audit found 1 unchecked task checklist item, so this spec remains active and was reviewed for current lifecycle ownership rather than archived. Evidence: `out/linear-b642e879-ba50-45ef-b0d9-b059afa9e932-recovery/spec-preexpiry-local-classification.json`.
---

## Canonical Reference
- Canonical TECH_SPEC: `tasks/specs/linear-f2c25700-a367-4e69-923f-cdf3e7f726be.md`
- TECH_SPEC mirror: `docs/TECH_SPEC-linear-f2c25700-a367-4e69-923f-cdf3e7f726be.md`
- PRD: `docs/PRD-linear-f2c25700-a367-4e69-923f-cdf3e7f726be.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-f2c25700-a367-4e69-923f-cdf3e7f726be.md`
- Task checklist: `tasks/tasks-linear-f2c25700-a367-4e69-923f-cdf3e7f726be.md`

## Traceability
- Linear issue: `CO-236` / `f2c25700-a367-4e69-923f-cdf3e7f726be`
- Shared source anchor: `ctx:sha256:55bbd81d18cf34c96d4d069e257260e0ca3c460478c7cbafb7ed3e1372bb3ade#chunk:c000001`
- Origin manifest: `.runs/linear-f2c25700-a367-4e69-923f-cdf3e7f726be/cli/2026-04-18T10-17-30-911Z-84ff6b3d/manifest.json`
- Active workpad source: `out/linear-f2c25700-a367-4e69-923f-cdf3e7f726be/manual/workpad.md`
- Same-turn child lane: `docs-source-evidence` (launched; parent closeout pending)

## Summary
- Objective: remove the clean-tree dependency on `dist/bin/codex-orchestrator.js` from the CLI command-surface suite while preserving the direct-dist delegation setup contract and the current assertions around that contract.
- Scope:
  - docs-first packet and registry/task mirrors
  - focused current-tree reproduction of the cold failure
  - one bounded harness fix in `tests/cli-command-surface.spec.ts`
  - focused reruns plus final clean-tree `npm run test`
- Constraints:
  - preserve the exact issue wording
  - keep `CO-229` explicitly separate
  - do not change the direct-dist delegation setup contract or delete coverage

## Issue-Shaping Contract
- User-request translation carried forward: this lane is about making `tests/cli-command-surface.spec.ts` hermetic on a clean tree; it is not about changing delegation setup semantics and not about reopening the doctor timeout cluster.
- Protected terms / exact artifact and surface names:
  - `tests/cli-command-surface.spec.ts`
  - `npm run clean:dist`
  - `npm run test`
  - `clean-tree`
  - `prebuilt dist`
  - `dist/bin/codex-orchestrator.js`
  - `Delegation MCP requires a built dist entrypoint for stdio startup`
- Nearby wrong interpretations to reject:
  - this is a `CO-229` doctor-suite issue
  - this should be fixed by deleting or weakening the setup/delegation tests
  - this requires changing the direct-dist delegation transport contract
  - warmed validation still fails in the same way
- Explicit non-goals carried forward:
  - no `CO-229` doctor-suite timeout work
  - no broader delegation runtime changes
  - no coverage reduction

## Parity / Alignment Matrix
- Current truth:
  - after `npm run clean:dist`, focused `tests/cli-command-surface.spec.ts` fails 4 apply-path cases with the missing-dist stderr
  - warmed validation after `npm run build` passes because the dist entrypoint exists again
  - apply-path tests assert the planned direct-dist command line but use fake Codex wiring for `mcp add`
- Reference truth:
  - clean-tree command-surface coverage should be runnable without a prebuilt dist artifact
  - the product contract can still require the direct dist entrypoint for actual delegation MCP startup
- Target truth / intended delta:
  - the affected apply-path tests stage their own bounded artifact/setup when needed
  - clean-tree focused coverage and full `npm run test` pass without a manual build
  - direct-dist assertions remain intact
- Explicitly out-of-scope differences:
  - actual delegation MCP startup semantics
  - doctor-suite stabilization
  - unrelated setup/devtools/control-host behavior

## Readiness Gate
- Not done if:
  - clean-tree `npm run test` still fails because `dist/bin/codex-orchestrator.js` is missing
  - the fix only hides the missing-artifact dependency without making the test path hermetic or intentionally staged
  - the fix drifts into broader delegation behavior or `CO-229`
- Pre-implementation issue-quality review evidence:
  - 2026-04-18: current-tree reproduction proves the active failure is narrower than a product runtime regression. The failure sits in apply-path tests that use fake Codex wiring but still trip the product existence guard before any real delegate-server startup is attempted.
  - 2026-04-18: the issue is not plausibly narrower than a clean-tree command-surface lane because the acceptance criteria explicitly require cold reproduction, warm-vs-cold explanation, and a bounded hermetic or intentionally staged fix.
- Safeguard ownership split:
  - parent lane owns implementation, validation, workpad, docs-review, PR lifecycle, and final child-lane acceptance/rejection
  - same-issue child lane owns only `out/linear-f2c25700-a367-4e69-923f-cdf3e7f726be/manual/child-docs-source-evidence.md`

## Technical Requirements
- Functional requirements:
  1. Preserve the exact issue wording in the docs packet.
  2. Reproduce the clean-tree failure with current evidence.
  3. Explain why warmed validation passes while cold validation fails.
  4. Keep the apply-path direct-dist command-line assertions intact.
  5. Make the clean-tree suite hermetic or intentionally staged without a hidden prebuilt-dist dependency.
- Non-functional requirements:
  - smallest viable change
  - deterministic artifact cleanup
  - no product behavior drift unless new evidence demands it
- Interfaces / contracts:
  - `tests/cli-command-surface.spec.ts`
  - `tests/helpers/inProcessEntrypoint.ts`
  - `orchestrator/tests/Doctor.test.ts`
  - `orchestrator/src/cli/delegationSetup.ts`
  - `orchestrator/src/cli/utils/delegationMcpHealth.ts`

## Architecture & Data
- Architecture / design adjustments:
  - prefer a test-only `resolveDelegationServerInvocation(...)` mock inside the four failing apply-path tests instead of mutating the repo `dist/` tree
  - keep the direct-dist command-line assertions intact by returning a temp-local `dist/bin/codex-orchestrator.js` path from that mock
  - keep the doctor direct-dist readiness test hermetic with a temp-local direct-dist responder that still exercises the real startup probe
- Required artifact/content expectations:
  - the command-surface harness must not overwrite or remove a real built repo dist artifact
  - the doctor test may materialize a temp-local executable at `dist/bin/codex-orchestrator.js` only inside its temp directory
- Data model changes / migrations:
  - none expected
- External dependencies / integrations:
  - no Linear mutation from child lanes
  - no new runtime/provider dependencies

## Current Truth
- `runCli(...)` in `tests/cli-command-surface.spec.ts` uses `runEntrypointLikeExec(...)` from `tests/helpers/inProcessEntrypoint.ts`.
- `setup --yes` and `delegation setup --yes` eventually call `runDelegationSetup({ apply: true, ... })`.
- `runDelegationSetup(...)` calls `resolveDelegationServerInvocation({ allowMissingDist: false, ... })`.
- `resolveDelegationServerInvocation(...)` throws when `/Users/kbediako/Code/CO/dist/bin/codex-orchestrator.js` is absent.
- The failing apply-path tests use `writeFakeCodexBinary(...)` and `CODEX_TEST_MCP_ADD_LOG`; they care about the logged `mcp add` command, not about executing the real delegate-server entrypoint.
- The current cold reproduction on branch `linear/co-236-cli-clean-tree-dist` after `npm run clean:dist` fails exactly the four expected cases with the missing-dist stderr, while `111` other tests in the file still pass.

## Proposed Design
- In `tests/cli-command-surface.spec.ts`, mock `resolveDelegationServerInvocation(...)` only in the four failing apply-path tests so they:
  - keep the direct-dist `dist/bin/codex-orchestrator.js` command-line assertions
  - avoid touching the repo `dist/` tree during clean-tree runs
  - keep product enforcement unchanged because the mock is test-local
- In `orchestrator/tests/Doctor.test.ts`, write a tiny temp-local executable at `dist/bin/codex-orchestrator.js` that consumes the initialize payload and emits a valid JSON-RPC initialize response quickly enough for the real startup probe path.
- Keep the rest of the suite unchanged unless the focused rerun reveals another missing-artifact case.

## Protected Expectations
- Preserve exact issue wording around `tests/cli-command-surface.spec.ts`, `npm run test`, `clean-tree`, `prebuilt dist`, and `dist/bin/codex-orchestrator.js`.
- Preserve the direct-dist delegation setup command-line assertions.
- Keep `CO-229` separate.
- Prefer the smallest harness fix over product behavior drift.

## Reject These Wrong Interpretations
- `Delete the failing tests.`
- `Change delegation setup so it no longer points at dist.`
- `Treat warmed full-suite success as proof that no follow-up is needed.`
- `Expand this lane into doctor timeout behavior.`

## Validation Plan
- Focused cold-state reproduction:
  - `npm run clean:dist`
  - `npm run test:orchestrator -- tests/cli-command-surface.spec.ts`
- Post-fix focused check:
  - rerun the same focused cold-state command
- Overlap proof:
  - `npm run clean:dist`
  - `npm run test:orchestrator -- tests/cli-command-surface.spec.ts orchestrator/tests/Doctor.test.ts tests/cli-build-config.spec.ts`
- Final clean-tree confirmation:
  - `npm run clean:dist`
  - `npm run test`
- Handoff floor:
  - `node scripts/delegation-guard.mjs`
  - `node scripts/spec-guard.mjs --dry-run`
  - `npm run build`
  - `npm run lint`
  - `npm run test`
  - `npm run docs:check`
  - `npm run docs:freshness`
  - `npm run repo:stewardship`
  - `node scripts/diff-budget.mjs`
  - `codex-orchestrator review`
  - `npm run pack:smoke`

## Approvals
- Reviewer: pending parent docs-review + implementation review
- Date: 2026-04-18
