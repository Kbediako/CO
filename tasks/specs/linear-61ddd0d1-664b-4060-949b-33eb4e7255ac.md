---
id: 20260422-linear-61ddd0d1-664b-4060-949b-33eb4e7255ac
title: "CO-297 stop-hook structured sentinel contract"
status: done
relates_to: docs/PRD-linear-61ddd0d1-664b-4060-949b-33eb4e7255ac.md
risk: medium
owners:
  - Codex
last_review: 2026-05-18
related_action_plan: docs/ACTION_PLAN-linear-61ddd0d1-664b-4060-949b-33eb4e7255ac.md
task_checklists:
  - tasks/tasks-linear-61ddd0d1-664b-4060-949b-33eb4e7255ac.md
review_notes:
  - 2026-05-18: CO-522 spec lifecycle audit found the linked task checklist has zero unchecked items (26 checked), so this spec is terminal and eligible for implementation-docs archive. Evidence: `out/linear-b642e879-ba50-45ef-b0d9-b059afa9e932-recovery/spec-preexpiry-local-classification.json`.
---

## Summary
- Objective: prevent false stop sentinels from persisting `~/.codex/hooks/co_orchestration_autocontinue.json` as `enabled=false`.
- Scope:
  - repo-owned hook source at `scripts/hooks/continue_co_orchestration.py`
  - installed hook sync at `/Users/kbediako/.codex/hooks/continue_co_orchestration.py`
  - focused Vitest regressions for false read-only stops, quoted sentinel text, and structured real blocker/stop flows
- Constraints:
  - preserve fail-open hook behavior
  - preserve normal resume prompting while the gate is enabled
  - apply the global hook only to the configured repo root and its path descendants, not string-prefix sibling paths
  - do not rewrite unrelated provider-worker or Linear workflow logic

## Issue-Shaping Contract
- User-request translation carried forward: the bug is the stop-hook persistence seam, not a generic policy discussion about sentinel wording. The fix must stop substring-based persistence and keep true structured stop flows working.
- Protected terms / exact artifact and surface names:
  - `CO stop hook`
  - `co_orchestration_autocontinue.json`
  - `/Users/kbediako/.codex/hooks/continue_co_orchestration.py`
  - `last_assistant_message`
  - `CO_ORCHESTRATOR_DONE`
  - `CO_ORCHESTRATOR_CRITICAL_BLOCKER`
  - `CO_ORCHESTRATOR_DESTRUCTIVE_DECISION_REQUIRED`
  - `CO_ORCHESTRATOR_STOP: <sentinel>`
- Nearby wrong interpretations to reject:
  - arbitrary sentinel mentions should disable auto-continue
  - read-only mode or `approval_policy=never` is itself a critical blocker
  - structured stop detection should suppress the resume prompt in non-stop cases
- Explicit non-goals carried forward:
  - no broader Codex lifecycle hook redesign
  - no global state schema migration
  - no provider-worker workflow-state rewrite

## Parity / Alignment Matrix

| Surface | Current truth | Reference truth | Target truth | Explicitly out of scope |
| --- | --- | --- | --- | --- |
| Stop persistence | Substring scan across the whole assistant message disables the gate. | Intentional terminal/blocker/destructive stops are machine-checkable. | A final structured line, `CO_ORCHESTRATOR_STOP: <sentinel>`, is required to disable the gate. | Changing all historical sentinel documentation. |
| False read-only blocker | A read-only audit mention of `CO_ORCHESTRATOR_CRITICAL_BLOCKER` can persist `enabled=false`. | Read-only/approval limits should be reported unless user intervention is truly required. | Prompt and parser prevent read-only explanatory mentions from disabling the gate. | Removing all critical-blocker semantics. |
| Tests | No focused repo-owned hook regression suite. | Acceptance requires regressions for false and true stop paths. | Vitest invokes the Python hook with a temp state path and asserts state persistence. | Full interactive Codex hook integration tests. |

## Readiness Gate
- Not done if:
  - quoted sentinel text disables the gate
  - arbitrary substring detection remains
  - true structured critical-blocker/done/destructive stops no longer disable the gate
  - tests touch the real user state file
- Pre-implementation issue-quality review evidence: parent provider worker translated the issue into the PRD/spec and confirmed the scope is the stop-hook persistence seam with no attached PR.
- Safeguard ownership split: parent owns docs, implementation, installed-hook sync, focused tests, validation, and PR lifecycle; no same-issue child lane is active.

## Technical Requirements
- Functional requirements:
  1. Add `parse_structured_stop_sentinel(last_message)` or equivalent so only a final structured control line disables auto-continue.
  2. Update the resume prompt to instruct agents to end with `CO_ORCHESTRATOR_STOP: <sentinel>` only for true stop conditions.
  3. Explicitly discourage `CO_ORCHESTRATOR_CRITICAL_BLOCKER` for read-only or `approval_policy=never` limitations that can continue without user action.
  4. Keep the gate enabled and emit the resume prompt for incidental sentinel text.
  5. Keep true structured `CO_ORCHESTRATOR_CRITICAL_BLOCKER`, `CO_ORCHESTRATOR_DONE`, and `CO_ORCHESTRATOR_DESTRUCTIVE_DECISION_REQUIRED` disabling behavior.
  6. Treat the configured repo root as a path boundary, so sibling directories with the same string prefix remain out of scope.
- Non-functional requirements:
  - fail open on hook exceptions
  - deterministic tests using isolated temp state
  - no writes to the real `co_orchestration_autocontinue.json` during tests
  - sibling-prefix cwd regressions stay covered by isolated temp paths
- Interfaces / contracts:
  - `CO_ORCHESTRATION_AUTOCONTINUE_STATE_PATH` may override the state path for tests
  - default state path remains `~/.codex/hooks/co_orchestration_autocontinue.json` for the current operator home

## Architecture & Data
- Architecture / design adjustments: introduce a repo-owned hook source and keep the installed hook synchronized with it.
- Data model changes / migrations: none.
- External dependencies / integrations: Python 3 for the hook and Node/Vitest for the regression harness.

## Validation Plan
- Tests / checks:
  - focused Vitest hook regression suite
  - checksum or diff proof that the installed hook matches the repo-owned hook source
  - relevant repo checks for touched files
- Rollout verification:
  - installed hook updated in place
  - isolated hook invocations show false sentinel mentions leave `enabled=true`
- Monitoring / alerts: workpad records validation and any review fallback.

## Open Questions
- None.

## Approvals
- Reviewer: parent provider worker
- Date: 2026-04-22
