---
id: 20260505-linear-a243025f-d1e4-4960-ac11-ddc1b9806ffe
title: CO-462 run-review override-prefixed mock cleanup
relates_to: docs/PRD-linear-a243025f-d1e4-4960-ac11-ddc1b9806ffe.md
risk: medium
owners:
  - Codex
last_review: 2026-05-05
related_action_plan: docs/ACTION_PLAN-linear-a243025f-d1e4-4960-ac11-ddc1b9806ffe.md
task_checklists:
  - tasks/tasks-linear-a243025f-d1e4-4960-ac11-ddc1b9806ffe.md
---

## Canonical Reference
- PRD: `docs/PRD-linear-a243025f-d1e4-4960-ac11-ddc1b9806ffe.md`
- Canonical TECH_SPEC: `tasks/specs/linear-a243025f-d1e4-4960-ac11-ddc1b9806ffe.md`
- TECH_SPEC mirror: `docs/TECH_SPEC-linear-a243025f-d1e4-4960-ac11-ddc1b9806ffe.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-a243025f-d1e4-4960-ac11-ddc1b9806ffe.md`
- Task checklist: `tasks/tasks-linear-a243025f-d1e4-4960-ac11-ddc1b9806ffe.md`
- `.agent` mirror: `.agent/task/linear-a243025f-d1e4-4960-ac11-ddc1b9806ffe.md`
- Source anchor: `ctx:sha256:e3b40af6e404697c60cd43d7e91ea7a900dbdb13c2c2d451918c05e4ca044a3f#chunk:c000001`

## Summary
- Objective: clean up run-review fake Codex mock processes when config overrides appear before the `review` subcommand.
- Scope: docs-first packet, focused `tests/run-review.spec.ts` helper/regression, exact-path process matching, focused validation, and process-health proof.
- Constraints: no provider-worker admission changes, no generic process supervisor rewrite, no cleanup of unrelated user processes.

## Issue-Shaping Contract
- User-request translation carried forward: CO-462 is a CO-205 follow-up variant for `codex-mock.sh -c mcp_servers.delegation.enabled=true review ...` processes that can survive test cleanup.
- Protected terms / exact artifact and surface names: `run-review`, `tests/run-review.spec.ts`, `codex-mock.sh`, `-c mcp_servers.delegation.enabled=true review`, `process-health checks`, `CO-205`.
- Nearby wrong interpretations to reject: provider-worker admission blocker, queue recovery, live worker kill lane, status-output filtering, broad process supervisor rewrite, basename-only process cleanup.
- Explicit non-goals carried forward: no provider/Linear behavior changes, no manual host orphan cleanup as completion, no real review process cleanup, no broad run-review rewrite.

## Parity / Alignment Matrix

| Surface | Current truth | Reference truth | Target truth | Explicitly out-of-scope differences |
| --- | --- | --- | --- | --- |
| Direct mock cleanup | Existing CO-205 path targets direct `codex-mock.sh review`. | Direct shape must stay covered. | Preserve direct cleanup. | No regression of CO-205 behavior. |
| Override-prefixed mock cleanup | `-c ... review` can be missed by direct substring matching. | Codex config overrides precede subcommands. | Match exact fake binary plus config override flags before `review`. | No generic Codex process cleanup or unrelated pre-review option cleanup. |
| Strict cleanup scope | Exact sandbox fake-binary paths bound the helper. | Real user processes are outside cleanup ownership. | Retain exact path and `review` subcommand proof. | No basename-only or pathless matching. |
| Health evidence | Stale mocks pollute process-health checks. | Health checks should reveal leaked mocks. | Post-suite scan shows no stale review mocks from this lane. | No hiding health output. |

## Readiness Gate
- Not done if:
  - override-prefixed fake Codex review commands are not covered
  - cleanup can kill unrelated user processes or different fake binaries
  - process-health proof is omitted
  - CO-205 linkage is missing
  - provider-worker admission or queue recovery scope appears in the implementation
- Pre-implementation issue-quality review evidence:
  - 2026-05-05: provider worker reconciled live Linear state and confirmed CO-462 is a bounded CO-205 follow-up variant with no attached PR.
  - 2026-05-05: micro-task path is unavailable because this is process cleanup/fallback-seam work and correctness depends on exact command-shape boundaries.
- Safeguard ownership split:
  - parent lane owns docs packet, source implementation, validation, workpad, PR lifecycle, and review handoff
  - no same-issue child lane owns files this turn because the implementation and regression likely touch the same focused test helper

## Technical Requirements
- Functional requirements:
  1. Extend run-review test helper process matching to recognize Codex config override flags before `review`.
  2. Preserve exact `codexBin` path matching.
  3. Add regression coverage for `codex-mock.sh -c mcp_servers.delegation.enabled=true review ...`.
  4. Preserve direct `codex-mock.sh review` cleanup coverage.
  5. Assert or prove unrelated user/different-binary processes are not targeted.
  6. Run passive `ps` health scan after focused validation.
- Non-functional requirements:
  - deterministic test behavior under macOS/Linux `ps`
  - no Windows-specific failure; existing Windows skips may remain for process inspection tests
  - no runtime behavior change outside the test harness unless source inspection proves production cleanup owns the bug
- Interfaces / contracts:
  - `findRunReviewMockProcesses(codexBin, options?)`
  - `cleanupRunReviewMockProcesses(codexBin)`
  - `runReviewCommandSubprocess(...)` finalizer

## Fallback Expiry / Refactor Decision
- Applies to fallback, compatibility, legacy, stale, cached, break-glass, or minor-seam behavior? `Yes`.
- Large-refactor check: another minor seam is acceptable because the observed defect is one command-shape gap in test-harness cleanup and the existing exact-path boundary remains correct.

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `tests/run-review.spec.ts` mock cleanup matching | Direct-only process match misses config overrides before `review`. | remove fallback | CO-462 | `codex-mock.sh -c ... review` survives cleanup. | CO-205 follow-up variant | 2026-05-05 | N/A after removal | Helper matches direct and override-prefixed review mocks. | Focused regression passes. |
| Exact fake-binary review mock cleanup | Tests use a cleanup finalizer for sandbox fake Codex review processes. | justify retaining fallback | CO-205 / CO-462 | Review wrapper tests intentionally spawn hanging fake Codex commands. | 2026-04-16 | 2026-05-05 | Supported test harness guardrail, not temporary fallback. | N/A; retain as safety cleanup. | Exact-path safety coverage and process scan. |

## Architecture & Data
- Architecture / design adjustments:
  - keep the cleanup helper local to the test harness unless production code is proven to own the leak
  - use token-aware command matching instead of direct substring matching where practical
- Data model changes / migrations:
  - none
- External dependencies / integrations:
  - local process inspection via `ps`

## Validation Plan
- Tests / checks:
  - focused `tests/run-review.spec.ts` regression around override-prefixed hanging mock cleanup
  - existing direct hanging mock cleanup regression
  - JSON parse for task registry and freshness registry
  - `node scripts/spec-guard.mjs --dry-run`
  - passive `ps -axo pid,ppid,pgid,etime,command | rg 'codex-mock\\.sh|run-review' || true`
- Rollout verification:
  - workpad records focused test, health scan, docs-review, standalone review, and elegance pass
- Monitoring / alerts:
  - existing process-health surfaces should stop seeing stale test-harness review mocks from this command shape

## Open Questions
- None.

## Approvals
- Docs-first packet: provider worker, 2026-05-05, produced; docs-review child stream completed `status=succeeded`, `review_outcome=clean-success`.
- Implementation review: codex-orchestrator review, 2026-05-05, completed `status=succeeded`, `review_outcome=bounded-success`; explicit elegance pass completed with no change.
- Date: 2026-05-05
