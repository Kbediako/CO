---
id: 20260410-linear-0878c751-6534-4352-ad6b-f18f67c239bb
title: CO: Fix CLI command-surface timeout and exec quoting blocker
status: done
owner: Codex
created: 2026-04-10
last_review: 2026-05-13
review_cadence_days: 30
risk_level: medium
related_prd: docs/PRD-linear-0878c751-6534-4352-ad6b-f18f67c239bb.md
related_action_plan: docs/ACTION_PLAN-linear-0878c751-6534-4352-ad6b-f18f67c239bb.md
related_tasks:
  - tasks/tasks-linear-0878c751-6534-4352-ad6b-f18f67c239bb.md
review_notes:
  - 2026-04-10: Opened from Linear issue `CO-136` in the provider-worker workspace after rechecking live CO workflow states with the packaged `linear issue-context` helper, confirming the issue is in `Rework`, and recording the required same-turn `forbid_parallel` decision with reason `parent_only_mutation`.
  - 2026-04-10: The stale `In Review` workpad from the unmerged r2 attempt was deleted with `linear delete-workpad`, the attached PR `#415` was confirmed already closed, and the workspace was recreated on branch `linear/co-136-cli-command-surface-timeout-quoting-r3` from fresh `origin/main`.
  - 2026-04-10: The issue packet still describes a possible raw source-entrypoint timeout seam and quoted single-token `exec` parsing seam, but the prior unmerged attempt claimed current main had already fixed runtime parsing and only the test seam remained. Fresh-main reproduction is therefore the first implementation gate.
  - 2026-04-10: The fresh Rework `## Codex Workpad` source is staged at `out/linear-0878c751-6534-4352-ad6b-f18f67c239bb/manual/workpad.md` and will become the single active Linear workpad for this attempt.
  - 2026-05-13: CO-523 live Linear audit verified CO-136 is Done/completed; reclassified this task spec as inactive done metadata for strict spec-guard evidence. Evidence: out/linear-8573da42-d9f9-44ce-a24e-224984539044/manual/20260512T1850Z-baseline/live-linear-states.json.
---

# Technical Specification

## Context
CO-136 is a narrow follow-up blocker discovered while finishing `CO-96`: clean validation now reportedly fails only in `tests/cli-command-surface.spec.ts`, with three raw source-entrypoint fast-fail cases too close to the existing `15_000ms` timeout budget and a quoted single-token `exec` case potentially splitting escaped quotes incorrectly. The issue is now in `Rework`, so the old handoff cannot be trusted as current truth even though it claimed runtime parsing was already fixed on main.

## Requirements
1. Re-establish current `origin/main` truth for the five cases cited in the issue packet before editing code.
2. Keep the fix bounded to `tests/cli-command-surface.spec.ts`, the direct CLI startup/parsing seam it exercises, and the required docs/evidence paths.
3. Preserve raw source-entrypoint truth for `rlm` and `exec` coverage.
4. Keep `CO-96` memory observability behavior unchanged.
5. Make `tests/cli-command-surface.spec.ts` deterministic and green under routine validation.
6. Record the new Rework attempt through a single fresh workpad, docs-review evidence, and the required validation/review gates.

## Design
- Rework reset:
  - use the already-closed PR `#415` only as historical context
  - delete the stale workpad
  - start from fresh `origin/main` on `linear/co-136-cli-command-surface-timeout-quoting-r3`
- Reproduction-first decision:
  - run direct raw source-entrypoint probes for the three fast-fail cases and the two quoted-argv cases
  - rerun the historical five-case focused subset and then the full `tests/cli-command-surface.spec.ts`
  - decide whether the smallest correct fix is:
    - a truthful timeout-budget/test-path adjustment,
    - a product parsing fix,
    - or a narrowly combined change
- Guardrails:
  - do not move the lane back into generic CO-114 performance optimization
  - do not widen into provider-worker or memory-observability surfaces
  - keep the review/workpad notes explicit about whether the remaining blocker is runtime behavior or test truthfulness

## Implementation Surface
- Expected codepaths:
  - `tests/cli-command-surface.spec.ts`
  - `bin/codex-orchestrator.ts`
  - any helper that currently parses or preserves `exec` argv
- Expected evidence:
  - fresh direct probe output/timing for the five issue cases
  - targeted subset and full-file test results
  - docs-review manifest and later review/elegance evidence

## Protected Expectations
- Treat CO-136 as an unrelated CLI command-surface blocker discovered while finishing `CO-96`, not as part of the memory-provenance scope.
- Preserve source-entrypoint behavior while making validation deterministic.
- Keep the final change tightly bounded.

## Reject These Wrong Interpretations
- "CO-96 should absorb this unrelated command-surface cleanup."
- "The issue is automatically caused by the new memory fields because it appeared after CO-96."
- "The answer is to weaken validation generally."
- "The prior unmerged r2 handoff settles the current-main truth without rerunning probes."

## Current Truth
- The issue packet reports:
  - `npm run test` otherwise passes with `tests/cli-command-surface.spec.ts` as the only failing file
  - three raw source-entrypoint fast-fail cases completing around `13s` to `14s`
  - a quoted single-token `exec` case that misparses escaped quotes
- The prior unmerged r2 attempt claimed:
  - the quoted-argv runtime seam was already fixed on then-current main
  - the remaining blocker was test truthfulness for five regressions in `tests/cli-command-surface.spec.ts`
- This lane must resolve the contradiction on fresh `origin/main` before implementation.

## Proposed Design
- Reproduce current-main truth first.
- If product behavior is already correct:
  - make the command-surface tests truthful to real startup cost or path selection.
- If quoted-argv parsing is still broken:
  - fix the minimal parser/argv seam and keep the quoted regression direct.
- If both seams remain:
  - apply the smallest combined correction and record which part fixed which failure.

## Non-Goals
- Reopening `CO-96` memory work.
- Broad Core Lane performance cleanup.
- Rewriting unrelated CLI subcommands or provider-worker flows.
- General test-harness redesign outside the failing command-surface cases.

## Parity / Alignment Matrix
- Current truth:
  - issue packet and stale r2 notes disagree on whether runtime parsing is still broken
  - current main must be revalidated
- Reference truth:
  - command-surface coverage should reflect the real raw source entrypoint and remain deterministic
- Target truth / intended delta:
  - one fresh authoritative reproduction decides whether the fix belongs in tests, product code, or both
  - the final suite is green with coverage still truthful to the raw entrypoint
- Explicitly out-of-scope differences:
  - unrelated review-wrapper work
  - non-command-surface performance work

## Not Done If
- The five cited cases are not all green.
- The suite still depends on an untruthful `15_000ms` budget for the fast-fail source-entrypoint cases even though the target is a `60s` source subprocess helper budget with truthful raw-entrypoint headroom.
- Escaped-quote argv still breaks when the product seam is actually at fault.
- The lane changes unrelated CO-96 or provider-worker behavior.

## Validation Plan
- `linear child-stream --pipeline docs-review`
- direct raw source-entrypoint probes for the five cited cases
- historical five-case focused subset in `tests/cli-command-surface.spec.ts`
- full `tests/cli-command-surface.spec.ts`
- full repo validation floor before review handoff

## Approvals
- Reviewer: pending `codex-orchestrator docs-review`
- Date: 2026-04-10
