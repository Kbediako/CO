---
id: 20260412-linear-b3286a9a-9cef-45a5-bd8a-532856a1188d
title: CO: harden run-review direct-exec symlink handling without regressing subprocess harnesses
status: in_progress
owner: Codex
created: 2026-04-12
last_review: 2026-04-12
review_cadence_days: 30
risk_level: high
related_prd: docs/PRD-linear-b3286a9a-9cef-45a5-bd8a-532856a1188d.md
related_action_plan: docs/ACTION_PLAN-linear-b3286a9a-9cef-45a5-bd8a-532856a1188d.md
related_tasks:
  - tasks/tasks-linear-b3286a9a-9cef-45a5-bd8a-532856a1188d.md
review_notes:
  - 2026-04-12: Live CO workflow state was rechecked with the packaged `linear issue-context` helper before mutation. The issue is already in active `Rework`, the attached PR `#402` is closed, and no active workpad comment currently exists on the issue.
  - 2026-04-12: Same-turn parallelization was recorded as `forbid_parallel` / `parent_only_mutation` because the Rework reset required parent-owned branch/workpad mutation before any same-issue child lane could safely share the workspace.
  - 2026-04-12: The workspace branch `linear/co-127-run-review-symlink-hardening` was reset to fresh `origin/main`; local workpad source is staged in `out/linear-b3286a9a-9cef-45a5-bd8a-532856a1188d/manual/workpad.md` while Linear writes are fail-closed under shared-budget cooldown until `2026-04-12 08:44 AEST`.
  - 2026-04-12: Current-main proof closed the design branch. `package.json` and current docs still expose only `codex-orchestrator review` plus the repo-local `npm run review` alias, but a same-directory symlinked direct invocation of `scripts/run-review.ts` under `NODE_OPTIONS=--preserve-symlinks-main` reproduced a silent exit-0/no-output failure. After broadening `isDirectExecution(...)` to accept resolved plus realpath entry URLs, the same source and rebuilt dist symlink probes print the expected help output, and focused tests stay green.
  - 2026-04-12: The audited docs-review child stream succeeded at `.runs/linear-b3286a9a-9cef-45a5-bd8a-532856a1188d-docs-review-rework/cli/2026-04-11T22-05-57-080Z-760b999c/manifest.json`, and `.runs/linear-b3286a9a-9cef-45a5-bd8a-532856a1188d-docs-review-rework/cli/2026-04-11T22-05-57-080Z-760b999c/review/telemetry.json` reports `review_outcome=clean-success`.
  - 2026-04-12: The parent standalone review succeeded at `.runs/linear-b3286a9a-9cef-45a5-bd8a-532856a1188d/cli/2026-04-11T14-43-18-011Z-10ef4027/review/telemetry.json` with `review_outcome=clean-success`, and the explicit minimality pass is recorded in `out/linear-b3286a9a-9cef-45a5-bd8a-532856a1188d/manual/20260412T081636Z-elegance-review.md`.
---

# Technical Specification

## Context
CO-127 is a narrow follow-up to the CLI-bin direct-exec hardening that landed in `7fd6aa427`. The open question is no longer whether `scripts/run-review.ts` differs from `bin/codex-orchestrator.ts`; that raw source mismatch is already obvious. The real question is whether the current supported `run-review` surface on `main` actually requires parity hardening, and if so whether that hardening can land without destabilizing the subprocess harness that already caught earlier naive patches.

## Requirements
1. Prove whether a supported symlinked `run-review` entrypoint exists today for repo-local or packaged workflows.
2. Preserve supported `npm run review` behavior.
3. Preserve packaged `codex-orchestrator review` launch behavior.
4. Do not edit `bin/codex-orchestrator.ts`.
5. Add focused regression coverage for the supported direct-exec path and any newly supported symlink-preserved path.
6. Keep `tests/run-review.spec.ts`, especially `does not crash when stdout pipe closes early`, green without weakening it.

## Issue-Shaping Contract
- User-request translation carried forward:
  - close the issue from the fresh rework reset by proving the supported surface on current main, not by replaying the closed PR or assuming symmetry automatically
  - land the smallest correct direct-exec resolution, whether that is hardening or explicit non-support codification
- Protected terms / exact artifact and surface names:
  - `scripts/run-review.ts`
  - `dist/scripts/run-review.js`
  - `NODE_OPTIONS=--preserve-symlinks-main`
  - `direct-execution guard parity with bin/codex-orchestrator.ts`
  - `tests/run-review.spec.ts`
  - `does not crash when stdout pipe closes early`
- Nearby wrong interpretations to reject:
  - reopen CO-114 timing work
  - change `bin/codex-orchestrator.ts` again
  - make the subprocess harness pass by weakening or deleting it
  - treat a shipped dist helper as a supported installed bin without launcher/doc evidence
- Explicit non-goals carried forward:
  - generic wrapper refactors
  - timeout inflation or regression masking
  - unrelated provider-worker or merge logic

## Parity / Alignment Matrix
- Current truth:
  - `bin/codex-orchestrator.ts` accepts both resolved and realpath entry URLs for direct execution.
  - `scripts/run-review.ts` now accepts both resolved and realpath entry URLs for direct execution, matching the intended parity for the helper seam without adding a new installed bin surface.
  - `package.json` publishes `dist/scripts/run-review.js` but only installs `codex-orchestrator` / `codex-orch` as bins.
  - `orchestrator/src/cli/reviewCliLaunchShell.ts` resolves review mode to direct source/dist helper paths.
- Reference truth:
  - the CLI-bin seam is already hardened in `7fd6aa427`.
  - the subprocess harness in `tests/run-review.spec.ts` is the truthful regression boundary for this lane.
- Target truth / intended delta:
  - the supported `run-review` surface is explicit and evidenced
  - the direct-exec behavior for that surface is covered by focused tests
  - the final diff stays bounded and does not regress the subprocess harness
- Explicitly out-of-scope differences:
  - new user-facing entrypoints
  - broader wrapper redesign
  - unrelated performance work

## Readiness Gate
- Not done if:
  - the current supported-path verdict is not proven
  - the direct-exec seam changes without focused validation of both the seam and the subprocess harness
  - the workpad/docs packet do not record the current verdict and review status
- Pre-implementation issue-quality review evidence:
  - 2026-04-12 current-tree audit shows the issue is still correctly bounded: it already names the exact surfaces, allowed outcomes, and rejected shortcuts. The missing ingredient is fresh evidence from `origin/main`, not broader scoping.
- Safeguard ownership split:
  - parent: Rework reset, docs packet, workpad, direct-exec seam, focused tests, review handoff
  - delegated stream: audited `docs-review` child stream after the packet is drafted and Linear writes are available

## Technical Requirements
- Functional requirements:
  - prove the supported repo-local and packaged entrypoints
  - if hardening is justified, keep the change local to `isDirectExecution(...)` and the minimum focused tests needed
  - if non-support remains the truthful answer, codify that surface clearly in tests/docs without widening runtime behavior
- Non-functional requirements (performance, reliability, security):
  - preserve subprocess-harness timing truthfulness
  - keep the lane bounded and reversible
  - fail closed on ambiguous unsupported launch surfaces
- Interfaces / contracts:
  - `package.json` `review` script contract
  - `codex-orchestrator review` launcher contract
  - `run-review` direct-exec contract under `NODE_OPTIONS=--preserve-symlinks-main`

## Architecture & Data
- Architecture / design adjustments:
  - favor a proof-first decision gate over committing to parity hardening before the supported surface is known
  - prefer focused helper/test updates over heavy subprocess additions where possible
- Data model changes / migrations:
  - none expected
- External dependencies / integrations:
  - Linear workpad and docs-review child-stream helpers
  - GitHub PR lifecycle for later handoff

## Validation Plan
- Tests / checks:
  - audited `docs-review` child stream
  - focused direct-exec and launcher tests
  - focused subprocess smoke rerun for `stdout pipe closes early`
  - repo validation floor plus standalone review and elegance review before handoff
- Rollout verification:
  - refresh the same workpad after docs, implementation, validation, and before any new handoff
- Monitoring / alerts:
  - record Linear write cooldown or review-wrapper boundary failures truthfully instead of treating them as code regressions

## Open Questions
- Resolved: current `main` still lacks an installed standalone `run-review` bin, but parity hardening on the helper direct-exec seam is safe and necessary because the same-directory symlink path reproduced a real silent no-op.
- Resolved: the focused validation run keeps the existing subprocess timeout budget intact.

## Approvals
- Reviewer: Self-approved for implementation start after the pre-implementation issue-quality review above; docs-review child stream and parent standalone review both reached clean success.
- Date: 2026-04-12
