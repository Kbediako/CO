---
id: 20260531-linear-0d975e30-67a5-4ea4-992a-61576c89d913
title: "CO-524 review_intent_matrix"
relates_to: docs/PRD-linear-0d975e30-67a5-4ea4-992a-61576c89d913.md
risk: high
owners:
  - Codex
last_review: 2026-05-31
related_action_plan: docs/ACTION_PLAN-linear-0d975e30-67a5-4ea4-992a-61576c89d913.md
task_checklists:
  - tasks/tasks-linear-0d975e30-67a5-4ea4-992a-61576c89d913.md
---

## Canonical Reference
- Linear issue: `CO-524` / `0d975e30-67a5-4ea4-992a-61576c89d913`
- PRD: `docs/PRD-linear-0d975e30-67a5-4ea4-992a-61576c89d913.md`
- TECH_SPEC mirror: `docs/TECH_SPEC-linear-0d975e30-67a5-4ea4-992a-61576c89d913.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-0d975e30-67a5-4ea4-992a-61576c89d913.md`
- Task checklist: `tasks/tasks-linear-0d975e30-67a5-4ea4-992a-61576c89d913.md`
- `.agent` mirror: `.agent/task/linear-0d975e30-67a5-4ea4-992a-61576c89d913.md`
- Source anchor: `ctx:sha256:6e71b8b5b90b781af5d2becb11953604969b8abe358cda6b629323af0a896360#chunk:c000001`

## Summary
- Objective: define `review_intent_matrix` as the canonical review intent bridge between docs-first sources, coding standards, proposals, review telemetry, and provider-worker handoff.
- Scope: matrix contract, row coverage, source/proposal separation, telemetry semantics, handoff gate effects, Codex CLI 0.130.0 compatibility, `goals advisory-only`, and focused validation plan.
- Constraints: no implementation/test edits in this child lane; no Linear/GitHub/PR/workpad actions; parent owns lifecycle and implementation.

## Issue-Shaping Contract
- User-request translation carried forward: CO-524 needs a docs-first packet for `review_intent_matrix`, not an implementation patch. The matrix must preserve the exact review source and telemetry terms that determine whether a review can support provider-worker handoff.
- Protected terms / exact artifact and surface names: `review_intent_matrix`, `original spec`, `PRD`, `TECH_SPEC`, `ACTION_PLAN`, `coding standards`, `code change proposals`, `agent loop change proposals`, `review_verdict`, `review_outcome`, `provider-worker handoff`, `Codex CLI 0.130.0`, `goals advisory-only`.
- Nearby wrong interpretations to reject: generic review checklist; PRD-only intent source; wrapper `review_outcome` substitutes for semantic `review_verdict`; missing matrix rows pass by default; advisory goals authorize handoff; child lane mutates Linear/GitHub/PR/workpad or implementation files.
- Explicit non-goals carried forward: no lifecycle mutation, no implementation, no broad review-wrapper rewrite, no Codex version promotion, no goal-driven authority, no weakening review gates.

## Parity / Alignment Matrix

| Surface | Current truth | Reference truth | Target truth / intended delta | Explicitly out-of-scope differences |
| --- | --- | --- | --- | --- |
| `original_spec` | Source intent can be cited in review prompts but is not guaranteed to have canonical packet evidence. | Original issue/spec wording remains the intent anchor, and docs-first packet evidence comes from `PRD`, `TECH_SPEC`, and `ACTION_PLAN`. | `review_intent_matrix.original_spec` records required evidence and fails closed to unknown when canonical packet evidence is unavailable. | No live Linear read in this child lane. |
| `coding_standards` | Standards can be prompt context without explicit axis-level effect. | Review should check coding standards alongside issue intent. | Matrix records coding standards as a required axis. | No new coding standard in this packet. |
| Proposals | Reviewer output can include code and agent-loop proposals. | Code fixes and agent-loop changes have different owners and handoff effects. | Matrix separates `code change proposals` from `agent loop change proposals`. | No automatic proposal execution. |
| Review telemetry | `review_verdict` and `review_outcome` describe different facts. | Handoff cleanliness requires semantic clean verdict, not wrapper success alone. | Matrix and handoff interpretation require clean `review_verdict` while retaining `review_outcome` as execution disposition. | No parser weakening. |
| Compatibility and goals | Codex CLI 0.130.0 evidence and goals context may appear beside review evidence. | Compatibility and goals must be explicit and fail closed. | Matrix treats Codex CLI 0.130.0 as compatibility evidence and keeps `goals advisory-only`. | No version promotion or goal authority. |

## Readiness Gate
- Not done if:
  - any required axis is absent
  - `original_spec` is clean without canonical `PRD`, `TECH_SPEC`, and `ACTION_PLAN` evidence
  - proposal classes are conflated
  - `review_outcome` overrides `review_verdict`
  - provider-worker handoff can proceed on findings, unknown, missing, stale, or mismatched semantic review evidence
  - Codex CLI 0.130.0 compatibility silently passes without current matrix evidence
  - `goals advisory-only` becomes review or lifecycle authority
  - this child lane edits implementation, tests, Linear, GitHub, PR, or workpad state
- Pre-implementation issue-quality review evidence:
  - 2026-05-31: this packet confirms CO-524 is a parity/alignment lane for review intent, telemetry, proposal classes, and handoff authority.
  - The micro-task path is unavailable because correctness depends on exact protected terms, exact surfaces, and review/handoff parity.
  - Source payload path from the prompt was not present in this child checkout; the packet is anchored on the parent-provided source anchor and issue details.
- Safeguard ownership split: parent owns Linear/workpad/review/implementation/PR lifecycle; child owned only scoped docs-first files.

## Technical Requirements
- Functional requirements:
  1. Define a structured `review_intent_matrix` with axes `original_spec`, `coding_standards`, `code_change_proposals`, and `agent_loop_change_proposals`.
  2. Require every axis to include `required`, `verdict`, `evidence`, and `proposed_fixes`.
  3. Require `original_spec` to prove canonical `PRD`, `TECH_SPEC`, and `ACTION_PLAN` evidence when clean, otherwise fail closed to unknown.
  4. Require separate proposal axes or classes for `code change proposals` and `agent loop change proposals`.
  5. Preserve `review_verdict` as semantic review state with clean, findings, unknown, missing, stale, and mismatched evidence handling.
  6. Preserve `review_outcome` as wrapper execution disposition and do not let it override semantic findings or unknown verdicts.
  7. Make `provider-worker handoff` fail closed when the matrix is missing, incomplete, stale, or contradicted by `review_verdict`.
  8. Preserve Codex CLI 0.130.0 compatibility as an explicit fixture-backed path that cannot silently pass without current matrix metadata.
  9. Preserve `goals advisory-only` as non-authoritative review context.
- Non-functional requirements: keep output compact, prefer local docs/manifest/review evidence, fail closed on missing or contradictory evidence, and avoid broad review-wrapper rewrites.
- Interfaces / contracts:
  - review prompt assembly
  - review telemetry output carrying `review_verdict` and `review_outcome`
  - provider-worker handoff gate
  - docs-first packet paths and task registry
  - Codex CLI 0.130.0 compatibility fixture
  - advisory goal evidence, with `goals advisory-only`

## Review Intent Matrix Contract

| Axis field | Requirement |
| --- | --- |
| `required` | Must be true for every required axis. |
| `verdict` | One of `clean`, `findings`, or `unknown`; clean requires supporting evidence. |
| `evidence` | Concise evidence strings, including canonical packet availability for `original_spec`. |
| `proposed_fixes` | Concise fixes or explicit no-change evidence for clean axes. |

## Fallback Expiry / Refactor Decision
- Applies to fallback, compatibility, legacy, stale, cached, break-glass, or minor-seam behavior? Yes.
- Required decision table:

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Review intent classification | Review intent inferred from ad hoc prompt prose instead of a structured `review_intent_matrix`. | remove fallback | CO-524 | Provider-worker docs-review, implementation-gate, standalone review, or pre-handoff review prompt assembly. | 2026-05-31 | 2026-05-31 | This issue | `review_intent_matrix` axes are generated and validated for `original_spec`, `coding_standards`, `code_change_proposals`, and `agent_loop_change_proposals`; `original_spec` carries canonical `PRD`, `TECH_SPEC`, and `ACTION_PLAN` evidence. | Focused matrix generation, missing-axis, prompt transport, and provider-worker handoff tests. |
| Review telemetry compatibility | Codex CLI 0.130.0 or legacy review evidence can lack current intent matrix fields. | expire fallback | CO-524 | Review evidence comes from Codex CLI 0.130.0-era telemetry or lacks a current matrix binding. | 2026-05-31 | 2026-05-31 | 30 days, expires 2026-06-30 | Current review wrapper evidence always includes valid matrix metadata, or old evidence is explicitly rejected as stale/unsupported. | Codex CLI 0.130.0 fixture tests and fail-closed stale/unsupported evidence tests. |
| Goals context in review prompts | Goal evidence appears near review context and could be mistaken for lifecycle authority. | justify retaining fallback | CO-524 provider-worker handoff | Goals evidence is present in prompt, manifest, workpad, or status context. | 2026-05-31 | 2026-05-31 | non-expiring durable authority contract | A separately approved authority redesign replaces Linear/workpad/review authority and proves goal evidence can become authoritative. | Tests prove `goals advisory-only` cannot satisfy `review_verdict`, `review_outcome`, or provider-worker handoff gates. |

- Contract name: Review intent matrix with Linear-first and review-verdict-first provider-worker handoff.
- Owning surface: review prompt assembly, review telemetry interpretation, and provider-worker handoff checks.
- Steady-state proof: current handoff continues to require semantic review cleanliness; wrapper execution disposition and advisory goals remain context only.
- Tests/docs: this CO-524 packet, focused review intent matrix tests, review telemetry fixture tests, and provider-worker handoff tests.
- Non-expiring rationale: `goals advisory-only` is a durable authority boundary, not temporary fallback debt.
- Large refactor check: no broad review-wrapper replacement is selected; CO-524 adds the smallest structured matrix that removes prose-only intent inference while preserving existing gates.
- Minor seam decision: no retained minor seam; missing or stale matrix evidence fails closed instead of adding another compatibility branch.

## Architecture & Data
- Add matrix construction near review prompt assembly and persist matrix evidence through review telemetry/handoff checks.
- Data model change is additive only; missing legacy matrix fields are stale/unsupported unless explicitly waived.
- Dependencies remain the Codex review wrapper, Codex CLI 0.130.0 compatibility evidence, provider manifests, and advisory goal evidence.

## Validation Plan
- Tests / checks:
  - matrix generation test for all required axes
  - canonical original-spec evidence tests
  - missing-axis fail-closed tests
  - code proposal and agent-loop proposal separation tests
  - prompt transport tests proving the matrix is visible to review
  - review telemetry tests for `review_verdict` and `review_outcome` contradictions
  - provider-worker handoff tests for clean, findings, unknown, missing, stale, and mismatched states
  - Codex CLI 0.130.0 compatibility fixture tests
  - `goals advisory-only` authority-boundary tests
  - scoped docs checks for this packet before implementation
- Rollout verification:
  - parent-owned docs-review before implementation
  - focused implementation tests after source changes
  - parent-owned validation floor and review gates before PR lifecycle
- Monitoring / alerts:
  - handoff summaries should mention matrix coverage and separate semantic verdict from wrapper outcome.

## Completion Criteria
- Docs packet and task registry use provider issue task key `linear-0d975e30-67a5-4ea4-992a-61576c89d913` without title/retry/freshness suffixes.
- `review_intent_matrix` covers all required axes and canonical original-spec evidence.
- `review_verdict` remains the semantic review gate.
- `review_outcome` remains wrapper execution disposition.
- Codex CLI 0.130.0 compatibility and `goals advisory-only` are explicit and fail closed.
- Parent validation proves provider-worker handoff cannot proceed on incomplete or contradictory review evidence.
