---
id: 20260411-linear-b0aac010-12ec-4509-88a4-c2dbc6395e19
title: CO: Audit remaining review-launch and compatibility alias seams after CO-88
status: done
owner: Codex
created: 2026-04-11
last_review: 2026-05-12
review_cadence_days: 30
risk_level: high
related_prd: docs/PRD-linear-b0aac010-12ec-4509-88a4-c2dbc6395e19.md
related_action_plan: docs/ACTION_PLAN-linear-b0aac010-12ec-4509-88a4-c2dbc6395e19.md
related_tasks:
  - tasks/tasks-linear-b0aac010-12ec-4509-88a4-c2dbc6395e19.md
review_notes:
  - 2026-04-11: Live team workflow state was rechecked with the packaged `linear issue-context` helper before any mutation; the issue then moved from `Ready` to `In Progress`, the single persistent workpad comment was created, and the detached workspace switched onto branch `co-137-audit-review-launch-compat-seams`.
  - 2026-04-11: Pre-implementation source audit confirmed `review-launch-attempt`, the collab env alias helpers, and `orchestrator/src/sync/**` still have active imports, tests, or explicit current docs, so this lane starts from retain-with-rationale rather than blind deletion.
  - 2026-04-11: Current-turn parallelization was recorded as `parallelize_now` / `independent_scope_available`; the parent owns the docs packet plus the review-launch, collab-env, and sync seams, while child stream `requires-cloud-alias-audit` owns the bounded `requiresCloud` compatibility-family audit.
  - 2026-05-12: CO-523 live Linear audit verified CO-137 is Done/completed; reclassified this task spec as inactive done metadata for strict spec-guard evidence. Evidence: .runs/linear-8573da42-d9f9-44ce-a24e-224984539044/cli/2026-05-12T18-47-35-293Z-376d8842/provider-linear-issue-context-cache-b0aac010-12ec-4509-88a4-c2dbc6395e19.json.
---

# Technical Specification

## Context
CO-88 already removed the clearly dead truthfulness debt. The remaining seams are smaller but riskier because they still look live: the standalone-review launch shell has multiple wrapper consumers, the collab env aliases are still part of the canonical-to-legacy migration surface, the `requiresCloud` family still crosses planner and execution-mode boundaries, and cloud sync is still a documented opt-in integration seam. This lane decides which of those surfaces genuinely still need to exist and makes the retained ones explicit.

## Requirements
1. Produce an explicit keep/delete decision for `scripts/lib/review-launch-attempt.ts`.
2. Produce an explicit keep/delete decision for the legacy collab env aliases in `orchestrator/src/cli/rlm/rlmCodexRuntimeShell.ts`.
3. Produce an explicit keep/delete decision for the `requiresCloud` / `requires_cloud` compatibility family.
4. Produce an explicit keep/delete decision for `orchestrator/src/sync/**`.
5. Land any required truthfulness update in code comments or repo-facing docs for every retained seam touched by the lane.
6. Validate any narrowed or deleted seam with focused tests before review handoff.

## Issue-Shaping Contract
- User-request translation carried forward:
  - this is a keep-or-delete audit for the remaining still-live CO-88 compatibility seams, not another repo-wide cleanup pass
  - behavior-heavy seams require current import, runtime, and test evidence before deletion
- Protected terms / exact artifact and surface names:
  - `review-launch-attempt`
  - `requiresCloud`
  - `requires_cloud`
  - `rlmCodexRuntimeShell.ts`
  - `orchestrator/src/sync/**`
- Nearby wrong interpretations to reject:
  - delete the seams merely because they are compatibility code
  - reopen already-completed CO-88 deletions
  - broaden into provider observability, STATUS work, or new feature work
- Explicit non-goals carried forward:
  - new review features
  - new runtime modes
  - new cloud-sync feature wiring
  - CO-82 or CO-83 scope

## Parity / Alignment Matrix
- Current truth:
  - `review-launch-attempt` is still the launch shell used by `run-review`, preflight, and non-interactive handoff helpers and is covered by focused tests.
  - legacy collab env keys are still read and tested while the canonical multi-agent env migration remains documented.
  - `sync/**` is still a tested opt-in integration point, but the default CLI does not wire it.
  - the `requiresCloud` family still crosses planner and execution-mode helpers and needs a narrower audit.
- Reference truth:
  - retained seams should say clearly why they still exist and which current consumer still needs them.
- Target truth / intended delta:
  - each seam ends with a clear keep/delete verdict and truthful rationale.
- Explicitly out-of-scope differences:
  - broader wrapper refactors
  - broader collab migration removal
  - new cloud-sync or runtime behavior

## Readiness Gate
- Not done if:
  - retained seams remain undocumented or ambiguous
  - deleted behavior-heavy seams lack focused proof
  - the packet does not capture the actual current-consumer evidence behind each verdict
- Pre-implementation issue-quality review evidence:
  - 2026-04-11 direct current-tree audit found active imports and tests for the named seams, which means the correct framing is a bounded audit with explicit rationale, not a second opportunistic deletion sweep.
- Safeguard ownership split:
  - parent: docs packet, workpad, `review-launch-attempt`, `run-review`, `rlmCodexRuntimeShell`, `sync/**`, repo-facing summary docs
  - child lane: `requiresCloud` / `requires_cloud` compatibility family and focused execution-mode regression coverage

## Technical Requirements
- Functional requirements:
  - preserve current behavior for retained seams
  - add only the minimal comment or doc wording needed to explain retained compatibility surfaces
  - integrate the child-lane `requiresCloud` verdict into the same docs packet and workpad
- Non-functional requirements (performance, reliability, security):
  - no additional runtime or network dependencies
  - no degradation to standalone review, collab runtime, or execution-mode safety behavior
- Interfaces / contracts:
  - standalone review wrapper contract
  - collab env compatibility contract
  - planner/output execution-mode compatibility contract
  - optional EventBus-backed cloud-sync contract

## Architecture & Data
- Architecture / design adjustments:
  - prefer code-comment or doc truthfulness adjustments over structural rewrites for retained seams
  - if a seam can now be narrowed safely, keep the change local to that seam and its focused tests
- Data model changes / migrations:
  - none expected
- External dependencies / integrations:
  - Linear provider-worker helpers for workpad, transition, docs-review, and child-lane evidence

## Validation Plan
- Tests / checks:
  - focused review wrapper tests
  - focused collab runtime tests
  - focused cloud-sync tests
  - focused execution-mode alias tests from the child lane
  - repo validation floor plus standalone review and elegance pass
- Rollout verification:
  - workpad updated after docs-first, implementation, validation, and before any review handoff
- Monitoring / alerts:
  - record any docs-review or review-wrapper boundary fallback truthfully instead of treating it as a product result

## Open Questions
- Pending child-lane result: whether the `requiresCloud` alias family still needs both spellings in all current planner and execution-mode surfaces.

## Approvals
- Reviewer: Self-approved for implementation start after the pre-implementation issue-quality review above; docs-review pending.
- Date: 2026-04-11
