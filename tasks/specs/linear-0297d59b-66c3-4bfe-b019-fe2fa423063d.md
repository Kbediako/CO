---
id: 20260422-linear-0297d59b-66c3-4bfe-b019-fe2fa423063d
title: CO: re-enforce CO-185 provider-helper preflight guarantees in live worker attempts
status: in_progress
owner: Codex
created: 2026-04-22
last_review: 2026-04-22
review_cadence_days: 30
risk_level: high
related_prd: docs/PRD-linear-0297d59b-66c3-4bfe-b019-fe2fa423063d.md
related_action_plan: docs/ACTION_PLAN-linear-0297d59b-66c3-4bfe-b019-fe2fa423063d.md
related_tasks:
  - tasks/tasks-linear-0297d59b-66c3-4bfe-b019-fe2fa423063d.md
review_notes:
  - 2026-04-22: docs child lane produced the packet, but accept failed closed after `updated_at` drift; parent imported the packet into the authoritative workspace.
  - 2026-04-22: docs-review failed only on unrelated `CO-282` baseline debt and fallback was recorded under `out/linear-0297d59b-66c3-4bfe-b019-fe2fa423063d/manual/20260422T083251Z-docs-review-fallback.md`.
---

# Technical Specification

## Context
- `CO-306` is a bounded restore/re-audit lane for live CO-185 guarantees.
- Apr 22 evidence still shows repeated same-attempt `linear_follow_up_parity_matrix_missing` and `provider_worker_child_lane_parent_dirty` churn in `CO-295`, `CO-299`, and `CO-302`.
- The lane is not permission to weaken parity-matrix enforcement, weaken clean-parent checks, or redesign scheduler/dispatch/concurrency policy.

## Requirements
1. Reproduce the live failure shapes from `CO-295`, `CO-299`, and `CO-302` and isolate why CO-185 did not hold.
2. After `linear_follow_up_parity_matrix_missing`, unchanged same-attempt retries must not reissue the mutation.
3. Preserve clean-parent checks while preventing repeated same-attempt `provider_worker_child_lane_parent_dirty` launch churn through deterministic sequencing/guidance.
4. Land the fix on the live provider-worker path, not only on dead helper seams.
5. Add focused regression coverage for both guarantees.
6. Preserve fail-closed parity-matrix and clean-parent contracts.

## Issue-Shaping Contract
- Preserve: `CO-185`, `CO-295`, `CO-299`, `CO-302`, `provider_worker_child_lane_parent_dirty`, `linear_follow_up_parity_matrix_missing`, `provider truth/prompting/retry suppression`, `provider-linear-worker-linear-audit.jsonl`, and `provider-linear-worker-proof.json`.
- Reject: weakening parity-matrix enforcement, weakening clean-parent checks, broad remediation of the related issue family, or treating docs-only work as closure.
- Not done if the same attempt can still repeat either deterministic failure shape without materially changed inputs or improved sequencing/guidance.

## Parity Matrix

| Surface | Current | Reference | Target |
| --- | --- | --- | --- |
| Same-attempt parity retry | `CO-299` / `CO-302` still repeat `linear_follow_up_parity_matrix_missing`. | CO-185 intended unchanged same-attempt suppression. | Unchanged parity follow-up retries stop after the first deterministic failure. |
| Parent-dirty launch recovery | `CO-295` / `CO-299` still repeat `provider_worker_child_lane_parent_dirty`. | Clean-parent stays fail-closed, but the next step changes deterministically. | Parent-dirty launch churn converges instead of repeating. |
| Live leverage of helper truth | Current live worker path still ignores the first deterministic failure. | The next same-attempt step must change after the first failure. | Live prompting or retry behavior materially changes after the first failure. |
| Guardrails | Pressure exists to bypass checks. | Parity-matrix and clean-parent checks remain authoritative. | Churn drops without weakening either contract. |

## Validation Plan
- Audit/proof reproduction for `CO-295`, `CO-299`, and `CO-302`.
- Focused helper-path tests for same-attempt parity suppression.
- Focused child-lane tests for parent-dirty sequencing/guidance.
- Normal implementation validation and review gates after code changes.

## Approvals
- Reviewer: pending parent review.
- Date: pending.
