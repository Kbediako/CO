---
id: 20260501-linear-7b8e48c8-3971-42ab-b779-949d810d4e6c
title: "CO-457 provider-worker resolved model provenance"
status: in_progress
owner: Codex
created: 2026-05-01
last_review: 2026-05-05
review_cadence_days: 30
risk_level: medium
related_prd: docs/PRD-linear-7b8e48c8-3971-42ab-b779-949d810d4e6c.md
related_action_plan: docs/ACTION_PLAN-linear-7b8e48c8-3971-42ab-b779-949d810d4e6c.md
related_tasks:
  - tasks/tasks-linear-7b8e48c8-3971-42ab-b779-949d810d4e6c.md
review_notes:
  - 2026-05-03: Orchestrator packet setup created the CO-457 traceability docs so the issue can leave Backlog with protected model-posture and provenance boundaries intact.
  - 2026-05-05: Parent implementation aligned resolved model provenance schema with the packet and added CO-382 expiry evidence for the degraded runtime-metadata path.
---

# TECH_SPEC - CO-457 provider-worker resolved model provenance

## Canonical Reference
- PRD: `docs/PRD-linear-7b8e48c8-3971-42ab-b779-949d810d4e6c.md`
- TECH_SPEC mirror: `docs/TECH_SPEC-linear-7b8e48c8-3971-42ab-b779-949d810d4e6c.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-7b8e48c8-3971-42ab-b779-949d810d4e6c.md`
- Task checklist: `tasks/tasks-linear-7b8e48c8-3971-42ab-b779-949d810d4e6c.md`
- Agent mirror: `.agent/task/linear-7b8e48c8-3971-42ab-b779-949d810d4e6c.md`

## Summary
- Objective: record provider-worker resolved model provenance so worker proof/read-model surfaces no longer require manual inference from host config.
- Scope:
  - CO-457 docs-first packet and task mirrors
  - `tasks/index.json`
  - `docs/TASKS.md`
  - `docs/docs-freshness-registry.json`
  - implementation follow-up for proof, hydration, CO STATUS, read-model, and focused tests
- Constraints:
  - current local posture remains `gpt-5.5` / `xhigh` when available
  - portable fallback remains `gpt-5.4` / `xhigh`
  - missing runtime model evidence is degraded provenance, not a launch failure
  - binary provenance stays in `CO-450`
  - auth provenance stays in `CO-451`

## Issue-Shaping Contract
- User-request translation:
  - Provider workers should make their resolved model evidence explicit.
  - No-`--model` inherited-config launches should be able to record `gpt-5.5` truthfully with source and confidence.
  - Runtime-emitted model/reasoning metadata from `codex exec --json` or `codex exec resume --json` is preferred.
  - Explicit `--model` overrides, when supported, must be recorded distinctly.
  - Missing runtime metadata should produce `unknown` or degraded proof, not an authoritative claim.
- Protected terms / exact artifact and surface names:
  - `gpt-5.5`
  - `gpt-5.4`
  - `provider-linear-worker-proof.json`
  - `provider-linear-worker-session-log-hydration.json`
  - `codex exec --json`
  - `codex exec resume --json`
  - `--model`
  - `model_reasoning_effort`
  - resolved model provenance
  - `CO STATUS`
  - read-model
- Nearby wrong interpretations to reject:
  - auto-switching worker model defaults
  - making portable templates default to `gpt-5.5`
  - treating binary/auth provenance as equivalent to model provenance
  - treating config-derived model as runtime-proven
  - blocking launch solely because Codex does not emit a model slug
- Explicit non-goals:
  - no automatic model migration or fallback removal
  - no app-bundle versus active-CLI provenance beyond the `CO-450` link
  - no auth provenance beyond the `CO-451` link
  - no broad lifecycle refactor
  - no docs-freshness/spec-guard baseline repair

## Parity / Alignment Matrix

| Surface | Current Truth | Reference Truth | Target Truth | Explicitly Out Of Scope |
| --- | --- | --- | --- | --- |
| Worker model posture | Workers inherit host defaults when `--model` is omitted. | CO-local ChatGPT-auth/appserver posture is `gpt-5.5` / `xhigh` when available. | Proof records inherited `gpt-5.5` with source/confidence or runtime proof. | Default migration. |
| Portable fallback | Packaged/generated config can still use `gpt-5.4` fallback. | Fallback is contingency-only for unavailable local `gpt-5.5` or portability gaps. | Proof can represent fallback without making it the local target. | Removing fallback policy. |
| Runtime model events | Codex JSON may not expose a model slug consistently. | Runtime metadata is strongest evidence when present. | Missing runtime metadata writes degraded/unknown provenance. | Launch failure solely from missing metadata. |
| Operator projections | CO STATUS/read-model currently omit resolved model provenance. | Operators need audit-friendly source/confidence. | Projection exposes model, effort, source, confidence, and degraded reason. | Binary/auth provenance. |

## Technical Requirements
- Functional requirements:
  1. Create six CO-457 packet/mirror docs.
  2. Register the task in `tasks/index.json`.
  3. Add a `docs/TASKS.md` snapshot.
  4. Add six active rows to `docs/docs-freshness-registry.json`.
  5. Record that the packet clears `backlog_head_follow_up_traceability_pending`.
  6. Preserve protected terms and Not Done If conditions across packet/checklist surfaces.
  7. In implementation, add structured resolved-model provenance to `provider-linear-worker-proof.json`.
  8. In implementation, add corresponding provenance to `provider-linear-worker-session-log-hydration.json`.
  9. In implementation, expose provenance through `CO STATUS` and read-model.
  10. In implementation, cover inherited `gpt-5.5`, explicit override, and missing-runtime-model degraded output.
- Non-functional requirements:
  - packet diff is docs/config only
  - JSON remains parseable
  - implementation does not change model defaults
  - degraded evidence is visible and not flattened into authoritative model proof
- Interfaces / contracts:
  - task id: `linear-7b8e48c8-3971-42ab-b779-949d810d4e6c`
  - registry id: `20260501-linear-7b8e48c8-3971-42ab-b779-949d810d4e6c`
  - source issue: `CO-352`

## Fallback Expiry / Refactor Decision
- Applies to fallback, compatibility, legacy, stale, cached, break-glass, or minor-seam behavior? Yes.

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Provider-worker resolved model provenance | Degraded proof path for missing runtime model or effort metadata | expire fallback | CO-457 | Codex JSONL omits runtime model metadata from provider-worker `codex exec --json` or resume output | 2026-05-05 | 2026-05-19 | 2026-06-04 | Codex runtime always emits authoritative model/reasoning metadata for provider-worker runs, or CO adopts a stronger proof source | Focused runtime-reported, inherited-config, command override, unknown/degraded, hydration, and read-model projection tests |

- Large refactor decision: no large refactor is justified; this remains a narrow proof/hydration/status projection extension under existing provider-worker authority.
- Minor seam decision: the temporary degraded-provenance seam is acceptable only with source, confidence, degraded reason, expiry metadata, and focused validation.

## Acceptance Criteria
- CO-457 packet docs are created in the declared file scope.
- `tasks/index.json` registers the canonical task id.
- `docs/TASKS.md` includes the CO-457 traceability snapshot.
- `docs/docs-freshness-registry.json` includes six active rows with `last_review=2026-05-03`.
- Worker proof carries structured resolved-model provenance.
- CO STATUS/read-model expose model provenance or explicit degraded/unknown state.
- No-`--model` inherited config records current local `gpt-5.5` truthfully without pretending runtime proof.
- Explicit override and missing-runtime-model degraded cases have focused tests.
- Current model defaults remain unchanged.

## Validation Plan
- Packet setup:
  - JSON parse for `tasks/index.json`
  - JSON parse for `docs/docs-freshness-registry.json`
  - targeted path scan for `linear-7b8e48c8-3971-42ab-b779-949d810d4e6c` and `CO-457`
  - targeted protected-term scan
  - `node scripts/spec-guard.mjs --dry-run`
  - `npm run docs:check`
  - `npm run docs:freshness`
- Implementation follow-up:
  - focused proof test for inherited `gpt-5.5`
  - focused proof test for explicit override when supported
  - focused proof test for missing runtime model metadata
  - CO STATUS/read-model projection checks

## Open Questions
- Exact implementation owner path is intentionally left to the active CO-457 provider worker after packet setup.

## Approvals
- Reviewer: CO operator / provider-worker parent lane
- Date: 2026-05-03
