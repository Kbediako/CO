---
id: 20260421-linear-937a8e46-5f29-49b6-8272-83db7a3f1a83
title: CO-286 attach already-active provider-worker runs before capacity gating
status: done
relates_to: docs/PRD-linear-937a8e46-5f29-49b6-8272-83db7a3f1a83.md
risk: high
owners:
  - Codex
last_review: 2026-05-18
related_action_plan: docs/ACTION_PLAN-linear-937a8e46-5f29-49b6-8272-83db7a3f1a83.md
task_checklists:
  - tasks/tasks-linear-937a8e46-5f29-49b6-8272-83db7a3f1a83.md
review_notes:
  - 2026-05-18: CO-522 spec lifecycle audit found the linked task checklist has zero unchecked items (28 checked), so this spec is terminal and eligible for implementation-docs archive. Evidence: `out/linear-b642e879-ba50-45ef-b0d9-b059afa9e932-recovery/spec-preexpiry-local-classification.json`.
---

## Canonical Reference
- PRD: `docs/PRD-linear-937a8e46-5f29-49b6-8272-83db7a3f1a83.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-937a8e46-5f29-49b6-8272-83db7a3f1a83.md`
- Task checklist: `tasks/tasks-linear-937a8e46-5f29-49b6-8272-83db7a3f1a83.md`
- Source anchor: `ctx:sha256:084012f69b4bb6720b2260bd7bede38839f9e787c0260245a9c9144c905aad91#chunk:c000001`

## Summary
- Objective: define the CO-286 docs-first contract for attaching/projecting already-active discovered provider-worker runs before capacity gating suppresses new dispatch.
- Scope:
  - docs-first packet and checklist mirrors for `CO-286`
  - `tasks/index.json` registration and `docs/TASKS.md` snapshot
  - parent-owned implementation in the narrow `providerIssueHandoff.ts` attach/rehydration ordering seam
  - parent-owned focused regression coverage around full occupancy
- Constraints:
  - child lane does not edit source, tests, package metadata, Linear state, workpads, or PR lifecycle files
  - `max_concurrent_agents` remains the authoritative capacity contract
  - missing provider-control-host provenance debt remains separate handling

## Issue-Shaping Contract
- User-request translation carried forward: `CO-286` is not a stale-host restart, cap bump, manual-start, dashboard-only, or validation-weakening lane. It is the narrow ordering seam where already-active discovered provider-worker runs must attach/project before capacity gating suppresses fresh dispatch.
- Protected terms / exact artifact and surface names:
  - `control host`
  - `provider-intake`
  - `provider-intake-state.json`
  - `co-status`
  - `CO STATUS`
  - `already-active discovered runs`
  - `capacity gating`
  - `max_concurrent_agents`
  - `providerIssueHandoff.ts`
  - `attach/rehydration ordering`
  - `missing provider-control-host provenance debt`
  - `CO-267`
  - `CO-281`
  - `CO-282`
- Nearby wrong interpretations to reject:
  - stale-host restart incident
  - cap-only fix from 3 to 4
  - manual worker starts as steady state
  - display-only dashboard cosmetic fix
  - weakening active-run truth validation
  - resolving missing provider-control-host provenance debt as part of this ordering fix
- Explicit non-goals carried forward:
  - no `max_concurrent_agents` change
  - no source/test edits in this child lane
  - no Linear mutation from this child lane
  - no broad scheduler, control-host restart, or dashboard redesign

## Parity / Alignment Matrix
- Current truth:
  - already-active provider-worker runs can be discovered by the control host
  - capacity gating can suppress fresh dispatch at full `max_concurrent_agents` occupancy
  - the ordering seam may suppress dispatch before already-active discovered runs are attached into provider-intake and projected into `co-status` / `CO STATUS`
  - the shared source payload is available under `.runs/linear-937a8e46-5f29-49b6-8272-83db7a3f1a83/cli/2026-04-21T15-33-33-161Z-3d4bdcc4/memory/source-0/source.txt`; current source implementation details remain parent-owned until source inspection
- Reference truth:
  - discovered active provider-worker runs are already real work and should be attached/projected before slot availability is used to suppress new dispatch
  - `max_concurrent_agents` is the capacity contract, not a value to change for CO-286
  - missing provider-control-host provenance debt is separate handling and should not be used to bypass attach/rehydration ordering or validation
- Target truth / intended delta:
  - parent reorders or splits the handoff path so already-active discovered provider-worker runs attach/project before capacity gating returns a no-slot result
  - `provider-intake`, `co-status`, and `CO STATUS` show the already-active work at full occupancy
  - focused tests cover full occupancy with already-active discovered runs
- Explicitly out-of-scope differences:
  - stale-host restart handling
  - cap-only changes from 3 to 4
  - manual worker starts as steady-state recovery
  - display-only dashboard cosmetic changes
  - active-run truth-validation weakening
  - missing provider-control-host provenance debt repair

## Readiness Gate
- Not done if:
  - already-active discovered provider-worker runs remain unattached or unprojected when capacity gating suppresses dispatch
  - full occupancy still hides active provider-worker truth from provider-intake, `co-status`, or `CO STATUS`
  - parent changes `max_concurrent_agents` instead of fixing attach/rehydration ordering
  - parent relies on manual worker starts as steady state
  - parent ships a display-only fix without changing the ordering seam
  - parent weakens active-run truth validation
  - parent treats missing provider-control-host provenance debt as closed by CO-286
- Pre-implementation issue-quality review evidence:
  - 2026-04-21: issue-quality review confirms the issue is not plausibly narrower than the user request. The protected request explicitly names `control host`, `provider-intake`, `co-status`, already-active discovered runs, capacity gating, `max_concurrent_agents`, `providerIssueHandoff.ts`, `CO-267`, `CO-281`, `CO-282`, attach/rehydration ordering, and separate missing provider-control-host provenance debt handling; the spec carries all of those terms and rejects the nearby wrong interpretations.
  - 2026-04-21: the micro-task path is ineligible because correctness depends on exact protected wording, exact surfaces, and the current/reference/target parity matrix.
- Safeguard ownership split:
  - child lane owns only the declared docs/checklist/registry files
  - parent lane owns source/test inspection, implementation, docs-review, validation, Linear/workpad reconciliation, PR lifecycle, and merge

## Technical Requirements
- Functional requirements:
  1. Create the CO-286 docs-first packet and declared mirrors.
  2. Preserve the protected issue wording and non-goals.
  3. Parent must attach/project already-active discovered provider-worker runs before capacity gating suppresses fresh dispatch.
  4. Parent must keep `max_concurrent_agents` authoritative and unchanged.
  5. Parent must prove full occupancy still projects already-active discovered runs.
  6. Parent must keep missing provider-control-host provenance debt as separate handling.
- Non-functional requirements:
  - smallest viable change in the existing handoff/attach/projection path
  - deterministic active-run truth under full occupancy
  - no weakening of validation or provenance boundaries
- Interfaces / contracts:
  - `providerIssueHandoff.ts`
  - provider-intake state/update surface
  - `co-status` / `CO STATUS` projection surface
  - `max_concurrent_agents` capacity contract

## Architecture & Data
- Architecture / design adjustments:
  - parent should inspect the ordering between already-active discovery, attach/rehydration, provider-intake projection, and capacity gating
  - the corrected path should attach/project already-active discovered provider-worker runs even when capacity is already full
  - fresh dispatch can remain suppressed after attachment/projection proves full occupancy
- Data model changes / migrations:
  - no destructive data migration is expected
  - additive projection or status fields are acceptable only if they support the attach-before-capacity contract
- External dependencies / integrations:
  - no Linear mutation from this child lane
  - parent may use existing control-host/provider-intake/projection helpers rather than introducing a new subsystem

## Current Truth
- The source payload referenced by the parent prompt is absent in this child checkout, so this packet does not claim a live source inspection beyond the prompt.
- The protected implementation target is `providerIssueHandoff.ts` and the attach/rehydration ordering path around provider-intake, projection, and capacity gating.
- Adjacent issue/debt references `CO-267`, `CO-281`, and `CO-282` remain protected context, not scope expansion.

## Proposed Design
- Parent should implement the smallest ordering fix that ensures:
  - already-active discovered provider-worker runs are validated as active truth
  - validated already-active runs attach to provider-intake state and project to `co-status` / `CO STATUS`
  - only after that attachment/projection does capacity gating suppress fresh dispatch due to `max_concurrent_agents`
- The fix should preserve active-run validation and should not convert absent/invalid active-run proof into accepted occupancy.

## Validation Plan
- Child-lane scoped checks:
  - JSON parse for `tasks/index.json`
  - protected-term grep across touched packet and mirror files
  - `git diff --check` over the declared docs file scope
- Parent-lane focused checks:
  - regression in the provider handoff/attach path proving already-active discovered runs are attached/projected before capacity gating under full occupancy
  - projection/status regression proving `provider-intake`, `co-status`, or `CO STATUS` surfaces include the already-active runs before reporting no fresh dispatch capacity
  - negative regression proving invalid active-run truth is still rejected and validation is not weakened
  - parent docs-review before implementation and parent-selected validation after source edits

## Approvals
- Reviewer: pending parent docs-review / implementation
- Date: 2026-04-21
