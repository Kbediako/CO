---
id: 20260330-linear-af97d673-43a4-4a36-8738-b7f61e5b71a1
title: CO-41 post-PR #547 provider refresh stall recurrence
status: in_progress
owner: Codex
created: 2026-03-30
last_review: 2026-05-18
review_cadence_days: 30
risk_level: high
related_prd: docs/PRD-linear-af97d673-43a4-4a36-8738-b7f61e5b71a1.md
related_action_plan: docs/ACTION_PLAN-linear-af97d673-43a4-4a36-8738-b7f61e5b71a1.md
related_tasks:
  - tasks/tasks-linear-af97d673-43a4-4a36-8738-b7f61e5b71a1.md
review_notes:
  - 2026-03-30: Opened from Linear issue `CO-41` after rechecking the live workflow states with `linear issue-context`; the CO team exposes `Ready`, `In Progress`, `In Review`, `Merging`, and `Rework`, and this lane moved the issue from `Ready` to `In Progress` before coding.
  - 2026-03-30: Docs-review succeeded via `.runs/linear-af97d673-43a4-4a36-8738-b7f61e5b71a1-docs-review/cli/2026-03-30T01-25-36-879Z-17cd2f7d/manifest.json`; the `linear child-stream` wrapper returned `provider_worker_child_stream_output_invalid` because orchestrator logs preceded the final JSON payload, so the lane recorded the manifest-backed approval directly.
  - 2026-03-30: March implementation landed in `PR #324` / commit `330581458` as the original stuck polling / refresh watchdog fix.
  - 2026-04-18: `CO-41` was reopened for a recurrence where `provider-intake-state.json` reported `stuck=true`, `restart_required=true`, `last_error=provider_refresh_lifecycle_stuck`, `refresh_phase=refresh:claim_reconcile`, and free capacity (`running=2`, `max_allowed=3`) while a `Ready` issue (`CO-252`) was not admitted until restart.
  - 2026-04-18: Implementation landed in `PR #547` / commit `0484cd803` for the repeated active-worker restart quarantine seam and `counts.max_allowed` supervision diagnostic preservation; this is now an Apr 18 baseline, not Apr 19 completion proof.
  - 2026-04-19: Linear comments reopened the post-#547 recurrence with `restart_required` recurrences, stale `released:not_active` / `provider_issue_released:not_active` rows, `active_worker_proof_missing`, freshness-gauge stale verdict, partial restart recovery, and spare-capacity no-admission.
  - 2026-05-18: CO-522 active-spec audit found 1 unchecked task checklist item, so this spec remains active and was reviewed for current lifecycle ownership rather than archived. Evidence: `out/linear-b642e879-ba50-45ef-b0d9-b059afa9e932-recovery/spec-preexpiry-local-classification.json`.
---

# Technical Specification

## Context

`CO-41` originally addressed a March 30, 2026 control-host provider refresh stall where newly eligible `Ready` issues were not admitted until the host restarted. That original stuck polling / refresh watchdog repair landed in `PR #324` / commit `330581458`.

The issue recurred on Apr 18 and produced the `PR #547` repair baseline: the supervision health path now preserves `counts.max_allowed` and does not treat repeated active-worker restart quarantine as healthy when capacity remains available. That Apr 18 repair is important reference behavior, but it is not current completion proof.

The issue was reopened again on Apr 19 after `PR #547` merged. The new Linear-comment evidence is specific: post-#547 `restart_required` recurrences, stale `released:not_active` / `provider_issue_released:not_active` rows, `active_worker_proof_missing`, freshness-gauge stale verdict, partial restart recovery, and spare-capacity no-admission. Parent implementation must identify the current blocker before changing source.

## Protected Terms / Exact Artifact Names
- `provider-intake-state.json`
- `provider_refresh_lifecycle_stuck`
- `refresh:claim_reconcile`
- `Ready`
- `In Progress`
- `restart_required`
- `stuck`
- `released:not_active`
- `provider_issue_released:not_active`
- `active_worker_proof_missing`
- `freshness-gauge stale`
- `partial restart recovery`
- `spare-capacity no-admission`
- `CO-41`
- `CO-252`
- `CO-217`
- `CO-211`
- `CO-214`
- `CO-248`

## User-Request Translation
- Refresh the `CO-41` docs packet for the Apr 19 reopened recurrence only.
- Keep March `PR #324` as the reference fix for the original watchdog work.
- Keep Apr 18 `PR #547` as the reference fix for the previous free-capacity quarantine seam.
- Preserve the Apr 19 Linear-comment facts exactly enough for implementation and review.
- Do not mark Apr 19 implementation, tests, review, Linear, workpad, or PR lifecycle complete in this docs child lane.

## Requirements

1. The implementation must identify why the Apr 19 post-#547 recurrence still produced `restart_required`.
2. The repair must preserve truthful `provider_refresh_lifecycle_stuck`, `restart_required`, and `stuck` evidence.
3. Stale `released:not_active` / `provider_issue_released:not_active` rows must not silently suppress eligible intake.
4. `active_worker_proof_missing` must remain visible and must not masquerade as healthy active-worker protection.
5. Freshness-gauge stale verdict and partial restart recovery must be machine-checkable before closeout.
6. Spare-capacity no-admission must be reproduced or simulated and either fixed or explained by explicit blocker evidence.
7. The implementation must preserve serialized refresh correctness and avoid duplicate claims or unsafe forced concurrency.
8. Validation must include focused coverage for the identified current seam and no-regression evidence for adjacent contracts.

## Nearby Wrong Interpretations To Reject
- `PR #324 already closed this recurrence.` No: `PR #324` is the March reference baseline.
- `PR #547 already closed this recurrence.` No: `PR #547` is the Apr 18 baseline; Apr 19 is a reopened post-merge recurrence.
- `This is capacity exhaustion.` No: Apr 19 evidence includes spare-capacity no-admission.
- `This is only Linear status drift.` No: the local control-host provider refresh/admission state remains the issue family.
- `This is only CO-211.` No: `CO-211` covers repeated restart churn with healthy active workers; `CO-41` remains the lifecycle-stall lineage anchor for this recurrence.
- `This is only CO-214.` No: `CO-214` covers stale merged-closeout residue after `CO-211` / `PR #506`.
- `This is only CO-248.` No: `CO-248` covers a `refresh:rehydrated` starvation shape; Apr 19 `CO-41` evidence is post-#547 recurrence evidence.
- `This should be widened into CO-39, CO-40, or CO-33.` No: those are out of this bounded issue scope.
- `Docs refresh alone closes CO-41.` No: source repair and validation are required.

## Current / Reference / Target Parity Matrix

| Contract | Reference: March `PR #324` | Reference: Apr 18 `PR #547` | Current: Apr 19 recurrence | Target |
| --- | --- | --- | --- | --- |
| Issue scope | Original `CO-41` watchdog repair for stuck refresh reporting. | Free-capacity repeated active-worker restart quarantine repair. | Reopened post-#547 recurrence with stale release rows and missing active-worker proof. | Fix the current seam without erasing either baseline. |
| Health state | Stuck lifecycle should surface rather than hang silently. | `restart_required` remains unhealthy when free capacity exists. | `restart_required` recurs after `PR #547`. | The current blocker is explicit and machine-checkable. |
| Release rows | Not the March focus. | Not the Apr 18 primary seam. | Stale `released:not_active` / `provider_issue_released:not_active` rows are current evidence. | Stale release rows cannot suppress eligible intake silently. |
| Active-worker proof | Not the March focus. | Active-worker series protection was narrowed by capacity. | `active_worker_proof_missing` appears in the recurrence evidence. | Missing proof remains unhealthy or explicitly blocked, not healthy. |
| Freshness / recovery | Restart historically let `Ready` issues progress. | Supervision recovery was expected under free capacity. | Freshness-gauge stale verdict and partial restart recovery remain. | Recovery is measured before closeout. |
| Capacity / admission | Restart historically let `Ready` issues progress. | `running=2`, `max_allowed=3` captured free-capacity no-admission. | Spare-capacity no-admission recurs after `PR #547`. | Available capacity admits work or emits explicit blocker truth. |
| Adjacent issues | `CO-41` is the lineage anchor. | `CO-217`, `CO-211`, `CO-214`, and `CO-248` remain nearby boundaries. | `CO-39`, `CO-40`, `CO-33`, and unrelated issue families stay out of scope. | Keep boundaries explicit and add no-regression coverage only where source changes touch them. |

## Non-Goals
- No source or test edits by this child lane.
- No package, Linear workflow, provider issue handoff, merge closeout, or workpad redesign in this child lane.
- No broad provider scheduler redesign.
- No weakening or suppression of `provider_refresh_lifecycle_stuck` / `restart_required` truth.
- No closing `CO-41` from docs refresh alone.
- No replacement of `CO-39`, `CO-40`, `CO-33`, `CO-211`, `CO-214`, `CO-248`, `CO-217`, or `CO-252`.

## Not Done If
- The packet still implies March `PR #324` or Apr 18 `PR #547` is sufficient for the Apr 19 recurrence.
- Post-#547 `restart_required`, stale `released:not_active` rows, `active_worker_proof_missing`, freshness-gauge stale verdict, partial restart recovery, or spare-capacity no-admission evidence is omitted.
- Apr 19 implementation, tests, review, or PR lifecycle are marked done by this docs child lane.
- Adjacent issue boundaries are missing.

## Validation Plan

- Child-lane docs validation:
  - JSON parse check for `tasks/index.json`
  - `git diff --check` over the touched docs/task files
- Parent implementation validation:
  - reproduce or simulate the Apr 19 post-#547 recurrence
  - add focused coverage for stale release rows, `active_worker_proof_missing`, freshness-gauge stale verdict, partial restart recovery, and spare-capacity no-admission
  - prove genuine `provider_refresh_lifecycle_stuck`, `restart_required`, and `stuck` outcomes remain visible
  - review adjacent boundaries for `CO-39`, `CO-40`, `CO-33`, `CO-211`, `CO-214`, `CO-248`, `CO-217`, and `CO-252`
  - run validation and review gates before PR handoff
