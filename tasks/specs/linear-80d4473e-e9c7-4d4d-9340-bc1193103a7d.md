---
id: 20260423-linear-80d4473e-e9c7-4d4d-9340-bc1193103a7d
title: CO: route docs-freshness owner reuse by canonical cohort without broadening CO-320
status: in_progress
owner: Codex
created: 2026-04-23
last_review: 2026-05-18
review_cadence_days: 30
risk_level: high
related_prd: docs/PRD-linear-80d4473e-e9c7-4d4d-9340-bc1193103a7d.md
related_action_plan: docs/ACTION_PLAN-linear-80d4473e-e9c7-4d4d-9340-bc1193103a7d.md
related_tasks:
  - tasks/tasks-linear-80d4473e-e9c7-4d4d-9340-bc1193103a7d.md
review_notes:
  - 2026-04-23: Docs-first child lane opened for CO-323 with docs-only scope; implementation, Linear state, workpad, validation, and PR lifecycle remain parent-owned.
  - 2026-04-23: The parent-provided source payload path was absent from this child checkout, so the issue description was recovered through a read-only Linear fetch with no mutation helpers.
  - 2026-05-18: CO-522 active-spec audit found 8 unchecked task checklist items, so this spec remains active and was reviewed for current lifecycle ownership rather than archived. Evidence: `out/linear-b642e879-ba50-45ef-b0d9-b059afa9e932-recovery/spec-preexpiry-local-classification.json`.
---

# Technical Specification

## Summary
- Objective: make docs freshness maintenance reuse stamped live owner issues by exact `canonical_owner_key` without broadening cohort-specific `CO-320` through the single global `policies.rolling_freshness_cohorts.owner_issue` field.
- Scope: owner-resolution contract for `docs:freshness:maintain`, exact-key reuse of stamped live owners, negative routing for unrelated stale cohorts, and terminal owner evidence-only behavior.
- Constraints: do not edit source implementation in this child lane; do not change `docs/docs-catalog.json`; do not weaken docs freshness, owner verification, or duplicate-prevention behavior.

## Issue-Shaping Contract
- User-request translation carried forward: future provider lanes need a way to reuse the stamped live owner issue for an exact canonical owner key without making that issue the owner for unrelated stale cohorts.
- Protected terms / exact artifact and surface names: `docs/docs-catalog.json`, `policies.rolling_freshness_cohorts.owner_issue`, `docs:freshness:maintain`, `owner_issue`, `owner_issue_action`, `canonical_owner_key`, `docs_freshness_candidate|doc_class:task_packet|path_family:tasks/tasks-*|last_review:2026-03-23|cadence_days:30`, `CO-320`, `CO-300`, `block_policy_over_budget`, `configured_owner_terminal`.
- Nearby wrong interpretations to reject: reopening `CO-300`, broadening `CO-320` into the owner for every stale historical cohort, weakening `docs:freshness` or `docs:freshness:maintain`, and rolling the full historical Mar 23 refresh back into `CO-319`.
- Explicit non-goals carried forward: no global owner repoint to `CO-320`, no policy weakening, no `CO-320` broadening beyond the exact Mar 23 `tasks/tasks-*` cohort, and no terminal `CO-300` live owner reuse.

## Parity / Alignment Matrix

| Surface | Current Truth | Reference Truth | Target Truth / Intended Delta | Out Of Scope |
| --- | --- | --- | --- | --- |
| `policies.rolling_freshness_cohorts.owner_issue` | Single global owner field can make unrelated cohorts inherit one issue. | Global policy metadata should not represent cohort-specific live ownership. | Implementation must not repoint this field to `CO-320`; it remains non-authoritative for exact live stamped owner reuse. | Redesigning all docs catalog policy metadata. |
| `canonical_owner_key` | `CO-320` is stamped for the exact Mar 23 `tasks/tasks-*` canonical key. | Existing duplicate-prevention semantics key owner reuse by exact marker. | `docs:freshness:maintain` resolves live stamped owners by exact key. | Fuzzy owner matching or title/body duplicate detection. |
| Unrelated candidate cohorts | Policy re-home attempt showed Jan 22 and adjacent Mar 23 cohorts under `owner_issue=CO-320`. | Unrelated cohorts need their own owner path or explicit no-owner/blocked policy result. | Those cohorts must not inherit `CO-320` unless their exact canonical key matches. | Refreshing unrelated historical cohorts in this lane. |
| Terminal owner metadata | `CO-300` is terminal evidence from prior owner paths. | Terminal owners are evidence only. | `CO-300` cannot be reused as the live owner path and cannot be the only way to clear `configured_owner_terminal`. | Reopening `CO-300`. |

## Readiness Gate
- Not done if: future provider lanes still need to broaden `CO-320` through the single global `owner_issue` field just to avoid `configured_owner_terminal`; `docs:freshness:maintain` still presents unrelated candidate cohorts as owned by `CO-320`; exact-key reuse cannot prevent duplicates; or the fix depends on reopening/reusing terminal `CO-300`.
- Pre-implementation issue-quality review evidence: CO-323 is the narrow follow-up after CO-319 proved that owner creation and safe owner routing are separate; the issue is broader than packet truthfulness and narrower than a historical docs freshness refresh.
- Safeguard ownership split: this child lane owns docs packet and registry mirrors only; parent owns source implementation, Linear/workpad state, validation, and PR lifecycle.

## Technical Requirements
- Functional requirements:
  - `docs:freshness:maintain` can surface a live stamped owner issue when the candidate cohort's `canonical_owner_key` exactly matches the stamped owner marker.
  - The Mar 23 `tasks/tasks-*` canonical key resolves to `CO-320` without changing `docs/docs-catalog.json` `policies.rolling_freshness_cohorts.owner_issue` to `CO-320`.
  - Unrelated candidate cohorts, including older Jan 22 90-day rows and adjacent Mar 23 `.agent/task`, `docs/ACTION_PLAN-*`, `docs/PRD-*`, and `docs/TECH_SPEC-*` cohorts, do not inherit `CO-320`.
  - Existing duplicate-prevention behavior remains intact so repeated lanes for the same exact canonical key reuse the stamped owner rather than creating duplicates.
  - Terminal configured owner metadata such as `CO-300` is reported as evidence only and cannot be reused as the live owner path.
- Non-functional requirements (performance, reliability, security): keep owner lookup deterministic and fail-closed; avoid fuzzy duplicate matching; avoid extra Linear mutations from maintenance-report generation; preserve docs freshness policy enforcement.
- Interfaces / contracts: maintenance output must remain truthful for `owner_issue`, `owner_issue_action`, `block_policy_over_budget`, and `configured_owner_terminal` while adding or using exact-key live owner resolution.

## Architecture & Data
- Architecture / design adjustments: add or reuse a small owner-resolution step that checks stamped live owner markers by exact `canonical_owner_key` before falling back to policy owner metadata; do not treat the global policy owner as a cohort-specific replacement.
- Data model changes / migrations: no repo data migration is required by the docs packet; parent implementation may reuse existing Linear issue marker sections.
- External dependencies / integrations: Linear issue marker lookup, docs freshness maintenance candidate cohort construction, and provider Linear canonical-owner duplicate-prevention behavior.

## Validation Plan
- Tests / checks:
  - Focused maintenance-output regression where the exact Mar 23 `tasks/tasks-*` key reports `owner_issue=CO-320`.
  - Negative regression proving unrelated Jan 22 and adjacent Mar 23 candidate cohorts do not report `owner_issue=CO-320`.
  - Terminal owner regression proving `CO-300` remains evidence only and cannot be the live owner path.
  - Duplicate-prevention regression proving repeated exact-key lanes reuse the existing stamped owner.
- Rollout verification: parent workpad and review handoff should cite focused output showing exact-key `CO-320` reuse and unrelated-cohort non-reuse.
- Monitoring / alerts: existing docs freshness report output should remain machine-readable and truthful about blocked policy status.

## Open Questions
- None blocking for docs-first. Parent implementation should confirm the smallest source seam for exact stamped owner lookup.

## Approvals
- Reviewer: parent provider worker.
- Date: 2026-04-23.
