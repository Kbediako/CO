---
id: 20260419-linear-5348c2fb-8897-48e3-a848-7831778b1b00
title: "CO workflow: restore Apr 19 spec/docs freshness baseline blocking unrelated handoffs"
status: in_progress
relates_to: docs/PRD-linear-5348c2fb-8897-48e3-a848-7831778b1b00.md
risk: high
owners:
  - Codex
last_review: 2026-06-17
review_notes:
  - 2026-06-17: CO-579 pre-expiry review kept this spec active-current; no verified terminal/archive evidence was established in this stream, CO-579 is the live non-terminal docs-freshness owner, and docs/spec gates remain unchanged.
  - 2026-05-18: CO-522 active-spec audit found 2 unchecked task checklist items, so this spec remains active and was reviewed for current lifecycle ownership rather than archived. Evidence: `out/linear-b642e879-ba50-45ef-b0d9-b059afa9e932-recovery/spec-preexpiry-local-classification.json`.
---

## Canonical Reference
- PRD: `docs/PRD-linear-5348c2fb-8897-48e3-a848-7831778b1b00.md`
- TECH_SPEC mirror: `docs/TECH_SPEC-linear-5348c2fb-8897-48e3-a848-7831778b1b00.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-5348c2fb-8897-48e3-a848-7831778b1b00.md`
- Task checklist: `tasks/tasks-linear-5348c2fb-8897-48e3-a848-7831778b1b00.md`
- Classification: `docs/findings/linear-5348c2fb-8897-48e3-a848-7831778b1b00-docs-freshness-classification.md`

## Summary
- Objective: restore the Apr 19 spec/docs freshness baseline while preserving gate signal and the boundary between repo-wide freshness debt and per-PR diff health.
- Scope:
  - CO-254 docs-first packet and mirrors
  - before/after `docs:freshness` and `spec-guard --dry-run` artifacts
  - classification of the `47` blocking stale docs and `17` stale spec rows
  - reviewed refresh or explicit owner disposition for the Apr 19 stale set
  - guidance and registry updates needed for future lanes to cite CO-254
- Constraints:
  - no CO-231 Doctor implementation changes
  - no CO-239 rework without fresh evidence
  - no gate weakening, mute, warning-only downgrade, or blind mass date bump
  - preserve CO-175 rolling debt visibility

## Issue-Shaping Contract
- User-request translation carried forward: this is the Apr 19 baseline owner for repo-wide spec/docs freshness debt discovered during CO-231 validation; it is not a Doctor readiness regression and not CO-231 diff-local work.
- Protected terms / exact artifact and surface names:
  - `docs:freshness`
  - `spec-guard`
  - `repo-wide freshness debt`
  - `per-PR diff health`
  - `Apr 19`
  - `last_review=2026-03-19`
  - `1299-1304`
  - `Task Packet`
  - `Task Mirror`
  - `Report Only`
  - `Active Guide`
  - `Public Guide`
  - `CO-231`
  - `CO-239`
  - `Doctor.test`
  - `machine-checkable evidence`
- Nearby wrong interpretations to reject:
  - "CO-231 should fix this because it hit the gate"
  - "CO-239 was incomplete"
  - "The docs freshness gate should be relaxed"
  - "The stale set can be mass-bumped without classification"
  - "CO-175 rolling rows should be hidden to make validation look clean"
- Explicit non-goals carried forward:
  - no Doctor implementation or `Doctor.test` expectation changes
  - no CO-220 stale attachment, intake projection, or `co-status` changes
  - no global disabling or broad weakening of `docs:freshness`, `docs:freshness:maintain`, or `spec-guard`

## Parity / Alignment Matrix
- Current truth:
  - `npm run docs:freshness` fails with `47` blocking stale docs and `221` rolling CO-175 entries
  - stale doc class split is `Task Packet=31`, `Task Mirror=6`, `Report Only=6`, `Active Guide=2`, and `Public Guide=2`
  - `node scripts/spec-guard.mjs --dry-run` reports `17` stale active spec frontmatters, all with `last_review=2026-03-19`
  - CO-231 Doctor validation is green and contributes `0` changed stale docs to this baseline blocker
- Reference truth:
  - CO-239 refreshed the Apr 18 Mar 18 `1289-1298` cohort and restored `docs:freshness` while preserving CO-175
  - prior baseline owner lanes record before/after artifacts, path classification, and reviewed refresh decisions
  - provider-worker gates preserve machine-visible rolling debt rather than forcing unrelated feature lanes to absorb it
- Target truth / intended delta:
  - CO-254 owns and clears the Apr 19 stale set or records an explicit reviewed owner policy
  - `docs:freshness` returns green without hiding the CO-175 rolling cohort
  - `spec-guard --dry-run` no longer reports the Apr 19 stale frontmatter set
  - future CO-231 or feature lanes can cite CO-254 as the repo-wide freshness owner
- Explicitly out-of-scope differences:
  - any Doctor readiness semantics
  - CO-239 rework without new contradictory evidence
  - policy-cap/window changes
  - unrelated historical cleanup outside the classified Apr 19 set

## Readiness Gate
- Not done if:
  - `docs:freshness` remains red on the Apr 19 stale-doc set without explicit owner action
  - `spec-guard --dry-run` still reports the `last_review=2026-03-19` stale spec set without accepted classification or policy
  - the closeout hides CO-175 rolling debt
  - before/after artifacts omit exact counts, class breakdown, path families, changed-path impact, or residual risk
- Pre-implementation issue-quality review evidence:
  - 2026-04-19: current-main baseline reproduction matches the issue's reported shape: `47` blocking stale docs, `221` CO-175 rolling rows, and `17` stale spec rows.
  - 2026-04-19: the micro-task path is ineligible because correctness depends on exact protected terms, parity/alignment, and owner-policy visibility.
- Safeguard ownership split:
  - parent lane owns all Linear state/workpad mutation, registry mirrors, source edits, validation, PR lifecycle, and merge
  - attempted same-issue child lane failed closed before startup because the parent provider-worker manifest lacks control-host provenance; this is recorded in the workpad and audit JSONL instead of fabricated delegation evidence

## Technical Requirements
- Functional requirements:
  1. Reproduce the Apr 19 `docs:freshness` and `spec-guard --dry-run` current-main state into issue-owned artifacts.
  2. Classify stale docs by class, path family, lineage, changed-path impact, and relation to CO-175 / CO-239.
  3. Classify stale specs by exact path, lineage/date, and disposition.
  4. Refresh, archive, reclassify, or explicitly policy-own the stale set without weakening gates.
  5. Update docs/task registry mirrors so CO-254 is the future citation point.
  6. Preserve CO-175 rolling cohort visibility and residual-risk notes.
- Non-functional requirements:
  - machine-checkable before/after evidence under `out/linear-5348c2fb-8897-48e3-a848-7831778b1b00/`
  - minimal metadata/content changes needed for the reviewed disposition
  - no new dependencies or runtime policy surface unless classification proves it necessary
- Interfaces / contracts:
  - `docs/docs-freshness-registry.json` stores docs freshness metadata for markdown docs
  - active specs under `tasks/specs/**` and `docs/design/specs/**` carry frontmatter `last_review` enforced by `spec-guard`
  - `docs/guides/docs-freshness-cohorts.md` documents date-boundary owner decisions
  - `docs/TASKS.md` and `tasks/index.json` are the registry mirrors for future workers and automation

## Architecture & Data
- Architecture / design adjustments:
  - no script or policy architecture change expected
  - use existing freshness metadata and spec frontmatter review dates as the source of truth
  - use the classification finding as the reviewed rationale for metadata refresh
- Data model changes / migrations:
  - add CO-254 entries to `tasks/index.json` and `docs/docs-freshness-registry.json`
  - refresh selected existing registry rows and spec frontmatter dates after classification
  - update `docs/guides/docs-freshness-cohorts.md` with Apr 19 owner evidence
- External dependencies / integrations:
  - Linear issue workpad and state via the repo-local `linear` helper
  - no app runtime proof or screenshot proof required

## Validation Plan
- Tests / checks:
  - `npm run docs:freshness` before and after
  - `node scripts/spec-guard.mjs --dry-run` before and after
  - `npm run docs:freshness:maintain` for provider-gate maintenance posture
  - `npm run docs:check`
  - full required floor before handoff: delegation guard, build, lint, test, docs freshness, repo stewardship, diff budget, standalone review, elegance pass, and PR drain
- Rollout verification:
  - after artifacts show `0` blocking stale docs and only owned rolling rows, if any
  - workpad records whether CO-231 can proceed without absorbing the baseline work
- Monitoring / alerts:
  - future Apr 20+ date-boundary rows should file or cite the appropriate owner issue instead of being folded into CO-231

## Open Questions
- None blocking. Current evidence supports reviewed refresh of the exact stale doc/spec set rather than a new rolling deferral or policy change.

## Approvals
- Reviewer: pending docs-review / standalone review.
- Date: 2026-04-19
