---
id: 20260505-linear-937a5264-87ce-47fa-a932-f940d70c6600
title: "CO-484 keep co-status running row workspace paths issue-local"
relates_to: docs/PRD-linear-937a5264-87ce-47fa-a932-f940d70c6600.md
risk: high
owners:
  - Codex
last_review: 2026-05-05
related_action_plan: docs/ACTION_PLAN-linear-937a5264-87ce-47fa-a932-f940d70c6600.md
task_checklists:
  - tasks/tasks-linear-937a5264-87ce-47fa-a932-f940d70c6600.md
---

# TECH_SPEC - CO-484 keep co-status running row workspace paths issue-local

## Canonical Reference
- PRD: `docs/PRD-linear-937a5264-87ce-47fa-a932-f940d70c6600.md`
- TECH_SPEC mirror: `docs/TECH_SPEC-linear-937a5264-87ce-47fa-a932-f940d70c6600.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-937a5264-87ce-47fa-a932-f940d70c6600.md`
- Task checklist: `tasks/tasks-linear-937a5264-87ce-47fa-a932-f940d70c6600.md`
- Linear issue: `CO-484` / https://linear.app/asabeko/issue/CO-484

## Summary
- Objective: register CO-484 before it leaves `Backlog` and define the implementation contract for row-local `workspace_path` authority in `co-status --format json`.
- Scope:
  - packet files, task mirrors, `tasks/index.json`, `docs/TASKS.md`, and docs freshness registry rows in this packet lane
  - parent-owned implementation across control-host status projection and presenter/API/UI surfaces
- Constraints:
  - no implementation files in this packet lane
  - no provider admission, queue selection, docs freshness, PR check, or CO-456/CO-474 implementation behavior changes
  - no Linear transition or PR lifecycle work in this packet lane

## Issue-Shaping Contract
- User-request translation carried forward: CO-484 is a control-host/status projection bug where `.running[]` row identity can remain correct while `workspace_path` is borrowed from a neighboring selected provider worker. The parent implementation must preserve row-local proof and fallback provenance without letting selected-run or last-row workspace data override another row.
- Protected terms / exact artifact and surface names:
  - `co-status --format json`
  - `.running[]`
  - `workspace_path`
  - `provider_issue_rehydrated_active_run`
  - `issue_id`
  - `task_id`
  - `run_id`
  - concurrent provider workers
  - row-local proof
  - `CO-456`
  - `CO-474`
  - `CO-398`
- Nearby wrong interpretations to reject:
  - treating the incident as a provider-worker code bug in `CO-456` or `CO-474`
  - treating the mismatch as harmless display-only typo
  - hiding or removing `workspace_path`
  - weakening fallback provenance fields
  - collapsing multiple active rows into only the selected issue
- Explicit non-goals carried forward:
  - no provider-worker admission limits or queue selection changes
  - no docs freshness, PR check, or adjacent implementation behavior changes
  - no broad control-host status-authority refactor unless the parent proves a focused repair cannot be safe

## Parity / Alignment Matrix

| Surface | Current Truth | Reference Truth | Target Truth | Explicitly Out Of Scope |
| --- | --- | --- | --- | --- |
| `co-status --format json` `.running[]` | Row identity can be correct while `workspace_path` belongs to another active row. | Status output must preserve live authority and retained proof without masking disagreement. | Every row derives `workspace_path` from its own issue/task/run/proof. | Hiding the field or selected-only output. |
| `provider_issue_rehydrated_active_run` | Rehydrated fallback rows preserve active-run visibility. | Fallback rows must stay source-labeled and cannot override row identity. | Rehydrated rows preserve provenance and row-local workspace authority. | Removing fallback audit fields without replacement. |
| JSON/API/UI projections | Machine and UI consumers depend on the same status truth. | CLI/API/UI must not silently diverge. | JSON/API/UI all preserve row-local workspace coupling. | Human-readable-only fix. |
| Concurrent provider workers | Selected and rehydrated rows can coexist. | Multi-row projection must preserve per-row authority. | Regression covers at least two active provider runs. | Queue/admission behavior changes. |

## Readiness Gate
- Not done if:
  - CO-484 can leave `Backlog` without packet and registry mirrors
  - concurrent active provider workers can still produce a `.running[]` row whose `workspace_path` UUID disagrees with row `issue_id`, `task_id`, or `run_id`
  - `co-status --format json` falls back to selected-run or last-row workspace data instead of row-local run/proof data
  - only human-readable output is fixed while JSON/API/UI remains vulnerable
  - no regression covers at least two active rehydrated provider runs
  - fallback provenance is hidden or weakened
- Pre-implementation issue-quality review evidence:
  - 2026-05-05: micro-task path is unavailable because correctness depends on exact protected terms, fallback/seam behavior, and multi-row status projection surfaces.
  - 2026-05-05: issue scope is narrow and actionable once packet mirrors exist; implementation remains row-local status projection only.
- Safeguard ownership split:
  - this packet lane owns packet and registry mirrors only
  - parent owns live Linear reconciliation, docs-review, implementation, validation, workpad, PR lifecycle, and merge

## Technical Requirements
- Functional requirements:
  - Create the CO-484 PRD, canonical TECH_SPEC, TECH_SPEC mirror, ACTION_PLAN, task checklist, and `.agent` mirror.
  - Add the canonical `tasks/index.json` item for `20260505-linear-937a5264-87ce-47fa-a932-f940d70c6600`.
  - Add one `docs/TASKS.md` snapshot for CO-484.
  - Add docs freshness registry rows for the six CO-484 packet/checklist surfaces.
  - Preserve protected terms and reject adjacent wrong interpretations.
- Parent-owned functional requirements:
  - Add focused regression coverage with at least two active provider runs where one row is selected and another is rehydrated.
  - Ensure `.running[]` `workspace_path` derives from row-local issue/task/run/proof evidence.
  - Preserve existing fallback provenance fields with explicit source labels.
  - Verify `co-status --format json` no longer cross-contaminates workspace paths under concurrent workers.
  - Include JSON/API/UI or presenter coverage so the fix is not text-only.
- Non-functional requirements:
  - Fail closed or degrade truthfully when row-local workspace authority is unavailable.
  - Preserve audit-visible proof without letting proof override row identity.
  - Keep the implementation minimal and scoped to status projection.
- Interfaces / contracts:
  - `tasks/index.json` remains valid JSON.
  - `docs/docs-freshness-registry.json` remains valid JSON.
  - `docs/TASKS.md` keeps one current CO-484 snapshot.
  - `co-status --format json` `.running[]` output preserves row-local workspace authority.

## Fallback Expiry / Refactor Decision
- Applies to fallback, compatibility, legacy, stale, cached, break-glass, or minor-seam behavior? `Yes`.
- Required decision table:

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `control-host status surfaces` | Selected-run or last-row `workspace_path` reused for a neighboring `.running[]` row. | remove fallback | CO-484 | A rehydrated running row lacks direct workspace authority and projection borrows selected/neighbor row data. | Observed 2026-05-02 | N/A after removal | N/A after removal | Row-local multi-run regression proves each `.running[]` row keeps its own `workspace_path`. | Focused status projection tests plus `co-status --format json` fixture. |
| `provider_issue_rehydrated_active_run` fallback rows | Rehydrated active-run rows preserve visibility when live state is incomplete. | justify retaining fallback | CO-398 / CO-484 | Live provider state is incomplete but retained run/proof evidence exists. | CO-398 lineage, reviewed 2026-05-05 | 2026-05-26 | Non-expiring only for source-labeled audit proof that cannot override row-local authority | Remove only if live authority fully replaces retained proof visibility. | JSON/API/UI projection tests preserve provenance and row-local coupling. |
| `control-host status surfaces` | Compatibility/proof provenance fields attached to status output. | justify retaining fallback | CO-398 | Operators need retained proof/audit evidence alongside live authority. | CO-398 lineage, reviewed 2026-05-05 | 2026-05-26 | Non-expiring durable audit contract while source-labeled | Replace only with an equivalent source-labeled proof model. | Tests prove provenance survives and cannot overwrite row identity. |

- For `justify retaining fallback`, contract name: row-local status proof and source-label projection.
- Owning surface: `control-host status surfaces`.
- Steady-state proof expectation: parent implementation proves source labels, row-local authority, retained proof, degraded reasons, and `/ui/data.json` status truth stay distinct.
- Large-refactor check: a large refactor is not required for the packet lane. Parent should escalate only if a focused row-local projection repair would add another hidden authority source.

## Architecture & Data
- Architecture / design adjustments:
  - none in this packet lane
  - parent should centralize row-local workspace authority before output rendering where practical
- Data model changes / migrations:
  - none in this packet lane
  - parent may add explicit row-local authority/degraded metadata if required for truthful status output
- External dependencies / integrations:
  - Linear issue context and provider-intake state
  - retained run manifests/proofs
  - `co-status --format json`
  - control-host API and `/ui/data.json`

## Validation Plan
- Packet checks:
  - parse `tasks/index.json`
  - parse `docs/docs-freshness-registry.json`
  - protected-term scan over packet files and `docs/TASKS.md`
  - `npm run docs:check`
  - `npm run docs:freshness`
  - `node scripts/spec-guard.mjs --dry-run`
  - scoped diff/status check
- Parent implementation checks:
  - docs-review before implementation
  - focused status projection regression with selected plus rehydrated active rows
  - focused `co-status --format json` fixture or CLI coverage
  - API/UI or presenter coverage for the same row-local authority
  - normal validation floor and review loop
- Rollout verification:
  - parent records evidence that retained fallback provenance remains visible and source-labeled
  - parent proves row-local proof cannot silently borrow selected-row `workspace_path`

## Open Questions
- Which projection helper currently copies selected or last-row workspace data into rehydrated rows?
- What degraded reason should be emitted if row-local workspace proof is absent and cannot be derived from run/manifest evidence?

## Approvals
- Reviewer: CO-484 packet issue-quality review, pending
- Date: 2026-05-05
