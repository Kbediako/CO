---
id: 20260502-linear-b7381274-d6eb-4886-86f4-3976b154c26d
title: CO-465 Doctor gpt-5.5 defaults full-suite validation gate
relates_to: docs/PRD-linear-b7381274-d6eb-4886-86f4-3976b154c26d.md
risk: high
owners:
  - Codex
last_review: 2026-06-17
review_notes:
  - 2026-06-17: CO-579 pre-expiry review kept this spec active-current; no verified terminal/archive evidence was established in this stream, CO-579 is the live non-terminal docs-freshness owner, and docs/spec gates remain unchanged.
related_action_plan: docs/ACTION_PLAN-linear-b7381274-d6eb-4886-86f4-3976b154c26d.md
task_checklists:
  - tasks/tasks-linear-b7381274-d6eb-4886-86f4-3976b154c26d.md
---

## Canonical Reference
- PRD: `docs/PRD-linear-b7381274-d6eb-4886-86f4-3976b154c26d.md`
- Canonical task spec: `tasks/specs/linear-b7381274-d6eb-4886-86f4-3976b154c26d.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-b7381274-d6eb-4886-86f4-3976b154c26d.md`
- Task checklist: `tasks/tasks-linear-b7381274-d6eb-4886-86f4-3976b154c26d.md`
- `.agent` mirror: `.agent/task/linear-b7381274-d6eb-4886-86f4-3976b154c26d.md`
- Source anchor: `ctx:sha256:16b4be52eda49eabac589fc41aca4b843e4a04597237ee4143aa87a4ae0c4603#chunk:c000001`
- Child lane manifest: `.runs/linear-b7381274-d6eb-4886-86f4-3976b154c26d-co465-docs-packet/cli/2026-05-01T19-14-31-418Z-9a2afec6/manifest.json`

## Summary
- Objective: register the CO-465 docs-first packet for the Doctor `gpt-5.5 defaults` full-suite timeout blocker.
- Scope: docs packet, task mirrors, `tasks/index.json`, `docs/TASKS.md`, and `docs/docs-freshness-registry.json` only.
- Constraints: preserve exact protected terms, reject `CO-456` spec-guard drift, avoid Doctor policy weakening, and leave implementation plus validation to the parent lane.

## Issue-Shaping Contract
- User-request translation carried forward: CO-465 exists because the provider-worker validation gate is blocked by `npm run test` timing out in `orchestrator/tests/Doctor.test.ts`, even though the isolated rerun passes. The key Doctor policy case is `accepts unmarked gpt-5.5 defaults when access is verified`, and parent work must preserve that policy rather than skip or dilute it.
- Protected terms / exact artifact and surface names:
  - `npm run test`
  - `orchestrator/tests/Doctor.test.ts`
  - `accepts unmarked gpt-5.5 defaults when access is verified`
  - `gpt-5.5 defaults`
  - `full-suite timeout`
  - `isolated rerun passes`
  - `provider-worker validation gate`
  - `CO-456`
- Nearby wrong interpretations to reject:
  - this is a `CO-456` spec-guard behavior change
  - Doctor model-default policy assertions should be weakened to clear validation
  - `orchestrator/tests/Doctor.test.ts` should be skipped
  - isolated rerun success is enough while the full suite times out
  - a blanket global timeout increase is sufficient without explaining the full-suite-only slow path
- Explicit non-goals carried forward:
  - no source/test/config edits from this child lane
  - no Linear/GitHub/workpad/PR lifecycle mutation
  - no full repo validation suite from this child lane
  - no unrelated stale-doc or spec-guard policy cleanup

## Parity / Alignment Matrix

- Current truth:
  - `npm run test` blocks on a `full-suite timeout` in `orchestrator/tests/Doctor.test.ts`
  - the isolated Doctor rerun passes
  - the protected case is `accepts unmarked gpt-5.5 defaults when access is verified`
  - the provider-worker validation gate remains blocked until the full-suite path is explained and stabilized
- Reference truth:
  - full-suite validation should stay authoritative for provider-worker handoff
  - focused rerun success is necessary preservation evidence, not a substitute for the full suite
  - Doctor should accept `gpt-5.5 defaults` only when access is verified
  - `CO-456` owns spec-guard behavior, not this Doctor validation lane
- Target truth / intended delta:
  - parent identifies the full-suite-only slow path
  - parent lands a bounded fix that preserves the Doctor model-default assertion
  - `npm run test` and the isolated Doctor rerun both pass after the fix
  - no spec-guard behavior or policy assertion is weakened
- Explicitly out-of-scope differences:
  - source/test edits by this child lane
  - CO-456 spec-guard changes
  - global timeout-only masking
  - Doctor coverage deletion or skip markers

## Readiness Gate
- Not done if:
  - `npm run test` still times out in `orchestrator/tests/Doctor.test.ts`
  - the isolated Doctor rerun is the only green evidence
  - `accepts unmarked gpt-5.5 defaults when access is verified` is removed, skipped, or weakened
  - the full-suite-only slow path is not explained
  - the fix changes `CO-456` spec-guard behavior
  - the solution relies only on a blanket global timeout increase
- Pre-implementation issue-quality review evidence:
  - 2026-05-02: approved for docs packet bootstrap. The micro-task path is not appropriate because correctness depends on exact protected wording, validation-gate traceability, and negative boundaries around Doctor policy and `CO-456`.
  - 2026-05-02: referenced source payload path was unavailable in the child checkout, so the parent prompt and source anchor are the operative issue-shaping contract.
- Safeguard ownership split:
  - child lane owns only the declared docs and registry files
  - parent owns source inspection, tests, docs-review, validation, Linear/workpad state, PR lifecycle, and final patch integration

## Technical Requirements
1. Create the CO-465 PRD, TECH_SPEC mirror, canonical task spec, ACTION_PLAN, task checklist, and `.agent` mirror.
2. Register task id `20260502-linear-b7381274-d6eb-4886-86f4-3976b154c26d` in `tasks/index.json`.
3. Add a current CO-465 snapshot to `docs/TASKS.md`.
4. Add docs freshness rows for all six packet/mirror files.
5. Preserve all protected terms exactly.
6. Make `CO-456` spec-guard behavior an explicit non-owned boundary.
7. Require parent evidence for both the full-suite `npm run test` path and the isolated Doctor rerun.
8. Reject global timeout-only handling unless the full-suite-only slow path is explained and bounded.

## Architecture & Data
- Architecture / design adjustments: none in this child lane. Parent implementation should localize the timeout cause around Doctor test setup, model-default access verification, temporary environment handling, or other proven full-suite-only pressure.
- Expected parent implementation surfaces:
  - `orchestrator/tests/Doctor.test.ts`
  - Doctor access-verification helpers or model-default policy code only if reproduction proves they are involved
  - provider-worker validation evidence surfaces that record the full-suite and isolated rerun split
- Data model changes / migrations: none expected from the docs packet.

## Validation Plan
- Child-lane checks:
  - protected-term scan across the six packet/mirror files
  - JSON parse check for `tasks/index.json` and `docs/docs-freshness-registry.json`
  - scoped `git diff --check --` over declared files
  - changed-file review to confirm declared file scope only
- Parent-owned checks:
  - reproduce `npm run test` full-suite timeout before the fix when feasible
  - focused `orchestrator/tests/Doctor.test.ts` rerun remains green
  - focused assertion coverage for `accepts unmarked gpt-5.5 defaults when access is verified`
  - final provider-worker validation gate and review handoff checks
- Rollout verification: parent records final full-suite and isolated rerun evidence in task mirrors, workpad, PR, and review handoff.

## Risks
- Risk: isolated rerun success is mistaken for full provider-worker readiness.
  - Mitigation: acceptance requires `npm run test` validation or an explicit parent-owned blocker classification.
- Risk: Doctor model-default policy is weakened to clear a timeout.
  - Mitigation: protected terms and Not Done If sections require preserving `accepts unmarked gpt-5.5 defaults when access is verified`.
- Risk: the lane drifts into `CO-456` spec-guard behavior.
  - Mitigation: CO-456 is a rejected interpretation and explicit out-of-scope boundary.
- Risk: a global timeout increase hides the root cause.
  - Mitigation: timeout changes alone are insufficient unless the full-suite-only slow path is explained and bounded.

## Completion Criteria
- CO-465 packet and mirrors exist in declared paths.
- `tasks/index.json` and `docs/docs-freshness-registry.json` parse as JSON.
- Protected-term scan confirms Doctor/defaults/validation-gate wording.
- Scoped diff review shows no edits outside declared file scope.
- Parent can continue with docs-review, implementation, validation, and lifecycle handoff.

## Parent Implementation Update
- 2026-05-02: Parent classified the full-suite-only slow path as ambient real Codex CLI feature/cloud probes in Doctor model-default tests that already mocked debug-model access through `CODEX_ORCHESTRATOR_DEBUG_MODELS_JSON`.
- 2026-05-02: Parent fixed the slow path in `orchestrator/tests/Doctor.test.ts` by scoping those model-default tests to the existing fake Codex binary, preserving the `gpt-5.5 defaults` access-verification assertions and avoiding any CO-456 spec-guard behavior changes.
- 2026-05-02: Validation evidence: focused Doctor rerun passed after the patch, full `orchestrator/tests/Doctor.test.ts` passed all 60 tests, and full `npm run test` passed 358 files / 5210 tests.

## 2026-05-25 Review
- CO-585 pre-expiry review checked the Doctor `gpt-5.5 defaults` validation contract against current `orchestrator/tests/Doctor.test.ts`, including the preserved `accepts unmarked gpt-5.5 defaults when access is verified` case, and current permission-profile diagnostics. The spec remains accurate and active; no content change beyond truthful review metadata is required.

## Approvals
- Docs packet child lane: produced in this workspace for parent patch export.
- Parent docs-review and implementation validation: pending parent ownership.
- Date: 2026-05-02
