---
id: 20260401-linear-a34ce3f3-8e78-40f7-aabd-9e510572323e
title: 'CO: clear repo-wide docs:freshness baseline blocking review handoffs'
status: done
owner: Codex
created: 2026-04-01
last_review: 2026-05-02
review_cadence_days: 30
risk_level: high
related_prd: docs/PRD-linear-a34ce3f3-8e78-40f7-aabd-9e510572323e.md
related_action_plan: docs/ACTION_PLAN-linear-a34ce3f3-8e78-40f7-aabd-9e510572323e.md
related_tasks:
  - tasks/tasks-linear-a34ce3f3-8e78-40f7-aabd-9e510572323e.md
review_notes:
  - 2026-05-02: CO-479 live Linear audit confirmed CO-63 is Done (state_type=completed, updated_at=2026-04-14T01:14:40.810Z) with merged PR #342 attached; this completed-lane spec is reclassified to inactive `done` under canonical owner key `spec-guard:active-specs:last_review=2026-04-01` so historical implementation evidence remains preserved without staying in active-spec freshness.
  - 2026-04-01: Opened from Linear issue `CO-63` in the provider-worker workspace after confirming the live CO workflow states with `linear issue-context`; the team exposes `In Progress`, `In Review`, `Merging`, and `Rework`, and the issue was moved from `Ready` to `In Progress`.
  - 2026-04-01: The workspace started detached at `028c85ccd7b342221322ee276e0e1ef202fa7795` and was switched to branch `linear/co-63-clear-docs-freshness-baseline` before repo edits.
  - 2026-04-01: Initial reproduction shows `npm run docs:freshness` failing immediately with `171` stale docs while the issue still has no attached PR; the blocker is repo-wide baseline debt rather than a `CO-62` implementation defect.
  - 2026-04-01: The stale set currently appears seeded around `last_review: 2025-12-31`; the lane will first classify which entries are historical implementation docs suitable for archival or reclassification under existing policy versus still-active guidance that needs a real review-date refresh.
  - 2026-04-01: Delegation will be satisfied with an audited `linear child-stream --pipeline docs-review` run before implementation rather than a blanket delegation override.
  - 2026-04-01: Pre-implementation approval: the issue is already scoped correctly as a repo-wide docs-freshness baseline repair lane. Implementation should stay bounded to registry truth, archive-policy application, and active-doc review rather than widening into `CO-62` feature work or broader runtime changes.
  - 2026-04-01: The final repaired classification was verified directly against the original child docs-review stale report: the `171` stale entries now map to `77` archived historical docs and `94` active docs in the current registry. That active set includes `38` docs restored after task-status review showed their tasks remain non-terminal in `tasks/index.json`, plus the `0303` autonomy PRD / tech spec / action plan restored locally because the overview docs still route readers through that still-open `approved` planning lane.
  - 2026-04-01: The final `co-63-docs-review-final` child rerun reached green `docs:check` and `docs:freshness` stages, but its nested `npm run review` stage stalled without a terminal wrapper verdict. This lane will treat that child rerun as a truthful docs-review fallback record and rely on the parent-run standalone review for the required handoff review evidence.
  - 2026-04-01: Parent-run standalone review surfaced one more truthful-baseline defect after the archive sweep: active overview docs and examples still pointed at archived primary docs. The final remediation removed those active-source references instead of restoring the archived docs wholesale.
  - 2026-04-01: The parent-run standalone review rerun finished with `status: succeeded` and `review_outcome: bounded-success` in `.runs/linear-a34ce3f3-8e78-40f7-aabd-9e510572323e/cli/2026-04-01T07-00-46-013Z-488d525d/review/telemetry.json`. This lane treats the `relevant-reinspection-dwell` termination boundary as successful bounded review completion per provider-worker policy, not a blocker.
  - 2026-04-01: Final local validation on the repaired tree is green for delegation guard, spec guard, build, lint, test, docs check, docs freshness, and diff-budget override acceptance; `pack:smoke` was intentionally skipped because the final diff is docs-only and does not touch downstream CLI/package/skills/review-wrapper surfaces.
---

# Technical Specification

## Context

`CO-62` is otherwise ready for review handoff, but provider-worker closeout cannot move truthfully because the repository baseline already fails `npm run docs:freshness` on unrelated stale docs. The issue explicitly rejects narrowing the repair to the `CO-62` packet or waiving the validation gate. The repair therefore belongs in its own docs-first lane that restores a truthful repo-wide baseline.

## Requirements

1. Reproduce the current `docs:freshness` failure in this workspace and preserve the current stale-entry count and report path.
2. Register the `CO-63` docs-first packet and mirror it across `tasks/index.json`, `docs/TASKS.md`, `docs/docs-freshness-registry.json`, `tasks/tasks-*.md`, and `.agent/task/*.md`.
3. Classify the stale set into:
   - still-active guidance or reference docs that need an explicit review refresh
   - completed or otherwise non-active implementation docs that should be archived or reclassified truthfully
4. Land the smallest truthful remediation that makes `npm run docs:freshness` pass without weakening the gate or hiding active stale docs.
5. Keep the lane clearly separate from `CO-62` implementation logic and unrelated runtime or workflow changes.
6. Preserve provider-worker workflow evidence: child docs-review manifest, workpad refreshes, standalone review, elegance review, PR attachment, and review-handoff prerequisites.

## Current Truth

- Initial reproduction in this workspace failed with `3160 docs, 3170 registry entries` and `171` stale docs.
- The final repaired state now passes with `docs:freshness OK - 3166 docs, 3176 registry entries`.
- The original `171`-entry stale set now maps to `77` archived docs and `94` active docs in the current registry, with no missing paths from the original report.
- Every stale entry sampled during reproduction carried `last_review: 2025-12-31`, which confirmed an old seeded baseline rather than a new per-lane drift signal.
- The stale set spanned `.agent/`, `.ai-dev-tasks/`, `docs/`, and `tasks/`, so this was never limited to one issue packet family.
- Existing archive automation already exists for completed implementation docs and updates the docs-freshness registry when it archives them.
- There is currently no attached PR on the issue.

## Validation Plan

- Run an audited child `docs-review` stream after the packet exists and record the manifest plus truthful fallback if the review command fails without a classified wrapper boundary.
- Use repo-local tools to classify the stale set and verify the repair:
  - `npm run docs:freshness`
  - `node scripts/implementation-docs-archive.mjs --dry-run`
- Run the full required validation floor for any non-trivial final diff:
  - `node scripts/delegation-guard.mjs`
  - `node scripts/spec-guard.mjs --dry-run`
  - `npm run build`
  - `npm run lint`
  - `npm run test`
  - `npm run docs:check`
  - `npm run docs:freshness`
  - `node scripts/diff-budget.mjs`
  - `FORCE_CODEX_REVIEW=1 npm run review`
  - `npm run pack:smoke` when the final diff is not docs-only (skip it for docs-only closeouts like this lane)

## Manifest Evidence

- Linear issue read: packaged `linear issue-context --issue-id a34ce3f3-8e78-40f7-aabd-9e510572323e`
- Linear transition: packaged `linear transition --issue-id a34ce3f3-8e78-40f7-aabd-9e510572323e --state "In Progress"`
- Active workpad comment: packaged `linear upsert-workpad --issue-id a34ce3f3-8e78-40f7-aabd-9e510572323e --body-file /tmp/co-63-workpad.md`
- Current worker manifest: `.runs/linear-a34ce3f3-8e78-40f7-aabd-9e510572323e/cli/2026-04-01T07-00-46-013Z-488d525d/manifest.json`
- Parent review telemetry: `.runs/linear-a34ce3f3-8e78-40f7-aabd-9e510572323e/cli/2026-04-01T07-00-46-013Z-488d525d/review/telemetry.json`
