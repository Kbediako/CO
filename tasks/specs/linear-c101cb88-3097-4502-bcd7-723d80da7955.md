---
id: 20260416-linear-c101cb88-3097-4502-bcd7-723d80da7955
title: CO workflow: refresh Apr 16 docs:freshness stale cohort blocking handoffs
status: done
owner: Codex
created: 2026-04-16
last_review: 2026-05-17
review_cadence_days: 30
risk_level: high
related_prd: docs/PRD-linear-c101cb88-3097-4502-bcd7-723d80da7955.md
related_action_plan: docs/ACTION_PLAN-linear-c101cb88-3097-4502-bcd7-723d80da7955.md
related_tasks:
  - tasks/tasks-linear-c101cb88-3097-4502-bcd7-723d80da7955.md
review_notes:
  - 2026-04-16: `linear issue-context` showed CO-201 in `Ready`; the issue was moved to `In Progress` before active coding.
  - 2026-04-16: Pre-turn matrix found a safe docs-scoped child lane for classification artifact ownership, and `linear parallelization` recorded `parallelize_now` / `independent_scope_available`.
  - 2026-04-16: Current `origin/main` baseline is `79cb4a193d99b08f989f5e9b51ebe79edaca8b0a`.
  - 2026-04-16: Baseline reproduction saved under `out/linear-c101cb88-3097-4502-bcd7-723d80da7955/before/`: `spec-guard` exit 1 on `tasks/specs/1220...` with `last_review=2026-03-16`, `docs:freshness` exit 1 with 192 blocking stale rows and 221 CO-175 rolling rows, and `docs:freshness:maintain` exit 1 with `freshness_decision=block_diff_local` plus over-budget candidate capacity.
  - 2026-05-17: CO-543 strict spec-guard audit reclassified this stale Apr 16 row inactive done; live Linear evidence verified CO-201 is Done/completed. No completed_at was inferred or fabricated.
---

## CO-382 Fallback Decision Table
- Large-refactor check: no new fallback mechanism or guard split is warranted; CO-543 removes lifecycle metadata drift while keeping the existing strict spec-guard and docs-freshness ownership surfaces authoritative.
- Minor-seam decision: the bounded seam is acceptable because completed packet rows are reclassified inactive or archived with evidence, and future active specs surface through the existing docs-freshness-maintain owner-action route before hard expiry.

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Active spec lifecycle freshness | Completed task/spec packet rows remained active and recurred as stale spec-guard debt | remove fallback | CO-543 | Apr 16 terminal packet rows reached the 30-day strict spec-guard boundary | 2026-04-16 | 2026-05-17 | N/A after removal | Completed rows are inactive done; related terminal packet registry rows are archived; future active specs surface pre-expiry owner action | node scripts/spec-guard.mjs; npm run docs:check; focused docs-freshness/spec-guard tests; diff-budget override |

# Technical Specification

## Context
CO-201 owns the Apr 16 docs freshness rollover baseline. The failure is repo-wide and historical: current `origin/main` has no missing registry rows, missing files, invalid registry entries, or uncatalogued docs, but the Apr 16 stale cohort blocks unrelated handoffs.

CO-175 remains the owner for its Apr 14 rolling freshness cohort. CO-201 must not hide or broaden that rolling policy. The Apr 16 blocking rows need direct reviewed action because combining them with CO-175 would exceed the current rolling caps.

## Current Truth
- `node scripts/spec-guard.mjs` fails on `tasks/specs/1220-coordinator-symphony-aligned-standalone-review-run-review-orchestration-boundary-reassessment.md`.
- `npm run docs:freshness` reports:
  - `3909` docs scanned
  - `3912` registry entries
  - `192` blocking stale entries
  - `221` rolling cohort entries
  - no missing/invalid/uncatalogued rows
- The blocking stale classes are only:
  - Task Packet: `137`
  - Task Mirror: `27`
  - Report Only: `28`
- The blocking date cohorts are:
  - `last_review=2026-01-15`, `cadence_days=90`, `age_days=91`: 2 rows
  - `last_review=2026-03-16`, `cadence_days=30`, `age_days=31`: 190 rows
- `docs:freshness:maintain` reports `freshness_decision=block_diff_local`; `policy_capacity_status` is over budget at `413` candidate entries and `10` candidate cohorts against caps `300` and `2`.

## Requirements
1. Preserve `docs:freshness`, `spec-guard`, and `docs:freshness:maintain` fail-closed behavior.
2. Classify the Apr 16 blocking cohort with machine-readable evidence before refreshing metadata.
3. Refresh only rows that were reviewed as truthful historical Task Packet, Task Mirror, or Report Only records, unless evidence proves archive or reclassification is smaller.
4. Keep CO-175 policy ownership, window, caps, and baseline cohort unchanged.
5. Record the Apr 16 decision in `docs/guides/docs-freshness-cohorts.md` or another docs-freshness policy surface.
6. Keep current CO-201 docs-first packet and registry rows fresh.
7. Re-run the required guard trio after the fix and save before/after artifacts.

## Issue-Shaping Contract
- User-request translation: restore the repo-wide docs baseline so unrelated feature/version lanes can pass validation without absorbing historical stale packet maintenance.
- Protected terms: `docs:freshness`, `spec-guard`, `docs:freshness:maintain`, `block_diff_local`, `block_policy_over_budget`, `CO-175`, `rolling freshness cohorts`, `Task Packet`, `Task Mirror`, `Report Only`, `last_review=2026-03-16`, `current diff health`, `repo-wide docs baseline`.
- Nearby wrong interpretations to reject:
  - fold this into CO-195's Codex CLI posture decision
  - weaken guard policy
  - silently mass-bump dates without a classification note
  - increase rolling caps
  - claim cloud-canary readiness from this lane

## Implementation Plan
1. Preserve before artifacts:
   - `before/spec-guard.log`
   - `before/docs-freshness.json`
   - `before/docs-freshness-maintenance.json`
2. Generate a classification artifact for the Apr 16 stale entries.
3. Update `docs/docs-freshness-registry.json` only for reviewed Apr 16 stale rows, or archive/reclassify only if evidence proves that is smaller and truthful.
4. Update `docs/guides/docs-freshness-cohorts.md` with Apr 16 decision provenance and CO-175 preservation.
5. Update task/checklist mirrors and workpad with artifacts and validation.
6. Run validation and review gates before PR handoff.

## Data / Interfaces
- `docs/docs-freshness-registry.json`: freshness metadata source of truth.
- `docs/docs-catalog.json`: rolling policy source of truth.
- `docs/guides/docs-freshness-cohorts.md`: human policy/provenance surface.
- `scripts/docs-freshness.mjs`: docs freshness report generator.
- `scripts/docs-freshness-maintain.mjs`: provider-gate maintenance decision generator.
- `scripts/spec-guard.mjs`: active spec freshness guard.

## Validation Plan
- `node scripts/spec-guard.mjs`
- `npm run docs:freshness`
- `BASE_SHA=origin/main npm run docs:freshness:maintain`
- `node scripts/delegation-guard.mjs`
- `npm run build`
- `npm run lint`
- `npm run test`
- `npm run docs:check`
- `npm run repo:stewardship`
- `node scripts/diff-budget.mjs`
- Manifest-backed `codex-orchestrator review` / `npm run review` under `FORCE_CODEX_REVIEW=1`
- Explicit elegance/minimality pass

## Not Done If
- `spec-guard` still fails on the Apr 16 stale spec cohort.
- `docs:freshness` still reports blocking stale docs without an explicit reviewed owner/action.
- `docs:freshness:maintain` still reports a blocking decision for unrelated clean feature diffs.
- The fix is only a blind date bump with no cohort classification.
- CO-195 or another unrelated lane still needs to refresh historical docs to pass review handoff.
