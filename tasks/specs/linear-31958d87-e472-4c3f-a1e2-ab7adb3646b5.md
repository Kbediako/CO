---
id: 20260514-linear-31958d87-e472-4c3f-a1e2-ab7adb3646b5
title: CO-538 verify create-follow-up labels with live post-mutation reads
relates_to: docs/PRD-linear-31958d87-e472-4c3f-a1e2-ab7adb3646b5.md
risk: medium
owners:
  - Codex
last_review: 2026-05-14
related_action_plan: docs/ACTION_PLAN-linear-31958d87-e472-4c3f-a1e2-ab7adb3646b5.md
task_checklists:
  - tasks/tasks-linear-31958d87-e472-4c3f-a1e2-ab7adb3646b5.md
---

# TECH_SPEC - CO-538 verify create-follow-up labels with live post-mutation reads

## Canonical Reference
- PRD: `docs/PRD-linear-31958d87-e472-4c3f-a1e2-ab7adb3646b5.md`
- TECH_SPEC mirror: `docs/TECH_SPEC-linear-31958d87-e472-4c3f-a1e2-ab7adb3646b5.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-31958d87-e472-4c3f-a1e2-ab7adb3646b5.md`
- Task checklist: `tasks/tasks-linear-31958d87-e472-4c3f-a1e2-ab7adb3646b5.md`
- Linear issue: `CO-538` / `31958d87-e472-4c3f-a1e2-ab7adb3646b5`
- Source anchor: `ctx:sha256:0b7864e23887d0b023799ab19858917af9f490c4a4e62af57b5913aebe5861dc#chunk:c000001`
- Child lane manifest: `.runs/linear-31958d87-e472-4c3f-a1e2-ab7adb3646b5-docs-packet-local/cli/2026-05-14T03-58-04-115Z-a02ced60/manifest.json`
- Child lane limitation: the referenced source payload path was absent in this checkout; this spec uses the embedded parent prompt as source.

## Summary
- Objective: extend `codex-orchestrator linear create-follow-up` so created and reused follow-up issue labels are verified from live Linear issue state after mutation, repaired once with `addedLabelIds` when missing, and failed closed when terminal live labels still do not match expected source-derived labels.
- Scope:
  - provider Linear workflow facade create/reuse path.
  - post-create live verification.
  - post-reuse live verification for canonical owner reuse.
  - bounded `addedLabelIds` repair for missing expected labels.
  - JSON and human terminal output that reports terminal live labels.
  - focused tests for propagation delay, reuse repair, persistent missing labels, and output.
- Constraints:
  - no Linear label taxonomy changes.
  - no CO-400 projection semantics changes.
  - no provider admission, WIP cap, or queue prioritization changes.
  - no manual labeling sweep.
  - no cached or mutation-return labels as clean-success authority.
  - no full repo validation from the docs-only child lane.

## Issue-Shaping Contract
- User-request translation carried forward: CO-538 is the narrow live verification follow-up to CO-482 label assignment; `create-follow-up` must prove terminal live labels after create/reuse instead of trusting mutation-return labels.
- Protected terms / exact surfaces: `codex-orchestrator linear create-follow-up`, `CO-482`, `CO-537`, `labels: []`, `labelIds`, `addedLabelIds`, `live linear issue-context`, `post-create live verification`, `canonical owner reuse`, `source-derived labels`, `fail closed`.
- Reject interpretations that trust mutation-return labels, change CO-400 projection behavior, change label taxonomy, alter provider admission/queue priority, manually sweep labels, or weaken CO-482 source-derived label assignment.
- Non-goals: no Linear label taxonomy changes, CO-400 projection changes, provider admission/queue changes, manual label cleanup, or Linear/GitHub/PR lifecycle work in this child lane.

## Parity / Alignment Matrix

| Surface | Current truth | Reference truth | Target truth | Explicitly out-of-scope differences |
| --- | --- | --- | --- | --- |
| Create success | A created follow-up can receive `labelIds`, but mutation-return labels may not reflect terminal live issue state. | Live Linear issue labels after creation are authoritative. | Post-create live verification compares expected source-derived labels to observed live labels before clean success. | Label taxonomy changes or manual repair outside helper. |
| Reuse success | Canonical owner reuse can call `addedLabelIds`, but returned issue data alone is not terminal proof. | Reused owner labels must be verified from live issue state. | Post-reuse live verification repairs missing expected labels once with `addedLabelIds`, rereads live state, and fails closed if still missing. | Canonical owner matching redesign. |
| Failure evidence | Label failures can be under-specified or tied to mutation response shape. | Operators need the exact target issue and label diff. | Partial failure includes issue id or identifier, expected labels, observed labels, and missing labels. | Provider queue or PR lifecycle changes. |
| Output | Output can conflate requested or mutation-return labels with live labels. | Terminal output should show live issue labels. | JSON and human terminal output include terminal live labels and label verification status. | Workpad or co-status output redesign unless parent chooses a narrow helper citation. |

## Readiness Gate
- Not done if `create-follow-up` can report clean success while live target labels are `labels: []` or missing source-derived labels, if create/reuse success depends on mutation-return labels or stale cached state, if bounded repair lacks a final live reread, if persistent missing labels lack issue id plus expected/observed/missing label details, or if terminal JSON/human output omits live labels.
- Pre-implementation issue-quality review evidence: 2026-05-14 docs child lane translated the parent prompt into a narrow live-verification contract and rejected taxonomy/projection/provider-admission interpretations.
- Safeguard ownership split: CO-482 owns source-derived `labelIds` / `addedLabelIds`; CO-538 owns post-mutation live verification; CO-400 owns projection semantics.

## Architecture & Data
- Current behavior: `create-follow-up` derives source labels and applies them via `labelIds` or `addedLabelIds`, but mutation response labels/local projection can still leak into success/output authority.
- Target behavior: resolve expected source-derived labels, create or reuse through existing guarded paths, live-read the follow-up, compare by id, repair once with missing expected ids via `addedLabelIds`, live-reread, and return clean success only when terminal live labels match.
- Persistent missing labels produce partial failure with issue id or identifier, expected labels, observed labels, missing labels, and repair attempt metadata.
- Data model: no schema migration; facade/CLI result shape may expose terminal live labels and label verification details.

## Technical Requirements
- Preserve CO-482 source-derived label resolution, add post-create and post-reuse live verification, repair missing expected labels through one bounded `addedLabelIds` attempt, reread live state before success, and fail closed with issue id plus expected/observed/missing labels.
- JSON and human terminal output must show terminal live labels while existing issue-shaping guards, canonical owner reuse, related issue behavior, and CO-482 label policy remain intact.
- Non-functional requirements: bounded retries, deterministic propagation/failure tests, no new label catalog/projection cache, and fail-closed handling for missing, malformed, paginated, stale, or incomplete live label evidence.
- Interfaces: `orchestrator/src/cli/control/providerLinearWorkflowFacade.ts`, `orchestrator/src/cli/linearCliShell.ts`, `orchestrator/tests/ProviderLinearWorkflowFacade.test.ts`.

## Fallback Expiry / Refactor Decision
- Applies to fallback, compatibility, legacy, stale, cached, break-glass, or minor-seam behavior? `Yes`.
- Large-refactor decision: not required; live post-mutation verification belongs inside the existing `create-follow-up` helper and does not add a second label authority.
- Minor-seam decision: acceptable because it removes reliance on mutation-return/cached labels as terminal proof.

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Follow-up live label proof | Mutation-return or cached labels can be treated as terminal label authority. | `remove fallback` | CO-538 | `create-follow-up` creates or reuses a follow-up issue. | Existing pre-CO-538 behavior | 2026-05-14 | This issue | Clean success requires live post-mutation issue labels matching expected source-derived labels. | Focused create/reuse/repair/failure/output tests. |

- Contract: `create-follow-up` live post-mutation label verification. Owning surfaces: provider Linear workflow facade and CLI shell. Steady-state proof: focused create/reuse/repair/failure/output tests. Adjacent owner note: CO-482 source-derived label assignment remains intact; CO-400 projection semantics remain out of scope.

## Validation Plan
- Focused tests cover create success followed by live `labels: []`, post-create verification, post-reuse verification, bounded `addedLabelIds` repair/reread, propagation delay, persistent missing labels, JSON terminal labels, and human terminal labels.
- Parent validation gates: focused facade tests, focused CLI output tests if changed, `node scripts/spec-guard.mjs --dry-run`, and parent-required build/lint/test/docs/review gates.
- Child-lane validation: JSON parse checks for `tasks/index.json` and `docs/docs-freshness-registry.json`, target file presence checks, and `git diff --check`.

## Open Questions
- Parent implementation should choose the exact live read helper and whether to include a short wait before the bounded repair.
- Parent implementation should name the final failure code, but it must preserve issue id or identifier plus expected, observed, and missing labels.

## Approvals
- Reviewer: provider-worker parent lane.
- Date: 2026-05-14.
