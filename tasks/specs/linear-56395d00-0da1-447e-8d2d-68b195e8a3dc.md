---
id: 20260418-linear-56395d00-0da1-447e-8d2d-68b195e8a3dc
title: CO stabilize unrelated orchestrator tests Doctor full suite timeouts blocking CO-219 handoff
status: done
relates_to: docs/PRD-linear-56395d00-0da1-447e-8d2d-68b195e8a3dc.md
risk: high
owners:
  - Codex
last_review: 2026-05-18
review_notes:
  - 2026-05-18: CO-522 spec lifecycle audit found the linked task checklist has zero unchecked items (32 checked), so this spec is terminal and eligible for implementation-docs archive. Evidence: `out/linear-b642e879-ba50-45ef-b0d9-b059afa9e932-recovery/spec-preexpiry-local-classification.json`.
---

## Canonical Reference
- Canonical TECH_SPEC: `tasks/specs/linear-56395d00-0da1-447e-8d2d-68b195e8a3dc.md`
- TECH_SPEC mirror: `docs/TECH_SPEC-linear-56395d00-0da1-447e-8d2d-68b195e8a3dc.md`
- PRD: `docs/PRD-linear-56395d00-0da1-447e-8d2d-68b195e8a3dc.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-56395d00-0da1-447e-8d2d-68b195e8a3dc.md`
- Task checklist: `tasks/tasks-linear-56395d00-0da1-447e-8d2d-68b195e8a3dc.md`

## Traceability
- Linear issue: `CO-226` / `56395d00-0da1-447e-8d2d-68b195e8a3dc`
- Source issue: `CO-219` / `9f101328-ca10-47b3-9dc9-b0984d339794`
- Prompt handoff referenced upstream origin manifest id `2026-04-17T21-37-49-533Z-7f05b822`, but that file is not materialized under this workspace `.runs/`; local evidence for this attempt comes from the manifests created during the active docs-review, child-lane, review wrapper, and PR handoff runs.
- Source anchor: `ctx:sha256:dc802d183aad52d63c661a623eea2dfb1a6331c8142f57e36c744ffe0706b069#chunk:c000001`
- Shared source note: the shared source payload in this workspace provides provider-worker run metadata only; the Linear issue body remains the authoritative problem statement and checksum source.

## Summary
- Objective: explain and remove the unrelated `Doctor.test.ts` full-suite timeout instability that is blocking CO-219 handoff, while keeping Doctor coverage intact and restoring a green repo-wide `npm run test`.
- Scope:
  - docs-first packet and registry/checklist mirrors
  - evidence-backed comparison of standalone/trio Doctor success versus repo-wide timeout behavior
  - the smallest fix or validated runner/config adjustment that restores full-suite stability
  - parent-owned validation, review, PR, and Linear/workpad reconciliation
- Constraints:
  - preserve the exact command surfaces and failing case names from the issue body
  - do not reopen CO-219 logic work
  - do not weaken Doctor assertions or rely on targeted-only green results

## Issue-Shaping Contract
- User-request translation carried forward: this is a repo-baseline test stabilization lane for intermittent full-suite Doctor timeouts, not a CO-219 feature lane and not a targeted-test waiver lane.
- Protected terms / exact artifact and surface names:
  - `npm run test`
  - `orchestrator/tests/Doctor.test.ts`
  - `npx vitest run orchestrator/tests/Doctor.test.ts`
  - `npx vitest run orchestrator/tests/ControlRuntime.test.ts orchestrator/tests/ControlServer.test.ts orchestrator/tests/Doctor.test.ts`
  - `prefers task roots over nested provider templates when both signals exist`
  - `resolves provider readiness from the repo root when doctor runs in a nested directory`
  - `CO-219`
  - `CO-226`
- Nearby wrong interpretations to reject:
  - targeted Doctor green means the required repo-wide gate can be skipped
  - the issue already proved a deterministic Doctor logic bug
  - the right fix is to increase timeouts blindly
  - weakening or deleting Doctor coverage is acceptable because the failures are intermittent
  - this lane should broaden into general test-suite tuning without tying changes back to the actual Doctor timeout story
- Explicit non-goals carried forward:
  - no CO-219 feature reopen
  - no weakened Doctor assertions or deleted coverage
  - no `co-status` or `max_concurrent_agents` changes
  - no targeted-test-only waiver in place of a green full suite

## Parity / Alignment Matrix
- Current truth:
  - standalone `Doctor.test.ts` passed
  - targeted trio `ControlRuntime` + `ControlServer` + `Doctor` passed with `243` tests
  - full `npm run test` reruns timed out in `Doctor.test.ts` with different failing-case counts and names across attempts
- Reference truth:
  - the repo-wide `npm run test` gate must be green before CO-219 returns to review
  - Doctor coverage must remain intact
  - the cause classification must be evidence-backed: host load, Vitest scheduling, shared state, or Doctor-specific performance
- Target truth / intended delta:
  - parent lane reproduces and explains the timeout behavior under the full-suite surface
  - the smallest fix restores a green `npm run test`
  - targeted Doctor success and repo-wide stability align again
- Explicitly out-of-scope differences:
  - CO-219 product logic changes
  - unrelated suite cleanups not needed for this timeout story
  - coverage reduction or assertion weakening

## Readiness Gate
- Not done if:
  - `npm run test` still intermittently times out in `orchestrator/tests/Doctor.test.ts`
  - the difference between standalone/trio success and full-suite timeout remains unexplained
  - the lane depends on weakening coverage, targeted-only waiver logic, or speculative “host flake” claims
- Pre-implementation issue-quality review evidence:
  - 2026-04-18: the issue body makes this a full-suite-versus-targeted stability lane with exact protected surfaces, so the micro-task path is ineligible. Correctness depends on preserving the exact command surfaces, failing-case names, non-goals, and `Not Done If` contract before implementation starts.
  - 2026-04-18: pre-implementation standalone review of the docs packet approves the bounded scope: focus on `Doctor.test.ts` timeout causality and the smallest fix that restores repo-wide `npm run test`, with no CO-219 widening and no coverage weakening.
- Safeguard ownership split:
  - child lane `doctor-targeted-tests` owns `orchestrator/tests/Doctor.test.ts` under the `tests` phase while active
  - parent owns docs/registry files, full-suite reproduction, any non-Doctor runner/config changes, validation, review, PR, and Linear/workpad state

## Technical Requirements
- Functional requirements:
  1. Preserve the exact failing and passing command surfaces named in the issue.
  2. Capture evidence that distinguishes standalone/trio Doctor success from full-suite timeout behavior.
  3. Determine whether the dominant instability mechanism is host load, Vitest scheduling, shared test state, or Doctor-specific performance.
  4. Restore a green repo-wide `npm run test` without weakening Doctor coverage.
  5. Keep CO-219 blocked from review until the repo-wide gate is green or the blocker is explicitly resolved.
- Non-functional requirements:
  - deterministic enough full-suite behavior to pass the required gate
  - evidence-backed root-cause classification
  - smallest viable diff rather than broad suite retuning
- Interfaces / contracts:
  - `npm run test`
  - Vitest execution contract for the repo
  - Doctor test harness/setup/teardown behavior under isolated and full-suite execution

## Architecture & Data
- Architecture / design adjustments:
  - start from observed suite behavior, not a predetermined code fix
  - compare isolated Doctor execution, the targeted trio, and the full suite to determine where contention or slow setup is introduced
  - prefer localized test/harness fixes; use broader runner/config changes only when evidence shows the problem is outside Doctor test code
- Required artifact/content expectations:
  - task packet records the exact passing/failing commands and case names
  - workpad and validation logs remain aligned with the reproduced timeout story
  - final validation includes a green repo-wide `npm run test`
- Data model changes / migrations:
  - none expected
- External dependencies / integrations:
  - no production systems
  - local filesystem/process load and Vitest runner behavior are in scope

## Current Truth
- The issue documents one standalone Doctor pass and one targeted-trio pass after the CO-219 changes landed, proving the Doctor surface is not failing deterministically in isolation.
- The same issue documents three repo-wide reruns with non-deterministic timeout outcomes in `Doctor.test.ts`: one with `11` timeouts, one with a single timeout in `prefers task roots over nested provider templates when both signals exist`, and one with a single timeout in `resolves provider readiness from the repo root when doctor runs in a nested directory`.
- The rest of the required CO-219 non-test validation gates already passed, so the remaining blocker is specifically the repo-wide test gate.
- The child lane `doctor-targeted-tests` completed and the parent accepted its bounded `Doctor.test.ts` patch: replace one real direct-dist delegation startup with a local fake `dist/bin/codex-orchestrator.js` entrypoint that still waits for the `initialize` request before returning the expected probe payload.
- The first current-lane full-suite rerun after accepting that patch got past `Doctor.test.ts` and failed only because this fresh workspace did not yet have `dist/bin/codex-orchestrator.js`; after `npm run build`, the previously failing `tests/cli-command-surface.spec.ts` and `tests/run-review.spec.ts` surfaces passed in isolation.
- The next exact `npm run test` rerun finished green with `344` files and `4119` tests, and `Doctor.test.ts` completed inside the full suite in `74023ms`.
- PR `#522` now carries the Doctor stabilization diff, and the follow-up Codex review fix commit `651e17b4a` tightened the fake direct-dist entrypoint so it responds only after seeing the real `initialize` request and verifies the `delegate-server` argv instead of unconditionally writing a success payload.
- After merging current `origin/main`, including the CO-233 SelectedRunProjection fix, the latest exact `npm run test` rerun finished green with `345` files and `4246` tests; `Doctor.test.ts` passed inside the full suite in `65455ms`.
- CO-226 can proceed to review handoff once remote PR checks and the `pr ready-review` drain are clean; CO-219 can resume normal handoff using this green repo-wide gate evidence.

## Proposed Design
- Record the docs-first packet first, then run an audited `docs-review` child stream before source edits.
- Reproduce `npm run test` with enough local evidence to compare the full-suite timeout surface against the passing targeted commands.
- If the dominant cause is inside `Doctor.test.ts` or its shared test harness, land the smallest local fix there; if evidence points to broader Vitest execution behavior, keep the fix bounded to the exact runner/config seam needed for this suite.
- Preserve Doctor assertions and coverage, and use focused targeted reruns plus a final green `npm run test` to validate the fix.

## Protected Expectations
- Preserve the exact command surfaces and failure-case names from the issue body.
- Keep CO-219 out of review until the repo-wide gate is green.
- Keep targeted Doctor success as context, not as a waiver.
- Prefer the smallest correct fix over speculative suite-wide tuning.

## Reject These Wrong Interpretations
- `This is already solved because Doctor passes standalone.`
- `The repo-wide gate can be bypassed with a targeted trio run.`
- `The right fix is to raise timeouts without root-cause evidence.`
- `The easiest path is to weaken or delete Doctor assertions.`
- `Any unrelated repo suite noise can be ignored without classification.`

## Validation Plan
- Pre-implementation:
  - audited `docs-review` child stream after the docs packet lands
  - child lane completion evidence for the targeted Doctor tests slice
- Parent implementation:
  - focused targeted reruns around Doctor and adjacent suites as needed by the diagnosed seam
  - repo validation floor required by the issue workflow, including final `npm run test`
  - manifest-backed standalone review followed by an explicit elegance pass before review handoff

## Approvals
- Docs-review: succeeded under `.runs/linear-56395d00-0da1-447e-8d2d-68b195e8a3dc-co226-docs-review/cli/2026-04-17T21-47-30-660Z-b185d824/manifest.json`.
- Child lane: accepted under `.runs/linear-56395d00-0da1-447e-8d2d-68b195e8a3dc-doctor-targeted-tests/cli/2026-04-17T21-40-57-908Z-850bb2ea/manifest.json`.
- Standalone review: forced wrapper run completed with `status=succeeded` and `review_outcome=bounded-success` under `.runs/linear-56395d00-0da1-447e-8d2d-68b195e8a3dc/cli/2026-04-18T22-36-13-247Z-90cb1362/review/telemetry.json`; the command-intent boundary retry produced a read-only no-findings verdict and is successful bounded review completion.
- Elegance review: explicit minimality pass after standalone review kept the local fake direct-dist helper and argv/initialize assertions as the smallest coverage-preserving fix; no simplification patch was needed.
- Closeout state: PR `#522` is attached to CO-226 and current validation is green; remaining handoff work is push, clean remote checks, clean `pr ready-review` drain, workpad refresh, and transition to `In Review`.
