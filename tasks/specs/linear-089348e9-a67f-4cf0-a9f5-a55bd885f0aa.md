---
id: 20260516-linear-089348e9-a67f-4cf0-a9f5-a55bd885f0aa
title: CO-516 reconcile provider-intake terminal merge closeout truth
relates_to: docs/PRD-linear-089348e9-a67f-4cf0-a9f5-a55bd885f0aa.md
risk: high
owners:
  - Codex
last_review: 2026-05-16
related_action_plan: docs/ACTION_PLAN-linear-089348e9-a67f-4cf0-a9f5-a55bd885f0aa.md
task_checklists:
  - tasks/tasks-linear-089348e9-a67f-4cf0-a9f5-a55bd885f0aa.md
---

# TECH_SPEC - CO-516 reconcile provider-intake terminal merge closeout truth

## Objective
Reconcile provider-intake claims that retain stale review-handoff or merge-closeout metadata after the PR is merged and Linear is terminal, while preserving historical audit details and excluding terminal evidence from active non-release alarms.

## Scope
- provider-intake claim reconciliation for terminal merged/Done closeout
- active/non-release classification for retained terminal closeout claims
- focused provider handoff/projection regression coverage
- docs-first packet and registry mirrors

## Issue-Shaping Contract
- User-request translation carried forward: terminal merged/Done closeout must supersede cached `Merging` or stale `Blocked` provider-intake metadata without deleting audit history.
- Protected terms / exact artifact and surface names: `provider-intake-state.json`, `review_promotion`, `merge_closeout`, `provider_issue_merge_closeout_merged`, `provider_issue_released:not_active`, `selected-run`, `co-status`, `ProviderIssueHandoff`, `ProviderIntakeState`.
- Nearby wrong interpretations to reject: manual intake cleanup, broad warning suppression, merge/review policy changes, `goal_evidence` authority changes, broad manifest serialization.
- Explicit non-goals carried forward: no Linear/GitHub mutations, no dispatch pilot config changes, no CO-514 manifest serialization work.

## Readiness Gate
- Not done if: terminal merged/Done claims still appear as active closeout work, stale non-terminal metadata remains authoritative, audit fields are deleted, or warning suppression is not source-labeled.
- Pre-implementation issue-quality review evidence: this spec carries the issue contract, protected terms, non-goals, Not Done If, and parity matrix from the CO-516 prompt; the issue is not plausibly narrower than provider-intake terminal closeout truth.
- Safeguard ownership split: this worker owns docs/test/source changes only; parent owns Linear/GitHub lifecycle.

## Decomposition Matrix

| Candidate lane | File/phase scope | Dependencies | Overlap risk | Expected validation artifact | Owner | Cap-slot use |
| --- | --- | --- | --- | --- | --- | --- |
| Docs packet | CO-516 PRD/spec/action/checklists/registry | none | low | docs packet + spec guard | parent worker | 0 |
| Provider-intake reconciliation | provider handoff/intake state source + focused tests | docs packet | medium with adjacent provider lanes | focused Vitest regression | parent worker | 0 |
| Status projection classification | selected-run/status tests if needed | source inspection and failing test | medium with provider-intake fix | focused projection tests | parent worker | 0 |
| Review/validation | build/lint/spec/diff checks | implementation done | low | command logs in task checklist | parent worker | 0 |

Parallelization decision: `stay_serial` / `single_bounded_change`. Reason: the issue is one provider-intake terminal-authority seam; child lanes would overlap the same source/test files, and this manual replacement worker is explicitly forbidden from Linear/GitHub lifecycle mutations.

## Technical Requirements
- Functional requirements:
  - Detect claims with terminal merged closeout evidence and live terminal Linear truth.
  - Refresh stale issue state metadata for retained terminal claims when fresh terminal truth is available.
  - Convert terminal completed claims to a non-active hygiene state or classify them with terminal evidence so active non-release alarms exclude them.
  - Preserve `review_promotion` / `merge_closeout` audit details.
  - Add CO-492 / PR #793-style In Review -> Merging -> Done regression coverage.
- Non-functional requirements:
  - Keep changes small and local to existing provider-intake/control-host patterns.
  - Fail closed when terminal source evidence is missing or stale.
  - Avoid widening dispatch, review, merge, and manifest serialization policy.
- Interfaces / contracts:
  - `ProviderIntakeClaimRecord` retained audit fields
  - provider issue handoff refresh/reconciliation
  - selected-run / status projection active classification if touched

## Fallback Expiry / Refactor Decision
- Applies to fallback, compatibility, legacy, stale, cached, break-glass, or minor-seam behavior? `Yes`.

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Terminal closeout metadata | Cached `Merging` / stale non-terminal issue fields survive after merged PR plus Linear `Done`. | `remove fallback` | CO-516 | Live terminal issue truth is available for a claim with merged closeout evidence. | Existing provider closeout behavior | 2026-05-16 | This issue | Claim state/summary no longer treats cached non-terminal closeout metadata as active authority. | Focused provider-intake closeout regression. |
| Retained closeout audit row | Historical `review_promotion` / `merge_closeout` evidence remains on non-active claims. | `justify retaining fallback` | Provider-intake control-host | Claim has terminal closeout history after PR merge/Done. | Existing provider-intake retention behavior | 2026-05-16 | Durable audit contract | Separate archival policy replaces retained provider-intake audit rows. | Regression asserts audit fields remain present while active counts exclude the claim. |

- Contract name: provider-intake terminal closeout audit retention.
- Owning surface: provider-intake control-host claim retention and projection.
- Steady-state proof: terminal source evidence stays visible, but retained rows are not active work authority.
- Tests/docs: CO-516 packet plus focused ProviderIssueHandoff regressions.
- Non-expiring rationale: retaining historical closeout evidence is an audit requirement, not temporary compatibility debt.

- Large-refactor check: broader provider-intake authority consolidation remains a family-level concern, but this lane removes stale terminal authority with no new fallback branch or launch path.

## Architecture & Data
- Architecture / design adjustments:
  - Prefer a shared predicate/helper for terminal merged closeout evidence if the source already has closeout classifiers.
  - Update existing reconciliation path rather than adding a separate cleanup command.
- Data model changes / migrations:
  - No migration.
  - Retained claims may have refreshed `issue_state`, `issue_state_type`, `issue_updated_at`, state/reason, or classification.
- External dependencies / integrations:
  - No live Linear/GitHub mutation from this worker.

## Validation Plan
- Tests / checks:
  - focused provider-intake/provider-handoff regression for terminal merged/Done claim
  - projection/status regression if active alarm classification changes
  - `node scripts/spec-guard.mjs --dry-run`
  - `npm run build`
  - `npm run lint`
  - focused changed tests
  - `node scripts/diff-budget.mjs`
- Rollout verification:
  - Parent can restart/observe control-host after PR lifecycle if needed.
- Monitoring / alerts:
  - Active claim counts and selected-run status should no longer report terminal closeout residue as live work.

## Open Questions
- Whether a source-only reconciliation test is sufficient or a projection test is also needed depends on the first failing regression.

## Approvals
- Reviewer: CO-516 manual replacement worker
- Date: 2026-05-16
