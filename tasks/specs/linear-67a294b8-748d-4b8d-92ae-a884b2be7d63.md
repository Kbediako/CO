---
id: 20260428-linear-67a294b8-748d-4b8d-92ae-a884b2be7d63
title: CO-415 repair current-main core validation timeout cluster
relates_to: docs/PRD-linear-67a294b8-748d-4b8d-92ae-a884b2be7d63.md
risk: high
owners:
  - Codex
last_review: 2026-04-28
---

## Added by Provider Worker 2026-04-28

## Summary
- Objective: Restore current-main core validation so CO-409 can rerun its required validation floor without a waiver.
- Scope: Core Vitest non-interactive worker cap, focused config test, reduced timeout cluster, and validation evidence.
- Constraints: Keep all named tests active; do not change CO-409 docs freshness metadata; do not weaken docs or review gates.

## Issue-Shaping Contract
- User-request translation carried forward: Repair the current-main baseline timeout cluster affecting `ControlRuntime.test.ts`, `ProviderLinearChildLaneRunner.test.ts`, `Doctor.test.ts`, and `cli-command-surface.spec.ts`.
- Protected terms / exact artifact and surface names: `npm run test`; `vitest.config.core.ts`; `ControlRuntime.test.ts`; `ProviderLinearChildLaneRunner.test.ts`; `Doctor.test.ts`; `cli-command-surface.spec.ts`; current-main baseline; timeout failures; `projects authoritative budget exhaustion event text into running rows`; CO-409; docs:freshness.
- Nearby wrong interpretations to reject: CO-409 docs metadata bypass; test deletion/quarantine; timeout-only fix; unrelated provider queue/release work; docs freshness widening.
- Explicit non-goals carried forward: No CO-409 packet edits, no weakened gates, no validation waiver, no broad cloud/release policy changes.

## Technical Requirements
- Reproduce the reduced cluster failure or a fresher equivalent.
- Confirm the root cause is non-interactive worker pressure across subprocess-heavy suites, not an assertion or docs freshness issue.
- Lower the capped non-interactive core-suite worker count from the current unsafe value to a value proven by the reduced cluster.
- Update `tests/vitest-progress-config.spec.ts` so the cap contract is explicit for `CI`, `CODEX_VITEST_PROGRESS`, `CODEX_NON_INTERACTIVE`, `CODEX_NONINTERACTIVE`, and `CODEX_NO_INTERACTIVE`.
- Preserve uncapped interactive local runs.

## Validation Plan
- Focused config contract test.
- Reduced four-file cluster with provider-worker progress reporter enabled.
- Full `npm run test`.
- Required repo gates and review/elegance checks before handoff.

## Open Questions
- None currently blocking. If full-suite runtime becomes excessive with cap `2`, capture timing evidence and decide whether additional file-level isolation is needed instead of widening this lane.
