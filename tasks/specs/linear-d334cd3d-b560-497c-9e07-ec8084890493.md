---
id: 20260419-linear-d334cd3d-b560-497c-9e07-ec8084890493
title: CO: stabilize ControlServer read-only refresh temp cleanup ENOTEMPTY flake
status: in_progress
relates_to: docs/PRD-linear-d334cd3d-b560-497c-9e07-ec8084890493.md
risk: high
owners:
  - Codex
last_review: 2026-06-17
review_notes:
  - 2026-06-17: CO-579 pre-expiry review kept this spec active-current; no verified terminal/archive evidence was established in this stream, CO-579 is the live non-terminal docs-freshness owner, and docs/spec gates remain unchanged.
  - 2026-05-18: CO-522 active-spec audit found 1 unchecked task checklist item, so this spec remains active and was reviewed for current lifecycle ownership rather than archived. Evidence: `out/linear-b642e879-ba50-45ef-b0d9-b059afa9e932-recovery/spec-preexpiry-local-classification.json`.
---

## Canonical Reference
- Canonical TECH_SPEC: `tasks/specs/linear-d334cd3d-b560-497c-9e07-ec8084890493.md`
- TECH_SPEC mirror: `docs/TECH_SPEC-linear-d334cd3d-b560-497c-9e07-ec8084890493.md`
- PRD: `docs/PRD-linear-d334cd3d-b560-497c-9e07-ec8084890493.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-d334cd3d-b560-497c-9e07-ec8084890493.md`
- Task checklist: `tasks/tasks-linear-d334cd3d-b560-497c-9e07-ec8084890493.md`

## Traceability
- Linear issue: `CO-261` / `d334cd3d-b560-497c-9e07-ec8084890493`
- Linear URL: https://linear.app/asabeko/issue/CO-261
- Source anchor: `ctx:sha256:ce789eed64ab33a58fd40f1d77f4e6b2752cfaa7e0b79fefd4c6e638bc4f1128#chunk:c000001`
- Source payload: `.runs/linear-d334cd3d-b560-497c-9e07-ec8084890493-docs-packet/cli/2026-04-19T09-43-09-262Z-5b353e4d/memory/source-0/source.txt`
- Origin manifest: `.runs/linear-d334cd3d-b560-497c-9e07-ec8084890493-docs-packet/cli/2026-04-19T09-43-09-262Z-5b353e4d/manifest.json`
- Reported Core Lane evidence: run `24623956000`, commit `c382730e0e6364cbfeb736a1999fc160c98b03cf`

## Summary
- Objective: remove the `ENOTEMPTY` temp cleanup flake in `ControlServer.test.ts` case `acknowledges read-only refresh requests without mutating control state` while preserving the read-only refresh contract.
- Scope:
  - docs-first registration for `CO-261`
  - parent-owned investigation of temp cleanup / async lifecycle ownership
  - smallest owning implementation fix in the relevant lifecycle or test harness seam
  - focused and stress/focused validation of the read-only refresh case
  - full `npm run test`
  - later main Core Lane pass/non-repro classification for the `24623956000` / `c382730e0e6364cbfeb736a1999fc160c98b03cf` failure lineage
- Constraints:
  - child lane output is docs-only
  - no Linear mutation from this child lane
  - `CO-181` retained/released refresh fairness is out of scope
  - `CO-150` ProviderIssueHandoff retry investigation is out of scope

## Issue-Shaping Contract
- User-request translation carried forward: `CO-261` owns the `ControlServer.test.ts` read-only refresh cleanup race where `ENOTEMPTY` appears under `/tmp/control-server-*/.runs/task-0940/cli/run-1`; the parent must investigate the async lifecycle owner, repair only the smallest relevant seam, and validate with focused/stress/full-suite/Core Lane evidence.
- Protected terms / exact artifact and surface names: `ControlServer.test.ts`, `acknowledges read-only refresh requests without mutating control state`, `ENOTEMPTY`, `/tmp/control-server-*/.runs/task-0940/cli/run-1`, `control_seq`, `latest_action`, `/api/v1/refresh`, `/api/v1/state`, `npm run test`, `Core Lane`, `24623956000`, `c382730e0e6364cbfeb736a1999fc160c98b03cf`, `CO-181`, `CO-150`.
- Nearby wrong interpretations to reject: mutating control state to avoid the flake, force-deleting temp roots without lifecycle ownership, timeout inflation, broad ControlServer rewrite, retained/released refresh fairness work, or ProviderIssueHandoff retry repair.
- Explicit non-goals carried forward: no implementation/test edits from this child lane, no Linear/workpad/PR lifecycle mutation from this child lane, no `CO-181` or `CO-150` scope.

## Parity / Alignment Matrix
- Current truth:
  - Core Lane run `24623956000` on commit `c382730e0e6364cbfeb736a1999fc160c98b03cf` reported `ENOTEMPTY` under `/tmp/control-server-*/.runs/task-0940/cli/run-1`.
  - The failing test is `ControlServer.test.ts` case `acknowledges read-only refresh requests without mutating control state`.
  - The test's product contract is that `/api/v1/refresh` refreshes state visibility while `control_seq` and `latest_action` remain unchanged.
  - The child lane has no authority to edit source/tests or mutate Linear state.
- Reference truth:
  - Read-only refresh remains an acknowledgement/runtime-refresh path, not a control-state mutation.
  - `await server.close()` plus the test cleanup path should leave the temp run root removable.
  - Async lifecycle tasks spawned by the test must be awaited, closed, or isolated before cleanup.
  - CI classification should tie back to the observed run and commit.
- Target truth / intended delta:
  - the parent identifies the actual async writer/handle or cleanup ordering issue behind `ENOTEMPTY`
  - the parent lands the smallest owning fix without broad retries, timeout inflation, or unrelated behavior changes
  - focused/stress validation repeatedly covers the read-only refresh case and adjacent read-only refresh behavior
  - full `npm run test` passes
  - a later main Core Lane result records pass/non-repro for the protected failure lineage
- Explicitly out-of-scope differences:
  - `CO-181` retained/released refresh fairness and fresh Ready admission
  - `CO-150` ProviderIssueHandoff snapshot-only retry behavior
  - Linear state/workpad/PR lifecycle mutation from this child lane
  - generic scheduler, provider admission, or broad ControlServer refactor work

## Readiness Gate
- Not done if:
  - the protected read-only refresh test can still leave `/tmp/control-server-*/.runs/task-0940/cli/run-1` non-empty at cleanup
  - the fix weakens the assertions that `control_seq` and `latest_action` do not change
  - the implementation masks `ENOTEMPTY` with broad retry/delete logic without owning the underlying async lifecycle
  - focused validation runs only once with no stress/focused repetition
  - full `npm run test` or later Core Lane classification is skipped by the parent implementation lane
  - the lane widens into `CO-181` or `CO-150`
- Pre-implementation issue-quality review evidence:
  - 2026-04-19: child lane self-review confirms the issue is narrowly shaped around one `ControlServer.test.ts` temp cleanup / async lifecycle flake with exact protected path, test name, Core Lane run, and commit. The micro-task path is ineligible because correctness depends on protected wording, CI lineage, and validation classification.
- Safeguard ownership split:
  - child lane owns docs packet and registry mirrors only
  - parent lane owns Linear state, workpad, source/test implementation, validation, PR lifecycle, and patch integration

## Technical Requirements
- Functional requirements:
  - preserve the `/api/v1/refresh` acknowledgement contract for `acknowledges read-only refresh requests without mutating control state`
  - preserve `control_seq` and `latest_action` immutability assertions
  - ensure temp cleanup after `await server.close()` can remove `/tmp/control-server-*/.runs/task-0940/cli/run-1` without `ENOTEMPTY`
  - identify whether the owner is a pending refresh/runtime write, HTTP server lifecycle, provider handoff refresh promise, file/event transport, or test cleanup ordering
  - close, await, or isolate the real lifecycle owner before cleanup
  - keep the adjacent read-only refresh without provider handoff behavior covered if shared lifecycle code changes
  - keep `CO-181` and `CO-150` behavior unchanged
- Non-functional requirements:
  - smallest viable fix localized to the relevant lifecycle/test harness seam
  - no broad timeout increase as the primary repair
  - no blind retry/delete loop that hides active async work
  - no private token/auth/temp payload leakage in diagnostics or logs
  - no source/test edits from this docs-only child lane
- Interfaces / contracts:
  - `orchestrator/tests/ControlServer.test.ts`
  - `orchestrator/src/cli/control/controlServer.ts`
  - `/api/v1/refresh`
  - `/api/v1/state`
  - temporary run roots under `/tmp/control-server-*`
  - GitHub Actions `Core Lane`

## Architecture & Data
- Architecture / design adjustments:
  - first inspect the failing test's cleanup path and all async work that can touch `paths.root`, `paths.runDir`, or `.runs/task-0940/cli/run-1` after the refresh response resolves
  - prefer an explicit lifecycle await/close/isolation point over a filesystem retry workaround
  - if the fix belongs in test harness cleanup, keep it specific to the run-root lifecycle rather than broadening production behavior
  - if the fix belongs in `ControlServer.close()`, keep the public close contract unchanged while draining the leaked resource deterministically
- Data model changes / migrations:
  - none expected
- External dependencies / integrations:
  - GitHub Actions Core Lane result for final pass/non-repro classification

## Implementation Findings
- Local exact-case reproduction before the fix passed, so the reported `ENOTEMPTY` is classified as intermittent/CI-flake locally rather than always reproducible.
- The owned async lifecycle is provider polling health persistence: `/api/v1/refresh` can acknowledge an accepted provider refresh before the fire-and-forget provider polling health update chain finishes writing `provider-intake-state.json` under `.runs/task-0940/cli/run-1`.
- The production close seam owns the fix. `ControlServer.close()` now drains active/queued `ProviderIssueHandoff` refresh operations and flushes provider polling health updates before closing the owned runtime, so test cleanup is not racing a still-active writer.
- Focused regression coverage blocks the provider polling health write and proves `server.close()` does not resolve until that write can complete.

## Validation Plan
- Tests / checks:
  - parent focused test: `npx vitest run --config vitest.config.core.ts orchestrator/tests/ControlServer.test.ts -t "acknowledges read-only refresh requests without mutating control state"`
  - parent adjacent focused test if lifecycle code changes: include `acknowledges read-only refresh requests without provider handoff by reconciling runtime only`
  - parent focused file run: `npx vitest run --config vitest.config.core.ts orchestrator/tests/ControlServer.test.ts`
  - parent stress/focused repeat of the protected test shape, with iteration count recorded in the workpad or closeout evidence
  - parent full validation: `npm run test`
  - parent later main Core Lane run, with pass/non-repro classification tied to run `24623956000` and commit `c382730e0e6364cbfeb736a1999fc160c98b03cf`
- Required focused coverage:
  - read-only refresh acknowledges and returns expected refresh metadata
  - state refresh visibility updates after manifest reseed
  - `control_seq` and `latest_action` remain unchanged
  - temp run directory cleanup no longer hits `ENOTEMPTY`
  - adjacent no-provider-handoff read-only refresh behavior remains intact if shared code changes
- Rollout verification:
  - collect focused/stress/full-suite logs in parent-owned artifacts
  - record whether the post-fix main Core Lane passes or no longer reproduces this failure
- Monitoring / alerts:
  - no new monitoring; rely on CI/Core Lane and the protected test name/path for recurrence triage

## Open Questions
- Answered: the async resource is provider polling health persistence to `provider-intake-state.json`, queued after accepted read-only provider refresh acknowledgement.
- Answered: the fix belongs in production `ControlServer.close()` draining/flush behavior because the lifecycle owner is not test-only cleanup ordering.
- Current local evidence uses 20 focused `read-only refresh` iterations plus full-suite validation; later Core Lane classification remains pending PR/CI evidence.

## Approvals
- Reviewer: parent implementation validation and bounded standalone review completed locally; later Core Lane classification pending
- Date: 2026-04-19
