---
id: 20260418-linear-91749283-6dc8-4df8-aee3-5c9127c1200c
title: Doctor validation: classify and restore current Doctor readiness stability for CO-220 handoff
status: done
owner: Codex
created: 2026-04-18
last_review: 2026-05-18
review_cadence_days: 30
risk_level: high
related_prd: docs/PRD-linear-91749283-6dc8-4df8-aee3-5c9127c1200c.md
related_action_plan: docs/ACTION_PLAN-linear-91749283-6dc8-4df8-aee3-5c9127c1200c.md
review_notes:
  - 2026-04-18: Opened from provider-worker source anchor `ctx:sha256:2e68cd2fda454db25cb1007f4066d1660e095ddaaf609630cabcc4697c11670e#chunk:c000001` with the preserved issue statement that clean `origin/main` and the `CO-220` current branch failed on different `Doctor.test` buckets.
  - 2026-04-18: Same-issue docs child lanes `docs-packet` and `docs-packet-cli` stalled under appserver, and `docs-packet-fallback` drifted into unrelated packet content. The parent lane therefore authored the docs-first packet locally after the successful `classification-note` lane preserved the comparison buckets under forced CLI fallback.
  - 2026-04-18: Retrospective issue-quality review rejects reopening `CO-220` stale-attachment logic or weakening validation. Accepted scope is the Doctor validation seam only: preserve the exact `current branch` vs `origin/main` vs `CO-220` buckets, repair the non-hermetic direct-dist fixture assumption, and keep the current target head green on isolated and full-suite Doctor validation.
  - 2026-04-19: After merging current `origin/main`, validation is green through delegation guard, spec guard, build, lint, isolated `Doctor.test`, full `npm run test`, docs:check, docs:freshness, repo:stewardship, diff-budget, and pack:smoke. Manifest-backed standalone review completed as `review_outcome=bounded-success` via command-intent retry, the stale checklist finding was corrected, and the rerun reported no actionable diff-local findings before the explicit elegance pass.
  - 2026-05-18: CO-522 spec lifecycle audit found the linked task checklist has zero unchecked items (30 checked), so this spec is terminal and eligible for implementation-docs archive. Evidence: `out/linear-b642e879-ba50-45ef-b0d9-b059afa9e932-recovery/spec-preexpiry-local-classification.json`.
---

# Technical Specification

## Canonical Reference
- Canonical TECH_SPEC: `tasks/specs/linear-91749283-6dc8-4df8-aee3-5c9127c1200c.md`
- TECH_SPEC mirror: `docs/TECH_SPEC-linear-91749283-6dc8-4df8-aee3-5c9127c1200c.md`
- PRD: `docs/PRD-linear-91749283-6dc8-4df8-aee3-5c9127c1200c.md`
- ACTION_PLAN: `docs/ACTION_PLAN-linear-91749283-6dc8-4df8-aee3-5c9127c1200c.md`
- Task checklist: `tasks/tasks-linear-91749283-6dc8-4df8-aee3-5c9127c1200c.md`

## Traceability
- Linear issue: `CO-231` / `91749283-6dc8-4df8-aee3-5c9127c1200c`
- Source anchor: `ctx:sha256:2e68cd2fda454db25cb1007f4066d1660e095ddaaf609630cabcc4697c11670e#chunk:c000001`
- Parent-provided source payload reference: `.runs/linear-91749283-6dc8-4df8-aee3-5c9127c1200c/cli/2026-04-18T02-10-26-787Z-56ab47e0/memory/source-0/source.txt`
- Initial worker manifest: `.runs/linear-91749283-6dc8-4df8-aee3-5c9127c1200c/cli/2026-04-18T02-10-26-787Z-56ab47e0/manifest.json`
- Successful classification-note child-lane manifest: `.runs/linear-91749283-6dc8-4df8-aee3-5c9127c1200c-classification-note/cli/2026-04-18T02-34-58-947Z-42530f5e/manifest.json`
- Advisory classification artifact: `out/linear-91749283-6dc8-4df8-aee3-5c9127c1200c/manual/20260418T023500Z-child-lane-doctor-classification.md`
- Current workpad artifact: `out/linear-91749283-6dc8-4df8-aee3-5c9127c1200c/manual/workpad-latest.md`

## Summary
- Objective: restore stable `Doctor.test` validation semantics for the current target head while preserving the exact clean-main and `CO-220` comparison buckets needed for truthful handoff.
- Scope:
  - preserve the issue checksum and comparison buckets
  - replace the non-hermetic direct-dist Doctor fixture assumption
  - prove the repaired current head is green on both isolated and full-suite Doctor validation
  - backfill docs/spec/task mirrors and registry entries for `CO-231`
- Constraints:
  - do not reopen `CO-220` stale-attachment logic
  - do not weaken readiness truth
  - keep the implementation local to `orchestrator/tests/Doctor.test.ts`

## Issue-Shaping Contract
- User-request translation carried forward: classify and restore current Doctor readiness stability for `CO-220` handoff without conflating Doctor validation debt with stale-attachment logic.
- Protected terms / exact artifact and surface names:
  - `Doctor.test`
  - `full-suite`
  - `isolated rerun`
  - `provider readiness`
  - `direct-dist delegation readiness and initialize latency`
  - `keeps Telegram incomplete when transport is allowed but mutations are disabled`
  - `falls back to legacy collab feature key when canonical key is absent`
  - `current branch`
  - `origin/main`
  - `CO-220`
- Nearby wrong interpretations to reject:
  - the issue is just `CO-220` stale-attachment logic under a new label
  - the problem can be solved by deleting tests, muting failures, or inflating timeouts
  - the fix requires changing runtime provider truth rather than the Doctor test seam
  - the comparison buckets can be collapsed into generic full-suite flakiness
- Explicit non-goals carried forward:
  - no stale-attachment / projection logic changes from `CO-220`
  - no provider-readiness truth change without evidence that the existing expectation is wrong
  - no skipped validation, retry blanket, or timeout blanket
  - no unrelated clean-main suite reclassification

## Parity / Alignment Matrix
- Current truth:
  - the current target head now passes `Doctor.test` in isolated rerun and full-suite validation after the test fixture repair
  - the direct-dist Doctor seam was previously non-hermetic because it assumed the workspace `dist/` tree existed
  - the preserved clean-main bucket had a baseline-only isolated direct-dist failure
  - the preserved `CO-220` current branch had additional branch-only isolated and full-suite Doctor failures
- Reference truth:
  - `CO-231` owns Doctor validation stability only
  - `CO-220` owns stale-attachment / truth-projection logic only
  - clean-main and `CO-220` buckets must stay auditable as separate evidence sets
- Target truth / intended delta:
  - the current target head stays green on isolated and full-suite Doctor validation
  - the direct-dist Doctor test becomes hermetic and independent of ambient build state
  - the issue packet remains explicit about baseline-only vs branch-only vs non-repro Doctor cases
- Explicitly out-of-scope differences:
  - any `CO-220` intake / projection / stale attachment behavior
  - any live provider-readiness contract change without evidence
  - unrelated full-suite failures not caused by the Doctor seam

## Readiness Gate
- Not done if:
  - the repaired current head still fails `Doctor.test` in isolated or full-suite validation
  - the clean-main and `CO-220` buckets are no longer machine-checkable and separate
  - the fix depends on hidden build state rather than a hermetic test fixture
  - the lane broadens into runtime semantics or `CO-220` stale-attachment logic
- Safeguard ownership split:
  - successful child lane `classification-note` owns only the advisory comparison artifact
  - parent lane owns the authoritative docs packet, Doctor test repair, validation, workpad, PR, and final handoff

## Technical Requirements
1. Preserve the exact clean-main and `CO-220` Doctor comparison buckets from the issue packet and current artifacts.
2. The current target head must no longer fail the named Doctor surfaces in either isolated rerun or full-suite validation.
3. The direct-dist Doctor fixture must no longer assume the workspace `dist/` tree is already built.
4. The issue packet must explicitly preserve baseline-only, branch-only, and non-repro classification on the current target head.
5. The lane must remain separate from `CO-220` stale-attachment logic and from live provider truth changes.

## Architecture & Data
- Architecture / design adjustments:
  - keep the repair in the Doctor test seam, not the runtime seam
  - construct a fake direct-dist delegate server inside the temporary repo under test
  - preserve the comparison artifacts in `out/` and the workpad rather than relying on transient shell output
- Expected implementation surface:
  - `orchestrator/tests/Doctor.test.ts`
- Data model / artifact constraints:
  - no data model changes
  - keep the classification note, workpad, and source anchor paths as the auditable evidence pack

## Validation Plan
- Completed current-head checks:
  - `npm run build`
  - `npx vitest run --config vitest.config.core.ts orchestrator/tests/Doctor.test.ts`
  - `npm test`
- Remaining repo-gate checks before review handoff:
  - `node scripts/delegation-guard.mjs`
  - `node scripts/spec-guard.mjs --dry-run`
  - `npm run lint`
  - `npm run docs:check`
  - `npm run docs:freshness`
  - `npm run repo:stewardship`
  - `node scripts/diff-budget.mjs`
  - `codex-orchestrator review`
  - `npm run pack:smoke`
- Review / handoff requirements:
  - explicit elegance pass after the standalone review
  - refreshed workpad before any PR or review-state transition

## Open Questions
- Do any remaining repo-gate failures reveal unrelated blocking debt that should keep `CO-231` in progress even though the Doctor surface is green?
- Should the preserved classification note stay as an `out/` artifact only, or should the final PR description also summarize the clean-main vs `CO-220` buckets explicitly?

## Approvals
- Reviewer: provider worker retrospective issue-quality review.
- Date: 2026-04-18
