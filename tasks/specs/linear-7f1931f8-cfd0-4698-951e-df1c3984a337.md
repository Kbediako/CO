---
id: 20260406-linear-7f1931f8-cfd0-4698-951e-df1c3984a337
title: CO STATUS: bound provider proof session-log refresh cost
status: in_progress
owner: Codex
created: 2026-04-06
last_review: 2026-04-06
review_cadence_days: 30
risk_level: high
related_prd: docs/PRD-linear-7f1931f8-cfd0-4698-951e-df1c3984a337.md
related_action_plan: docs/ACTION_PLAN-linear-7f1931f8-cfd0-4698-951e-df1c3984a337.md
related_tasks:
  - tasks/tasks-linear-7f1931f8-cfd0-4698-951e-df1c3984a337.md
review_notes:
  - 2026-04-06: Opened from Linear issue `CO-99` in the provider-worker workspace using issue id `7f1931f8-cfd0-4698-951e-df1c3984a337`.
  - 2026-04-06: Live `linear issue-context` confirmed the CO workflow states (`Ready`, `In Progress`, `In Review`, `Merging`, `Rework`), showed no attached PR and no existing workpad, and the issue was transitioned from `Ready` to `In Progress` before active coding.
  - 2026-04-06: The workspace started detached at `9118bf368` (equal to `origin/main`) and moved onto branch `linear/co-99-bound-proof-session-log-refresh-cost` before tracked repo edits.
  - 2026-04-06: Pre-implementation audit confirmed the in-progress read path is `selectedRunProjection.ts` -> `refreshProviderLinearWorkerProofSnapshot(...)` -> `hydrateProviderLinearWorkerProofFromSessionLog(...)`, and that the current implementation reconstructs a fresh tail state with `offsetBytes: 0` for each refresh even when the same active `rollout-*.jsonl` file is still growing.
  - 2026-04-06: Issue-quality review approves one bounded proof-refresh optimization lane: preserve the authoritative runtime telemetry restored in `CO-98` while making repeated in-progress hydration incremental or equivalently bounded. Refresh-throttling, heuristic tail reads that can miss updates, and UI-only fixes are rejected.
  - 2026-04-06: Audited docs-review rerun after packet fixes succeeded in `.runs/linear-7f1931f8-cfd0-4698-951e-df1c3984a337-co-99-docs-review-r2/cli/2026-04-05T23-12-59-925Z-cae6b004/manifest.json`, and review telemetry reported `status=succeeded`, `review_outcome=clean-success`.
---

# Technical Specification

## Context

`CO-98` restored truthful provider-worker runtime telemetry to the live root control-host path, but the selected-run refresh path still reconstructs session-log hydration from scratch each time it refreshes an in-progress proof. The current `hydrateProviderLinearWorkerProofFromSessionLog(...)` implementation discovers the active `rollout-*.jsonl` file, seeds a new tail state at byte `0`, rereads the file, and then rewrites the proof even when the same worker session is still active and only a small amount of new data has been appended.

That means repeated root `CO STATUS` refreshes can keep paying full-file reread cost for the same active worker until all telemetry fields are populated. The rendered values remain correct, but the refresh path is needlessly `O(file-size)` for every refresh on long-running workers.

This follow-up therefore needs one bounded refresh optimization on the authoritative proof refresh seam, without regressing the truthful `Tokens`, `SESSION`, `Throughput`, and Codex `5-hour` / `weekly` telemetry restored under `CO-98`.

## Requirements

1. Repeated in-progress proof refreshes for the same active provider-worker session must not reread the already-consumed session-log bytes from byte `0`.
2. The refresh path must still surface truthful runtime telemetry updates for `Tokens`, `SESSION`, `Throughput`, and Codex `5-hour` / `weekly` windows when they appear later in the session log.
3. The implementation must fail closed to a safe full reread whenever the active session-log file changes, truncates, or otherwise invalidates the persisted hydration cursor.
4. The change must remain bounded to the authoritative proof refresh seam; no refresh-cadence throttling, heuristic data loss, or unrelated schema redesign.
5. Tests must cover repeated refreshes against a growing `rollout-*.jsonl` file and prove both bounded rereads and preserved correctness.
6. Tests must also cover the fail-closed path when the active session-log file rotates or truncates so stale cursor state cannot suppress later telemetry.

## Proposed Design

### 1. Persist minimal hydration progress for the current session-log file

- Extend the proof refresh contract with an internal hydration cursor that records the currently discovered session-log path plus the minimum state required to resume parsing safely.
- Reuse that cursor only when the refresh discovers the same active file and the file still appears to have grown monotonically.
- Keep the cursor opaque and local to the proof refresh path so the broader control-host/read-model contract does not need to reason about it.

### 2. Fail closed on mismatch and preserve authoritative parsing

- If discovery returns a different `rollout-*.jsonl` path, or if the previously recorded cursor is inconsistent with the current file, drop the cursor and reread from the start.
- Keep telemetry derivation exactly on the existing parser path so correctness remains identical to the current full-reread behavior.
- Treat bounded rereads as an optimization only; correctness must continue to prefer authoritative session-log data.

### 3. Keep selected-run projection behavior unchanged except for cheaper refreshes

- `selectedRunProjection.ts` should keep deciding when an in-progress proof is incomplete enough to refresh.
- The optimization belongs in `refreshProviderLinearWorkerProofSnapshot(...)` / `hydrateProviderLinearWorkerProofFromSessionLog(...)`, not in refresh cadence or projection heuristics.
- The selected-run projection should continue receiving the same truthful proof fields after the change.

## Non-Goals

- No browser or `/ui` dashboard redesign.
- No synthetic or inferred telemetry placeholders.
- No refresh-throttling workaround that leaves byte-zero rereads unchanged.
- No heuristic tail read that can miss real telemetry updates.

## Parity / Alignment Matrix

| Surface | Current CO truth | Reference truth | Target CO truth |
| --- | --- | --- | --- |
| In-progress proof refresh cost | rereads same active `rollout-*.jsonl` from byte `0` each refresh | `CO-98` restored correctness but did not bound reread cost | reread only newly appended data for the stable-file case |
| Root `CO STATUS` telemetry truth | truthful values already restored under `CO-98` | authoritative runtime telemetry must remain the source of truth | same truthful telemetry with bounded refresh work |
| Failure behavior on file mismatch | full reread from zero | safe, correctness-preserving fallback | keep safe full reread whenever cursor trust is lost |

## Not Done If

- `refreshProviderLinearWorkerProofSnapshot` still rereads the same session log from byte `0` on repeated in-progress refreshes for the same active worker.
- A long-running worker with a large `rollout-*.jsonl` file still causes repeated full-file reads during root control-host status refreshes.
- The optimization drops or delays truthful runtime telemetry that `CO-98` restored for `Tokens`, `SESSION`, `Throughput`, or Codex `5-hour` / `weekly` segments.

## Validation Plan

- Run audited `linear child-stream --pipeline docs-review` before implementation.
- Add focused regressions for repeated refreshes against a growing session log and for fail-closed rotation/truncation fallback behavior.
- Re-run the required validation floor after implementation.
- Run manifest-backed standalone review and explicit elegance review before review handoff.

## Approvals

- Reviewer: `codex-orchestrator docs-review`
- Date: `2026-04-06`
- Manifest: `.runs/linear-7f1931f8-cfd0-4698-951e-df1c3984a337-co-99-docs-review-r2/cli/2026-04-05T23-12-59-925Z-cae6b004/manifest.json`
- Review telemetry: `.runs/linear-7f1931f8-cfd0-4698-951e-df1c3984a337-co-99-docs-review-r2/cli/2026-04-05T23-12-59-925Z-cae6b004/review/telemetry.json`
