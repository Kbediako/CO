---
id: 20260425-linear-16623322-4b8d-4e12-8f57-688aa5f71a23
title: CO-363 CO STATUS active issue projection and current-host-unhealthy classification
status: in_progress
owner: Codex
created: 2026-04-25
last_review: 2026-06-17
review_cadence_days: 30
risk_level: high
related_prd: docs/PRD-linear-16623322-4b8d-4e12-8f57-688aa5f71a23.md
related_action_plan: docs/ACTION_PLAN-linear-16623322-4b8d-4e12-8f57-688aa5f71a23.md
related_tasks:
  - tasks/tasks-linear-16623322-4b8d-4e12-8f57-688aa5f71a23.md
review_notes:
  - 2026-06-17: CO-579 pre-expiry review kept this spec active-current; no verified terminal/archive evidence was established in this stream, CO-579 is the live non-terminal docs-freshness owner, and docs/spec gates remain unchanged.
  - 2026-04-25: Issue-quality review approves CO-363 as the status-projection/current-host-health lane. It must preserve exact status surfaces, active identifiers/counts, reopened metadata, CO-345, CO-356, CO-330, and current-host-unhealthy classification before parent implementation.
  - 2026-05-18: CO-522 active-spec audit found 2 unchecked task checklist items, so this spec remains active and was reviewed for current lifecycle ownership rather than archived. Evidence: `out/linear-b642e879-ba50-45ef-b0d9-b059afa9e932-recovery/spec-preexpiry-local-classification.json`.
---

# Technical Specification

## Context
`CO-363` is a docs-first lane for aligning CO status projection truth. The parent handoff requires preserving `CO STATUS`, `control-host`, `co-status --format json`, `/ui/data.json`, `control_endpoint.json`, `provider-intake-state.json`, `active issue projection`, `active identifiers and counts`, `CO-345`, `CO-356`, `CO-330`, `reopened issue metadata`, and `current-host-unhealthy classification`. This child lane creates the packet only; parent implementation will inspect and update the runtime/read-model surfaces.

## Requirements
1. Keep active issue projection consistent across `CO STATUS`, `co-status --format json`, and `/ui/data.json`.
2. Preserve `active identifiers and counts` as explicit machine-checkable status truth.
3. Surface `current-host-unhealthy classification` separately from active issue lifecycle state.
4. Preserve reopened issue metadata for `CO-345`, `CO-356`, and `CO-330`.
5. Use `provider-intake-state.json` as retained evidence without destructive cleanup.
6. Prevent stale provider-intake history from inflating active counts without live or reopened issue corroboration.
7. Keep `control_endpoint.json` endpoint/host-health concerns distinct from issue lifecycle truth.
8. Keep CO-345, CO-356, and CO-330 as protected related identifiers and do not absorb their implementation scope into CO-363.

## Issue-Shaping Contract
- User-request translation carried forward: create the CO-363 docs-first packet only. Parent implementation must make status surfaces agree on active issue projection and counts while separating current host health from issue lifecycle truth.
- Protected terms / exact artifact and surface names: `CO STATUS`, `control-host`, `co-status --format json`, `/ui/data.json`, `control_endpoint.json`, `provider-intake-state.json`, `active issue projection`, `active identifiers and counts`, `CO-345`, `CO-356`, `CO-330`, `reopened issue metadata`, `current-host-unhealthy classification`.
- Nearby wrong interpretations to reject: cosmetic renderer-only work, destructive local-state cleanup, treating host unhealthiness as issue lifecycle truth, stale provider-intake history as active proof by itself, direct implementation of CO-345/CO-356/CO-330, and Linear/workpad/PR mutations from this child lane.
- Explicit non-goals carried forward: no implementation/test edits, no full repo validation, no broad control-host restart redesign, no frontend UI redesign, no Linear mutation, no PR lifecycle work, and no destructive cleanup of status artifacts.

## Parity / Alignment Matrix
- Current truth: `co-status --format json` reads through `control_endpoint.json` and `/ui/data.json`; `/ui/data.json` is the control-host status dataset; `provider-intake-state.json` retains claim evidence; active issue projection can become ambiguous when the current host is unhealthy or reopened metadata is not represented.
- Reference truth: host-health classification, endpoint health, provider-intake evidence, and issue lifecycle truth are separate status dimensions that should be reconciled but not collapsed into one another.
- Target truth / intended delta: `CO STATUS`, `co-status --format json`, and `/ui/data.json` agree on active identifiers/counts; `current-host-unhealthy classification` is explicit; reopened metadata for `CO-345`, `CO-356`, and `CO-330` is preserved; stale intake history does not create active rows by itself.
- Explicitly out-of-scope differences: status renderer redesign, Linear issue mutation, CO-345/CO-356/CO-330 implementation, control-host restart redesign, and destructive artifact cleanup.

## Readiness Gate
- Not done if: status surfaces disagree on active identifiers/counts; `current-host-unhealthy classification` is absent or conflated with lifecycle truth; reopened metadata for `CO-345`, `CO-356`, or `CO-330` is missing from the contract; stale intake history can inflate active counts alone; or the solution deletes local artifacts instead of correcting projection.
- Pre-implementation issue-quality review evidence: approved for parent implementation after packet creation because the protected terms and non-goals are explicit. Micro-task path is unavailable because correctness depends on exact surface names, protected issue identifiers, parity/alignment, and Not Done If conditions.
- Safeguard ownership split: child lane owns only declared docs files. Parent owns implementation, focused tests, docs-review, validation, Linear/workpad state, PR lifecycle, and final closeout.

## Technical Requirements
- Functional requirements:
  - define one active issue projection contract shared by `CO STATUS`, `co-status --format json`, and `/ui/data.json`
  - keep active identifiers/counts stable for the same source snapshot
  - carry reopened issue metadata for protected related issue identifiers
  - classify current host unhealthiness without hiding active projection
  - treat `provider-intake-state.json` as evidence with precedence rules instead of cleanup fodder
  - fail closed when live issue or host evidence is unavailable
- Non-functional requirements (performance, reliability, security): avoid added request burn where possible, avoid leaking auth or raw private payloads in debug fields, preserve auditability of retained local state, and keep status output deterministic.
- Interfaces / contracts: local `control-host`, `control_endpoint.json`, `/ui/data.json`, `co-status --format json`, `provider-intake-state.json`, status/read-model projection helpers, and parent-owned live Linear issue evidence for `CO-345`, `CO-356`, and `CO-330`.

## Architecture & Data
- Architecture / design adjustments: parent should inspect the shared read-model/status projection path so terminal and JSON consumers use the same active issue classification. `current-host-unhealthy classification` should be an additive field or classification rather than a branch that suppresses active rows.
- Data model changes / migrations: no migrations expected. Additive status/read-model fields may be acceptable if needed to keep host-health and issue-lifecycle truth separate.
- External dependencies / integrations: no new external dependencies expected. Parent may use live Linear reads for issue metadata verification and focused local fixtures for deterministic tests.

## Validation Plan
- Tests / checks:
  - child scoped protected-term scan over the packet and mirrors
  - child `tasks/index.json` parse check
  - child scoped `git diff --check`
  - parent focused tests for active identifiers/counts parity across `CO STATUS`, `co-status --format json`, and `/ui/data.json`
  - parent focused test for `current-host-unhealthy classification` preserving active projection
  - parent focused test for reopened metadata on `CO-345`, `CO-356`, and `CO-330`
  - parent focused test proving stale `provider-intake-state.json` terminal history alone does not count active
- Rollout verification: parent records status payload evidence and workpad notes before PR review handoff.
- Monitoring / alerts: existing status surfaces only.

## Open Questions
- Which status field should carry `current-host-unhealthy classification`?
- What exact reopened metadata fields should active rows expose?
- Should parent add a single shared helper for active projection parity or reuse an existing read-model projection seam?

## Approvals
- Reviewer: docs child lane self-review.
- Date: 2026-04-25
