---
id: 20260322-linear-856c1318-524f-4db3-8d4a-b357ec51c304
title: Archive Automation Policy Mismatch for Linked PRD TECH_SPEC and ACTION_PLAN Docs
status: in_progress
owner: Codex
created: 2026-03-22
last_review: 2026-03-22
review_cadence_days: 30
risk_level: medium
related_prd: docs/PRD-linear-856c1318-524f-4db3-8d4a-b357ec51c304.md
related_action_plan: docs/ACTION_PLAN-linear-856c1318-524f-4db3-8d4a-b357ec51c304.md
related_tasks:
  - tasks/tasks-linear-856c1318-524f-4db3-8d4a-b357ec51c304.md
review_notes:
  - 2026-03-22: Opened from Linear issue `CO-2` after confirming the team workflow state is `In Progress` and the issue evidence points to a real policy-versus-implementation mismatch in archive pattern handling.
  - 2026-03-22: Existing policy and legacy archive specs already keep linked PRD/TECH_SPEC/ACTION_PLAN docs in scope, so this lane fixes implementation to match policy rather than narrowing policy.
  - 2026-03-22: Delegation is explicitly overridden for this worker run because subagent spawning is unavailable in-session; the planned fix stays bounded to one archive-pattern patch plus focused regression coverage.
  - 2026-03-22: Pre-implementation review approved the implementation path: keep policy scope unchanged, patch only archive-pattern expansion, and prove the linked-doc behavior with a focused regression before broader validation.
  - 2026-03-22: Implementation also needed to honor canonical `tasks/index.json` `paths.docs` entries, because current task registration no longer relies on legacy `path` for the indexed TECH_SPEC seed.
---

# Technical Specification

## Context

The implementation-docs archive policy already includes `docs/PRD-*`, `docs/TECH_SPEC-*`, and `docs/ACTION_PLAN-*`, but the current archiver fails to archive linked docs matching those patterns. The issue evidence and current script behavior point to plain `*` glob handling in the regex conversion path, not to an intentional policy narrowing.

## Requirements

1. Preserve the current broader archive scope already declared in `docs/implementation-docs-archive-policy.json`.
2. Ensure policy patterns using plain `*` match linked implementation docs consistently during task-linked doc extraction.
3. Preserve the current archive payload-before-stub behavior.
4. Add focused regression coverage that proves linked PRD/TECH_SPEC/ACTION_PLAN docs archive into payloads and stubs when eligible.
5. Keep the issue in an active state until a PR is attached and the team review handoff is ready.

## Current Truth

- `docs/implementation-docs-archive-policy.json` already lists linked PRD/TECH_SPEC/ACTION_PLAN docs in `doc_patterns`.
- `docs/TECH_SPEC-implementation-docs-archive-automation.md` and `docs/TECH_SPEC-implementation-docs-archive-allowlist.md` both describe the broader archive scope rather than a narrowed checklist/spec/.agent-only scope.
- The current script implementation appears to build regexes in a way that does not expand plain `*` correctly for linked-doc matching.
- The current script also relies on legacy `item.path` when seeding linked-doc extraction, but canonical task registration now stores the indexed TECH_SPEC under `paths.docs`.
- This worker run cannot use spawned subagents, so delegation guard must be satisfied through an explicit override rather than by pretending delegation happened.

## Validation Plan

- docs-review before implementation with explicit delegation override
- focused regression in `tests/implementation-docs-archive.spec.ts`
- required repo validation floor after implementation
- PR attachment to Linear before transition to `In Review`
