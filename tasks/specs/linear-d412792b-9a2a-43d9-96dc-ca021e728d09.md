---
id: 20260501-linear-d412792b-9a2a-43d9-96dc-ca021e728d09
title: CO-452 retire js_repl posture after Codex CLI 0.128.0 removal
relates_to: docs/PRD-linear-d412792b-9a2a-43d9-96dc-ca021e728d09.md
risk: medium
owners:
  - Codex
last_review: 2026-05-24
review_notes:
  - "2026-05-24: CO-579 pre-expiry review: retained active or blocked scope; no spec contract changes required."
---

# Technical Specification

## Summary
- Objective: align CO's active `js_repl` posture with Codex CLI `0.128.0`, where `js_repl` and `js_repl_tools_only` are removed and disabled.
- Scope: current-facing docs, cloud feature-toggle test examples, the old `js_repl` usage canary surface, and historical evidence packet labeling.
- Constraints: preserve generic feature flag pass-through for non-removed features; keep CO-449, CO-450, and CO-451 separate.

## Issue-Shaping Contract
- User-request translation carried forward: retire active `js_repl` posture and feature toggle guidance now that `0.128.0` removed the feature.
- Protected terms / exact artifact and surface names: `js_repl`, `js_repl_tools_only`, `removed false`, `CODEX_CLOUD_ENABLE_FEATURES`, `CODEX_CLOUD_DISABLE_FEATURES`, `codex features enable js_repl`, `codex features enable js_repl_tools_only`, `codex features disable js_repl`, `codex features disable js_repl_tools_only`, `orchestrator/tests/CodexCloudTaskExecutor.test.ts`, `orchestrator/tests/OrchestratorCloudTargetExecutor.test.ts`, scripts/js-repl-usage-matrix.mjs.
- Nearby wrong interpretations to reject: default-on posture, break-glass toggles for a removed feature, deletion of generic cloud feature support, broad `0.128.0` release-intake scope.
- Explicit non-goals carried forward: no CO-450 binary provenance, no CO-451 auth provenance, no broad feature posture cleanup beyond `js_repl` unless directly adjacent examples must avoid removed names.

## Parity / Alignment Matrix
- Current truth: `codex-cli 0.128.0` reports `js_repl` and `js_repl_tools_only` as `removed false`.
- Reference truth: current-facing CO guidance should describe removed upstream feature surfaces as unavailable, not as disabled-by-choice or default-on.
- Target truth / intended delta: docs and tests no longer recommend or expect `js_repl` toggles; the old evidence packet is explicitly history-only.
- Explicitly out-of-scope differences: broader release promotion and adjacent provenance issues remain in their existing Linear owners.

## Readiness Gate
- Not done if:
  - current docs still say `js_repl` is enabled by default
  - active examples still use `CODEX_CLOUD_ENABLE_FEATURES=js_repl` or `CODEX_CLOUD_DISABLE_FEATURES=js_repl`
  - tests still encode `js_repl` as a normal cloud feature toggle
  - historical evidence docs are still readable as current policy
- Pre-implementation issue-quality review evidence:
  - 2026-05-01: parent worker reviewed the issue body, live `codex features list`, and repo references. The issue is a concrete posture implementation lane, not merely docs wording. It requires current docs plus code/test audit and the historical packet must be marked history-only, so the micro-task shortcut is ineligible.
- Safeguard ownership split:
  - Parent owns current-facing docs, CO-452 packet, code/test audit, validation, review, PR, and Linear handoff.
  - Child lane `archive-js-repl-packet` owns only `docs/PRD-js-repl-cloud-usage-evidence-gate.md`, `docs/TECH_SPEC-js-repl-cloud-usage-evidence-gate.md`, and `docs/ACTION_PLAN-js-repl-cloud-usage-evidence-gate.md`.

## Technical Requirements
- Functional requirements:
  - Replace active `js_repl` default-on guidance with removed-in-`0.128.0` guidance.
  - Keep `CODEX_CLOUD_ENABLE_FEATURES` and `CODEX_CLOUD_DISABLE_FEATURES` documented as generic feature controls for non-removed active features only.
  - Remove active package/script affordances whose purpose is `js_repl` usage evidence gating.
  - Update cloud executor tests so feature pass-through coverage uses active non-removed feature names.
  - Preserve the old evidence-gate packet as historical/archive material.
- Non-functional requirements:
  - Keep the change narrow and reviewable.
  - Preserve generic cloud execution behavior.
  - Keep docs freshness and spec guard metadata current for the new packet.
- Interfaces / contracts:
  - `CODEX_CLOUD_ENABLE_FEATURES` and `CODEX_CLOUD_DISABLE_FEATURES` continue to accept comma- or whitespace-delimited feature names.
  - Current docs must warn that removed features such as `js_repl` should not be supplied.

## Fallback Expiry / Refactor Decision
- Applies to fallback, compatibility, legacy, stale, cached, break-glass, or minor-seam behavior? `Yes`.
- Required decision table:

| Surface | Fallback / seam | Decision | Owner | Trigger | Introduced date | Review date | Maximum lifetime | Removal condition | Validation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `js_repl` default-on / break-glass docs | guidance for enabling/disabling a feature that no longer exists | remove fallback | CO-452 | Codex CLI `0.128.0` reports `js_repl` removed | historical | 2026-05-01 | immediate | current docs and tests no longer recommend or expect `js_repl` toggles | `rg`, docs checks, focused cloud feature tests |
| scripts/js-repl-usage-matrix.mjs | canary matrix for a removed feature | remove fallback | CO-452 | feature removed upstream | 2026-03-02 | 2026-05-01 | immediate | package script and active source checkout no longer expose the canary as current guidance | package script audit and tests |

- Large-refactor check: no new compatibility seam is introduced; the current lane removes stale feature posture.

## Architecture & Data
- Architecture / design adjustments: none beyond deleting stale active canary surfaces and updating tests/docs.
- Data model changes / migrations: none.
- External dependencies / integrations: active Codex CLI feature surface and CO docs/review gates.

## Validation Plan
- Tests / checks:
  - focused Vitest for `CodexCloudTaskExecutor` and `OrchestratorCloudTargetExecutor`
  - `node scripts/delegation-guard.mjs`
  - `node scripts/spec-guard.mjs --dry-run`
  - `npm run build`
  - `npm run lint`
  - `npm run test`
  - `npm run docs:check`
  - `npm run docs:freshness`
  - `npm run repo:stewardship`
  - `node scripts/diff-budget.mjs`
  - manifest-backed standalone review and explicit elegance review
- Rollout verification: PR checks, actionable feedback sweep, and `pr ready-review` drain before `In Review`.
- Monitoring / alerts: none.

## Open Questions
- None blocking implementation.

## Approvals
- Reviewer: parent provider worker self-review before implementation.
- Date: 2026-05-01
