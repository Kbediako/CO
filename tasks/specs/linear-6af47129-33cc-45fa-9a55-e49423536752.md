---
id: 20260413-linear-6af47129-33cc-45fa-9a55-e49423536752
title: CO validation: classify current-main docs:freshness stale-doc baseline after CO-165 rebase
status: in_progress
owner: Codex
created: 2026-04-13
last_review: 2026-04-13
review_cadence_days: 30
risk_level: medium
related_prd: docs/PRD-linear-6af47129-33cc-45fa-9a55-e49423536752.md
related_action_plan: docs/ACTION_PLAN-linear-6af47129-33cc-45fa-9a55-e49423536752.md
related_tasks:
  - tasks/tasks-linear-6af47129-33cc-45fa-9a55-e49423536752.md
review_notes:
  - 2026-04-13: Opened from Linear issue `CO-166` in the provider-worker workspace after rechecking live CO workflow states with the packaged `linear issue-context` helper, moving the issue from `Ready` to `In Progress`, recording the required same-turn `stay_serial` / `single_bounded_change` parallelization decision, and confirming there is no attached PR and no existing `## Codex Workpad` comment yet.
  - 2026-04-13: The workspace was fast-forwarded cleanly to `origin/main`, then switched onto branch `linear-6af47129-33cc-45fa-9a55-e49423536752` at commit `bc78b1866` before any repo edits.
  - 2026-04-13: The single reusable workpad source for this attempt is staged at `out/linear-6af47129-33cc-45fa-9a55-e49423536752/manual/workpad.md` before the first Linear upsert.
  - 2026-04-13: The issue snapshot reports a post-`CO-165` `docs:freshness` regression on current `origin/main` with `198` stale docs split across `Task Packet=142`, `Task Mirror=29`, `Report Only=26`, and `Active Guide=1`, and the live repro on the clean current-main workspace confirmed those exact totals.
  - 2026-04-13: Pre-implementation issue-quality review approves the bounded scope: bootstrap the `CO-166` packet, publish the workpad, run audited `docs-review`, capture a fresh `docs:freshness` repro, classify the exact stale sources, and only then decide whether a small metadata repair or a blocker/follow-up path is truthful.
  - 2026-04-13: `linear child-stream --pipeline docs-review` failed closed with `provider_worker_child_stream_provenance_invalid` because the parent provider-worker manifest lacks control-host provenance. The truthful direct fallback run failed first on a packet-local `docs/TASKS.md` line-budget overflow (`451/450`), `npm run docs:archive-tasks` repaired that local defect, and the rerun then passed `spec-guard` plus `docs:check` and failed only on `docs:freshness`.
  - 2026-04-13: The live stale set is two cohorts, not one: `182` stale docs on the March 13 / 30-day window spanning packet ids `1138` through `1163`, plus `16` stale docs on the January 12 / 90-day release-doc window covering `.agent/SOPs/release.md` and packets `0942` through `0944`.
  - 2026-04-13: Because the actual repair would touch `198` docs across two cohorts, this lane stops at classification and truthful blocker routing. Follow-up `CO-167` was created to own the stale-cohort refresh without reopening `CO-165`.
---

# Technical Specification

## Context
`CO-166` exists because the `CO-165` rebase reportedly turned a previously green `docs:freshness` baseline red on current `origin/main`. The task is not to reopen `CO-165` runtime or test fixes. The task is to classify the live baseline truth on today’s mainline and keep the validation story honest.

## Requirements
1. Preserve the issue wording and traceability while capturing a fresh local repro against the latest current mainline.
2. Classify the stale-doc set by exact path, doc class, and cadence/age source.
3. Keep any repair bounded to the stale source proven by the repro.
4. Record audited docs-review evidence and saved repro artifacts under the current task id.
5. Leave closeout evidence other workers can cite directly without conflating repo-baseline drift with `CO-165` implementation scope.

## Current Truth
- `npm run docs:freshness` now fails on current `origin/main` after the `CO-165` rebase, and the live repro on the clean current-main workspace confirmed the issue snapshot exactly.
- Confirmed totals are:
  - `198` stale docs
  - `Task Packet=142`
  - `Task Mirror=29`
  - `Report Only=26`
  - `Active Guide=1`
- There are no missing registry entries, missing files, invalid entries, or uncatalogued docs.
- The same workspace reportedly had `docs:freshness OK - 3719 docs, 3722 registry entries` before the rebase.
- Exact stale cohorts:
  - March 13 / 30-day cohort: `182` docs across packet ids `1138` through `1163`
  - January 12 / 90-day cohort: `16` docs across `.agent/SOPs/release.md` plus packets `0942`, `0943`, and `0944`

## Issue-Shaping Contract
- User-request translation carried forward:
  - classify whether the failure is current-main baseline drift
  - identify the concrete stale-doc sources
  - restore a truthful validation story without reopening `CO-165`
- Protected terms / exact artifact and surface names:
  - `docs:freshness`
  - `current-main stale-doc baseline`
  - `Task Packet`
  - `Task Mirror`
  - `Report Only`
  - `docs/docs-freshness-registry.json`
- Nearby wrong interpretations to reject:
  - this lane should repair `CO-165` runtime/test code
  - the right answer is to suppress `docs:freshness`
  - unrelated task packets should be refreshed before the live stale source is known
- Explicit non-goals carried forward:
  - reopening `CO-165` runtime/test fixes
  - policy weakening
  - speculative broad docs refresh

## Parity / Alignment Matrix
- Current truth: current-main baseline failure is confirmed on a clean workspace, and the stale set is now classified into the March 13 packet cohort and the January release-doc cohort.
- Reference truth: pre-rebase evidence from the same workspace reported a green baseline.
- Target truth / intended delta: after this lane, the repo should either have a green `docs:freshness` baseline again or an exact blocker description with approved handling and saved evidence.
- Explicitly out-of-scope differences: `CO-165` code/test changes and unrelated docs cleanup not proven by the repro.

## Readiness Gate
- Not done if:
  - the stale set is still unclassified
  - the lane cannot say whether the failure is baseline or local
  - the closeout is only a vague waiver
- Pre-implementation issue-quality review evidence:
  - the issue snapshot already constrains the lane to one bounded baseline-classification problem, so docs-first registration plus a fresh repro is the correct starting move
- Safeguard ownership split:
  - this lane owns docs packet/workpad, current-main repro, stale-source classification, and any minimal metadata repair
  - any broader workflow or policy change requires a separate follow-up issue

## Technical Requirements
- Functional requirements:
  - create the `CO-166` docs-first packet and workpad source
  - run audited `linear child-stream --pipeline docs-review`
  - save a fresh `docs:freshness` log/report pair
  - classify the stale entries and decide on bounded repair versus blocker path
- Non-functional requirements:
  - keep the diff reviewable
  - preserve truthful issue-local versus repo-baseline attribution
  - prefer mechanical metadata changes over narrative churn if a fix is needed
- Interfaces / contracts:
  - `scripts/docs-freshness.mjs`
  - `docs/docs-freshness-registry.json`
  - `tasks/index.json`
  - `docs/TASKS.md`

## Architecture & Data
- Architecture / design adjustments:
  - none are assumed up front; the lane begins as a diagnosis and classification step
  - if a fix is required, it should be a bounded metadata refresh only
- Data model changes / migrations:
  - add the `linear-6af47129-33cc-45fa-9a55-e49423536752` packet to the task/docs registries
  - refresh existing metadata only for the stale set proven by the repro
- External dependencies / integrations:
  - audited `linear child-stream --pipeline docs-review`
  - saved repro artifacts in `out/linear-6af47129-33cc-45fa-9a55-e49423536752/manual/`

## Validation Plan
- Tests / checks:
  - `MCP_RUNNER_TASK_ID=linear-6af47129-33cc-45fa-9a55-e49423536752 "/opt/homebrew/Cellar/node/25.2.1/bin/node" "/Users/kbediako/Code/CO/bin/codex-orchestrator.js" linear child-stream --pipeline docs-review --stream co-166-docs-review --format json`
  - `npm run docs:freshness`
  - targeted reruns after any bounded fix
- Rollout verification:
  - preserve the first baseline repro artifacts
  - record the exact stale cohort or blocker source in this packet and the workpad
  - refresh the workpad after docs-first, after classification, and before any stop point
- Monitoring / alerts:
  - ongoing signal remains the saved `docs:freshness` report and any post-fix rerun; no new automation is added here

## Approvals
- Reviewer: live repro captured, docs-review fallback recorded, follow-up `CO-167` created for the actual repair lane
- Date: 2026-04-13
